---
title: "EBCloud | Models"
description: "Use EBCloud models with Mastra. 4 models available."
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/ebcloud.svg" alt="EBCloud logo" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />EBCloud

Access 4 EBCloud models through Mastra's model router. Authentication is handled automatically using the `EBCLOUD_API_KEY` environment variable.

Learn more in the [EBCloud documentation](https://docs.ebtech.com/ai/model-api.html).

```bash title=".env"
EBCLOUD_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "ebcloud/DeepSeek-V4-Flash"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra uses the OpenAI-compatible `/chat/completions` endpoint. Some provider-specific features may not be available. Check the [EBCloud documentation](https://docs.ebtech.com/ai/model-api.html) for details.

:::

## Models

<ProviderModelsTable
  models={[
  {
    "model": "ebcloud/DeepSeek-V4-Flash",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1000000,
    "maxOutput": 384000,
    "inputCost": 0.143,
    "outputCost": 0.2857
  },
  {
    "model": "ebcloud/DeepSeek-V4-Pro",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1000000,
    "maxOutput": 384000,
    "inputCost": 0.4286,
    "outputCost": 0.8571
  },
  {
    "model": "ebcloud/GLM-5.1",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 200000,
    "maxOutput": 131072,
    "inputCost": 0.8571,
    "outputCost": 3.4286
  },
  {
    "model": "ebcloud/Kimi-K2.6",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 262144,
    "maxOutput": 262144,
    "inputCost": 0.9286,
    "outputCost": 3.8571
  }
]}
/>

## Advanced configuration

### Custom headers

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://maas-api.ebcloud.com/v1",
    id: "ebcloud/DeepSeek-V4-Flash",
    apiKey: process.env.EBCLOUD_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### Dynamic model selection

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "ebcloud/Kimi-K2.6"
      : "ebcloud/DeepSeek-V4-Flash";
  }
});
```


