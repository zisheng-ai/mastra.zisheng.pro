---
title: "Thinking Machines | Models"
description: "Use Thinking Machines models with Mastra. 2 models available."
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/thinkingmachines.svg" alt="Thinking Machines logo" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Thinking Machines

Access 2 Thinking Machines models through Mastra's model router. Authentication is handled automatically using the `TINKER_API_KEY` environment variable.

Learn more in the [Thinking Machines documentation](https://tinker-docs.thinkingmachines.ai/tinker/compatible-apis/anthropic/).

```bash title=".env"
TINKER_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "thinkingmachines/thinkingmachines/Inkling"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra uses the OpenAI-compatible `/chat/completions` endpoint. Some provider-specific features may not be available. Check the [Thinking Machines documentation](https://tinker-docs.thinkingmachines.ai/tinker/compatible-apis/anthropic/) for details.

:::

## Models

<ProviderModelsTable
  models={[
  {
    "model": "thinkingmachines/thinkingmachines/Inkling",
    "imageInput": true,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 65536,
    "maxOutput": 65536,
    "inputCost": 1.87,
    "outputCost": 4.68
  },
  {
    "model": "thinkingmachines/thinkingmachines/Inkling:peft:262144",
    "imageInput": true,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 262144,
    "maxOutput": 262144,
    "inputCost": 3.74,
    "outputCost": 9.36
  }
]}
/>

## Advanced configuration

### Custom headers

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://tinker.thinkingmachines.dev/services/tinker-prod/anthropic/api/v1",
    id: "thinkingmachines/thinkingmachines/Inkling",
    apiKey: process.env.TINKER_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### Dynamic model selection

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "thinkingmachines/thinkingmachines/Inkling:peft:262144"
      : "thinkingmachines/thinkingmachines/Inkling";
  }
});
```



## Direct provider installation

This provider can also be installed directly as a standalone package, which can be used instead of the Mastra model router string. View the [package documentation](https://www.npmjs.com/package/@ai-sdk/anthropic) for more details.

```bash npm2yarn
npm install @ai-sdk/anthropic
```

