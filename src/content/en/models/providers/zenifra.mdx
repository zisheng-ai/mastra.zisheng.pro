---
title: "Zenifra | Models"
description: "Use Zenifra models with Mastra. 1 model available."
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/zenifra.svg" alt="Zenifra logo" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Zenifra

Access 1 Zenifra model through Mastra's model router. Authentication is handled automatically using the `ZENIFRA_AI_KEY` environment variable.

Learn more in the [Zenifra documentation](https://docs.zenifra.com).

```bash title=".env"
ZENIFRA_AI_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "zenifra/alibaba/qwen3.6-35b-a3b"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra uses the OpenAI-compatible `/chat/completions` endpoint. Some provider-specific features may not be available. Check the [Zenifra documentation](https://docs.zenifra.com) for details.

:::

## Models

<ProviderModelsTable
  models={[
  {
    "model": "zenifra/alibaba/qwen3.6-35b-a3b",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 262144,
    "maxOutput": 65536,
    "inputCost": 0.19,
    "outputCost": 0.48
  }
]}
/>

## Advanced configuration

### Custom headers

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://ai.zenifra.com/v1",
    id: "zenifra/alibaba/qwen3.6-35b-a3b",
    apiKey: process.env.ZENIFRA_AI_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### Dynamic model selection

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "zenifra/alibaba/qwen3.6-35b-a3b"
      : "zenifra/alibaba/qwen3.6-35b-a3b";
  }
});
```


