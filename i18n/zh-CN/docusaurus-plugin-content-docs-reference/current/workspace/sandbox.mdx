---
title: "参考：WorkspaceSandbox | Workspace"
description: "WorkspaceSandbox 接口的文档，该接口定义命令执行操作。"
packages:
  - "@mastra/core"
---

# WorkspaceSandbox

**添加于：** `@mastra/core@1.1.0`

`WorkspaceSandbox` 接口定义 Workspace 如何执行命令和管理后台进程。

## 属性 {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'processes',
    type: 'SandboxProcessManager',
    description: '后台进程管理器。如果未实现，则进程管理 Tool 将不可用。请参阅 SandboxProcessManager 参考文档。',
    isOptional: true,
  },
]}
/>

## 方法 {/* #methods */}

### `start()` {/* #start */}

启动 Sandbox。此方法由 `workspace.init()` 或首次调用 `executeCommand()` 时自动调用。

```typescript
await sandbox.start()
```

此方法为执行命令准备 Sandbox。

### `stop()` {/* #stop */}

停止 Sandbox。

```typescript
await sandbox.stop?.()
```

### `destroy()` {/* #destroy */}

清理 Sandbox 资源。由 `workspace.destroy()` 调用。

```typescript
await sandbox.destroy()
```

### `executeCommand(command, args?, options?)` {/* #executecommandcommand-args-options */}

执行 shell 命令。

```typescript
const result = await sandbox.executeCommand('ls', ['-la', '/docs'])
const result = await sandbox.executeCommand('npm', ['install', 'lodash'])
```

**参数：**

<PropertiesTable
  content={[
  {
    name: 'command',
    type: 'string',
    description: '要执行的命令',
  },
  {
    name: 'args',
    type: 'string[]',
    description: '命令参数',
    isOptional: true,
  },
  {
    name: 'options',
    type: 'Options',
    description: '配置选项。',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'timeout',
            type: 'number',
            description: '执行超时时间（毫秒）',
            isOptional: true,
          },
          {
            name: 'cwd',
            type: 'string',
            description: '命令的工作目录',
            isOptional: true,
          },
          {
            name: 'env',
            type: 'Record<string, string>',
            description: '额外的环境变量',
            isOptional: true,
          },
          {
            name: 'onStdout',
            type: '(data: string) => void',
            description: '用于 stdout 流式输出的回调',
            isOptional: true,
          },
          {
            name: 'onStderr',
            type: '(data: string) => void',
            description: '用于 stderr 流式输出的回调',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

### `getInfo()` {/* #getinfo */}

获取 Sandbox 状态和资源信息。

```typescript
const info = await sandbox.getInfo()
// { status: 'running', resources: { memoryMB: 512, cpuPercent: 5 } }
```

### `getInstructions(opts?)` {/* #getinstructionsopts */}

返回此 Sandbox 工作方式的说明。将 Workspace 分配给 Agent 时，该说明会注入 Agent 的系统消息中。

```typescript
const instructions = sandbox.getInstructions?.()
// 'Local command execution. Working directory: "/workspace".'
```

**参数：**

<PropertiesTable
  content={[
  {
    name: 'opts.requestContext',
    type: 'RequestContext',
    description: '如果构造函数中提供了 `instructions` 函数，则转发给该函数。',
    isOptional: true,
  },
]}
/>

**返回值：** `string`

## 相关内容 {/* #related */}

- [SandboxProcessManager 参考文档](/reference/workspace/process-manager)
- [Sandbox](/docs/workspace/sandbox)
- [Workspace 类](/reference/workspace/workspace-class)
- [Filesystem 接口](/reference/workspace/filesystem)
