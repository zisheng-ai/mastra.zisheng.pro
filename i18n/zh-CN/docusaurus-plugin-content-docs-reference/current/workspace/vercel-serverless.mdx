---
title: "参考：VercelServerlessSandbox | Workspace"
description: "VercelServerlessSandbox 的 API 参考文档，该无服务器 Sandbox Provider 将命令部署为 Vercel Functions 并执行。"
packages:
  - "@mastra/vercel"
---

# VercelServerlessSandbox

以 [Vercel](https://vercel.com) 无服务器函数的形式执行命令。提供全球分布式、零基础设施且可自动扩缩容的执行环境。有关接口的详细信息，请参阅 [WorkspaceSandbox 接口](/reference/workspace/sandbox)。

:::warning

VercelServerlessSandbox 是无状态的。它不提供持久化文件系统、交互式 shell 或长时间运行的后台进程。只有 `/tmp` 可写，并且其中的内容在两次调用之间不会保留。

:::

:::note

如果需要具备持久化文件系统、`sudo` 访问权限、端口暴露和后台进程的完整 Linux MicroVM，请使用 [`VercelSandbox`](/reference/workspace/vercel-sandbox)，它由 [Vercel Sandbox](https://vercel.com/docs/vercel-sandbox) 产品提供支持。

:::

## 安装 {/* #installation */}

```bash npm2yarn
npm install @mastra/vercel
```

## 使用方法 {/* #usage */}

将 `VercelServerlessSandbox` 添加到 Workspace 并分配给 Agent：

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { VercelServerlessSandbox } from '@mastra/vercel'

const workspace = new Workspace({
  sandbox: new VercelServerlessSandbox({
    token: process.env.VERCEL_TOKEN,
  }),
})

const agent = new Agent({
  id: 'dev-agent',
  name: 'dev-agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})
```

### 使用自定义资源进行团队范围的部署 {/* #team-scoped-deployment-with-custom-resources */}

```typescript
const workspace = new Workspace({
  sandbox: new VercelServerlessSandbox({
    token: process.env.VERCEL_TOKEN,
    teamId: 'team_abc123',
    regions: ['iad1', 'sfo1'],
    maxDuration: 120,
    memory: 3008,
    env: {
      NODE_ENV: 'production',
    },
  }),
})
```

## 构造函数参数 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'token',
    type: 'string',
    description: 'Vercel API 令牌。未提供时使用 `VERCEL_TOKEN` 环境变量。',
    isOptional: true,
  },
  {
    name: 'teamId',
    type: 'string',
    description: '用于团队范围部署的 Vercel 团队 ID。',
    isOptional: true,
  },
  {
    name: 'projectName',
    type: 'string',
    description: '现有 Vercel 项目名称。省略时自动生成。',
    isOptional: true,
  },
  {
    name: 'regions',
    type: 'string[]',
    description: '部署区域。',
    isOptional: true,
    defaultValue: "['iad1']",
  },
  {
    name: 'maxDuration',
    type: 'number',
    description: '函数的最长运行时间（秒）。',
    isOptional: true,
    defaultValue: '60',
  },
  {
    name: 'memory',
    type: 'number',
    description: '函数内存（MB）。',
    isOptional: true,
    defaultValue: '1024',
  },
  {
    name: 'env',
    type: 'Record<string, string>',
    description: '嵌入已部署函数源代码的环境变量。这些变量在部署时嵌入，而不是动态设置。',
    isOptional: true,
  },
  {
    name: 'commandTimeout',
    type: 'number',
    description: '每次调用的命令超时时间（毫秒）。',
    isOptional: true,
    defaultValue: '55000',
  },
  {
    name: 'instructions',
    type: 'string | ((opts) => string)',
    description:
      '自定义说明，用于覆盖 `getInstructions()` 返回的默认说明。传入字符串可完全替换默认说明，传入函数可扩展默认说明。',
    isOptional: true,
  },
  {
    name: 'onStart',
    type: 'SandboxLifecycleHook',
    description: 'Sandbox 达到 `running` 状态后调用的生命周期钩子。',
    isOptional: true,
  },
  {
    name: 'onStop',
    type: 'SandboxLifecycleHook',
    description: 'Sandbox 停止前调用的生命周期钩子。',
    isOptional: true,
  },
  {
    name: 'onDestroy',
    type: 'SandboxLifecycleHook',
    description: 'Sandbox 销毁前调用的生命周期钩子。',
    isOptional: true,
  },
]}
/>

## 属性 {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '此 Sandbox 实例的唯一标识符。',
  },
  {
    name: 'name',
    type: "'VercelServerlessSandbox'",
    description: '便于阅读的名称。',
  },
  {
    name: 'provider',
    type: "'vercel-serverless'",
    description: 'Provider 类型标识符。',
  },
  {
    name: 'status',
    type: 'ProviderStatus',
    description:
      "当前生命周期状态：`'pending'`、`'starting'`、`'running'`、`'stopping'`、`'stopped'`、`'destroying'`、`'destroyed'` 或 `'error'`。",
  },
]}
/>

## 限制 {/* #limitations */}

VercelServerlessSandbox 底层使用无服务器函数，这意味着：

- **无持久化文件系统**：写入 `/tmp` 的文件是临时文件，会在两次调用之间被清除。
- **无交互式 shell**：命令通过 `/bin/sh -c` 运行，不支持 stdin 流式传输。
- **无后台进程**：不提供进程管理器、PID 或长时间运行的任务。
- **不支持挂载**：不支持挂载云存储（S3、GCS）。
- **超时限制**：最长执行时间受 `maxDuration` 限制（默认 60 秒）。

## 相关内容 {/* #related */}

- [WorkspaceSandbox 接口](/reference/workspace/sandbox)
- [Workspace 类](/reference/workspace/workspace-class)
- [E2BSandbox](/reference/workspace/e2b-sandbox)：提供持久化文件系统和进程管理的全功能云 Sandbox
