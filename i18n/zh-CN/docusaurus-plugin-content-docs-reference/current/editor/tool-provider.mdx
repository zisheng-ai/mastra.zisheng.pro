---
title: "参考：ToolProvider | Editor"
description: "ToolProvider 接口以及内置 Composio 和 Arcade 实现的文档。"
packages:
  - "@mastra/editor"
---

# ToolProvider

`ToolProvider` 接口定义 Editor 如何发现和解析外部平台提供的集成 Tool。Mastra 包含两个内置实现：`ComposioToolProvider` 和 `ArcadeToolProvider`。

有关 Provider 设置和 Studio 工作流，请参阅 [Editor Tool](/docs/editor/overview#tools)。有关已存储选择和解析行为，请参阅 [Tool 配置](/reference/editor/tools)。

## ToolProvider 接口 {/* #toolprovider-interface */}

Provider 公开元数据以及旧版发现和解析方法。Agent Builder 集成还可以实现可选的 VNext catalog、连接、授权和健康状态方法。

<PropertiesTable
  content={[
  {
    name: 'info',
    type: 'ToolProviderInfo',
    description: 'Provider ID、名称和描述。',
  },
  {
    name: 'displayName',
    type: 'string',
    description: 'Tool 选择器中显示的可选名称。默认为 info.name。',
    isOptional: true,
  },
  {
    name: 'capabilities',
    type: 'ToolProviderCapabilities',
    description: '静态连接和撤销能力。VNext Provider 必须提供。',
    isOptional: true,
  },
  {
    name: 'defaultScope',
    type: "'per-author' | 'caller-supplied'",
    description: "默认连接身份作用域。省略时默认为 'per-author'。",
    isOptional: true,
  },
  {
    name: 'listToolkits()',
    type: '() => Promise<ToolProviderListResult<ToolProviderToolkit>>',
    description: '通过旧版接口列出可用 toolkit。',
    isOptional: true,
  },
  {
    name: 'listTools(params?)',
    type: '(params?: ListToolProviderToolsOptions) => Promise<ToolProviderListResult<ToolProviderToolInfo>>',
    description: '列出 Tool，可选择按 toolkit、搜索条件和分页条件筛选。',
  },
  {
    name: 'getToolSchema(slug)',
    type: '(slug: string) => Promise<Record<string, unknown> | null>',
    description: '通过旧版接口返回 Tool 输入 schema。',
    isOptional: true,
  },
  {
    name: 'resolveTools(slugs, configs?, options?)',
    type: '(slugs: string[], configs?: Record<string, StorageToolConfig>, options?: ResolveToolProviderToolsOptions) => Promise<Record<string, ToolAction>>',
    description: '将旧版 Tool 选择解析为可执行的 Mastra Tool。',
  },
  {
    name: 'listToolkitsVNext()',
    type: '() => Promise<ListToolkitsResult>',
    description: '列出 Agent Builder 和 Editor 允许使用的 toolkit。',
    isOptional: true,
  },
  {
    name: 'listToolsVNext(options?)',
    type: '(options?: ListToolsOpts) => Promise<ListToolsResult>',
    description: '使用 toolkit、搜索和分页选项列出允许的 Tool。',
    isOptional: true,
  },
  {
    name: 'resolveToolsVNext(options)',
    type: '(options: ResolveToolsOpts) => Promise<Record<string, ToolAction>>',
    description: '为一组 slug 和一个已授权连接解析 Tool。',
    isOptional: true,
  },
  {
    name: 'authorize(options)',
    type: '(options: AuthorizeOpts) => Promise<{ url: string; authId: string }>',
    description: '启动授权流程。',
    isOptional: true,
  },
  {
    name: 'listConnectionFields(options)',
    type: '(options: { toolkit: string }) => Promise<ConnectionField[]>',
    description: '列出授权 toolkit 所需的 Provider 专用值。',
    isOptional: true,
  },
  {
    name: 'getAuthStatus(authId)',
    type: '(authId: string) => Promise<AuthFlowStatus>',
    description: '返回授权流程的状态。',
    isOptional: true,
  },
  {
    name: 'getConnectionStatus(options)',
    type: '(options: { items: Array<{ connectionId: string; toolkit: string }> }) => Promise<Record<string, { connected: boolean }>>',
    description: '检查一批连接是否仍处于活动状态。',
    isOptional: true,
  },
  {
    name: 'listConnections(options)',
    type: '(options: ListConnectionsOpts) => Promise<ListConnectionsResult>',
    description: '列出用户和 toolkit 的现有 Provider 连接。',
    isOptional: true,
  },
  {
    name: 'getHealth()',
    type: '() => Promise<ToolProviderHealth>',
    description: '返回 Provider 配置和可达性健康状态。',
    isOptional: true,
  },
  {
    name: 'revokeConnection(connectionId)',
    type: '(connectionId: string) => Promise<void>',
    description: '撤销 Provider 连接。',
    isOptional: true,
  },
]}
/>

---

## ComposioToolProvider {/* #composiotoolprovider */}

连接到 [Composio](https://composio.dev)，以访问数百个集成 Tool。

### 使用示例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'
import { ComposioToolProvider } from '@mastra/editor/composio'

const editor = new MastraEditor({
  toolProviders: {
    composio: new ComposioToolProvider({
      apiKey: process.env.COMPOSIO_API_KEY!,
    }),
  },
})
```

### 构造函数参数 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'apiKey',
    type: 'string',
    description: '你的 Composio API key。',
  },
  {
    name: 'allowedToolkits',
    type: 'readonly string[]',
    description: 'Toolkit slug allowlist。支持精确匹配和后缀通配符。',
    isOptional: true,
  },
  {
    name: 'allowedTools',
    type: 'Readonly<Record<string, readonly string[]>>',
    description: '每个 toolkit 的 Tool slug allowlist。支持精确匹配和前缀通配符。',
    isOptional: true,
  },
  {
    name: 'defaultScope',
    type: "'per-author' | 'caller-supplied'",
    description: '连接身份作用域。默认为 per-author。',
    isOptional: true,
    defaultValue: "'per-author'",
  },
]}
/>

### Tool slug {/* #tool-slugs */}

Composio Tool 使用大写 slug 格式：`GITHUB_CREATE_ISSUE`、`SLACK_SEND_MESSAGE`。

### 身份验证 {/* #authentication */}

连接默认使用 per-author 作用域。设置 `defaultScope: 'caller-supplied'`，可按 request context 中通过 `MASTRA_RESOURCE_ID_KEY` 解析出的调用者身份对授权进行分组。确保每个经过身份验证的请求都提供稳定且唯一的 resource ID。使用 `MastraAuthWorkos` 时，请配置 `mapUserToResourceId`，根据已验证用户设置此值。

### 连接管理 Tool {/* #connection-management-tools */}

Composio 提供可从 Agent 聊天中启动和监控授权的 Tool。设置 `allowedToolkits` 时，请包含 `composio` 以启用这些 Tool：

```typescript title="src/mastra/index.ts"
const editor = new MastraEditor({
  toolProviders: {
    composio: new ComposioToolProvider({
      apiKey: process.env.COMPOSIO_API_KEY!,
      allowedToolkits: ['composio', 'gmail'],
      defaultScope: 'caller-supplied',
    }),
  },
})
```

仅添加 Agent 所需的连接管理 Tool：

| Tool | 行为 |
| - | - |
| `COMPOSIO_MANAGE_CONNECTIONS` | 通过调用者拥有的 session 在聊天中创建授权链接。 |
| `COMPOSIO_WAIT_FOR_CONNECTIONS` | 在 Agent 继续之前等待调用者完成授权。 |

`COMPOSIO_WAIT_FOR_CONNECTIONS` 是可选的。如果不使用它，请完成授权并返回聊天，然后要求 Agent 继续。已连接账户会继续与调用者 resource ID 关联，以供后续请求使用。

---

## ArcadeToolProvider {/* #arcadetoolprovider */}

连接到 [Arcade](https://arcade.dev)，以使用精选的 Tool catalog 和内置身份验证。

### 使用示例 {/* #usage-example-1 */}

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'
import { ArcadeToolProvider } from '@mastra/editor/arcade'

const editor = new MastraEditor({
  toolProviders: {
    arcade: new ArcadeToolProvider({
      apiKey: process.env.ARCADE_API_KEY!,
    }),
  },
})
```

### 构造函数参数 {/* #constructor-parameters-1 */}

<PropertiesTable
  content={[
  {
    name: 'apiKey',
    type: 'string',
    description: '你的 Arcade API key。',
  },
  {
    name: 'baseURL',
    type: 'string',
    description: 'Arcade API 的自定义 base URL。',
    isOptional: true,
  },
]}
/>

### Tool slug {/* #tool-slugs-1 */}

Arcade Tool 使用 `Toolkit.ToolName` 格式：`Github.GetRepository`、`Slack.SendMessage`。

### 身份验证 {/* #authentication-1 */}

旧版 Arcade resolver 会优先使用 request context 中的 `resourceId`。如果不可用，则依次回退到提供的 `userId` 和共享的 `default` 身份。仅对有意共享的集成使用 `default`。在租户隔离的部署中，请提供可信且稳定的 `resourceId` 或显式 `userId`。两者均省略时，调用者之间不会隔离。
