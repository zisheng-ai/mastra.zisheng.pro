---
title: "参考：ProcessorProvider | Editor"
description: "ProcessorProvider 的参考文档，该接口用于向 MastraEditor 注册可配置的 Processor。"
packages:
  - "@mastra/core"
---

# ProcessorProvider

`ProcessorProvider` 是包用来向 [`MastraEditor`](/reference/editor/mastra-editor) 注册可配置 Processor 的接口。Processor Provider 有两个用途：

1. **发现：** UI 使用 `info`、`configSchema` 和 `availablePhases` 渲染配置表单。
1. **运行时：** Editor 在 Agent 实例化期间调用 `createProcessor(config)`，根据已存储配置创建 Processor 实例。

Provider 通过 `MastraEditorConfig.processorProviders` 提供。

## 使用示例

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'
import { moderationProcessorProvider } from '@mastra/processors/moderation'

new MastraEditor({
  processorProviders: {
    moderation: moderationProcessorProvider,
  },
})
```

## 属性

<PropertiesTable
  content={[
  {
    name: 'info',
    type: 'ProcessorProviderInfo',
    description: 'Provider 的元数据。',
    properties: [
      {
        parameters: [
          {
            name: 'id',
            type: 'string',
            description: '唯一标识符（例如 `"moderation"`、`"token-limiter"`）。',
          },
          {
            name: 'name',
            type: 'string',
            description: '易读名称。',
          },
          {
            name: 'description',
            type: 'string',
            description: 'Provider 的简短描述。',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'configSchema',
    type: 'ZodSchema',
    description:
      '描述此 Provider 所接受配置的 Zod schema。UI 使用它渲染表单。验证后的配置对象会传递给 `createProcessor()`。',
  },
  {
    name: 'availablePhases',
    type: 'ProcessorPhase[]',
    description:
      "此 Provider 的 Processor 所支持的阶段。可为 `'processInput'`、`'processInputStep'`、`'processOutputStream'`、`'processOutputResult'`、`'processOutputStep'` 中的一个或多个。",
  },
  {
    name: 'createProcessor',
    type: '(config: Record<string, unknown>) => Processor',
    description:
      '根据验证后的配置创建 `Processor` 实例。在 Agent 实例化期间调用，将已存储的 Processor 配置解析为活动实例。',
  },
]}
/>

## 实现 Provider

```typescript title="src/providers/my-processor.ts"
import type { ProcessorProvider } from '@mastra/core/processor-provider'
import { z } from 'zod'
import { MyProcessor } from './my-processor-impl'

export const myProcessorProvider: ProcessorProvider = {
  info: {
    id: 'my-processor',
    name: 'My Processor',
    description: 'Filters output for sensitive content.',
  },
  configSchema: z.object({
    threshold: z.number().min(0).max(1),
  }),
  availablePhases: ['processOutputStream', 'processOutputResult'],
  createProcessor(config) {
    return new MyProcessor(config as { threshold: number })
  },
}
```

## 相关内容

- [MastraEditor 类](/reference/editor/mastra-editor)：Provider 注册表。
