---
title: "参考：Memory.deleteMessages() | Memory"
description: "Mastra `Memory.deleteMessages()` 方法的参考文档。该方法按 ID 删除多条消息。"
packages:
  - "@mastra/core"
---

# Memory.deleteMessages()

`.deleteMessages()` 方法根据消息 ID 删除多条消息。

## 用法示例 {/* #usage-example */}

```typescript
await memory?.deleteMessages(['671ae63f-3a91-4082-a907-fe7de78e10ec'])
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messageIds',
    type: 'string[]',
    description: '要删除的消息 ID 数组',
    isOptional: false,
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'void',
    type: 'Promise<void>',
    description: '在所有消息删除后兑现的 Promise',
  },
]}
/>

## 扩展用法示例 {/* #extended-usage-example */}

```typescript title="src/test-memory.ts"
import { mastra } from './mastra'
import { MastraDBMessage } from '@mastra/core'

const agent = mastra.getAgent('agent')
const memory = await agent.getMemory()

const { messages } = await memory!.recall({ threadId: 'thread-123' })

const messageIds = messages.map((message: MastraDBMessage) => message.id)
await memory?.deleteMessages([...messageIds])
```

## 相关内容 {/* #related */}

- [Memory 类参考](/reference/memory/memory-class)
- [recall](/reference/memory/recall)
- [Memory 入门](/docs/memory/overview)
