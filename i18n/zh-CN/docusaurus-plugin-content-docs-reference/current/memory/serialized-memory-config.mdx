---
title: "参考：SerializedMemoryConfig | Memory"
description: "SerializedMemoryConfig 的参考文档。它是存储在 Agent 记录中、可序列化为 JSON 的 Memory 配置子集。"
packages:
  - "@mastra/core"
---

# SerializedMemoryConfig

`SerializedMemoryConfig` 是 [`Memory`](/reference/memory/memory-class) 配置中可进行 JSON 序列化的子集，存储在 Agent 记录中。运行时会根据已配置的 `Mastra` 实例解析 vector 和 embedder ID，并将其还原为 `Memory` 实例。

它是 [`BuilderAgentDefaults.memory`](https://agent-builder.mastra.ai/reference/builder-agent-defaults) 和 `EditorAgentNamespace.create({ memory })` 使用的类型。

## 使用示例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'

new MastraEditor({
  builder: {
    enabled: true,
    configuration: {
      agent: {
        memory: {
          observationalMemory: true,
          options: { lastMessages: 50 },
        },
      },
    },
  },
})
```

## 属性 {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'vector',
    type: 'string | false',
    description:
      'Vector store 标识符。必须在 Mastra 实例中注册对应的 vector 实例，才能通过此 ID 解析。设为 `false` 可禁用向量搜索。',
    isOptional: true,
  },
  {
    name: 'embedder',
    type: 'string',
    description: '格式为 `"provider/model"` 的 Embedding 模型 ID（例如 `"openai/text-embedding-3-small"`）。',
    isOptional: true,
  },
  {
    name: 'embedderOptions',
    type: "Omit<MastraEmbeddingOptions, 'telemetry'>",
    description: '传递给 embedder 的选项。Telemetry 选项会被移除。',
    isOptional: true,
  },
  {
    name: 'options',
    type: 'SerializedMemoryOptions',
    description: 'Memory 行为配置。省略 working memory 和 threads 等不可序列化字段。',
    isOptional: true,
    properties: [
      {
        type: 'SerializedMemoryOptions',
        parameters: [
          {
            name: 'readOnly',
            type: 'boolean',
            description: '将 memory 视为只读，不存储新消息。',
            isOptional: true,
          },
          {
            name: 'lastMessages',
            type: 'number | false',
            description:
              '要包含在上下文中的最近消息数量；设为 `false` 可完全禁用消息历史记录（消息既不会加载，也不会保存）。',
            isOptional: true,
          },
          {
            name: 'semanticRecall',
            type: 'boolean | SemanticRecall',
            description: 'Semantic recall 配置。完整结构请参阅 Memory class 参考文档。',
            isOptional: true,
          },
          {
            name: 'generateTitle',
            type: 'boolean | { model: ModelRouterModelId; instructions?: string }',
            description: '标题生成配置。传入包含 `model`（格式为 `"provider/model"`）和可选 `instructions` 的对象。',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'observationalMemory',
    type: 'boolean | SerializedObservationalMemoryConfig',
    description:
      '长期事实提取。传入 `true` 可使用默认设置启用，或传入对象以覆盖 Observer/Reflector 模型、作用域和激活行为。',
    isOptional: true,
  },
]}
/>

## `SerializedObservationalMemoryConfig` {/* #serializedobservationalmemoryconfig */}

<PropertiesTable
  content={[
  {
    name: 'model',
    type: 'string',
    description: 'Observer 和 Reflector 共同使用的模型 ID（例如 `"google/gemini-2.5-flash"`）。',
    isOptional: true,
  },
  {
    name: 'scope',
    type: "'resource' | 'thread'",
    description: '观察结果的作用域是 resource 还是单个 thread。',
    isOptional: true,
  },
  {
    name: 'activateAfterIdle',
    type: 'ObservationalMemoryActivationTTL',
    description: '强制激活已缓冲观察结果前的非活动 TTL。',
    isOptional: true,
  },
  {
    name: 'activateOnProviderChange',
    type: 'boolean',
    description: '当 actor 模型发生变化时，强制激活已缓冲的观察结果。',
    isOptional: true,
  },
  {
    name: 'shareTokenBudget',
    type: 'boolean',
    description: '在消息和观察结果之间共享 token 预算。',
    isOptional: true,
  },
  {
    name: 'temporalMarkers',
    type: 'boolean',
    description: '为消息之间的长时间停顿持久化内联时间间隔标记。',
    isOptional: true,
  },
  {
    name: 'retrieval',
    type: "boolean | { vector?: boolean; scope?: 'thread' | 'resource' }",
    description: '启用检索模式的观察组，作为指向原始消息历史记录的持久指针。',
    isOptional: true,
  },
  {
    name: 'observation',
    type: 'SerializedObservationalMemoryObservationConfig',
    description: 'Observer 步骤配置（模型、token 阈值、缓冲）。',
    isOptional: true,
  },
  {
    name: 'reflection',
    type: 'SerializedObservationalMemoryReflectionConfig',
    description: 'Reflector 步骤配置（模型、观察 token 阈值、缓冲）。',
    isOptional: true,
  },
]}
/>

## 相关 {/* #related */}

- [Memory class](/reference/memory/memory-class)：此配置还原得到的运行时类型。
- [Observational memory](/reference/memory/observational-memory)：完整的 Observational memory 参考文档。
- [BuilderAgentDefaults](https://agent-builder.mastra.ai/reference/builder-agent-defaults)：此类型被固定为 Builder 默认值的位置。
- [Agent Builder：Memory](https://agent-builder.mastra.ai/memory)：概念和完整示例。
