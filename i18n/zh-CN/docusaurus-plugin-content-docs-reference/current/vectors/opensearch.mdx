---
title: "参考：OpenSearch Vector 存储 | Vectors"
description: Mastra 中 OpenSearchVector 类的文档，该类使用 OpenSearch 提供 Vector 搜索功能。
packages:
  - "@mastra/opensearch"
---

# OpenSearch Vector 存储

OpenSearchVector 类使用开源搜索与分析引擎 [OpenSearch](https://opensearch.org/) 提供 Vector 搜索。它利用 OpenSearch 的 k-NN 功能执行 Vector 相似度搜索。

## 构造函数选项

构造函数接受所有 [OpenSearch ClientOptions](https://opensearch.org/docs/latest/clients/javascript/index/)，以及一个必需的 `id` 字段。

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '此 Vector 存储实例的唯一标识符',
  },
  {
    name: 'node',
    type: 'string | string[] | NodeOptions | NodeOptions[]',
    description: "OpenSearch 节点 URL（例如 'http://localhost:9200'）",
  },
  {
    name: 'auth',
    type: 'BasicAuth | BearerAuth | AwsSigv4Auth',
    description: '身份验证配置',
    isOptional: true,
  },
  {
    name: 'ssl',
    type: 'ConnectionOptions',
    description: 'SSL/TLS 配置',
    isOptional: true,
  },
  {
    name: 'compression',
    type: "'gzip'",
    description: '启用 gzip 压缩',
    isOptional: true,
  },
]}
/>

## 方法

### `createIndex()`

使用指定配置创建新索引。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要创建的索引名称',
  },
  {
    name: 'dimension',
    type: 'number',
    description: '要存储在索引中的 Vector 维度',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    description: 'Vector 相似度使用的距离度量',
    defaultValue: "'cosine'",
    isOptional: true,
  },
]}
/>

### `listIndexes()`

列出 OpenSearch 实例中的所有索引。

返回：`Promise<string[]>`

### `describeIndex()`

获取索引信息。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要描述的索引名称',
  },
]}
/>

### `deleteIndex()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要删除的索引名称',
  },
]}
/>

### `upsert()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要 upsert Vector 的索引名称',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: '要插入的嵌入 Vector 数组',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    description: '与每个 Vector 对应的元数据对象数组',
    isOptional: true,
  },
  {
    name: 'ids',
    type: 'string[]',
    description: '可选的 Vector ID 数组。未提供时将生成随机 ID',
    isOptional: true,
  },
]}
/>

### `query()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要查询的索引名称',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: '用于查找相似 Vector 的查询 Vector',
  },
  {
    name: 'topK',
    type: 'number',
    description: '要返回的结果数量',
    defaultValue: '10',
    isOptional: true,
  },
  {
    name: 'filter',
    type: 'VectorFilter',
    description: '应用于查询的可选筛选条件（MongoDB 风格的查询语法）',
    isOptional: true,
  },
]}
/>

### `updateVector()`

按 ID 或元数据筛选条件更新单个 Vector。必须提供 `id` 或 `filter`，但不能同时提供两者。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含待更新 Vector 的索引名称',
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: '要更新的 Vector ID（不能与 filter 同时使用）',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '用于标识待更新 Vector 的元数据筛选条件（不能与 id 同时使用）',
  },
  {
    name: 'update',
    type: '{ vector?: number[]; metadata?: Record<string, any>; }',
    description: '包含待更新 Vector 和/或元数据的对象',
  },
]}
/>

### `deleteVector()`

按 ID 从索引中删除单个 Vector。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要从中删除 Vector 的索引名称',
  },
  {
    name: 'id',
    type: 'string',
    description: '要删除的 Vector ID',
  },
]}
/>

### `deleteVectors()`

按 ID 或元数据筛选条件删除多个 Vector。必须提供 `ids` 或 `filter`，但不能同时提供两者。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含待删除 Vector 的索引名称',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '要删除的 Vector ID 数组（不能与 filter 同时使用）',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '用于标识待删除 Vector 的元数据筛选条件（不能与 ids 同时使用）',
  },
]}
/>

## 相关内容

- [元数据筛选器](../rag/metadata-filters)
