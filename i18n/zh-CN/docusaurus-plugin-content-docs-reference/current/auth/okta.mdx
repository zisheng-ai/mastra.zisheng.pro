---
title: "参考：MastraAuthOkta 和 MastraRBACOkta | Auth"
description: "MastraAuthOkta 和 MastraRBACOkta 类的 API 参考；这些类使用 Okta 对 Mastra 应用进行身份验证和授权。"
packages:
  - "@mastra/auth-okta"
  - "@mastra/core"
---

# MastraAuthOkta 和 MastraRBACOkta 类

## MastraAuthOkta 类 {/* #mastraauthokta-class */}

`MastraAuthOkta` 类使用 Okta 为 Mastra 提供身份验证。它实现使用加密 Session cookie 的 OAuth 2.0 / OIDC 登录流程，并使用 `auth` 选项与 Mastra server 集成。

### 使用示例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthOkta } from '@mastra/auth-okta'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthOkta({
      domain: process.env.OKTA_DOMAIN,
      clientId: process.env.OKTA_CLIENT_ID,
      clientSecret: process.env.OKTA_CLIENT_SECRET,
      redirectUri: process.env.OKTA_REDIRECT_URI,
    }),
  },
})
```

:::note

如果已设置所需的环境变量，可以省略构造函数参数。在这种情况下，直接使用不带参数的 `new MastraAuthOkta()`。

:::

### 构造函数参数 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'domain',
    type: 'string',
    description: 'Okta 域（例如 `dev-123456.okta.com`），用于构造 issuer URL 和 API endpoint。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_DOMAIN',
  },
  {
    name: 'clientId',
    type: 'string',
    description: '来自 Okta 应用的 OAuth client ID。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_CLIENT_ID',
  },
  {
    name: 'clientSecret',
    type: 'string',
    description: 'OAuth client secret。SSO authorization code 流程必需。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_CLIENT_SECRET',
  },
  {
    name: 'issuer',
    type: 'string',
    description: 'Token issuer URL。使用自定义 authorization server 时请覆盖此值。',
    isOptional: true,
    defaultValue: '`https://{domain}/oauth2/default`',
  },
  {
    name: 'redirectUri',
    type: 'string',
    description: 'SSO callback 的 OAuth redirect URI。必须与 Okta 应用中配置的 redirect URI 一致。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_REDIRECT_URI',
  },
  {
    name: 'scopes',
    type: 'string[]',
    description: '登录流程中请求的 OAuth scope。',
    isOptional: true,
    defaultValue: "['openid', 'profile', 'email', 'groups']",
  },
  {
    name: 'apiToken',
    type: 'string',
    description: '通过 Users API 查找用户所需的 Okta API token。要让 `getUser()` 按 ID 返回用户数据，必须提供此项。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_API_TOKEN',
  },
  {
    name: 'session',
    type: 'OktaSessionOptions',
    description: 'Session cookie 配置。',
    isOptional: true,
    properties: [
      {
        type: 'OktaSessionOptions',
        parameters: [
          {
            name: 'cookieName',
            type: 'string',
            description: 'Session cookie 的名称。',
            isOptional: true,
            defaultValue: "'okta_session'",
          },
          {
            name: 'cookieMaxAge',
            type: 'number',
            description: 'Cookie 的最大有效期，单位为秒。',
            isOptional: true,
            defaultValue: '86400 (24 hours)',
          },
          {
            name: 'cookiePassword',
            type: 'string',
            description:
              '用于加密 Session cookie 的密码，长度必须至少为 32 个字符。如果未设置，则会使用自动生成且重启后不会保留的值。',
            isOptional: true,
            defaultValue: 'process.env.OKTA_COOKIE_PASSWORD',
          },
          {
            name: 'secureCookies',
            type: 'boolean',
            description: '为 Session cookie 设置 `Secure` 标志。',
            isOptional: true,
            defaultValue: 'true in production, false otherwise',
          },
        ],
      },
    ],
  },
  {
    name: 'name',
    type: 'string',
    description: 'Auth Provider 实例的自定义名称。',
    isOptional: true,
    defaultValue: "'okta'",
  },
]}
/>

### 环境变量 {/* #environment-variables */}

未提供构造函数选项时，会自动使用以下环境变量：

<PropertiesTable
  content={[
  {
    name: 'OKTA_DOMAIN',
    type: 'string',
    description: 'Okta 域（例如 `dev-123456.okta.com`），可在 Okta 管理控制台中找到。',
  },
  {
    name: 'OKTA_CLIENT_ID',
    type: 'string',
    description: '来自 Okta 应用的 OAuth client ID。',
  },
  {
    name: 'OKTA_CLIENT_SECRET',
    type: 'string',
    description: '来自 Okta 应用的 OAuth client secret。',
  },
  {
    name: 'OKTA_ISSUER',
    type: 'string',
    description: 'Token issuer URL。未设置时默认为 `https://{domain}/oauth2/default`。',
    isOptional: true,
  },
  {
    name: 'OKTA_REDIRECT_URI',
    type: 'string',
    description: 'SSO callback 的 OAuth redirect URI。',
  },
  {
    name: 'OKTA_COOKIE_PASSWORD',
    type: 'string',
    description: '用于加密 Session cookie 的密码，长度必须至少为 32 个字符。',
    isOptional: true,
  },
  {
    name: 'OKTA_API_TOKEN',
    type: 'string',
    description: '用于用户查找和 RBAC group 解析的 Okta API token。',
    isOptional: true,
  },
]}
/>

### 身份验证流程 {/* #authentication-flow */}

`MastraAuthOkta` 按以下顺序对请求进行身份验证：

1. **Session cookie**：读取并解密已加密的 Session cookie。如果 Session 有效且未过期，则用户身份验证成功。
1. **JWT fallback**：如果不存在 Session cookie，则根据 Okta 的 JWKS endpoint 验证 `Authorization` header 中的 token。

身份验证完成后，`authorizeUser` 会检查用户是否具有有效的 `oktaId`。可以提供自定义 `authorizeUser` 函数来实现其他逻辑。

### `OktaUser` 类型 {/* #oktauser-type */}

`OktaUser` 类型扩展了基础 `EEUser` interface，并增加 Okta 专用字段：

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '用户标识符（映射到 `sub` claim）。',
  },
  {
    name: 'oktaId',
    type: 'string',
    description: 'Okta user ID（与 `id` 相同）。',
  },
  {
    name: 'email',
    type: 'string',
    description: '用户电子邮件地址。',
    isOptional: true,
  },
  {
    name: 'name',
    type: 'string',
    description: '根据 token claim 构造的用户显示名称。',
    isOptional: true,
  },
  {
    name: 'avatarUrl',
    type: 'string',
    description: '用户头像的 URL。',
    isOptional: true,
  },
  {
    name: 'groups',
    type: 'string[]',
    description: '用户所属的 Okta group，由 `groups` claim 填充。',
    isOptional: true,
  },
]}
/>

## MastraRBACOkta 类 {/* #mastrarbacokta-class */}

`MastraRBACOkta` 类将 Okta group 映射到 Mastra permission。它从 Okta API 获取用户 group，并根据可配置的 role mapping 进行解析。可以将其与 `MastraAuthOkta` 或其他 Auth Provider 搭配使用。

:::note

RBAC 需要有效的 Enterprise Edition license。在开发环境中无需 license 即可运行，方便本地试用；生产环境则需要 license。有关详细信息，请[联系销售团队](https://mastra.ai/contact)。

:::

### 使用示例 {/* #usage-example-1 */}

将 `MastraRBACOkta` 传给 `rbac` 选项，即可与 Auth Provider 搭配使用：

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthOkta, MastraRBACOkta } from '@mastra/auth-okta'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthOkta(),
    rbac: new MastraRBACOkta({
      roleMapping: {
        Admin: ['*'],
        Engineering: ['agents:*', 'workflows:*', 'tools:*'],
        Viewer: ['agents:read', 'workflows:read'],
        _default: [],
      },
    }),
  },
})
```

如果要将 Okta RBAC 与其他 Auth Provider 搭配使用，请传入 `getUserId` 函数，从其他 Provider 的用户对象中解析 Okta user ID：

```typescript title="src/mastra/index.ts"
import { MastraAuthAuth0 } from '@mastra/auth-auth0'
import { MastraRBACOkta } from '@mastra/auth-okta'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthAuth0(),
    rbac: new MastraRBACOkta({
      getUserId: user => user.metadata?.oktaUserId || user.email,
      roleMapping: {
        Engineering: ['agents:*', 'workflows:*'],
        Admin: ['*'],
        _default: [],
      },
    }),
  },
})
```

### 构造函数参数 {/* #constructor-parameters-1 */}

<PropertiesTable
  content={[
  {
    name: 'roleMapping',
    type: 'RoleMapping',
    description:
      "将 Okta group 名称映射到 Mastra permission 字符串数组。使用 `'_default'` 为不匹配任何 group 的用户分配 permission。支持 `'*'`（完全访问权限）和 `'agents:*'`（所有 Agent 操作）等通配符。",
    isOptional: false,
  },
  {
    name: 'domain',
    type: 'string',
    description: 'Okta 域，用于初始化 Okta management SDK。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_DOMAIN',
  },
  {
    name: 'apiToken',
    type: 'string',
    description: 'Management SDK 使用的 Okta API token。从 Okta API 获取用户 group 时必需。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_API_TOKEN',
  },
  {
    name: 'getUserId',
    type: '(user: unknown) => string | undefined',
    description:
      '从用户对象中提取 Okta user ID。将 Okta RBAC 与其他 Auth Provider 组合使用时请提供此项。未提供时，会 fallback 到用户对象中的 `oktaId` 或 `id`。',
    isOptional: true,
  },
  {
    name: 'cache',
    type: 'PermissionCacheOptions',
    description: '配置用于 group 查找的 LRU cache。',
    isOptional: true,
    properties: [
      {
        type: 'PermissionCacheOptions',
        parameters: [
          {
            name: 'maxSize',
            type: 'number',
            description: '可缓存的最大用户数。',
            isOptional: true,
            defaultValue: '1000',
          },
          {
            name: 'ttlMs',
            type: 'number',
            description: '有效期，单位为毫秒。',
            isOptional: true,
            defaultValue: '60000',
          },
        ],
      },
    ],
  },
]}
/>
