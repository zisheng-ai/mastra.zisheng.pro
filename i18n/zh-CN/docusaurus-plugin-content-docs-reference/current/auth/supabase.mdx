---
title: "参考：MastraAuthSupabase 类 | Auth"
description: "MastraAuthSupabase 类的 API 参考；该类使用 Supabase Auth 对 Mastra 应用进行身份验证。"
packages:
  - "@mastra/auth-supabase"
  - "@mastra/core"
---

# MastraAuthSupabase 类

`MastraAuthSupabase` 类使用 Supabase Auth 为 Mastra 提供身份验证。它通过 Supabase 的身份验证系统验证传入请求，并使用 `auth` 选项与 Mastra server 集成。

## 使用示例

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthSupabase } from '@mastra/auth-supabase'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthSupabase({
      url: process.env.SUPABASE_URL,
      anonKey: process.env.SUPABASE_ANON_KEY,
    }),
  },
})
```

## 构造函数参数

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: 'Supabase 项目的 URL，可在 Supabase 项目设置中找到。',
    isOptional: true,
    defaultValue: 'process.env.SUPABASE_URL',
  },
  {
    name: 'anonKey',
    type: 'string',
    description: 'Supabase 项目的匿名公钥，用于客户端身份验证。',
    isOptional: true,
    defaultValue: 'process.env.SUPABASE_ANON_KEY',
  },
  {
    name: 'name',
    type: 'string',
    description: '身份验证 Provider 实例的自定义名称。',
    isOptional: true,
  },
  {
    name: 'authorizeUser',
    type: '(user: User, request: HoneRequest) => Promise<boolean> | boolean',
    description:
      "用于确定是否应授予用户访问权限的自定义授权函数。在 token 验证后调用。默认检查 'users' 表中的 'isAdmin' 列。",
    isOptional: true,
  },
]}
/>

## 相关内容

[MastraAuthSupabase](/docs/server/auth/supabase)
