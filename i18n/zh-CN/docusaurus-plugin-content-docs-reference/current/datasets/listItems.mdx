---
title: "参考：dataset.listItems() | 数据集"
description: "`dataset.listItems()` 方法的文档，该方法列出数据集中的条目，并支持可选的分页和筛选。"
packages:
  - "@mastra/core"
---

# dataset.listItems()

**添加于：** `@mastra/core@1.4.0`

列出数据集中的条目。仅提供 `version` 时，返回该版本中所有条目的 `DatasetItem[]` 裸数组快照。其他所有情况（不传参数，或提供 `search` / `page` / `perPage`，无论是否同时提供 `version`）均返回分页的 `{ items, pagination }` 结构。

## 使用示例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Paginated list (default: page 0, 20 per page)
const result = await dataset.listItems()

// List with search
const filtered = await dataset.listItems({ search: 'TypeScript', page: 0, perPage: 10 })

// Paginated list scoped to a specific version
const versionedPage = await dataset.listItems({ version: 2, page: 0, perPage: 20 })

// Version-only snapshot returns a bare DatasetItem[] (deprecated form —
// prefer passing page/perPage to receive the paginated shape).
const versionedItems = await dataset.listItems({ version: 2 })
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description:
      '要列出条目时对应的数据集版本。单独传入时，返回该版本所有条目的 `DatasetItem[]` 裸数组快照。将 `search` / `page` / `perPage` 与 `version` 一起传入时，则改为返回分页结构。',
  },
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: '分页页码。默认为 `0`。',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: '每页的条目数。默认为 `20`。',
  },
  {
    name: 'search',
    type: 'string',
    isOptional: true,
    description: '用于筛选条目的搜索字符串。',
  },
]}
/>

## 返回值 {/* #returns */}

仅提供 `version`（不提供 `search`、`page` 或 `perPage`）时：

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description:
      '指定数据集版本中所有条目的裸数组。保留此结构是为了向后兼容，但它已弃用——请传入 `page` / `perPage`（或 `search`），以始终接收下方的分页结构。',
  },
]}
/>

<br />

其他所有情况（不传参数，或提供 `search` / `page` / `perPage`，无论是否同时提供 `version`）：

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: '分页响应。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'items',
            type: 'DatasetItem[]',
            description: '当前页面的条目数组。',
          },
          {
            name: 'pagination',
            type: 'object',
            description: '分页元数据。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'total',
                    type: 'number',
                    description: '条目总数。',
                  },
                  {
                    name: 'page',
                    type: 'number',
                    description: '当前页码。',
                  },
                  {
                    name: 'perPage',
                    type: 'number | false',
                    description: '每页条目数；如果未分页则为 `false`。',
                  },
                  {
                    name: 'hasMore',
                    type: 'boolean',
                    description: '是否还有更多页面。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>
