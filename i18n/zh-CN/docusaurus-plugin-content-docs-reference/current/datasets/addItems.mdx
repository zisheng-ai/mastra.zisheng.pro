---
title: "参考：dataset.addItems() | 数据集"
description: "`dataset.addItems()` 方法的文档，该方法向数据集批量添加多个条目。"
packages:
  - "@mastra/core"
---

# dataset.addItems()

**添加于：** `@mastra/core@1.4.0`

通过一次批量操作向数据集添加多个条目。

## 使用示例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const items = await dataset.addItems({
  items: [
    { input: { question: 'What is AI?' }, groundTruth: { answer: 'Artificial Intelligence' } },
    { input: { question: 'What is ML?' }, groundTruth: { answer: 'Machine Learning' } },
    { input: { question: 'What is DL?' }, metadata: { category: 'deep-learning' } },
  ],
})

console.log(`Added ${items.length} items`)
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'items',
    type: 'Array<object>',
    description: '要添加的条目数组。每个条目的结构都与 dataset.addItem() 的输入相同。',
    properties: [
      {
        type: 'DatasetItem',
        parameters: [
          {
            name: 'input',
            type: 'unknown',
            description: '条目的输入数据。',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: '预期输出或 ground truth。',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: '任意元数据。',
          },
        ],
      },
    ],
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description: '已创建的数据集条目数组。条目结构请参阅 dataset.addItem()。',
  },
]}
/>
