---
title: "参考：dataset.getExperiment() | 数据集"
description: "`dataset.getExperiment()` 方法的文档，该方法按 ID 获取特定实验。"
packages:
  - "@mastra/core"
---

# dataset.getExperiment()

**添加于：** `@mastra/core@1.4.0`

按 ID 获取特定实验（运行）。

## 使用示例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const experiment = await dataset.getExperiment({ experimentId: 'exp-id' })

console.log(`Status: ${experiment.status}`)
console.log(`${experiment.succeededCount}/${experiment.totalItems} succeeded`)
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: '要获取的实验 ID。',
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Experiment>',
    description: '实验记录。完整结构请参阅 dataset.listExperiments()。',
  },
]}
/>
