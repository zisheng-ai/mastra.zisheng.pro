---
title: "参考：dataset.deleteItem() | 数据集"
description: "`dataset.deleteItem()` 方法的文档，该方法从数据集中删除单个条目。"
packages:
  - "@mastra/core"
---

# dataset.deleteItem()

**添加于：** `@mastra/core@1.4.0`

按 ID 从数据集中删除单个条目。

## 使用示例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItem({ itemId: 'item-id' })
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: '要删除的条目 ID。',
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: '条目删除后，Promise 完成。',
  },
]}
/>
