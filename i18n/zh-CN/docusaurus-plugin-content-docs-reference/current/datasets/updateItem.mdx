---
title: "参考：dataset.updateItem() | 数据集"
description: "`dataset.updateItem()` 方法的文档，该方法更新数据集中的现有条目。"
packages:
  - "@mastra/core"
---

# dataset.updateItem()

**添加于：** `@mastra/core@1.4.0`

更新数据集中的现有条目。仅更新提供的字段。更新条目会创建新版本。

## 使用示例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const updated = await dataset.updateItem({
  itemId: 'item-id',
  input: { question: 'What is TypeScript?' },
  groundTruth: { answer: 'A typed superset of JavaScript' },
  metadata: { reviewed: true },
})
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: '要更新的条目 ID。',
  },
  {
    name: 'input',
    type: 'unknown',
    isOptional: true,
    description: '更新后的输入数据。',
  },
  {
    name: 'groundTruth',
    type: 'unknown',
    isOptional: true,
    description: '更新后的 ground truth。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '更新后的元数据。',
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem>',
    description: '更新后的数据集条目。条目结构请参阅 dataset.addItem()。',
  },
]}
/>
