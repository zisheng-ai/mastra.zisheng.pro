---
title: "参考：dataset.deleteExperiment() | 数据集"
description: "`dataset.deleteExperiment()` 方法的文档，该方法按 ID 删除实验。"
packages:
  - "@mastra/core"
---

# dataset.deleteExperiment()

**添加于：** `@mastra/core@1.4.0`

按 ID 删除实验（运行），包括所有关联结果。

## 使用示例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteExperiment({ experimentId: 'exp-id' })
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: '要删除的实验 ID。',
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: '实验及其结果删除后，Promise 完成。',
  },
]}
/>
