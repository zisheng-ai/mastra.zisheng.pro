---
title: "参考：dataset.getDetails() | 数据集"
description: "`dataset.getDetails()` 方法的文档，该方法从存储中获取完整的数据集记录。"
packages:
  - "@mastra/core"
---

# dataset.getDetails()

**添加于：** `@mastra/core@1.4.0`

从存储中获取完整的数据集记录，包括元数据、schema 和版本信息。

## 使用示例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Get dataset details
const details = await dataset.getDetails()
console.log(details.name, details.version)
```

## 参数

此方法不接受任何参数。

## 返回值

返回 `Promise<DatasetRecord>`。如果未找到数据集，则抛出 `MastraError`。

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '数据集的唯一标识符。',
  },
  {
    name: 'name',
    type: 'string',
    description: '数据集的显示名称。',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: '数据集的描述。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '与数据集关联的任意元数据。',
  },
  {
    name: 'inputSchema',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '条目输入的 JSON Schema。',
  },
  {
    name: 'groundTruthSchema',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '条目 ground truth 的 JSON Schema。',
  },
  {
    name: 'version',
    type: 'number',
    description: '数据集的当前版本号。',
  },
  {
    name: 'createdAt',
    type: 'Date',
    description: '数据集的创建时间。',
  },
  {
    name: 'updatedAt',
    type: 'Date',
    description: '数据集的最后更新时间。',
  },
]}
/>
