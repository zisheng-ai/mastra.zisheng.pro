---
title: "参考：dataset.getItemHistory() | 数据集"
description: "`dataset.getItemHistory()` 方法的文档，该方法获取数据集条目的完整 SCD-2 变更历史记录。"
packages:
  - "@mastra/core"
---

# dataset.getItemHistory()

**添加于：** `@mastra/core@1.4.0`

获取特定条目在所有数据集版本中的完整 SCD-2（缓慢变化维度类型 2）历史记录。

## 使用示例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const history = await dataset.getItemHistory({ itemId: 'item-id' })

for (const row of history) {
  console.log(`Version ${row.datasetVersion}: ${row.isDeleted ? 'deleted' : 'active'}`)
}
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: '要获取历史记录的条目 ID。',
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItemRow[]>',
    description: '按版本排序的条目版本记录数组。',
    properties: [
      {
        type: 'DatasetItemRow',
        parameters: [
          {
            name: 'id',
            type: 'string',
            description: '记录的唯一标识符。',
          },
          {
            name: 'datasetId',
            type: 'string',
            description: '父数据集的 ID。',
          },
          {
            name: 'datasetVersion',
            type: 'number',
            description: '此记录对应的数据集版本。',
          },
          {
            name: 'validTo',
            type: 'number | null',
            description: '此记录被取代时的版本；如果仍为当前记录，则为 `null`。',
          },
          {
            name: 'isDeleted',
            type: 'boolean',
            description: '此记录是否表示删除操作。',
          },
          {
            name: 'input',
            type: 'unknown',
            description: '此版本的输入数据。',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: '此版本的 ground truth。',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: '此版本的元数据。',
          },
          {
            name: 'createdAt',
            type: 'Date',
            description: '此记录的创建时间。',
          },
          {
            name: 'updatedAt',
            type: 'Date',
            description: '此记录的最后更新时间。',
          },
        ],
      },
    ],
  },
]}
/>
