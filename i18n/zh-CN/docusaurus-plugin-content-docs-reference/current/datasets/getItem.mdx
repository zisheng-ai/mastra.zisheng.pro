---
title: "参考：dataset.getItem() | 数据集"
description: "`dataset.getItem()` 方法的文档，该方法按 ID 获取单个条目。"
packages:
  - "@mastra/core"
---

# dataset.getItem()

**添加于：** `@mastra/core@1.4.0`

按 ID 获取单个数据集条目，也可以指定数据集版本。

## 使用示例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Get latest version of an item
const item = await dataset.getItem({ itemId: 'item-id' })

// Get item at a specific dataset version
const versionedItem = await dataset.getItem({ itemId: 'item-id', version: 2 })
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: '条目的唯一标识符。',
  },
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description: '要获取条目时对应的数据集版本。默认为最新版本。',
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem | null>',
    description: '数据集条目；如果未找到则为 null。条目结构请参阅 dataset.addItem()。',
  },
]}
/>
