---
title: "参考：dataset.startExperiment() | 数据集"
description: "`dataset.startExperiment()` 方法的文档，该方法在数据集上运行实验并等待完成。"
packages:
  - "@mastra/core"
---

# dataset.startExperiment()

**添加于：** `@mastra/core@1.4.0`

在数据集上运行实验并等待其完成。针对目标（Agent、Workflow 或 Scorer）执行所有条目，并可选择进行评分。

## 使用示例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Run against a registered agent with a flat scorer list
const summary = await dataset.startExperiment({
  targetType: 'agent',
  targetId: 'my-agent',
  scorers: ['accuracy', 'relevancy'],
  maxConcurrency: 10,
})

// Or pass the same categorised shape accepted by runEvals
const summary2 = await dataset.startExperiment({
  targetType: 'agent',
  targetId: 'my-agent',
  scorers: {
    agent: [accuracyScorer],
    trajectory: [toolOrderScorer],
  },
})

// For workflow targets, score individual steps with their own scorers
const summary3 = await dataset.startExperiment({
  targetType: 'workflow',
  targetId: 'my-workflow',
  scorers: {
    workflow: [overallScorer],
    steps: {
      'fetch-data': [fetchScorer],
      transform: [transformScorer],
    },
    trajectory: [executionPathScorer],
  },
})

console.log(`${summary.succeededCount}/${summary.totalItems} succeeded`)
console.log(`Status: ${summary.status}`)
console.log(`${summary2.succeededCount}/${summary2.totalItems} succeeded`)
console.log(`Status: ${summary2.status}`)
```

## 参数

<PropertiesTable
  content={[
  {
    name: 'targetType',
    type: "'agent' | 'workflow' | 'scorer'",
    isOptional: true,
    description: '要针对其运行条目的已注册目标类型。与 `targetId` 一起使用。',
  },
  {
    name: 'targetId',
    type: 'string',
    isOptional: true,
    description: '已注册目标的 ID。与 `targetType` 一起使用。',
  },
  {
    name: 'scorers',
    type: '(MastraScorer | string)[] | AgentScorerConfig | WorkflowScorerConfig',
    isOptional: true,
    description:
      '用于评估每个结果的 Scorer。接受由 `MastraScorer` 实例或已注册 Scorer ID 组成的扁平数组，也接受与 `runEvals` 相同的分类配置结构（`AgentScorerConfig` / `WorkflowScorerConfig`）。无论使用哪种形式，Trajectory Scorer（`type: "trajectory"`）都会自动接收预先提取的 `Trajectory` 作为输出。对于 Workflow 目标，可通过 `scorers: { steps: { stepId: [...] } }` 传入各步骤的 Scorer，并针对每个步骤的输出运行；其结果会携带来源 `stepId`，并保持 `targetScope: "span"`（与 `runEvals` 一致）。',
  },
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: '实验的显示名称。',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: '实验的描述。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '实验的任意元数据。',
  },
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description: '固定使用特定的数据集版本。默认为最新版本。',
  },
  {
    name: 'maxConcurrency',
    type: 'number',
    isOptional: true,
    description: '条目执行的最大并发数。默认为 `5`。',
  },
  {
    name: 'signal',
    type: 'AbortSignal',
    isOptional: true,
    description: '用于取消实验的 AbortSignal。',
  },
  {
    name: 'itemTimeout',
    type: 'number',
    isOptional: true,
    description: '每个条目的执行超时时间，单位为毫秒。',
  },
  {
    name: 'maxRetries',
    type: 'number',
    isOptional: true,
    description: '每个条目失败后的最大重试次数。默认为 `0`（不重试）。Abort 错误永远不会重试。',
  },
  {
    name: 'unmockedToolPolicy',
    type: "'allow' | 'deny'",
    isOptional: true,
    defaultValue: "'allow'",
    description:
      '控制未声明的 Agent Tool 调用。`allow` 会实际执行这些调用；`deny` 会在执行前让条目以 `TOOL_MOCK_NOT_DECLARED` 失败。条目级值会覆盖此实验默认值。',
  },
  {
    name: 'persistence',
    type: 'ExperimentPersistencePolicy',
    isOptional: true,
    description:
      '控制本次运行是否写入实验记录和评分记录。目标和 Scorer 仍会执行，结果仍可在返回的摘要中获取。',
    properties: [
      {
        type: 'ExperimentPersistencePolicy',
        parameters: [
          {
            name: 'experiments',
            type: "'default' | 'none'",
            isOptional: true,
            defaultValue: "'default'",
            description:
              '设为 `none` 可跳过实验创建以及条目结果、进度和最终状态的写入。',
          },
          {
            name: 'scores',
            type: "'default' | 'none'",
            isOptional: true,
            defaultValue: "'default'",
            description: '设为 `none` 可跳过分数写入，同时仍运行 Scorer。',
          },
        ],
      },
    ],
  },
]}
/>

## 返回值

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<ExperimentSummary>',
    description: '已完成实验的摘要。',
    properties: [
      {
        type: 'ExperimentSummary',
        parameters: [
          {
            name: 'experimentId',
            type: 'string',
            description: '实验的唯一 ID。',
          },
          {
            name: 'status',
            type: "'pending' | 'running' | 'completed' | 'failed'",
            description: '实验的最终状态。',
          },
          {
            name: 'totalItems',
            type: 'number',
            description: '数据集中的条目总数。',
          },
          {
            name: 'succeededCount',
            type: 'number',
            description: '成功的条目数。',
          },
          {
            name: 'failedCount',
            type: 'number',
            description: '失败的条目数。',
          },
          {
            name: 'skippedCount',
            type: 'number',
            description: '跳过的条目数（例如因中止而跳过）。',
          },
          {
            name: 'completedWithErrors',
            type: 'boolean',
            description: '如果运行已完成但有部分条目失败，则为 `true`。',
          },
          {
            name: 'startedAt',
            type: 'Date',
            description: '实验开始时间。',
          },
          {
            name: 'completedAt',
            type: 'Date',
            description: '实验完成时间。',
          },
          {
            name: 'results',
            type: 'ItemWithScores[]',
            description: '所有条目结果及其分数。',
            properties: [
              {
                type: 'ItemWithScores',
                parameters: [
                  {
                    name: 'itemId',
                    type: 'string',
                    description: '数据集条目的 ID。',
                  },
                  {
                    name: 'itemVersion',
                    type: 'number',
                    description: '执行条目时的数据集版本。',
                  },
                  {
                    name: 'input',
                    type: 'unknown',
                    description: '传递给目标的输入数据。',
                  },
                  {
                    name: 'output',
                    type: 'unknown | null',
                    description: '目标输出；如果失败则为 `null`。',
                  },
                  {
                    name: 'groundTruth',
                    type: 'unknown | null',
                    description: '数据集条目的预期输出。',
                  },
                  {
                    name: 'error',
                    type: '{ message: string; stack?: string; code?: string } | null',
                    description: '执行失败时的结构化错误。',
                  },
                  {
                    name: 'startedAt',
                    type: 'Date',
                    description: '条目执行开始时间。',
                  },
                  {
                    name: 'completedAt',
                    type: 'Date',
                    description: '条目执行完成时间。',
                  },
                  {
                    name: 'retryCount',
                    type: 'number',
                    description: '重试次数。',
                  },
                  {
                    name: 'scores',
                    type: 'ScorerResult[]',
                    description: '所有 Scorer 针对此条目的结果。',
                    properties: [
                      {
                        type: 'ScorerResult',
                        parameters: [
                          {
                            name: 'scorerId',
                            type: 'string',
                            description: 'Scorer 的 ID。',
                          },
                          {
                            name: 'scorerName',
                            type: 'string',
                            description: 'Scorer 的显示名称。',
                          },
                          {
                            name: 'score',
                            type: 'number | null',
                            description: '计算得到的分数；如果 Scorer 失败则为 `null`。',
                          },
                          {
                            name: 'reason',
                            type: 'string | null',
                            description: '分数的原因或解释。',
                          },
                          {
                            name: 'error',
                            type: 'string | null',
                            description: 'Scorer 失败时的错误消息。',
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## 相关内容

- [dataset.startExperimentAsync()](/reference/datasets/startExperimentAsync)
- [dataset.listExperiments()](/reference/datasets/listExperiments)
- [DatasetsManager.compareExperiments()](/reference/datasets/compareExperiments)
