---
title: "参考：dataset.listVersions() | 数据集"
description: "`dataset.listVersions()` 方法的文档，该方法列出数据集的所有版本。"
packages:
  - "@mastra/core"
---

# dataset.listVersions()

**添加于：** `@mastra/core@1.4.0`

以分页方式列出数据集的所有版本。

## 使用示例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const { versions, pagination } = await dataset.listVersions({ page: 0, perPage: 10 })

for (const version of versions) {
  console.log(`Version ${version.version} created at ${version.createdAt}`)
}
```

## 参数

<PropertiesTable
  content={[
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: '页码。默认为 `0`。',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: '每页的版本数。默认为 `20`。',
  },
]}
/>

## 返回值

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: '分页的版本列表。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'versions',
            type: 'DatasetVersion[]',
            description: '版本记录数组。',
            properties: [
              {
                type: 'DatasetVersion',
                parameters: [
                  {
                    name: 'id',
                    type: 'string',
                    description: '版本记录的唯一标识符。',
                  },
                  {
                    name: 'datasetId',
                    type: 'string',
                    description: '父数据集的 ID。',
                  },
                  {
                    name: 'version',
                    type: 'number',
                    description: '版本号。',
                  },
                  {
                    name: 'createdAt',
                    type: 'Date',
                    description: '此版本的创建时间。',
                  },
                ],
              },
            ],
          },
          {
            name: 'pagination',
            type: 'object',
            description: '分页元数据。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'total',
                    type: 'number',
                    description: '版本总数。',
                  },
                  {
                    name: 'page',
                    type: 'number',
                    description: '当前页码。',
                  },
                  {
                    name: 'perPage',
                    type: 'number | false',
                    description: '每页版本数；如果未分页则为 `false`。',
                  },
                  {
                    name: 'hasMore',
                    type: 'boolean',
                    description: '是否还有更多页面。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>
