---
title: "参考：dataset.update() | 数据集"
description: "`dataset.update()` 方法的文档，该方法更新数据集元数据和 schema。"
packages:
  - "@mastra/core"
---

# dataset.update()

**添加于：** `@mastra/core@1.4.0`

更新数据集的元数据、名称、描述和/或 schema。使用 Standard JSON Schema（Zod、Valibot、ArkType 等）定义 schema。

## 使用示例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'
import { z } from 'zod'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Update with plain metadata
const updated = await dataset.update({
  name: 'Updated QA pairs',
  description: 'Revised evaluation set',
})

// Update with Zod schema (library that supports Standard JSON Schema)
const updated2 = await dataset.update({
  inputSchema: z.object({
    question: z.string(),
    context: z.string().optional(),
  }),
})
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: '新的显示名称。',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: '新的描述。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '更新后的元数据。',
  },
  {
    name: 'inputSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: '条目输入的 Standard JSON Schema。',
  },
  {
    name: 'groundTruthSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: '条目 ground truth 的 Standard JSON Schema。',
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetRecord>',
    description: '更新后的数据集记录。完整结构请参阅 dataset.getDetails()。',
  },
]}
/>
