---
title: "参考：Agent.getMemory() | Agent"
description: "Mastra Agent 中 `Agent.getMemory()` 方法的文档，该方法用于获取与 Agent 关联的内存系统。"
packages:
  - "@mastra/core"
---

# Agent.getMemory()

`.getMemory()` 方法用于获取与 Agent 关联的内存系统。该方法用于访问 Agent 的内存能力，以便跨对话存储和检索信息。

## 用法示例

```typescript
await agent.getMemory()
```

## 参数

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ requestContext?: RequestContext }',
    isOptional: true,
    defaultValue: '{}',
    description: '包含请求上下文的可选配置对象。',
    properties: [
      {
        type: '{ requestContext?: RequestContext }',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            defaultValue: 'new RequestContext()',
            description: '用于依赖注入和提供上下文信息的 Request Context。',
          },
        ],
      },
    ],
  },
]}
/>

## 返回值

<PropertiesTable
  content={[
  {
    name: 'memory',
    type: 'Promise<MastraMemory | undefined>',
    description:
      '一个解析为 Agent 所配置内存系统的 promise；如果未配置内存系统，则解析为 undefined。',
  },
]}
/>

## 扩展用法示例

```typescript
await agent.getMemory({
  requestContext: new RequestContext(),
})
```

## 相关内容

- [内存](/docs/memory/overview)
- [Request Context](/docs/server/request-context)
