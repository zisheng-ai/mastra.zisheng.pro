---
title: '参考：Agent.getMetadata() | Agent'
description: "Mastra Agent 中 `Agent.getMetadata()` 方法的文档，该方法用于获取为 Agent 配置的元数据。"
packages:
  - '@mastra/core'
---

# Agent.getMetadata()

`.getMetadata()` 方法用于获取为 Agent 配置的元数据；如果元数据是函数，则会解析该函数。你可以使用元数据在客户端中对 Agent 进行分类，而无需将这些数据编码到 Agent ID 或名称中。

## 用法示例 {/* #usage-example */}

```typescript
await agent.getMetadata()
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ requestContext?: RequestContext }',
    isOptional: true,
    defaultValue: '{}',
    description: '包含请求上下文的可选配置对象。',
    properties: [
      {
        type: '{ requestContext?: RequestContext }',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            defaultValue: 'undefined',
            description: '传递给动态元数据函数的 Request Context。元数据为静态值时会忽略此参数。',
          },
        ],
      },
    ],
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'metadata',
    type: 'Record<string, unknown> | Promise<Record<string, unknown>>',
    description:
      '为 Agent 配置的元数据；如果未配置元数据，则为 `undefined`。如果将元数据定义为函数，可以直接返回元数据，也可以返回一个解析为元数据的 promise。',
  },
]}
/>

## 扩展用法示例 {/* #extended-usage-example */}

静态元数据：

```typescript title="src/mastra/agents/support-agent.ts"
import { Agent } from '@mastra/core/agent'

export const supportAgent = new Agent({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'You help customers with support requests.',
  model: '__GATEWAY_OPENAI_MODEL__',
  metadata: { type: 'support' },
})

const metadata = supportAgent.getMetadata()
```

从请求上下文解析的动态元数据：

```typescript title="src/mastra/agents/support-agent.ts"
import { Agent } from '@mastra/core/agent'

export const supportAgent = new Agent({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'You help customers with support requests.',
  model: '__GATEWAY_OPENAI_MODEL__',
  metadata: ({ requestContext }) => ({
    type: 'support',
    tenant: requestContext.get('tenant'),
  }),
})

const metadata = await supportAgent.getMetadata({ requestContext })
```

## 相关内容 {/* #related */}

- [Agent 概览](/docs/agents/overview)
- [Request Context](/docs/server/request-context)
