---
title: "参考：.listSkills() | Agents"
description: "Agent .listSkills() 方法的 API 参考。该方法返回所有已配置 Skill 的元数据。"
packages:
  - "@mastra/core"
---

# `.listSkills()`

返回 Agent 可用的所有 Skill 的元数据，包括 Agent 级 Skill 和 Workspace Skill。

## 使用示例 {/* #usage-example */}

```typescript title="src/routes/skills.ts"
import { agent } from '../mastra/agents'

const skills = await agent.listSkills()

for (const skill of skills) {
  console.log(`${skill.name}: ${skill.description}`)
}
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'object',
    isOptional: true,
    description: '用于解析 Skill 的选项。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            description: '传递给动态 Skill 解析器的 request context。',
          },
        ],
      },
    ],
  },
]}
/>

## 返回值 {/* #return-value */}

返回 `Promise<SkillMetadata[]>`。

每个条目都包含所发现 Skill 的元数据：

```typescript
interface SkillMetadata {
  name: string
  description: string
  path: string
  source: { type: string; projectPath: string }
  references: string[]
  scripts: string[]
  assets: string[]
  license?: string
  compatibility?: string[]
  'user-invocable'?: boolean
  metadata?: Record<string, unknown>
}
```

## 合并行为 {/* #merging-behavior */}

同时配置 Agent 级 Skill 和 Workspace Skill 时，`.listSkills()` 返回合并后的集合。名称冲突时，Agent 级 Skill 优先。如果两者都定义了名为 `code-review` 的 Skill，则只返回 Agent 级版本。

## 相关内容 {/* #related */}

- [Agent Skill](/docs/agents/skills)
- [`.getSkill()` 参考](/reference/agents/getSkill)
- [`createSkill()` 参考](/reference/agents/createSkill)
