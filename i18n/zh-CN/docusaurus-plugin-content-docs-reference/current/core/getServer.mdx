---
title: "参考： Mastra.getServer() | Core"
description: "关于 `Mastra.getServer()` 方法的文档，该方法获取已配置的 server 配置。"
packages:
  - "@mastra/core"
---

# Mastra.getServer()

`.getServer()` 方法用于获取 Mastra 实例中已配置的 server 配置。

## 使用示例

```typescript
mastra.getServer()
```

## 参数

此方法不接受任何参数。

## 返回值

<PropertiesTable
  content={[
  {
    name: 'server',
    type: 'ServerConfig | undefined',
    description:
      '已配置的 server 配置，包括 port、host、studioBase、timeout、API route、middleware、CORS 设置和构建选项；如果未配置 server，则为 undefined。',
  },
]}
/>

## 相关内容

- [Server 部署](/docs/deployment/mastra-server)
- [Server 配置](/docs/server/custom-api-routes)
