---
title: "参考： Mastra.getToolById() | Core"
description: "关于 `Mastra.getToolById()` 方法的文档，该方法按固有 ID 或注册键获取 Tool。"
packages:
  - "@mastra/core"
---

# Mastra.getToolById()

`.getToolById()` 方法首先在 Mastra 级注册表中搜索固有 `id` 匹配的 Tool。如果没有匹配的固有 ID，则将该值视为注册键。

## 使用示例 {/* #usage-example */}

此示例使用 `weather` 作为注册键，并使用 `weather-tool` 作为 Tool 的固有 ID。

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core/mastra'
import { createTool } from '@mastra/core/tools'
import { z } from 'zod'

const weatherTool = createTool({
  id: 'weather-tool',
  description: 'Fetches weather for a location',
  inputSchema: z.object({
    location: z.string(),
  }),
  outputSchema: z.object({
    weather: z.string(),
  }),
  execute: async ({ location }) => ({
    weather: `Weather for ${location}`,
  }),
})

export const mastra = new Mastra({
  tools: {
    weather: weatherTool,
  },
})

const toolById = mastra.getToolById('weather-tool')
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: "TTools[TToolName]['id']",
    description: '要查找的 Tool 固有 ID。如果没有 Tool 具有该 ID，Mastra 会将此值用作注册键。',
  },
]}
/>

## 相关内容 {/* #related */}

- [在 Agent 之间共享 Tool](/docs/agents/using-tools#share-tools-across-agents)
- [Mastra 类](/reference/core/mastra-class)
- [Mastra.getTool()](/reference/core/getTool)
- [Mastra.listTools()](/reference/core/listTools)
