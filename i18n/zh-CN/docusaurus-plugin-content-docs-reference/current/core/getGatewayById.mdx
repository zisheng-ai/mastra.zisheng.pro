---
title: "参考： Mastra.getGatewayById() | Core"
description: "按唯一 ID 获取已注册的 gateway"
packages:
  - "@mastra/core"
---

# Mastra.getGatewayById()

按唯一 ID（gateway 实例的 `id` 属性）获取 gateway。需要按固有标识符而非注册键查找 gateway 时，此方法非常有用。

## 使用示例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({
  gateways: {
    customKey: new MyCustomGateway(), // id = 'my-custom-gateway'
  },
})

// Retrieve by ID (not registration key)
const gateway = mastra.getGatewayById('my-custom-gateway')
console.log(gateway.name) // 'My Custom Gateway'
```

## 使用场景

- Gateway 版本控制：不同版本使用不同的唯一 ID (`'gateway-v1'`, `'gateway-v2'`)
- 在注册键未知时查找 gateway
- 跨不同 Mastra 实例识别 gateway

## 参数

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'gateway 的唯一 ID（`gateway.id` 属性）',
  },
]}
/>

## 返回值

<PropertiesTable
  content={[
  {
    name: 'gateway',
    type: 'MastraModelGateway | undefined',
    description: 'gateway 实例；如果未找到，则为 undefined',
  },
]}
/>

## 相关内容

- [Mastra.getGateway()](/reference/core/getGateway): 按注册键获取 gateway
- [Mastra.listGateways()](/reference/core/listGateways): 列出所有 gateway
- [Mastra.addGateway()](/reference/core/addGateway): 添加 gateway
- [MastraModelGateway](/reference/core/mastra-model-gateway): Gateway 基类
- [自定义 Gateway 指南](/models/gateways/custom-gateways): 创建自定义 gateway
