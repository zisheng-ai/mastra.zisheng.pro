---
title: "参考： Mastra.listTools() | Core"
description: "关于 `Mastra.listTools()` 方法的文档，该方法返回 Mastra 级 Tool 注册表。"
packages:
  - "@mastra/core"
---

# Mastra.listTools()

`.listTools()` 方法返回 Mastra 实例上配置的 Tool。返回记录以 Mastra 注册键作为键，以 Tool 实例作为值。

## 使用示例

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core/mastra'
import { weatherTool } from './tools/weather-tool'

export const mastra = new Mastra({
  tools: {
    weather: weatherTool,
  },
})

const tools = mastra.listTools()
const weather = tools?.weather
```

## 参数

此方法不接受任何参数。

## 返回值

<PropertiesTable
  content={[
  {
    name: 'tools',
    type: 'TTools | undefined',
    description:
      'Mastra 级 Tool 注册表，其中每个键都是注册键，每个值都是 Tool 实例。没有可用注册表时返回 `undefined`。',
  },
]}
/>

此方法列出 Mastra 级注册表，不包括仅存在于函数式 Agent `tools` 回调中的 Tool。

## 相关内容

- [在 Agent 之间共享 Tool](/docs/agents/using-tools#share-tools-across-agents)
- [Mastra 类](/reference/core/mastra-class)
- [Mastra.getTool()](/reference/core/getTool)
- [Mastra.getToolById()](/reference/core/getToolById)
