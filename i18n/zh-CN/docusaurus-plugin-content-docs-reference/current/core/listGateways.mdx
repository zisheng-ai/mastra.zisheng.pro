---
title: "参考： Mastra.listGateways() | Core"
description: "列出所有已注册的 gateway"
packages:
  - "@mastra/core"
---

# Mastra.listGateways()

返回以注册键为索引的所有已注册 gateway 的记录。

## 使用示例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({
  gateways: {
    myGateway: new MyCustomGateway(),
    anotherGateway: new AnotherGateway(),
  },
})

const gateways = mastra.listGateways()
console.log(Object.keys(gateways)) // ['myGateway', 'anotherGateway']

// Iterate over all gateways
for (const [key, gateway] of Object.entries(gateways)) {
  console.log(`${key}: ${gateway.name}`)
}
```

## 参数 {/* #parameters */}

无。

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'gateways',
    type: 'Record<string, MastraModelGateway>',
    description: '以注册键为索引的 gateway 实例记录',
  },
]}
/>

## 相关内容 {/* #related */}

- [Mastra.getGateway()](/reference/core/getGateway): 按注册键获取 gateway
- [Mastra.getGatewayById()](/reference/core/getGatewayById): 按 ID 获取 gateway
- [Mastra.addGateway()](/reference/core/addGateway): 添加 gateway
- [MastraModelGateway](/reference/core/mastra-model-gateway): Gateway 基类
- [自定义 Gateway 指南](/models/gateways/custom-gateways): 创建自定义 gateway
