---
title: "参考： Mastra.getAgentById() | Core"
description: "关于 `Mastra.getAgentById()` 方法的文档，该方法按 ID 获取 Agent。"
packages:
  - "@mastra/core"
---

# Mastra.getAgentById()

`.getAgentById()` 方法按 `id` 获取已注册的 Agent。它首先按 `agent.id` 搜索已注册的 Agent。如果没有匹配项，则回退到 [`Mastra.getAgent()`](/reference/core/getAgent)，并将提供的值视为 Agent 注册键。

使用 `mastra.getAgentById(id)` 获取代码中定义的 Agent。使用 `await mastra.getAgentById(id, version)` 获取版本化 Agent。

## 使用示例 {/* #usage-example */}

以下示例注册一个 Agent，按 ID 获取它，然后使用返回的 Agent。

```typescript title="src/mastra/index.ts"
import { Agent } from '@mastra/core/agent'
import { Mastra } from '@mastra/core/mastra'

const supportAgent = new Agent({
  id: 'support-agent-id',
  name: 'Support Agent',
  instructions: 'Answer support questions clearly and concisely.',
  model: '__GATEWAY_OPENAI_MODEL__',
})

export const mastra = new Mastra({
  agents: {
    supportAgent,
  },
})

const agent = mastra.getAgentById('support-agent-id')

const response = await agent.generate('How can I reset my password?')
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description:
      '要获取的 Agent ID。Mastra 首先按 `agent.id` 搜索已注册的 Agent。如果没有匹配项，则将此值用作 Agent 注册键并调用 `getAgent()`。',
  },
  {
    name: 'version',
    type: "{ versionId: string } | { status?: 'draft' | 'published' }",
    description: '用于获取版本化 Agent 的可选版本选择器。提供该参数时，此方法返回 Promise。',
    isOptional: true,
    properties: [
      {
        type: 'VersionSelector',
        parameters: [
          {
            name: 'versionId',
            type: 'string',
            description: '要获取的特定 Agent 版本的 ID。',
          },
          {
            name: 'status',
            type: "'draft' | 'published'",
            description: '选择具有此发布状态的最新 Agent 版本。',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'TAgents[TAgentName]',
    description: '省略 `version` 时，返回具有指定 ID 的 Agent 实例。',
  },
  {
    name: 'agent',
    type: 'Promise<TAgents[TAgentName]>',
    description: '提供 `version` 时，返回解析为版本化 Agent 实例的 Promise。',
  },
]}
/>

按 ID 或注册键找不到 Agent 时，会抛出 `MastraError`。

## 相关内容 {/* #related */}

- [Mastra.getAgent()](/reference/core/getAgent)
- [Agent 概览](/docs/agents/overview)
