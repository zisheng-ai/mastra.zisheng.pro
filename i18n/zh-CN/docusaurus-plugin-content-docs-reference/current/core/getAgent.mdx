---
title: "参考： Mastra.getAgent() | Core"
description: "关于 `Agent.getAgent()` 方法的文档，该方法按名称获取 Agent。"
packages:
  - "@mastra/core"
---

# Mastra.getAgent()

`.getAgent()` 方法用于获取 Agent。此方法接受一个 `string` 参数，用于指定 Agent 名称。

## 使用示例

```typescript
mastra.getAgent('testAgent')
```

## 参数

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'TAgentName extends keyof TAgents',
    description: '要获取的 Agent 名称。必须是 Mastra 配置中存在的有效 Agent 名称。',
  },
]}
/>

## 返回值

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'TAgents[TAgentName]',
    description: '具有指定名称的 Agent 实例。如果未找到该 Agent，则抛出错误。',
  },
]}
/>

## 相关内容

- [Agent 概览](/docs/agents/overview)
