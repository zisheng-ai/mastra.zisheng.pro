---
title: "参考： Mastra.getGateway() | Core"
description: "按注册键获取已注册的 gateway"
packages:
  - "@mastra/core"
---

# Mastra.getGateway()

按注册键（将 gateway 添加到 Mastra 时使用的键）获取 gateway。

## 使用示例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({
  gateways: {
    myGateway: new MyCustomGateway(),
  },
})

const gateway = mastra.getGateway('myGateway')
console.log(gateway.name) // 'My Custom Gateway'
```

## 参数

<PropertiesTable
  content={[
  {
    name: 'key',
    type: 'string',
    description: 'gateway 的注册键',
  },
]}
/>

## 返回值

<PropertiesTable
  content={[
  {
    name: 'gateway',
    type: 'MastraModelGateway',
    description: '该 gateway 实例',
  },
]}
/>

## 抛出错误

如果找不到具有指定键的 gateway，则抛出错误。

## 相关内容

- [Mastra.getGatewayById()](/reference/core/getGatewayById): 按 ID 获取 gateway
- [Mastra.listGateways()](/reference/core/listGateways): 列出所有 gateway
- [Mastra.addGateway()](/reference/core/addGateway): 添加 gateway
- [MastraModelGateway](/reference/core/mastra-model-gateway): Gateway 基类
- [自定义 Gateway 指南](/models/gateways/custom-gateways): 创建自定义 gateway
