---
title: "参考： Mastra.getTool() | Core"
description: "关于 `Mastra.getTool()` 方法的文档，该方法按 Mastra 注册键获取 Tool。"
packages:
  - "@mastra/core"
---

# Mastra.getTool()

`.getTool()` 方法按注册键从 Mastra 级注册表获取 Tool。

## 使用示例 {/* #usage-example */}

使用 `weather` 键注册 Tool，然后使用该键获取它。

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core/mastra'
import { weatherTool } from './tools/weather-tool'

export const mastra = new Mastra({
  tools: {
    weather: weatherTool,
  },
})

const tool = mastra.getTool('weather')
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'TToolName extends keyof TTools',
    description: 'Tool 在 Mastra `tools` 注册表中的注册键。',
  },
]}
/>

## 相关内容 {/* #related */}

- [在 Agent 之间共享 Tool](/docs/agents/using-tools#share-tools-across-agents)
- [Mastra 类](/reference/core/mastra-class)
- [Mastra.getToolById()](/reference/core/getToolById)
- [Mastra.listTools()](/reference/core/listTools)
