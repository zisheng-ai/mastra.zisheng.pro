---
title: "参考： Mastra.listMCPServers() | Core"
description: "关于 `Mastra.listMCPServers()` 方法的文档，该方法获取所有已注册的 MCP server 实例。"
packages:
  - "@mastra/core"
---

# Mastra.listMCPServers()

`.listMCPServers()` 方法用于获取 Mastra 实例中注册的所有 MCP server 实例。返回记录以注册键（`mcpServers` 配置中的键）作为键。

## 使用示例

```typescript
// Register MCP servers
const server1 = new MCPServer({
  id: 'server-one',
  name: 'Server One',
  version: '1.0.0',
  tools: {/* ... */},
})

const server2 = new MCPServer({
  id: 'server-two',
  name: 'Server Two',
  version: '1.0.0',
  tools: {/* ... */},
})

export const mastra = new Mastra({
  mcpServers: {
    firstServer: server1, // Registry key: 'firstServer'
    secondServer: server2, // Registry key: 'secondServer'
  },
})

// List all registered servers
const servers = mastra.listMCPServers()
// Returns: { firstServer: MCPServerBase, secondServer: MCPServerBase }
```

## 参数

此方法不接受任何参数。

## 返回值

<PropertiesTable
  content={[
  {
    name: 'servers',
    type: 'Record<string, MCPServerBase> | undefined',
    description:
      '包含所有已注册 MCP server 实例的记录，其中键为注册键（来自 `mcpServers` 配置），值为 MCPServerBase 实例；如果未注册 server，则为 undefined。',
  },
]}
/>

## 相关方法

- [Mastra.getMCPServer()](/reference/core/getMCPServer): 按注册键获取 MCP server
- [Mastra.getMCPServerById()](/reference/core/getMCPServerById): 按固有 `id` 属性获取 MCP server

## 另请参阅

- [MCP 概览](/docs/mcp/overview)
- [MCP server 参考](/reference/tools/mcp-server)
