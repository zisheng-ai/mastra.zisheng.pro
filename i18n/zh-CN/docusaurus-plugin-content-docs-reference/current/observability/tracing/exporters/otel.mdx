---
title: "参考：OtelExporter | 可观测性"
description: 用于 Trace 和日志的 OpenTelemetry Exporter
packages:
  - "@mastra/otel-exporter"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# OtelExporter

将 Trace 和日志发送到任何兼容 OpenTelemetry 的可观测性平台。Trace 使用标准化的 GenAI 语义约定；日志会保留其原始严重级别和消息正文，并通过 OTEL 日志记录的原生 Trace 上下文以及 `mastra.traceId` / `mastra.spanId` 属性与 Trace 关联。

## 构造函数

```typescript prettier:false
new OtelExporter(config: OtelExporterConfig)
```

## `OtelExporterConfig`

```typescript
interface OtelExporterConfig {
  provider?: ProviderConfig
  signals?: {
    traces?: boolean
    logs?: boolean
  }
  timeout?: number
  batchSize?: number
  logLevel?: 'debug' | 'info' | 'warn' | 'error'
}
```

<PropertiesTable
  props={[
  {
    name: 'provider',
    type: 'ProviderConfig',
    description: 'Provider 特定的配置（见下文）',
    required: true,
  },
  {
    name: 'signals',
    type: '{ traces?: boolean; logs?: boolean }',
    description:
      '按信号切换。两者默认均为 true。将某个值设为 false 可禁用对应信号。导出日志还要求安装与所选协议匹配的 `@opentelemetry/exporter-logs-otlp-*` 包。',
    required: false,
  },
  {
    name: 'timeout',
    type: 'number',
    description: '导出超时时间，以毫秒为单位（默认值：30000）',
    required: false,
  },
  {
    name: 'batchSize',
    type: 'number',
    description: '每批的 Span/日志数量（默认值：512）',
    required: false,
  },
  {
    name: 'logLevel',
    type: "'debug' | 'info' | 'warn' | 'error'",
    description: "Logger 级别（默认值：'warn'）",
    required: false,
  },
  {
    name: 'resourceAttributes',
    type: 'DetectedResourceAttributes',
    description: '可选的 OpenTelemetry Resource Attributes（此处的值会覆盖所有默认值）',
    required: false,
  },
]}
/>

## Provider 配置

### `Dash0Config`

```typescript
interface Dash0Config {
  apiKey?: string
  endpoint?: string
  dataset?: string
}
```

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Dash0 API key。未设置时使用 DASH0_API_KEY 环境变量。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: 'Dash0 ingress endpoint。未设置时使用 DASH0_ENDPOINT 环境变量。',
    required: false,
  },
  {
    name: 'dataset',
    type: 'string',
    description: '数据集名称。未设置时使用 DASH0_DATASET 环境变量。',
    required: false,
  },
]}
/>

### `SignozConfig`

```typescript
interface SignozConfig {
  apiKey?: string
  region?: 'us' | 'eu' | 'in'
  endpoint?: string
}
```

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'SigNoz ingestion key。未设置时使用 SIGNOZ_API_KEY 环境变量。',
    required: false,
  },
  {
    name: 'region',
    type: "'us' | 'eu' | 'in'",
    description: "SigNoz cloud region。未设置时使用 SIGNOZ_REGION 环境变量。（默认值：'us'）",
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: '自定义 endpoint。未设置时使用 SIGNOZ_ENDPOINT 环境变量。',
    required: false,
  },
]}
/>

### `NewRelicConfig`

```typescript
interface NewRelicConfig {
  apiKey?: string
  endpoint?: string
}
```

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'New Relic license key。未设置时使用 NEW_RELIC_LICENSE_KEY 环境变量。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description:
      '自定义 endpoint。未设置时使用 NEW_RELIC_ENDPOINT 环境变量。（默认值：https://otlp.nr-data.net:443/v1/traces）',
    required: false,
  },
]}
/>

### `TraceloopConfig`

```typescript
interface TraceloopConfig {
  apiKey?: string
  destinationId?: string
  endpoint?: string
}
```

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Traceloop API key。未设置时使用 TRACELOOP_API_KEY 环境变量。',
    required: false,
  },
  {
    name: 'destinationId',
    type: 'string',
    description: '目标标识符。未设置时使用 TRACELOOP_DESTINATION_ID 环境变量。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description:
      '自定义 endpoint。未设置时使用 TRACELOOP_ENDPOINT 环境变量。（默认值：https://api.traceloop.com/v1/traces）',
    required: false,
  },
]}
/>

### `LaminarConfig`

```typescript
interface LaminarConfig {
  apiKey?: string
  endpoint?: string
}
```

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Laminar 项目 API key。未设置时使用 LMNR_PROJECT_API_KEY 环境变量。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: '自定义 endpoint。未设置时使用 LAMINAR_ENDPOINT 环境变量。（默认值：https://api.lmnr.ai/v1/traces）',
    required: false,
  },
]}
/>

### `CustomConfig`

```typescript
interface CustomConfig {
  endpoint: string
  protocol?: 'http/json' | 'http/protobuf' | 'grpc' | 'zipkin'
  headers?: Record<string, string>
}
```

<PropertiesTable
  props={[
  {
    name: 'endpoint',
    type: 'string',
    description: 'OTEL collector endpoint URL',
    required: true,
  },
  {
    name: 'protocol',
    type: "'http/json' | 'http/protobuf' | 'grpc' | 'zipkin'",
    description: "导出协议（默认值：'http/json'）",
    required: false,
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: '用于身份验证的自定义 header',
    required: false,
  },
]}
/>

## 方法

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

将 Tracing 事件导出到所配置的 OTEL 后端。

### flush

```typescript prettier:false
async flush(): Promise<void>
```

强制将缓冲区中的所有 Span 刷新到 OTEL 后端，而不关闭 Exporter。适用于需要确保运行时终止前已导出 Span 的 serverless 环境。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

刷新待处理的 Trace 并关闭 Exporter。

## 用法示例

### 零配置（使用环境变量）

```typescript
import { OtelExporter } from '@mastra/otel-exporter'

// Set SIGNOZ_API_KEY, SIGNOZ_REGION environment variables
const exporter = new OtelExporter({ provider: { signoz: {} } })

// Or for other providers:
// Set DASH0_API_KEY, DASH0_ENDPOINT for Dash0
// Set NEW_RELIC_LICENSE_KEY for New Relic
// Set TRACELOOP_API_KEY for Traceloop
// Set LMNR_PROJECT_API_KEY for Laminar
```

### 显式配置

```typescript
import { OtelExporter } from '@mastra/otel-exporter'

const exporter = new OtelExporter({
  provider: {
    signoz: {
      apiKey: process.env.SIGNOZ_API_KEY,
      region: 'us',
    },
  },
})
```

### 使用自定义 Endpoint

```typescript
const exporter = new OtelExporter({
  provider: {
    custom: {
      endpoint: 'https://my-collector.example.com/v1/traces',
      protocol: 'http/protobuf',
      headers: {
        'x-api-key': process.env.API_KEY,
      },
    },
  },
  timeout: 60000,
  logLevel: 'debug',
})
```

## 协议要求

不同 Provider 需要不同的 OTEL Exporter 包。Trace 和日志导出彼此独立：如果只需要 Trace，请仅安装 Trace 包；如果还需要导出日志，请同时安装所选协议对应的 Trace 包和日志包。Zipkin 不支持 OTLP 日志。

| 协议 | Trace 包 | 日志包 |
| - | - | - |
| gRPC | `@opentelemetry/exporter-trace-otlp-grpc` (+ `@grpc/grpc-js`) | `@opentelemetry/exporter-logs-otlp-grpc` (+ `@grpc/grpc-js`) |
| HTTP/Protobuf | `@opentelemetry/exporter-trace-otlp-proto` | `@opentelemetry/exporter-logs-otlp-proto` |
| HTTP/JSON | `@opentelemetry/exporter-trace-otlp-http` | `@opentelemetry/exporter-logs-otlp-http` |
| Zipkin | `@opentelemetry/exporter-zipkin` | 不支持 |

## 标签支持

OtelExporter 支持使用 Trace 标签进行分类和筛选。标签仅应用于根 Span，并存储为 `mastra.tags` 属性。

### 用法

```typescript
const result = await agent.generate('Hello', {
  tracingOptions: {
    tags: ['production', 'experiment-v2', 'user-request'],
  },
})
```

### 标签的存储方式

标签以 JSON 字符串化数组的形式存储在 `mastra.tags` Span 属性中，以最大程度兼容后端：

```json
{
  "mastra.tags": "[\"production\",\"experiment-v2\",\"user-request\"]"
}
```

:::note
尽管 OpenTelemetry 规范支持原生数组属性，但许多后端（Jaeger、Zipkin、Tempo）对数组的支持有限。JSON 字符串可确保在所有可观测性平台上保持一致的行为。
:::

## 相关内容

- [OtelExporter 指南](/docs/observability/integrations/exporters/otel)：包含 Provider 配置的设置指南
- [OtelBridge](/docs/observability/integrations/bridges/otel)：用于双向 OTEL 上下文集成
- [Tracing 概述](/docs/observability/tracing/overview)：常规 Tracing 概念
