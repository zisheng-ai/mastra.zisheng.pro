---
title: "参考：BraintrustExporter | 可观测性"
description: 用于 Tracing 的 Braintrust Exporter
packages:
  - "@mastra/braintrust"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# BraintrustExporter

将 Tracing 数据发送到 Braintrust，用于评估和可观测性。

## 构造函数

```typescript prettier:false
new BraintrustExporter(config: BraintrustExporterConfig)
```

## `BraintrustExporterConfig`

```typescript
interface BraintrustExporterConfig extends BaseExporterConfig {
  apiKey?: string
  endpoint?: string
  projectName?: string
  braintrustLogger?: Logger<true>
  currentSpan?: () => Span | undefined
  tuningParameters?: Record<string, any>
}
```

扩展 `BaseExporterConfig`，其中包含：

- `logger?: IMastraLogger` - Logger 实例
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - 日志级别（默认值：INFO）

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Braintrust API key。未设置时使用 BRAINTRUST_API_KEY 环境变量。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: '自定义 Braintrust endpoint。未设置时使用 BRAINTRUST_ENDPOINT 环境变量。',
    required: false,
  },
  {
    name: 'projectName',
    type: 'string',
    description: "项目名称（默认值：'mastra-tracing'）",
    required: false,
  },
  {
    name: 'braintrustLogger',
    type: 'Logger<true>',
    description: '用于集成评估和已追踪 Span 上下文的 Braintrust logger 实例。',
    required: false,
  },
  {
    name: 'currentSpan',
    type: '() => Span | undefined',
    description:
      '来自应用 Braintrust 软件包实例的 resolver。当 Eval() 或 logger.traced() Span 由另一个已安装的 Braintrust SDK 副本创建时，请将其与 braintrustLogger 一起使用。',
    required: false,
  },
  {
    name: 'logLevel',
    type: "'debug' | 'info' | 'warn' | 'error'",
    description: "logger 级别（默认值：'warn'）",
    required: false,
  },
  {
    name: 'tuningParameters',
    type: 'Record<string, any>',
    description: 'Braintrust 的调优参数',
    required: false,
  },
]}
/>

## 方法

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

将 Tracing 事件导出到 Braintrust。

### export

```typescript prettier:false
async export(spans: ReadOnlySpan[]): Promise<void>
```

将 Span 批量导出到 Braintrust。

### flush

```typescript prettier:false
async flush(): Promise<void>
```

强制将缓冲区中的所有 Span 刷新到 Braintrust，而不关闭 Exporter。适用于需要确保运行时终止前已导出 Span 的 serverless 环境。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

刷新待处理数据并关闭客户端。

## 用法

### 零配置（使用环境变量）

```typescript
import { BraintrustExporter } from '@mastra/braintrust'

// Reads from BRAINTRUST_API_KEY, BRAINTRUST_ENDPOINT
const exporter = new BraintrustExporter()
```

### 显式配置

```typescript
import { BraintrustExporter } from '@mastra/braintrust'

const exporter = new BraintrustExporter({
  apiKey: process.env.BRAINTRUST_API_KEY,
  projectName: 'my-ai-project',
})
```

## Span 类型映射

| Span 类型 | Braintrust 类型 |
| - | - |
| `MODEL_GENERATION` | `llm` |
| `MODEL_CHUNK` | `llm` |
| `TOOL_CALL` | `tool` |
| `MCP_TOOL_CALL` | `tool` |
| `PROVIDER_TOOL_CALL` | `tool` |
| `WORKFLOW_CONDITIONAL_EVAL` | `function` |
| `WORKFLOW_WAIT_EVENT` | `function` |
| 所有其他类型 | `task` |
