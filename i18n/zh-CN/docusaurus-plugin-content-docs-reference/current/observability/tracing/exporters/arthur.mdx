---
title: "参考：ArthurExporter | 可观测性"
description: 使用 OpenInference 进行 Tracing 的 Arthur AI Exporter
packages:
  - "@mastra/arthur"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# ArthurExporter

使用 OpenTelemetry 和 OpenInference 语义约定将 Tracing 数据发送到 [Arthur Engine](https://github.com/arthur-ai/arthur-engine)。

## 构造函数

```typescript prettier:false
new ArthurExporter(config: ArthurExporterConfig)
```

## `ArthurExporterConfig`

```typescript
type ArthurExporterConfig = Omit<OtelExporterConfig, 'provider' | 'exporter'> & {
  apiKey?: string
  endpoint?: string
  taskId?: string
  headers?: Record<string, string>
}
```

继承自 `OtelExporterConfig`（不包括 `provider` 和 `exporter`），其中包含：

- `timeout?: number` - 导出超时时间（毫秒，默认值：30000）
- `batchSize?: number` - 每批 Span 的数量（默认值：512）
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - 日志级别（默认值：WARN）
- `resourceAttributes?: Record<string, any>` - 自定义资源属性

### 元数据透传

非保留的 Span 属性会序列化到 OpenInference `metadata` payload 中。可通过 `tracingOptions.metadata` 添加这些属性（例如 `companyId`、`tier`）。`input`、`output`、`sessionId`、thread/user ID 和 OpenInference ID 等保留字段会自动排除。

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: '用于身份验证的 Arthur API key。未设置时使用 ARTHUR_API_KEY 环境变量。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: 'Arthur Engine endpoint（例如 http://localhost:3030）。未设置时使用 ARTHUR_BASE_URL 环境变量。',
    required: false,
  },
  {
    name: 'taskId',
    type: 'string',
    description:
      '按 ID 将 Trace 路由到已有的 Arthur task。未设置时使用 ARTHUR_TASK_ID 环境变量。如果省略，Arthur Engine 会改为按 serviceName 路由 Trace。',
    required: false,
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: 'OTLP 请求的额外 header。提供 apiKey 时，Authorization header 由内部管理。',
    required: false,
  },
  {
    name: 'timeout',
    type: 'number',
    description: '导出 Span 前的超时时间（毫秒，默认值：30000）',
    required: false,
  },
  {
    name: 'batchSize',
    type: 'number',
    description: '导出前每批包含的 Span 数量（默认值：512）',
    required: false,
  },
  {
    name: 'logLevel',
    type: "'debug' | 'info' | 'warn' | 'error'",
    description: "logger 级别（默认值：'warn'）",
    required: false,
  },
  {
    name: 'resourceAttributes',
    type: 'Record<string, any>',
    description: '添加到每个 Span 的自定义资源属性',
    required: false,
  },
]}
/>

## 方法

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

将 Tracing 事件导出到配置的 endpoint。

### export

```typescript prettier:false
async export(spans: ReadOnlySpan[]): Promise<void>
```

使用 OpenTelemetry 和 OpenInference 语义约定批量导出 Span。

### flush

```typescript prettier:false
async flush(): Promise<void>
```

强制将缓冲区中的所有 Span 刷新到配置的 endpoint，而不关闭 Exporter。适用于需要确保运行时终止前已导出 Span 的 serverless 环境。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

刷新待处理数据并关闭客户端。

## 用法

### 零配置（使用环境变量）

```typescript
import { ArthurExporter } from '@mastra/arthur'

// Set ARTHUR_API_KEY and ARTHUR_BASE_URL (optionally ARTHUR_TASK_ID)
const exporter = new ArthurExporter()
```

### 显式配置

```typescript
import { ArthurExporter } from '@mastra/arthur'

const exporter = new ArthurExporter({
  apiKey: process.env.ARTHUR_API_KEY!,
  endpoint: 'http://localhost:3030',
  taskId: process.env.ARTHUR_TASK_ID, // Optional
})
```

## `OpenInference` 语义约定

ArthurExporter 为生成式 AI 应用实现了 [OpenInference 语义约定](https://github.com/Arize-ai/openinference/tree/main/spec)，可在不同可观测性平台之间提供标准化的 Trace 结构。

## Tag 支持

ArthurExporter 支持为 Trace 添加 tag，以便分类和筛选。Tag 仅应用于根 Span，并映射到原生 OpenInference `tag.tags` 语义约定。

### 用法

```typescript
const result = await agent.generate('Hello', {
  tracingOptions: {
    tags: ['production', 'experiment-v2', 'user-request'],
  },
})
```

### Tag 的存储方式

Tag 使用 OpenInference `tag.tags` 属性存储：

```json
{
  "tag.tags": ["production", "experiment-v2", "user-request"]
}
```

## 相关内容

- [ArthurExporter 文档](/docs/observability/integrations/exporters/arthur)
- [Arthur Engine 文档](https://docs.arthur.ai/)
- [Arthur Engine 仓库](https://github.com/arthur-ai/arthur-engine)
