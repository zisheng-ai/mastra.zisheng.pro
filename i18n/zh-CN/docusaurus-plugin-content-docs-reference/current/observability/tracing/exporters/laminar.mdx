---
title: "参考：LaminarExporter | 可观测性"
description: 用于 Tracing 的 Laminar Exporter
packages:
  - "@mastra/laminar"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# LaminarExporter

通过 OTLP/HTTP (protobuf) 将 Tracing 数据发送到 Laminar。

## 构造函数

```typescript prettier:false
new LaminarExporter(config?: LaminarExporterConfig)
```

## `LaminarExporterConfig`

```typescript
interface LaminarExporterConfig extends BaseExporterConfig {
  apiKey?: string
  baseUrl?: string
  endpoint?: string
  headers?: Record<string, string>
  realtime?: boolean
  disableBatch?: boolean
  batchSize?: number
  timeoutMillis?: number
}
```

扩展 `BaseExporterConfig`，其中包含：

- `logger?: IMastraLogger` - Logger 实例
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - 日志级别（默认值：INFO）

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Laminar 项目 API key。未设置时使用 LMNR_PROJECT_API_KEY 环境变量。',
    required: false,
  },
  {
    name: 'baseUrl',
    type: 'string',
    description:
      'Laminar base URL（用于导出 Trace 和评分）。未设置时使用 LMNR_BASE_URL 环境变量（默认值：https://api.lmnr.ai）。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description:
      'OTLP/HTTP Trace endpoint（以 /v1/traces 结尾）。未设置时使用 LAMINAR_ENDPOINT 环境变量（默认值：`${baseUrl}/v1/traces`）。',
    required: false,
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: 'OTLP 请求中包含的额外 header（与 Authorization 合并）。',
    required: false,
  },
  {
    name: 'realtime',
    type: 'boolean',
    description: '在每个 Span 后刷新，以便立即查看（默认值：false）。',
    required: false,
  },
  {
    name: 'disableBatch',
    type: 'boolean',
    description: '禁用批处理（SimpleSpanProcessor）。默认值：false。',
    required: false,
  },
  {
    name: 'batchSize',
    type: 'number',
    description: '每批最多包含的 Span 数量（BatchSpanProcessor）。默认值：512。',
    required: false,
  },
  {
    name: 'timeoutMillis',
    type: 'number',
    description: 'OTLP 导出超时时间（毫秒）。默认值：30000。',
    required: false,
  },
]}
/>

## 方法

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

将 Tracing 事件导出到 Laminar。

### flush

```typescript prettier:false
async flush(): Promise<void>
```

强制将缓冲区中的所有 Span 刷新到 Laminar，而不关闭 Exporter。适用于需要确保运行时终止前已导出 Span 的 serverless 环境。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

刷新待处理数据并关闭 Exporter。

## 用法

### 零配置（使用环境变量）

```typescript
import { LaminarExporter } from '@mastra/laminar'

// Reads from LMNR_PROJECT_API_KEY, LMNR_BASE_URL, LAMINAR_ENDPOINT
const exporter = new LaminarExporter()
```

### 显式配置

```typescript
import { LaminarExporter } from '@mastra/laminar'

const exporter = new LaminarExporter({
  apiKey: process.env.LMNR_PROJECT_API_KEY,
  baseUrl: 'https://api.lmnr.ai',
  realtime: true,
})
```
