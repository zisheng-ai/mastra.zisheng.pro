---
title: "参考：Tracing | Observability"
description: Tracing 核心类和方法
packages:
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# Observability 实例

## `DefaultObservabilityInstance` {/* #defaultobservabilityinstance */}

ObservabilityInstance 接口的默认实现。

### 构造函数 {/* #constructor */}

```typescript prettier:false
new DefaultObservabilityInstance(config: ObservabilityInstanceConfig)
```

使用指定配置创建新的 DefaultObservabilityInstance。

### 属性 {/* #properties */}

继承 BaseObservabilityInstance 的所有属性和方法。

## `BaseObservabilityInstance` {/* #baseobservabilityinstance */}

自定义 ObservabilityInstance 实现的基类。

### 方法 {/* #methods */}

#### `getConfig` {/* #getconfig */}

```typescript prettier:false
getConfig(): Readonly<Required<ObservabilityInstanceConfig>>
```

返回当前的可观测性配置。

#### `getExporters` {/* #getexporters */}

```typescript prettier:false
getExporters(): readonly ObservabilityExporter[]
```

返回所有已配置的 Exporter。

#### `getSpanOutputProcessors` {/* #getspanoutputprocessors */}

```typescript prettier:false
getSpanOutputProcessors(): readonly SpanOutputProcessor[]
```

返回所有已配置的 Span 输出处理器。

#### `getLogger` {/* #getlogger */}

```typescript prettier:false
getLogger(): IMastraLogger
```

返回供 Exporter 和其他组件使用的日志记录器实例。

#### `startSpan` {/* #startspan */}

```typescript prettier:false
startSpan<TType extends SpanType>(
  options: StartSpanOptions<TType>
): Span<TType>
```

启动指定 SpanType 的新 Span。如果未提供父 Span，则创建 Trace 的根 Span。

<PropertiesTable
  props={[
  {
    name: 'type',
    type: 'SpanType',
    description: '要创建的 Span 类型',
    required: true,
  },
  {
    name: 'name',
    type: 'string',
    description: 'Span 的名称',
    required: true,
  },
  {
    name: 'parent',
    type: 'AnySpan',
    description: '父 Span（非根 Span 时）',
    required: false,
  },
  {
    name: 'attributes',
    type: 'SpanTypeMap[TType]',
    description: '特定于类型的属性',
    required: false,
  },
  {
    name: 'metadata',
    type: 'Record<string, any>',
    description: '用户定义的元数据',
    required: false,
  },
  {
    name: 'input',
    type: 'any',
    description: '初始输入数据',
    required: false,
  },
  {
    name: 'customSamplerOptions',
    type: 'CustomSamplerOptions',
    description: '自定义采样器的选项',
    required: false,
  },
]}
/>

#### shutdown {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

关闭所有 Exporter 和处理器，并清理资源。

## 自定义实现 {/* #custom-implementation */}

要创建自定义 ObservabilityInstance 实现，请扩展 BaseObservabilityInstance：

```typescript
class CustomObservabilityInstance extends BaseObservabilityInstance {
  constructor(config: ObservabilityInstanceConfig) {
    super(config)
    // Custom initialization
  }

  // Override methods as needed
  startSpan<TType extends SpanType>(options: StartSpanOptions<TType>): Span<TType> {
    // Custom span creation logic
    return super.startSpan(options)
  }
}
```

## 另请参阅 {/* #see-also */}

### 文档 {/* #documentation */}

- [Tracing 概览](/docs/observability/tracing/overview)：概念和使用指南
- [配置参考](/reference/observability/tracing/configuration)：配置选项
- [接口参考](/reference/observability/tracing/interfaces)：类型定义
- [Span 参考](/reference/observability/tracing/spans)：Span 生命周期和方法

### Exporter {/* #exporters */}

- [MastraStorageExporter](/reference/observability/tracing/exporters/mastra-storage-exporter)：存储持久化
- [MastraPlatformExporter](/reference/observability/tracing/exporters/mastra-platform-exporter)：Mastra 平台集成
- [ConsoleExporter](/reference/observability/tracing/exporters/console-exporter)：调试输出
