---
title: "参考：Workflow.commit() | Workflows"
description: workflows 中 `Workflow.commit()` 方法的文档，该方法完成 workflow 并返回最终结果。
packages:
  - "@mastra/core"
---

# Workflow\.commit()

`.commit()` 方法完成 workflow 并返回最终结果。

## 使用示例

```typescript
workflow.then(step1).commit()
```

## 返回值

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'Workflow',
    description: '用于方法链式调用的 workflow 实例',
  },
]}
/>

## 相关内容

- [控制流](/docs/workflows/control-flow)
