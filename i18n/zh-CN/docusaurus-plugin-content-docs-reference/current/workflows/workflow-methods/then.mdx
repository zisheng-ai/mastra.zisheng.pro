---
title: "参考：Workflow.then() | Workflows"
description: workflows 中 `Workflow.then()` 方法的文档，该方法在步骤之间创建顺序依赖关系。
packages:
  - "@mastra/core"
---

# Workflow\.then()

`.then()` 方法在 workflow 步骤之间创建顺序依赖关系，确保步骤按特定顺序执行。

## 使用示例 {/* #usage-example */}

```typescript
workflow.then(step1).then(step2)
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'step',
    type: 'Step',
    description: '前一个步骤完成后应执行的步骤实例',
    isOptional: false,
  },
]}
/>

## 返回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'NewWorkflow',
    description: '用于方法链式调用的 workflow 实例',
  },
]}
/>

## 相关内容 {/* #related */}

- [控制流](/docs/workflows/control-flow)
