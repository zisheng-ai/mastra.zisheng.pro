---
title: "参考：Workflow 类 | Workflows"
description: Mastra 中 `Workflow` 类的文档，该类用于为包含条件分支和数据验证的复杂操作序列创建状态机。
packages:
  - "@mastra/core"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Workflow 类

`Workflow` 类用于为包含条件分支和数据验证的复杂操作序列创建状态机。

## 使用示例 {/* #usage-example */}

```typescript title="src/mastra/workflows/test-workflow.ts"
import { createWorkflow } from '@mastra/core/workflows'
import { z } from 'zod'

export const workflow = createWorkflow({
  id: 'test-workflow',
  inputSchema: z.object({
    value: z.string(),
  }),
  outputSchema: z.object({
    value: z.string(),
  }),
})
```

## 定义 schema {/* #define-schemas */}

可以使用任何支持 [Standard JSON Schema](https://standardschema.dev/json-schema) 的库定义 workflow 的 `inputSchema` 和 `outputSchema`，包括 [Zod](https://zod.dev/)、[Valibot](https://valibot.dev/) 和 [ArkType](https://arktype.io/) 等库。

<Tabs>
  <TabItem value="zod" label="Zod">
    ```typescript {9,12,15,16} title="src/mastra/workflows/test-workflow.ts" prettier:false
    import { createWorkflow, createStep } from "@mastra/core/workflows";
    import { z } from "zod";

    const step1 = createStep({...});

    export const testWorkflow = createWorkflow({
      id: "test-workflow",
      inputSchema: z.object({
        message: z.string()
      }),
      outputSchema: z.object({
        output: z.string()
      })
    })
      .then(step1)
      .commit();
    ```
  </TabItem>

  <TabItem value="valibot" label="Valibot">
    ```typescript {10,13,16,17} title="src/mastra/workflows/test-workflow.ts" prettier:false
    import { createWorkflow, createStep } from "@mastra/core/workflows";
    import * as v from "valibot";
    import { toStandardJsonSchema } from "@valibot/to-json-schema";

    const step1 = createStep({...});

    export const testWorkflow = createWorkflow({
      id: "test-workflow",
      inputSchema: toStandardJsonSchema(v.object({
        message: v.string()
      })),
      outputSchema: toStandardJsonSchema(v.object({
        output: v.string()
      }))
    })
      .then(step1)
      .commit();
    ```
  </TabItem>

  <TabItem value="arktype" label="ArkType">
    ```typescript {9,12,15,16} title="src/mastra/workflows/test-workflow.ts" prettier:false
    import { createWorkflow, createStep } from "@mastra/core/workflows";
    import { type } from "arktype";

    const step1 = createStep({...});

    export const testWorkflow = createWorkflow({
      id: "test-workflow",
      inputSchema: type({
        message: "string"
      }),
      outputSchema: type({
        output: "string"
      })
    })
      .then(step1)
      .commit();
    ```
  </TabItem>
</Tabs>

## 构造函数参数 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'workflow 的唯一标识符',
  },
  {
    name: 'inputSchema',
    type: 'StandardJSONSchemaV1',
    description: '定义 workflow 输入结构的 Standard JSON Schema',
  },
  {
    name: 'outputSchema',
    type: 'StandardJSONSchemaV1',
    description: '定义 workflow 输出结构的 Standard JSON Schema',
  },
  {
    name: 'stateSchema',
    type: 'StandardJSONSchemaV1',
    description:
      "用于 workflow 状态的可选 Standard JSON Schema。使用 Mastra 的状态系统时会自动注入。若未指定，类型为 'any'。",
    isOptional: true,
  },
  {
    name: 'requestContextSchema',
    type: 'StandardJSONSchemaV1',
    description:
      '用于验证请求上下文值的 Standard JSON Schema。提供后，会在 run.start() 开始时验证上下文；验证失败则抛出错误。',
    isOptional: true,
  },
  {
    name: 'schedule',
    type: 'WorkflowScheduleConfig | WorkflowScheduleConfig[]',
    description:
      'workflow 的可选 cron 调度。接受单个配置或配置数组，以按多个周期触发。设置此项会自动将 workflow 提升为使用事件驱动的执行引擎。用法请参阅计划 workflow 指南。',
    isOptional: true,
    properties: [
      {
        type: 'WorkflowScheduleConfig',
        parameters: [
          {
            name: 'id',
            type: 'string',
            description: '调度的稳定标识符。传入调度数组时必需；传入单个调度对象时默认为 workflow ID。',
            isOptional: true,
          },
          {
            name: 'cron',
            type: 'string',
            description: '由 5、6 或 7 个部分组成的 cron 表达式。在构造 workflow 时验证。',
          },
          {
            name: 'timezone',
            type: 'string',
            description:
              'IANA 时区，例如 "America/New_York"。默认为主机的本地时区。请在生产环境中明确设置此项，避免触发时间依赖服务器 locale。',
            isOptional: true,
          },
          {
            name: 'inputData',
            type: 'TInput',
            description: '每次触发时作为 workflow 输入传递的 payload。',
            isOptional: true,
          },
          {
            name: 'initialState',
            type: 'TState',
            description: 'run 的初始状态。',
            isOptional: true,
          },
          {
            name: 'requestContext',
            type: 'Record<string, unknown>',
            description: '附加到 run 的请求上下文。',
            isOptional: true,
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            description: '与调度记录一同持久化的任意 metadata。',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'options',
    type: 'WorkflowOptions',
    description: 'workflow 的可选配置',
    isOptional: true,
    properties: [
      {
        type: 'WorkflowOptions',
        parameters: [
          {
            name: 'tracingPolicy',
            type: 'TracingPolicy',
            description: 'workflow 的可选 tracing 策略',
            isOptional: true,
          },
          {
            name: 'validateInputs',
            type: 'boolean',
            description:
              '用于确定是否验证 workflow 输入的可选标志。它还会应用 workflow/步骤输入或恢复数据中 zodSchemas 的默认值。如果在 start/resume 时输入或恢复数据验证失败，workflow 不会启动或恢复，而会抛出错误。如果执行步骤时输入数据验证失败，该步骤会失败，导致 workflow 失败并返回错误。',
            isOptional: true,
            defaultValue: 'true',
          },
          {
            name: 'shouldPersistSnapshot',
            type: '(params: { stepResults: Record<string, StepResult<any, any, any, any>>; workflowStatus: WorkflowRunStatus }) => boolean',
            description: '用于确定是否持久化 workflow 快照的可选标志',
            isOptional: true,
            defaultValue: '() => true',
          },
          {
            name: 'pruneSnapshot',
            type: '(params: { snapshot: WorkflowRunState; workflowStatus: WorkflowRunStatus }) => WorkflowRunState',
            description:
              '在持久化 workflow 快照前立即转换快照的可选 hook。必须返回可 JSON 序列化的数据，并保留 workflow 恢复所需的一切内容（已暂停步骤的 suspendPayloads、suspendedPaths、executionPath 等）。agent run 在内部使用它以保持快照精简；用户 workflow 默认持久化完整快照。',
            isOptional: true,
          },
          {
            name: 'onFinish',
            type: '(result: WorkflowFinishCallbackResult) => void | Promise<void>',
            description:
              'workflow 以任意状态（success、failed、suspended、tripwire）完成时调用的回调。接收 workflow 结果，包括状态、输出、错误和步骤结果。此回调中抛出的错误会被捕获并记录，而不会向外传播。',
            isOptional: true,
            properties: [
              {
                type: 'WorkflowFinishCallbackResult',
                parameters: [
                  {
                    name: 'status',
                    type: 'WorkflowRunStatus',
                    description: "workflow 状态：'success'、'failed'、'suspended' 或 'tripwire'",
                  },
                  {
                    name: 'result',
                    type: 'any',
                    description: "workflow 输出（status 为 'success' 时）",
                    isOptional: true,
                  },
                  {
                    name: 'error',
                    type: 'SerializedError',
                    description: "错误详情（status 为 'failed' 时）",
                    isOptional: true,
                  },
                  {
                    name: 'steps',
                    type: 'Record<string, StepResult>',
                    description: '包含状态和输出的各步骤结果',
                  },
                  {
                    name: 'tripwire',
                    type: 'StepTripwireInfo',
                    description: "tripwire 信息（status 为 'tripwire' 时）",
                    isOptional: true,
                  },
                  {
                    name: 'runId',
                    type: 'string',
                    description: '此 workflow run 的唯一标识符',
                  },
                  {
                    name: 'workflowId',
                    type: 'string',
                    description: 'workflow 的标识符',
                  },
                  {
                    name: 'resourceId',
                    type: 'string',
                    description: '可选资源标识符（如果在创建 run 时提供）',
                    isOptional: true,
                  },
                  {
                    name: 'getInitData',
                    type: '() => any',
                    description: '返回传给 workflow 的初始输入数据的函数',
                  },
                  {
                    name: 'mastra',
                    type: 'Mastra',
                    description: 'Mastra 实例（如果 workflow 已在 Mastra 中注册）',
                    isOptional: true,
                  },
                  {
                    name: 'requestContext',
                    type: 'RequestContext',
                    description: '请求范围的上下文数据',
                  },
                  {
                    name: 'logger',
                    type: 'IMastraLogger',
                    description: 'workflow 的 logger 实例',
                  },
                  {
                    name: 'state',
                    type: 'Record<string, any>',
                    description: 'workflow 的当前状态对象',
                  },
                ],
              },
            ],
          },
          {
            name: 'onError',
            type: '(errorInfo: WorkflowErrorCallbackInfo) => void | Promise<void>',
            description:
              '仅在 workflow 失败（failed 或 tripwire 状态）时调用的回调。接收错误详情和步骤结果。此回调中抛出的错误会被捕获并记录，而不会向外传播。',
            isOptional: true,
            properties: [
              {
                type: 'WorkflowErrorCallbackInfo',
                parameters: [
                  {
                    name: 'status',
                    type: "'failed' | 'tripwire'",
                    description: "workflow 状态（'failed' 或 'tripwire'）",
                  },
                  {
                    name: 'error',
                    type: 'SerializedError',
                    description: '错误详情',
                    isOptional: true,
                  },
                  {
                    name: 'steps',
                    type: 'Record<string, StepResult>',
                    description: '包含状态和输出的各步骤结果',
                  },
                  {
                    name: 'tripwire',
                    type: 'StepTripwireInfo',
                    description: "tripwire 信息（status 为 'tripwire' 时）",
                    isOptional: true,
                  },
                  {
                    name: 'runId',
                    type: 'string',
                    description: '此 workflow run 的唯一标识符',
                  },
                  {
                    name: 'workflowId',
                    type: 'string',
                    description: 'workflow 的标识符',
                  },
                  {
                    name: 'resourceId',
                    type: 'string',
                    description: '可选资源标识符（如果在创建 run 时提供）',
                    isOptional: true,
                  },
                  {
                    name: 'getInitData',
                    type: '() => any',
                    description: '返回传给 workflow 的初始输入数据的函数',
                  },
                  {
                    name: 'mastra',
                    type: 'Mastra',
                    description: 'Mastra 实例（如果 workflow 已在 Mastra 中注册）',
                    isOptional: true,
                  },
                  {
                    name: 'requestContext',
                    type: 'RequestContext',
                    description: '请求范围的上下文数据',
                  },
                  {
                    name: 'logger',
                    type: 'IMastraLogger',
                    description: 'workflow 的 logger 实例',
                  },
                  {
                    name: 'state',
                    type: 'Record<string, any>',
                    description: 'workflow 的当前状态对象',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## 使用初始状态运行 {/* #running-with-initial-state */}

启动 workflow run 时，可以传入 `initialState`，为 workflow 状态设置初始值：

```typescript
const run = await workflow.createRun()

const result = await run.start({
  inputData: { value: 'hello' },
  initialState: {
    counter: 0,
    items: [],
  },
})
```

`initialState` 对象应与 workflow 的 `stateSchema` 所定义的结构匹配。更多详情请参阅 [Workflow 状态](/docs/workflows/workflow-state)。

## Workflow 状态 {/* #workflow-status */}

workflow 的 `status` 表示其当前执行状态。可能的值如下：

<PropertiesTable
  content={[
  {
    name: 'success',
    type: 'string',
    description: '所有步骤均已成功执行完毕，并产生有效的结果输出',
  },
  {
    name: 'failed',
    type: 'string',
    description: 'workflow 执行过程中遇到错误，并提供错误详情',
  },
  {
    name: 'suspended',
    type: 'string',
    description: 'workflow 执行已暂停并等待恢复，同时提供已暂停步骤的信息',
  },
  {
    name: 'tripwire',
    type: 'string',
    description:
      'workflow 已被 processor tripwire 终止。当 workflow 中的 agent 步骤触发 tripwire 时（例如内容被 guardrail 阻止），就会发生这种情况。结果中包含 tripwire 信息。',
  },
]}
/>

### 处理 tripwire 状态 {/* #handling-tripwire-status */}

当 workflow 包含触发 tripwire 的 agent 步骤时，workflow 会返回 `status: 'tripwire'` 并包含 tripwire 详情：

```typescript
const run = await workflow.createRun()
const result = await run.start({ inputData: { message: 'Hello' } })

if (result.status === 'tripwire') {
  console.log('Workflow terminated by tripwire:', result.tripwire?.reason)
  console.log('Processor ID:', result.tripwire?.processorId)
  console.log('Retry requested:', result.tripwire?.retry)
}
```

这与表示意外错误的 `status: 'failed'` 不同。tripwire 状态表示 processor 有意停止了执行（例如为了内容审核）。

## 相关内容 {/* #related */}

- [Step 类](./step)
- [Workflow 状态](/docs/workflows/workflow-state)
- [控制流](/docs/workflows/control-flow)
