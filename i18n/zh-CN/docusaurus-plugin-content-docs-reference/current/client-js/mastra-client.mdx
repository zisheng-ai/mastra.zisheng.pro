---
title: "参考：Mastra client SDK | Client SDK"
description: 了解如何使用 client-js SDK 与 Mastra 交互。
packages:
  - "@mastra/client-js"
---

# Mastra client SDK

Mastra Client SDK 提供类型安全的接口，用于从客户端环境与 [Mastra Server](/docs/deployment/mastra-server) 交互。

## 使用示例 {/* #usage-example */}

```typescript title="lib/mastra/mastra-client.ts"
import { MastraClient } from '@mastra/client-js'

export const mastraClient = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})
```

## `RequestContext` {/* #requestcontext */}

在 client SDK 中使用 `RequestContext` 时，请从 `@mastra/client-js` 导入。

```typescript
import { MastraClient, RequestContext } from '@mastra/client-js'

const client = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})

const requestContext = new RequestContext()
requestContext.set('userId', 'user-123')

const agent = client.getAgent('support-agent')

const response = await agent.generate('Summarize this ticket', {
  requestContext,
})
```

你也可以将 `requestContext` 作为 `Record<string, any>` 传入。

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: 'Mastra API 的基础 URL。所有请求都将相对于此 URL 发送。',
    isOptional: false,
  },
  {
    name: 'retries',
    type: 'number',
    description: '请求失败后、抛出错误前的重试次数。',
    isOptional: true,
    defaultValue: '3',
  },
  {
    name: 'backoffMs',
    type: 'number',
    description: '重试失败请求前的初始延迟（以毫秒为单位）。每次重试时，此值都会加倍（指数退避）。',
    isOptional: true,
    defaultValue: '300',
  },
  {
    name: 'maxBackoffMs',
    type: 'number',
    description: '最大退避时间（以毫秒为单位）。防止两次尝试之间等待过久。',
    isOptional: true,
    defaultValue: '5000',
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: '包含自定义 HTTP 标头的对象，这些标头会随每个请求一起发送。',
    isOptional: true,
  },
  {
    name: 'credentials',
    type: '"omit" | "same-origin" | "include"',
    description:
      '请求的凭据模式。更多信息请参阅 https://developer.mozilla.org/en-US/docs/Web/API/Request/credentials。',
    isOptional: true,
  },
]}
/>

## 方法 {/* #methods */}

<PropertiesTable
  content={[
  {
    name: 'listAgents()',
    type: 'Promise<Record<string, GetAgentResponse>>',
    description: '返回所有可用的 Agent 实例。',
  },
  {
    name: 'getAgent(agentId)',
    type: 'Agent',
    description: '通过 ID 检索特定 Agent 实例。',
  },
  {
    name: 'listMemoryThreads(params)',
    type: 'Promise<StorageThreadType[]>',
    description: '检索指定资源和 Agent 的 memory thread。需要 `resourceId` 和 `agentId`。',
  },
  {
    name: 'createMemoryThread(params)',
    type: 'Promise<MemoryThread>',
    description: '使用给定参数创建新的 memory thread。',
  },
  {
    name: 'getMemoryThread({ threadId, agentId })',
    type: 'MemoryThread',
    description: '通过 ID 获取特定 memory thread。',
  },
  {
    name: 'saveMessageToMemory(params)',
    type: 'Promise<{ messages: (MastraMessageV1 | MastraDBMessage)[] }>',
    description: '将一条或多条消息保存到 memory 系统。返回已保存的消息。',
  },
  {
    name: 'getMemoryStatus()',
    type: 'Promise<MemoryStatus>',
    description: '返回 memory 系统的当前状态。',
  },
  {
    name: 'listTools()',
    type: 'Record<string, Tool>',
    description: '返回所有可用的 Tool。',
  },
  {
    name: 'getTool(toolId)',
    type: 'Tool',
    description: '通过 ID 检索特定 Tool 实例。',
  },
  {
    name: 'listWorkflows()',
    type: 'Record<string, Workflow>',
    description: '返回所有可用的 Workflow 实例。',
  },
  {
    name: 'getWorkflow(workflowId)',
    type: 'Workflow',
    description: '通过 ID 检索特定 Workflow 实例。',
  },
  {
    name: 'getAgentBuilderActions()',
    type: 'Promise<Record<string, WorkflowInfo>>',
    description:
      '返回所有可用的 Agent Builder action。请参阅 [Agent Builder API](https://agent-builder.mastra.ai/reference/client)。',
  },
  {
    name: 'getAgentBuilderAction(actionId)',
    type: 'AgentBuilder',
    description:
      '通过 ID 检索 Agent Builder action。请参阅 [Agent Builder API](https://agent-builder.mastra.ai/reference/client)。',
  },
  {
    name: 'responses',
    type: 'Responses',
    description: '提供 OpenAI 风格的 Responses API 辅助方法，包括 `create()`、`retrieve()`、`stream()` 和 `delete()`。',
  },
  {
    name: 'conversations',
    type: 'Conversations',
    description: '提供对话辅助方法，包括 `create()`、`retrieve()`、`delete()` 和 `items.list()`。',
  },
  {
    name: 'getVector(vectorName)',
    type: 'MastraVector',
    description: '按名称返回向量存储实例。',
  },
  {
    name: 'listLogs(params)',
    type: 'Promise<LogEntry[]>',
    description: '获取与所提供筛选条件匹配的系统日志。',
  },
  {
    name: 'getLog(params)',
    type: 'Promise<LogEntry>',
    description: '通过 ID 或筛选条件检索特定日志条目。',
  },
  {
    name: 'listLogTransports()',
    type: 'string[]',
    description: '返回已配置日志 transport 类型的列表。',
  },
  {
    name: 'getTrace(traceId)',
    type: 'Promise<TraceRecord>',
    description: '通过 ID 检索特定 Trace，包括其所有 span 和详细信息。',
  },
  {
    name: 'getTraces(params)',
    type: 'Promise<GetTracesResponse>',
    description: '检索可选筛选的 Trace 根 span 分页列表。使用 getTrace() 获取包含所有 span 的完整 Trace。',
  },
]}
/>
