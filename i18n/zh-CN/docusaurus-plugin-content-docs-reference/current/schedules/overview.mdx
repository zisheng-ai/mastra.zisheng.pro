---
title: "Reference：mastra.schedules | 调度"
description: "mastra.schedules 的 API Reference，用于创建和管理 Agent 与 Workflow 的 Cron 调度。"
packages:
  - "@mastra/core"
---

# `mastra.schedules`

**引入版本：** `@mastra/core@1.50.0`

`mastra.schedules` 是用于持久化 Cron 调度的 CRUD 服务。你可以用它为 Agent 或 Workflow 创建、列出、更新、暂停、恢复、手动运行和删除调度。

有关用法模式和概念，请参阅[调度](/docs/long-running-agents/schedules)。

## 用法示例 {/* #usage-example */}

创建 Agent 调度：

```typescript title="src/mastra/schedules.ts"
const schedule = await mastra.schedules.create({
  agentId: 'pinger',
  cron: '0 * * * *',
  prompt: 'Give me a status update.',
})
```

创建 Workflow 调度：

```typescript title="src/mastra/schedules.ts"
const schedule = await mastra.schedules.create({
  workflowId: 'daily-report',
  cron: '0 9 * * *',
  inputData: { reportType: 'summary' },
})
```

调度需要一个实现 schedules domain 的 Storage adapter。支持的 adapter 包括 `@mastra/libsql`、`@mastra/pg`、`@mastra/mysql`、`@mastra/mongodb`、`@mastra/convex` 和 `@mastra/spanner`。

## 方法 {/* #methods */}

### 创建调度 {/* #create-schedules */}

#### `create(input)` {/* #createinput */}

创建 Agent 或 Workflow 调度。传入 `agentId` 可创建 Agent 调度，传入 `workflowId` 可创建 Workflow 调度。

```typescript
const schedule = await mastra.schedules.create({
  agentId: 'pinger',
  cron: '0 * * * *',
  prompt: 'Give me a status update.',
})
```

##### Agent 调度输入 {/* #agent-schedule-input */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '可选的稳定调度 ID。该值会规范化为 `agent_<slug>`。如果省略，Mastra 会生成一个 `agent_<uuid>` ID。',
    isOptional: true,
  },
  {
    name: 'agentId',
    type: 'string',
    description: '每次触发调度时要运行的 Agent ID。',
  },
  {
    name: 'cron',
    type: 'string',
    description: '调度的 Cron 表达式。接受由 5、6 或 7 个部分组成的 Cron 表达式以及 Croner 昵称。',
  },
  {
    name: 'prompt',
    type: 'string',
    description: '每次触发调度时发送给 Agent 的提示词。',
  },
  {
    name: 'name',
    type: 'string',
    description: '自由格式标签，用于区分同一 Agent 或 thread 上的多个调度。',
    isOptional: true,
  },
  {
    name: 'timezone',
    type: 'string',
    description: '用于确定 Cron 触发时间的 IANA 时区，例如 `America/New_York`。',
    isOptional: true,
  },
  {
    name: 'threadId',
    type: 'string',
    description: '接收调度信号的 thread。如果省略，每次触发都会通过 `agent.generate()` 在不使用 thread 的情况下运行。',
    isOptional: true,
  },
  {
    name: 'resourceId',
    type: 'string',
    description: '使用 thread 的调度所对应的资源 ID。设置 `threadId` 时必填。',
    isOptional: true,
  },
  {
    name: 'signalType',
    type: 'AgentSignalType',
    description: '使用 thread 的调度触发时所用的信号类型。默认为 `notification`。',
    isOptional: true,
  },
  {
    name: 'tagName',
    type: 'string',
    description: '用于渲染调度信号的 XML 标签名称。默认为 `schedule`。',
    isOptional: true,
  },
  {
    name: 'attributes',
    type: 'AgentSignalAttributes',
    description: '渲染到调度信号 XML 标签上的属性。',
    isOptional: true,
  },
  {
    name: 'providerOptions',
    type: 'Record<string, unknown>',
    description: '每次触发时合并到调度信号 payload 中、可安全用于 JSON 的 Provider 选项。',
    isOptional: true,
  },
  {
    name: 'ifActive',
    type: 'ScheduleIfActive',
    description: '目标 thread 正在主动进行流式传输时的行为。需要 `threadId`。',
    isOptional: true,
  },
  {
    name: 'ifIdle',
    type: 'ScheduleIfIdle',
    description: '目标 thread 空闲时的行为。需要 `threadId`。',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: '与调度记录行一同存储的任意 metadata。',
    isOptional: true,
  },
  {
    name: 'status',
    type: "'active' | 'paused'",
    description: '初始生命周期状态。默认为 `active`。',
    isOptional: true,
    defaultValue: "'active'",
  },
]}
/>

##### Workflow 调度输入 {/* #workflow-schedule-input */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description:
      '可选的稳定调度 ID。该值会规范化为 `schedule_<slug>`。如果省略，Mastra 会生成一个 `schedule_<uuid>` ID。',
    isOptional: true,
  },
  {
    name: 'workflowId',
    type: 'string',
    description: '每次触发调度时要启动的 Workflow ID。',
  },
  {
    name: 'cron',
    type: 'string',
    description: '调度的 Cron 表达式。接受由 5、6 或 7 个部分组成的 Cron 表达式以及 Croner 昵称。',
  },
  {
    name: 'timezone',
    type: 'string',
    description: '用于确定 Cron 触发时间的 IANA 时区。',
    isOptional: true,
  },
  {
    name: 'inputData',
    type: 'unknown',
    description: '传递给 Workflow 运行的输入数据。',
    isOptional: true,
  },
  {
    name: 'initialState',
    type: 'unknown',
    description: '调度运行时 Workflow 的初始状态。',
    isOptional: true,
  },
  {
    name: 'requestContext',
    type: 'Record<string, unknown>',
    description: '传递给 Workflow 运行的请求上下文。',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: '与调度记录行一同存储的任意 metadata。',
    isOptional: true,
  },
  {
    name: 'status',
    type: "'active' | 'paused'",
    description: '初始生命周期状态。默认为 `active`。',
    isOptional: true,
    defaultValue: "'active'",
  },
]}
/>

### 读取调度 {/* #read-schedules */}

#### `get(id)` {/* #getid */}

按 ID 获取调度。未带前缀的 Agent 调度 ID 也会解析为规范化的 `agent_<slug>` 形式。

```typescript
const schedule = await mastra.schedules.get('pinger')
```

#### `list(filter?)` {/* #listfilter */}

列出调度。不指定 filter 时，会返回 Agent 和 Workflow 调度。

```typescript
const schedules = await mastra.schedules.list({
  agentId: 'pinger',
  status: 'active',
})
```

<PropertiesTable
  content={[
  {
    name: 'filter',
    type: 'ListSchedulesFilter',
    description: '列表操作的可选 filter。',
    isOptional: true,
    properties: [
      {
        type: 'ListSchedulesFilter',
        parameters: [
          {
            name: 'agentId',
            type: 'string',
            description: '仅返回此 Agent 的 Agent 调度。',
            isOptional: true,
          },
          {
            name: 'workflowId',
            type: 'string',
            description: '仅返回此 Workflow 的 Workflow 调度。',
            isOptional: true,
          },
          {
            name: 'threadId',
            type: 'string',
            description: '仅返回此 thread 的 Agent 调度。',
            isOptional: true,
          },
          {
            name: 'resourceId',
            type: 'string',
            description: '仅返回此资源的 Agent 调度。',
            isOptional: true,
          },
          {
            name: 'name',
            type: 'string',
            description: '仅返回具有此标签的 Agent 调度。',
            isOptional: true,
          },
          {
            name: 'status',
            type: "'active' | 'paused'",
            description: '仅返回具有此状态的调度。',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

### 更新调度 {/* #update-schedules */}

#### `update(id, patch)` {/* #updateid-patch */}

更新调度。更改 `cron` 或 `timezone` 会重新计算下次触发时间。将 `status` 从 `paused` 更新为 `active` 时，也会重新计算下次触发时间。

```typescript
const updated = await mastra.schedules.update('pinger', {
  cron: '*/30 * * * *',
  prompt: 'Give me a status update every 30 minutes.',
})
```

Agent 调度的 patch 可以更新 `cron`、`timezone`、`prompt`、`name`、`signalType`、`tagName`、`attributes`、`providerOptions`、`ifActive`、`ifIdle`、`metadata` 和 `status`。`threadId` 和 `resourceId` 无法通过 patch 更新。当 thread 目标需要更改时，请创建新的调度。

Workflow 调度的 patch 可以更新 `cron`、`timezone`、`inputData`、`initialState`、`requestContext`、`metadata` 和 `status`。对 Workflow 调度传入 `prompt`、`signalType` 和 `ifIdle` 等仅限 Agent 的 patch 字段时，会抛出错误。

### 生命周期 {/* #lifecycle */}

#### `pause(id)` {/* #pauseid */}

暂停调度。暂停操作是持久且幂等的。

```typescript
const paused = await mastra.schedules.pause('pinger')
```

#### `resume(id)` {/* #resumeid */}

恢复已暂停的调度，并根据当前时间重新计算下次触发时间。

```typescript
const active = await mastra.schedules.resume('pinger')
```

#### `run(id)` {/* #runid */}

立即触发一次调度，但不更改其 Cron 周期。

```typescript
const run = await mastra.schedules.run('pinger')
```

对于 Agent 调度，`claimId` 使用 `manual_<scheduleId>_<timestamp>`。对于 Workflow 调度，`claimId` 使用 `sched_<scheduleId>_<timestamp>`，并会复用为 Workflow 运行 ID。

#### `delete(id)` {/* #deleteid */}

删除调度。删除不存在的调度时不会执行任何操作。

```typescript
await mastra.schedules.delete('pinger')
```

## 调度行为 {/* #schedule-behavior */}

- Agent 调度 ID 使用 `agent_` 前缀。通过 `mastra.schedules.create()` 创建的 Workflow 调度 ID 使用 `schedule_` 前缀。
- 设置 `threadId` 时，使用 thread 的 Agent 调度需要 `resourceId`。
- `signalType`、`ifActive`、`ifIdle` 和 `resourceId` 需要 `threadId`。
- Workflow 调度不接受 `prompt`、`signalType` 或 `ifIdle` 等仅限 Agent 的 patch 字段。
- `run()` 会立即发布一次手动触发，且不会更改存储的 Cron 周期。
