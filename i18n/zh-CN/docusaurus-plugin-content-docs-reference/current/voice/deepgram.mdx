---
title: "参考：Deepgram | Voice"
description: "Deepgram Voice 实现的文档，介绍使用多种语音模型和语言进行文本转语音与语音转文本的功能。"
packages:
  - "@mastra/voice-deepgram"
---

# Deepgram

Mastra 中的 Deepgram Voice 实现使用 Deepgram API 提供文本转语音 (TTS) 和语音转文本 (STT) 功能。它支持多种语音模型和语言，并为语音合成和转录提供可配置选项。

## 使用示例

```typescript
import { DeepgramVoice } from '@mastra/voice-deepgram'

// Initialize with default configuration (uses DEEPGRAM_API_KEY environment variable)
const voice = new DeepgramVoice()

// Initialize with custom configuration
const voice = new DeepgramVoice({
  speechModel: {
    name: 'aura',
    apiKey: 'your-api-key',
  },
  listeningModel: {
    name: 'nova-2',
    apiKey: 'your-api-key',
  },
  speaker: 'asteria-en',
})

// Text-to-Speech
const audioStream = await voice.speak('Hello, world!')

// Speech-to-Text
const transcript = await voice.listen(audioStream)
```

## 构造函数参数

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'DeepgramVoiceConfig',
    description: '文本转语音功能的配置。',
    isOptional: true,
    defaultValue: "{ name: 'aura' }",
    properties: [
      {
        type: 'DeepgramVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'DeepgramModel',
            description: '要使用的 Deepgram 模型',
            isOptional: true,
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Deepgram API 密钥。未提供时使用 DEEPGRAM_API_KEY 环境变量',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'Record<string, any>',
            description: '要传递给 Deepgram API 的其他属性',
            isOptional: true,
          },
          {
            name: 'language',
            type: 'string',
            description: '模型的语言代码',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'listeningModel',
    type: 'DeepgramVoiceConfig',
    description: '语音转文本功能的配置。',
    isOptional: true,
    defaultValue: "{ name: 'nova' }",
    properties: [
      {
        type: 'DeepgramVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'DeepgramModel',
            description: '要使用的 Deepgram 模型',
            isOptional: true,
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Deepgram API 密钥。未提供时使用 DEEPGRAM_API_KEY 环境变量',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'Record<string, any>',
            description: '要传递给 Deepgram API 的其他属性',
            isOptional: true,
          },
          {
            name: 'language',
            type: 'string',
            description: '模型的语言代码',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'DeepgramVoiceId',
    description: '文本转语音使用的默认声音',
    isOptional: true,
    defaultValue: "'asteria-en'",
  },
]}
/>

## 方法

### `speak()`

使用配置的语音模型和声音将文本转换为语音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要转换为语音的文本。如果提供流，则先将其转换为文本。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '语音合成的其他选项',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '为此次请求覆盖默认 speaker',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

返回：`Promise<NodeJS.ReadableStream>`

### `listen()`

使用配置的听写模型将语音转换为文本。

<PropertiesTable
  content={[
  {
    name: 'audioStream',
    type: 'NodeJS.ReadableStream',
    description: '要转录的音频流',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '要传递给 Deepgram API 的其他选项',
    isOptional: true,
  },
]}
/>

返回：`Promise<string>`

### `getSpeakers()`

返回可用的声音选项列表。

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '声音的唯一标识符',
    isOptional: false,
  },
]}
/>
