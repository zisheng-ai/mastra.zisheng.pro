---
title: "参考：voice.updateConfig() | Voice"
description: "Voice Provider 中用于在 runtime 更新 Provider 配置的 updateConfig() 方法文档。"
packages:
  - "@mastra/voice-openai-realtime"
---

# voice.updateConfig()

`updateConfig()` 方法允许你在 runtime 更新 Voice Provider 的配置。需要更改 Voice 设置、API key 或特定于 Provider 的其他选项而又不想创建新实例时，此方法非常有用。

## 使用示例 {/* #usage-example */}

```typescript
import { OpenAIRealtimeVoice } from '@mastra/voice-openai-realtime'

// Initialize a real-time voice provider
const voice = new OpenAIRealtimeVoice({
  realtimeConfig: {
    model: 'gpt-5.1-realtime',
    apiKey: process.env.OPENAI_API_KEY,
  },
  speaker: 'alloy',
})

// Connect to the real-time service
await voice.connect()

// Later, update the configuration
voice.updateConfig({
  voice: 'nova', // Change the default voice
  turn_detection: {
    type: 'server_vad',
    threshold: 0.5,
    silence_duration_ms: 1000,
  },
})

// The next speak() call will use the new configuration
await voice.speak('Hello with my new voice!')
```

## 参数 {/* #parameters */}

<br />

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'Record<string, unknown>',
    description: '要更新的配置选项。具体属性取决于 Voice Provider。',
    isOptional: false,
  },
]}
/>

## 返回值 {/* #return-value */}

此方法不返回值。

## 配置选项 {/* #configuration-options */}

不同 Voice Provider 支持不同的配置选项：

### OpenAI Realtime {/* #openai-realtime */}

<br />

<PropertiesTable
  content={[
  {
    name: 'voice',
    type: 'string',
    description: "用于语音合成的 Voice ID（例如 'alloy'、'echo'、'nova'）",
    isOptional: true,
  },
  {
    name: 'turn_detection',
    type: '{ type: string, threshold?: number, silence_duration_ms?: number }',
    description: '用于检测用户何时结束发言的配置',
    isOptional: true,
  },
]}
/>

## 注意事项 {/* #notes */}

- 如果 Provider 不支持此方法，默认实现会记录警告
- 配置更新通常会应用于后续操作，而不是正在进行的操作
- 并非构造函数中可设置的所有属性都能在 runtime 更新
- 具体行为取决于 Voice Provider 的实现
- 对于实时 Voice Provider，某些配置更改可能需要重新连接服务
