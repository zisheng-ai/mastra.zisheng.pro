---
title: "参考：withMastra() | AI SDK"
description: withMastra() 的 API 参考；该函数用于在 AI SDK 中使用 Mastra 功能。
packages:
  - "@mastra/ai-sdk"
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# withMastra()

使用 Mastra 处理器和/或 memory 封装 AI SDK 模型。

## 使用示例 {/* #usage-example */}

```typescript title="src/example.ts"
import { openai } from '@ai-sdk/openai'
import { generateText } from 'ai'
import { withMastra } from '@mastra/ai-sdk'
import type { Processor } from '@mastra/core/processors'

const loggingProcessor: Processor<'logger'> = {
  id: 'logger',
  async processInput({ messages }) {
    console.log('Input:', messages.length, 'messages')
    return messages
  },
}

const model = withMastra(openai('gpt-5.4'), {
  inputProcessors: [loggingProcessor],
})

const { text } = await generateText({
  model,
  prompt: 'What is 2 + 2?',
})
```

## 参数 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'model',
    type: 'LanguageModelV2 | LanguageModelV3',
    description: "任意 AI SDK v5 或 v6 语言模型（例如 `openai('gpt-5.4')`、`anthropic('claude-opus-4-6')`）。",
    isOptional: false,
  },
  {
    name: 'options',
    type: 'WithMastraOptions',
    description: '处理器和 memory 的配置对象。',
    isOptional: true,
    properties: [
      {
        type: 'WithMastraOptions',
        parameters: [
          {
            name: 'inputProcessors',
            type: 'InputProcessor[]',
            description: '在调用 LLM 前运行的 input processor。',
            isOptional: true,
          },
          {
            name: 'outputProcessors',
            type: 'OutputProcessor[]',
            description: '对 LLM 响应运行的 output processor。',
            isOptional: true,
          },
          {
            name: 'memory',
            type: 'WithMastraMemoryOptions',
            description: 'Memory 配置，用于启用消息历史记录的自动持久化。',
            isOptional: true,
            properties: [
              {
                type: 'WithMastraMemoryOptions',
                parameters: [
                  {
                    name: 'storage',
                    type: 'MemoryStorage',
                    description:
                      "用于持久化消息的 memory storage domain。可通过 `await storage.getStore('memory')` 从 composite store 获取。",
                    isOptional: false,
                  },
                  {
                    name: 'threadId',
                    type: 'string',
                    description: '用于持久化对话的 thread ID。',
                    isOptional: false,
                  },
                  {
                    name: 'resourceId',
                    type: 'string',
                    description: 'Resource ID（用户/session 标识符）。',
                    isOptional: true,
                  },
                  {
                    name: 'lastMessages',
                    type: 'number | false',
                    description: '要检索的近期消息数量；设为 false 可禁用。',
                    isOptional: true,
                  },
                  {
                    name: 'semanticRecall',
                    type: 'WithMastraSemanticRecallOptions',
                    description: 'semantic recall 配置（基于 RAG 的 memory 检索）。',
                    isOptional: true,
                  },
                  {
                    name: 'workingMemory',
                    type: "MemoryConfig['workingMemory']",
                    description: 'working memory 配置（持久化用户数据）。',
                    isOptional: true,
                  },
                  {
                    name: 'readOnly',
                    type: 'boolean',
                    description: '只读模式，可阻止保存新消息。',
                    isOptional: true,
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## 返回值 {/* #returns */}

封装后的模型，与 `generateText`、`streamText`、`generateObject` 和 `streamObject` 兼容。

## 流式传输行为 {/* #streaming-behavior */}

实现 `processOutputResult` 的 output processor 会在 stream 结束后运行。请完整消费 stream，以持久化消息历史记录和 semantic recall。
