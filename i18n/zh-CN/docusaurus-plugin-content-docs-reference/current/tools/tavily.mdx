---
title: "参考：Tavily Tools | Tools 与 MCP"
description: "@mastra/tavily 的 API 参考文档，该包为 Mastra Agent 提供 Web 搜索、内容提取、网站抓取和网站结构映射 Tool。"
packages:
  - "@mastra/tavily"
---

# Tavily Tools

`@mastra/tavily` 包将 [Tavily](https://app.tavily.com) API 封装为与 Mastra 兼容的 Tool。它提供搜索、提取、抓取和网站结构映射 factory function。每个函数都返回使用 [`createTool()`](./create-tool) 创建的 Tool，并包含完整的 Zod 输入/输出 schema。

## 安装

```sh npm2yarn
npm install @mastra/tavily @tavily/core zod
```

## 快速开始

使用 `createTavilyTools()` 以共用设置取得全部四个 Tool：

```typescript title="src/mastra/tools/index.ts"
import { createTavilyTools } from '@mastra/tavily'

const tools = createTavilyTools()
// Or pass an explicit API key:
// const tools = createTavilyTools({ apiKey: 'tvly-...' })
```

你也可以分别创建每个 Tool：

```typescript title="src/mastra/tools/index.ts"
import { createTavilySearchTool, createTavilyExtractTool } from '@mastra/tavily'

const searchTool = createTavilySearchTool()
const extractTool = createTavilyExtractTool({ apiKey: 'tvly-...' })
```

根据缺省，所有 Tool 都会从环境中读取 `TAVILY_API_KEY`。你可以明确传入 `{ apiKey }` 来覆写此值。

## 设置

所有 factory function 都接受来自 `@tavily/core` 的 `TavilyClientOptions`：

<PropertiesTable
  content={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Tavily API key。未设置时会使用 `TAVILY_API_KEY` 环境变量。',
    isOptional: true,
  },
  {
    name: 'clientName',
    type: 'string',
    description: '每次请求时通过 `X-Client-Name` header 发送的归属字符串。',
    isOptional: true,
    defaultValue: "'mastra'",
  },
  {
    name: 'apiBaseURL',
    type: 'string',
    description: 'Tavily API 的 base URL。',
    isOptional: true,
  },
  {
    name: 'proxies',
    type: 'object',
    description: '传给底层 HTTP client 的 proxy 设置。',
    isOptional: true,
  },
  {
    name: 'projectId',
    type: 'string',
    description: '用于限定请求范围的 Tavily project ID。',
    isOptional: true,
  },
]}
/>

## `createTavilyTools()`

返回一个对象，其中包含共用设置的全部四个 Tool。

```typescript
import { createTavilyTools } from '@mastra/tavily'

const tools = createTavilyTools({ apiKey: 'tvly-...' })
// tools.tavilySearch, tools.tavilyExtract, tools.tavilyCrawl, tools.tavilyMap
```

**返回值：** `{ tavilySearch, tavilyExtract, tavilyCrawl, tavilyMap }`

## `createTavilySearchTool()`

创建一个使用 Tavily 搜索网络的 Tool。它会返回相关结果，包括内容摘要、选填的 AI 产生答案与图片。

**Tool ID：** `tavily-search`

```typescript
import { createTavilySearchTool } from '@mastra/tavily'

const searchTool = createTavilySearchTool()
```

### 输入

<PropertiesTable
  content={[
  {
    name: 'query',
    type: 'string',
    description: '搜索查找。',
  },
  {
    name: 'searchDepth',
    type: "'basic' | 'advanced' | 'fast' | 'ultra-fast'",
    description:
      "搜索深度。使用 'basic' 取得标准结果、'advanced' 取得更完整的结果，或使用 'fast'／'ultra-fast' 降低延迟。",
    isOptional: true,
  },
  {
    name: 'maxResults',
    type: 'number',
    description: '返回结果的数量上限（1–20）。',
    isOptional: true,
  },
  {
    name: 'includeAnswer',
    type: "boolean | 'basic' | 'advanced'",
    description: '包含由 AI 产生的答案摘要。',
    isOptional: true,
  },
  {
    name: 'includeImages',
    type: 'boolean',
    description: '在回应中包含与查找相关的图片。',
    isOptional: true,
  },
  {
    name: 'includeImageDescriptions',
    type: 'boolean',
    description: '包含返回图片的说明。',
    isOptional: true,
  },
  {
    name: 'includeRawContent',
    type: "false | 'markdown' | 'text'",
    description: "包含每个结果经清理的 HTML 内容。传入 false 可停用，或传入 'markdown'／'text' 指定格式。",
    isOptional: true,
  },
  {
    name: 'includeDomains',
    type: 'string[]',
    description: '将结果限制在这些网域。',
    isOptional: true,
  },
  {
    name: 'excludeDomains',
    type: 'string[]',
    description: '排除来自这些网域的结果。',
    isOptional: true,
  },
  {
    name: 'timeRange',
    type: "'day' | 'week' | 'month' | 'year'",
    description: '依时间新近程度筛选结果。',
    isOptional: true,
  },
]}
/>

### 输出

<PropertiesTable
  content={[
  {
    name: 'query',
    type: 'string',
    description: '原始搜索查找。',
  },
  {
    name: 'answer',
    type: 'string',
    description: '由 AI 产生的答案摘要。',
    isOptional: true,
  },
  {
    name: 'images',
    type: '{ url: string; description?: string }[]',
    description: '相关图片。',
    isOptional: true,
  },
  {
    name: 'results',
    type: 'SearchResult[]',
    description: '搜索结果数组。',
    properties: [
      {
        type: 'SearchResult',
        parameters: [
          { name: 'title', type: 'string', description: '结果标题。' },
          { name: 'url', type: 'string', description: '结果 URL。' },
          { name: 'content', type: 'string', description: '内容摘要。' },
          { name: 'score', type: 'number', description: '相关性分数。' },
          { name: 'rawContent', type: 'string', description: '完整页面内容（提出要求时）。', isOptional: true },
        ],
      },
    ],
  },
  {
    name: 'responseTime',
    type: 'number',
    description: '服务器回应时间（秒）。',
  },
]}
/>

## `createTavilyExtractTool()`

创建一个从一个或多个 URL 截取内容的 Tool。它会以 Markdown 或文本格式返回原始页面内容，每次请求最多可处理 20 个 URL。

**Tool ID：** `tavily-extract`

```typescript
import { createTavilyExtractTool } from '@mastra/tavily'

const extractTool = createTavilyExtractTool()
```

### 输入

<PropertiesTable
  content={[
  {
    name: 'urls',
    type: 'string[]',
    description: '要截取内容的 URL（1–20 个）。',
  },
  {
    name: 'extractDepth',
    type: "'basic' | 'advanced'",
    description: "截取深度。使用 'advanced' 可截取表格与嵌入内容。",
    isOptional: true,
  },
  {
    name: 'query',
    type: 'string',
    description: '用户意图，用来依相关性对截取的内容区块重新排序。',
    isOptional: true,
  },
  {
    name: 'includeImages',
    type: 'boolean',
    description: '包含从页面截取的图片。',
    isOptional: true,
  },
  {
    name: 'format',
    type: "'markdown' | 'text'",
    description: '截取内容的输出格式。',
    isOptional: true,
    defaultValue: "'markdown'",
  },
]}
/>

### 输出

<PropertiesTable
  content={[
  {
    name: 'results',
    type: 'ExtractResult[]',
    description: '成功截取的页面。',
    properties: [
      {
        type: 'ExtractResult',
        parameters: [
          { name: 'url', type: 'string', description: '页面 URL。' },
          { name: 'rawContent', type: 'string', description: '截取的页面内容。' },
          { name: 'images', type: 'string[]', description: '截取的图片 URL。', isOptional: true },
        ],
      },
    ],
  },
  {
    name: 'failedResults',
    type: 'FailedResult[]',
    description: '截取失败的 URL。',
    properties: [
      {
        type: 'FailedResult',
        parameters: [
          { name: 'url', type: 'string', description: '失败的 URL。' },
          { name: 'error', type: 'string', description: '错误消息。' },
        ],
      },
    ],
  },
  {
    name: 'responseTime',
    type: 'number',
    description: '服务器回应时间（秒）。',
  },
]}
/>

## `createTavilyCrawlTool()`

创建一个从指定 URL 开始抓取网站的 Tool。它会从找到的页面截取内容，并可设置深度、广度与网域限制。

**Tool ID：** `tavily-crawl`

```typescript
import { createTavilyCrawlTool } from '@mastra/tavily'

const crawlTool = createTavilyCrawlTool()
```

### 输入

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: '开始抓取的根 URL。',
  },
  {
    name: 'maxDepth',
    type: 'number',
    description: '从 base URL 开始抓取的深度上限。',
    isOptional: true,
  },
  {
    name: 'maxBreadth',
    type: 'number',
    description: '每页要跟随的链接数量上限。',
    isOptional: true,
  },
  {
    name: 'limit',
    type: 'number',
    description: 'Crawler 停止前处理的页面总数。',
    isOptional: true,
  },
  {
    name: 'instructions',
    type: 'string',
    description: '提供给 crawler 的自然语言 instructions。',
    isOptional: true,
  },
  {
    name: 'selectPaths',
    type: 'string[]',
    description: '用来选取特定 URL 路径的 regex pattern。',
    isOptional: true,
  },
  {
    name: 'selectDomains',
    type: 'string[]',
    description: '用来限制特定网域的 regex pattern。',
    isOptional: true,
  },
  {
    name: 'excludePaths',
    type: 'string[]',
    description: '用来排除特定 URL 路径的 regex pattern。',
    isOptional: true,
  },
  {
    name: 'excludeDomains',
    type: 'string[]',
    description: '用来排除特定网域的 regex pattern。',
    isOptional: true,
  },
  {
    name: 'allowExternal',
    type: 'boolean',
    description: '是否跟随前往外部网域的链接。',
    isOptional: true,
  },
  {
    name: 'extractDepth',
    type: "'basic' | 'advanced'",
    description: "截取深度。使用 'advanced' 可截取表格与嵌入内容。",
    isOptional: true,
  },
  {
    name: 'includeImages',
    type: 'boolean',
    description: '包含抓取页面中的图片。',
    isOptional: true,
  },
  {
    name: 'format',
    type: "'markdown' | 'text'",
    description: '截取内容的输出格式。',
    isOptional: true,
    defaultValue: "'markdown'",
  },
]}
/>

### 输出

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: '已抓取的根 URL。',
  },
  {
    name: 'results',
    type: 'CrawlResult[]',
    description: '从找到的页面截取的内容。',
    properties: [
      {
        type: 'CrawlResult',
        parameters: [
          { name: 'url', type: 'string', description: '页面 URL。' },
          { name: 'rawContent', type: 'string', description: '截取的页面内容。' },
          { name: 'images', type: 'string[]', description: '页面上找到的图片 URL。', isOptional: true },
        ],
      },
    ],
  },
  {
    name: 'responseTime',
    type: 'number',
    description: '服务器回应时间（秒）。',
  },
]}
/>

## `createTavilyMapTool()`

创建一个从指定 URL 开始映射网站结构的 Tool。它会探索并返回 URL 清单，但不截取页面内容。可在针对特定目标截取前，使用此 Tool 了解网站结构。

**Tool ID：** `tavily-map`

```typescript
import { createTavilyMapTool } from '@mastra/tavily'

const mapTool = createTavilyMapTool()
```

### 输入

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: '开始映射网站结构的根 URL。',
  },
  {
    name: 'maxDepth',
    type: 'number',
    description: '从 base URL 开始映射网站结构的深度上限。',
    isOptional: true,
  },
  {
    name: 'maxBreadth',
    type: 'number',
    description: '每页要跟随的链接数量上限。',
    isOptional: true,
  },
  {
    name: 'limit',
    type: 'number',
    description: 'Mapper 停止前处理的链接总数。',
    isOptional: true,
  },
  {
    name: 'instructions',
    type: 'string',
    description: '提供给 mapper 的自然语言 instructions。',
    isOptional: true,
  },
  {
    name: 'selectPaths',
    type: 'string[]',
    description: '用来选取特定 URL 路径的 regex pattern。',
    isOptional: true,
  },
  {
    name: 'selectDomains',
    type: 'string[]',
    description: '用来限制特定网域的 regex pattern。',
    isOptional: true,
  },
  {
    name: 'excludePaths',
    type: 'string[]',
    description: '用来排除特定 URL 路径的 regex pattern。',
    isOptional: true,
  },
  {
    name: 'excludeDomains',
    type: 'string[]',
    description: '用来排除特定网域的 regex pattern。',
    isOptional: true,
  },
  {
    name: 'allowExternal',
    type: 'boolean',
    description: '是否包含外部网域链接。',
    isOptional: true,
  },
]}
/>

### 输出

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: '已完成结构映射的根 URL。',
  },
  {
    name: 'results',
    type: 'string[]',
    description: '找到的 URL。',
  },
  {
    name: 'responseTime',
    type: 'number',
    description: '服务器回应时间（秒）。',
  },
]}
/>

## Agent 示例

下列示例示范结合搜索与截取功能的研究 Agent：

```typescript title="src/mastra/agents/index.ts"
import { Agent } from '@mastra/core/agent'
import { createTavilySearchTool, createTavilyExtractTool } from '@mastra/tavily'

const agent = new Agent({
  id: 'web-search-agent',
  name: 'Web Search Agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  instructions:
    'You are a web search assistant. Use search tool to find relevant pages, then use extract tool to get full content from the best results.',
  tools: {
    search: createTavilySearchTool(),
    extract: createTavilyExtractTool(),
  },
})
```

## 环境变量

| 变量 | 说明 |
| - | - |
| `TAVILY_API_KEY` | 你的 Tavily API key。若未将 `apiKey` 传给 factory function，则缺省使用此值。 |

## 相关内容

- [`createTool()`](./create-tool)
- [Tavily API 文档](https://docs.tavily.com)
