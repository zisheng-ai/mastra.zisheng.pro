---
title: "参考：buildBasePrompt() | Coding Agent"
description: "buildBasePrompt() 的 API 参考；该函数根据 PromptContext 为 Coding Agent 构建共享的基础系统 Prompt。"
packages:
  - "@mastra/core"
---

# buildBasePrompt()

`buildBasePrompt()` 为 Coding Agent 构建共享的基础系统 Prompt，即让 Agent 成为优秀编程助手的行为指令。它接收描述当前环境（项目、平台、Git 分支、模式和模型）的 `PromptContext`，并以字符串形式返回 Prompt。

产品特定字符串通过 `productName`、`coAuthorName` 和 `coAuthorEmail` 参数化，因此无需 Fork 即可更换 Prompt 和 Commit Trailer 中的品牌。这些字段默认为 `"Mastra Code"` / `"noreply@mastra.ai"`，因此现有调用方的输出保持不变。

为 Agent 构建运行时定义的指令时，可将它与 [`createCodingAgent()`](/reference/coding-agent/create-coding-agent) 配合使用。

## 用法示例

根据 `PromptContext` 构建基础 Prompt：

```typescript title="src/mastra/prompt.ts"
import { buildBasePrompt } from '@mastra/core/coding-agent'

const prompt = buildBasePrompt({
  projectPath: process.cwd(),
  projectName: 'my-app',
  gitBranch: 'main',
  platform: process.platform,
  date: new Date().toDateString(),
  mode: 'build',
  modelId: '__GATEWAY_OPENAI_MODEL_BASE__',
  toolGuidance: '',
})
```

如需更换 Prompt 品牌，请传入产品和共同作者字段：

```typescript
const prompt = buildBasePrompt({
  // ...environment fields
  productName: 'Acme Coder',
  coAuthorName: 'Acme Bot',
  coAuthorEmail: 'bot@acme.dev',
})
```

## 参数

`buildBasePrompt()` 接收一个 `PromptContext` 对象。

<PropertiesTable
  content={[
  {
    name: 'projectPath',
    type: 'string',
    description: '项目根目录的绝对路径。',
  },
  {
    name: 'projectName',
    type: 'string',
    description: '项目的显示名称。',
  },
  {
    name: 'gitBranch',
    type: 'string',
    description: '当前 Git 分支（如果项目是 Git 仓库）。',
    isOptional: true,
  },
  {
    name: 'platform',
    type: 'string',
    description: '操作系统平台（例如 process.platform 的值）。',
  },
  {
    name: 'commonBinaries',
    type: '{ name: string; path: string | null }[]',
    description: '在系统中检测到的常见二进制文件；每项包含名称和解析后的路径（未找到时为 null）。',
    isOptional: true,
  },
  {
    name: 'date',
    type: 'string',
    description: '注入 Prompt 的当前日期字符串。',
  },
  {
    name: 'mode',
    type: 'string',
    description: '当前启用的 Agent 模式（例如 "build" 或 "plan"）。',
  },
  {
    name: 'modelId',
    type: 'string',
    description: '当前启用模型的标识符；如果提供，会包含在 Commit 的 Co-Authored-By 行中。',
    isOptional: true,
  },
  {
    name: 'activePlan',
    type: '{ title: string; plan: string; approvedAt: string } | null',
    description: '当前已批准的 Plan（如果有）。',
    isOptional: true,
  },
  {
    name: 'toolGuidance',
    type: 'string',
    description: '附加到 Prompt 的 Tool 使用指南部分。',
  },
  {
    name: 'productName',
    type: 'string',
    description: 'Prompt 标题中使用的显示名称。',
    isOptional: true,
    defaultValue: 'Mastra Code',
  },
  {
    name: 'coAuthorName',
    type: 'string',
    description: 'Commit 的 Co-Authored-By 行中使用的名称。',
    isOptional: true,
    defaultValue: 'Mastra Code',
  },
  {
    name: 'coAuthorEmail',
    type: 'string',
    description: 'Commit 的 Co-Authored-By 行中使用的电子邮件地址。',
    isOptional: true,
    defaultValue: 'noreply@mastra.ai',
  },
]}
/>

## 返回值

<PropertiesTable
  content={[
  {
    name: 'prompt',
    type: 'string',
    description: 'Coding Agent 的基础系统 Prompt。',
  },
]}
/>

## 相关内容

- [`createCodingAgent()`](/reference/coding-agent/create-coding-agent)
