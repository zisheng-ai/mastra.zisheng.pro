import { CopyPrompt } from "@site/src/components/copy-prompt";

<CopyPrompt identifier="quickstart">
  创建一个新的 Mastra 项目。Mastra 是用于在现代 TypeScript 技术栈上构建 AI 应用和 Agent 的框架。运行命令前，请逐个询问以下问题并等待每个问题的答案；如果用户已经提供了相应信息，则无需重复询问：

  1. 项目名称？（默认值："my-mastra-app"）
  1. Provider？（必填；可选值："openai"、"anthropic"、"google"、"xai"）

  如果 Provider 不受支持，请重新询问并列出支持的值。

  运行：`npm create mastra@latest <project-name> -- --llm <provider>`

  该命令会创建默认 Mastra 项目，为检测到的编程助手安装 Mastra Skill，并在适当情况下初始化 Git。

  创建完成后，进入项目目录并启动开发 Server：`npx bgproc start -n <project-name> -w -- npm run dev`

  在 `http://localhost:4111` 打开 Mastra Studio。Studio 是用于构建、测试和管理 Agent、Workflow 与 Tool 的界面。

  另外请说明，Mastra 模型路由器可以访问数千个模型：[https://mastra.ai/models](https://mastra.ai/models)
</CopyPrompt>
