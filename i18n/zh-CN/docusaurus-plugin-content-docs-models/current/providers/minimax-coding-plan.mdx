---
title: "MiniMax Token Plan (minimax.io) | 模型"
description: "在 Mastra 中使用 MiniMax Token Plan (minimax.io) 模型。共有 7 个可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/minimax-coding-plan.svg" alt="MiniMax Token Plan (minimax.io) 标志" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />MiniMax Token Plan (minimax.io)

通过 Mastra 的模型路由器访问 7 个 MiniMax Token Plan (minimax.io) 模型。系统会自动使用 `MINIMAX_API_KEY` 环境变量完成身份验证。

更多信息请参阅[MiniMax Token Plan (minimax.io) 文档](https://platform.minimax.io/docs/token-plan/intro)。

```bash title=".env"
MINIMAX_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'minimax-coding-plan/MiniMax-M2',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用兼容 OpenAI 的 `/chat/completions` endpoint。部分 Provider 特有功能可能不可用，详情请参阅[MiniMax Token Plan (minimax.io) 文档](https://platform.minimax.io/docs/token-plan/intro)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'minimax-coding-plan/MiniMax-M2',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 196608,
    maxOutput: 128000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'minimax-coding-plan/MiniMax-M2.1',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 204800,
    maxOutput: 131072,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'minimax-coding-plan/MiniMax-M2.5',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 204800,
    maxOutput: 131072,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'minimax-coding-plan/MiniMax-M2.5-highspeed',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 204800,
    maxOutput: 131072,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'minimax-coding-plan/MiniMax-M2.7',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 204800,
    maxOutput: 131072,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'minimax-coding-plan/MiniMax-M2.7-highspeed',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 204800,
    maxOutput: 131072,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'minimax-coding-plan/MiniMax-M3',
    imageInput: true,
    audioInput: false,
    videoInput: true,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1000000,
    maxOutput: 128000,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 高级配置 {/* #advanced-configuration */}

### 自定义请求头 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://api.minimax.io/anthropic/v1',
    id: 'minimax-coding-plan/MiniMax-M2',
    apiKey: process.env.MINIMAX_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 动态选择模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'minimax-coding-plan/MiniMax-M3' : 'minimax-coding-plan/MiniMax-M2'
  },
})
```

## 直接安装 Provider {/* #direct-provider-installation */}

你也可以直接将此 Provider 安装为独立 package，用它代替 Mastra 模型路由字符串。详情请参阅[package 文档](https://www.npmjs.com/package/@ai-sdk/anthropic)。

```bash npm2yarn
npm install @ai-sdk/anthropic
```
