---
title: "Upstage | 模型"
description: "在 Mastra 中使用 Upstage 模型。共有 4 个可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/upstage.svg" alt="Upstage 标志" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Upstage

通过 Mastra 的模型路由器访问 4 个 Upstage 模型。系统会自动使用 `UPSTAGE_API_KEY` 环境变量完成身份验证。

更多信息请参阅[Upstage 文档](https://developers.upstage.ai)。

```bash title=".env"
UPSTAGE_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'upstage/solar-mini',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用兼容 OpenAI 的 `/chat/completions` endpoint。部分 Provider 特有功能可能不可用，详情请参阅[Upstage 文档](https://developers.upstage.ai)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'upstage/solar-mini',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 32768,
    maxOutput: 4096,
    inputCost: 0.15,
    outputCost: 0.15,
  },
  {
    model: 'upstage/solar-pro2',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 65536,
    maxOutput: 8192,
    inputCost: 0.25,
    outputCost: 0.25,
  },
  {
    model: 'upstage/solar-pro3',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 8192,
    inputCost: 0.25,
    outputCost: 0.25,
  },
  {
    model: 'upstage/solar-pro4',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 524288,
    maxOutput: 131072,
    inputCost: 0.3,
    outputCost: 1.2,
  },
]}
/>

## 高级配置 {/* #advanced-configuration */}

### 自定义请求头 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://api.upstage.ai/v1/solar',
    id: 'upstage/solar-mini',
    apiKey: process.env.UPSTAGE_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 动态选择模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'upstage/solar-pro4' : 'upstage/solar-mini'
  },
})
```
