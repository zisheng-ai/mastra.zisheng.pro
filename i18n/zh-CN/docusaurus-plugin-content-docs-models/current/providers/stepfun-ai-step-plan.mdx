---
title: "StepFun Step Plan (Global) | 模型"
description: "在 Mastra 中使用 StepFun Step Plan (Global) 模型。共有 3 个可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/stepfun-ai-step-plan.svg" alt="StepFun Step Plan (Global) 标志" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />StepFun Step Plan (Global)

通过 Mastra 的模型路由器访问 3 个 StepFun Step Plan (Global) 模型。系统会自动使用 `STEPFUN_API_KEY` 环境变量完成身份验证。

更多信息请参阅[StepFun Step Plan (Global) 文档](https://platform.stepfun.ai/docs/en/step-plan/integrations/reasoning-api)。

```bash title=".env"
STEPFUN_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'stepfun-ai-step-plan/step-3.5-flash',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用兼容 OpenAI 的 `/chat/completions` endpoint。部分 Provider 特有功能可能不可用，详情请参阅[StepFun Step Plan (Global) 文档](https://platform.stepfun.ai/docs/en/step-plan/integrations/reasoning-api)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'stepfun-ai-step-plan/step-3.5-flash',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 256000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'stepfun-ai-step-plan/step-3.5-flash-2603',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 256000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'stepfun-ai-step-plan/step-3.7-flash',
    imageInput: true,
    audioInput: false,
    videoInput: true,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 256000,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 高级配置 {/* #advanced-configuration */}

### 自定义请求头 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://api.stepfun.ai/step_plan/v1',
    id: 'stepfun-ai-step-plan/step-3.5-flash',
    apiKey: process.env.STEPFUN_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 动态选择模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced
      ? 'stepfun-ai-step-plan/step-3.7-flash'
      : 'stepfun-ai-step-plan/step-3.5-flash'
  },
})
```
