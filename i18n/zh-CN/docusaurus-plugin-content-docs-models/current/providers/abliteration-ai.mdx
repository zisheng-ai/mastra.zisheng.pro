---
title: "abliteration.ai | 模型"
description: "在 Mastra 中使用 abliteration.ai 模型。共有 2 个可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/abliteration-ai.svg" alt="abliteration.ai 标志" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />abliteration.ai

通过 Mastra 的模型路由器访问 2 个 abliteration.ai 模型。系统会自动使用 `ABLIT_KEY` 环境变量完成身份验证。

更多信息请参阅[abliteration.ai 文档](https://docs.abliteration.ai/models)。

```bash title=".env"
ABLIT_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "abliteration-ai/abliterated-model"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra 使用兼容 OpenAI 的 `/chat/completions` endpoint。部分 Provider 特有功能可能不可用，详情请参阅[abliteration.ai 文档](https://docs.abliteration.ai/models)。

:::

## 模型

<ProviderModelsTable
  models={[
  {
    "model": "abliteration-ai/abliterated-model",
    "imageInput": true,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 150000,
    "maxOutput": 8192,
    "inputCost": 3,
    "outputCost": 3
  },
  {
    "model": "abliteration-ai/abliterated-model-large",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1000000,
    "maxOutput": 999990,
    "inputCost": 5,
    "outputCost": 5
  }
]}
/>

## 高级配置

### 自定义请求头

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.abliteration.ai/v1",
    id: "abliteration-ai/abliterated-model",
    apiKey: process.env.ABLIT_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 动态选择模型

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "abliteration-ai/abliterated-model-large"
      : "abliteration-ai/abliterated-model";
  }
});
```


