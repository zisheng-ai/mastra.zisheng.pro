---
title: "Thinking Machines | 模型"
description: "在 Mastra 中使用 Thinking Machines 模型。共有 2 个可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/thinkingmachines.svg" alt="Thinking Machines 标志" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Thinking Machines

通过 Mastra 的模型路由器访问 2 个 Thinking Machines 模型。系统会自动使用 `TINKER_API_KEY` 环境变量完成身份验证。

更多信息请参阅[Thinking Machines 文档](https://tinker-docs.thinkingmachines.ai/tinker/compatible-apis/anthropic/)。

```bash title=".env"
TINKER_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "thinkingmachines/thinkingmachines/Inkling"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra 使用兼容 OpenAI 的 `/chat/completions` endpoint。部分 Provider 特有功能可能不可用，详情请参阅[Thinking Machines 文档](https://tinker-docs.thinkingmachines.ai/tinker/compatible-apis/anthropic/)。

:::

## 模型

<ProviderModelsTable
  models={[
  {
    "model": "thinkingmachines/thinkingmachines/Inkling",
    "imageInput": true,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 65536,
    "maxOutput": 65536,
    "inputCost": 1.87,
    "outputCost": 4.68
  },
  {
    "model": "thinkingmachines/thinkingmachines/Inkling:peft:262144",
    "imageInput": true,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 262144,
    "maxOutput": 262144,
    "inputCost": 3.74,
    "outputCost": 9.36
  }
]}
/>

## 高级配置

### 自定义请求头

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://tinker.thinkingmachines.dev/services/tinker-prod/anthropic/api/v1",
    id: "thinkingmachines/thinkingmachines/Inkling",
    apiKey: process.env.TINKER_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 动态选择模型

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "thinkingmachines/thinkingmachines/Inkling:peft:262144"
      : "thinkingmachines/thinkingmachines/Inkling";
  }
});
```



## 直接安装 Provider

你也可以直接将此 Provider 安装为独立 package，用它代替 Mastra 模型路由字符串。详情请参阅[package 文档](https://www.npmjs.com/package/@ai-sdk/anthropic)。

```bash npm2yarn
npm install @ai-sdk/anthropic
```

