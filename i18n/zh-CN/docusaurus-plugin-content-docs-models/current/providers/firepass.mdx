---
title: "Fireworks (Firepass) | 模型"
description: "在 Mastra 中使用 Fireworks (Firepass) 模型。共有 1 个可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/firepass.svg" alt="Fireworks (Firepass) 标志" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Fireworks (Firepass)

通过 Mastra 的模型路由器访问 1 个 Fireworks (Firepass) 模型。系统会自动使用 `FIREPASS_API_KEY` 环境变量完成身份验证。

更多信息请参阅[Fireworks (Firepass) 文档](https://docs.fireworks.ai/firepass)。

```bash title=".env"
FIREPASS_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'firepass/accounts/fireworks/routers/kimi-k2p6-turbo',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用兼容 OpenAI 的 `/chat/completions` endpoint。部分 Provider 特有功能可能不可用，详情请参阅[Fireworks (Firepass) 文档](https://docs.fireworks.ai/firepass)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'firepass/accounts/fireworks/routers/kimi-k2p6-turbo',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262000,
    maxOutput: 262000,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 高级配置 {/* #advanced-configuration */}

### 自定义请求头 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://api.fireworks.ai/inference/v1/',
    id: 'firepass/accounts/fireworks/routers/kimi-k2p6-turbo',
    apiKey: process.env.FIREPASS_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 动态选择模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced
      ? 'firepass/accounts/fireworks/routers/kimi-k2p6-turbo'
      : 'firepass/accounts/fireworks/routers/kimi-k2p6-turbo'
  },
})
```
