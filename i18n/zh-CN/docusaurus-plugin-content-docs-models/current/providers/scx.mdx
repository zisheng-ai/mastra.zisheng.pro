---
title: "SCX.ai | 模型"
description: "在 Mastra 中使用 SCX.ai 模型。共有 2 个可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/scx.svg" alt="SCX.ai 标志" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />SCX.ai

通过 Mastra 的模型路由器访问 2 个 SCX.ai 模型。系统会自动使用 `SCX_API_KEY` 环境变量完成身份验证。

更多信息请参阅[SCX.ai 文档](https://platform.scx.ai/docs)。

```bash title=".env"
SCX_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'scx/MiniMax-M2.7',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用兼容 OpenAI 的 `/chat/completions` endpoint。部分 Provider 特有功能可能不可用，详情请参阅[SCX.ai 文档](https://platform.scx.ai/docs)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'scx/gpt-oss-120b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 131072,
    inputCost: 0.17,
    outputCost: 0.55,
  },
  {
    model: 'scx/MiniMax-M2.7',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 192000,
    maxOutput: 64000,
    inputCost: 0.48,
    outputCost: 1.79,
  },
]}
/>

## 高级配置 {/* #advanced-configuration */}

### 自定义请求头 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://api.scx.ai/v1',
    id: 'scx/MiniMax-M2.7',
    apiKey: process.env.SCX_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 动态选择模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'scx/gpt-oss-120b' : 'scx/MiniMax-M2.7'
  },
})
```
