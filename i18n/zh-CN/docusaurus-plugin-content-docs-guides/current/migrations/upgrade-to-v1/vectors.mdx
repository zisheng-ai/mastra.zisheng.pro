---
title: "Vector | v1 迁移指南"
description: "了解升级到 v1 时如何迁移 Vector 相关变更。"
---

# Vector

Vector Store 构造函数现在必须提供 `id` 属性，相关方法也已重命名，以遵循一致的命名约定。

## 已变更

### Vector Store 实例必须提供 `id` 属性

Vector Store 实例现在必须提供 `id` 属性。此唯一标识符用于在 Mastra 中跟踪和管理 Vector Store 实例。应用中的每个 Vector Store 都应使用描述清晰且唯一的 `id` 字符串。

迁移时，请在 Vector Store 构造函数中添加 `id` 字段。

```diff
  const vectorStore = new PgVector({
+   id: 'main-vector-store',
    connectionString: process.env.POSTGRES_CONNECTION_STRING,
  });

  const chromaStore = new ChromaVector({
+   id: 'chroma-embeddings',
    url: process.env.CHROMA_URL,
  });

  const pineconeStore = new PineconeVector({
+   id: 'pinecone-production',
    apiKey: process.env.PINECONE_API_KEY,
  });
```

### `getVectors` 改为 `listVectors`

`getVectors()` 方法已重命名为 `listVectors()`。此变更与整个 API 的命名约定保持一致：返回集合的 getter 方法使用 `list` 前缀。

迁移时，请将所有 `getVectors()` 调用替换为 `listVectors()`。

```diff
  const vectorStore = new VectorStore({ ... });

- const vectors = await vectorStore.getVectors({ ... });
+ const vectors = await vectorStore.listVectors({ ... });
```

### `LibSQLVector`：`connectionUrl` 改为 `url`

`connectionUrl` 参数已重命名为 `url`，以与 `@libsql/client` API 和 LibSQLStorage 保持一致。

```diff
  const vectorStore = new LibSQLVector({
    id: 'my-vector',
-   connectionUrl: 'file:./db.sqlite',
+   url: 'file:./db.sqlite',
  });
```

### `OpenSearchVector`：`url` 改为 `node`

`url` 参数已重命名为 `node`，以匹配 OpenSearch `ClientOptions` API。构造函数现在接受所有 OpenSearch 客户端选项，包括身份验证、SSL 和压缩。

```diff
  const vectorStore = new OpenSearchVector({
    id: 'my-vector',
-   url: 'http://localhost:9200',
+   node: 'http://localhost:9200',
  });
```

### `PineconeVector`：移除 `environment`

`environment` 参数已移除。如需指定自定义控制器主机，请改用 `controllerHostUrl`。构造函数现在接受所有 Pinecone 配置选项。

```diff
  const vectorStore = new PineconeVector({
    id: 'my-vector',
    apiKey: process.env.PINECONE_API_KEY,
-   environment: process.env.PINECONE_ENVIRONMENT,
  });
```

如果需要自定义控制器主机：

```ts
const vectorStore = new PineconeVector({
  id: 'my-vector',
  apiKey: process.env.PINECONE_API_KEY,
  controllerHostUrl: 'https://api.pinecone.io',
})
```
