---
title: "JSON Web Token | Authentification"
description: "Documentation sur l’utilisation des JSON Web Tokens dans Mastra."
packages:
  - "@mastra/auth"
  - "@mastra/client-js"
  - "@mastra/core"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# JSON Web Token

La classe `MastraJwtAuth` fournit à Mastra un mécanisme d’authentification léger fondé sur les JSON Web Tokens (JWT). Elle vérifie les requêtes entrantes à l’aide d’un secret partagé et s’intègre au serveur Mastra via l’option `auth`.

## Installation {/* #installation */}

Avant d’utiliser la classe `MastraJwtAuth`, vous devez installer le package `@mastra/auth`.

```bash npm2yarn
npm install @mastra/auth@latest
```

## Créer un JWT {/* #creating-a-jwt */}

Pour authentifier les requêtes envoyées à votre serveur Mastra, vous avez besoin d’un JSON Web Token (JWT) valide signé avec votre `MASTRA_JWT_SECRET`.

Le moyen le plus simple d’en générer un consiste à utiliser [jwt.io](https://www.jwt.io/) :

1. Sélectionnez **JWT Encoder**.
1. Faites défiler la page jusqu’à la section **Sign JWT: Secret**.
1. Saisissez votre secret (par exemple : `supersecretdevkeythatishs256safe!`).
1. Cliquez sur **Generate example** pour créer un JWT valide.
1. Copiez le jeton généré et définissez-le comme `MASTRA_JWT_TOKEN` dans votre fichier `.env`.

## Exemple d’utilisation {/* #usage-example */}

Utilisez le JWT généré pour configurer `MastraJwtAuth` dans votre serveur Mastra :

```typescript {2,6-8} title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraJwtAuth } from '@mastra/auth'

export const mastra = new Mastra({
  server: {
    auth: new MastraJwtAuth({
      secret: process.env.MASTRA_JWT_SECRET,
    }),
  },
})
```

Consultez la [référence de MastraJwtAuth](/reference/auth/jwt) pour découvrir toutes les options de configuration disponibles.

Dans [Studio](/docs/studio/overview), ouvrez **Settings**, puis sélectionnez le bouton **« Add Header »** sous **Headers**. Saisissez `Authorization` comme nom d’en-tête et `Bearer <your-jwt>` comme valeur.

## Configurer `MastraClient` {/* #configuring-mastraclient */}

Lorsque `auth` est activé, toutes les requêtes effectuées avec `MastraClient` doivent inclure un JWT valide dans l’en-tête `Authorization` :

```typescript {6} title="lib/mastra/mastra-client.ts"
import { MastraClient } from '@mastra/client-js'

export const mastraClient = new MastraClient({
  baseUrl: 'https://<mastra-api-url>',
  headers: {
    Authorization: `Bearer ${process.env.MASTRA_JWT_TOKEN}`,
  },
})
```

Consultez le [SDK client Mastra](/docs/server/mastra-client) pour davantage d’options de configuration.

### Effectuer des requêtes authentifiées {/* #making-authenticated-requests */}

Une fois `MastraClient` configuré, vous pouvez envoyer des requêtes authentifiées depuis votre application frontend ou utiliser `curl` pour effectuer rapidement des tests locaux :

<Tabs>
  <TabItem value="react" label="React">
    ```tsx title="src/components/test-agent.tsx"
    import { mastraClient } from '../../lib/mastra-client'

    export const TestAgent = () => {
      async function handleClick() {
        const agent = mastraClient.getAgent('weatherAgent')

        const response = await agent.generate('Weather in London')

        console.log(response)
      }

      return <button onClick={handleClick}>Test Agent</button>
    }
    ```
  </TabItem>

  <TabItem value="curl" label="cURL">
    ```bash
    curl -X POST http://localhost:4111/api/agents/weatherAgent/generate \
      -H "Content-Type: application/json" \
      -H "Authorization: Bearer <your-jwt>" \
      -d '{
        "messages": "Weather in London"
      }'
    ```
  </TabItem>
</Tabs>
