---
title: "Subconscious | Modèles"
description: "Utilisez les modèles Subconscious avec Mastra. 2 modèles sont disponibles."
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/subconscious.svg" alt="Subconscious logo" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Subconscious

Accédez à 2 modèles Subconscious via le routeur de modèles de Mastra. L’authentification est gérée automatiquement à l’aide de la variable d’environnement `SUBCONSCIOUS_API_KEY`.

Pour en savoir plus, consultez la [documentation Subconscious](https://docs.subconscious.dev).

```bash title=".env"
SUBCONSCIOUS_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "subconscious/subconscious/glm-5.2"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra utilise le point de terminaison `/chat/completions` compatible avec OpenAI. Certaines fonctionnalités propres au Provider peuvent ne pas être disponibles. Consultez la [documentation Subconscious](https://docs.subconscious.dev) pour plus de détails.

:::

## Modèles

<ProviderModelsTable
  models={[
  {
    "model": "subconscious/subconscious/glm-5.2",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1000000,
    "maxOutput": 131072,
    "inputCost": 1.4,
    "outputCost": 4.4
  },
  {
    "model": "subconscious/subconscious/tim-qwen3.6-27b",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 8192,
    "maxOutput": 5000,
    "inputCost": 0.3,
    "outputCost": 3
  }
]}
/>

## Configuration avancée

### En-têtes personnalisés

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.subconscious.dev/v1",
    id: "subconscious/subconscious/glm-5.2",
    apiKey: process.env.SUBCONSCIOUS_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### Sélection dynamique du modèle

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "subconscious/subconscious/tim-qwen3.6-27b"
      : "subconscious/subconscious/glm-5.2";
  }
});
```



## Installation directe du Provider

Ce Provider peut aussi être installé directement comme package autonome, utilisable à la place de la chaîne du routeur de modèles Mastra. Consultez la [documentation du package](https://www.npmjs.com/package/@ai-sdk/anthropic) for more details.

```bash npm2yarn
npm install @ai-sdk/anthropic
```

