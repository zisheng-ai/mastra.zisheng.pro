---
title: "KUAE Cloud Coding Plan | Modèles"
description: "Utilisez les modèles KUAE Cloud Coding Plan avec Mastra. 1 modèle est disponible."
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/kuae-cloud-coding-plan.svg" alt="KUAE Cloud Coding Plan logo" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />KUAE Cloud Coding Plan

Accédez à 1 modèle KUAE Cloud Coding Plan via le routeur de modèles de Mastra. L’authentification est gérée automatiquement à l’aide de la variable d’environnement `KUAE_API_KEY`.

Pour en savoir plus, consultez la [documentation KUAE Cloud Coding Plan](https://docs.mthreads.com/kuaecloud/kuaecloud-doc-online/coding_plan/).

```bash title=".env"
KUAE_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "kuae-cloud-coding-plan/GLM-4.7"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra utilise le point de terminaison `/chat/completions` compatible avec OpenAI. Certaines fonctionnalités propres au Provider peuvent ne pas être disponibles. Consultez la [documentation KUAE Cloud Coding Plan](https://docs.mthreads.com/kuaecloud/kuaecloud-doc-online/coding_plan/) pour plus de détails.

:::

## Modèles

<ProviderModelsTable
  models={[
  {
    "model": "kuae-cloud-coding-plan/GLM-4.7",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 204800,
    "maxOutput": 131072,
    "inputCost": null,
    "outputCost": null
  }
]}
/>

## Advanced configuration

### Custom headers

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://coding-plan-endpoint.kuaecloud.net/v1",
    id: "kuae-cloud-coding-plan/GLM-4.7",
    apiKey: process.env.KUAE_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### Dynamic model selection

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "kuae-cloud-coding-plan/GLM-4.7"
      : "kuae-cloud-coding-plan/GLM-4.7";
  }
});
```


