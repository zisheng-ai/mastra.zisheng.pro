---
title: "Référence : Feedback | Observability"
description: "Référence de l'API des enregistrements de feedback d'Observability, des filtres et des méthodes de requête."
packages:
  - "@mastra/core"
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# Feedback

**Ajouté dans :** `@mastra/core@1.18.0`

Les API de Feedback stockent et interrogent les signaux d'Observability impliquant un humain, tels que les évaluations, les pouces, les commentaires et les corrections. Consultez le [guide du Feedback](/docs/observability/feedback) pour découvrir les modèles d'utilisation.

## Exemple d'utilisation {/* #usage-example */}

L'exemple suivant enregistre une évaluation pour une Trace persistée via le point d'entrée d'Observability. `addFeedback()` est facultatif sur ce point d'entrée ; vérifiez donc que l'implémentation active de l'Observability le prend en charge avant de l'appeler.

```typescript
if (!mastra.observability.addFeedback) {
  throw new Error('Feedback is not supported by the active observability implementation')
}

await mastra.observability.addFeedback({
  traceId: 'trace-123',
  spanId: 'span-456',
  feedback: {
    feedbackSource: 'user',
    feedbackType: 'rating',
    value: 1,
    comment: 'Helpful answer.',
  },
})
```

## Création de feedback {/* #create-feedback */}

### `addFeedback(args)` {/* #addfeedbackargs */}

Ajoute du feedback à une Trace ou à un span persisté via le point d'entrée d'Observability.

```typescript
await mastra.observability.addFeedback?.({
  traceId: 'trace-123',
  spanId: 'span-456',
  feedback: {
    feedbackSource: 'user',
    feedbackType: 'rating',
    value: 1,
    comment: 'Helpful answer.',
  },
})
```

<PropertiesTable
  content={[
  { name: 'traceId', type: 'string', description: 'Trace qui sert de point d’ancrage à la cible du feedback.', isOptional: true },
  { name: 'spanId', type: 'string', description: 'Span qui sert de point d’ancrage à la cible du feedback.', isOptional: true },
  {
    name: 'correlationContext',
    type: 'CorrelationContext',
    description: 'Contexte de span ou de Trace actif depuis lequel émettre sans réhydrater la cible depuis le stockage.',
    isOptional: true,
  },
  { name: 'feedback', type: 'FeedbackInput', description: 'Payload de feedback à ajouter.' },
]}
/>

### `createFeedback(args)` {/* #createfeedbackargs */}

Crée un enregistrement de feedback via le domaine de stockage de l'Observability. Les appels au niveau du stockage écrivent directement dans celui-ci ; incluez donc `timestamp`.

```typescript
await observability.createFeedback({
  feedback: {
    feedbackId: 'feedback-1',
    timestamp: new Date(),
    traceId: 'trace-123',
    spanId: 'span-456',
    feedbackSource: 'user',
    feedbackType: 'rating',
    value: 1,
    comment: 'Helpful answer.',
  },
})
```

La route de création HTTP et du SDK client accepte `CreateFeedbackBody` et définit `timestamp` côté serveur. Elle génère `feedbackId` lorsque celui-ci est omis :

```typescript
await mastraClient.createFeedback({
  feedback: {
    traceId: 'trace-123',
    spanId: 'span-456',
    feedbackSource: 'user',
    feedbackType: 'rating',
    value: 1,
  },
})
```

### `batchCreateFeedback(args)` {/* #batchcreatefeedbackargs */}

Crée plusieurs enregistrements de feedback via le domaine de stockage de l'Observability. Cette méthode n'est pas exposée par les routes HTTP ni par `@mastra/client-js`.

```typescript
await observability.batchCreateFeedback({
  feedbacks: [
    {
      feedbackId: 'feedback-1',
      timestamp: new Date(),
      traceId: 'trace-123',
      feedbackSource: 'user',
      feedbackType: 'rating',
      value: 1,
    },
    {
      feedbackId: 'feedback-2',
      timestamp: new Date(),
      traceId: 'trace-123',
      feedbackSource: 'qa',
      feedbackType: 'comment',
      value: 'Needs a citation before shipping.',
    },
  ],
})
```

## Liste du feedback {/* #list-feedback */}

### `listFeedback(args?)` {/* #listfeedbackargs */}

Renvoie les enregistrements de feedback en mode page ou delta.

```typescript
const response = await mastraClient.listFeedback({
  filters: {
    feedbackType: 'rating',
    feedbackSource: 'studio',
  },
  pagination: { page: 0, perPage: 20 },
  orderBy: { field: 'timestamp', direction: 'DESC' },
})
```

<PropertiesTable
  content={[
  { name: 'mode', type: "'page' | 'delta'", description: "Mode de liste. Utilise 'page' par défaut.", isOptional: true },
  { name: 'filters', type: 'FeedbackFilter', description: 'Filtres des enregistrements de feedback.', isOptional: true },
  {
    name: 'pagination',
    type: '{ page?: number; perPage?: number }',
    description: 'Pagination du mode page. `page` commence à zéro.',
    isOptional: true,
  },
  {
    name: 'orderBy',
    type: "{ field?: 'timestamp'; direction?: 'ASC' | 'DESC' }",
    description: 'Configuration du tri en mode page.',
    isOptional: true,
  },
  {
    name: 'after',
    type: 'string',
    description: "Curseur delta pour l'interrogation incrémentielle. Valide uniquement en mode delta.",
    isOptional: true,
  },
  {
    name: 'limit',
    type: 'number',
    description: 'Nombre maximal de mises à jour à renvoyer en mode delta.',
    isOptional: true,
  },
]}
/>

## Requêtes OLAP {/* #olap-queries */}

Les requêtes OLAP de feedback s'appliquent aux champs `value` numériques.

### `getFeedbackAggregate(args)` {/* #getfeedbackaggregateargs */}

Renvoie une valeur agrégée de feedback.

```typescript
const response = await mastraClient.getFeedbackAggregate({
  feedbackType: 'rating',
  feedbackSource: 'user',
  aggregation: 'avg',
  comparePeriod: 'previous_day',
})
```

<PropertiesTable
  content={[
  { name: 'feedbackType', type: 'string', description: 'Type de feedback à agréger.' },
  { name: 'feedbackSource', type: 'string', description: 'Source de feedback à agréger.', isOptional: true },
  {
    name: 'aggregation',
    type: "'sum' | 'avg' | 'min' | 'max' | 'count' | 'count_distinct' | 'last'",
    description: 'Agrégation à appliquer.',
  },
  { name: 'filters', type: 'FeedbackFilter', description: 'Filtres supplémentaires.', isOptional: true },
  {
    name: 'comparePeriod',
    type: "'previous_period' | 'previous_day' | 'previous_week'",
    description: "Comparaison facultative d'une période à l'autre.",
    isOptional: true,
  },
]}
/>

### `getFeedbackBreakdown(args)` {/* #getfeedbackbreakdownargs */}

Renvoie les valeurs de feedback regroupées par dimensions.

```typescript
const response = await mastraClient.getFeedbackBreakdown({
  feedbackType: 'rating',
  groupBy: ['entityName'],
  aggregation: 'avg',
})
```

### `getFeedbackTimeSeries(args)` {/* #getfeedbacktimeseriesargs */}

Renvoie les valeurs de feedback regroupées par intervalles.

```typescript
const response = await mastraClient.getFeedbackTimeSeries({
  feedbackType: 'rating',
  interval: '1h',
  aggregation: 'avg',
  groupBy: ['feedbackSource'],
})
```

### `getFeedbackPercentiles(args)` {/* #getfeedbackpercentilesargs */}

Renvoie les valeurs de percentile regroupées par intervalles.

```typescript
const response = await mastraClient.getFeedbackPercentiles({
  feedbackType: 'rating',
  percentiles: [0.5, 0.95],
  interval: '1d',
})
```

## Types {/* #types */}

### `FeedbackRecord` {/* #feedbackrecord */}

<PropertiesTable
  content={[
  {
    name: 'feedbackId',
    type: 'string | null',
    description: 'Identifiant unique de cet événement de feedback. La route serveur en génère un lorsqu’il est omis.',
    isOptional: true,
  },
  { name: 'timestamp', type: 'Date', description: "Date et heure d'enregistrement du feedback." },
  {
    name: 'traceId',
    type: 'string | null',
    description: 'Trace qui sert de point d’ancrage à la cible du feedback, si disponible.',
    isOptional: true,
  },
  {
    name: 'spanId',
    type: 'string | null',
    description: 'Span qui sert de point d’ancrage à la cible du feedback, si disponible.',
    isOptional: true,
  },
  {
    name: 'feedbackSource',
    type: 'string | null',
    description: "Métadonnées de source facultatives, telles que 'user', 'qa', 'studio' ou 'system'.",
    isOptional: true,
  },
  {
    name: 'source',
    type: 'string | null',
    description: 'Alias obsolète de `feedbackSource`.',
    isOptional: true,
  },
  {
    name: 'feedbackType',
    type: 'string',
    description: "Type de feedback, tel que 'rating', 'thumbs', 'comment' ou 'correction'.",
  },
  {
    name: 'value',
    type: 'number | string',
    description: 'Valeur du feedback. Les valeurs numériques prennent en charge les requêtes d’agrégation, de ventilation, de série temporelle et de percentile.',
  },
  {
    name: 'comment',
    type: 'string | null',
    description: 'Commentaire ou contexte supplémentaire du feedback.',
    isOptional: true,
  },
  {
    name: 'feedbackUserId',
    type: 'string | null',
    description: 'Utilisateur ayant fourni le feedback.',
    isOptional: true,
  },
  {
    name: 'sourceId',
    type: 'string | null',
    description: "Identifiant de l'enregistrement source auquel ce feedback est associé, tel que l'identifiant d'un résultat d'expérience.",
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown> | null',
    description: "Métadonnées définies par l'utilisateur pour l'enregistrement de feedback.",
    isOptional: true,
  },
]}
/>

### Champs de contexte partagés {/* #shared-context-fields */}

Les enregistrements de feedback peuvent inclure des champs de contexte d'Observability partagés afin de filtrer, regrouper et corréler les Traces, les logs, les métriques et les scores.

<PropertiesTable
  content={[
  {
    name: 'entityType',
    type: 'EntityType | null',
    description: "Type de l'entité ayant produit le signal.",
    isOptional: true,
  },
  {
    name: 'entityId',
    type: 'string | null',
    description: "Identifiant de l'entité ayant produit le signal.",
    isOptional: true,
  },
  {
    name: 'entityName',
    type: 'string | null',
    description: "Nom de l'entité ayant produit le signal.",
    isOptional: true,
  },
  {
    name: 'parentEntityType',
    type: 'EntityType | null',
    description: "Type de l'entité parente.",
    isOptional: true,
  },
  { name: 'parentEntityId', type: 'string | null', description: "Identifiant de l'entité parente.", isOptional: true },
  { name: 'parentEntityName', type: 'string | null', description: "Nom de l'entité parente.", isOptional: true },
  {
    name: 'rootEntityType',
    type: 'EntityType | null',
    description: "Type de l'entité racine.",
    isOptional: true,
  },
  { name: 'rootEntityId', type: 'string | null', description: "Identifiant de l'entité racine.", isOptional: true },
  { name: 'rootEntityName', type: 'string | null', description: "Nom de l'entité racine.", isOptional: true },
  { name: 'userId', type: 'string | null', description: "Utilisateur humain final ayant déclenché l'exécution.", isOptional: true },
  {
    name: 'organizationId',
    type: 'string | null',
    description: 'Organisation ou compte mutualisé.',
    isOptional: true,
  },
  { name: 'resourceId', type: 'string | null', description: 'Contexte plus large de la ressource.', isOptional: true },
  { name: 'runId', type: 'string | null', description: "Identifiant de l'exécution.", isOptional: true },
  {
    name: 'sessionId',
    type: 'string | null',
    description: 'Identifiant de session permettant de regrouper les Traces.',
    isOptional: true,
  },
  { name: 'threadId', type: 'string | null', description: 'Identifiant du fil de conversation.', isOptional: true },
  { name: 'requestId', type: 'string | null', description: 'Identifiant de requête HTTP pour la corrélation.', isOptional: true },
  { name: 'environment', type: 'string | null', description: 'Environnement de déploiement.', isOptional: true },
  { name: 'serviceName', type: 'string | null', description: 'Nom du service.', isOptional: true },
  {
    name: 'scope',
    type: 'Record<string, unknown> | null',
    description: "Métadonnées du package, de la version de l'application ou du déploiement.",
    isOptional: true,
  },
  {
    name: 'entityVersionId',
    type: 'string | null',
    description: "Identifiant de version de l'entité ayant produit le signal.",
    isOptional: true,
  },
  {
    name: 'parentEntityVersionId',
    type: 'string | null',
    description: "Identifiant de version de l'entité parente.",
    isOptional: true,
  },
  {
    name: 'rootEntityVersionId',
    type: 'string | null',
    description: "Identifiant de version de l'entité racine.",
    isOptional: true,
  },
  { name: 'experimentId', type: 'string | null', description: "Identifiant d'exécution d'expérience ou d'Eval.", isOptional: true },
  {
    name: 'executionSource',
    type: 'string | null',
    description: "Source de l'exécution, telle que locale, cloud ou CI.",
    isOptional: true,
  },
  { name: 'tags', type: 'string[] | null', description: 'Labels destinés au filtrage.', isOptional: true },
]}
/>

### `FeedbackInput` {/* #feedbackinput */}

Utilisez `FeedbackInput` avec `mastra.observability.addFeedback()`, `recordedTrace.addFeedback()` et `recordedSpan.addFeedback()`.

<PropertiesTable
  content={[
  {
    name: 'feedbackSource',
    type: 'string',
    description: 'Métadonnées de source facultatives du feedback.',
    isOptional: true,
  },
  { name: 'source', type: 'string', description: 'Alias obsolète de `feedbackSource`.', isOptional: true },
  { name: 'feedbackType', type: 'string', description: 'Type de feedback à enregistrer.' },
  { name: 'value', type: 'number | string', description: 'Valeur de feedback à enregistrer.' },
  { name: 'comment', type: 'string', description: 'Commentaire ou contexte supplémentaire.', isOptional: true },
  { name: 'feedbackUserId', type: 'string', description: 'Utilisateur ayant fourni le feedback.', isOptional: true },
  { name: 'userId', type: 'string', description: 'Alias obsolète de `feedbackUserId`.', isOptional: true },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'Métadonnées supplémentaires propres au feedback.',
    isOptional: true,
  },
  { name: 'experimentId', type: 'string', description: "Identifiant d'exécution d'expérience ou d'Eval.", isOptional: true },
  {
    name: 'sourceId',
    type: 'string',
    description: "Identifiant de l'enregistrement source auquel ce feedback est associé.",
    isOptional: true,
  },
]}
/>

### `FeedbackFilter` {/* #feedbackfilter */}

Utilisez `FeedbackFilter` dans `listFeedback()` et dans les `filters` des requêtes OLAP.

<PropertiesTable
  content={[
  {
    name: 'timestamp',
    type: '{ start?: Date; end?: Date; startExclusive?: boolean; endExclusive?: boolean }',
    description: "Filtre selon une plage d'horodatages.",
    isOptional: true,
  },
  { name: 'traceId', type: 'string', description: "Filtre selon l'identifiant de Trace.", isOptional: true },
  { name: 'spanId', type: 'string', description: "Filtre selon l'identifiant du span.", isOptional: true },
  {
    name: 'feedbackType',
    type: 'string | string[]',
    description: 'Filtre selon un ou plusieurs types de feedback.',
    isOptional: true,
  },
  { name: 'feedbackSource', type: 'string', description: 'Filtre selon la source du feedback.', isOptional: true },
  { name: 'source', type: 'string', description: 'Alias obsolète de `feedbackSource`.', isOptional: true },
  {
    name: 'feedbackUserId',
    type: 'string',
    description: 'Filtre selon l’utilisateur ayant fourni le feedback.',
    isOptional: true,
  },
  { name: 'entityType', type: 'EntityType', description: "Filtre selon le type d'entité.", isOptional: true },
  { name: 'entityName', type: 'string', description: "Filtre selon le nom de l'entité.", isOptional: true },
  { name: 'entityVersionId', type: 'string', description: "Filtre selon l'identifiant de version de l'entité.", isOptional: true },
  { name: 'parentEntityType', type: 'EntityType', description: "Filtre selon le type de l'entité parente.", isOptional: true },
  { name: 'parentEntityName', type: 'string', description: "Filtre selon le nom de l'entité parente.", isOptional: true },
  {
    name: 'parentEntityVersionId',
    type: 'string',
    description: "Filtre selon l'identifiant de version de l'entité parente.",
    isOptional: true,
  },
  { name: 'rootEntityType', type: 'EntityType', description: "Filtre selon le type de l'entité racine.", isOptional: true },
  { name: 'rootEntityName', type: 'string', description: "Filtre selon le nom de l'entité racine.", isOptional: true },
  { name: 'rootEntityVersionId', type: 'string', description: "Filtre selon l'identifiant de version de l'entité racine.", isOptional: true },
  { name: 'userId', type: 'string', description: "Filtre selon l'identifiant de l'utilisateur humain final.", isOptional: true },
  { name: 'organizationId', type: 'string', description: "Filtre selon l'identifiant de l'organisation.", isOptional: true },
  { name: 'resourceId', type: 'string', description: "Filtre selon l'identifiant de la ressource.", isOptional: true },
  { name: 'runId', type: 'string', description: "Filtre selon l'identifiant de l'exécution.", isOptional: true },
  { name: 'sessionId', type: 'string', description: "Filtre selon l'identifiant de la session.", isOptional: true },
  { name: 'threadId', type: 'string', description: "Filtre selon l'identifiant du fil de discussion.", isOptional: true },
  { name: 'requestId', type: 'string', description: "Filtre selon l'identifiant de la requête.", isOptional: true },
  { name: 'serviceName', type: 'string', description: 'Filtre selon le nom du service.', isOptional: true },
  { name: 'environment', type: 'string', description: "Filtre selon l'environnement.", isOptional: true },
  { name: 'executionSource', type: 'string', description: "Filtre selon la source de l'exécution.", isOptional: true },
  {
    name: 'experimentId',
    type: 'string',
    description: "Filtre selon l'identifiant d'exécution de l'expérience ou de l'Eval.",
    isOptional: true,
  },
  {
    name: 'tags',
    type: 'string[]',
    description: 'Filtre selon les tags. Les enregistrements correspondants doivent posséder tous les tags indiqués.',
    isOptional: true,
  },
]}
/>

## Routes HTTP {/* #http-routes */}

| Méthode | Chemin | Objectif | Autorisation |
| - | - | - | - |
| `GET` | `/api/observability/feedback` | Répertorier les enregistrements de feedback | Aucune autorisation dérivée |
| `POST` | `/api/observability/feedback` | Créer un enregistrement de feedback | Aucune autorisation dérivée |
| `POST` | `/api/observability/feedback/aggregate` | Renvoyer une valeur agrégée | `observability:read` |
| `POST` | `/api/observability/feedback/breakdown` | Regrouper le feedback par dimensions | `observability:read` |
| `POST` | `/api/observability/feedback/timeseries` | Regrouper le feedback par intervalles | `observability:read` |
| `POST` | `/api/observability/feedback/percentiles` | Renvoyer une série de percentiles | `observability:read` |

## Voir aussi {/* #related */}

- [Guide du Feedback](/docs/observability/feedback)
- [Référence de l'Observability du SDK client](/reference/client-js/observability)
- [Configuration de l'Observability](/reference/observability/tracing/configuration)
