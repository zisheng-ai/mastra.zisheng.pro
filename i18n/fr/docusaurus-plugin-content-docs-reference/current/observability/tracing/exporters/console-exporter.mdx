---
title: "Référence : ConsoleExporter | Observabilité"
description: Référence de l’API de ConsoleExporter
packages:
  - "@mastra/core"
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# ConsoleExporter

Affiche les événements de Trace dans la console à des fins de débogage et de développement.

## Constructeur {/* #constructor */}

```typescript prettier:false
new ConsoleExporter(config?: BaseExporterConfig)
```

<PropertiesTable
  content={[
  {
    name: 'config',
    type: 'BaseExporterConfig',
    description: 'Options de configuration',
    isOptional: true,
    properties: [
      {
        type: 'BaseExporterConfig',
        parameters: [
          {
            name: 'logger',
            type: 'IMastraLogger',
            description: 'Instance du logger (utilise par défaut ConsoleLogger avec le niveau INFO)',
            isOptional: true,
          },
          {
            name: 'logLevel',
            type: "LogLevel | 'debug' | 'info' | 'warn' | 'error'",
            description: 'Niveau de log de l’exportateur (valeur par défaut : INFO)',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

## Propriétés {/* #properties */}

```typescript prettier:false
readonly name = 'tracing-console-exporter';
```

## Méthodes {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

Exporte un événement de tracing vers la console.

<PropertiesTable
  props={[
  {
    name: 'event',
    type: 'TracingEvent',
    description: 'Événement de tracing à exporter',
    required: true,
  },
]}
/>

### shutdown {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

Enregistre un message d’arrêt dans les logs.

## Format de sortie {/* #output-format */}

L’exportateur produit différents formats selon le type d’événement :

### SPAN\_STARTED {/* #span_started */}

```
🚀 SPAN_STARTED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Trace ID: [trace id]
   Input: [formatted input]
   Attributes: [formatted attributes]
────────────────────────────────────────
```

### SPAN\_ENDED {/* #span_ended */}

```
✅ SPAN_ENDED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Duration: [duration]ms
   Trace ID: [trace id]
   Input: [formatted input]
   Output: [formatted output]
   Error: [formatted error if present]
   Attributes: [formatted attributes]
────────────────────────────────────────
```

### SPAN\_UPDATED {/* #span_updated */}

```
📝 SPAN_UPDATED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Trace ID: [trace id]
   Input: [formatted input]
   Output: [formatted output]
   Error: [formatted error if present]
   Updated Attributes: [formatted attributes]
────────────────────────────────────────
```

## Utilisation {/* #usage */}

```typescript
import { ConsoleExporter } from '@mastra/observability'
import { ConsoleLogger, LogLevel } from '@mastra/core/logger'

// Use default logger (INFO level)
const exporter = new ConsoleExporter()

// Use custom log level
const exporter = new ConsoleExporter({
  logLevel: 'debug',
})

// Use custom logger instance
const customLogger = new ConsoleLogger({ level: LogLevel.DEBUG })
const exporterWithLogger = new ConsoleExporter({
  logger: customLogger,
})
```

## Détails de l’implémentation {/* #implementation-details */}

- Formate les attributs en JSON avec une indentation de 2 espaces
- Calcule et affiche la durée des spans en millisecondes
- Gère proprement les erreurs de sérialisation
- Enregistre les types d’événements non implémentés comme avertissements
- Utilise des lignes de séparation de 80 caractères entre les événements

## Voir aussi {/* #see-also */}

### Documentation {/* #documentation */}

- [Présentation du tracing](/docs/observability/tracing/overview) : guide complet
- [Exportateurs](/docs/observability/integrations/overview#exporters) : concepts des exportateurs

### Autres exportateurs {/* #other-exporters */}

- [MastraStorageExporter](/reference/observability/tracing/exporters/mastra-storage-exporter) : persistance dans le stockage
- [MastraPlatformExporter](/reference/observability/tracing/exporters/mastra-platform-exporter) : plateforme Mastra
- [Langfuse](/reference/observability/tracing/exporters/langfuse) : intégration à Langfuse
- [Braintrust](/reference/observability/tracing/exporters/braintrust) : intégration à Braintrust

### Référence {/* #reference */}

- [Configuration](/reference/observability/tracing/configuration) : options de configuration
- [Interfaces](/reference/observability/tracing/interfaces) : définitions de types
