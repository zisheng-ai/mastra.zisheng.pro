---
title: "Référence : Scorer de pertinence de la réponse | Evals"
description: Documentation du Scorer de pertinence de la réponse de Mastra, qui évalue dans quelle mesure les sorties du LLM répondent à la requête d’entrée.
packages:
  - "@mastra/core"
  - "@mastra/evals"
---

# Scorer de pertinence de la réponse

La fonction `createAnswerRelevancyScorer()` accepte un objet d’options unique doté des propriétés suivantes :

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'model',
    type: 'LanguageModel',
    required: true,
    description: 'Configuration du modèle utilisé pour évaluer la pertinence.',
  },
  {
    name: 'uncertaintyWeight',
    type: 'number',
    required: false,
    defaultValue: '0.3',
    description: "Poids accordé aux verdicts 'unsure' dans l’évaluation (de 0 à 1).",
  },
  {
    name: 'scale',
    type: 'number',
    required: false,
    defaultValue: '1',
    description: 'Valeur maximale du score.',
  },
]}
/>

Cette fonction renvoie une instance de la classe MastraScorer. La méthode `.run()` accepte la même entrée que les autres Scorers — consultez la [référence de MastraScorer](./mastra-scorer) — mais la valeur renvoyée comprend les champs propres aux LLM décrits ci-dessous.

## Valeur renvoyée par `.run()` {/* #run-returns */}

<PropertiesTable
  content={[
  {
    name: 'runId',
    type: 'string',
    description: 'ID de l’exécution (facultatif).',
  },
  {
    name: 'score',
    type: 'number',
    description: 'Score de pertinence (de 0 à scale, de 0 à 1 par défaut)',
  },
  {
    name: 'preprocessPrompt',
    type: 'string',
    description: 'Prompt envoyé au LLM pour l’étape de prétraitement (facultatif).',
  },
  {
    name: 'preprocessStepResult',
    type: 'object',
    description: 'Objet contenant les affirmations extraites : { statements: string[] }',
  },
  {
    name: 'analyzePrompt',
    type: 'string',
    description: 'Prompt envoyé au LLM pour l’étape d’analyse (facultatif).',
  },
  {
    name: 'analyzeStepResult',
    type: 'object',
    description: "Objet contenant les résultats : { results: Array<{ result: 'yes' | 'unsure' | 'no', reason: string }> }",
  },
  {
    name: 'generateReasonPrompt',
    type: 'string',
    description: 'Prompt envoyé au LLM pour l’étape de justification (facultatif).',
  },
  {
    name: 'reason',
    type: 'string',
    description: 'Explication du score.',
  },
]}
/>

## Détails de l’évaluation {/* #scoring-details */}

Le Scorer évalue la pertinence en mesurant l’adéquation entre la requête et la réponse. Il prend en compte la complétude et le niveau de détail, mais pas l’exactitude factuelle.

### Processus d’évaluation {/* #scoring-process */}

1. **Prétraitement des affirmations :**
   - divise la sortie en affirmations utiles tout en préservant le contexte.
1. **Analyse de la pertinence :**
   - chaque affirmation reçoit l’une des évaluations suivantes :
     - "yes" : poids complet pour les correspondances directes
     - "unsure" : poids partiel (0,3 par défaut) pour les correspondances approximatives
     - "no" : poids nul pour le contenu non pertinent
1. **Calcul du score :**
   - `((direct + uncertainty * partial) / total_statements) * scale`

### Interprétation du score {/* #score-interpretation */}

Pour un score de pertinence compris entre 0 et 1 :

- **1.0** : la réponse traite entièrement la requête avec des informations pertinentes et ciblées.
- **0.7 à 0.9** : la réponse traite l’essentiel de la requête, mais peut contenir quelques informations sans rapport.
- **0.4 à 0.6** : la réponse traite partiellement la requête et mélange des informations pertinentes et sans rapport.
- **0.1 à 0.3** : la réponse contient très peu d’informations pertinentes et passe largement à côté de l’intention de la requête.
- **0.0** : la réponse est totalement sans rapport et ne traite pas la requête.

## Exemple {/* #example */}

Évaluez la pertinence des réponses de l’Agent dans différents scénarios :

```typescript title="src/example-answer-relevancy.ts"
import { runEvals } from '@mastra/core/evals'
import { createAnswerRelevancyScorer } from '@mastra/evals/scorers/prebuilt'
import { myAgent } from './agent'

const scorer = createAnswerRelevancyScorer({ model: '__GATEWAY_OPENAI_MODEL__' })

const result = await runEvals({
  data: [
    {
      input: 'What are the health benefits of regular exercise?',
    },
    {
      input: 'What should a healthy breakfast include?',
    },
    {
      input: 'What are the benefits of meditation?',
    },
  ],
  scorers: [scorer],
  target: myAgent,
  onItemComplete: ({ scorerResults }) => {
    console.log({
      score: scorerResults[scorer.id].score,
      reason: scorerResults[scorer.id].reason,
    })
  },
})

console.log(result.scores)
```

Pour en savoir plus sur `runEvals`, consultez la [référence de runEvals](/reference/evals/run-evals).

Pour ajouter ce Scorer à un Agent, consultez le guide de [présentation des Scorers](/docs/evals/overview).

## Voir aussi {/* #related */}

- [Scorer de fidélité](./faithfulness)
