---
title: "Référence : Content similarity scorer | Evals"
description: Documentation pour the Content Similarity Scorer dans Mastra, which measures textual similarity entre strings et provides a matching score.
packages:
  - "@mastra/core"
  - "@mastra/evals"
---

# Content similarity scorer

The `createContentSimilarityScorer()` fonction measures the textual similarity entre deux strings, providing a score that indicates how closely they match. It supports configurable options pour case sensitivity et whitespace handling.

## Paramètres

The `createContentSimilarityScorer()` fonction accepts a unique options objet avec the following properties:

<PropertiesTable
  content={[
  {
    name: 'ignoreCase',
    type: 'boolean',
    required: false,
    defaultValue: 'true',
    description: 'Whether à ignore case differences when comparing strings.',
  },
  {
    name: 'ignoreWhitespace',
    type: 'boolean',
    required: false,
    defaultValue: 'true',
    description: 'Whether à normalize whitespace when comparing strings.',
  },
]}
/>

Ce fonction returns an instance de the MastraScorer class. Consultez the [MastraScorer reference](./mastra-scorer) pour details sur the `.run()` method et its input/output.

## `.run()` returns

<PropertiesTable
  content={[
  {
    name: 'runId',
    type: 'string',
    description: 'The id de the exécute (optional).',
  },
  {
    name: 'preprocessStepResult',
    type: 'object',
    description: 'Object avec processed input et output: { processedEntrée: string, processedSortie: chaîne }',
  },
  {
    name: 'analyzeStepResult',
    type: 'object',
    description: 'Object avec similarity: { similarity: nombre }',
  },
  {
    name: 'score',
    type: 'number',
    description: 'Similarity score (0-1) where 1 indicates perfect similarity.',
  },
]}
/>

## Scoring details

The scorer evaluates textual similarity via character-level matching et configurable text normalization.

### Scoring Process

1. Normalizes text:
   - Case normalization (if ignoreCase: true)
   - Whitespace normalization (if ignoreWhitespace: true)
1. Compares processed strings using string-similarity algorithm:
   - Analyzes character sequences
   - Aligns word boundaries
   - Considers relative positions
   - Accounts pour length differences

Final score: `similarity_value * scale`

## Exemple

Evaluate textual similarity entre expected et actual agent outputs:

```typescript title="src/example-content-similarity.ts"
import { runEvals } from '@mastra/core/evals'
import { createContentSimilarityScorer } from '@mastra/evals/scorers/prebuilt'
import { myAgent } from './agent'

const scorer = createContentSimilarityScorer()

const result = await runEvals({
  data: [
    {
      input: 'Summarize the benefits of TypeScript',
      groundTruth:
        'TypeScript provides static typing, better tooling support, and improved code maintainability.',
    },
    {
      input: 'What is machine learning?',
      groundTruth:
        'Machine learning is a subset of AI that enables systems to learn from data without explicit programming.',
    },
  ],
  scorers: [scorer],
  target: myAgent,
  onItemComplete: ({ scorerResults }) => {
    console.log({
      score: scorerResults[scorer.id].score,
      groundTruth: scorerResults[scorer.id].groundTruth,
    })
  },
})

console.log(result.scores)
```

Pour plus de détails sur `runEvals`, consultez la [référence runEvals](/reference/evals/run-evals).

Pour ajouter ce scorer à un Agent, consultez le guide de [présentation des Scorers](/docs/evals/overview#adding-scorers-to-agents).

### Score interpretation

Un similarity score entre 0 et 1:

- **1.0**: Perfect match, content est nearly identical.
- **0.7 à 0.9**: High similarity, minor differences dans word choice ou structure.
- **0.4 à 0.6**: Moderate similarity, general overlap avec noticeable variation.
- **0.1 à 0.3**: Low similarity, few common elements ou shared meaning.
- **0.0**: No similarity, completely different content.

## Voir aussi

- [Completness Scorer](./completeness)
- [Textual Difference Scorer](./textual-difference)
- [Answer Relevancy Scorer](./answer-relevancy)
- [Keyword Coverage Scorer](./keyword-coverage)
