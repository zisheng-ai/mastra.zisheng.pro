---
title: "Référence : scorer de fidélité | Evals"
description: Documentation du scorer de fidélité de Mastra, qui évalue l'exactitude factuelle des sorties d'un LLM par rapport au contexte fourni.
packages:
  - "@mastra/core"
  - "@mastra/evals"
---

# Scorer de fidélité

La fonction `createFaithfulnessScorer()` évalue l'exactitude factuelle de la sortie d'un LLM par rapport au contexte fourni. Elle extrait les affirmations de la sortie et les vérifie à l'aide du contexte, ce qui en fait un outil essentiel pour mesurer la fiabilité des réponses d'un pipeline RAG.

## Paramètres {/* #parameters */}

La fonction `createFaithfulnessScorer()` accepte un seul objet d'options doté des propriétés suivantes :

<PropertiesTable
  content={[
  {
    name: 'model',
    type: 'LanguageModel',
    required: true,
    description: 'Configuration du modèle utilisé pour évaluer la fidélité.',
  },
  {
    name: 'context',
    type: 'string[]',
    required: true,
    description: "Tableau de segments de contexte par rapport auxquels les affirmations de la sortie seront vérifiées.",
  },
  {
    name: 'scale',
    type: 'number',
    required: false,
    defaultValue: '1',
    description: 'Valeur maximale du score. Le score final sera normalisé selon cette échelle.',
  },
]}
/>

Cette fonction renvoie une instance de la classe MastraScorer. La méthode `.run()` accepte les mêmes entrées que les autres scorers (consultez la [référence de MastraScorer](./mastra-scorer)), mais la valeur renvoyée comprend les champs propres aux LLM décrits ci-dessous.

## Valeur renvoyée par `.run()` {/* #run-returns */}

<PropertiesTable
  content={[
  {
    name: 'runId',
    type: 'string',
    description: "L'identifiant de l'exécution (facultatif).",
  },
  {
    name: 'preprocessStepResult',
    type: 'string[]',
    description: 'Tableau des affirmations extraites de la sortie.',
  },
  {
    name: 'preprocessPrompt',
    type: 'string',
    description: 'Le prompt envoyé au LLM pour l\'étape `preprocess` (facultatif).',
  },
  {
    name: 'analyzeStepResult',
    type: 'object',
    description: "Objet contenant les verdicts : { verdicts: Array<{ verdict: 'yes' | 'no' | 'unsure', reason: string }> }",
  },
  {
    name: 'analyzePrompt',
    type: 'string',
    description: 'Le prompt envoyé au LLM pour l\'étape `analyze` (facultatif).',
  },
  {
    name: 'score',
    type: 'number',
    description:
      "Score compris entre 0 et l'échelle configurée, qui représente la proportion d'affirmations étayées par le contexte.",
  },
  {
    name: 'reason',
    type: 'string',
    description:
      'Explication détaillée du score, notamment les affirmations étayées, contredites ou marquées comme incertaines.',
  },
  {
    name: 'generateReasonPrompt',
    type: 'string',
    description: 'Le prompt envoyé au LLM pour l\'étape `generateReason` (facultatif).',
  },
]}
/>

## Détails de l'évaluation {/* #scoring-details */}

Le scorer évalue la fidélité en vérifiant les affirmations par rapport au contexte fourni.

### Processus d'évaluation {/* #scoring-process */}

1. Analyse les affirmations et le contexte :
   - Extrait toutes les affirmations (factuelles et spéculatives)
   - Vérifie chaque affirmation par rapport au contexte
   - Attribue l'un des trois verdicts :
     - « yes » : l'affirmation est étayée par le contexte
     - « no » : l'affirmation contredit le contexte
     - « unsure » : l'affirmation ne peut pas être vérifiée
1. Calcule le score de fidélité :
   - Compte les affirmations étayées
   - Divise ce nombre par le nombre total d'affirmations
   - Adapte le résultat à la plage configurée

Score final : `(supported_claims / total_claims) * scale`

### Interprétation du score {/* #score-interpretation */}

Pour un score de fidélité compris entre 0 et 1 :

- **1.0** : toutes les affirmations sont exactes et directement étayées par le contexte.
- **0.7 à 0.9** : la plupart des affirmations sont correctes, avec quelques ajouts ou omissions mineurs.
- **0.4 à 0.6** : certaines affirmations sont étayées, mais d'autres ne peuvent pas être vérifiées.
- **0.1 à 0.3** : la majeure partie du contenu est inexacte ou non étayée.
- **0.0** : toutes les affirmations sont fausses ou contredisent le contexte.

## Exemple {/* #example */}

Évaluez la fidélité des réponses d'un Agent au contexte fourni :

```typescript title="src/example-faithfulness.ts"
import { runEvals } from '@mastra/core/evals'
import { createFaithfulnessScorer } from '@mastra/evals/scorers/prebuilt'
import { myAgent } from './agent'

// Context is typically populated from agent tool calls or RAG retrieval
const scorer = createFaithfulnessScorer({
  model: '__GATEWAY_OPENAI_MODEL__',
})

const result = await runEvals({
  data: [
    {
      input: 'Tell me about the Tesla Model 3.',
    },
    {
      input: 'What are the key features of this electric vehicle?',
    },
  ],
  scorers: [scorer],
  target: myAgent,
  onItemComplete: ({ scorerResults }) => {
    console.log({
      score: scorerResults[scorer.id].score,
      reason: scorerResults[scorer.id].reason,
    })
  },
})

console.log(result.scores)
```

Pour en savoir plus sur `runEvals`, consultez la [référence de runEvals](/reference/evals/run-evals).

Pour ajouter ce scorer à un Agent, consultez le guide [Présentation des scorers](/docs/evals/overview#adding-scorers-to-agents).

## Voir aussi {/* #related */}

- [Scorer de pertinence de la réponse](./answer-relevancy)
- [Scorer d'hallucination](./hallucination)
