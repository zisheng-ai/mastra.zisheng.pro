---
title: "Référence : toAISdkV4Messages() | AI SDK"
description: Référence d’API de toAISdkV4Messages(), a fonction à convert Mastra messages à AI SDK v4 UI messages.
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdkV4Messages()

Convertit messages depuis input formats à AI SDK V4 UI message format. Ce fonction accepts messages dans plusieurs formats (strings, AI SDK V4/V5 messages, Mastra DB messages, etc.) et normalizes them à the AI SDK V4 `UIMessage` format, which est suitable pour utilise avec AI SDK UI components like `useChat()`.

## Exemple d’utilisation

```typescript title="app/chat/page.tsx"
import { toAISdkV4Messages } from '@mastra/ai-sdk'
import { useChat } from 'ai/react' // AI SDK V4

// Stored messages from your database, memory or API
const storedMessages = [
  { id: '1', role: 'user', parts: [{ type: 'text', text: 'Hello' }] },
  { id: '2', role: 'assistant', parts: [{ type: 'text', text: 'Hi there!' }] },
]

export default function Chat() {
  const { messages } = useChat({
    initialMessages: toAISdkV4Messages(storedMessages),
  })

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role}: {message.content}
        </div>
      ))}
    </div>
  )
}
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'MessageListInput',
    description:
      'Messages à convert. Can be a string, tableau de strings, a unique message object, ou an tableau de message objects dans tout pris en charge format.',
    isOptional: false,
  },
]}
/>

## Renvoie

Renvoie an tableau de AI SDK V4 `UIMessage` objects avec the following structure:

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Unique message identifiant.',
  },
  {
    name: 'role',
    type: "'user' | 'assistant' | 'system'",
    description: 'The role de the message sender.',
  },
  {
    name: 'content',
    type: 'string',
    description: 'Text content de the message.',
  },
  {
    name: 'parts',
    type: 'UIMessagePart[]',
    description: 'Array de UI parts including text, tool-invocation, file, reasoning, source, et step markers.',
  },
  {
    name: 'createdAt',
    type: 'Date',
    description: 'Message creation timestamp.',
  },
  {
    name: 'toolInvocations',
    type: 'ToolInvocation[]',
    description: 'Array de Tool invocations pour assistant messages.',
    isOptional: true,
  },
  {
    name: 'experimental_attachments',
    type: 'Attachment[]',
    description: 'Fichier attachments sur the message.',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'Personnalisé metadata attached à the message.',
    isOptional: true,
  },
]}
/>

## Exemples

### Converting text messages

```typescript
import { toAISdkV4Messages } from '@mastra/ai-sdk'

const messages = toAISdkV4Messages(['Hello', 'How can I help you today?'])
// Returns array of UIMessage objects with user role and content string
```

### Loading messages avec Mastra client

```typescript
import { MastraClient } from '@mastra/client-js'
import { toAISdkV4Messages } from '@mastra/ai-sdk'

const client = new MastraClient()

const { messages } = await client.listThreadMessages('thread-id', { agentId: 'myAgent' })
const uiMessages = toAISdkV4Messages(messages)
```
