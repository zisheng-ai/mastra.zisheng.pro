---
title: "Référence : workflowRoute() | AI SDK"
description: Référence d’API de workflowRoute(), a fonction à crée Workflow route handlers pour streaming Workflow execution dans AI SDK-compatible format.
packages:
  - "@mastra/ai-sdk"
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# workflowRoute()

Crée a Workflow route handler pour streaming Workflow execution using the AI SDK format. Ce fonction registers an HTTP `POST` endpoint that accepts input data, executes a workflow, et streams the réponse back à the client dans AI SDK-compatible format. You ont à utilise it inside a [custom API route](/docs/server/custom-api-routes).

Use [`handleWorkflowStream()`](/reference/ai-sdk/handle-workflow-stream) if you need a framework-agnostic handler.

`workflowRoute()` keeps the existant AI SDK v5/default behavior. Si vousr app est typed against AI SDK v6, transmet `version: 'v6'`.

:::tip Agent streaming dans workflows

Lorsqu’un Workflow step pipes an agent's stream à the Workflow writer (e.g., `await response.fullStream.pipeTo(writer)`), the agent's text chunks et Tool calls sont forwarded à the UI stream dans real time, even when the agent runs inside Workflow steps.

See [Workflow Fluxing](/docs/workflows/overview#inspecting-workflow-stream-payloads) pour more details.

:::

## Exemple d’utilisation

Ce example shows how à définit up a Workflow route à the `/workflow` endpoint that uses a Workflow avec the ID `weatherWorkflow`.

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { workflowRoute } from '@mastra/ai-sdk'

export const mastra = new Mastra({
  server: {
    apiRoutes: [
      workflowRoute({
        path: '/workflow',
        workflow: 'weatherWorkflow',
      }),
    ],
  },
})
```

Vous pouvez également utilise runtime-defined Workflow routing based sur a `workflowId`. The URL `/workflow/weatherWorkflow` va résout à the Workflow avec the ID `weatherWorkflow`.

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { workflowRoute } from '@mastra/ai-sdk'

export const mastra = new Mastra({
  server: {
    apiRoutes: [
      workflowRoute({
        path: '/workflow/:workflowId',
      }),
    ],
  },
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'version',
    type: "'v5' | 'v6'",
    description:
      "Selects the AI SDK stream contract à emit. Omit it ou transmet `'v5'` pour the existant par défaut behavior. Pass `'v6'` when your app est typed against AI SDK v6 réponse helpers.",
    isOptional: true,
    defaultValue: "'v5'",
  },
  {
    name: 'path',
    type: 'string',
    description:
      'The route path (e.g., `/workflow` ou `/workflow/:workflowId`). Include `:workflowId` pour dynamic Workflow routing.',
    isOptional: true,
    defaultValue: "'/api/workflows/:workflowId/stream'",
  },
  {
    name: 'workflow',
    type: 'string',
    description: 'Fixed Workflow ID when ne pas using dynamic routing.',
    isOptional: true,
    defaultValue: 'undefined',
  },
  {
    name: 'includeTextStreamParts',
    type: 'boolean',
    description: 'Whether à inclut text stream parts dans the output.',
    isOptional: true,
    defaultValue: 'true',
  },
]}
/>

## Supplémentaire configuration

Vous pouvez utiliser [`prepareSendMessagesRequest`](https://ai-sdk.dev/docs/reference/ai-sdk-ui/use-chat#transport.default-chat-transport.prepare-send-messages-request) à customize the requête sent à the Workflow route, pour example à transmet additional configuration à the workflow:

```typescript
const { error, status, sendMessage, messages, regenerate, stop } = useChat({
  transport: new DefaultChatTransport({
    api: 'http://localhost:4111/workflow',
    prepareSendMessagesRequest({ messages }) {
      return {
        body: {
          inputData: {
            city: messages[messages.length - 1].parts[0].text,
          },
          // Or resumeData for resuming a suspended workflow
          resumeData: {
            confirmation: messages[messages.length - 1].parts[0].text,
          },
        },
      }
    },
  }),
})
```
