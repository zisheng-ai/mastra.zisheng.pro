---
title: "Référence : toAISdkStream() | AI SDK"
description: Référence de l'API de toAISdkStream(), une fonction qui convertit les flux Mastra en flux compatibles avec AI SDK.
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdkStream()

Convertit les flux Mastra (Agent, Network ou Workflow) en flux compatibles avec AI SDK. Utilisez cette fonction lorsque vous devez transformer manuellement des flux Mastra afin de les utiliser avec les fonctions `createUIMessageStream()` et `createUIMessageStreamResponse()` d'AI SDK.

Cette fonction est utile pour créer des endpoints de streaming personnalisés sans employer les fonctions utilitaires de route fournies par Mastra, telles que [`chatRoute()`](/reference/ai-sdk/chat-route) ou [`workflowRoute()`](/reference/ai-sdk/workflow-route).

`toAISdkStream()` conserve le comportement existant et par défaut d'AI SDK v5. Si votre application est typée pour AI SDK v6, transmettez `version: 'v6'` dans l'objet d'options.

## Sortie structurée dans les flux UI {/* #structured-output-in-ui-streams */}

Lorsque le flux de l'Agent source comprend un objet de sortie structurée final, `toAISdkStream()` l'émet sous la forme d'une partie de données UI AI SDK personnalisée :

```json
{
  "type": "data-structured-output",
  "data": {
    "object": {}
  }
}
```

Le champ `object` contient la valeur complète de votre sortie structurée. Cela associe le chunk final de sortie structurée de Mastra au flux UI AI SDK. Les chunks de sortie structurée partiels ne sont pas émis.

## Exemple d'utilisation {/* #usage-example */}

Exemple avec App Router de Next.js :

```typescript title="app/api/chat/route.ts"
import { mastra } from '../../mastra'
import { createUIMessageStream, createUIMessageStreamResponse } from 'ai'
import { toAISdkStream } from '@mastra/ai-sdk'

export async function POST(req: Request) {
  const { messages } = await req.json()
  const myAgent = mastra.getAgent('weatherAgent')
  const stream = await myAgent.stream(messages)

  const uiMessageStream = createUIMessageStream({
    originalMessages: messages,
    execute: async ({ writer }) => {
      for await (const part of toAISdkStream(stream, { from: 'agent' })) {
        await writer.write(part)
      }
    },
  })

  return createUIMessageStreamResponse({
    stream: uiMessageStream,
  })
}
```

:::tip

Transmettez `messages` à `originalMessages` dans `createUIMessageStream()` afin d'éviter la duplication des messages de l'Assistant dans l'interface utilisateur. Consultez [Résolution des problèmes : messages répétés de l'Assistant](https://ai-sdk.dev/docs/troubleshooting/repeated-assistant-messages) pour plus de détails.

:::

## Paramètres {/* #parameters */}

Le premier paramètre est le flux Mastra à convertir. Il peut s'agir de l'un des éléments suivants :

- `MastraModelOutput` - Flux d'Agent provenant de `agent.stream()`
- `MastraAgentNetworkStream` - Flux Network provenant de `agent.network()`
- `MastraWorkflowStream` ou `WorkflowRunOutput` - Flux de Workflow

Le deuxième paramètre est un objet d'options :

<PropertiesTable
  content={[
  {
    name: 'version',
    type: "'v5' | 'v6'",
    description:
      "Sélectionne le contrat de flux AI SDK à émettre. Omettez cette option ou transmettez `'v5'` pour conserver le comportement existant par défaut. Transmettez `'v6'` lorsque votre application est typée pour les fonctions utilitaires de réponse d'AI SDK v6.",
    isOptional: true,
    defaultValue: "'v5'",
  },
  {
    name: 'from',
    type: "'agent' | 'network' | 'workflow'",
    description: 'Type du flux Mastra en cours de conversion.',
    isOptional: false,
    defaultValue: "'agent'",
  },
  {
    name: 'lastMessageId',
    type: 'string',
    description: '(Agent uniquement) Identifiant du dernier message de la conversation.',
    isOptional: true,
  },
  {
    name: 'sendStart',
    type: 'boolean',
    description: '(Agent uniquement) Indique si les événements de démarrage doivent être envoyés dans le flux.',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'sendFinish',
    type: 'boolean',
    description: '(Agent uniquement) Indique si les événements de fin doivent être envoyés dans le flux.',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'sendReasoning',
    type: 'boolean',
    description:
      '(Agent uniquement) Indique si les chunks reasoning-delta doivent être inclus dans le flux. Définissez cette option sur true pour diffuser le contenu de raisonnement des modèles prenant en charge le raisonnement étendu.',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'sendSources',
    type: 'boolean',
    description: '(Agent uniquement) Indique si les citations des sources doivent être incluses dans la sortie.',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'includeTextStreamParts',
    type: 'boolean',
    description: '(Workflow uniquement) Indique si les parties du flux de texte doivent être incluses dans la sortie.',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'messageMetadata',
    type: '(options: { part: UIMessageStreamPart }) => Record<string, unknown> | undefined',
    description:
      '(Agent uniquement) Fonction qui reçoit la partie actuelle du flux et renvoie les métadonnées à associer aux chunks de démarrage et de fin.',
    isOptional: true,
  },
  {
    name: 'onError',
    type: '(error: unknown) => string',
    description:
      '(Agent uniquement) Fonction qui gère les erreurs pendant la conversion du flux. Elle reçoit l\'erreur et doit en renvoyer une représentation sous forme de chaîne.',
    isOptional: true,
  },
]}
/>

## Exemples {/* #examples */}

### Conversion d'un flux de Workflow {/* #converting-a-workflow-stream */}

```typescript title="app/api/workflow/route.ts" {13}
import { mastra } from '../../mastra'
import { createUIMessageStream, createUIMessageStreamResponse } from 'ai'
import { toAISdkStream } from '@mastra/ai-sdk'

export async function POST(req: Request) {
  const { input } = await req.json()
  const workflow = mastra.getWorkflow('myWorkflow')
  const run = workflow.createRun()
  const stream = await run.stream({ inputData: input })

  const uiMessageStream = createUIMessageStream({
    execute: async ({ writer }) => {
      for await (const part of toAISdkStream(stream, { from: 'workflow' })) {
        await writer.write(part)
      }
    },
  })

  return createUIMessageStreamResponse({
    stream: uiMessageStream,
  })
}
```

### Conversion d'un flux Network {/* #converting-a-network-stream */}

```typescript title="app/api/network/route.ts" {12}
import { mastra } from '../../mastra'
import { createUIMessageStream, createUIMessageStreamResponse } from 'ai'
import { toAISdkStream } from '@mastra/ai-sdk'

export async function POST(req: Request) {
  const { messages } = await req.json()
  const routingAgent = mastra.getAgent('routingAgent')
  const stream = await routingAgent.network(messages)

  const uiMessageStream = createUIMessageStream({
    execute: async ({ writer }) => {
      for await (const part of toAISdkStream(stream, { from: 'network' })) {
        await writer.write(part)
      }
    },
  })

  return createUIMessageStreamResponse({
    stream: uiMessageStream,
  })
}
```

### Conversion d'un flux d'Agent avec le raisonnement activé {/* #converting-an-agent-stream-with-reasoning-enabled */}

```typescript title="app/api/reasoning/route.ts" {8-12,17-20}
import { mastra } from '../../mastra'
import { createUIMessageStream, createUIMessageStreamResponse } from 'ai'
import { toAISdkStream } from '@mastra/ai-sdk'

export async function POST(req: Request) {
  const { messages } = await req.json()
  const reasoningAgent = mastra.getAgent('reasoningAgent')
  const stream = await reasoningAgent.stream(messages, {
    providerOptions: {
      openai: { reasoningEffort: 'high' },
    },
  })

  const uiMessageStream = createUIMessageStream({
    originalMessages: messages,
    execute: async ({ writer }) => {
      for await (const part of toAISdkStream(stream, {
        from: 'agent',
        sendReasoning: true,
      })) {
        await writer.write(part)
      }
    },
  })

  return createUIMessageStreamResponse({
    stream: uiMessageStream,
  })
}
```

### Conversion d'un flux d'Agent pour AI SDK v6 {/* #converting-an-agent-stream-for-ai-sdk-v6 */}

```typescript title="app/api/chat-v6/route.ts" {14-18}
import { mastra } from '../../mastra'
import { createUIMessageStream, createUIMessageStreamResponse } from 'ai'
import { toAISdkStream } from '@mastra/ai-sdk'

export async function POST(req: Request) {
  const { messages } = await req.json()
  const myAgent = mastra.getAgent('weatherAgent')
  const stream = await myAgent.stream(messages)

  const uiMessageStream = createUIMessageStream({
    originalMessages: messages,
    execute: async ({ writer }) => {
      for await (const part of toAISdkStream(stream, {
        from: 'agent',
        version: 'v6',
      })) {
        await writer.write(part)
      }
    },
  })

  return createUIMessageStreamResponse({
    stream: uiMessageStream,
  })
}
```

### Utilisation de `messageMetadata` {/* #using-messagemetadata */}

```typescript title="app/api/chat-with-metadata/route.ts" {13-19}
import { mastra } from '../../mastra'
import { createUIMessageStream, createUIMessageStreamResponse } from 'ai'
import { toAISdkStream } from '@mastra/ai-sdk'

export async function POST(req: Request) {
  const { messages } = await req.json()
  const myAgent = mastra.getAgent('weatherAgent')
  const stream = await myAgent.stream(messages)

  const uiMessageStream = createUIMessageStream({
    originalMessages: messages,
    execute: async ({ writer }) => {
      for await (const part of toAISdkStream(stream, {
        from: 'agent',
        messageMetadata: ({ part }) => ({
          timestamp: Date.now(),
          partType: part.type,
        }),
      })) {
        await writer.write(part)
      }
    },
  })

  return createUIMessageStreamResponse({
    stream: uiMessageStream,
  })
}
```

### Transformation du flux côté client {/* #client-side-stream-transformation */}

Si vous utilisez le SDK client Mastra (`@mastra/client-js`) côté client et souhaitez convertir les flux au format AI SDK :

```typescript title="client-stream-to-ai-sdk.ts" {14-23,25-35}
import { MastraClient } from '@mastra/client-js'
import { createUIMessageStream } from 'ai'
import { toAISdkStream } from '@mastra/ai-sdk'
import type { ChunkType, MastraModelOutput } from '@mastra/core/stream'

const client = new MastraClient({
  baseUrl: 'http://localhost:4111',
})

const agent = client.getAgent('weatherAgent')
const response = await agent.stream('What is the weather in Tokyo?')

// Convert the client SDK stream to a ReadableStream<ChunkType>
const chunkStream = new ReadableStream<ChunkType>({
  async start(controller) {
    await response.processDataStream({
      onChunk: async chunk => {
        controller.enqueue(chunk)
      },
    })
    controller.close()
  },
})

// Transform to AI SDK format
const uiMessageStream = createUIMessageStream({
  execute: async ({ writer }) => {
    for await (const part of toAISdkStream(chunkStream as unknown as MastraModelOutput, {
      from: 'agent',
    })) {
      await writer.write(part)
    }
  },
})

for await (const part of uiMessageStream) {
  console.log(part)
}
```
