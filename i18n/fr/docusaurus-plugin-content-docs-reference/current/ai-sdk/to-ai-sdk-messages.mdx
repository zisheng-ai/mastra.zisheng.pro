---
title: "Référence : toAISdkMessages() | AI SDK"
description: Référence de l’API toAISdkMessages(), une fonction qui convertit les messages Mastra en messages d’interface utilisateur AI SDK pour AI SDK v5 ou v6.
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdkMessages()

Convertit des messages provenant de différents formats d’entrée au format de message d’interface utilisateur AI SDK. `toAISdkMessages()` conserve le comportement par défaut existant d’AI SDK v5. Si votre application est typée pour AI SDK v6, transmettez `version: 'v6'`.

`version: 'v6'` utilise le chemin de sortie d’interface utilisateur AI SDK v6 de MessageList. La détection et l’ingestion des entrées MessageList restent inchangées.

## Exemple d’utilisation {/* #usage-example */}

```typescript title="app/chat/page.tsx"
import { toAISdkMessages } from '@mastra/ai-sdk/ui'
import { useChat } from 'ai/react'

// Stored messages from your database, memory, or API
const storedMessages = [
  { id: '1', role: 'user', content: 'Hello', parts: [{ type: 'text', text: 'Hello' }] },
  {
    id: '2',
    role: 'assistant',
    content: 'Hi there!',
    parts: [{ type: 'text', text: 'Hi there!' }],
  },
]

export default function Chat() {
  const { messages } = useChat({
    initialMessages: toAISdkMessages(storedMessages, { version: 'v6' }),
  })

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role}: {message.parts.map(part => (part.type === 'text' ? part.text : null))}
        </div>
      ))}
    </div>
  )
}
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'MessageListInput',
    description:
      'Messages à convertir. Il peut s’agir d’une chaîne, d’un tableau de chaînes, d’un objet message unique ou d’un tableau d’objets message dans n’importe quel format pris en charge.',
    isOptional: false,
  },
  {
    name: 'options.version',
    type: "'v5' | 'v6'",
    description:
      "Sélectionne le type de message AI SDK à renvoyer. Omettez cette option ou transmettez `'v5'` pour conserver le comportement par défaut existant. Transmettez `'v6'` lorsque votre application est typée pour les types de messages `useChat()` d’AI SDK v6.",
    isOptional: true,
  },
]}
/>

## Valeur renvoyée {/* #returns */}

Renvoie un tableau d’objets `UIMessage` AI SDK typés pour la version sélectionnée.

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique du message.',
  },
  {
    name: 'role',
    type: "'user' | 'assistant' | 'system'",
    description: 'Rôle de l’expéditeur du message.',
  },
  {
    name: 'parts',
    type: 'UIMessagePart[]',
    description: 'Tableau d’éléments d’interface utilisateur comprenant du texte, des résultats de Tools, des fichiers, du raisonnement, des sources et des marqueurs d’étape.',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'Métadonnées facultatives comprenant createdAt, threadId, resourceId et des champs personnalisés.',
    isOptional: true,
  },
]}
/>

## Exemples {/* #examples */}

### Utiliser les types AI SDK v5 par défaut {/* #using-the-default-ai-sdk-v5-types */}

```typescript
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const messages = toAISdkMessages(['Hello', 'How can I help you today?'])
```

### Renvoyer les types de messages AI SDK v6 {/* #returning-ai-sdk-v6-message-types */}

```typescript
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const messages = toAISdkMessages(['Hello', 'How can I help you today?'], {
  version: 'v6',
})
```

### Charger des messages avec le client Mastra {/* #loading-messages-with-mastra-client */}

```typescript
import { MastraClient } from '@mastra/client-js'
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const client = new MastraClient()

const { messages } = await client.listThreadMessages('thread-id', { agentId: 'myAgent' })
const uiMessages = toAISdkMessages(messages)
```

## Voir aussi {/* #related */}

- [`toAISdkStream()`](/reference/ai-sdk/to-ai-sdk-stream)
- [`toAISdkV5Messages()`](/reference/ai-sdk/to-ai-sdk-v5-messages)
- [`toAISdkV4Messages()`](/reference/ai-sdk/to-ai-sdk-v4-messages)
