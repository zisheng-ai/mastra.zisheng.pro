---
title: "Référence : toAISdkMessages() | AI SDK"
description: Référence d’API de toAISdkMessages(), a fonction à convert Mastra messages à AI SDK UI messages pour AI SDK v5 ou v6.
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdkMessages()

Convertit messages depuis input formats à AI SDK UI message format. `toAISdkMessages()` keeps the existant AI SDK v5/default behavior. Si vousr app est typed against AI SDK v6, transmet `version: 'v6'`.

`version: 'v6'` uses the MessageLister AI SDK v6 UI output path. MessageLister input detection et ingestion remain unchanged.

## Exemple d’utilisation

```typescript title="app/chat/page.tsx"
import { toAISdkMessages } from '@mastra/ai-sdk/ui'
import { useChat } from 'ai/react'

// Stored messages from your database, memory, or API
const storedMessages = [
  { id: '1', role: 'user', content: 'Hello', parts: [{ type: 'text', text: 'Hello' }] },
  {
    id: '2',
    role: 'assistant',
    content: 'Hi there!',
    parts: [{ type: 'text', text: 'Hi there!' }],
  },
]

export default function Chat() {
  const { messages } = useChat({
    initialMessages: toAISdkMessages(storedMessages, { version: 'v6' }),
  })

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role}: {message.parts.map(part => (part.type === 'text' ? part.text : null))}
        </div>
      ))}
    </div>
  )
}
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'MessageListInput',
    description:
      'Messages à convert. Can be a string, tableau de strings, a unique message object, ou an tableau de message objects dans tout pris en charge format.',
    isOptional: false,
  },
  {
    name: 'options.version',
    type: "'v5' | 'v6'",
    description:
      "Selects the AI SDK message type à return. Omit it ou transmet `'v5'` pour the existant par défaut behavior. Pass `'v6'` when your app est typed against AI SDK v6 `useChat()` message types.",
    isOptional: true,
  },
]}
/>

## Renvoie

Renvoie an tableau de AI SDK `UIMessage` objects typed pour the sélectionné version.

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Unique message identifiant.',
  },
  {
    name: 'role',
    type: "'user' | 'assistant' | 'system'",
    description: 'The role de the message sender.',
  },
  {
    name: 'parts',
    type: 'UIMessagePart[]',
    description: 'Array de UI parts including text, Tool results, files, reasoning, sources, et step markers.',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'Facultatif metadata including createdAt, threadId, resourceId, et personnalisé fields.',
    isOptional: true,
  },
]}
/>

## Exemples

### Using the par défaut AI SDK v5 types

```typescript
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const messages = toAISdkMessages(['Hello', 'How can I help you today?'])
```

### Returning AI SDK v6 message types

```typescript
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const messages = toAISdkMessages(['Hello', 'How can I help you today?'], {
  version: 'v6',
})
```

### Loading messages avec Mastra client

```typescript
import { MastraClient } from '@mastra/client-js'
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const client = new MastraClient()

const { messages } = await client.listThreadMessages('thread-id', { agentId: 'myAgent' })
const uiMessages = toAISdkMessages(messages)
```

## Voir aussi

- [`toAISdkStream()`](/reference/ai-sdk/to-ai-sdk-stream)
- [`toAISdkV5Messages()`](/reference/ai-sdk/to-ai-sdk-v5-messages)
- [`toAISdkV4Messages()`](/reference/ai-sdk/to-ai-sdk-v4-messages)
