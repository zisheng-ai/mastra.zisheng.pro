---
title: "Référence : toAISdkV5Messages() | AI SDK"
description: Référence de l’API toAISdkV5Messages(), une fonction qui convertit les messages Mastra en messages d’interface utilisateur AI SDK v5.
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdkV5Messages()

Convertit des messages provenant de différents formats d’entrée au format de message d’interface utilisateur AI SDK V5 et versions ultérieures. Cette fonction accepte plusieurs formats de messages (chaînes, messages AI SDK V4/V5, messages de base de données Mastra, etc.) et les normalise au format `UIMessage` d’AI SDK V5+, adapté aux composants d’interface utilisateur AI SDK tels que `useChat()`.

## Exemple d’utilisation {/* #usage-example */}

```typescript title="app/chat/page.tsx"
import { toAISdkV5Messages } from '@mastra/ai-sdk/ui'
import { useChat } from 'ai/react'

// Stored messages from your database, memory or API
const storedMessages = [
  { id: '1', role: 'user', content: 'Hello', parts: [{ type: 'text', text: 'Hello' }] },
  {
    id: '2',
    role: 'assistant',
    content: 'Hi there!',
    parts: [{ type: 'text', text: 'Hi there!' }],
  },
]

export default function Chat() {
  const { messages } = useChat({
    initialMessages: toAISdkV5Messages(storedMessages),
  })

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role}: {message.parts.map(part => (part.type === 'text' ? part.text : null))}
        </div>
      ))}
    </div>
  )
}
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'MessageListInput',
    description:
      'Messages à convertir. Il peut s’agir d’une chaîne, d’un tableau de chaînes, d’un objet message unique ou d’un tableau d’objets message dans n’importe quel format pris en charge.',
    isOptional: false,
  },
]}
/>

## Valeur renvoyée {/* #returns */}

Renvoie un tableau d’objets `UIMessage` AI SDK V5+ présentant la structure suivante :

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique du message.',
  },
  {
    name: 'role',
    type: "'user' | 'assistant' | 'system'",
    description: 'Rôle de l’expéditeur du message.',
  },
  {
    name: 'parts',
    type: 'UIMessagePart[]',
    description: 'Tableau d’éléments d’interface utilisateur comprenant du texte, des résultats de Tools, des fichiers, du raisonnement, des sources et des marqueurs d’étape.',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'Métadonnées facultatives comprenant createdAt, threadId, resourceId et des champs personnalisés.',
    isOptional: true,
  },
]}
/>

## Exemples {/* #examples */}

### Convertir des messages texte {/* #converting-text-messages */}

```typescript
import { toAISdkV5Messages } from '@mastra/ai-sdk/ui'

const messages = toAISdkV5Messages(['Hello', 'How can I help you today?'])
// Returns array of UIMessage objects with user role
```

### Charger des messages avec le client Mastra {/* #loading-messages-with-mastra-client */}

```typescript
import { MastraClient } from '@mastra/client-js'
import { toAISdkV5Messages } from '@mastra/ai-sdk/ui'

const client = new MastraClient()

const { messages } = await client.listThreadMessages('thread-id', { agentId: 'myAgent' })
const uiMessages = toAISdkV5Messages(messages)
```
