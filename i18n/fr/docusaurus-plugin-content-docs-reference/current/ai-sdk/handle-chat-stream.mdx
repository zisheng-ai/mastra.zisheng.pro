---
title: "Référence : handleChatStream() | AI SDK"
description: Référence de l'API handleChatStream(), un gestionnaire indépendant de tout framework permettant de diffuser en streaming les conversations d'un Agent dans un format compatible avec AI SDK.
packages:
  - "@mastra/ai-sdk"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# handleChatStream()

Gestionnaire indépendant de tout framework permettant de diffuser en streaming les conversations d'un Agent dans un format compatible avec AI SDK. Utilisez cette fonction directement lorsque vous devez gérer le streaming d'une conversation en dehors de Hono ou de la fonctionnalité [apiRoutes](/docs/server/custom-api-routes) de Mastra.

`handleChatStream()` renvoie un `ReadableStream` que vous pouvez encapsuler avec [`createUIMessageStreamResponse()`](https://ai-sdk.dev/docs/reference/ai-sdk-ui/create-ui-message-stream-response).

`handleChatStream()` conserve le comportement existant par défaut d'AI SDK v5. Si votre application est typée pour AI SDK v6, transmettez `version: 'v6'`.

Utilisez [`chatRoute()`](/reference/ai-sdk/chat-route) si vous souhaitez créer une route de conversation dans un serveur Mastra.

## Sortie structurée dans les flux d'interface utilisateur {/* #structured-output-in-ui-streams */}

Lorsque vous transmettez `structuredOutput` à l'exécution sous-jacente de l'Agent, l'objet de sortie structurée final est émis dans le flux d'interface utilisateur compatible avec AI SDK sous la forme d'une partie de données personnalisée :

```json
{
  "type": "data-structured-output",
  "data": {
    "object": {}
  }
}
```

Le champ `object` contient la valeur complète de votre sortie structurée. Mastra émet cet événement uniquement pour l'objet de sortie structurée final. Les segments partiels de la sortie structurée ne sont pas exposés dans le flux d'interface utilisateur.

Lisez cet événement au moyen de la gestion des données personnalisées d'AI SDK UI, par exemple avec `onData`, ou affichez-le à partir des parties de données du message.

## Exemple d'utilisation {/* #usage-example */}

Exemple avec l'App Router de Next.js :

```typescript title="app/api/chat/route.ts"
import { handleChatStream } from '@mastra/ai-sdk'
import { createUIMessageStreamResponse } from 'ai'
import { mastra } from '@/src/mastra'

export async function POST(req: Request) {
  const params = await req.json()
  const stream = await handleChatStream({
    mastra,
    agentId: 'weatherAgent',
    params,
    messageMetadata: () => ({ createdAt: new Date().toISOString() }),
  })
  return createUIMessageStreamResponse({ stream })
}
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'version',
    type: "'v5' | 'v6'",
    description:
      "Sélectionne le contrat de flux AI SDK à émettre. Omettez cette option ou transmettez `'v5'` pour conserver le comportement par défaut existant. Transmettez `'v6'` lorsque votre application est typée pour les fonctions utilitaires de réponse d'AI SDK v6.",
    isOptional: true,
    defaultValue: "'v5'",
  },
  {
    name: 'mastra',
    type: 'Mastra',
    description: 'Instance Mastra contenant les Agents enregistrés.',
    isOptional: false,
  },
  {
    name: 'agentId',
    type: 'string',
    description: "Identifiant de l'Agent à utiliser pour la conversation.",
    isOptional: false,
  },
  {
    name: 'agentVersion',
    type: "{ versionId: string } | { status?: 'draft' | 'published' }",
    description:
      "Sélectionne une version précise de l'Agent. Transmettez `{ versionId: '<id>' }` pour cibler une version exacte, ou `{ status: 'draft' }` / `{ status: 'published' }` pour effectuer la résolution selon le statut. Nécessite la configuration de l'[Editor](/docs/editor/overview).",
    isOptional: true,
  },
  {
    name: 'params',
    type: 'ChatStreamHandlerParams',
    description: 'Paramètres du flux de conversation, notamment les messages et les données de reprise facultatives.',
    isOptional: false,
  },
  {
    name: 'params.messages',
    type: 'UIMessage[]',
    description: 'Tableau des messages de la conversation.',
    isOptional: false,
  },
  {
    name: 'params.resumeData',
    type: 'Record<string, any>',
    description: "Données permettant de reprendre l'exécution suspendue d'un Agent. Nécessite la définition de `runId`.",
    isOptional: true,
  },
  {
    name: 'params.runId',
    type: 'string',
    description: "Identifiant de l'exécution. Obligatoire lorsque `resumeData` est fourni.",
    isOptional: true,
  },
  {
    name: 'params.providerOptions',
    type: 'Record<string, Record<string, unknown>>',
    description:
      'Options propres au Provider transmises au modèle de langage (par exemple, `{ openai: { reasoningEffort: "high" } }`). Elles sont fusionnées avec `defaultOptions.providerOptions`, les valeurs de `params` étant prioritaires.',
    isOptional: true,
  },
  {
    name: 'params.requestContext',
    type: 'RequestContext',
    description: "Contexte de requête à transmettre à l'exécution de l'Agent.",
    isOptional: true,
  },
  {
    name: 'defaultOptions',
    type: 'AgentExecutionOptions',
    description:
      "Options par défaut transmises à l'exécution de l'Agent. Elles sont fusionnées avec params, les valeurs de params étant prioritaires.",
    isOptional: true,
  },
  {
    name: 'sendStart',
    type: 'boolean',
    description: 'Indique si les événements de début doivent être envoyés dans le flux.',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'sendFinish',
    type: 'boolean',
    description: 'Indique si les événements de fin doivent être envoyés dans le flux.',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'sendReasoning',
    type: 'boolean',
    description: 'Indique si les étapes de raisonnement doivent être incluses dans le flux.',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'sendSources',
    type: 'boolean',
    description: 'Indique si les citations des sources doivent être incluses dans le flux.',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'onError',
    type: '(error: unknown) => string',
    description:
      "Appelée lorsque le flux rencontre une erreur. Renvoyez la chaîne qui sera envoyée au client comme message d'erreur. Utilisez cette fonction pour nettoyer les erreurs avant qu'elles n'atteignent le client, par exemple pour empêcher la divulgation de détails sur l'infrastructure interne aux utilisateurs finaux.",
    isOptional: true,
  },
  {
    name: 'messageMetadata',
    type: '(options: { part: UIMessageStreamPart }) => Record<string, unknown> | undefined',
    description:
      "Fonction qui reçoit la partie actuelle du flux et renvoie les métadonnées à joindre aux segments de début et de fin. Pour en savoir plus, consultez la [documentation d'AI SDK sur les métadonnées des messages](https://ai-sdk.dev/docs/ai-sdk-ui/message-metadata).",
    isOptional: true,
  },
]}
/>
