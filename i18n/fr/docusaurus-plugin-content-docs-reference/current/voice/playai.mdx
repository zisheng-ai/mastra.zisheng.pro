---
title: "Référence : PlayAI | Voice"
description: "Documentation de l'implémentation vocale PlayAI, qui fournit des fonctionnalités de synthèse vocale."
packages:
  - "@mastra/voice-playai"
---

# PlayAI

L'implémentation vocale PlayAI de Mastra fournit des fonctionnalités de synthèse vocale au moyen de l'API PlayAI.

## Exemple d'utilisation {/* #usage-example */}

```typescript
import { PlayAIVoice } from '@mastra/voice-playai'

// Initialize with default configuration (uses PLAYAI_API_KEY environment variable and PLAYAI_USER_ID environment variable)
const voice = new PlayAIVoice()

// Initialize with default configuration
const voice = new PlayAIVoice({
  speechModel: {
    name: 'PlayDialog',
    apiKey: process.env.PLAYAI_API_KEY,
    userId: process.env.PLAYAI_USER_ID,
  },
  speaker: 'Angelo', // Default voice
})

// Convert text to speech with a specific voice
const audioStream = await voice.speak('Hello, world!', {
  speaker:
    's3://voice-cloning-zero-shot/b27bc13e-996f-4841-b584-4d35801aea98/original/manifest.json', // Dexter voice
})
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'PlayAIConfig',
    description: 'Configuration de la fonctionnalité de synthèse vocale',
    isOptional: true,
    defaultValue: "{ name: 'PlayDialog' }",
    properties: [
      {
        type: 'PlayAIConfig',
        parameters: [
          {
            name: 'name',
            type: "'PlayDialog' | 'Play3.0-mini'",
            description: 'Modèle PlayAI à utiliser',
            isOptional: true,
            defaultValue: "'PlayDialog'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: "Clé d'API PlayAI. Utilise par défaut la variable d'environnement PLAYAI_API_KEY",
            isOptional: true,
          },
          {
            name: 'userId',
            type: 'string',
            description: "Identifiant d'utilisateur PlayAI. Utilise par défaut la variable d'environnement PLAYAI_USER_ID",
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: 'Identifiant vocal à utiliser par défaut pour la synthèse vocale',
    isOptional: true,
    defaultValue: 'Premier identifiant vocal disponible',
  },
]}
/>

## Méthodes {/* #methods */}

### `speak()` {/* #speak */}

Convertit du texte en parole au moyen du modèle vocal et de la voix configurés.

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: "Texte à convertir en parole. Si un flux est fourni, il est d'abord converti en texte.",
    isOptional: false,
  },
  {
    name: 'options',
    type: 'Options',
    description: 'Options de configuration.',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: 'Remplace la voix par défaut pour cette requête',
            isOptional: true,
            defaultValue: 'Valeur speaker du constructeur',
          },
        ],
      },
    ],
  },
]}
/>

Renvoie : `Promise<NodeJS.ReadableStream>`.

### `getSpeakers()` {/* #getspeakers */}

Renvoie un tableau des options vocales disponibles, dans lequel chaque élément contient :

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: 'Nom de la voix',
    isOptional: false,
  },
  {
    name: 'accent',
    type: 'string',
    description: "Accent de la voix (par exemple, 'US', 'British', 'Australian')",
    isOptional: false,
  },
  {
    name: 'gender',
    type: "'M' | 'F'",
    description: 'Genre de la voix',
    isOptional: false,
  },
  {
    name: 'age',
    type: "'Young' | 'Middle' | 'Old'",
    description: "Catégorie d'âge de la voix",
    isOptional: false,
  },
  {
    name: 'style',
    type: "'Conversational' | 'Narrative'",
    description: "Style d'élocution de la voix",
    isOptional: false,
  },
  {
    name: 'voiceId',
    type: 'string',
    description: 'Identifiant unique de la voix',
    isOptional: false,
  },
]}
/>

### `listen()` {/* #listen */}

Cette méthode n'est pas prise en charge par PlayAI et lève une erreur. PlayAI ne fournit aucune fonctionnalité de reconnaissance vocale.

## Remarques {/* #notes */}

- PlayAI nécessite à la fois une clé d'API et un identifiant d'utilisateur pour l'authentification
- Le service propose deux modèles : 'PlayDialog' et 'Play3.0-mini'
- Chaque voix possède un identifiant de manifeste S3 unique qui doit être utilisé lors des appels à l'API
