---
title: "Référence : Inworld | Voice"
description: "Documentation de la classe InworldVoice, qui fournit des fonctionnalités de synthèse vocale en streaming et de transcription vocale par lots avec Inworld AI."
packages:
  - "@mastra/voice-inworld"
---

# Inworld

L'implémentation vocale Inworld dans Mastra fournit des fonctionnalités de synthèse vocale (TTS) en streaming et de transcription vocale (STT) par lots à l'aide de l'API Inworld AI. Elle prend en charge plusieurs modèles TTS et STT, des encodages audio configurables et le streaming audio progressif.

Pour les échanges vocaux bidirectionnels simultanés en temps réel, le même package exporte [`InworldRealtimeVoice`](/reference/voice/inworld-realtime).

## Exemple d'utilisation {/* #usage-example */}

```typescript
import { InworldVoice } from '@mastra/voice-inworld'

// Initialize with default configuration (uses INWORLD_API_KEY environment variable)
const voice = new InworldVoice()

// Initialize with custom configuration
const voice = new InworldVoice({
  speechModel: {
    name: 'inworld-tts-2',
    apiKey: 'your-api-key',
  },
  listeningModel: {
    name: 'groq/whisper-large-v3',
    apiKey: 'your-api-key',
  },
  speaker: 'Dennis',
})

// Text-to-Speech (streaming)
const audioStream = await voice.speak('Hello, world!')

// Speech-to-Text
const transcript = await voice.listen(audioStream)
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'InworldVoiceConfig',
    description: 'Configuration de la fonctionnalité de synthèse vocale.',
    isOptional: true,
    defaultValue: "{ name: 'inworld-tts-2' }",
    properties: [
      {
        type: 'InworldVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: "'inworld-tts-2' | 'inworld-tts-1.5-max' | 'inworld-tts-1.5-mini'",
            description: 'Modèle TTS Inworld à utiliser.',
            isOptional: true,
            defaultValue: "'inworld-tts-2'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Clé API Inworld. Utilise par défaut la variable d’environnement INWORLD_API_KEY.',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'listeningModel',
    type: 'InworldListeningConfig',
    description: 'Configuration de la fonctionnalité de transcription vocale.',
    isOptional: true,
    defaultValue: "{ name: 'groq/whisper-large-v3' }",
    properties: [
      {
        type: 'InworldListeningConfig',
        parameters: [
          {
            name: 'name',
            type: "'groq/whisper-large-v3'",
            description: 'Modèle STT Inworld à utiliser.',
            isOptional: true,
            defaultValue: "'groq/whisper-large-v3'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Clé API Inworld. Utilise par défaut la variable d’environnement INWORLD_API_KEY.',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: 'ID de voix par défaut à utiliser pour la synthèse vocale.',
    isOptional: true,
    defaultValue: "'Dennis'",
  },
  {
    name: 'audioEncoding',
    type: "'LINEAR16' | 'MP3' | 'OGG_OPUS' | 'ALAW' | 'MULAW' | 'FLAC' | 'PCM' | 'WAV'",
    description: 'Encodage audio par défaut de la sortie TTS.',
    isOptional: true,
    defaultValue: "'MP3'",
  },
  {
    name: 'sampleRateHertz',
    type: 'number',
    description: 'Fréquence d’échantillonnage par défaut de la sortie TTS.',
    isOptional: true,
    defaultValue: '48000',
  },
  {
    name: 'language',
    type: 'string',
    description: 'Code de langue BCP-47 par défaut pour la STT.',
    isOptional: true,
    defaultValue: "'en-US'",
  },
]}
/>

## Méthodes {/* #methods */}

### `speak(input, options?)` {/* #speakinput-options */}

Convertit du texte en parole à l'aide du point de terminaison TTS en streaming d'Inworld. Renvoie un stream lisible qui émet progressivement les segments audio à mesure de leur arrivée.

```typescript
const audioStream = await voice.speak('Hello, world!', {
  speaker: 'Olivia',
  audioEncoding: 'WAV',
  sampleRateHertz: 24000,
  speakingRate: 1.2,
  temperature: 0.8,
})
```

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: "Texte à convertir en parole. Si un stream est fourni, il sera d'abord converti en texte.",
    isOptional: false,
  },
  {
    name: 'options',
    type: 'InworldSpeakOptions',
    description: 'Options supplémentaires de synthèse vocale.',
    isOptional: true,
    properties: [
      {
        type: 'InworldSpeakOptions',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: 'Remplace le locuteur par défaut pour cette requête.',
            isOptional: true,
          },
          {
            name: 'audioEncoding',
            type: 'AudioEncoding',
            description: 'Remplace l’encodage audio par défaut.',
            isOptional: true,
          },
          {
            name: 'sampleRateHertz',
            type: 'number',
            description: 'Remplace la fréquence d’échantillonnage par défaut.',
            isOptional: true,
          },
          {
            name: 'speakingRate',
            type: 'number',
            description: 'Règle la vitesse de parole.',
            isOptional: true,
          },
          {
            name: 'temperature',
            type: 'number',
            description:
              'Contrôle la variabilité de la voix. Pris en compte par les modèles `inworld-tts-1.5-*` ; ignoré par `inworld-tts-2`.',
            isOptional: true,
          },
          {
            name: 'deliveryMode',
            type: "'STABLE' | 'BALANCED' | 'CREATIVE'",
            description: 'Contrôle d’orientation du style d’élocution. Pris en compte uniquement par `inworld-tts-2`.',
            isOptional: true,
          },
          {
            name: 'language',
            type: 'string',
            description: 'Code de langue BCP-47 de cette requête. Détecté automatiquement s’il est omis.',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

**Renvoie :** `Promise<NodeJS.ReadableStream>`

### `listen(input, options?)` {/* #listeninput-options */}

Convertit la parole en texte à l'aide du point de terminaison STT par lots d'Inworld.

```typescript
const transcript = await voice.listen(audioStream, {
  audioEncoding: 'MP3',
  sampleRateHertz: 44100,
  language: 'ja-JP',
})
```

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'NodeJS.ReadableStream',
    description: 'Stream audio à transcrire.',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'InworldListenOptions',
    description: 'Options supplémentaires de transcription.',
    isOptional: true,
    properties: [
      {
        type: 'InworldListenOptions',
        parameters: [
          {
            name: 'audioEncoding',
            type: "'LINEAR16' | 'MP3' | 'OGG_OPUS' | 'FLAC' | 'AUTO_DETECT'",
            description: 'Encodage audio du stream d’entrée.',
            isOptional: true,
            defaultValue: "'AUTO_DETECT'",
          },
          {
            name: 'sampleRateHertz',
            type: 'number',
            description: 'Fréquence d’échantillonnage de l’audio d’entrée.',
            isOptional: true,
            defaultValue: '16000',
          },
          {
            name: 'language',
            type: 'string',
            description: 'Code de langue BCP-47 de la transcription.',
            isOptional: true,
            defaultValue: "'en-US'",
          },
          {
            name: 'numberOfChannels',
            type: 'number',
            description: 'Nombre de canaux audio dans l’entrée.',
            isOptional: true,
            defaultValue: '1',
          },
        ],
      },
    ],
  },
]}
/>

**Renvoie :** `Promise<string>`

### `getSpeakers()` {/* #getspeakers */}

Renvoie la liste des voix disponibles dans l'API Inworld.

```typescript
const speakers = await voice.getSpeakers()
// [{ voiceId: 'Dennis', name: 'Dennis', language: 'en', description: '...', tags: ['friendly'], source: 'SYSTEM' }, ...]
```

**Renvoie :** `Promise<Array<{ voiceId: string; name: string; language: string; description: string; tags: string[]; source: string }>>`

## Remarques {/* #notes */}

- Le point de terminaison TTS utilise le streaming NDJSON progressif ; la lecture audio peut donc commencer avant la réception de la réponse complète.
- Une clé API peut être fournie dans la configuration `speechModel` ou `listeningModel`, ou avec la variable d'environnement `INWORLD_API_KEY`. Les clés TTS et STT sont résolues indépendamment : transmettre des valeurs `speechModel.apiKey` et `listeningModel.apiKey` distinctes permet à chaque service d'utiliser ses propres identifiants. Si une seule clé est fournie, elle est réutilisée pour les deux services comme valeur de repli avant la variable d'environnement.
- `inworld-tts-2` est le modèle phare par défaut. Utilisez `deliveryMode` (`STABLE` | `BALANCED` | `CREATIVE`) pour orienter le style d'élocution de ce modèle. L'option `temperature` est ignorée sur `inworld-tts-2`.
- Le modèle `inworld-tts-1.5-mini` offre une latence plus faible au prix d'une qualité vocale inférieure à celle de `inworld-tts-1.5-max`.
