---
title: "Référence : Deepgram | Voice"
description: "Documentation de l'implémentation vocale Deepgram, qui fournit des fonctionnalités de synthèse et de reconnaissance vocales avec plusieurs modèles vocaux et langues."
packages:
  - "@mastra/voice-deepgram"
---

# Deepgram

L'implémentation vocale Deepgram de Mastra fournit des fonctionnalités de synthèse vocale (TTS) et de reconnaissance vocale (STT) au moyen de l'API Deepgram. Elle prend en charge plusieurs modèles vocaux et plusieurs langues, avec des options configurables pour la synthèse vocale et la transcription.

## Exemple d'utilisation {/* #usage-example */}

```typescript
import { DeepgramVoice } from '@mastra/voice-deepgram'

// Initialize with default configuration (uses DEEPGRAM_API_KEY environment variable)
const voice = new DeepgramVoice()

// Initialize with custom configuration
const voice = new DeepgramVoice({
  speechModel: {
    name: 'aura',
    apiKey: 'your-api-key',
  },
  listeningModel: {
    name: 'nova-2',
    apiKey: 'your-api-key',
  },
  speaker: 'asteria-en',
})

// Text-to-Speech
const audioStream = await voice.speak('Hello, world!')

// Speech-to-Text
const transcript = await voice.listen(audioStream)
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'DeepgramVoiceConfig',
    description: 'Configuration de la fonctionnalité de synthèse vocale.',
    isOptional: true,
    defaultValue: "{ name: 'aura' }",
    properties: [
      {
        type: 'DeepgramVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'DeepgramModel',
            description: 'Modèle Deepgram à utiliser',
            isOptional: true,
          },
          {
            name: 'apiKey',
            type: 'string',
            description: "Clé d'API Deepgram. Utilise par défaut la variable d'environnement DEEPGRAM_API_KEY",
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'Record<string, any>',
            description: "Propriétés supplémentaires à transmettre à l'API Deepgram",
            isOptional: true,
          },
          {
            name: 'language',
            type: 'string',
            description: 'Code de langue du modèle',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'listeningModel',
    type: 'DeepgramVoiceConfig',
    description: 'Configuration de la fonctionnalité de reconnaissance vocale.',
    isOptional: true,
    defaultValue: "{ name: 'nova' }",
    properties: [
      {
        type: 'DeepgramVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'DeepgramModel',
            description: 'Modèle Deepgram à utiliser',
            isOptional: true,
          },
          {
            name: 'apiKey',
            type: 'string',
            description: "Clé d'API Deepgram. Utilise par défaut la variable d'environnement DEEPGRAM_API_KEY",
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'Record<string, any>',
            description: "Propriétés supplémentaires à transmettre à l'API Deepgram",
            isOptional: true,
          },
          {
            name: 'language',
            type: 'string',
            description: 'Code de langue du modèle',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'DeepgramVoiceId',
    description: 'Voix à utiliser par défaut pour la synthèse vocale',
    isOptional: true,
    defaultValue: "'asteria-en'",
  },
]}
/>

## Méthodes {/* #methods */}

### `speak()` {/* #speak */}

Convertit du texte en parole au moyen du modèle vocal et de la voix configurés.

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: "Texte à convertir en parole. Si un flux est fourni, il est d'abord converti en texte.",
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: 'Options supplémentaires pour la synthèse vocale',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: 'Remplace la voix par défaut pour cette requête',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

Renvoie : `Promise<NodeJS.ReadableStream>`

### `listen()` {/* #listen */}

Convertit la parole en texte au moyen du modèle de reconnaissance configuré.

<PropertiesTable
  content={[
  {
    name: 'audioStream',
    type: 'NodeJS.ReadableStream',
    description: 'Flux audio à transcrire',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: "Options supplémentaires à transmettre à l'API Deepgram",
    isOptional: true,
  },
]}
/>

Renvoie : `Promise<string>`

### `getSpeakers()` {/* #getspeakers */}

Renvoie la liste des options vocales disponibles.

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: 'Identifiant unique de la voix',
    isOptional: false,
  },
]}
/>
