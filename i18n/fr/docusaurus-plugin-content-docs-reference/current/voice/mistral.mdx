---
title: "Référence : Mistral | Voice"
description: "Référence de l'API de MistralVoice, qui fournit des fonctionnalités de synthèse et de reconnaissance vocales avec les modèles Voxtral de Mistral."
packages:
  - "@mastra/voice-mistral"
---

# Mistral

La classe MistralVoice fournit des fonctionnalités de synthèse et de reconnaissance vocales à l'aide des modèles audio Voxtral de Mistral. Elle prend en charge la TTS mise en mémoire tampon et en streaming, la transcription par lots avec diarisation et le clonage de voix à partir d'un audio de référence.

## Exemple d'utilisation {/* #usage-example */}

```typescript
import { MistralVoice } from '@mastra/voice-mistral'

const voice = new MistralVoice()

// Text-to-speech
const audioStream = await voice.speak('Hello, how can I help you?', {
  responseFormat: 'mp3',
})

// Speech-to-text
const text = await voice.listen(audioStream, {
  language: 'en',
})

// List available voices
const speakers = await voice.getSpeakers()
```

```typescript
import { MistralVoice } from '@mastra/voice-mistral'

// Initialize with specific configuration
const voice = new MistralVoice({
  speechModel: {
    name: 'voxtral-mini-tts-2603',
    apiKey: 'your-mistral-api-key',
  },
  listeningModel: {
    name: 'voxtral-mini-latest',
    apiKey: 'your-mistral-api-key',
  },
  speaker: 'en_paul_neutral',
})
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'MistralModelConfig',
    description: 'Configuration de la synthèse vocale.',
    isOptional: true,
    defaultValue: "{ name: 'voxtral-mini-tts-2603' }",
    properties: [
      {
        type: 'MistralModelConfig',
        parameters: [
          {
            name: 'name',
            type: 'string',
            description: 'Identifiant du modèle de synthèse vocale.',
            isOptional: true,
            defaultValue: "'voxtral-mini-tts-2603'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: "Clé API Mistral. Utilise la variable d'environnement MISTRAL_API_KEY à défaut.",
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'listeningModel',
    type: 'MistralModelConfig',
    description: 'Configuration de la reconnaissance vocale.',
    isOptional: true,
    defaultValue: "{ name: 'voxtral-mini-latest' }",
    properties: [
      {
        type: 'MistralModelConfig',
        parameters: [
          {
            name: 'name',
            type: 'string',
            description: "Identifiant du modèle de transcription. Utilisez 'voxtral-mini-2507' pour une version fixe.",
            isOptional: true,
            defaultValue: "'voxtral-mini-latest'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: "Clé API Mistral. Utilise la variable d'environnement MISTRAL_API_KEY à défaut.",
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: 'Identifiant par défaut de la voix pour la synthèse vocale. Récupérez les identifiants disponibles avec getSpeakers().',
    isOptional: true,
    defaultValue: "'en_paul_neutral'",
  },
]}
/>

## Méthodes {/* #methods */}

### `speak(input, options?)` {/* #speakinput-options */}

Convertit du texte en parole à l'aide du modèle TTS Voxtral de Mistral. Prend en charge les sorties mises en mémoire tampon et en streaming.

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: 'Texte ou flux de texte à convertir en parole.',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'MistralSpeakOptions',
    description: 'Options de configuration.',
    isOptional: true,
    properties: [
      {
        type: 'MistralSpeakOptions',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: 'Identifiant de la voix à utiliser. Remplace la valeur par défaut du constructeur.',
            isOptional: true,
          },
          {
            name: 'responseFormat',
            type: "'pcm' | 'wav' | 'mp3' | 'flac' | 'opus'",
            description: "Format de sortie audio. Utilisez 'pcm' pour obtenir la plus faible latence en streaming.",
            isOptional: true,
          },
          {
            name: 'refAudio',
            type: 'string',
            description: 'Audio de référence encodé en base64 pour un clonage ponctuel de la voix (2 à 3 secondes au minimum).',
            isOptional: true,
          },
          {
            name: 'model',
            type: 'string',
            description: 'Remplace le modèle de synthèse vocale pour cet appel.',
            isOptional: true,
          },
          {
            name: 'stream',
            type: 'boolean',
            description: 'Active la TTS en streaming. Les chunks audio sont écrits dans le flux à mesure de leur arrivée.',
            isOptional: true,
            defaultValue: 'false',
          },
        ],
      },
    ],
  },
]}
/>

Renvoie : `Promise<NodeJS.ReadableStream>`

### `listen(audioStream, options?)` {/* #listenaudiostream-options */}

Transcrit un contenu audio à l'aide du modèle de transcription Voxtral de Mistral. Prend en charge la diarisation, le biais contextuel et le niveau de détail des horodatages.

<PropertiesTable
  content={[
  {
    name: 'audioStream',
    type: 'NodeJS.ReadableStream',
    description: 'Flux audio à transcrire.',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'MistralListenOptions',
    description: 'Options de configuration.',
    isOptional: true,
    properties: [
      {
        type: 'MistralListenOptions',
        parameters: [
          {
            name: 'language',
            type: 'string',
            description: "Code de langue (par exemple, 'en'). Améliore la précision lorsqu'il est indiqué.",
            isOptional: true,
          },
          {
            name: 'diarize',
            type: 'boolean',
            description: 'Active la diarisation des locuteurs.',
            isOptional: true,
            defaultValue: 'false',
          },
          {
            name: 'contextBias',
            type: 'string[]',
            description: 'Mots ou expressions destinés à orienter le vocabulaire.',
            isOptional: true,
          },
          {
            name: 'timestampGranularities',
            type: "('segment' | 'word')[]",
            description: "Niveau de détail des horodatages pour les segments de transcription.",
            isOptional: true,
          },
          {
            name: 'filetype',
            type: 'string',
            description: "Indication de l'extension du fichier audio pour le flux d'entrée.",
            isOptional: true,
            defaultValue: "'mp3'",
          },
        ],
      },
    ],
  },
]}
/>

Renvoie : `Promise<string>`

### `getSpeakers()` {/* #getspeakers */}

Récupère les voix prédéfinies disponibles depuis l'API Mistral Voices. Chaque entrée contient :

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: 'Identifiant unique de la voix.',
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom d'affichage de la voix.",
    isOptional: false,
  },
  {
    name: 'languages',
    type: 'string[]',
    description: 'Langues prises en charge par la voix.',
    isOptional: true,
  },
  {
    name: 'gender',
    type: 'string | null',
    description: 'Genre de la voix.',
    isOptional: true,
  },
]}
/>

### `getListener()` {/* #getlistener */}

Renvoie `{ enabled: true }`.

## Remarques {/* #notes */}

- Les clés API peuvent être fournies au moyen des options du constructeur ou de la variable d'environnement `MISTRAL_API_KEY`
- La TTS prend en charge 9 langues : anglais, français, espagnol, portugais, italien, néerlandais, allemand, hindi et arabe
- La STT prend en charge 13 langues : anglais, chinois, hindi, espagnol, arabe, français, portugais, russe, allemand, japonais, coréen, italien et néerlandais
- Pour obtenir les meilleurs résultats, le texte d'entrée de la TTS doit rester inférieur à 300 mots par requête
- La STT prend en charge jusqu'à 3 heures d'audio par requête
- Le clonage de voix au moyen de `refAudio` nécessite au moins 2 à 3 secondes d'audio de référence
- `getSpeakers()` renvoie des voix prédéfinies avec des identifiants UUID. La valeur par défaut `en_paul_neutral` est un alias nommé accepté par l'endpoint TTS, mais elle ne figure pas dans la liste
