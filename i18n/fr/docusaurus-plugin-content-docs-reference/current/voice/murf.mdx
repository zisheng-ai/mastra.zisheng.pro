---
title: "Référence : Murf | Voice"
description: "Documentation de l'implémentation vocale Murf, qui fournit des fonctionnalités de synthèse vocale."
packages:
  - "@mastra/voice-murf"
---

# Murf

L'implémentation vocale Murf dans Mastra fournit des fonctionnalités de synthèse vocale (TTS) à l'aide du service vocal d'IA de Murf. Elle prend en charge plusieurs voix dans différentes langues.

## Exemple d'utilisation {/* #usage-example */}

```typescript
import { MurfVoice } from '@mastra/voice-murf'

// Initialize with default configuration (uses MURF_API_KEY environment variable)
const voice = new MurfVoice()

// Initialize with custom configuration
const voice = new MurfVoice({
  speechModel: {
    name: 'GEN2',
    apiKey: 'your-api-key',
    properties: {
      format: 'MP3',
      rate: 1.0,
      pitch: 1.0,
      sampleRate: 48000,
      channelType: 'STEREO',
    },
  },
  speaker: 'en-US-cooper',
})

// Text-to-Speech with default settings
const audioStream = await voice.speak('Hello, world!')

// Text-to-Speech with custom properties
const audioStream = await voice.speak('Hello, world!', {
  speaker: 'en-UK-hazel',
  properties: {
    format: 'WAV',
    rate: 1.2,
    style: 'casual',
  },
})

// Get available voices
const voices = await voice.getSpeakers()
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'MurfConfig',
    description: 'Configuration de la fonctionnalité de synthèse vocale',
    isOptional: true,
    defaultValue: "{ name: 'GEN2' }",
    properties: [
      {
        type: 'MurfConfig',
        parameters: [
          {
            name: 'name',
            type: "'GEN1' | 'GEN2'",
            description: 'Génération du modèle Murf à utiliser',
            isOptional: false,
            defaultValue: "'GEN2'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: "Clé API Murf. Utilise par défaut la variable d'environnement MURF_API_KEY",
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'object',
            description: 'Propriétés par défaut de toutes les requêtes de synthèse vocale',
            isOptional: true,
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'style',
                    type: 'string',
                    description: "Style d'élocution de la voix",
                    isOptional: true,
                  },
                  {
                    name: 'rate',
                    type: 'number',
                    description: 'Multiplicateur de la vitesse de parole',
                    isOptional: true,
                  },
                  {
                    name: 'pitch',
                    type: 'number',
                    description: 'Réglage de la hauteur de la voix',
                    isOptional: true,
                  },
                  {
                    name: 'sampleRate',
                    type: '8000 | 24000 | 44100 | 48000',
                    description: "Fréquence d'échantillonnage audio en Hz",
                    isOptional: true,
                  },
                  {
                    name: 'format',
                    type: "'MP3' | 'WAV' | 'FLAC' | 'ALAW' | 'ULAW'",
                    description: 'Format audio de sortie',
                    isOptional: true,
                  },
                  {
                    name: 'channelType',
                    type: "'STEREO' | 'MONO'",
                    description: 'Configuration des canaux audio',
                    isOptional: true,
                  },
                  {
                    name: 'pronunciationDictionary',
                    type: 'Record<string, string>',
                    description: 'Correspondances de prononciation personnalisées',
                    isOptional: true,
                  },
                  {
                    name: 'encodeAsBase64',
                    type: 'boolean',
                    description: "Indique s'il faut encoder l'audio en base64",
                    isOptional: true,
                  },
                  {
                    name: 'variation',
                    type: 'number',
                    description: 'Paramètre de variation de la voix',
                    isOptional: true,
                  },
                  {
                    name: 'audioDuration',
                    type: 'number',
                    description: 'Durée audio cible en secondes',
                    isOptional: true,
                  },
                  {
                    name: 'multiNativeLocale',
                    type: 'string',
                    description: 'Locale utilisée pour la prise en charge multilingue',
                    isOptional: true,
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: 'ID de voix par défaut à utiliser pour la synthèse vocale',
    isOptional: true,
    defaultValue: "'en-UK-hazel'",
  },
]}
/>

## Méthodes {/* #methods */}

### `speak()` {/* #speak */}

Convertit du texte en parole à l'aide de l'API Murf.

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: "Texte à convertir en parole. Si un stream est fourni, il sera d'abord converti en texte.",
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: 'Options de synthèse vocale',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: 'Remplace le locuteur par défaut pour cette requête',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'object',
            description: 'Remplace les propriétés vocales par défaut pour cette requête',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

Renvoie : `Promise<NodeJS.ReadableStream>`

### `getSpeakers()` {/* #getspeakers */}

Renvoie un tableau des options vocales disponibles, où chaque élément contient :

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: 'Identifiant unique de la voix',
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom d'affichage de la voix",
    isOptional: false,
  },
  {
    name: 'language',
    type: 'string',
    description: 'Code de langue de la voix',
    isOptional: false,
  },
  {
    name: 'gender',
    type: 'string',
    description: 'Genre de la voix',
    isOptional: false,
  },
]}
/>

### `listen()` {/* #listen */}

Cette méthode n'est pas prise en charge par Murf et lève une erreur. Murf ne fournit aucune fonctionnalité de transcription vocale.

## Remarques importantes {/* #important-notes */}

1. Une clé API Murf est requise. Définissez-la avec la variable d'environnement `MURF_API_KEY` ou transmettez-la au constructeur.
1. Le service utilise GEN2 comme version de modèle par défaut.
1. Les propriétés vocales peuvent être définies au niveau du constructeur et remplacées pour chaque requête.
1. Le service permet de personnaliser finement l'audio grâce à des propriétés comme le format, la fréquence d'échantillonnage et le type de canal.
1. La transcription vocale n'est pas prise en charge.
