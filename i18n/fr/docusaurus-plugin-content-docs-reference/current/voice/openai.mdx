---
title: "Référence : OpenAI | Voix"
description: "Documentation de la classe OpenAIVoice, qui fournit des fonctionnalités de synthèse et de transcription vocales."
packages:
  - "@mastra/voice-openai"
---

# OpenAI

La classe OpenAIVoice de Mastra fournit des fonctionnalités de synthèse et de transcription vocales au moyen des modèles OpenAI.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { OpenAIVoice } from '@mastra/voice-openai'

// Initialize with default configuration using environment variables
const voice = new OpenAIVoice()

// Or initialize with specific configuration
const voiceWithConfig = new OpenAIVoice({
  speechModel: {
    name: 'tts-1-hd',
    apiKey: 'your-openai-api-key',
  },
  listeningModel: {
    name: 'whisper-1',
    apiKey: 'your-openai-api-key',
  },
  speaker: 'alloy', // Default voice
})

// Convert text to speech
const audioStream = await voice.speak('Hello, how can I help you?', {
  speaker: 'nova', // Override default voice
  speed: 1.2, // Adjust speech speed
})

// Convert speech to text
const text = await voice.listen(audioStream, {
  filetype: 'mp3',
})
```

## Configuration {/* #configuration */}

### Options du constructeur {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'OpenAIConfig',
    description: 'Configuration de la synthèse vocale.',
    isOptional: true,
    defaultValue: "{ name: 'tts-1' }",
    properties: [
      {
        type: 'OpenAIConfig',
        parameters: [
          {
            name: 'name',
            type: "'tts-1' | 'tts-1-hd' | 'whisper-1'",
            description: "Nom du modèle. Utilisez 'tts-1-hd' pour obtenir un son de meilleure qualité.",
            isOptional: true,
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Clé d’API OpenAI. Utilise par défaut la variable d’environnement OPENAI_API_KEY.',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'listeningModel',
    type: 'OpenAIConfig',
    description: 'Configuration de la transcription vocale.',
    isOptional: true,
    defaultValue: "{ name: 'whisper-1' }",
    properties: [
      {
        type: 'OpenAIConfig',
        parameters: [
          {
            name: 'name',
            type: "'tts-1' | 'tts-1-hd' | 'whisper-1'",
            description: "Nom du modèle. Utilisez 'tts-1-hd' pour obtenir un son de meilleure qualité.",
            isOptional: true,
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Clé d’API OpenAI. Utilise par défaut la variable d’environnement OPENAI_API_KEY.',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'OpenAIVoiceId',
    description: 'ID de la voix par défaut pour la synthèse vocale.',
    isOptional: true,
    defaultValue: "'alloy'",
  },
]}
/>

## Méthodes {/* #methods */}

### `speak()` {/* #speak */}

Convertit du texte en parole au moyen des modèles de synthèse vocale d’OpenAI.

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: 'Texte ou flux de texte à convertir en parole.',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'Options',
    description: 'Options de configuration.',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'speaker',
            type: 'OpenAIVoiceId',
            description: 'ID de la voix à utiliser pour la synthèse vocale.',
            isOptional: true,
            defaultValue: 'Valeur de speaker du constructeur',
          },
          {
            name: 'speed',
            type: 'number',
            description: 'Multiplicateur de la vitesse d’élocution.',
            isOptional: true,
            defaultValue: '1.0',
          },
        ],
      },
    ],
  },
]}
/>

Renvoie : `Promise<NodeJS.ReadableStream>`

### `listen()` {/* #listen */}

Transcrit le son au moyen du modèle Whisper d’OpenAI.

<PropertiesTable
  content={[
  {
    name: 'audioStream',
    type: 'NodeJS.ReadableStream',
    description: 'Flux audio à transcrire.',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'Options',
    description: 'Options de configuration.',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'filetype',
            type: 'string',
            description: 'Format audio du flux d’entrée.',
            isOptional: true,
            defaultValue: "'mp3'",
          },
        ],
      },
    ],
  },
]}
/>

Renvoie : `Promise<string>`

### `getSpeakers()` {/* #getspeakers */}

Renvoie un tableau des options de voix disponibles, dans lequel chaque élément contient :

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: 'Identifiant unique de la voix',
    isOptional: false,
  },
]}
/>

## Remarques {/* #notes */}

- Les clés d’API peuvent être fournies au moyen des options du constructeur ou de la variable d’environnement `OPENAI_API_KEY`
- Le modèle `tts-1-hd` produit un son de meilleure qualité, mais son traitement peut être plus lent
- La transcription vocale prend en charge plusieurs formats audio, notamment mp3, wav et webm
