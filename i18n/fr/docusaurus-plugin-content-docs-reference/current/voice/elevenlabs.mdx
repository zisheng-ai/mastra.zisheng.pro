---
title: "Référence : ElevenLabs | Voix"
description: "Documentation de l’implémentation vocale d’ElevenLabs, qui offre des fonctionnalités de synthèse vocale de haute qualité avec plusieurs modèles de voix et un rendu naturel."
packages:
  - "@mastra/voice-elevenlabs"
---

# ElevenLabs

L’implémentation vocale d’ElevenLabs dans Mastra fournit des fonctionnalités de synthèse vocale (TTS) et de transcription vocale (STT) de haute qualité au moyen de l’API ElevenLabs.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { ElevenLabsVoice } from '@mastra/voice-elevenlabs'

// Initialize with default configuration (uses ELEVENLABS_API_KEY environment variable)
const voice = new ElevenLabsVoice()

// Initialize with custom configuration
const voice = new ElevenLabsVoice({
  speechModel: {
    name: 'eleven_multilingual_v2',
    apiKey: 'your-api-key',
  },
  speaker: 'custom-speaker-id',
})

// Text-to-Speech
const audioStream = await voice.speak('Hello, world!')

// Get available speakers
const speakers = await voice.getSpeakers()
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'ElevenLabsVoiceConfig',
    description: 'Configuration de la fonctionnalité de synthèse vocale.',
    isOptional: true,
    defaultValue: "{ name: 'eleven_multilingual_v2' }",
    properties: [
      {
        type: 'ElevenLabsVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'ElevenLabsModel',
            description: 'Modèle ElevenLabs à utiliser',
            isOptional: true,
            defaultValue: "'eleven_multilingual_v2'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Clé d’API ElevenLabs. Utilise par défaut la variable d’environnement ELEVENLABS_API_KEY',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: 'ID de la voix à utiliser pour la synthèse vocale',
    isOptional: true,
    defaultValue: "'9BWtsMINqrJLrRacOk9x' (voix Aria)",
  },
]}
/>

## Méthodes {/* #methods */}

### `speak()` {/* #speak */}

Convertit du texte en parole à l’aide du modèle vocal et de la voix configurés.

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: 'Texte à convertir en parole. Si un flux est fourni, il est d’abord converti en texte.',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: 'Options supplémentaires de synthèse vocale',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: 'Remplace l’ID de la voix par défaut pour cette requête',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

Renvoie : `Promise<NodeJS.ReadableStream>`

### `getSpeakers()` {/* #getspeakers */}

Renvoie un tableau des options de voix disponibles, dans lequel chaque élément contient :

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: 'Identifiant unique de la voix',
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: 'Nom d’affichage de la voix',
    isOptional: false,
  },
  {
    name: 'language',
    type: 'string',
    description: 'Code de langue de la voix',
    isOptional: false,
  },
  {
    name: 'gender',
    type: 'string',
    description: 'Genre de la voix',
    isOptional: false,
  },
]}
/>

### `listen()` {/* #listen */}

Convertit une entrée audio en texte au moyen de l’API Speech-to-Text d’ElevenLabs.

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'NodeJS.ReadableStream',
    description: 'Flux lisible contenant les données audio à transcrire',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: 'Options de configuration de la transcription',
    isOptional: true,
  },
]}
/>

L’objet d’options prend en charge les propriétés suivantes :

<PropertiesTable
  content={[
  {
    name: 'language_code',
    type: 'string',
    description: "Code de langue ISO (par exemple, 'en', 'fr', 'es')",
    isOptional: true,
  },
  {
    name: 'tag_audio_events',
    type: 'boolean',
    description: 'Indique si les événements audio tels que [MUSIC], [LAUGHTER], etc. doivent être balisés',
    isOptional: true,
  },
  {
    name: 'num_speakers',
    type: 'number',
    description: 'Nombre de voix à détecter dans le son',
    isOptional: true,
  },
  {
    name: 'filetype',
    type: 'string',
    description: "Format du fichier audio (par exemple, 'mp3', 'wav', 'ogg')",
    isOptional: true,
  },
  {
    name: 'timeoutInSeconds',
    type: 'number',
    description: 'Délai d’expiration de la requête, en secondes',
    isOptional: true,
  },
  {
    name: 'maxRetries',
    type: 'number',
    description: 'Nombre maximal de nouvelles tentatives',
    isOptional: true,
  },
  {
    name: 'abortSignal',
    type: 'AbortSignal',
    description: 'Signal permettant d’interrompre la requête',
    isOptional: true,
  },
]}
/>

Renvoie : `Promise<string>` - Promise résolue avec le texte transcrit

## Remarques importantes {/* #important-notes */}

1. Une clé d’API ElevenLabs est requise. Définissez-la au moyen de la variable d’environnement `ELEVENLABS_API_KEY` ou transmettez-la au constructeur.
1. La voix par défaut est Aria (ID : '9BWtsMINqrJLrRacOk9x').
1. La fonctionnalité de transcription vocale n’est pas prise en charge par ElevenLabs.
1. Les voix disponibles peuvent être récupérées à l’aide de la méthode `getSpeakers()`, qui renvoie des informations détaillées sur chacune d’elles, notamment sa langue et son genre.
