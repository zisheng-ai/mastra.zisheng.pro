---
title: "Référence : voix OpenAI Realtime | Voice"
description: "Documentation de la classe OpenAIRealtimeVoice, qui fournit des fonctionnalités de synthèse et de reconnaissance vocales en temps réel via WebSockets."
packages:
  - "@mastra/node-audio"
  - "@mastra/voice-openai-realtime"
---

# Voix OpenAI Realtime

La classe OpenAIRealtimeVoice fournit des fonctionnalités d'interaction vocale en temps réel au moyen de l'API OpenAI fondée sur WebSocket. Elle prend en charge les échanges vocaux en temps réel, la détection d'activité vocale et le streaming audio fondé sur des événements.

## Exemple d'utilisation {/* #usage-example */}

```typescript
import { OpenAIRealtimeVoice } from '@mastra/voice-openai-realtime'
import { playAudio, getMicrophoneStream } from '@mastra/node-audio'

// Initialize with default configuration using environment variables
const voice = new OpenAIRealtimeVoice()

// Or initialize with specific configuration
const voiceWithConfig = new OpenAIRealtimeVoice({
  apiKey: 'your-openai-api-key',
  model: 'gpt-5.1-realtime-preview-2024-12-17',
  speaker: 'alloy', // Default voice
})

voiceWithConfig.updateSession({
  turn_detection: {
    type: 'server_vad',
    threshold: 0.6,
    silence_duration_ms: 1200,
  },
})

// Establish connection
await voice.connect()

// Set up event listeners
voice.on('speaker', ({ audio }) => {
  // Handle audio data (Int16Array) pcm format by default
  playAudio(audio)
})

voice.on('writing', ({ text, role }) => {
  // Handle transcribed text
  console.log(`${role}: ${text}`)
})

// Convert text to speech
await voice.speak('Hello, how can I help you today?', {
  speaker: 'echo', // Override default voice
})

// Process audio input
const microphoneStream = getMicrophoneStream()
await voice.send(microphoneStream)

// When done, disconnect
voice.connect()
```

## Configuration {/* #configuration */}

### Options du constructeur {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'model',
    type: 'string',
    description: "Identifiant du modèle à utiliser pour les interactions vocales en temps réel.",
    isOptional: true,
    defaultValue: "'gpt-5.1-realtime-preview-2024-12-17'",
  },
  {
    name: 'apiKey',
    type: 'string',
    description: "Clé d'API OpenAI. Utilise la variable d'environnement OPENAI_API_KEY comme valeur de repli.",
    isOptional: true,
  },
  {
    name: 'speaker',
    type: 'string',
    description: "Identifiant de voix par défaut pour la synthèse vocale.",
    isOptional: true,
    defaultValue: "'alloy'",
  },
]}
/>

### Configuration de la détection d'activité vocale (VAD) {/* #voice-activity-detection-vad-configuration */}

<PropertiesTable
  content={[
  {
    name: 'type',
    type: 'string',
    description: 'Type de VAD à utiliser. La VAD côté serveur offre une meilleure précision.',
    isOptional: true,
    defaultValue: "'server_vad'",
  },
  {
    name: 'threshold',
    type: 'number',
    description: 'Sensibilité de la détection vocale (0.0-1.0).',
    isOptional: true,
    defaultValue: '0.5',
  },
  {
    name: 'prefix_padding_ms',
    type: 'number',
    description: "Nombre de millisecondes d'audio à inclure avant la détection de la parole.",
    isOptional: true,
    defaultValue: '1000',
  },
  {
    name: 'silence_duration_ms',
    type: 'number',
    description: "Nombre de millisecondes de silence avant de terminer un tour de parole.",
    isOptional: true,
    defaultValue: '1000',
  },
]}
/>

## Méthodes {/* #methods */}

### `connect()` {/* #connect */}

Établit une connexion au service OpenAI Realtime. Cette méthode doit être appelée avant d'utiliser les fonctions speak, listen ou send.

<PropertiesTable
  content={[
  {
    name: 'returns',
    type: 'Promise<void>',
    description: 'Promise résolue une fois la connexion établie.',
  },
]}
/>

### `speak()` {/* #speak */}

Émet un événement speaking au moyen du modèle vocal configuré. Accepte en entrée une chaîne ou un stream lisible.

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: 'Texte ou stream de texte à convertir en parole.',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'Options',
    description: 'Options de configuration.',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: 'Identifiant de voix à utiliser pour cette requête vocale précise.',
            isOptional: true,
            defaultValue: "Valeur speaker du constructeur",
          },
        ],
      },
    ],
  },
]}
/>

Renvoie : `Promise<void>`

### `listen()` {/* #listen */}

Traite une entrée audio pour la reconnaissance vocale. Accepte un stream lisible de données audio et émet un événement 'listening' contenant le texte transcrit.

<PropertiesTable
  content={[
  {
    name: 'audioData',
    type: 'NodeJS.ReadableStream',
    description: 'Stream audio à transcrire.',
    isOptional: false,
  },
]}
/>

Renvoie : `Promise<void>`

### `send()` {/* #send */}

Diffuse les données audio en temps réel vers le service OpenAI pour des scénarios de streaming audio continu, tels que l'entrée d'un microphone en direct.

<PropertiesTable
  content={[
  {
    name: 'audioData',
    type: 'NodeJS.ReadableStream',
    description: 'Stream audio à envoyer au service.',
    isOptional: false,
  },
]}
/>

Renvoie : `Promise<void>`

### `updateConfig()` {/* #updateconfig */}

Met à jour la configuration de session de l'instance Voice. Cette méthode permet de modifier les paramètres de voix, la détection des tours de parole et d'autres paramètres.

<PropertiesTable
  content={[
  {
    name: 'sessionConfig',
    type: 'Realtime.SessionConfig',
    description: 'Nouvelle configuration de session à appliquer.',
    isOptional: false,
  },
]}
/>

Renvoie : `void`

### `addTools()` {/* #addtools */}

Ajoute un ensemble de Tools à l'instance Voice. Les Tools permettent au modèle d'effectuer des actions supplémentaires pendant les conversations. Lorsque OpenAIRealtimeVoice est ajouté à un Agent, tous les Tools configurés pour cet Agent sont automatiquement accessibles à l'interface Voice.

<PropertiesTable
  content={[
  {
    name: 'tools',
    type: 'ToolsInput',
    description: 'Configuration des Tools à fournir.',
    isOptional: true,
  },
]}
/>

Renvoie : `void`

### `close()` {/* #close */}

Se déconnecte de la session OpenAI Realtime et libère les ressources. Cette méthode doit être appelée lorsque vous avez terminé d'utiliser l'instance Voice.

Renvoie : `void`

### `getSpeakers()` {/* #getspeakers */}

Renvoie la liste des voix disponibles.

Renvoie : `Promise<Array<{ voiceId: string; [key: string]: any }>>`

### `on()` {/* #on */}

Enregistre un écouteur d'événements Voice.

<PropertiesTable
  content={[
  {
    name: 'event',
    type: 'string',
    description: "Nom de l'événement à écouter.",
    isOptional: false,
  },
  {
    name: 'callback',
    type: 'Function',
    description: "Fonction à appeler lorsque l'événement se produit.",
    isOptional: false,
  },
]}
/>

Renvoie : `void`

### `off()` {/* #off */}

Supprime un écouteur d'événements précédemment enregistré.

<PropertiesTable
  content={[
  {
    name: 'event',
    type: 'string',
    description: "Nom de l'événement à ne plus écouter.",
    isOptional: false,
  },
  {
    name: 'callback',
    type: 'Function',
    description: 'Fonction de rappel précise à supprimer.',
    isOptional: false,
  },
]}
/>

Renvoie : `void`

## Événements {/* #events */}

La classe OpenAIRealtimeVoice émet les événements suivants :

<PropertiesTable
  content={[
  {
    name: 'speaking',
    type: 'event',
    description: 'Émis lorsque des données audio sont reçues du modèle. Le callback reçoit { audio: Int16Array }.',
  },
  {
    name: 'writing',
    type: 'event',
    description: 'Émis lorsque le texte transcrit est disponible. Le callback reçoit { text: string, role: string }.',
  },
  {
    name: 'error',
    type: 'event',
    description: "Émis lorsqu'une erreur se produit. Le callback reçoit l'objet d'erreur.",
  },
]}
/>

### Événements OpenAI Realtime {/* #openai-realtime-events */}

Vous pouvez également écouter les [événements utilitaires OpenAI Realtime](https://github.com/openai/openai-realtime-api-beta#reference-client-utility-events) en leur ajoutant le préfixe 'openAIRealtime:' :

<PropertiesTable
  content={[
  {
    name: 'openAIRealtime:conversation.created',
    type: 'event',
    description: "Émis lorsqu'une nouvelle conversation est créée.",
  },
  {
    name: 'openAIRealtime:conversation.interrupted',
    type: 'event',
    description: "Émis lorsqu'une conversation est interrompue.",
  },
  {
    name: 'openAIRealtime:conversation.updated',
    type: 'event',
    description: "Émis lorsqu'une conversation est mise à jour.",
  },
  {
    name: 'openAIRealtime:conversation.item.appended',
    type: 'event',
    description: "Émis lorsqu'un élément est ajouté à la conversation.",
  },
  {
    name: 'openAIRealtime:conversation.item.completed',
    type: 'event',
    description: "Émis lorsqu'un élément de la conversation est terminé.",
  },
]}
/>

## Voix disponibles {/* #available-voices */}

Les options de voix suivantes sont disponibles :

- `alloy` : neutre et équilibrée
- `ash` : claire et précise
- `ballad` : mélodieuse et douce
- `coral` : chaleureuse et amicale
- `echo` : résonnante et profonde
- `sage` : calme et posée
- `shimmer` : claire et énergique
- `verse` : polyvalente et expressive

## Remarques {/* #notes */}

- Les clés d'API peuvent être fournies au moyen des options du constructeur ou de la variable d'environnement `OPENAI_API_KEY`
- L'API OpenAI Realtime Voice utilise WebSockets pour la communication en temps réel
- La détection d'activité vocale (VAD) côté serveur offre une meilleure précision pour détecter la parole
- Toutes les données audio sont traitées au format Int16Array
- L'instance Voice doit être connectée avec `connect()` avant d'utiliser les autres méthodes
- Appelez toujours `close()` lorsque vous avez terminé afin de libérer correctement les ressources
- La gestion de la mémoire est assurée par l'API OpenAI Realtime
