---
title: "Référence : Memory.createThread() | Memory"
description: "Documentation de la méthode `Memory.createThread()` de Mastra, qui crée un nouveau fil de discussion dans le système de mémoire."
packages:
  - "@mastra/memory"
---

# Memory.createThread()

La méthode `.createThread()` crée un nouveau fil de discussion dans le système de mémoire. Chaque fil représente une conversation ou un contexte distinct et peut contenir plusieurs messages.

## Exemple d’utilisation {/* #usage-example */}

```typescript
await memory?.createThread({ resourceId: 'user-123' })
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'resourceId',
    type: 'string',
    description: 'Identifiant de la ressource à laquelle appartient ce fil de discussion (par exemple, ID d’utilisateur ou de projet)',
    isOptional: false,
  },
  {
    name: 'threadId',
    type: 'string',
    description: 'ID personnalisé facultatif du fil de discussion. S’il n’est pas fourni, un ID est généré.',
    isOptional: true,
  },
  {
    name: 'title',
    type: 'string',
    description: 'Titre facultatif du fil de discussion',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'Métadonnées facultatives à associer au fil de discussion',
    isOptional: true,
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique du fil de discussion créé',
  },
  {
    name: 'resourceId',
    type: 'string',
    description: 'ID de la ressource associée au fil de discussion',
  },
  {
    name: 'title',
    type: 'string',
    description: 'Titre du fil de discussion, s’il a été fourni',
  },
  {
    name: 'createdAt',
    type: 'Date',
    description: 'Horodatage de la création du fil de discussion',
  },
  {
    name: 'updatedAt',
    type: 'Date',
    description: 'Horodatage de la dernière mise à jour du fil de discussion',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'Métadonnées supplémentaires associées au fil de discussion',
  },
]}
/>

## Exemple d’utilisation avancée {/* #extended-usage-example */}

```typescript title="src/test-memory.ts"
import { mastra } from './mastra'

const agent = mastra.getAgent('agent')
const memory = await agent.getMemory()

const thread = await memory?.createThread({
  resourceId: 'user-123',
  title: 'Memory Test Thread',
  metadata: {
    source: 'test-script',
    purpose: 'memory-testing',
  },
})

const response = await agent.generate('message for agent', {
  memory: {
    thread: thread!.id,
    resource: thread!.resourceId,
  },
})

console.log(response.text)
```

### Voir aussi {/* #related */}

- [Référence de la classe Memory](/reference/memory/memory-class)
- [Prise en main de Memory](/docs/memory/overview) (présente le concept de fils de discussion)
- [getThreadById](/reference/memory/getThreadById)
- [listThreads](/reference/memory/listThreads)
- [recall](/reference/memory/recall)
