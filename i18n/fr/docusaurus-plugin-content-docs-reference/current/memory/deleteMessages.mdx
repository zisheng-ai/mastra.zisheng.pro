---
title: "Référence : Memory.deleteMessages() | Memory"
description: "Documentation de la méthode `Memory.deleteMessages()` de Mastra, qui supprime plusieurs messages à partir de leurs ID."
packages:
  - "@mastra/core"
---

# Memory.deleteMessages()

La méthode `.deleteMessages()` supprime plusieurs messages à partir de leurs ID.

## Exemple d’utilisation

```typescript
await memory?.deleteMessages(['671ae63f-3a91-4082-a907-fe7de78e10ec'])
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'messageIds',
    type: 'string[]',
    description: 'Tableau des ID de messages à supprimer',
    isOptional: false,
  },
]}
/>

## Valeur renvoyée

<PropertiesTable
  content={[
  {
    name: 'void',
    type: 'Promise<void>',
    description: 'Promesse qui se résout lorsque tous les messages sont supprimés',
  },
]}
/>

## Exemple d’utilisation étendu

```typescript title="src/test-memory.ts"
import { mastra } from './mastra'
import { MastraDBMessage } from '@mastra/core'

const agent = mastra.getAgent('agent')
const memory = await agent.getMemory()

const { messages } = await memory!.recall({ threadId: 'thread-123' })

const messageIds = messages.map((message: MastraDBMessage) => message.id)
await memory?.deleteMessages([...messageIds])
```

## Voir aussi

- [Référence de la classe Memory](/reference/memory/memory-class)
- [recall](/reference/memory/recall)
- [Prise en main de Memory](/docs/memory/overview)
