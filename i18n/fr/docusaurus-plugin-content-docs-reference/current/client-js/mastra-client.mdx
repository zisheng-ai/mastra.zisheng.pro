---
title: "Référence : Mastra client SDK | Client SDK"
description: Learn how à interact avec Mastra using the client-js SDK.
packages:
  - "@mastra/client-js"
---

# Mastra client SDK

The Mastra Client SDK provides a type-safe interface pour interacting avec your [Mastra Serveur](/docs/deployment/mastra-server) depuis your client environment.

## Exemple d’utilisation

```typescript title="lib/mastra/mastra-client.ts"
import { MastraClient } from '@mastra/client-js'

export const mastraClient = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})
```

## `RequestContext`

Lorsque vous utilise `RequestContext` avec the client SDK, import it depuis `@mastra/client-js`.

```typescript
import { MastraClient, RequestContext } from '@mastra/client-js'

const client = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})

const requestContext = new RequestContext()
requestContext.set('userId', 'user-123')

const agent = client.getAgent('support-agent')

const response = await agent.generate('Summarize this ticket', {
  requestContext,
})
```

Vous pouvez également transmet `requestContext` comme a `Record<string, any>`.

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: 'The base URL pour the Mastra API. Tous les requests va be sent relative à this URL.',
    isOptional: false,
  },
  {
    name: 'retries',
    type: 'number',
    description: 'The nombre de times a requête va be retried sur failure avant throwing an error.',
    isOptional: true,
    defaultValue: '3',
  },
  {
    name: 'backoffMs',
    type: 'number',
    description:
      'The initial delay dans milliseconds avant retrying a failed request. Ce valeur est doubled avec chaque retry (exponential backoff).',
    isOptional: true,
    defaultValue: '300',
  },
  {
    name: 'maxBackoffMs',
    type: 'number',
    description: 'The maximum backoff time dans milliseconds. Prevents retries depuis waiting too long entre attempts.',
    isOptional: true,
    defaultValue: '5000',
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: 'Un objet containing personnalisé HTTP headers à inclut avec every request.',
    isOptional: true,
  },
  {
    name: 'credentials',
    type: '"omit" | "same-origin" | "include"',
    description:
      'Credentials mode pour requests. Consultez https://developer.mozilla.org/en-US/docs/Web/API/Request/credentials pour more info.',
    isOptional: true,
  },
]}
/>

## Méthodes

<PropertiesTable
  content={[
  {
    name: 'listAgents()',
    type: 'Promise<Record<string, GetAgentResponse>>',
    description: 'Renvoie tous les disponible agent instances.',
  },
  {
    name: 'getAgent(agentId)',
    type: 'Agent',
    description: 'Retrieves a specific agent instance par ID.',
  },
  {
    name: 'listMemoryThreads(params)',
    type: 'Promise<StorageThreadType[]>',
    description:
      'Retrieves memory threads pour the specified resource et agent. Requires a `resourceId` et an `agentId`.',
  },
  {
    name: 'createMemoryThread(params)',
    type: 'Promise<MemoryThread>',
    description: 'Crée a nouveau memory thread avec the given parameters.',
  },
  {
    name: 'getMemoryThread({ threadId, agentId })',
    type: 'MemoryThread',
    description: 'Fetches a specific memory thread par ID.',
  },
  {
    name: 'saveMessageToMemory(params)',
    type: 'Promise<{ messages: (MastraMessageV1 | MastraDBMessage)[] }>',
    description: 'Enregistre un ou more messages à the memory system. Renvoie the saved messages.',
  },
  {
    name: 'getMemoryStatus()',
    type: 'Promise<MemoryStatus>',
    description: 'Renvoie the actuel status de the memory system.',
  },
  {
    name: 'listTools()',
    type: 'Record<string, Tool>',
    description: 'Renvoie tous les disponible tools.',
  },
  {
    name: 'getTool(toolId)',
    type: 'Tool',
    description: 'Retrieves a specific Tool instance par ID.',
  },
  {
    name: 'listWorkflows()',
    type: 'Record<string, Workflow>',
    description: 'Renvoie tous les disponible Workflow instances.',
  },
  {
    name: 'getWorkflow(workflowId)',
    type: 'Workflow',
    description: 'Retrieves a specific Workflow instance par ID.',
  },
  {
    name: 'getAgentBuilderActions()',
    type: 'Promise<Record<string, WorkflowInfo>>',
    description:
      'Renvoie tous les disponible Agent Builder actions. Consultez [Agent Builder API](https://agent-builder.mastra.ai/reference/client).',
  },
  {
    name: 'getAgentBuilderAction(actionId)',
    type: 'AgentBuilder',
    description:
      'Retrieves an Agent Builder action par ID. Consultez [Agent Builder API](https://agent-builder.mastra.ai/reference/client).',
  },
  {
    name: 'responses',
    type: 'Responses',
    description:
      'Fournit OpenAI-style Réponses API helpers avec `create()`, `retrieve()`, `stream()`, et `delete()`.',
  },
  {
    name: 'conversations',
    type: 'Conversations',
    description: 'Fournit conversation helpers avec `create()`, `retrieve()`, `delete()`, et `items.list()`.',
  },
  {
    name: 'getVector(vectorName)',
    type: 'MastraVector',
    description: 'Renvoie a vector store instance par name.',
  },
  {
    name: 'listLogs(params)',
    type: 'Promise<LogEntry[]>',
    description: 'Fetches system logs matching the fourni filters.',
  },
  {
    name: 'getLog(params)',
    type: 'Promise<LogEntry>',
    description: 'Retrieves a specific log entry par ID ou filter.',
  },
  {
    name: 'listLogTransports()',
    type: 'string[]',
    description: 'Renvoie the liste de configuré log transport types.',
  },
  {
    name: 'getTrace(traceId)',
    type: 'Promise<TraceRecord>',
    description: 'Retrieves a specific trace par ID, including tous les its spans et details.',
  },
  {
    name: 'getTraces(params)',
    type: 'Promise<GetTracesResponse>',
    description:
      'Retrieves paginated liste de trace root spans avec facultatif filtering. Use getTrace() à obtient complete traces avec tous les spans.',
  },
]}
/>
