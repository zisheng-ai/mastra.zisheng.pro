---
title: "Référence : SDK client Mastra | SDK client"
description: Découvrez comment interagir avec Mastra au moyen du SDK client-js.
packages:
  - "@mastra/client-js"
---

# SDK client Mastra

Le SDK client Mastra fournit une interface avec sûreté des types permettant d’interagir avec votre [serveur Mastra](/docs/deployment/mastra-server) depuis votre environnement client.

## Exemple d’utilisation {/* #usage-example */}

```typescript title="lib/mastra/mastra-client.ts"
import { MastraClient } from '@mastra/client-js'

export const mastraClient = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})
```

## `RequestContext` {/* #requestcontext */}

Lorsque vous utilisez `RequestContext` avec le SDK client, importez-le depuis `@mastra/client-js`.

```typescript
import { MastraClient, RequestContext } from '@mastra/client-js'

const client = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})

const requestContext = new RequestContext()
requestContext.set('userId', 'user-123')

const agent = client.getAgent('support-agent')

const response = await agent.generate('Summarize this ticket', {
  requestContext,
})
```

Vous pouvez également transmettre `requestContext` sous forme de `Record<string, any>`.

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: 'URL de base de l’API Mastra. Toutes les requêtes sont envoyées relativement à cette URL.',
    isOptional: false,
  },
  {
    name: 'retries',
    type: 'number',
    description: 'Nombre de nouvelles tentatives d’une requête en cas d’échec avant qu’une erreur ne soit levée.',
    isOptional: true,
    defaultValue: '3',
  },
  {
    name: 'backoffMs',
    type: 'number',
    description:
      'Délai initial, en millisecondes, avant une nouvelle tentative après l’échec d’une requête. Cette valeur double à chaque tentative (backoff exponentiel).',
    isOptional: true,
    defaultValue: '300',
  },
  {
    name: 'maxBackoffMs',
    type: 'number',
    description: 'Durée maximale du backoff, en millisecondes. Évite une attente trop longue entre les tentatives.',
    isOptional: true,
    defaultValue: '5000',
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: 'Objet contenant les en-têtes HTTP personnalisés à inclure dans chaque requête.',
    isOptional: true,
  },
  {
    name: 'credentials',
    type: '"omit" | "same-origin" | "include"',
    description:
      'Mode d’identification des requêtes. Pour en savoir plus, consultez https://developer.mozilla.org/en-US/docs/Web/API/Request/credentials.',
    isOptional: true,
  },
]}
/>

## Méthodes {/* #methods */}

<PropertiesTable
  content={[
  {
    name: 'listAgents()',
    type: 'Promise<Record<string, GetAgentResponse>>',
    description: 'Renvoie toutes les instances d’Agent disponibles.',
  },
  {
    name: 'getAgent(agentId)',
    type: 'Agent',
    description: 'Récupère une instance d’Agent donnée selon son ID.',
  },
  {
    name: 'listMemoryThreads(params)',
    type: 'Promise<StorageThreadType[]>',
    description:
      'Récupère les fils de mémoire de la ressource et de l’Agent indiqués. Nécessite un `resourceId` et un `agentId`.',
  },
  {
    name: 'createMemoryThread(params)',
    type: 'Promise<MemoryThread>',
    description: 'Crée un nouveau fil de mémoire avec les paramètres fournis.',
  },
  {
    name: 'getMemoryThread({ threadId, agentId })',
    type: 'MemoryThread',
    description: 'Récupère un fil de mémoire donné selon son ID.',
  },
  {
    name: 'saveMessageToMemory(params)',
    type: 'Promise<{ messages: (MastraMessageV1 | MastraDBMessage)[] }>',
    description: 'Enregistre un ou plusieurs messages dans le système de mémoire. Renvoie les messages enregistrés.',
  },
  {
    name: 'getMemoryStatus()',
    type: 'Promise<MemoryStatus>',
    description: 'Renvoie l’état actuel du système de mémoire.',
  },
  {
    name: 'listTools()',
    type: 'Record<string, Tool>',
    description: 'Renvoie tous les Tools disponibles.',
  },
  {
    name: 'getTool(toolId)',
    type: 'Tool',
    description: 'Récupère une instance de Tool donnée selon son ID.',
  },
  {
    name: 'listWorkflows()',
    type: 'Record<string, Workflow>',
    description: 'Renvoie toutes les instances de Workflow disponibles.',
  },
  {
    name: 'getWorkflow(workflowId)',
    type: 'Workflow',
    description: 'Récupère une instance de Workflow donnée selon son ID.',
  },
  {
    name: 'getAgentBuilderActions()',
    type: 'Promise<Record<string, WorkflowInfo>>',
    description:
      'Renvoie toutes les actions Agent Builder disponibles. Consultez l’[API Agent Builder](https://agent-builder.mastra.ai/reference/client).',
  },
  {
    name: 'getAgentBuilderAction(actionId)',
    type: 'AgentBuilder',
    description:
      'Récupère une action Agent Builder selon son ID. Consultez l’[API Agent Builder](https://agent-builder.mastra.ai/reference/client).',
  },
  {
    name: 'responses',
    type: 'Responses',
    description:
      'Fournit des utilitaires de l’API Responses dans le style OpenAI avec `create()`, `retrieve()`, `stream()` et `delete()`.',
  },
  {
    name: 'conversations',
    type: 'Conversations',
    description: 'Fournit des utilitaires de conversation avec `create()`, `retrieve()`, `delete()` et `items.list()`.',
  },
  {
    name: 'getVector(vectorName)',
    type: 'MastraVector',
    description: 'Renvoie une instance de stockage vectoriel selon son nom.',
  },
  {
    name: 'listLogs(params)',
    type: 'Promise<LogEntry[]>',
    description: 'Récupère les logs système correspondant aux filtres fournis.',
  },
  {
    name: 'getLog(params)',
    type: 'Promise<LogEntry>',
    description: 'Récupère une entrée de log donnée selon son ID ou un filtre.',
  },
  {
    name: 'listLogTransports()',
    type: 'string[]',
    description: 'Renvoie la liste des types de transport de logs configurés.',
  },
  {
    name: 'getTrace(traceId)',
    type: 'Promise<TraceRecord>',
    description: 'Récupère une Trace donnée selon son ID, avec tous ses spans et leurs détails.',
  },
  {
    name: 'getTraces(params)',
    type: 'Promise<GetTracesResponse>',
    description:
      'Récupère une liste paginée des spans racines des Traces avec un filtrage facultatif. Utilisez getTrace() pour obtenir des Traces complètes avec tous leurs spans.',
  },
]}
/>
