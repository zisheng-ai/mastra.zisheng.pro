---
title: "Référence : UnixSocketPubSub | PubSub"
description: Référence de l’API UnixSocketPubSub, l’implémentation pub/sub utilisant un socket Unix pour acheminer des événements entre plusieurs processus sur un même hôte.
packages:
  - "@mastra/core"
---

# UnixSocketPubSub

`UnixSocketPubSub` est une implémentation de [`PubSub`](/reference/pubsub/base) qui transmet des événements entre plusieurs processus sur un même hôte au moyen d’un socket de domaine Unix. Elle élit un processus comme broker, auquel les autres processus se connectent en tant que clients. Si le broker s’arrête, les clients restants élisent automatiquement un nouveau broker.

Utilisez-la lorsque plusieurs processus locaux doivent partager un flux, par exemple pour coordonner des flux de threads dans l’interface de terminal Mastra Code. Pour une diffusion au sein d’un seul processus, utilisez [`EventEmitterPubSub`](/reference/pubsub/event-emitter). Pour une diffusion distribuée entre plusieurs hôtes, utilisez [`RedisStreamsPubSub`](/reference/pubsub/redis-streams) ou [`GoogleCloudPubSub`](/reference/pubsub/google-cloud-pubsub).

`UnixSocketPubSub` est un transport push : les événements arrivent sans boucle de lecture, Mastra n’exécute donc aucun worker pull pour ce transport.

## Exemple d’utilisation {/* #usage-example */}

Transmettez un chemin de socket partagé par tous les processus participants.

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { UnixSocketPubSub } from '@mastra/core/events'

export const mastra = new Mastra({
  pubsub: new UnixSocketPubSub('/tmp/mastra/events.sock'),
})
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'socketPath',
    type: 'string',
    description: 'Chemin du socket de domaine Unix. Tous les processus qui partagent un flux doivent utiliser le même chemin.',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'UnixSocketPubSubOptions',
    description: 'Configuration facultative.',
    isOptional: true,
    properties: [
      {
        name: 'maxRemoteClientQueuedBytes',
        type: 'number',
        description:
          'Nombre maximal d’octets mis en mémoire tampon pour un client distant lent avant l’abandon de ses trames, afin d’éviter qu’un abonné bloqué ne bloque les autres.',
        isOptional: true,
        defaultValue: '67108864',
      },
    ],
  },
]}
/>

## Propriétés {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'socketPath',
    type: 'string',
    description: 'Chemin du socket transmis au constructeur.',
  },
  {
    name: 'supportedModes',
    type: 'ReadonlyArray<"pull" | "push">',
    description: 'Renvoie `["push"]`.',
  },
  {
    name: 'isBroker',
    type: 'boolean',
    description: 'Indique si cette instance joue actuellement le rôle de broker.',
  },
  {
    name: 'remoteClientCount',
    type: 'number',
    description: 'Nombre de clients distants connectés à ce broker. Toujours égal à 0 lorsque cette instance n’est pas le broker.',
  },
]}
/>

## Méthodes {/* #methods */}

`UnixSocketPubSub` implémente le contrat [`PubSub`](/reference/pubsub/base). La méthode ci-dessous est propre à cette implémentation.

### `close()` {/* #close */}

Ferme la connexion au socket et, lorsque cette instance est le broker, libère ce rôle. Appelez cette méthode lors d’un arrêt progressif.

```typescript
await pubsub.close()
```

## Élection du broker {/* #broker-election */}

Le premier processus qui se lie au socket devient le broker et achemine les événements entre tous les clients connectés. Les autres processus se connectent en tant que clients. Lorsque le broker s’arrête, un fichier de verrouillage exclusif sérialise l’élection suivante. Un seul client devient le nouveau broker. Les clients restants se réabonnent auprès de lui. Ce mécanisme évite un état de split-brain dans lequel deux processus joueraient simultanément le rôle de broker.
