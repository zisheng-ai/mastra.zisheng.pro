---
title: "Référence : Classe MastraJwtAuth | Auth"
description: "Référence d’API de la classe MastraJwtAuth, qui authentifie les applications Mastra à l’aide de JSON Web Tokens."
packages:
  - "@mastra/auth"
  - "@mastra/core"
---

# Classe MastraJwtAuth

La classe `MastraJwtAuth` fournit à Mastra un mécanisme d’authentification léger basé sur les JSON Web Tokens (JWT). Elle vérifie les requêtes entrantes à partir d’un secret partagé et s’intègre au serveur Mastra à l’aide de l’option `auth`.

## Exemple d’utilisation {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraJwtAuth } from '@mastra/auth'

export const mastra = new Mastra({
  server: {
    auth: new MastraJwtAuth({
      secret: '<your-secret>',
    }),
  },
})
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'secret',
    type: 'string',
    description: 'Chaîne unique utilisée pour signer et vérifier les JSON Web Tokens (JWT) afin d’authentifier les requêtes entrantes.',
    isOptional: false,
  },
]}
/>

## Voir aussi {/* #related */}

- [JSON Web Token](/docs/server/auth/jwt)
