---
title: "Référence : MastraJwtAuth class | Auth"
description: "Référence d’API de the MastraJwtAuth class, which authenticates Mastra applications using JSON Web Tokens."
packages:
  - "@mastra/auth"
  - "@mastra/core"
---

# MastraJwtAuth class

The `MastraJwtAuth` class provides a lightweight authentication mechanism pour Mastra using JSON Web Tokens (JWTs). It verifies incoming requests based sur a shared secret et integrates avec the Mastra server using the `auth` option.

## Exemple d’utilisation

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraJwtAuth } from '@mastra/auth'

export const mastra = new Mastra({
  server: {
    auth: new MastraJwtAuth({
      secret: '<your-secret>',
    }),
  },
})
```

## Paramètres du constructeur

<PropertiesTable
  content={[
  {
    name: 'secret',
    type: 'string',
    description: 'Un unique chaîne used à sign et verify JSON Web Tokens (JWTs) pour authenticating incoming requests.',
    isOptional: false,
  },
]}
/>

## Voir aussi

- [JSON Web Token](/docs/server/auth/jwt)
