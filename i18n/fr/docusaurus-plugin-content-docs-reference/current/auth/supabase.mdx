---
title: "Référence : classe MastraAuthSupabase | Auth"
description: "Référence de l’API de la classe MastraAuthSupabase, qui authentifie les applications Mastra à l’aide de Supabase Auth."
packages:
  - "@mastra/auth-supabase"
  - "@mastra/core"
---

# Classe MastraAuthSupabase

La classe `MastraAuthSupabase` fournit l’authentification pour Mastra à l’aide de Supabase Auth. Elle vérifie les requêtes entrantes avec le système d’authentification de Supabase et s’intègre au serveur Mastra au moyen de l’option `auth`.

## Exemple d’utilisation {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthSupabase } from '@mastra/auth-supabase'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthSupabase({
      url: process.env.SUPABASE_URL,
      anonKey: process.env.SUPABASE_ANON_KEY,
    }),
  },
})
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: 'URL de votre projet Supabase. Elle est disponible dans les paramètres de votre projet Supabase.',
    isOptional: true,
    defaultValue: 'process.env.SUPABASE_URL',
  },
  {
    name: 'anonKey',
    type: 'string',
    description: 'Clé anonyme/publique de votre projet Supabase. Utilisée pour l’authentification côté client.',
    isOptional: true,
    defaultValue: 'process.env.SUPABASE_ANON_KEY',
  },
  {
    name: 'name',
    type: 'string',
    description: 'Nom personnalisé de l’instance de fournisseur d’authentification.',
    isOptional: true,
  },
  {
    name: 'authorizeUser',
    type: '(user: User, request: HoneRequest) => Promise<boolean> | boolean',
    description:
      "Fonction d’autorisation personnalisée permettant de déterminer si un utilisateur doit recevoir l’accès. Appelée après la vérification du jeton. Par défaut, vérifie la colonne 'isAdmin' de la table 'users'.",
    isOptional: true,
  },
]}
/>

## Voir aussi {/* #related */}

[MastraAuthSupabase](/docs/server/auth/supabase)
