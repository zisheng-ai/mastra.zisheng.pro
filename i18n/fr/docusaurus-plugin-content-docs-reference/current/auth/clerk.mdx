---
title: "Référence : MastraAuthClerk class | Auth"
description: "Référence d’API de the MastraAuthClerk class, which authenticates Mastra applications using Clerk authentication."
packages:
  - "@mastra/auth-clerk"
  - "@mastra/core"
---

# MastraAuthClerk class

The `MastraAuthClerk` class provides authentication pour Mastra applications using Clerk. It verifies incoming requests avec Clerk-issued JWT tokens et integrates avec the Mastra server using the `auth` option.

## Exemple d’utilisation

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthClerk } from '@mastra/auth-clerk'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthClerk({
      jwksUri: process.env.CLERK_JWKS_URI,
      publishableKey: process.env.CLERK_PUBLISHABLE_KEY,
      secretKey: process.env.CLERK_SECRET_KEY,
    }),
  },
})
```

## Paramètres du constructeur

<PropertiesTable
  content={[
  {
    name: 'publishableKey',
    type: 'string',
    description: 'Your Clerk publishable key. Can be found dans your Clerk Dashboard under API Keys.',
    isOptional: true,
    defaultValue: 'process.env.CLERK_PUBLISHABLE_KEY',
  },
  {
    name: 'secretKey',
    type: 'string',
    description: 'Your Clerk secret key. Used pour server-side authentication et token verification.',
    isOptional: true,
    defaultValue: 'process.env.CLERK_SECRET_KEY',
  },
  {
    name: 'jwksUri',
    type: 'string',
    description: 'The JWKS URI depuis your Clerk application. Used à verify JWT signatures.',
    isOptional: true,
    defaultValue: 'process.env.CLERK_JWKS_URI',
  },
  {
    name: 'name',
    type: 'string',
    description: 'Personnalisé nom pour the auth provider instance.',
    isOptional: true,
  },
  {
    name: 'authorizeUser',
    type: '(user: User, request: HonoRequest) => Promise<boolean> | boolean',
    description:
      'Personnalisé authorization fonction à determine if a utilisateur doit be granted access. Called après token verification. Par défaut, allows tous les authenticated users.',
    isOptional: true,
  },
]}
/>

## Voir aussi

[MastraAuthClerk Class](/docs/server/auth/clerk)
