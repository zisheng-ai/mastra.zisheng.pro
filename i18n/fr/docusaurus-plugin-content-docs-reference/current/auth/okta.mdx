---
title: "Référence : MastraAuthOkta et MastraRBACOkta | Authentification"
description: "Référence de l’API des classes MastraAuthOkta et MastraRBACOkta, qui authentifient et autorisent les applications Mastra à l’aide d’Okta."
packages:
  - "@mastra/auth-okta"
  - "@mastra/core"
---

# Classes MastraAuthOkta et MastraRBACOkta

## Classe MastraAuthOkta {/* #mastraauthokta-class */}

La classe `MastraAuthOkta` fournit une authentification pour Mastra à l’aide d’Okta. Elle implémente un flux de connexion OAuth 2.0 / OIDC avec des cookies de session chiffrés et s’intègre au serveur Mastra par l’intermédiaire de l’option `auth`.

### Exemple d’utilisation {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthOkta } from '@mastra/auth-okta'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthOkta({
      domain: process.env.OKTA_DOMAIN,
      clientId: process.env.OKTA_CLIENT_ID,
      clientSecret: process.env.OKTA_CLIENT_SECRET,
      redirectUri: process.env.OKTA_REDIRECT_URI,
    }),
  },
})
```

:::note

Vous pouvez omettre les paramètres du constructeur si les variables d’environnement requises sont définies. Dans ce cas, utilisez `new MastraAuthOkta()` sans argument.

:::

### Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'domain',
    type: 'string',
    description: "Votre domaine Okta (par exemple, `dev-123456.okta.com`). Utilisé pour construire l’URL de l’émetteur et les endpoints d’API.",
    isOptional: true,
    defaultValue: 'process.env.OKTA_DOMAIN',
  },
  {
    name: 'clientId',
    type: 'string',
    description: "L’ID client OAuth de votre application Okta.",
    isOptional: true,
    defaultValue: 'process.env.OKTA_CLIENT_ID',
  },
  {
    name: 'clientSecret',
    type: 'string',
    description: "Le secret client OAuth. Requis pour le flux SSO de code d’autorisation.",
    isOptional: true,
    defaultValue: 'process.env.OKTA_CLIENT_SECRET',
  },
  {
    name: 'issuer',
    type: 'string',
    description: "L’URL de l’émetteur de jetons. Remplacez-la si vous utilisez un serveur d’autorisation personnalisé.",
    isOptional: true,
    defaultValue: '`https://{domain}/oauth2/default`',
  },
  {
    name: 'redirectUri',
    type: 'string',
    description:
      "L’URI de redirection OAuth pour le callback SSO. Elle doit correspondre à l’URI de redirection configurée dans votre application Okta.",
    isOptional: true,
    defaultValue: 'process.env.OKTA_REDIRECT_URI',
  },
  {
    name: 'scopes',
    type: 'string[]',
    description: "Scopes OAuth à demander pendant le flux de connexion.",
    isOptional: true,
    defaultValue: "['openid', 'profile', 'email', 'groups']",
  },
  {
    name: 'apiToken',
    type: 'string',
    description:
      "Jeton d’API Okta pour rechercher des utilisateurs via l’API Users. Requis pour que `getUser()` renvoie les données utilisateur par ID.",
    isOptional: true,
    defaultValue: 'process.env.OKTA_API_TOKEN',
  },
  {
    name: 'session',
    type: 'OktaSessionOptions',
    description: 'Configuration du cookie de session.',
    isOptional: true,
    properties: [
      {
        type: 'OktaSessionOptions',
        parameters: [
          {
            name: 'cookieName',
            type: 'string',
            description: 'Nom du cookie de session.',
            isOptional: true,
            defaultValue: "'okta_session'",
          },
          {
            name: 'cookieMaxAge',
            type: 'number',
            description: 'Durée de vie maximale du cookie, en secondes.',
            isOptional: true,
            defaultValue: '86400 (24 hours)',
          },
          {
            name: 'cookiePassword',
            type: 'string',
            description:
              'Mot de passe pour chiffrer les cookies de session. Il doit comporter au moins 32 caractères. S’il n’est pas défini, une valeur générée automatiquement est utilisée et ne survit pas aux redémarrages.',
            isOptional: true,
            defaultValue: 'process.env.OKTA_COOKIE_PASSWORD',
          },
          {
            name: 'secureCookies',
            type: 'boolean',
            description: 'Définit le marqueur `Secure` sur les cookies de session.',
            isOptional: true,
            defaultValue: 'true in production, false otherwise',
          },
        ],
      },
    ],
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom personnalisé de l’instance du Provider d’authentification.",
    isOptional: true,
    defaultValue: "'okta'",
  },
]}
/>

### Variables d’environnement {/* #environment-variables */}

Les variables d’environnement suivantes sont automatiquement utilisées lorsque les options du constructeur ne sont pas fournies :

<PropertiesTable
  content={[
  {
    name: 'OKTA_DOMAIN',
    type: 'string',
    description: "Votre domaine Okta (par exemple, `dev-123456.okta.com`). Vous le trouverez dans votre console d’administration Okta.",
  },
  {
    name: 'OKTA_CLIENT_ID',
    type: 'string',
    description: "L’ID client OAuth de votre application Okta.",
  },
  {
    name: 'OKTA_CLIENT_SECRET',
    type: 'string',
    description: "Le secret client OAuth de votre application Okta.",
  },
  {
    name: 'OKTA_ISSUER',
    type: 'string',
    description: "URL de l’émetteur de jetons. Utilise par défaut `https://{domain}/oauth2/default` si elle n’est pas définie.",
    isOptional: true,
  },
  {
    name: 'OKTA_REDIRECT_URI',
    type: 'string',
    description: 'URI de redirection OAuth pour le callback SSO.',
  },
  {
    name: 'OKTA_COOKIE_PASSWORD',
    type: 'string',
    description: 'Mot de passe pour chiffrer les cookies de session. Il doit comporter au moins 32 caractères.',
    isOptional: true,
  },
  {
    name: 'OKTA_API_TOKEN',
    type: 'string',
    description: 'Jeton d’API Okta pour les recherches d’utilisateurs et la résolution des groupes RBAC.',
    isOptional: true,
  },
]}
/>

### Flux d’authentification {/* #authentication-flow */}

`MastraAuthOkta` authentifie les requêtes dans l’ordre suivant :

1. **Cookie de session** : lit et déchiffre le cookie de session chiffré. Si la session est valide et n’a pas expiré, l’utilisateur est authentifié.
1. **Solution de repli JWT** : si aucun cookie de session n’est présent, vérifie le jeton de l’en-tête `Authorization` auprès de l’endpoint JWKS d’Okta.

Après l’authentification, `authorizeUser` vérifie que l’utilisateur possède un `oktaId` valide. Fournissez une fonction `authorizeUser` personnalisée pour implémenter une logique supplémentaire.

### Type `OktaUser` {/* #oktauser-type */}

Le type `OktaUser` étend l’interface de base `EEUser` avec des champs propres à Okta :

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "Identifiant de l’utilisateur (correspond au claim `sub`).",
  },
  {
    name: 'oktaId',
    type: 'string',
    description: "ID utilisateur Okta (identique à `id`).",
  },
  {
    name: 'email',
    type: 'string',
    description: "Adresse e-mail de l’utilisateur.",
    isOptional: true,
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom d’affichage de l’utilisateur, construit à partir des claims du jeton.",
    isOptional: true,
  },
  {
    name: 'avatarUrl',
    type: 'string',
    description: "URL de la photo de profil de l’utilisateur.",
    isOptional: true,
  },
  {
    name: 'groups',
    type: 'string[]',
    description: "Groupes Okta auxquels l’utilisateur appartient, renseignés à partir du claim `groups`.",
    isOptional: true,
  },
]}
/>

## Classe MastraRBACOkta {/* #mastrarbacokta-class */}

La classe `MastraRBACOkta` associe les groupes Okta aux autorisations Mastra. Elle récupère les groupes d’utilisateurs depuis l’API Okta et les résout par rapport à une association de rôles configurable. Utilisez-la avec `MastraAuthOkta` ou tout autre Provider d’authentification.

:::note

RBAC requiert une licence Enterprise Edition valide. Il fonctionne sans licence en développement afin que vous puissiez l’essayer localement, mais vous aurez besoin d’une licence en production. [Contactez l’équipe commerciale](https://mastra.ai/contact) pour plus d’informations.

:::

### Exemple d’utilisation {/* #usage-example-1 */}

Utilisez `MastraRBACOkta` avec un Provider d’authentification en le transmettant à l’option `rbac` :

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthOkta, MastraRBACOkta } from '@mastra/auth-okta'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthOkta(),
    rbac: new MastraRBACOkta({
      roleMapping: {
        Admin: ['*'],
        Engineering: ['agents:*', 'workflows:*', 'tools:*'],
        Viewer: ['agents:read', 'workflows:read'],
        _default: [],
      },
    }),
  },
})
```

Pour utiliser Okta RBAC avec un autre Provider d’authentification, transmettez une fonction `getUserId` afin de résoudre l’ID utilisateur Okta à partir de l’objet utilisateur de l’autre Provider :

```typescript title="src/mastra/index.ts"
import { MastraAuthAuth0 } from '@mastra/auth-auth0'
import { MastraRBACOkta } from '@mastra/auth-okta'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthAuth0(),
    rbac: new MastraRBACOkta({
      getUserId: user => user.metadata?.oktaUserId || user.email,
      roleMapping: {
        Engineering: ['agents:*', 'workflows:*'],
        Admin: ['*'],
        _default: [],
      },
    }),
  },
})
```

### Paramètres du constructeur {/* #constructor-parameters-1 */}

<PropertiesTable
  content={[
  {
    name: 'roleMapping',
    type: 'RoleMapping',
    description:
      "Associe les noms de groupes Okta à des tableaux de chaînes d’autorisation Mastra. Utilisez `'_default'` pour attribuer des autorisations aux utilisateurs qui ne correspondent à aucun groupe. Prend en charge les caractères génériques tels que `'*'` (accès complet) et `'agents:*'` (toutes les actions d’Agent).",
    isOptional: false,
  },
  {
    name: 'domain',
    type: 'string',
    description: "Votre domaine Okta. Utilisé pour initialiser le SDK de gestion Okta.",
    isOptional: true,
    defaultValue: 'process.env.OKTA_DOMAIN',
  },
  {
    name: 'apiToken',
    type: 'string',
    description: "Jeton d’API Okta pour le SDK de gestion. Requis pour récupérer les groupes d’utilisateurs depuis l’API Okta.",
    isOptional: true,
    defaultValue: 'process.env.OKTA_API_TOKEN',
  },
  {
    name: 'getUserId',
    type: '(user: unknown) => string | undefined',
    description:
      "Extrait l’ID utilisateur Okta d’un objet utilisateur. Utilisez cette option lorsque vous combinez Okta RBAC avec un autre Provider d’authentification. Si elle n’est pas fournie, utilise `oktaId` ou `id` de l’objet utilisateur.",
    isOptional: true,
  },
  {
    name: 'cache',
    type: 'PermissionCacheOptions',
    description: 'Configure le cache LRU pour les recherches de groupes.',
    isOptional: true,
    properties: [
      {
        type: 'PermissionCacheOptions',
        parameters: [
          {
            name: 'maxSize',
            type: 'number',
            description: "Nombre maximal d’utilisateurs à mettre en cache.",
            isOptional: true,
            defaultValue: '1000',
          },
          {
            name: 'ttlMs',
            type: 'number',
            description: 'Durée de vie en millisecondes.',
            isOptional: true,
            defaultValue: '60000',
          },
        ],
      },
    ],
  },
]}
/>
