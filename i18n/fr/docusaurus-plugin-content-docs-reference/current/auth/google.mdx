---
title: "Référence : MastraAuthGoogle et MastraRBACGoogle | Auth"
description: Référence API des classes MastraAuthGoogle et MastraRBACGoogle, qui authentifient les applications Mastra et gèrent leurs autorisations avec Google Workspace.
packages:
  - "@mastra/auth-google"
  - "@mastra/core"
---

# Classes MastraAuthGoogle et MastraRBACGoogle

## Classe MastraAuthGoogle {/* #mastraauthgoogle-class */}

La classe `MastraAuthGoogle` fournit à Mastra une authentification reposant sur Google Workspace. Elle met en œuvre un flux de connexion OAuth 2.0 / OIDC avec des cookies de session chiffrés, vérifie les jetons d’identité Google et s’intègre au serveur Mastra au moyen de l’option `auth`.

### Exemple d’utilisation {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthGoogle } from '@mastra/auth-google'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthGoogle({
      clientId: process.env.GOOGLE_CLIENT_ID,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET,
      redirectUri: process.env.GOOGLE_REDIRECT_URI,
      allowedDomains: ['example.com'],
    }),
  },
})
```

:::note

Vous pouvez omettre les paramètres du constructeur si les variables d’environnement requises sont définies. Dans ce cas, utilisez `new MastraAuthGoogle()` sans argument.

:::

### Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'clientId',
    type: 'string',
    description: 'Identifiant client OAuth Google.',
    isOptional: true,
    defaultValue: 'process.env.GOOGLE_CLIENT_ID',
  },
  {
    name: 'clientSecret',
    type: 'string',
    description: 'Secret client OAuth Google. Requis pour le SSO de Studio.',
    isOptional: true,
    defaultValue: 'process.env.GOOGLE_CLIENT_SECRET',
  },
  {
    name: 'redirectUri',
    type: 'string',
    description:
      'URI de redirection OAuth du callback SSO. Elle doit correspondre à l’URI de redirection configurée dans votre client OAuth Google Cloud.',
    isOptional: true,
    defaultValue: 'process.env.GOOGLE_REDIRECT_URI',
  },
  {
    name: 'scopes',
    type: 'string[]',
    description: 'Scopes OAuth demandés pendant le flux de connexion.',
    isOptional: true,
    defaultValue: "['openid', 'profile', 'email']",
  },
  {
    name: 'allowedDomains',
    type: 'string | string[]',
    description: 'Domaines hébergés Google Workspace autorisés. Mastra les compare à la claim `hd` vérifiée.',
    isOptional: true,
    defaultValue: 'process.env.GOOGLE_ALLOWED_DOMAINS',
  },
  {
    name: 'hostedDomain',
    type: 'string',
    description:
      'Indication de domaine hébergé transmise à Google sous la forme `hd` lors de la connexion. Il s’agit uniquement d’une indication, qui n’est pas utilisée pour l’autorisation.',
    isOptional: true,
    defaultValue: 'process.env.GOOGLE_HOSTED_DOMAIN ou le seul domaine autorisé',
  },
  {
    name: 'session',
    type: 'GoogleSessionOptions',
    description: 'Configuration du cookie de session.',
    isOptional: true,
    properties: [
      {
        type: 'GoogleSessionOptions',
        parameters: [
          {
            name: 'cookieName',
            type: 'string',
            description: 'Nom du cookie de session.',
            isOptional: true,
            defaultValue: "'google_session'",
          },
          {
            name: 'cookieMaxAge',
            type: 'number',
            description: 'Durée de vie maximale du cookie, en secondes.',
            isOptional: true,
            defaultValue: '86400 (24 heures)',
          },
          {
            name: 'cookiePassword',
            type: 'string',
            description:
              'Mot de passe utilisé pour chiffrer les cookies de session. Il doit comporter au moins 32 caractères. S’il n’est pas défini, une valeur générée automatiquement est utilisée en développement, mais elle n’est pas conservée après un redémarrage.',
            isOptional: true,
            defaultValue: 'process.env.GOOGLE_COOKIE_PASSWORD',
          },
          {
            name: 'secureCookies',
            type: 'boolean',
            description: 'Définit l’attribut `Secure` des cookies de session.',
            isOptional: true,
            defaultValue: 'true en production, false sinon',
          },
        ],
      },
    ],
  },
  {
    name: 'name',
    type: 'string',
    description: 'Nom personnalisé de l’instance du Provider d’authentification.',
    isOptional: true,
    defaultValue: "'google'",
  },
]}
/>

### Variables d’environnement {/* #environment-variables */}

Les variables d’environnement suivantes sont utilisées automatiquement lorsque les options du constructeur ne sont pas fournies :

<PropertiesTable
  content={[
  {
    name: 'GOOGLE_CLIENT_ID',
    type: 'string',
    description: 'Identifiant client OAuth Google provenant de votre client OAuth Google Cloud.',
  },
  {
    name: 'GOOGLE_CLIENT_SECRET',
    type: 'string',
    description: 'Secret client OAuth Google. Requis pour le flux SSO par code d’autorisation.',
    isOptional: true,
  },
  {
    name: 'GOOGLE_REDIRECT_URI',
    type: 'string',
    description: 'URI de redirection OAuth du callback SSO.',
    isOptional: true,
  },
  {
    name: 'GOOGLE_COOKIE_PASSWORD',
    type: 'string',
    description: 'Mot de passe utilisé pour chiffrer les cookies de session. Il doit comporter au moins 32 caractères.',
    isOptional: true,
  },
  {
    name: 'GOOGLE_ALLOWED_DOMAINS',
    type: 'string',
    description: 'Domaines hébergés Google Workspace à autoriser, séparés par des virgules.',
    isOptional: true,
  },
  {
    name: 'GOOGLE_HOSTED_DOMAIN',
    type: 'string',
    description: 'Indication de domaine hébergé transmise à Google pendant la connexion SSO.',
    isOptional: true,
  },
]}
/>

### Flux d’authentification {/* #authentication-flow */}

`MastraAuthGoogle` authentifie les requêtes dans l’ordre suivant :

1. **Cookie de session.** Lorsque le SSO est activé, le Provider lit et déchiffre le cookie de session chiffré. Une session valide et non expirée authentifie l’utilisateur.
1. **Repli sur le jeton d’identité Google** : si aucun cookie de session valide n’est présent, le jeton de l’en-tête `Authorization` est vérifié auprès de l’endpoint JWKS de Google.

Après l’authentification, `authorizeUser` vérifie que l’utilisateur possède un identifiant Google valide, que l’expiration issue du jeton n’est pas dépassée et, lorsque des domaines sont configurés, que la claim `hd` vérifiée de l’utilisateur correspond à `allowedDomains`.

### Méthodes d’authentification {/* #authentication-methods */}

#### `authenticateToken(token, request?)` {/* #authenticatetokentoken-request */}

Authentifie un jeton d’identité Google. Lorsque le SSO est activé, cette méthode vérifie le cookie de session chiffré avant de vérifier le jeton Bearer.

```typescript
const user = await auth.authenticateToken(idToken, request)
```

Renvoie : `Promise<GoogleUser | null>`

#### `getCurrentUser(request)` {/* #getcurrentuserrequest */}

Renvoie l’utilisateur authentifié à partir d’un cookie de session ou d’un jeton d’identité Google Bearer.

```typescript
const user = await auth.getCurrentUser(request)
```

Renvoie : `Promise<GoogleUser | null>`

#### `authorizeUser(user)` {/* #authorizeuseruser */}

Renvoie `true` si l’utilisateur possède un identifiant, n’a pas expiré et, lorsque des domaines sont configurés, correspond à `allowedDomains`.

```typescript
const allowed = auth.authorizeUser(user)
```

Renvoie : `boolean`

#### `getUser(userId)` {/* #getuseruserid */}

Renvoie `null`. Les jetons d’identité Google étant vérifiés directement, ce Provider ne recherche pas les utilisateurs par identifiant.

```typescript
const user = await auth.getUser(userId)
```

Renvoie : `Promise<GoogleUser | null>`

### Type `GoogleUser` {/* #googleuser-type */}

Le type `GoogleUser` étend l’interface de base `EEUser` avec des champs propres à Google :

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant utilisateur Mastra. Utilise la claim Google `sub`.',
  },
  {
    name: 'googleId',
    type: 'string',
    description: 'Identifiant de sujet du compte Google.',
  },
  {
    name: 'email',
    type: 'string',
    description: 'Adresse e-mail de l’utilisateur.',
    isOptional: true,
  },
  {
    name: 'name',
    type: 'string',
    description: 'Nom d’affichage de l’utilisateur issu des claims du profil Google.',
    isOptional: true,
  },
  {
    name: 'avatarUrl',
    type: 'string',
    description: 'URL de la photo de profil Google de l’utilisateur.',
    isOptional: true,
  },
  {
    name: 'hostedDomain',
    type: 'string',
    description: 'Domaine hébergé Google Workspace issu de la claim `hd` vérifiée.',
    isOptional: true,
  },
  {
    name: 'expiresAt',
    type: 'Date',
    description: 'Date d’expiration vérifiée du jeton d’identité, lorsqu’elle est disponible.',
    isOptional: true,
  },
  {
    name: 'emailVerified',
    type: 'boolean',
    description: 'Indique si Google considère l’adresse e-mail comme vérifiée.',
    isOptional: true,
  },
  {
    name: 'groups',
    type: 'string[]',
    description: 'Identifiants de rôles de groupes Google Workspace facultatifs, résolus au préalable.',
    isOptional: true,
  },
]}
/>

## Classe MastraRBACGoogle {/* #mastrarbacgoogle-class */}

La classe `MastraRBACGoogle` associe les groupes Google Workspace aux permissions Mastra. Elle récupère les groupes d’un utilisateur dans l’API Directory du Google Admin SDK, puis les résout à l’aide d’un mappage de rôles configurable. Utilisez-la avec `MastraAuthGoogle` ou tout autre Provider d’authentification.

:::note

RBAC nécessite une licence Enterprise Edition valide. La fonctionnalité est utilisable sans licence en développement afin de pouvoir l’essayer localement, mais une licence est nécessaire en production. [Contactez l’équipe commerciale](https://mastra.ai/contact) pour en savoir plus.

:::

### Exemple d’utilisation {/* #usage-example-1 */}

Utilisez `MastraRBACGoogle` avec un Provider d’authentification en le transmettant à l’option `rbac` :

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthGoogle, MastraRBACGoogle } from '@mastra/auth-google'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthGoogle(),
    rbac: new MastraRBACGoogle({
      serviceAccount: {
        clientEmail: process.env.GOOGLE_SERVICE_ACCOUNT_EMAIL!,
        privateKey: process.env.GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY!,
        subject: process.env.GOOGLE_WORKSPACE_ADMIN_EMAIL!,
      },
      roleMapping: {
        'admins@example.com': ['*'],
        'engineering@example.com': ['agents:*', 'workflows:*', 'tools:*'],
        'viewers@example.com': ['agents:read', 'workflows:read'],
        _default: [],
      },
    }),
  },
})
```

Pour utiliser Google Workspace RBAC avec un autre Provider d’authentification, transmettez une fonction `getUserKey` qui détermine la clé utilisateur de l’API Google Directory à partir de l’objet utilisateur de l’autre Provider :

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthAuth0 } from '@mastra/auth-auth0'
import { MastraRBACGoogle } from '@mastra/auth-google'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthAuth0(),
    rbac: new MastraRBACGoogle({
      getUserKey: user => user.email,
      serviceAccount: {
        clientEmail: process.env.GOOGLE_SERVICE_ACCOUNT_EMAIL!,
        privateKey: process.env.GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY!,
        subject: process.env.GOOGLE_WORKSPACE_ADMIN_EMAIL!,
      },
      roleMapping: {
        'engineering@example.com': ['agents:*', 'workflows:*'],
        'admins@example.com': ['*'],
        _default: [],
      },
    }),
  },
})
```

### Paramètres du constructeur {/* #constructor-parameters-1 */}

<PropertiesTable
  content={[
  {
    name: 'roleMapping',
    type: 'RoleMapping',
    description:
      "Associe les identifiants de rôles de groupes Google Workspace à des tableaux de chaînes de permissions Mastra. Utilisez `'_default'` pour attribuer des permissions aux utilisateurs qui ne correspondent à aucun groupe. Prend en charge les caractères génériques tels que `'*'` (accès complet) et `'agents:*'` (toutes les actions des Agents).",
    isOptional: false,
  },
  {
    name: 'accessToken',
    type: 'string',
    description: 'Jeton d’accès à l’API Workspace Directory obtenu au préalable.',
    isOptional: true,
  },
  {
    name: 'getAccessToken',
    type: '() => Promise<string> | string',
    description: 'Fonction de rappel qui renvoie un jeton d’accès à l’API Workspace Directory.',
    isOptional: true,
  },
  {
    name: 'serviceAccount',
    type: 'GoogleWorkspaceServiceAccount',
    description: 'Identifiants du compte de service pour un accès délégué à l’échelle du domaine à l’API Directory.',
    isOptional: true,
    properties: [
      {
        type: 'GoogleWorkspaceServiceAccount',
        parameters: [
          {
            name: 'clientEmail',
            type: 'string',
            description: 'Adresse e-mail du compte de service Google.',
          },
          {
            name: 'privateKey',
            type: 'string',
            description: 'Clé privée encodée au format PEM. Les valeurs `\\n` échappées provenant de fichiers .env sont prises en charge.',
          },
          {
            name: 'privateKeyId',
            type: 'string',
            description: 'Identifiant facultatif de la clé privée.',
            isOptional: true,
          },
          {
            name: 'subject',
            type: 'string',
            description: 'Utilisateur administrateur Workspace à représenter au moyen de la délégation à l’échelle du domaine.',
            isOptional: true,
          },
          {
            name: 'scopes',
            type: 'string[]',
            description: 'Scopes OAuth du jeton du compte de service.',
            isOptional: true,
            defaultValue: "['https://www.googleapis.com/auth/admin.directory.group.readonly']",
          },
        ],
      },
    ],
  },
  {
    name: 'getUserKey',
    type: '(user: unknown) => string | undefined',
    description: 'Extrait la valeur `userKey` de l’API Directory à partir d’un utilisateur authentifié. Valeur par défaut : `user.email`.',
    isOptional: true,
  },
  {
    name: 'mapGroupToRoles',
    type: '(group: GoogleWorkspaceGroup) => string[]',
    description: 'Associe un groupe Google Workspace à des identifiants de rôles. Valeur par défaut : `[group.email]`.',
    isOptional: true,
  },
  {
    name: 'cache',
    type: 'PermissionCacheOptions',
    description: 'Configure le cache LRU utilisé pour les recherches de groupes.',
    isOptional: true,
    properties: [
      {
        type: 'PermissionCacheOptions',
        parameters: [
          {
            name: 'maxSize',
            type: 'number',
            description: 'Nombre maximal d’utilisateurs à placer en cache.',
            isOptional: true,
            defaultValue: '1000',
          },
          {
            name: 'ttlMs',
            type: 'number',
            description: 'Durée de vie, en millisecondes.',
            isOptional: true,
            defaultValue: '60000',
          },
        ],
      },
    ],
  },
]}
/>

### Méthodes RBAC {/* #rbac-methods */}

#### `getRoles(user)` {/* #getrolesuser */}

Renvoie les identifiants de rôles des groupes Google Workspace d’un utilisateur.

```typescript
const roles = await rbac.getRoles(user)
```

Renvoie : `Promise<string[]>`

#### `getPermissions(user)` {/* #getpermissionsuser */}

Renvoie les permissions Mastra déterminées à partir des groupes Google Workspace et de `roleMapping`.

```typescript
const permissions = await rbac.getPermissions(user)
```

Renvoie : `Promise<string[]>`

#### `hasPermission(user, permission)` {/* #haspermissionuser-permission */}

Vérifie si un utilisateur possède une permission.

```typescript
const canReadAgents = await rbac.hasPermission(user, 'agents:read')
```

Renvoie : `Promise<boolean>`

#### `hasRole(user, role)` {/* #hasroleuser-role */}

Vérifie si un rôle de groupe Google Workspace précis a été associé à l’utilisateur.

```typescript
const isAdmin = await rbac.hasRole(user, 'admins@example.com')
```

Renvoie : `Promise<boolean>`

#### `hasAllPermissions(user, permissions)` {/* #hasallpermissionsuser-permissions */}

Vérifie si un utilisateur possède toutes les permissions demandées.

```typescript
const canManageAgents = await rbac.hasAllPermissions(user, ['agents:read', 'agents:update'])
```

Renvoie : `Promise<boolean>`

#### `hasAnyPermission(user, permissions)` {/* #hasanypermissionuser-permissions */}

Vérifie si un utilisateur possède au moins l’une des permissions demandées.

```typescript
const canReadSomething = await rbac.hasAnyPermission(user, ['agents:read', 'workflows:read'])
```

Renvoie : `Promise<boolean>`

#### `getAvailableRoles()` {/* #getavailableroles */}

Renvoie les identifiants de rôles configurés dans `roleMapping`, à l’exclusion de `_default`.

```typescript
const roles = await rbac.getAvailableRoles()
```

Renvoie : `Promise<{ id: string; name: string }[]>`

#### `getPermissionsForRole(roleId)` {/* #getpermissionsforroleroleid */}

Renvoie les permissions configurées pour un identifiant de rôle.

```typescript
const permissions = await rbac.getPermissionsForRole('engineering@example.com')
```

Renvoie : `Promise<string[]>`

#### `clearCache()` {/* #clearcache */}

Efface du cache toutes les recherches de groupes Google Workspace.

```typescript
rbac.clearCache()
```

Renvoie : `void`

#### `clearUserCache(userKey)` {/* #clearusercacheuserkey */}

Efface du cache la recherche de groupes associée à une clé utilisateur de l’API Directory, par exemple une adresse e-mail.

```typescript
rbac.clearUserCache('user@example.com')
```

Renvoie : `void`

#### `getCacheStats()` {/* #getcachestats */}

Renvoie la taille actuelle et la taille maximale du cache des recherches de groupes.

```typescript
const stats = rbac.getCacheStats()
```

Renvoie : `{ size: number; maxSize: number }`

### Configuration supplémentaire {/* #additional-configuration */}

`MastraRBACGoogle` utilise `GET https://admin.googleapis.com/admin/directory/v1/groups?userKey=...` et gère la pagination. Pour les déploiements Google Workspace en production, fournissez un compte de service doté d’une délégation à l’échelle du domaine. Si votre application gère déjà les jetons de l’API Google, transmettez plutôt `accessToken` ou `getAccessToken`.

Si `user.groups` est déjà un tableau, `MastraRBACGoogle` utilise cette valeur et n’appelle pas l’API Directory. Un tableau `groups` vide signifie que l’utilisateur ne possède aucun rôle de groupe Google ; les permissions `_default` lui sont alors attribuées si `_default` est configuré.

`MastraRBACGoogle` ne lit pas automatiquement les variables d’environnement du compte de service. Transmettez les identifiants du compte de service au moyen de l’option `serviceAccount`, ou fournissez `accessToken` ou `getAccessToken`.

## Voir aussi {/* #related */}

[Documentation de l’authentification Google](/docs/server/auth/google)
