---
title: "Référence : Mastra.listMemory() | Core"
description: "Documentation de la méthode `Mastra.listMemory()` de Mastra, qui renvoie toutes les instances de mémoire enregistrées."
packages:
  - "@mastra/core"
  - "@mastra/libsql"
  - "@mastra/memory"
---

# Mastra.listMemory()

La méthode `.listMemory()` renvoie toutes les instances de mémoire enregistrées auprès de l’instance Mastra.

## Exemple d’utilisation

```typescript
const memoryInstances = mastra.listMemory()

for (const [key, memory] of Object.entries(memoryInstances)) {
  console.log(`Memory "${key}": ${memory.id}`)
}
```

## Paramètres

Cette méthode ne prend aucun paramètre.

## Valeur renvoyée

<PropertiesTable
  content={[
  {
    name: 'memory',
    type: 'Record<string, MastraMemory>',
    description: 'Objet contenant toutes les instances de mémoire enregistrées, indexées par leurs clés de registre.',
  },
]}
/>

## Exemple : vérifier la mémoire enregistrée

```typescript
import { Mastra } from '@mastra/core'
import { Memory } from '@mastra/memory'
import { LibSQLStore } from '@mastra/libsql'

const conversationMemory = new Memory({
  id: 'conversation-memory',
  storage: new LibSQLStore({ id: 'conversation-store', url: ':memory:' }),
})

const analyticsMemory = new Memory({
  id: 'analytics-memory',
  storage: new LibSQLStore({ id: 'analytics-store', url: ':memory:' }),
})

const mastra = new Mastra({
  memory: {
    conversationMemory,
    analyticsMemory,
  },
})

// List all registered memory instances
const allMemory = mastra.listMemory()
console.log(Object.keys(allMemory)) // ["conversationMemory", "analyticsMemory"]
```

## Voir aussi

- [Mastra.getMemory()](/reference/core/getMemory)
- [Présentation de la mémoire](/docs/memory/overview)
