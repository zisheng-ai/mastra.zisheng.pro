---
title: "Référence : Mastra.getTool() | Core"
description: "Documentation de la méthode `Mastra.getTool()`, qui récupère un Tool à partir de sa clé d’enregistrement Mastra."
packages:
  - "@mastra/core"
---

# Mastra.getTool()

La méthode `.getTool()` récupère un Tool depuis le registre au niveau de Mastra à partir de sa clé d’enregistrement.

## Exemple d’utilisation {/* #usage-example */}

Enregistrez un Tool sous la clé `weather`, puis utilisez cette clé pour le récupérer.

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core/mastra'
import { weatherTool } from './tools/weather-tool'

export const mastra = new Mastra({
  tools: {
    weather: weatherTool,
  },
})

const tool = mastra.getTool('weather')
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'TToolName extends keyof TTools',
    description: 'Clé d’enregistrement du Tool dans le registre Mastra `tools`.',
  },
]}
/>

## Voir aussi {/* #related */}

- [Partager des Tools entre les agents](/docs/agents/using-tools#share-tools-across-agents)
- [Classe Mastra](/reference/core/mastra-class)
- [Mastra.getToolById()](/reference/core/getToolById)
- [Mastra.listTools()](/reference/core/listTools)
