---
title: "Référence : MastraModèleGateway | Core"
description: "Base class et interface pour creating personnalisé modèle gateways"
packages:
  - "@mastra/core"
---

# MastraModèleGateway

Abstract base class pour implementing personnalisé modèle gateways. Gateways gère provider-specific logic pour accessing language models, including provider configuration, authentication, URL construction, et modèle instantiation.

Use `MastraModelGatewayInterface` when you want à fournit a plain objet gateway instead de extending the base class.

## Class overview

```typescript
import { MastraModelGateway, type ProviderConfig } from '@mastra/core/llm'
import { createOpenAICompatible } from '@ai-sdk/openai-compatible-v5'
import type { LanguageModelV2 } from '@ai-sdk/provider-v5'

class MyCustomGateway extends MastraModelGateway {
  readonly id = 'custom'
  readonly name = 'My Custom Gateway'

  async fetchProviders(): Promise<Record<string, ProviderConfig>> {
    return {
      'my-provider': {
        name: 'My Provider',
        models: ['__GATEWAY_OPENAI_MODEL__', '__GATEWAY_ANTHROPIC_MODEL_SONNET__'],
        apiKeyEnvVar: 'MY_API_KEY',
        gateway: this.id,
      },
    }
  }

  buildUrl(modelId: string, envVars?: Record<string, string>): string {
    return 'https://api.my-provider.com/v1'
  }

  async getApiKey(modelId: string): Promise<string> {
    const apiKey = process.env.MY_API_KEY
    if (!apiKey) throw new Error('MY_API_KEY not set')
    return apiKey
  }

  async resolveLanguageModel({
    modelId,
    providerId,
    apiKey,
  }: {
    modelId: string
    providerId: string
    apiKey: string
  }): Promise<LanguageModelV2> {
    const baseURL = this.buildUrl(`${providerId}/${modelId}`)
    return createOpenAICompatible({
      name: providerId,
      apiKey,
      baseURL,
    }).chatModel(modelId)
  }
}
```

## Obligatoire properties

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description:
      'Unique identifiant pour the gateway. Ce ID est used comme the prefix pour tous les providers depuis this gateway (e.g., "netlify/anthropic"). Exception: models.dev est a provider registry et doesn\'t utilise a prefix.',
  },
  {
    name: 'name',
    type: 'string',
    description: 'Human-readable nom pour the gateway.',
  },
]}
/>

## Obligatoire methods

### `fetchProviders()`

Fetches provider configurations depuis the gateway.

**Renvoie:** `Promise<Record<string, ProviderConfig>>`

**FournisseurConfig Structure:**

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: 'Display nom de the provider',
  },
  {
    name: 'models',
    type: 'string[]',
    description: 'Array de disponible modèle IDs',
  },
  {
    name: 'apiKeyEnvVar',
    type: 'string | string[]',
    description: 'Environment variable(s) pour API key',
  },
  {
    name: 'gateway',
    type: 'string',
    description: 'Gateway identifier',
  },
  {
    name: 'url',
    type: 'string',
    isOptional: true,
    description: 'Facultatif base API URL',
  },
  {
    name: 'apiKeyHeader',
    type: 'string',
    isOptional: true,
    description: 'Facultatif personnalisé auth header name',
  },
  {
    name: 'docUrl',
    type: 'string',
    isOptional: true,
    description: 'Facultatif documentation URL',
  },
]}
/>

### `buildUrl()`

Génère the API URL pour a specific model/provider combination.

Si vousr provider URL contains placeholders such comme `${ACCOUNT_ID}`, résout them inside `buildUrl()` depuis `envVars` ou `process.env` avant returning the final URL.

**Paramètres:**

<PropertiesTable
  content={[
  {
    name: 'modelId',
    type: 'string',
    description: 'Full modèle ID (e.g., "custom/my-provider/model-1")',
  },
  {
    name: 'envVars',
    type: 'Record<string, string>',
    isOptional: true,
    description: 'Facultatif environment variables',
  },
]}
/>

**Renvoie:** `string | undefined | Promise<string | undefined>`

### `getApiKey()`

Retrieves the API clé pour authentication.

**Paramètres:**

<PropertiesTable
  content={[
  {
    name: 'modelId',
    type: 'string',
    description: 'Full modèle ID',
  },
]}
/>

**Renvoie:** `Promise<string>`

### `resolveAuth()`

Resolves credentials avant Mastra creates the language model. Implement this facultatif hook when a gateway owns authentication. If omitted, Mastra falls back à `getApiKey()`.

**Paramètres:**

<PropertiesTable
  content={[
  {
    name: 'request',
    type: 'GatewayAuthRequest',
    description:
      'Incoming requête context containing gatewayId, providerId, modelId, et routerId pour the gateway à inspect et validate.',
  },
]}
/>

**Renvoie:** `GatewayAuthResult | undefined | Promise<GatewayAuthResult | undefined>`

Un `GatewayAuthResult` peut inclut `apiKey`, `bearerToken`, `headers`, et an facultatif `source` field à trace where auth came from.

### `resolveLanguageModel()`

Crée a language modèle instance.

**Paramètres:**

<PropertiesTable
  content={[
  {
    name: 'modelId',
    type: 'string',
    description: 'Le modèle ID',
  },
  {
    name: 'providerId',
    type: 'string',
    description: 'The provider ID',
  },
  {
    name: 'apiKey',
    type: 'string',
    description: 'The API clé pour authentication',
  },
]}
/>

**Renvoie:** `Promise<LanguageModelV2> | LanguageModelV2`

## Instance methods

### `getId()`

Renvoie the gateway's unique identifiant.

**Renvoie:** `string` - The gateway's `id` property

## Modèle ID format

For true gateways, the gateway ID est used comme a prefix et models sont accessed using this format:

```
[gateway-id]/[provider]/[model]
```

Exemples:

- Gateway avec `id = 'custom'`: `'custom/my-provider/model-1'`

## Intégré implementations

- **NetlifyGateway** - Netlify AI Gateway integration
- **ModèlesDevGateway** - Registry de OpenAI-compatible providers

## Voir aussi

- [Personnalisé Gateways Guide](/models/gateways/custom-gateways): Complet guide à creating personnalisé gateways
- [Mastra.addGateway()](/reference/core/addGateway): Ajouter a gateway à Mastra
- [Mastra.getGateway()](/reference/core/getGateway): Obtenir gateway par registration key
- [Mastra.getGatewayById()](/reference/core/getGatewayById): Obtenir gateway par ID
- [Mastra.listGateways()](/reference/core/listGateways): Lister tous les gateways
