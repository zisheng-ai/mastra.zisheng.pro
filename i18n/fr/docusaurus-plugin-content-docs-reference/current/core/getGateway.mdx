---
title: "Référence : Mastra.getGateway() | Core"
description: "Retrieve a registered gateway par its registration key"
packages:
  - "@mastra/core"
---

# Mastra.getGateway()

Retrieves a gateway par its registration clé (the clé used when adding the gateway à Mastra).

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({
  gateways: {
    myGateway: new MyCustomGateway(),
  },
})

const gateway = mastra.getGateway('myGateway')
console.log(gateway.name) // 'My Custom Gateway'
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'key',
    type: 'string',
    description: 'The registration clé de the gateway',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'gateway',
    type: 'MastraModelGateway',
    description: 'The gateway instance',
  },
]}
/>

## Throws

Throws an error if aucun gateway est found avec the specified key.

## Voir aussi

- [Mastra.getGatewayById()](/reference/core/getGatewayById): Obtenir gateway par ID
- [Mastra.listGateways()](/reference/core/listGateways): Lister tous les gateways
- [Mastra.addGateway()](/reference/core/addGateway): Ajouter a gateway
- [MastraModèleGateway](/reference/core/mastra-model-gateway): Gateway base class
- [Personnalisé Gateways Guide](/models/gateways/custom-gateways): Creating personnalisé gateways
