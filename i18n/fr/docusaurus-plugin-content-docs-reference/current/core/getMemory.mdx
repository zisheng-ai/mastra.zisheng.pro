---
title: "Référence : Mastra.getMemory() | Core"
description: "Documentation de la méthode `Mastra.getMemory()` de Mastra, qui récupère une instance de mémoire enregistrée à partir de sa clé de registre."
packages:
  - "@mastra/core"
  - "@mastra/libsql"
  - "@mastra/memory"
---

# Mastra.getMemory()

La méthode `.getMemory()` récupère une instance de mémoire du registre Mastra à partir de sa clé. Les instances de mémoire sont enregistrées dans le constructeur Mastra et peuvent être référencées par les agents stockés.

## Exemple d’utilisation

```typescript
const memory = mastra.getMemory('conversationMemory')

// Use the memory instance
const thread = await memory.createThread({
  resourceId: 'user-123',
  title: 'New Conversation',
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'key',
    type: 'TMemoryKey extends keyof TMemory',
    description:
      'Clé de registre de l’instance de mémoire à récupérer. Elle doit correspondre à une clé utilisée lors de l’enregistrement d’une mémoire dans le constructeur Mastra.',
  },
]}
/>

## Valeur renvoyée

<PropertiesTable
  content={[
  {
    name: 'memory',
    type: 'TMemory[TMemoryKey]',
    description: 'Instance de mémoire correspondant à la clé spécifiée. Génère une erreur si la mémoire est introuvable.',
  },
]}
/>

## Exemple : enregistrer et récupérer une mémoire

```typescript
import { Mastra } from '@mastra/core'
import { Memory } from '@mastra/memory'
import { LibSQLStore } from '@mastra/libsql'

const conversationMemory = new Memory({
  storage: new LibSQLStore({ id: 'conversation-store', url: ':memory:' }),
})

const mastra = new Mastra({
  memory: {
    conversationMemory,
  },
})

// Later, retrieve the memory instance
const memory = mastra.getMemory('conversationMemory')
```

## Voir aussi

- [Mastra.listMemory()](/reference/core/listMemory)
- [Présentation de la mémoire](/docs/memory/overview)
