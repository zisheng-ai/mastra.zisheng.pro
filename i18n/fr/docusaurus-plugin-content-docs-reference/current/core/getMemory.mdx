---
title: "Référence : Mastra.getMémoire() | Core"
description: "Documentation pour the `Mastra.getMemory()` method dans Mastra, which retrieves a registered memory instance par its registry key."
packages:
  - "@mastra/core"
  - "@mastra/libsql"
  - "@mastra/memory"
---

# Mastra.getMémoire()

The `.getMemory()` method retrieves a memory instance depuis the Mastra registry par its key. Mémoire instances sont registered dans the Mastra constructor et peut be referenced par stored agents.

## Exemple d’utilisation

```typescript
const memory = mastra.getMemory('conversationMemory')

// Use the memory instance
const thread = await memory.createThread({
  resourceId: 'user-123',
  title: 'New Conversation',
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'key',
    type: 'TMemoryKey extends keyof TMemory',
    description:
      'The registry clé de the memory instance à retrieve. Must match a clé used when registering memory dans the Mastra constructor.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'memory',
    type: 'TMemory[TMemoryKey]',
    description: 'The memory instance avec the specified key. Throws an error if the memory est ne pas found.',
  },
]}
/>

## Exemple: Registering et retrieving memory

```typescript
import { Mastra } from '@mastra/core'
import { Memory } from '@mastra/memory'
import { LibSQLStore } from '@mastra/libsql'

const conversationMemory = new Memory({
  storage: new LibSQLStore({ id: 'conversation-store', url: ':memory:' }),
})

const mastra = new Mastra({
  memory: {
    conversationMemory,
  },
})

// Later, retrieve the memory instance
const memory = mastra.getMemory('conversationMemory')
```

## Voir aussi

- [Mastra.listMémoire()](/reference/core/listMemory)
- [Mémoire overview](/docs/memory/overview)
