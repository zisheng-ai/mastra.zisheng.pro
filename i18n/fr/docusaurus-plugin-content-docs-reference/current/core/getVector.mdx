---
title: "Référence : Mastra.getVector() | Core"
description: "Documentation pour the `Mastra.getVector()` method dans Mastra, which retrieves a vector store par name."
packages:
  - "@mastra/core"
---

# Mastra.getVector()

The `.getVector()` method est used à retrieve a vector store par its name. La méthode accepts a unique `string` paramètre pour the vector store's name.

## Exemple d’utilisation

```typescript
mastra.getVector('testVectorStore')
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'TVectorName extends keyof TVectors',
    description:
      'The nom de the vector store à retrieve. Must be a valid vector store nom that exists dans the Mastra configuration.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'vector',
    type: 'TVectors[TVectorName]',
    description: 'The vector store instance avec the specified name. Throws an error if the vector store est ne pas found.',
  },
]}
/>

## Voir aussi

- [Vector stores overview](/guides/rag/vector-databases)
- [RAG overview](/guides/rag/overview)
