---
title: "Référence : getScorerById() | Core"
description: "Documentation de la méthode `getScorerById()` de Mastra, qui récupère un Scorer par sa propriété id plutôt que par sa clé d’enregistrement."
packages:
  - "@mastra/core"
---

# getScorerById()

La méthode `getScorerById()` récupère un Scorer en recherchant sa propriété `id` (ou sa propriété `name` en solution de repli) plutôt que sa clé d’enregistrement. Elle est utile lorsque vous connaissez l’id du Scorer, sans nécessairement savoir comment il a été enregistré dans l’instance Mastra.

## Exemple d’utilisation

```typescript
import { mastra } from './mastra'

// Get a scorer by its id property
const relevancyScorer = mastra.getScorerById('answer-relevancy-scorer')

const weatherAgent = mastra.getAgent('weatherAgent')

// Use the scorer to evaluate an AI output
await weatherAgent.generate('What is the weather in Rome', {
  scorers: {
    answerRelevancy: {
      scorer: relevancyScorer,
    },
  },
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description:
      "Propriété id du Scorer à récupérer. Elle doit correspondre au champ 'id' spécifié lors de la création du Scorer avec createScorer(). La méthode recherche également la propriété 'name' en solution de repli.",
    isOptional: false,
  },
]}
/>

## Valeur renvoyée

<PropertiesTable
  content={[
  {
    name: 'scorer',
    type: 'MastraScorer',
    description: 'Instance MastraScorer qui possède la propriété id (ou name) correspondante.',
  },
]}
/>

## Gestion des erreurs

Cette méthode génère une `MastraError` si :

- Aucun Scorer avec l’id spécifié n’est trouvé
- Aucun Scorer n’est enregistré auprès de l’instance Mastra

```typescript
try {
  const scorer = mastra.getScorerById('non-existent-scorer')
} catch (error) {
  if (error.id === 'MASTRA_GET_SCORER_BY_ID_NOT_FOUND') {
    console.log('Scorer with that id not found')
  }
}
```

## Voir aussi

- [getScorer()](/reference/core/getScorer): Récupérer un Scorer par sa clé d’enregistrement
- [listScorers()](/reference/core/listScorers): Récupérer tous les Scorers enregistrés
- [createScorer()](/reference/evals/create-scorer): Créer des Scorers avec des id
