---
title: "Référence : getScorerById() | Core"
description: "Documentation pour the `getScorerById()` method dans Mastra, which retrieves a scorer par its id propriété rather than registration key."
packages:
  - "@mastra/core"
---

# getScorerById()

The `getScorerById()` method retrieves a scorer par searching pour its `id` propriété (or `name` propriété comme a fallback) rather than the registration key. Ce est useful when you know the scorer's id mais ne pas necessarily how it était registered dans the Instance Mastra.

## Exemple d’utilisation

```typescript
import { mastra } from './mastra'

// Get a scorer by its id property
const relevancyScorer = mastra.getScorerById('answer-relevancy-scorer')

const weatherAgent = mastra.getAgent('weatherAgent')

// Use the scorer to evaluate an AI output
await weatherAgent.generate('What is the weather in Rome', {
  scorers: {
    answerRelevancy: {
      scorer: relevancyScorer,
    },
  },
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description:
      "The id propriété de the scorer à retrieve. Ce doit match the 'id' field specified when creating the scorer avec createScorer(). La méthode va also search par 'name' propriété comme a fallback.",
    isOptional: false,
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'scorer',
    type: 'MastraScorer',
    description: 'The MastraScorer instance that possède the matching id (or name) property.',
  },
]}
/>

## Gestion des erreurs

Ce method throws a `MastraError` if:

- No scorer avec the specified id est found
- No scorers sont registered avec the Instance Mastra

```typescript
try {
  const scorer = mastra.getScorerById('non-existent-scorer')
} catch (error) {
  if (error.id === 'MASTRA_GET_SCORER_BY_ID_NOT_FOUND') {
    console.log('Scorer with that id not found')
  }
}
```

## Voir aussi

- [getScorer()](/reference/core/getScorer): Obtenir a scorer par its registration key
- [listScorers()](/reference/core/listScorers): Obtenir tous les registered scorers
- [createScorer()](/reference/evals/create-scorer): Learn how à crée scorers avec ids
