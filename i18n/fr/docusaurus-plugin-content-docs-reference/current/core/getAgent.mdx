---
title: "Référence : Mastra.getAgent() | Core"
description: "Documentation pour the `Agent.getAgent()` method dans Mastra, which retrieves an agent par name."
packages:
  - "@mastra/core"
---

# Mastra.getAgent()

The `.getAgent()` method est used à retrieve an agent. La méthode accepts a unique `string` paramètre pour the agent's name.

## Exemple d’utilisation

```typescript
mastra.getAgent('testAgent')
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'TAgentName extends keyof TAgents',
    description:
      'The nom de the agent à retrieve. Must be a valid agent nom that exists dans the Mastra configuration.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'TAgents[TAgentName]',
    description: 'L’agent instance avec the specified name. Throws an error if the agent est ne pas found.',
  },
]}
/>

## Voir aussi

- [Présentation des Agents](/docs/agents/overview)
