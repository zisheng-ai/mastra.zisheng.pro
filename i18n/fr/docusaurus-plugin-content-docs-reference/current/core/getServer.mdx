---
title: "Référence : Mastra.getServer() | Core"
description: "Documentation de la méthode `Mastra.getServer()` de Mastra, qui récupère la configuration de serveur définie."
packages:
  - "@mastra/core"
---

# Mastra.getServer()

La méthode `.getServer()` permet de récupérer la configuration de serveur définie dans l’instance Mastra.

## Exemple d’utilisation

```typescript
mastra.getServer()
```

## Paramètres

Cette méthode n’accepte aucun paramètre.

## Valeur renvoyée

<PropertiesTable
  content={[
  {
    name: 'server',
    type: 'ServerConfig | undefined',
    description:
      'Configuration de serveur définie, comprenant le port, l’hôte, studioBase, le délai d’expiration, les routes d’API, les middlewares, les paramètres CORS et les options de build, ou undefined si aucun serveur n’a été configuré.',
  },
]}
/>

## Voir aussi

- [Déploiement d’un serveur](/docs/deployment/mastra-server)
- [Configuration du serveur](/docs/server/custom-api-routes)
