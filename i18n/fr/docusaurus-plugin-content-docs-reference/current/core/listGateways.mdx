---
title: "Référence : Mastra.listGateways() | Core"
description: "Lister tous les registered gateways"
packages:
  - "@mastra/core"
---

# Mastra.listGateways()

Renvoie a record de tous les registered gateways, indexed par their registration keys.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({
  gateways: {
    myGateway: new MyCustomGateway(),
    anotherGateway: new AnotherGateway(),
  },
})

const gateways = mastra.listGateways()
console.log(Object.keys(gateways)) // ['myGateway', 'anotherGateway']

// Iterate over all gateways
for (const [key, gateway] of Object.entries(gateways)) {
  console.log(`${key}: ${gateway.name}`)
}
```

## Paramètres

None.

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'gateways',
    type: 'Record<string, MastraModelGateway>',
    description: 'Un record de gateway instances indexed par registration key',
  },
]}
/>

## Voir aussi

- [Mastra.getGateway()](/reference/core/getGateway): Obtenir gateway par registration key
- [Mastra.getGatewayById()](/reference/core/getGatewayById): Obtenir gateway par ID
- [Mastra.addGateway()](/reference/core/addGateway): Ajouter a gateway
- [MastraModèleGateway](/reference/core/mastra-model-gateway): Gateway base class
- [Personnalisé Gateways Guide](/models/gateways/custom-gateways): Creating personnalisé gateways
