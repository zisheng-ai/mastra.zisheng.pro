---
title: "Référence : Mastra.listGateways() | Core"
description: "Liste toutes les passerelles enregistrées"
packages:
  - "@mastra/core"
---

# Mastra.listGateways()

Renvoie un enregistrement de toutes les passerelles enregistrées, indexées par leurs clés d’enregistrement.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({
  gateways: {
    myGateway: new MyCustomGateway(),
    anotherGateway: new AnotherGateway(),
  },
})

const gateways = mastra.listGateways()
console.log(Object.keys(gateways)) // ['myGateway', 'anotherGateway']

// Iterate over all gateways
for (const [key, gateway] of Object.entries(gateways)) {
  console.log(`${key}: ${gateway.name}`)
}
```

## Paramètres

Aucun.

## Valeur renvoyée

<PropertiesTable
  content={[
  {
    name: 'gateways',
    type: 'Record<string, MastraModelGateway>',
    description: 'Enregistrement des instances de passerelle indexées par clé d’enregistrement',
  },
]}
/>

## Voir aussi

- [Mastra.getGateway()](/reference/core/getGateway): Récupérer une passerelle par clé d’enregistrement
- [Mastra.getGatewayById()](/reference/core/getGatewayById): Récupérer une passerelle par ID
- [Mastra.addGateway()](/reference/core/addGateway): Ajouter une passerelle
- [MastraModelGateway](/reference/core/mastra-model-gateway): Classe de base des passerelles
- [Guide des passerelles personnalisées](/models/gateways/custom-gateways): Créer des passerelles personnalisées
