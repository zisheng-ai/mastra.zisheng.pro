---
title: "Référence : classe Mastra | Core"
description: "Documentation de la classe `Mastra` dans Mastra, point d’entrée principal pour gérer les Agents, les Workflows, les serveurs MCP et les points de terminaison du serveur."
packages:
  - "@mastra/core"
  - "@mastra/libsql"
  - "@mastra/loggers"
---

# Classe Mastra

La classe `Mastra` est l’orchestrateur central de toute application Mastra ; elle gère les Agents, les Workflows, le stockage, les logs, l’observabilité et bien plus encore. En général, vous créez une seule instance de `Mastra` pour coordonner votre application.

Considérez `Mastra` comme un registre de niveau supérieur dans lequel vous enregistrez les Agents, les Workflows, les Tools et les autres composants qui doivent être accessibles dans toute votre application.

## Exemple d’utilisation {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { PinoLogger } from '@mastra/loggers'
import { LibSQLStore } from '@mastra/libsql'
import { weatherWorkflow } from './workflows/weather-workflow'
import { weatherAgent } from './agents/weather-agent'

export const mastra = new Mastra({
  workflows: { weatherWorkflow },
  agents: { weatherAgent },
  storage: new LibSQLStore({
    id: 'mastra-storage',
    url: ':memory:',
  }),
  logger: new PinoLogger({
    name: 'Mastra',
    level: 'info',
  }),
})
```

Activez l’envoi planifié des notifications lorsque les enregistrements de notifications différées et les résumés de notifications doivent être remis automatiquement par l’intermédiaire du planificateur de Workflows :

```typescript title="src/mastra/index.ts"
export const mastra = new Mastra({
  agents: { supportAgent },
  storage,
  notifications: {
    dispatch: {
      enabled: true,
      cron: '*/1 * * * *',
      batchSize: 100,
    },
  },
})
```

`notifications.dispatch.enabled` permet à un Workflow de distribution interne de s’exécuter avec l’expression cron par défaut `*/1 * * * *`. Le distributeur lit dans le stockage les enregistrements de notifications arrivés à échéance, regroupe les résumés par `agentId`, `resourceId` et `threadId`, puis émet des signaux via l’environnement d’exécution des fils de l’Agent. Il ne s’agit pas d’un point d’entrée destiné aux utilisateurs. La planification de la distribution (et le planificateur de Workflows qui la sous-tend) ne s’active qu’à la première notification différée ou résumée ; les applications qui ne différèrent jamais de notifications n’exécutent donc aucun planificateur.

## Paramètres du constructeur {/* #constructor-parameters */}

Consultez la [référence de configuration](/reference/configuration) pour obtenir une documentation détaillée de toutes les options de configuration disponibles.

<PropertiesTable
  content={[
  {
    name: 'agents',
    type: 'Record<string, Agent>',
    description: 'Instances d’Agent à enregistrer, indexées par nom',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'tools',
    type: 'Record<string, ToolApi>',
    description:
      'Instances de Tool à enregistrer. Les clés sont les clés d’enregistrement utilisées par [`getTool()`](/reference/core/getTool), et les valeurs sont les instances de Tool. Utilisez [`getToolById()`](/reference/core/getToolById) pour rechercher un ID intrinsèque et [`listTools()`](/reference/core/listTools) pour lire le registre.',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'storage',
    type: 'MastraCompositeStore',
    description: 'Instance du moteur de stockage utilisée pour conserver les données',
    isOptional: true,
  },
  {
    name: 'vectors',
    type: 'Record<string, MastraVector>',
    description:
      'Instance de base vectorielle utilisée pour la recherche sémantique et les Tools vectoriels (par exemple Pinecone, PgVector ou Qdrant)',
    isOptional: true,
  },
  {
    name: 'logger',
    type: 'Logger',
    description: 'Instance de logger créée avec new PinoLogger()',
    isOptional: true,
    defaultValue: 'Logger de console avec le niveau INFO',
  },
  {
    name: 'idGenerator',
    type: '(context?: IdGeneratorContext) => string',
    description:
      'Fonction personnalisée de génération d’ID. Utilisée par les Agents, les Workflows, la Memory et d’autres composants pour générer des identifiants uniques. Reçoit un contexte facultatif tel que idType, source, entityId et threadId afin de prendre en charge des formats d’ID dépendant du contexte.',
    isOptional: true,
  },
  {
    name: 'workflows',
    type: 'Record<string, Workflow>',
    description:
      'Workflows à enregistrer. Structurés sous forme de paires clé-valeur, où les clés sont les noms des Workflows et les valeurs leurs instances.',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'tts',
    type: 'Record<string, MastraVoice>',
    isOptional: true,
    description: 'Providers de synthèse vocale à partir de texte',
  },
  {
    name: 'observability',
    type: 'ObservabilityEntrypoint',
    isOptional: true,
    description: 'Configuration de l’observabilité pour le traçage et la surveillance',
  },
  {
    name: 'environment',
    type: 'string',
    isOptional: true,
    description:
      'Nom de l’environnement de déploiement (par exemple `production`, `staging`, `development`). Lorsqu’il est défini, il est automatiquement joint à tous les signaux d’observabilité afin de permettre leur filtrage par environnement sans transmettre `tracingOptions.metadata.environment` à chaque appel. Utilise `process.env.NODE_ENV` comme valeur de repli lorsqu’il n’est pas défini ; reste indéfini si aucune des deux valeurs ne l’est. La valeur `tracingOptions.metadata.environment` propre à l’appel est toujours prioritaire.',
  },
  {
    name: 'deployer',
    type: 'MastraDeployer',
    isOptional: true,
    description: 'Instance de MastraDeployer destinée à la gestion des déploiements.',
  },
  {
    name: 'server',
    type: 'ServerConfig',
    description:
      'Configuration du serveur comprenant le port, l’hôte, le délai d’expiration, les routes d’API, les middlewares, les paramètres CORS ainsi que les options de build pour Swagger UI, la journalisation des requêtes d’API et la documentation OpenAPI.',
    isOptional: true,
  },
  {
    name: 'mcpServers',
    type: 'Record<string, MCPServerBase>',
    isOptional: true,
    description:
      'Objet dont les clés sont les clés du registre (utilisées par getMCPServer()) et les valeurs des instances de MCPServer ou des classes qui étendent MCPServerBase. Chaque MCPServer doit posséder une propriété id. Les serveurs peuvent être récupérés par leur clé de registre à l’aide de getMCPServer(), ou par leur id intrinsèque à l’aide de getMCPServerById().',
  },
  {
    name: 'bundler',
    type: 'BundlerConfig',
    description:
      'Configuration du bundler de ressources avec des options pour externals, sourcemap, transpilePackages et dynamicPackages.',
    isOptional: true,
    defaultValue: '{ externals: [], sourcemap: false, transpilePackages: [], dynamicPackages: [] }',
  },
  {
    name: 'scorers',
    type: 'Record<string, Scorer>',
    description: 'Scorers servant à évaluer les réponses des Agents et les sorties des Workflows',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'processors',
    type: 'Record<string, Processor>',
    description: 'Processeurs d’entrée/sortie permettant de transformer les entrées et les sorties des Agents',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'gateways',
    type: 'Record<string, MastraModelGateway>',
    description:
      'Gateways de modèles personnalisées à enregistrer pour accéder aux modèles d’IA par l’intermédiaire de Providers alternatifs ou de déploiements privés. Structurées sous forme de paires clé-valeur, où les clés sont les clés du registre (utilisées par getGateway()) et les valeurs les instances de gateway.',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'memory',
    type: 'Record<string, MastraMemory>',
    description:
      'Instances de Memory à enregistrer. Elles peuvent être référencées par les Agents stockés et résolues au moment de l’exécution. Structurées sous forme de paires clé-valeur, où les clés sont les clés du registre et les valeurs les instances de Memory.',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'notifications',
    type: 'object',
    description: 'Configuration d’exécution pour la distribution des signaux de notification.',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'dispatch',
            type: 'NotificationDispatchConfig',
            isOptional: true,
            description:
              'Configuration de la distribution planifiée des notifications différées et des résumés de notifications. La distribution est activée par défaut.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'enabled',
                    type: 'boolean',
                    isOptional: true,
                    defaultValue: 'true',
                    description: 'Définissez sur `false` pour désactiver la distribution planifiée automatique des notifications.',
                  },
                  {
                    name: 'cron',
                    type: 'string',
                    isOptional: true,
                    defaultValue: '`*/1 * * * *`',
                    description: 'Planification cron utilisée par le Workflow interne de distribution des notifications.',
                  },
                  {
                    name: 'batchSize',
                    type: 'number',
                    isOptional: true,
                    defaultValue: '100',
                    description: 'Nombre maximal d’enregistrements de notifications arrivés à échéance à traiter par exécution de la distribution.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'versions',
    type: 'VersionOverrides',
    description:
      'Remplacements globaux de version pour la délégation aux sous-Agents. Lorsqu’un Agent superviseur délègue à un sous-Agent, ces remplacements déterminent la version stockée de ce sous-Agent à utiliser à la place de celle définie par défaut dans le code. Nécessite la configuration du package de l’éditeur. Pour en savoir plus, consultez le [versionnement dans l’éditeur](/docs/editor/overview#versioning).',
    isOptional: true,
    properties: [
      {
        type: 'VersionOverrides',
        parameters: [
          {
            name: 'agents',
            type: 'Record<string, VersionSelector>',
            isOptional: true,
            description:
              'Table de correspondance entre les ID d’Agents et leurs sélecteurs de version. Chaque sélecteur peut cibler une version donnée par son ID ou son statut de publication.',
            properties: [
              {
                type: 'VersionSelector',
                parameters: [
                  {
                    name: 'versionId',
                    type: 'string',
                    isOptional: true,
                    description: 'ID d’une version précise à utiliser.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'status',
                    type: "'draft' | 'published'",
                    isOptional: true,
                    description: 'Sélectionne la dernière version ayant ce statut de publication.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'workers',
    type: 'MastraWorker[] | false',
    description:
      'Configure les [workers](/docs/deployment/workers) qui s’exécutent dans cette instance Mastra. Lorsque cette option est omise, Mastra crée automatiquement des workers par défaut en fonction de votre PubSub et de votre configuration. Transmettez `false` pour désactiver tout traitement des événements (utile lorsque des workers autonomes sont exécutés séparément). Transmettez un `MastraWorker[]` pour ajouter des workers personnalisés : ils sont fusionnés avec les valeurs par défaut créées automatiquement, et un worker personnalisé ayant le même `name` qu’une valeur par défaut la remplace.',
    isOptional: true,
  },
  {
    name: 'backgroundTasks',
    type: 'BackgroundTaskManagerConfig',
    description:
      'Configure l’exécution des [tâches en arrière-plan](/docs/long-running-agents/background-tasks) pour les Agents. Consultez la [référence de configuration des tâches en arrière-plan](/reference/configuration#backgroundtasks) pour découvrir toutes les options.',
    isOptional: true,
    properties: [
      {
        type: 'BackgroundTaskManagerConfig',
        parameters: [
          {
            name: 'enabled',
            type: 'boolean',
            isOptional: true,
            defaultValue: 'false',
            description: 'Active la distribution des tâches en arrière-plan.',
          },
          {
            name: 'globalConcurrency',
            type: 'number',
            isOptional: true,
            defaultValue: '10',
            description: 'Nombre maximal de tâches simultanées pour l’ensemble des Agents.',
          },
          {
            name: 'perAgentConcurrency',
            type: 'number',
            isOptional: true,
            defaultValue: '5',
            description: 'Nombre maximal de tâches simultanées par Agent.',
          },
          {
            name: 'backpressure',
            type: "'queue' | 'reject' | 'fallback-sync'",
            isOptional: true,
            defaultValue: "'queue'",
            description: 'Comportement lorsque la limite d’accès concurrent est atteinte.',
          },
          {
            name: 'defaultTimeoutMs',
            type: 'number',
            isOptional: true,
            defaultValue: '300000',
            description: 'Délai d’expiration par défaut des tâches, en millisecondes.',
          },
          {
            name: 'defaultRetries',
            type: 'RetryConfig',
            isOptional: true,
            description: 'Configuration par défaut des nouvelles tentatives.',
          },
        ],
      },
    ],
  },
  {
    name: 'scheduler',
    type: 'object',
    description:
      'Configure le [worker de planification](/docs/deployment/workers#scheduler-worker) pour les déclencheurs de Workflow pilotés par cron. S’active automatiquement lorsqu’un Workflow déclare un `schedule`. Consultez les [Workflows planifiés](/docs/workflows/scheduled-workflows).',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'enabled',
            type: 'boolean',
            isOptional: true,
            description: 'Active ou désactive explicitement le planificateur.',
          },
        ],
      },
    ],
  },
  {
    name: 'recovery',
    type: 'MastraRecoveryConfig',
    description:
      'Comportement de récupération au démarrage pour les exécutions orphelines d’Agents et de Workflows. Consultez la [récupération après incident](/docs/long-running-agents/durable-agents#crash-recovery).',
    isOptional: true,
    defaultValue: "{ durableAgents: 'off' }",
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'durableAgents',
            type: "'auto' | 'off'",
            isOptional: true,
            defaultValue: "'off'",
            description:
              "Définissez sur `'auto'` pour relancer automatiquement au démarrage du serveur les exécutions RUNNING orphelines d’Agents durables. La récupération relance les appels au LLM et réexécute les appels de Tools ; les Tools doivent donc être idempotents. Consultez la [récupération après incident](/docs/long-running-agents/durable-agents#crash-recovery).",
          },
        ],
      },
    ],
  },
]}
/>

## Méthodes {/* #methods */}

### `recoverAllDurableAgents()` {/* #recoveralldurableagents */}

Relance chaque exécution `running` orpheline d’Agent durable pour l’ensemble des Agents durables enregistrés. Appelée automatiquement au démarrage lorsque `recovery.durableAgents` vaut `'auto'`. Vous pouvez également l’appeler directement pour effectuer une récupération manuelle ou depuis une tâche planifiée.

Nécessite un stockage persistant. Avec un stockage en mémoire, il n’y a rien à récupérer après le redémarrage d’un processus.

```typescript
const result = await mastra.recoverAllDurableAgents()
// { agents: 2, recovered: 3, succeeded: 3, failed: 0 }
```

Renvoie :

<PropertiesTable
  content={[
  {
    name: 'agents',
    type: 'number',
    description: 'Nombre d’Agents durables analysés.',
  },
  {
    name: 'recovered',
    type: 'number',
    description: 'Nombre total d’exécutions relancées.',
  },
  {
    name: 'succeeded',
    type: 'number',
    description: 'Exécutions redémarrées avec succès.',
  },
  {
    name: 'failed',
    type: 'number',
    description: 'Exécutions dont le redémarrage a déclenché une erreur.',
  },
]}
/>
