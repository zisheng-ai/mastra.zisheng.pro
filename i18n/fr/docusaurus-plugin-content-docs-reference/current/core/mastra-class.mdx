---
title: "Référence : Mastra class | Core"
description: "Documentation pour the `Mastra` class dans Mastra, the core entry point pour managing agents, workflows, MCP servers, et server endpoints."
packages:
  - "@mastra/core"
  - "@mastra/libsql"
  - "@mastra/loggers"
---

# Mastra class

The `Mastra` class est the central orchestrator dans tout Mastra application, managing agents, workflows, storage, logging, observability, et more. Typically, you crée a unique instance de `Mastra` à coordinate your application.

Think de `Mastra` comme a top-level registry where you register agents, workflows, tools, et autre components that need à be accessible throughout your application.

## Exemple d’utilisation

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { PinoLogger } from '@mastra/loggers'
import { LibSQLStore } from '@mastra/libsql'
import { weatherWorkflow } from './workflows/weather-workflow'
import { weatherAgent } from './agents/weather-agent'

export const mastra = new Mastra({
  workflows: { weatherWorkflow },
  agents: { weatherAgent },
  storage: new LibSQLStore({
    id: 'mastra-storage',
    url: ':memory:',
  }),
  logger: new PinoLogger({
    name: 'Mastra',
    level: 'info',
  }),
})
```

Actifr scheduled notification dispatch when deferred notification records et notification summaries doit be delivered automatically via the Workflow scheduler:

```typescript title="src/mastra/index.ts"
export const mastra = new Mastra({
  agents: { supportAgent },
  storage,
  notifications: {
    dispatch: {
      enabled: true,
      cron: '*/1 * * * *',
      batchSize: 100,
    },
  },
})
```

`notifications.dispatch.enabled` allows an internal dispatcher Workflow à exécute avec the par défaut cron `*/1 * * * *`. The dispatcher reads due notification records depuis storage, groups summaries par `agentId`, `resourceId`, et `threadId`, et emits signals via the agent thread runtime. It isn't a user-facing entrypoint. The dispatch schedule (and the Workflow scheduler backing it) activates lazily sur the premier deferred ou summarized notification, so apps that never defer notifications don't exécute a scheduler à all.

## Paramètres du constructeur

Visit the [Configuration reference](/reference/configuration) pour detailed documentation sur tous les disponible configuration options.

<PropertiesTable
  content={[
  {
    name: 'agents',
    type: 'Record<string, Agent>',
    description: 'Agent instances à register, keyed par name',
    isFacultatif: true,
    defaultValeur: '{}',
  },
  {
    name: 'tools',
    type: 'Record<string, ToolApi>',
    description:
      'Tool instances à register. Keys sont registration keys used par [
  {
    name: 'agents',
    type: 'Record<string, Agent>',
    description: 'Agent instances à register, keyed par name',
    isFacultatif: true,
    defaultValeur: '{}',
  },
  {
    name: 'tools',
    type: 'Record<string, ToolApi>',
    description:
      'Tool instances à register. Keys sont registration keys used par [`getTool()`](/reference/core/getTool), et values sont Tool instances. Use [`getToolById()`](/reference/core/getToolById) pour intrinsic ID lookup et [`listTools()`](/reference/core/listTools) à read the registry.',
    isFacultatif: true,
    defaultValeur: '{}',
  },
  {
    name: 'storage',
    type: 'MastraCompositeStore',
    description: 'Stockage engine instance pour persisting data',
    isFacultatif: true,
  },
  {
    name: 'vectors',
    type: 'Record<string, MastraVector>',
    description:
      'Vector store instance, used pour semantic search et vector-based tools (eg Pinecone, PgVector ou Qdrant)',
    isFacultatif: true,
  },
  {
    name: 'logger',
    type: 'Logger',
    description: 'Logger instance created avec nouveau PinoLogger()',
    isFacultatif: true,
    defaultValeur: 'Console logger avec INFO level',
  },
  {
    name: 'idGenerator',
    type: '(context?: IdGeneratorContexte) => string',
    description:
      'Personnalisé ID generator function. Used par agents, workflows, memory, et autre components à generate unique identifiers. Receives facultatif context such comme idType, source, entityId, et threadId à support context-aware ID formats.',
    isFacultatif: true,
  },
  {
    name: 'workflows',
    type: 'Record<string, Workflow>',
    description:
      'Workflows à register. Structured comme a key-value pair, avec keys being the Workflow nom et values being the Workflow instance.',
    isFacultatif: true,
    defaultValeur: '{}',
  },
  {
    name: 'tts',
    type: 'Record<string, MastraVoice>',
    isFacultatif: true,
    description: 'Text-to-speech providers pour voice synthesis',
  },
  {
    name: 'observability',
    type: 'ObservabilityEntrypoint',
    isFacultatif: true,
    description: 'Observability configuration pour tracing et monitoring',
  },
  {
    name: 'environment',
    type: 'string',
    isFacultatif: true,
    description:
      'Déploiement environment nom (e.g. `production`, `staging`, `development`). When set, automatically attached à tous les observability signals so they peut be filtered par environment sans passing `tracingOptions.metadata.environment` sur chaque call. Falls back à `process.env.NODE_ENV` when unset; left undefined if neither est set. Per-call `tracingOptions.metadata.environment` always takes precedence.',
  },
  {
    name: 'deployer',
    type: 'MastraDéployeur',
    isFacultatif: true,
    description: 'Un instance de a MastraDéployeur pour managing deployments.',
  },
  {
    name: 'server',
    type: 'ServeurConfig',
    description:
      'Serveur configuration including port, host, timeout, API routes, middleware, CORS settings, et génère options pour Swagger UI, API requête logging, et OpenAPI docs.',
    isFacultatif: true,
  },
  {
    name: 'mcpServeurs',
    type: 'Record<string, MCPServeurBase>',
    isFacultatif: true,
    description:
      'Un objet where keys sont registry keys (used pour getMCPServeur()) et values sont instances de MCPServeur ou classes extending MCPServeurBase. Chaque MCPServeur doit ont an id property. Serveurs peut be retrieved par registry clé using getMCPServeur() ou par their intrinsic id using getMCPServeurById().',
  },
  {
    name: 'bundler',
    type: 'BundlerConfig',
    description:
      'Configuration pour the asset bundler avec options pour externals, sourcemap, transpilePackages, et dynamicPackages.',
    isFacultatif: true,
    defaultValeur: '{ externals: [], sourcemap: false, transpilePackages: [], dynamicPackages: [] }',
  },
  {
    name: 'scorers',
    type: 'Record<string, Scorer>',
    description: 'Scorers pour evaluating agent responses et Workflow outputs',
    isFacultatif: true,
    defaultValeur: '{}',
  },
  {
    name: 'processors',
    type: 'Record<string, Processor>',
    description: 'Entrée/output processors pour transforming agent inputs et outputs',
    isFacultatif: true,
    defaultValeur: '{}',
  },
  {
    name: 'gateways',
    type: 'Record<string, MastraModèleGateway>',
    description:
      'Personnalisé modèle gateways à register pour accessing AI models via alternative providers ou private deployments. Structured comme a key-value pair, avec keys being the registry clé (used pour getGateway()) et values being gateway instances.',
    isFacultatif: true,
    defaultValeur: '{}',
  },
  {
    name: 'memory',
    type: 'Record<string, MastraMémoire>',
    description:
      'Mémoire instances à register. Ces peut be referenced par stored agents et resolved à runtime. Structured comme a key-value pair, avec keys being the registry clé et values being memory instances.',
    isFacultatif: true,
    defaultValeur: '{}',
  },
  {
    name: 'notifications',
    type: 'object',
    description: 'Runtime configuration pour notification signal dispatch.',
    isFacultatif: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'dispatch',
            type: 'NotificationDispatchConfig',
            isFacultatif: true,
            description:
              'Scheduled dispatch configuration pour deferred notifications et notification summaries. Dispatch est activé par default.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'enabled',
                    type: 'boolean',
                    isFacultatif: true,
                    defaultValeur: 'true',
                    description: 'Définir à `false` à opt out de automatic scheduled notification dispatch.',
                  },
                  {
                    name: 'cron',
                    type: 'string',
                    isFacultatif: true,
                    defaultValeur: '`*/1 * * * *`',
                    description: 'Cron schedule used par the internal notification dispatcher workflow.',
                  },
                  {
                    name: 'batchSize',
                    type: 'number',
                    isFacultatif: true,
                    defaultValeur: '100',
                    description: 'Maximum nombre de due notification records à traite per dispatch run.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'versions',
    type: 'VersionOverrides',
    description:
      'Global version overrides pour sub-agent delegation. Lorsqu’un supervisor agent delegates à a sub-agent, these overrides determine which stored version de that sub-agent à utilise instead de the code-defined default. Requires the editor package à be configured. Consultez [Éditeur versioning](/docs/editor/overview#versioning) pour details.',
    isFacultatif: true,
    properties: [
      {
        type: 'VersionOverrides',
        parameters: [
          {
            name: 'agents',
            type: 'Record<string, VersionSelector>',
            isFacultatif: true,
            description:
              'Un mappe de agent IDs à their version selectors. Chaque selector peut target a specific version par ID ou par publication status.',
            properties: [
              {
                type: 'VersionSelector',
                parameters: [
                  {
                    name: 'versionId',
                    type: 'string',
                    isFacultatif: true,
                    description: 'The ID de a specific version à use.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'status',
                    type: "'draft' | 'published'",
                    isFacultatif: true,
                    description: 'Select the latest version avec this publication status.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'workers',
    type: 'MastraWorker[] | false',
    description:
      'Configurer which [workers](/docs/deployment/workers) exécute dans this Instance Mastra. When omitted, Mastra auto-creates par défaut workers based sur your PubSub et config. Pass `false` à désactive tous les event processing (useful when running standalone workers separately). Pass a `MastraWorker[]` à ajoute personnalisé workers — they sont merged avec the auto-created defaults, et a personnalisé worker avec the same `name` comme a par défaut replaces it.',
    isFacultatif: true,
  },
  {
    name: 'backgroundTasks',
    type: 'BackgroundTaskManagerConfig',
    description:
      'Configurer [background task](/docs/long-running-agents/background-tasks) execution pour agents. Consultez [background tasks configuration reference](/reference/configuration#backgroundtasks) pour tous les options.',
    isFacultatif: true,
    properties: [
      {
        type: 'BackgroundTaskManagerConfig',
        parameters: [
          {
            name: 'enabled',
            type: 'boolean',
            isFacultatif: true,
            defaultValeur: 'false',
            description: 'Actifr background task dispatch.',
          },
          {
            name: 'globalConcurrency',
            type: 'number',
            isFacultatif: true,
            defaultValeur: '10',
            description: 'Max concurrent tasks across tous les agents.',
          },
          {
            name: 'perAgentConcurrency',
            type: 'number',
            isFacultatif: true,
            defaultValeur: '5',
            description: 'Max concurrent tasks per agent.',
          },
          {
            name: 'backpressure',
            type: "'queue' | 'reject' | 'fallback-sync'",
            isFacultatif: true,
            defaultValeur: "'queue'",
            description: 'Behavior when concurrency limit est reached.',
          },
          {
            name: 'defaultTimeoutMs',
            type: 'number',
            isFacultatif: true,
            defaultValeur: '300000',
            description: 'Par défaut task timeout dans milliseconds.',
          },
          {
            name: 'defaultRetries',
            type: 'RetryConfig',
            isFacultatif: true,
            description: 'Par défaut retry configuration.',
          },
        ],
      },
    ],
  },
  {
    name: 'scheduler',
    type: 'object',
    description:
      'Configurer the [scheduler worker](/docs/deployment/workers#scheduler-worker) pour cron-driven Workflow triggers. Auto-enables when tout Workflow declares a `schedule`. Consultez [Scheduled workflows](/docs/workflows/scheduled-workflows).',
    isFacultatif: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'enabled',
            type: 'boolean',
            isFacultatif: true,
            description: 'Explicitly active ou désactive the scheduler.',
          },
        ],
      },
    ],
  },
  {
    name: 'recovery',
    type: 'MastraRecoveryConfig',
    description:
      'Boot-time recovery behavior pour orphaned agent et Workflow runs. Consultez [Crash recovery](/docs/long-running-agents/durable-agents#crash-recovery).',
    isFacultatif: true,
    defaultValeur: "{ durableAgents: 'off' }",
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'durableAgents',
            type: "'auto' | 'off'",
            isFacultatif: true,
            defaultValeur: "'off'",
            description:
              "Définir à `'auto'` à automatically re-drive orphaned RUNNING durable agent runs sur server boot. Recovery re-issues LLM calls et re-executes Tool calls, so tools doit be idempotent. Consultez [Crash recovery](/docs/long-running-agents/durable-agents#crash-recovery).",
          },
        ],
      },
    ],
  },
]}
/>

## Méthodes

### `recoverAllDurableAgents()`

Re-drives every orphaned `running` durable-agent exécute across tous les registered durable agents. Called automatically sur boot when `recovery.durableAgents` est `'auto'`. Vous pouvez également call it directly pour manual recovery ou depuis a scheduled task.

Requires persistent storage. Avec un in-memory store, there's nothing à recover après a traite restart.

```typescript
const result = await mastra.recoverAllDurableAgents()
// { agents: 2, recovered: 3, succeeded: 3, failed: 0 }
```

Renvoie:

<PropertiesTable
  content={[
  {
    name: 'agents',
    type: 'number',
    description: 'Number de durable agents scanned.',
  },
  {
    name: 'recovered',
    type: 'number',
    description: 'Total nombre de runs that étaient re-driven.',
  },
  {
    name: 'succeeded',
    type: 'number',
    description: 'Exécute that restarted successfully.',
  },
  {
    name: 'failed',
    type: 'number',
    description: 'Exécute whose restart threw an error.',
  },
]}
/>
