---
title: "Référence : getScorer() | Core"
description: "Documentation pour the `getScorer()` method dans Mastra, which retrieves a specific scorer par its registration key."
packages:
  - "@mastra/core"
---

# getScorer()

The `getScorer()` method retrieves a specific scorer that était registered avec the Instance Mastra using its registration key. La méthode provides type-safe access à scorers et throws an error if the requested scorer isn't found.

## Exemple d’utilisation

```typescript
import { mastra } from './mastra'

// Get a specific scorer by key
const relevancyScorer = mastra.getScorer('relevancyScorer')

const weatherAgent = mastra.getAgent('weatherAgent')

// Use the scorer to evaluate an AI output
await weatherAgent.generate('What is the weather in Rome', {
  scorers: {
    answerRelevancy: {
      scorer: relevancyScorer,
    },
  },
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'key',
    type: 'string',
    description:
      'The registration clé de the scorer à retrieve. Ce doit match a clé used when registering scorers dans the Mastra constructor.',
    isOptional: false,
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'scorer',
    type: 'MastraScorer',
    description: 'The MastraScorer instance associated avec the fourni key.',
  },
]}
/>

## Gestion des erreurs

Ce method throws a `MastraError` if:

- The scorer avec the specified clé isn't found
- No scorers sont registered avec the Instance Mastra

```typescript
try {
  const scorer = mastra.getScorer('nonExistentScorer')
} catch (error) {
  if (error.id === 'MASTRA_GET_SCORER_NOT_FOUND') {
    console.log('Scorer not found, using default evaluation')
  }
}
```

## Voir aussi

- [listScorers()](/reference/core/listScorers): Obtenir tous les registered scorers
- [getScorerById()](/reference/core/getScorerById): Obtenir a scorer par its id property
- [Personnalisé Scorers](/docs/evals/custom-scorers): Learn how à crée personnalisé scorers
