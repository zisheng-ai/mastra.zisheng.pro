---
title: "Référence : getScorer() | Core"
description: "Documentation de la méthode `getScorer()` de Mastra, qui récupère un Scorer spécifique par sa clé d’enregistrement."
packages:
  - "@mastra/core"
---

# getScorer()

La méthode `getScorer()` récupère un Scorer spécifique enregistré auprès de l’instance Mastra avec sa clé d’enregistrement. Elle fournit un accès aux Scorers sécurisé par les types et génère une erreur si le Scorer demandé est introuvable.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { mastra } from './mastra'

// Get a specific scorer by key
const relevancyScorer = mastra.getScorer('relevancyScorer')

const weatherAgent = mastra.getAgent('weatherAgent')

// Use the scorer to evaluate an AI output
await weatherAgent.generate('What is the weather in Rome', {
  scorers: {
    answerRelevancy: {
      scorer: relevancyScorer,
    },
  },
})
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'key',
    type: 'string',
    description:
      'Clé d’enregistrement du Scorer à récupérer. Elle doit correspondre à une clé utilisée lors de l’enregistrement de Scorers dans le constructeur Mastra.',
    isOptional: false,
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'scorer',
    type: 'MastraScorer',
    description: 'Instance MastraScorer associée à la clé fournie.',
  },
]}
/>

## Gestion des erreurs {/* #error-handling */}

Cette méthode génère une `MastraError` si :

- Le Scorer avec la clé spécifiée est introuvable
- Aucun Scorer n’est enregistré auprès de l’instance Mastra

```typescript
try {
  const scorer = mastra.getScorer('nonExistentScorer')
} catch (error) {
  if (error.id === 'MASTRA_GET_SCORER_NOT_FOUND') {
    console.log('Scorer not found, using default evaluation')
  }
}
```

## Voir aussi {/* #related */}

- [listScorers()](/reference/core/listScorers): Récupérer tous les Scorers enregistrés
- [getScorerById()](/reference/core/getScorerById): Récupérer un Scorer par sa propriété id
- [Scorers personnalisés](/docs/evals/custom-scorers): Apprendre à créer des Scorers personnalisés
