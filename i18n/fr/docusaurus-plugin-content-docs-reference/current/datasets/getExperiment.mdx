---
title: "Reference: dataset.getExperiment() | Datasets"
description: "Documentation for the `dataset.getExperiment()` method, which retrieves a specific experiment by ID."
packages:
  - "@mastra/core"
---

# dataset.getExperiment()

**Added in:** `@mastra/core@1.4.0`

Retrieves a specific experiment (run) by its ID.

## Usage example

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const experiment = await dataset.getExperiment({ experimentId: 'exp-id' })

console.log(`Status: ${experiment.status}`)
console.log(`${experiment.succeededCount}/${experiment.totalItems} succeeded`)
```

## Parameters

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: 'ID of the experiment to retrieve.',
  },
]}
/>

## Returns

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Experiment>',
    description: 'The experiment record. See dataset.listExperiments() for the full shape.',
  },
]}
/>
