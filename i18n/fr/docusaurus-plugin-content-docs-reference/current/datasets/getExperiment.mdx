---
title: "Référence : dataset.getExperiment() | Datasets"
description: "Documentation de la méthode `dataset.getExperiment()`, qui récupère une expérience spécifique à partir de son ID."
packages:
  - "@mastra/core"
---

# dataset.getExperiment()

**Ajouté dans :** `@mastra/core@1.4.0`

Récupère une expérience spécifique (exécution) à partir de son ID.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const experiment = await dataset.getExperiment({ experimentId: 'exp-id' })

console.log(`Status: ${experiment.status}`)
console.log(`${experiment.succeededCount}/${experiment.totalItems} succeeded`)
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: 'ID de l’expérience à récupérer.',
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Experiment>',
    description: 'Enregistrement de l’expérience. Consultez dataset.listExperiments() pour connaître sa structure complète.',
  },
]}
/>
