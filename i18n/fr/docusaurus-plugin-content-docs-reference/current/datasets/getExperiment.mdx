---
title: "Référence : dataset.getExperiment() | Jeux de données"
description: "Documentation pour the `dataset.getExperiment()` method, which retrieves a specific experiment par ID."
packages:
  - "@mastra/core"
---

# dataset.getExperiment()

**Added in:** `@mastra/core@1.4.0`

Retrieves a specific experiment (run) par its ID.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const experiment = await dataset.getExperiment({ experimentId: 'exp-id' })

console.log(`Status: ${experiment.status}`)
console.log(`${experiment.succeededCount}/${experiment.totalItems} succeeded`)
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: 'ID de the experiment à retrieve.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Experiment>',
    description: 'The experiment record. Consultez dataset.listExperiments() pour the full shape.',
  },
]}
/>
