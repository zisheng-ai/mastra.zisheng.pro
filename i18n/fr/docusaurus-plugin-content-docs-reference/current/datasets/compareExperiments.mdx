---
title: "Référence : DatasetsManager.compareExperiments() | Datasets"
description: "Documentation de la méthode `DatasetsManager.compareExperiments()`, qui compare au moins deux expériences."
packages:
  - "@mastra/core"
---

# DatasetsManager.compareExperiments()

**Ajouté dans :** `@mastra/core@1.4.0`

Compare au moins deux expériences et produit des comparaisons par élément et par Scorer. Nécessite au moins deux ID d’expérience.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const comparison = await mastra.datasets.compareExperiments({
  experimentIds: ['exp-baseline', 'exp-new'],
  baselineId: 'exp-baseline',
})

console.log(`Baseline: ${comparison.baselineId}`)

for (const item of comparison.items) {
  console.log(`Item ${item.itemId}:`)
  console.log(`  Input: ${JSON.stringify(item.input)}`)

  for (const [expId, result] of Object.entries(item.results)) {
    if (result) {
      console.log(
        `  ${expId}: output=${JSON.stringify(result.output)}, scores=${JSON.stringify(result.scores)}`,
      )
    }
  }
}
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'experimentIds',
    type: 'string[]',
    description: 'Tableau des ID d’expérience à comparer. Doit en contenir au moins 2.',
  },
  {
    name: 'baselineId',
    type: 'string',
    isOptional: true,
    description: 'ID de l’expérience de référence. Correspond par défaut au premier ID de `experimentIds`.',
  },
]}
/>

## Valeur renvoyée {/* #returns */}

Lève une `MastraError` si moins de 2 ID d’expérience sont fournis.

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Résultats de la comparaison.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'baselineId',
            type: 'string',
            description: 'ID de l’expérience de référence utilisée pour la comparaison.',
          },
          {
            name: 'items',
            type: 'Array<object>',
            description: 'Données de comparaison par élément.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'itemId',
                    type: 'string',
                    description: 'ID de l’élément du dataset.',
                  },
                  {
                    name: 'input',
                    type: 'unknown | null',
                    description: 'Données d’entrée de l’élément.',
                  },
                  {
                    name: 'groundTruth',
                    type: 'unknown | null',
                    description: 'Vérité terrain de l’élément.',
                  },
                  {
                    name: 'results',
                    type: 'Record<string, { output: unknown; scores: Record<string, number | null> } | null>',
                    description:
                      'Résultats indexés par ID d’expérience. Chaque entrée contient la sortie et les résultats des Scorers pour cette expérience.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## Voir aussi {/* #related */}

- [dataset.startExperiment()](/reference/datasets/startExperiment)
- [dataset.listExperiments()](/reference/datasets/listExperiments)
