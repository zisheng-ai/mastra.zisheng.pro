---
title: "Référence : Jeux de donnéesManager.compareExperiments() | Jeux de données"
description: "Documentation pour the `DatasetsManager.compareExperiments()` method, which compares deux ou more experiments."
packages:
  - "@mastra/core"
---

# Jeux de donnéesManager.compareExperiments()

**Added in:** `@mastra/core@1.4.0`

Compares deux ou more experiments, producing per-item et per-scorer comparisons. Requires à least deux experiment IDs.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const comparison = await mastra.datasets.compareExperiments({
  experimentIds: ['exp-baseline', 'exp-new'],
  baselineId: 'exp-baseline',
})

console.log(`Baseline: ${comparison.baselineId}`)

for (const item of comparison.items) {
  console.log(`Item ${item.itemId}:`)
  console.log(`  Input: ${JSON.stringify(item.input)}`)

  for (const [expId, result] of Object.entries(item.results)) {
    if (result) {
      console.log(
        `  ${expId}: output=${JSON.stringify(result.output)}, scores=${JSON.stringify(result.scores)}`,
      )
    }
  }
}
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'experimentIds',
    type: 'string[]',
    description: 'Array de experiment IDs à compare. Must contain à least 2.',
  },
  {
    name: 'baselineId',
    type: 'string',
    isOptional: true,
    description: 'ID de the baseline experiment. Par défauts à the premier ID dans `experimentIds`.',
  },
]}
/>

## Renvoie

Throws `MastraError` if fewer than 2 experiment IDs sont provided.

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Comparison results.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'baselineId',
            type: 'string',
            description: 'ID de the baseline experiment used pour comparison.',
          },
          {
            name: 'items',
            type: 'Array<object>',
            description: 'Per-item comparison data.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'itemId',
                    type: 'string',
                    description: 'ID de the dataset item.',
                  },
                  {
                    name: 'input',
                    type: 'unknown | null',
                    description: 'Entrée données pour the item.',
                  },
                  {
                    name: 'groundTruth',
                    type: 'unknown | null',
                    description: 'Ground truth pour the item.',
                  },
                  {
                    name: 'results',
                    type: 'Record<string, { output: unknown; scores: Record<string, nombre | null> } | null>',
                    description:
                      'Résultats keyed par experiment ID. Chaque entry contains the output et scorer results pour that experiment.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## Voir aussi

- [dataset.startExperiment()](/reference/datasets/startExperiment)
- [dataset.listExperiments()](/reference/datasets/listExperiments)
