---
title: "Référence : dataset.listVersions() | Datasets"
description: "Documentation de la méthode `dataset.listVersions()`, qui répertorie toutes les versions d'un dataset."
packages:
  - "@mastra/core"
---

# dataset.listVersions()

**Ajouté dans :** `@mastra/core@1.4.0`

Répertorie toutes les versions du dataset avec pagination.

## Exemple d'utilisation {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const { versions, pagination } = await dataset.listVersions({ page: 0, perPage: 10 })

for (const version of versions) {
  console.log(`Version ${version.version} created at ${version.createdAt}`)
}
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'Numéro de page. La valeur par défaut est `0`.',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: 'Nombre de versions par page. La valeur par défaut est `20`.',
  },
]}
/>

## Valeur de retour {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Liste paginée des versions.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'versions',
            type: 'DatasetVersion[]',
            description: 'Tableau des enregistrements de version.',
            properties: [
              {
                type: 'DatasetVersion',
                parameters: [
                  {
                    name: 'id',
                    type: 'string',
                    description: "Identifiant unique de l'enregistrement de version.",
                  },
                  {
                    name: 'datasetId',
                    type: 'string',
                    description: 'ID du dataset parent.',
                  },
                  {
                    name: 'version',
                    type: 'number',
                    description: 'Numéro de version.',
                  },
                  {
                    name: 'createdAt',
                    type: 'Date',
                    description: 'Date de création de cette version.',
                  },
                ],
              },
            ],
          },
          {
            name: 'pagination',
            type: 'object',
            description: 'Métadonnées de pagination.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'total',
                    type: 'number',
                    description: 'Nombre total de versions.',
                  },
                  {
                    name: 'page',
                    type: 'number',
                    description: 'Numéro de la page actuelle.',
                  },
                  {
                    name: 'perPage',
                    type: 'number | false',
                    description: 'Nombre de versions par page, ou `false` en l’absence de pagination.',
                  },
                  {
                    name: 'hasMore',
                    type: 'boolean',
                    description: "Indique si d'autres pages sont disponibles.",
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>
