---
title: "Référence : dataset.deleteExperiment() | Datasets"
description: "Documentation de la méthode `dataset.deleteExperiment()`, qui supprime une expérience par son ID."
packages:
  - "@mastra/core"
---

# dataset.deleteExperiment()

**Ajouté dans :** `@mastra/core@1.4.0`

Supprime une expérience (exécution) par son ID, y compris tous les résultats associés.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteExperiment({ experimentId: 'exp-id' })
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: 'ID de l’expérience à supprimer.',
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: 'Se résout lorsque l’expérience et ses résultats sont supprimés.',
  },
]}
/>
