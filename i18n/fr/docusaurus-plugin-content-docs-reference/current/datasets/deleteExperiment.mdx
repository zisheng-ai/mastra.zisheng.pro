---
title: "Référence : dataset.deleteExperiment() | Jeux de données"
description: "Documentation pour the `dataset.deleteExperiment()` method, which deletes an experiment par ID."
packages:
  - "@mastra/core"
---

# dataset.deleteExperiment()

**Added in:** `@mastra/core@1.4.0`

Supprime an experiment (run) par ID, including tous les associated results.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteExperiment({ experimentId: 'exp-id' })
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: 'ID de the experiment à delete.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: 'Resolves when the experiment et its results sont deleted.',
  },
]}
/>
