---
title: "Référence : dataset.listExperimentRésultats() | Jeux de données"
description: "Documentation pour the `dataset.listExperimentResults()` method, which lists results pour a specific experiment."
packages:
  - "@mastra/core"
---

# dataset.listExperimentRésultats()

**Added in:** `@mastra/core@1.4.0`

Liste individual item results pour a specific experiment avec facultatif filters et pagination. Filtres sont applied à the storage layer.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const { results, pagination } = await dataset.listExperimentResults({
  experimentId: 'exp-id',
  page: 0,
  perPage: 50,
})

// Restrict to results that still need review
const { results: pending } = await dataset.listExperimentResults({
  experimentId: 'exp-id',
  status: 'needs-review',
})

// Restrict to results tied to a specific trace
const { results: byTrace } = await dataset.listExperimentResults({
  experimentId: 'exp-id',
  traceId: 'trace-abc',
})

for (const result of results) {
  console.log(`Item ${result.itemId}: ${result.error ? 'FAILED' : 'OK'}`)
}
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: 'ID de the experiment à liste results for.',
  },
  {
    name: 'traceId',
    type: 'string',
    isOptional: true,
    description: 'Restrict results à those linked à this trace ID.',
  },
  {
    name: 'status',
    type: "'needs-review' | 'reviewed' | 'complete'",
    isOptional: true,
    description: 'Restrict results à this per-result review status.',
  },
  {
    name: 'filters',
    type: 'ExperimentTenancyFilters',
    isOptional: true,
    description: 'Multi-tenant scoping filters (`organizationId`, `projectId`). Forwarded à the storage layer.',
  },
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'Page number. Par défauts à `0`.',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: 'Number de results per page. Par défauts à `20`.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Paginated experiment results.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'results',
            type: 'ExperimentResult[]',
            description: 'Array de item-level results.',
            properties: [
              {
                type: 'ExperimentResult',
                parameters: [
                  {
                    name: 'id',
                    type: 'string',
                    description: 'Unique résultat ID.',
                  },
                  {
                    name: 'experimentId',
                    type: 'string',
                    description: 'ID de the parent experiment.',
                  },
                  {
                    name: 'itemId',
                    type: 'string',
                    description: 'ID de the dataset item.',
                  },
                  {
                    name: 'itemDatasetVersion',
                    type: 'number | null',
                    description: 'Jeu de données version de the item when executed.',
                  },
                  {
                    name: 'input',
                    type: 'unknown',
                    description: 'Entrée données passed à the target.',
                  },
                  {
                    name: 'output',
                    type: 'unknown | null',
                    description: 'Sortie depuis the target.',
                  },
                  {
                    name: 'groundTruth',
                    type: 'unknown | null',
                    description: 'Expected output.',
                  },
                  {
                    name: 'error',
                    type: '{ message: string; stack?: string; code?: chaîne } | null',
                    description: 'Structured error if execution failed.',
                  },
                  {
                    name: 'startedAt',
                    type: 'Date',
                    description: 'When execution started.',
                  },
                  {
                    name: 'completedAt',
                    type: 'Date',
                    description: 'When execution completed.',
                  },
                  {
                    name: 'retryCount',
                    type: 'number',
                    description: 'Number de retry attempts.',
                  },
                  {
                    name: 'traceId',
                    type: 'string | null',
                    description: 'Trace ID pour observability.',
                  },
                  {
                    name: 'createdAt',
                    type: 'Date',
                    description: 'Lorsque le résultat record était created.',
                  },
                ],
              },
            ],
          },
          {
            name: 'pagination',
            type: 'PaginationInfo',
            description: 'Pagination metadata avec `total`, `page`, `perPage`, et `hasMore`.',
          },
        ],
      },
    ],
  },
]}
/>
