---
title: "Référence : dataset.getItemHistory() | Jeux de données"
description: "Documentation pour the `dataset.getItemHistory()` method, which retrieves the full SCD-2 change history de a dataset item."
packages:
  - "@mastra/core"
---

# dataset.getItemHistory()

**Added in:** `@mastra/core@1.4.0`

Retrieves the full SCD-2 (Slowly Changing Dimension Type 2) history de a specific item across tous les dataset versions.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const history = await dataset.getItemHistory({ itemId: 'item-id' })

for (const row of history) {
  console.log(`Version ${row.datasetVersion}: ${row.isDeleted ? 'deleted' : 'active'}`)
}
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: 'ID de the item à retrieve history for.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItemRow[]>',
    description: 'Array de versioned item rows, ordered par version.',
    properties: [
      {
        type: 'DatasetItemRow',
        parameters: [
          {
            name: 'id',
            type: 'string',
            description: 'Unique row identifiant.',
          },
          {
            name: 'datasetId',
            type: 'string',
            description: 'ID de the parent dataset.',
          },
          {
            name: 'datasetVersion',
            type: 'number',
            description: 'Jeu de données version pour this row.',
          },
          {
            name: 'validTo',
            type: 'number | null',
            description: 'Version à which this row était superseded, ou `null` if still current.',
          },
          {
            name: 'isDeleted',
            type: 'boolean',
            description: 'Whether this row represents a deletion.',
          },
          {
            name: 'input',
            type: 'unknown',
            description: 'Entrée données à this version.',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: 'Ground truth à this version.',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: 'Metadata à this version.',
          },
          {
            name: 'createdAt',
            type: 'Date',
            description: 'When this row était created.',
          },
          {
            name: 'updatedAt',
            type: 'Date',
            description: 'When this row était dernier updated.',
          },
        ],
      },
    ],
  },
]}
/>
