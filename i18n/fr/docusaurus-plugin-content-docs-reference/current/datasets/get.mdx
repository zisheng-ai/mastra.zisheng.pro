---
title: "Référence : Jeux de donnéesManager.get() | Jeux de données"
description: "Documentation pour the `DatasetsManager.get()` method, which retrieves an existant dataset par ID."
packages:
  - "@mastra/core"
---

# Jeux de donnéesManager.get()

**Added in:** `@mastra/core@1.4.0`

Retrieves an existant dataset par ID. Throws a `MastraError` if the dataset doesn't exist.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Now use the dataset
const details = await dataset.getDetails()
console.log(details.name)
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Unique identifiant de the dataset.',
  },
]}
/>

## Renvoie

Throws `MastraError` if the dataset isn't found.

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Dataset>',
    description: 'Un Jeu de données instance.',
  },
]}
/>
