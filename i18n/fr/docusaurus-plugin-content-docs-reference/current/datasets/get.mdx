---
title: "Référence : DatasetsManager.get() | Datasets"
description: "Documentation de la méthode `DatasetsManager.get()`, qui récupère un dataset existant à partir de son ID."
packages:
  - "@mastra/core"
---

# DatasetsManager.get()

**Ajouté dans :** `@mastra/core@1.4.0`

Récupère un dataset existant à partir de son ID. Génère une `MastraError` si le dataset n’existe pas.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Now use the dataset
const details = await dataset.getDetails()
console.log(details.name)
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique du dataset.',
  },
]}
/>

## Valeur renvoyée

Génère une `MastraError` si le dataset est introuvable.

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Dataset>',
    description: 'Instance de Dataset.',
  },
]}
/>
