---
title: "Référence : classe Dataset | Datasets"
description: "Documentation de la classe `Dataset` de Mastra, qui fournit l’API publique permettant d’interagir avec un dataset."
packages:
  - "@mastra/core"
---

# Classe Dataset

**Ajouté dans :** `@mastra/core@1.4.0`

La classe `Dataset` fournit des méthodes pour les opérations CRUD sur les éléments, la gestion des versions et la gestion des expériences d’un dataset. Elle s’obtient avec `mastra.datasets.get()` ou `mastra.datasets.create()`.

## Exemples d’utilisation {/* #usage-examples */}

### Ajouter des éléments et exécuter une expérience {/* #add-items-and-run-an-experiment */}

```typescript title="src/mastra/datasets/usage.ts"
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.create({
  name: 'QA pairs',
})

// Add items
await dataset.addItems({
  items: [
    { input: { question: 'What is AI?' }, groundTruth: { answer: 'Artificial Intelligence' } },
    { input: { question: 'What is ML?' }, groundTruth: { answer: 'Machine Learning' } },
  ],
})

// Run an experiment
const summary = await dataset.startExperiment({
  targetType: 'agent',
  targetId: 'my-agent',
  scorers: ['accuracy'],
})

console.log(`${summary.succeededCount}/${summary.totalItems} succeeded`)
```

### Gérer les versions et l’historique {/* #manage-versions-and-history */}

```typescript title="src/mastra/datasets/versioning.ts"
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// List versions
const { versions } = await dataset.listVersions()

// List items at a specific version
const items = await dataset.listItems({ version: 2 })

// Get item change history
const history = await dataset.getItemHistory({ itemId: 'item-id' })
```

## Accès {/* #access */}

`Dataset` n’est pas instanciée directement. Obtenez une instance avec `DatasetsManager` :

```typescript
const dataset = await mastra.datasets.get({ id: 'dataset-id' })
// or
const dataset = await mastra.datasets.create({ name: 'My dataset' })
```

## Propriétés {/* #properties */}

Pour obtenir l’enregistrement complet du dataset (nom, description, schémas, version et horodatages), appelez [`dataset.getDetails()`](/reference/datasets/getDetails).

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique du dataset. Lecture seule.',
  },
]}
/>

## Voir aussi {/* #related */}

- [Classe DatasetsManager](/reference/datasets/datasets-manager)
- [dataset.startExperiment()](/reference/datasets/startExperiment)
- [dataset.addItems()](/reference/datasets/addItems)
- [dataset.listVersions()](/reference/datasets/listVersions)
