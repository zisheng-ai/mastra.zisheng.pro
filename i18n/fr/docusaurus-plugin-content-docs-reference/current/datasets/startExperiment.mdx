---
title: "Référence : dataset.startExperiment() | Jeux de données"
description: "Documentation de la méthode `dataset.startExperiment()`, qui exécute une expérience sur un jeu de données et attend sa fin."
packages:
  - "@mastra/core"
---

# dataset.startExperiment()

**Ajouté dans :** `@mastra/core@1.4.0`

Exécute une expérience sur le jeu de données et attend sa fin. Exécute tous les éléments sur une cible (agent, workflow ou scorer), avec une évaluation facultative.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Run against a registered agent with a flat scorer list
const summary = await dataset.startExperiment({
  targetType: 'agent',
  targetId: 'my-agent',
  scorers: ['accuracy', 'relevancy'],
  maxConcurrency: 10,
})

// Or pass the same categorised shape accepted by runEvals
const summary2 = await dataset.startExperiment({
  targetType: 'agent',
  targetId: 'my-agent',
  scorers: {
    agent: [accuracyScorer],
    trajectory: [toolOrderScorer],
  },
})

// For workflow targets, score individual steps with their own scorers
const summary3 = await dataset.startExperiment({
  targetType: 'workflow',
  targetId: 'my-workflow',
  scorers: {
    workflow: [overallScorer],
    steps: {
      'fetch-data': [fetchScorer],
      transform: [transformScorer],
    },
    trajectory: [executionPathScorer],
  },
})

console.log(`${summary.succeededCount}/${summary.totalItems} succeeded`)
console.log(`Status: ${summary.status}`)
console.log(`${summary2.succeededCount}/${summary2.totalItems} succeeded`)
console.log(`Status: ${summary2.status}`)
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'targetType',
    type: "'agent' | 'workflow' | 'scorer'",
    isOptional: true,
    description: 'Type de cible enregistrée sur laquelle exécuter les éléments. À utiliser avec `targetId`.',
  },
  {
    name: 'targetId',
    type: 'string',
    isOptional: true,
    description: 'ID de la cible enregistrée. À utiliser avec `targetType`.',
  },
  {
    name: 'scorers',
    type: '(MastraScorer | string)[] | AgentScorerConfig | WorkflowScorerConfig',
    isOptional: true,
    description:
      'Scorers permettant d’évaluer chaque résultat. Accepte un tableau plat d’instances `MastraScorer` ou d’ID de scorers enregistrés, ou la même structure de configuration catégorisée qu’utilise `runEvals` (`AgentScorerConfig` / `WorkflowScorerConfig`). Les scorers de trajectoire (`type: "trajectory"`) reçoivent automatiquement une `Trajectory` préextraite comme sortie, quelle que soit la forme utilisée. Pour les cibles de workflow, des scorers par étape peuvent être transmis via `scorers: { steps: { stepId: [...] } }` et exécutés sur la sortie de chaque étape ; leurs résultats contiennent le `stepId` d’origine et conservent `targetScope: "span"` (comme avec `runEvals`).',
  },
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: 'Nom d’affichage de l’expérience.',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: 'Description de l’expérience.',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: 'Métadonnées arbitraires de l’expérience.',
  },
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description: 'Épingle une version précise du jeu de données. La valeur par défaut est la version la plus récente.',
  },
  {
    name: 'maxConcurrency',
    type: 'number',
    isOptional: true,
    description: 'Nombre maximal d’exécutions simultanées d’éléments. La valeur par défaut est `5`.',
  },
  {
    name: 'signal',
    type: 'AbortSignal',
    isOptional: true,
    description: 'AbortSignal permettant d’annuler l’expérience.',
  },
  {
    name: 'itemTimeout',
    type: 'number',
    isOptional: true,
    description: 'Délai d’expiration de l’exécution par élément, en millisecondes.',
  },
  {
    name: 'maxRetries',
    type: 'number',
    isOptional: true,
    description: 'Nombre maximal de tentatives par élément en cas d’échec. La valeur par défaut est `0` (aucune nouvelle tentative). Les erreurs d’annulation ne font jamais l’objet d’une nouvelle tentative.',
  },
  {
    name: 'unmockedToolPolicy',
    type: "'allow' | 'deny'",
    isOptional: true,
    defaultValue: "'allow'",
    description:
      'Contrôle les appels d’outils d’agent non déclarés. `allow` les exécute réellement. `deny` fait échouer l’élément avec `TOOL_MOCK_NOT_DECLARED` avant l’exécution. Une valeur définie au niveau de l’élément remplace cette valeur par défaut de l’expérience.',
  },
  {
    name: 'persistence',
    type: 'ExperimentPersistencePolicy',
    isOptional: true,
    description:
      'Contrôle si cette exécution écrit des enregistrements d’expérience et de scores. Les cibles et les scorers sont tout de même exécutés et les résultats restent disponibles dans le récapitulatif renvoyé.',
    properties: [
      {
        type: 'ExperimentPersistencePolicy',
        parameters: [
          {
            name: 'experiments',
            type: "'default' | 'none'",
            isOptional: true,
            defaultValue: "'default'",
            description:
              'Définissez cette valeur sur `none` pour ignorer la création de l’expérience ainsi que l’écriture des résultats d’éléments, de la progression et de l’état final.',
          },
          {
            name: 'scores',
            type: "'default' | 'none'",
            isOptional: true,
            defaultValue: "'default'",
            description: 'Définissez cette valeur sur `none` pour ne pas écrire les scores tout en exécutant les scorers.',
          },
        ],
      },
    ],
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<ExperimentSummary>',
    description: 'Récapitulatif de l’expérience terminée.',
    properties: [
      {
        type: 'ExperimentSummary',
        parameters: [
          {
            name: 'experimentId',
            type: 'string',
            description: 'ID unique de l’expérience.',
          },
          {
            name: 'status',
            type: "'pending' | 'running' | 'completed' | 'failed'",
            description: 'État final de l’expérience.',
          },
          {
            name: 'totalItems',
            type: 'number',
            description: 'Nombre total d’éléments du jeu de données.',
          },
          {
            name: 'succeededCount',
            type: 'number',
            description: 'Nombre d’éléments ayant réussi.',
          },
          {
            name: 'failedCount',
            type: 'number',
            description: 'Nombre d’éléments ayant échoué.',
          },
          {
            name: 'skippedCount',
            type: 'number',
            description: 'Nombre d’éléments ignorés (par exemple, à cause d’une annulation).',
          },
          {
            name: 'completedWithErrors',
            type: 'boolean',
            description: '`true` si l’exécution est terminée, mais que certains éléments ont échoué.',
          },
          {
            name: 'startedAt',
            type: 'Date',
            description: 'Date et heure de début de l’expérience.',
          },
          {
            name: 'completedAt',
            type: 'Date',
            description: 'Date et heure de fin de l’expérience.',
          },
          {
            name: 'results',
            type: 'ItemWithScores[]',
            description: 'Tous les résultats d’éléments avec leurs scores.',
            properties: [
              {
                type: 'ItemWithScores',
                parameters: [
                  {
                    name: 'itemId',
                    type: 'string',
                    description: 'ID de l’élément du jeu de données.',
                  },
                  {
                    name: 'itemVersion',
                    type: 'number',
                    description: 'Version du jeu de données de l’élément au moment de son exécution.',
                  },
                  {
                    name: 'input',
                    type: 'unknown',
                    description: 'Données d’entrée transmises à la cible.',
                  },
                  {
                    name: 'output',
                    type: 'unknown | null',
                    description: 'Sortie de la cible, ou `null` en cas d’échec.',
                  },
                  {
                    name: 'groundTruth',
                    type: 'unknown | null',
                    description: 'Sortie attendue de l’élément du jeu de données.',
                  },
                  {
                    name: 'error',
                    type: '{ message: string; stack?: string; code?: string } | null',
                    description: 'Erreur structurée si l’exécution a échoué.',
                  },
                  {
                    name: 'startedAt',
                    type: 'Date',
                    description: 'Date et heure de début de l’exécution de l’élément.',
                  },
                  {
                    name: 'completedAt',
                    type: 'Date',
                    description: 'Date et heure de fin de l’exécution de l’élément.',
                  },
                  {
                    name: 'retryCount',
                    type: 'number',
                    description: 'Nombre de tentatives de nouvelle exécution.',
                  },
                  {
                    name: 'scores',
                    type: 'ScorerResult[]',
                    description: 'Résultats de tous les scorers pour cet élément.',
                    properties: [
                      {
                        type: 'ScorerResult',
                        parameters: [
                          {
                            name: 'scorerId',
                            type: 'string',
                            description: 'ID du scorer.',
                          },
                          {
                            name: 'scorerName',
                            type: 'string',
                            description: 'Nom d’affichage du scorer.',
                          },
                          {
                            name: 'score',
                            type: 'number | null',
                            description: 'Score calculé, ou `null` si le scorer a échoué.',
                          },
                          {
                            name: 'reason',
                            type: 'string | null',
                            description: 'Motif ou explication du score.',
                          },
                          {
                            name: 'error',
                            type: 'string | null',
                            description: 'Message d’erreur si le scorer a échoué.',
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## Ressources associées {/* #related */}

- [dataset.startExperimentAsync()](/reference/datasets/startExperimentAsync)
- [dataset.listExperiments()](/reference/datasets/listExperiments)
- [DatasetsManager.compareExperiments()](/reference/datasets/compareExperiments)
