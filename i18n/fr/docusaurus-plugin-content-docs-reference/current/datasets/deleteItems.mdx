---
title: "Référence : dataset.deleteItems() | Datasets"
description: "Documentation de la méthode `dataset.deleteItems()`, qui supprime en bloc plusieurs éléments d’un dataset."
packages:
  - "@mastra/core"
---

# dataset.deleteItems()

**Ajouté dans :** `@mastra/core@1.4.0`

Supprime plusieurs éléments du dataset au cours d’une seule opération en bloc.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItems({
  itemIds: ['item-1', 'item-2', 'item-3'],
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'itemIds',
    type: 'string[]',
    description: 'Tableau des ID des éléments à supprimer.',
  },
]}
/>

## Valeur renvoyée

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: 'Se résout lorsque tous les éléments sont supprimés.',
  },
]}
/>
