---
title: "Référence : dataset.deleteItems() | Jeux de données"
description: "Documentation pour the `dataset.deleteItems()` method, which deletes plusieurs items depuis a dataset dans bulk."
packages:
  - "@mastra/core"
---

# dataset.deleteItems()

**Added in:** `@mastra/core@1.4.0`

Supprime plusieurs items depuis the dataset dans a unique bulk operation.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItems({
  itemIds: ['item-1', 'item-2', 'item-3'],
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'itemIds',
    type: 'string[]',
    description: 'Array de item IDs à delete.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: 'Resolves when tous les items sont deleted.',
  },
]}
/>
