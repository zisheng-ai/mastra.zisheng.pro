---
title: "Référence : dataset.update() | Datasets"
description: "Documentation de la méthode `dataset.update()`, qui met à jour les métadonnées et les schémas d’un dataset."
packages:
  - "@mastra/core"
---

# dataset.update()

**Ajouté dans :** `@mastra/core@1.4.0`

Met à jour les métadonnées, le nom, la description et/ou les schémas du dataset. Utilisez Standard JSON Schema (Zod, Valibot, ArkType, etc.) pour définir les schémas.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'
import { z } from 'zod'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Update with plain metadata
const updated = await dataset.update({
  name: 'Updated QA pairs',
  description: 'Revised evaluation set',
})

// Update with Zod schema (library that supports Standard JSON Schema)
const updated2 = await dataset.update({
  inputSchema: z.object({
    question: z.string(),
    context: z.string().optional(),
  }),
})
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: 'Nouveau nom d’affichage.',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: 'Nouvelle description.',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: 'Métadonnées mises à jour.',
  },
  {
    name: 'inputSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: 'Standard JSON Schema pour les entrées des éléments.',
  },
  {
    name: 'groundTruthSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: 'Standard JSON Schema pour les vérités terrain des éléments.',
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetRecord>',
    description: 'Enregistrement du dataset mis à jour. Consultez dataset.getDetails() pour connaître sa structure complète.',
  },
]}
/>
