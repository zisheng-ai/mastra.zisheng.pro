---
title: "Référence : dataset.update() | Jeux de données"
description: "Documentation pour the `dataset.update()` method, which updates dataset metadata et schemas."
packages:
  - "@mastra/core"
---

# dataset.update()

**Added in:** `@mastra/core@1.4.0`

Met à jour dataset metadata, name, description, and/or schemas. Use Standard JSON Schema (Zod, Valibot, ArkType, etc.) pour defining schemas.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'
import { z } from 'zod'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Update with plain metadata
const updated = await dataset.update({
  name: 'Updated QA pairs',
  description: 'Revised evaluation set',
})

// Update with Zod schema (library that supports Standard JSON Schema)
const updated2 = await dataset.update({
  inputSchema: z.object({
    question: z.string(),
    context: z.string().optional(),
  }),
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: 'New affiche name.',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: 'New description.',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: 'Updated metadata.',
  },
  {
    name: 'inputSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: 'Standard JSON Schema pour item inputs.',
  },
  {
    name: 'groundTruthSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: 'Standard JSON Schema pour item ground truths.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetRecord>',
    description: 'The updated dataset record. Consultez dataset.getDétails() pour the full shape.',
  },
]}
/>
