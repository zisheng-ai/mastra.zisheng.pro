---
title: "Référence : Jeux de donnéesManager.delete() | Jeux de données"
description: "Documentation pour the `DatasetsManager.delete()` method, which deletes a dataset par ID."
packages:
  - "@mastra/core"
---

# Jeux de donnéesManager.delete()

**Added in:** `@mastra/core@1.4.0`

Supprime a dataset par ID, including tous les items, versions, et experiments.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

await mastra.datasets.delete({ id: 'dataset-id' })
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'ID de the dataset à delete.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: 'Resolves when the dataset est deleted.',
  },
]}
/>
