---
title: "Référence : dataset.listItems() | Jeux de données"
description: "Documentation pour the `dataset.listItems()` method, which lists items dans a dataset avec facultatif pagination et filtering."
packages:
  - "@mastra/core"
---

# dataset.listItems()

**Added in:** `@mastra/core@1.4.0`

Liste items dans the dataset. When only `version` est provided, returns a bare `DatasetItem[]` snapshot de every item à that version. In tous les autre cases (no arguments, ou `search` / `page` / `perPage` fourni avec ou sans `version`), it returns a paginated `{ items, pagination }` shape.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Paginated list (default: page 0, 20 per page)
const result = await dataset.listItems()

// List with search
const filtered = await dataset.listItems({ search: 'TypeScript', page: 0, perPage: 10 })

// Paginated list scoped to a specific version
const versionedPage = await dataset.listItems({ version: 2, page: 0, perPage: 20 })

// Version-only snapshot returns a bare DatasetItem[] (deprecated form —
// prefer passing page/perPage to receive the paginated shape).
const versionedItems = await dataset.listItems({ version: 2 })
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description:
      'Jeu de données version à liste items at. When passed sur its own, returns every item à that version comme a bare `DatasetItem[]` snapshot. Passing `search` / `page` / `perPage` alongside `version` switches à the paginated shape.',
  },
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'Page nombre pour pagination. Par défauts à `0`.',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: 'Number de items per page. Par défauts à `20`.',
  },
  {
    name: 'search',
    type: 'string',
    isOptional: true,
    description: 'Recherche chaîne à filter items.',
  },
]}
/>

## Renvoie

When only `version` est fourni (no `search`, `page`, ou `perPage`):

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description:
      'Bare tableau de every item à the specified dataset version. Ce shape est retained pour backwards compatibility et est deprecated — transmet `page` / `perPage` (or `search`) à always reçoit the paginated shape below.',
  },
]}
/>

<br />

In tous les autre cases (no arguments, ou `search` / `page` / `perPage` fourni avec ou sans `version`):

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Paginated response.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'items',
            type: 'DatasetItem[]',
            description: 'Array de items pour the actuel page.',
          },
          {
            name: 'pagination',
            type: 'object',
            description: 'Pagination metadata.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'total',
                    type: 'number',
                    description: 'Total nombre de items.',
                  },
                  {
                    name: 'page',
                    type: 'number',
                    description: 'Current page number.',
                  },
                  {
                    name: 'perPage',
                    type: 'number | false',
                    description: 'Items per page, ou `false` if unpaginated.',
                  },
                  {
                    name: 'hasMore',
                    type: 'boolean',
                    description: 'Whether more pages sont available.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>
