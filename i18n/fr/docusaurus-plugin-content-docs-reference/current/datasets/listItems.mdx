---
title: "Référence : dataset.listItems() | Datasets"
description: "Documentation de la méthode `dataset.listItems()`, qui répertorie les éléments d’un dataset avec une pagination et un filtrage facultatifs."
packages:
  - "@mastra/core"
---

# dataset.listItems()

**Ajouté dans :** `@mastra/core@1.4.0`

Répertorie les éléments du dataset. Lorsque seul `version` est fourni, renvoie un instantané `DatasetItem[]` brut de tous les éléments dans cette version. Dans tous les autres cas — aucun argument, ou `search` / `page` / `perPage` fourni avec ou sans `version` — la méthode renvoie une structure paginée `{ items, pagination }`.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Paginated list (default: page 0, 20 per page)
const result = await dataset.listItems()

// List with search
const filtered = await dataset.listItems({ search: 'TypeScript', page: 0, perPage: 10 })

// Paginated list scoped to a specific version
const versionedPage = await dataset.listItems({ version: 2, page: 0, perPage: 20 })

// Version-only snapshot returns a bare DatasetItem[] (deprecated form —
// prefer passing page/perPage to receive the paginated shape).
const versionedItems = await dataset.listItems({ version: 2 })
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description:
      'Version du dataset dans laquelle répertorier les éléments. Lorsqu’il est transmis seul, ce paramètre renvoie tous les éléments de cette version sous la forme d’un instantané `DatasetItem[]` brut. La transmission de `search` / `page` / `perPage` avec `version` utilise la structure paginée.',
  },
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'Numéro de page pour la pagination. La valeur par défaut est `0`.',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: 'Nombre d’éléments par page. La valeur par défaut est `20`.',
  },
  {
    name: 'search',
    type: 'string',
    isOptional: true,
    description: 'Chaîne de recherche utilisée pour filtrer les éléments.',
  },
]}
/>

## Valeur renvoyée {/* #returns */}

Lorsque seul `version` est fourni, sans `search`, `page` ni `perPage` :

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description:
      'Tableau brut de tous les éléments dans la version indiquée du dataset. Cette structure est conservée pour la rétrocompatibilité et est obsolète. Transmettez `page` / `perPage` (ou `search`) pour toujours recevoir la structure paginée ci-dessous.',
  },
]}
/>

<br />

Dans tous les autres cas — aucun argument, ou `search` / `page` / `perPage` fourni avec ou sans `version` :

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Réponse paginée.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'items',
            type: 'DatasetItem[]',
            description: 'Tableau des éléments de la page actuelle.',
          },
          {
            name: 'pagination',
            type: 'object',
            description: 'Métadonnées de pagination.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'total',
                    type: 'number',
                    description: 'Nombre total d’éléments.',
                  },
                  {
                    name: 'page',
                    type: 'number',
                    description: 'Numéro de la page actuelle.',
                  },
                  {
                    name: 'perPage',
                    type: 'number | false',
                    description: 'Nombre d’éléments par page, ou `false` en l’absence de pagination.',
                  },
                  {
                    name: 'hasMore',
                    type: 'boolean',
                    description: 'Indique si d’autres pages sont disponibles.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>
