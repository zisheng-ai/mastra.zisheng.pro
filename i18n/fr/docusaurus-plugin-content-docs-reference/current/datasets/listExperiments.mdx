---
title: "Reference: dataset.listExperiments() | Datasets"
description: "Documentation for the `dataset.listExperiments()` method, which lists all experiments for a dataset."
packages:
  - "@mastra/core"
---

# dataset.listExperiments()

**Added in:** `@mastra/core@1.4.0`

Lists experiments (runs) for this dataset with optional filters and pagination. Filters are applied at the storage layer, so results are always scoped to the current dataset.

## Usage example

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Basic pagination
const { experiments, pagination } = await dataset.listExperiments({ page: 0, perPage: 10 })

// Filter to a specific agent version — useful for baseline vs variant comparisons
const { experiments: v2Runs } = await dataset.listExperiments({
  targetType: 'agent',
  targetId: 'my-agent',
  agentVersion: 'v2',
  status: 'completed',
})

for (const exp of experiments) {
  console.log(`${exp.id}: ${exp.status} (${exp.succeededCount}/${exp.totalItems})`)
}
```

## Parameters

<PropertiesTable
  content={[
  {
    name: 'targetType',
    type: "'agent' | 'workflow' | 'scorer' | 'processor'",
    isOptional: true,
    description: 'Restrict results to experiments run against this target type.',
  },
  {
    name: 'targetId',
    type: 'string',
    isOptional: true,
    description: 'Restrict results to experiments run against this target ID.',
  },
  {
    name: 'agentVersion',
    type: 'string',
    isOptional: true,
    description:
      'Restrict results to experiments recorded against this agent version. Useful for distinguishing baseline from variant runs.',
  },
  {
    name: 'status',
    type: "'pending' | 'running' | 'completed' | 'failed'",
    isOptional: true,
    description: 'Restrict results to experiments in this status.',
  },
  {
    name: 'filters',
    type: 'ExperimentTenancyFilters',
    isOptional: true,
    description: 'Multi-tenant scoping filters (`organizationId`, `projectId`). Forwarded to the storage layer.',
  },
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'Page number. Defaults to `0`.',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: 'Number of experiments per page. Defaults to `20`.',
  },
]}
/>

## Returns

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Paginated experiment list.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'experiments',
            type: 'Experiment[]',
            description: 'Array of experiment records.',
            properties: [
              {
                type: 'Experiment',
                parameters: [
                  {
                    name: 'id',
                    type: 'string',
                    description: 'Unique experiment ID.',
                  },
                  {
                    name: 'name',
                    type: 'string',
                    isOptional: true,
                    description: 'Display name.',
                  },
                  {
                    name: 'description',
                    type: 'string',
                    isOptional: true,
                    description: 'Description.',
                  },
                  {
                    name: 'metadata',
                    type: 'Record<string, unknown>',
                    isOptional: true,
                    description: 'Arbitrary metadata.',
                  },
                  {
                    name: 'datasetId',
                    type: 'string',
                    description: 'ID of the parent dataset.',
                  },
                  {
                    name: 'datasetVersion',
                    type: 'number | null',
                    description: 'Dataset version used for the experiment.',
                  },
                  {
                    name: 'targetType',
                    type: "'agent' | 'workflow' | 'scorer' | 'processor'",
                    description: 'Type of target used.',
                  },
                  {
                    name: 'targetId',
                    type: 'string',
                    description: 'ID of the target used.',
                  },
                  {
                    name: 'status',
                    type: "'pending' | 'running' | 'completed' | 'failed'",
                    description: 'Current status of the experiment.',
                  },
                  {
                    name: 'totalItems',
                    type: 'number',
                    description: 'Total number of items.',
                  },
                  {
                    name: 'succeededCount',
                    type: 'number',
                    description: 'Number of successful items.',
                  },
                  {
                    name: 'failedCount',
                    type: 'number',
                    description: 'Number of failed items.',
                  },
                  {
                    name: 'skippedCount',
                    type: 'number',
                    description: 'Number of skipped items.',
                  },
                  {
                    name: 'startedAt',
                    type: 'Date | null',
                    description: 'When the experiment started.',
                  },
                  {
                    name: 'completedAt',
                    type: 'Date | null',
                    description: 'When the experiment completed.',
                  },
                  {
                    name: 'createdAt',
                    type: 'Date',
                    description: 'When the experiment record was created.',
                  },
                  {
                    name: 'updatedAt',
                    type: 'Date',
                    description: 'When the experiment record was last updated.',
                  },
                ],
              },
            ],
          },
          {
            name: 'pagination',
            type: 'PaginationInfo',
            description: 'Pagination metadata with `total`, `page`, `perPage`, and `hasMore`.',
          },
        ],
      },
    ],
  },
]}
/>
