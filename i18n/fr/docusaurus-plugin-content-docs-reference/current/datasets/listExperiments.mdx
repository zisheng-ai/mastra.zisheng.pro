---
title: "Référence : dataset.listExperiments() | Jeux de données"
description: "Documentation pour the `dataset.listExperiments()` method, which lists tous les experiments pour a dataset."
packages:
  - "@mastra/core"
---

# dataset.listExperiments()

**Added in:** `@mastra/core@1.4.0`

Liste experiments (runs) pour this dataset avec facultatif filters et pagination. Filtres sont applied à the storage layer, so results sont always scoped à the actuel dataset.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Basic pagination
const { experiments, pagination } = await dataset.listExperiments({ page: 0, perPage: 10 })

// Filter to a specific agent version — useful for baseline vs variant comparisons
const { experiments: v2Runs } = await dataset.listExperiments({
  targetType: 'agent',
  targetId: 'my-agent',
  agentVersion: 'v2',
  status: 'completed',
})

for (const exp of experiments) {
  console.log(`${exp.id}: ${exp.status} (${exp.succeededCount}/${exp.totalItems})`)
}
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'targetType',
    type: "'agent' | 'workflow' | 'scorer' | 'processor'",
    isOptional: true,
    description: 'Restrict results à experiments exécute against this target type.',
  },
  {
    name: 'targetId',
    type: 'string',
    isOptional: true,
    description: 'Restrict results à experiments exécute against this target ID.',
  },
  {
    name: 'agentVersion',
    type: 'string',
    isOptional: true,
    description:
      'Restrict results à experiments recorded against this agent version. Useful pour distinguishing baseline depuis variant runs.',
  },
  {
    name: 'status',
    type: "'pending' | 'running' | 'completed' | 'failed'",
    isOptional: true,
    description: 'Restrict results à experiments dans this status.',
  },
  {
    name: 'filters',
    type: 'ExperimentTenancyFilters',
    isOptional: true,
    description: 'Multi-tenant scoping filters (`organizationId`, `projectId`). Forwarded à the storage layer.',
  },
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'Page number. Par défauts à `0`.',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: 'Number de experiments per page. Par défauts à `20`.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Paginated experiment list.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'experiments',
            type: 'Experiment[]',
            description: 'Array de experiment records.',
            properties: [
              {
                type: 'Experiment',
                parameters: [
                  {
                    name: 'id',
                    type: 'string',
                    description: 'Unique experiment ID.',
                  },
                  {
                    name: 'name',
                    type: 'string',
                    isOptional: true,
                    description: 'Display name.',
                  },
                  {
                    name: 'description',
                    type: 'string',
                    isOptional: true,
                    description: 'Description.',
                  },
                  {
                    name: 'metadata',
                    type: 'Record<string, unknown>',
                    isOptional: true,
                    description: 'Arbitrary metadata.',
                  },
                  {
                    name: 'datasetId',
                    type: 'string',
                    description: 'ID de the parent dataset.',
                  },
                  {
                    name: 'datasetVersion',
                    type: 'number | null',
                    description: 'Jeu de données version used pour the experiment.',
                  },
                  {
                    name: 'targetType',
                    type: "'agent' | 'workflow' | 'scorer' | 'processor'",
                    description: 'Type de target used.',
                  },
                  {
                    name: 'targetId',
                    type: 'string',
                    description: 'ID de the target used.',
                  },
                  {
                    name: 'status',
                    type: "'pending' | 'running' | 'completed' | 'failed'",
                    description: 'Current status de the experiment.',
                  },
                  {
                    name: 'totalItems',
                    type: 'number',
                    description: 'Total nombre de items.',
                  },
                  {
                    name: 'succeededCount',
                    type: 'number',
                    description: 'Number de successful items.',
                  },
                  {
                    name: 'failedCount',
                    type: 'number',
                    description: 'Number de failed items.',
                  },
                  {
                    name: 'skippedCount',
                    type: 'number',
                    description: 'Number de skipped items.',
                  },
                  {
                    name: 'startedAt',
                    type: 'Date | null',
                    description: 'Lorsque le experiment started.',
                  },
                  {
                    name: 'completedAt',
                    type: 'Date | null',
                    description: 'Lorsque le experiment completed.',
                  },
                  {
                    name: 'createdAt',
                    type: 'Date',
                    description: 'Lorsque le experiment record était created.',
                  },
                  {
                    name: 'updatedAt',
                    type: 'Date',
                    description: 'Lorsque le experiment record était dernier updated.',
                  },
                ],
              },
            ],
          },
          {
            name: 'pagination',
            type: 'PaginationInfo',
            description: 'Pagination metadata avec `total`, `page`, `perPage`, et `hasMore`.',
          },
        ],
      },
    ],
  },
]}
/>
