---
title: "Référence : Jeux de donnéesManager | Jeux de données"
description: "Documentation pour the `DatasetsManager` class dans Mastra, which provides the public API pour managing datasets."
packages:
  - "@mastra/core"
---

# Jeux de donnéesManager

**Added in:** `@mastra/core@1.4.0`

The `DatasetsManager` class provides the public API pour managing datasets, including CRUD operations et cross-dataset experiment comparisons. Access it via `mastra.datasets`.

## Exemples d’utilisation

### Créer et liste datasets

```typescript title="src/mastra/datasets/manage.ts"
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

// Create a dataset
const dataset = await mastra.datasets.create({
  name: 'QA pairs',
  description: 'Question-answer evaluation set',
})

// List all datasets
const { datasets } = await mastra.datasets.list()
```

### Obtenir et supprime a dataset

```typescript title="src/mastra/datasets/get-delete.ts"
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

// Get by ID
const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Delete by ID
await mastra.datasets.delete({ id: 'dataset-id' })
```

### Obtenir experiment

Retrieves a specific experiment (run) par ID. Unlike `dataset.getExperiment()`, this works across tous les datasets sans needing a dataset reference first.

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

// Get experiment directly without knowing the dataset
const experiment = await mastra.datasets.getExperiment({
  experimentId: 'exp-id',
})

console.log(`Dataset: ${experiment.datasetId}`)
console.log(`Status: ${experiment.status}`)
```

### Compare experiments

```typescript title="src/mastra/datasets/compare.ts"
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const comparison = await mastra.datasets.compareExperiments({
  experimentIds: ['exp-1', 'exp-2'],
  baselineId: 'exp-1',
})
```

## Access

`DatasetsManager` isn't instantiated directly. Access it depuis a `Mastra` instance:

```typescript
const mastra = new Mastra({/* storage config */})
const datasetsManager = mastra.datasets
```

## Voir aussi

- [Jeu de données class](/reference/datasets/dataset)
- [Jeux de donnéesManager.create()](/reference/datasets/create)
- [Jeux de donnéesManager.compareExperiments()](/reference/datasets/compareExperiments)
