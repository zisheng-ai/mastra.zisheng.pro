---
title: "Référence : Jeux de donnéesManager.list() | Jeux de données"
description: "Documentation pour the `DatasetsManager.list()` method, which lists tous les datasets avec pagination."
packages:
  - "@mastra/core"
---

# Jeux de donnéesManager.list()

**Added in:** `@mastra/core@1.4.0`

Liste datasets avec pagination et facultatif filters. Filtres sont applied à the storage layer, so callers don't need à post-filter results.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const { datasets, pagination } = await mastra.datasets.list({ page: 0, perPage: 10 })

for (const ds of datasets) {
  console.log(`${ds.id}: ${ds.name} (v${ds.version})`)
}

// Filter by target type and name (case-insensitive substring)
const filtered = await mastra.datasets.list({
  page: 0,
  perPage: 20,
  filters: { targetType: 'agent', name: 'qa' },
})

// Filter by any-overlap match on target IDs
const forAgents = await mastra.datasets.list({
  filters: { targetIds: ['agent-a', 'agent-b'] },
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'Page number. Par défauts à `0`.',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: 'Number de datasets per page. Par défauts à `20`.',
  },
  {
    name: 'filters',
    type: 'object',
    isOptional: true,
    description: 'Facultatif filters applied à the storage layer.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'organizationId',
            type: 'string',
            isOptional: true,
            description: 'Tenancy filter: restrict à datasets dans this organization.',
          },
          {
            name: 'projectId',
            type: 'string',
            isOptional: true,
            description: 'Tenancy filter: restrict à datasets dans this project.',
          },
          {
            name: 'candidateKey',
            type: 'string',
            isOptional: true,
            description: 'Restrict à datasets belonging à this candidate key.',
          },
          {
            name: 'candidateId',
            type: 'string',
            isOptional: true,
            description: 'Restrict à datasets belonging à this candidate ID.',
          },
          {
            name: 'targetType',
            type: "'agent' | 'workflow' | 'scorer' | 'processor'",
            isOptional: true,
            description: 'Restrict à datasets targeting this component type.',
          },
          {
            name: 'targetIds',
            type: 'string[]',
            isOptional: true,
            description:
              'Restrict à datasets whose `targetIds` intersect this liste (any-overlap match). Empty tableau est treated comme aucun filter.',
          },
          {
            name: 'name',
            type: 'string',
            isOptional: true,
            description: 'Case-insensitive substring match sur dataset `name`. Empty chaîne est treated comme aucun filter.',
          },
        ],
      },
    ],
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Paginated dataset list.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'datasets',
            type: 'DatasetRecord[]',
            description: 'Array de dataset records. Consultez dataset.getDétails() pour the record shape.',
          },
          {
            name: 'pagination',
            type: 'PaginationInfo',
            description: 'Pagination metadata avec `total`, `page`, `perPage`, et `hasMore`.',
          },
        ],
      },
    ],
  },
]}
/>
