---
title: "Référence : DatasetsManager.list() | Datasets"
description: "Documentation de la méthode `DatasetsManager.list()`, qui liste tous les datasets avec pagination."
packages:
  - "@mastra/core"
---

# DatasetsManager.list()

**Ajouté dans :** `@mastra/core@1.4.0`

Liste les datasets avec pagination et filtres facultatifs. Les filtres sont appliqués au niveau du stockage, afin que les appelants n’aient pas à filtrer de nouveau les résultats.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const { datasets, pagination } = await mastra.datasets.list({ page: 0, perPage: 10 })

for (const ds of datasets) {
  console.log(`${ds.id}: ${ds.name} (v${ds.version})`)
}

// Filter by target type and name (case-insensitive substring)
const filtered = await mastra.datasets.list({
  page: 0,
  perPage: 20,
  filters: { targetType: 'agent', name: 'qa' },
})

// Filter by any-overlap match on target IDs
const forAgents = await mastra.datasets.list({
  filters: { targetIds: ['agent-a', 'agent-b'] },
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'Numéro de page. Utilise `0` par défaut.',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: 'Nombre de datasets par page. Utilise `20` par défaut.',
  },
  {
    name: 'filters',
    type: 'object',
    isOptional: true,
    description: 'Filtres facultatifs appliqués au niveau du stockage.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'organizationId',
            type: 'string',
            isOptional: true,
            description: 'Filtre de cloisonnement : limite aux datasets de cette organisation.',
          },
          {
            name: 'projectId',
            type: 'string',
            isOptional: true,
            description: 'Filtre de cloisonnement : limite aux datasets de ce projet.',
          },
          {
            name: 'candidateKey',
            type: 'string',
            isOptional: true,
            description: 'Limite aux datasets appartenant à cette clé de candidat.',
          },
          {
            name: 'candidateId',
            type: 'string',
            isOptional: true,
            description: 'Limite aux datasets appartenant à cet ID de candidat.',
          },
          {
            name: 'targetType',
            type: "'agent' | 'workflow' | 'scorer' | 'processor'",
            isOptional: true,
            description: 'Limite aux datasets ciblant ce type de composant.',
          },
          {
            name: 'targetIds',
            type: 'string[]',
            isOptional: true,
            description:
              'Limite aux datasets dont les `targetIds` intersectent cette liste (correspondance avec chevauchement). Un tableau vide est traité comme l’absence de filtre.',
          },
          {
            name: 'name',
            type: 'string',
            isOptional: true,
            description: 'Correspondance de sous-chaîne insensible à la casse sur le `name` du dataset. Une chaîne vide est traitée comme l’absence de filtre.',
          },
        ],
      },
    ],
  },
]}
/>

## Valeur renvoyée

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Liste paginée de datasets.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'datasets',
            type: 'DatasetRecord[]',
            description: 'Tableau d’enregistrements de dataset. Consultez dataset.getDetails() pour connaître la structure de l’enregistrement.',
          },
          {
            name: 'pagination',
            type: 'PaginationInfo',
            description: 'Métadonnées de pagination avec `total`, `page`, `perPage` et `hasMore`.',
          },
        ],
      },
    ],
  },
]}
/>
