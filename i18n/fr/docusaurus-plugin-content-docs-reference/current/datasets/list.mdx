---
title: "Reference: DatasetsManager.list() | Datasets"
description: "Documentation for the `DatasetsManager.list()` method, which lists all datasets with pagination."
packages:
  - "@mastra/core"
---

# DatasetsManager.list()

**Added in:** `@mastra/core@1.4.0`

Lists datasets with pagination and optional filters. Filters are applied at the storage layer, so callers don't need to post-filter results.

## Usage example

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const { datasets, pagination } = await mastra.datasets.list({ page: 0, perPage: 10 })

for (const ds of datasets) {
  console.log(`${ds.id}: ${ds.name} (v${ds.version})`)
}

// Filter by target type and name (case-insensitive substring)
const filtered = await mastra.datasets.list({
  page: 0,
  perPage: 20,
  filters: { targetType: 'agent', name: 'qa' },
})

// Filter by any-overlap match on target IDs
const forAgents = await mastra.datasets.list({
  filters: { targetIds: ['agent-a', 'agent-b'] },
})
```

## Parameters

<PropertiesTable
  content={[
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'Page number. Defaults to `0`.',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: 'Number of datasets per page. Defaults to `20`.',
  },
  {
    name: 'filters',
    type: 'object',
    isOptional: true,
    description: 'Optional filters applied at the storage layer.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'organizationId',
            type: 'string',
            isOptional: true,
            description: 'Tenancy filter: restrict to datasets in this organization.',
          },
          {
            name: 'projectId',
            type: 'string',
            isOptional: true,
            description: 'Tenancy filter: restrict to datasets in this project.',
          },
          {
            name: 'candidateKey',
            type: 'string',
            isOptional: true,
            description: 'Restrict to datasets belonging to this candidate key.',
          },
          {
            name: 'candidateId',
            type: 'string',
            isOptional: true,
            description: 'Restrict to datasets belonging to this candidate ID.',
          },
          {
            name: 'targetType',
            type: "'agent' | 'workflow' | 'scorer' | 'processor'",
            isOptional: true,
            description: 'Restrict to datasets targeting this component type.',
          },
          {
            name: 'targetIds',
            type: 'string[]',
            isOptional: true,
            description:
              'Restrict to datasets whose `targetIds` intersect this list (any-overlap match). Empty array is treated as no filter.',
          },
          {
            name: 'name',
            type: 'string',
            isOptional: true,
            description: 'Case-insensitive substring match on dataset `name`. Empty string is treated as no filter.',
          },
        ],
      },
    ],
  },
]}
/>

## Returns

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'Paginated dataset list.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'datasets',
            type: 'DatasetRecord[]',
            description: 'Array of dataset records. See dataset.getDetails() for the record shape.',
          },
          {
            name: 'pagination',
            type: 'PaginationInfo',
            description: 'Pagination metadata with `total`, `page`, `perPage`, and `hasMore`.',
          },
        ],
      },
    ],
  },
]}
/>
