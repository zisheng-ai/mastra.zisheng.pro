---
title: "Référence : Jeux de donnéesManager.create() | Jeux de données"
description: "Documentation pour the `DatasetsManager.create()` method, which creates a nouveau dataset."
packages:
  - "@mastra/core"
---

# Jeux de donnéesManager.create()

**Added in:** `@mastra/core@1.4.0`

Crée a nouveau dataset. Use Standard JSON Schema (Zod, Valibot, ArkType, etc.) pour defining schemas.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'
import { z } from 'zod'

const mastra = new Mastra({/* storage config */})

// Create with metadata
const dataset = await mastra.datasets.create({
  name: 'QA evaluation set',
  description: 'Question-answer pairs for testing',
  metadata: { team: 'ml' },
})

// Create with Zod schema (library that supports Standard JSON Schema)
const typedDataset = await mastra.datasets.create({
  name: 'Typed QA set',
  inputSchema: z.object({
    question: z.string(),
    context: z.string().optional(),
  }),
  groundTruthSchema: z.object({
    answer: z.string(),
  }),
})
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: 'Display nom pour the dataset.',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: 'Description de the dataset.',
  },
  {
    name: 'inputSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: 'Standard JSON Schema pour item inputs.',
  },
  {
    name: 'groundTruthSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: 'Standard JSON Schema pour item ground truths.',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: 'Arbitrary metadata.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Dataset>',
    description: 'Un Jeu de données instance pour the newly created dataset.',
  },
]}
/>
