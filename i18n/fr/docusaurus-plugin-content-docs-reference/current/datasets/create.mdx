---
title: "Référence : DatasetsManager.create() | Datasets"
description: "Documentation de la méthode `DatasetsManager.create()`, qui crée un nouveau dataset."
packages:
  - "@mastra/core"
---

# DatasetsManager.create()

**Ajouté dans :** `@mastra/core@1.4.0`

Crée un nouveau dataset. Utilisez Standard JSON Schema (Zod, Valibot, ArkType, etc.) pour définir les schémas.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'
import { z } from 'zod'

const mastra = new Mastra({/* storage config */})

// Create with metadata
const dataset = await mastra.datasets.create({
  name: 'QA evaluation set',
  description: 'Question-answer pairs for testing',
  metadata: { team: 'ml' },
})

// Create with Zod schema (library that supports Standard JSON Schema)
const typedDataset = await mastra.datasets.create({
  name: 'Typed QA set',
  inputSchema: z.object({
    question: z.string(),
    context: z.string().optional(),
  }),
  groundTruthSchema: z.object({
    answer: z.string(),
  }),
})
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: 'Nom d’affichage du dataset.',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: 'Description du dataset.',
  },
  {
    name: 'inputSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: 'Standard JSON Schema pour les entrées des éléments.',
  },
  {
    name: 'groundTruthSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: 'Standard JSON Schema pour les vérités terrain des éléments.',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: 'Métadonnées arbitraires.',
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Dataset>',
    description: 'Instance de Dataset correspondant au dataset nouvellement créé.',
  },
]}
/>
