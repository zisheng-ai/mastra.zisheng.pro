---
title: "Référence : dataset.deleteItem() | Jeux de données"
description: "Documentation pour the `dataset.deleteItem()` method, which deletes a unique item depuis a dataset."
packages:
  - "@mastra/core"
---

# dataset.deleteItem()

**Added in:** `@mastra/core@1.4.0`

Supprime a unique item depuis the dataset par ID.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItem({ itemId: 'item-id' })
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: 'ID de the item à delete.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: 'Resolves when the item est deleted.',
  },
]}
/>
