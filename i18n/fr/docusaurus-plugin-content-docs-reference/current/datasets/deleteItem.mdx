---
title: "Référence : dataset.deleteItem() | Datasets"
description: "Documentation de la méthode `dataset.deleteItem()`, qui supprime un seul élément d’un dataset."
packages:
  - "@mastra/core"
---

# dataset.deleteItem()

**Ajouté dans :** `@mastra/core@1.4.0`

Supprime un seul élément du dataset à partir de son ID.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItem({ itemId: 'item-id' })
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: 'ID de l’élément à supprimer.',
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: 'Se résout lorsque l’élément est supprimé.',
  },
]}
/>
