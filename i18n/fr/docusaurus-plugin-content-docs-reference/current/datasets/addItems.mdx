---
title: "Référence : dataset.addItems() | Jeux de données"
description: "Documentation pour the `dataset.addItems()` method, which adds plusieurs items à a dataset dans bulk."
packages:
  - "@mastra/core"
---

# dataset.addItems()

**Added in:** `@mastra/core@1.4.0`

Ajoute plusieurs items à the dataset dans a unique bulk operation.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const items = await dataset.addItems({
  items: [
    { input: { question: 'What is AI?' }, groundTruth: { answer: 'Artificial Intelligence' } },
    { input: { question: 'What is ML?' }, groundTruth: { answer: 'Machine Learning' } },
    { input: { question: 'What is DL?' }, metadata: { category: 'deep-learning' } },
  ],
})

console.log(`Added ${items.length} items`)
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'items',
    type: 'Array<object>',
    description: 'Array de items à add. Chaque item possède the same shape comme the dataset.addItem() input.',
    properties: [
      {
        type: 'DatasetItem',
        parameters: [
          {
            name: 'input',
            type: 'unknown',
            description: 'Entrée données pour the item.',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: 'Expected output ou ground truth.',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: 'Arbitrary metadata.',
          },
        ],
      },
    ],
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description: 'Array de created dataset items. Consultez dataset.addItem() pour the item shape.',
  },
]}
/>
