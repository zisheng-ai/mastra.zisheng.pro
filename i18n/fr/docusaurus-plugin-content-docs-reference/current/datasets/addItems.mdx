---
title: "Référence : dataset.addItems() | Datasets"
description: "Documentation de la méthode `dataset.addItems()`, qui ajoute en bloc plusieurs éléments à un dataset."
packages:
  - "@mastra/core"
---

# dataset.addItems()

**Ajouté dans :** `@mastra/core@1.4.0`

Ajoute plusieurs éléments au dataset au cours d’une seule opération en bloc.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const items = await dataset.addItems({
  items: [
    { input: { question: 'What is AI?' }, groundTruth: { answer: 'Artificial Intelligence' } },
    { input: { question: 'What is ML?' }, groundTruth: { answer: 'Machine Learning' } },
    { input: { question: 'What is DL?' }, metadata: { category: 'deep-learning' } },
  ],
})

console.log(`Added ${items.length} items`)
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'items',
    type: 'Array<object>',
    description: 'Tableau des éléments à ajouter. Chaque élément a la même structure que l’entrée de dataset.addItem().',
    properties: [
      {
        type: 'DatasetItem',
        parameters: [
          {
            name: 'input',
            type: 'unknown',
            description: 'Données d’entrée de l’élément.',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: 'Sortie attendue ou vérité terrain.',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: 'Métadonnées arbitraires.',
          },
        ],
      },
    ],
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description: 'Tableau des éléments de dataset créés. Consultez dataset.addItem() pour connaître la structure de l’élément.',
  },
]}
/>
