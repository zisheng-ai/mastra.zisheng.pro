---
title: "Référence : dataset.getItem() | Jeux de données"
description: "Documentation pour the `dataset.getItem()` method, which retrieves a unique item par ID."
packages:
  - "@mastra/core"
---

# dataset.getItem()

**Added in:** `@mastra/core@1.4.0`

Retrieves a unique dataset item par ID, optionally à a specific dataset version.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Get latest version of an item
const item = await dataset.getItem({ itemId: 'item-id' })

// Get item at a specific dataset version
const versionedItem = await dataset.getItem({ itemId: 'item-id', version: 2 })
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: 'The unique identifiant de the item.',
  },
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description: 'Jeu de données version à retrieve the item at. Par défauts à the latest version.',
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem | null>',
    description: 'The dataset item, ou null if ne pas found. Consultez dataset.addItem() pour the item shape.',
  },
]}
/>
