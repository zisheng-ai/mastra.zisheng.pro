---
title: "Référence : dataset.getItem() | Datasets"
description: "Documentation de la méthode `dataset.getItem()`, qui récupère un seul élément à partir de son ID."
packages:
  - "@mastra/core"
---

# dataset.getItem()

**Ajouté dans :** `@mastra/core@1.4.0`

Récupère un seul élément de dataset à partir de son ID, éventuellement pour une version spécifique du dataset.

## Exemple d’utilisation

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Get latest version of an item
const item = await dataset.getItem({ itemId: 'item-id' })

// Get item at a specific dataset version
const versionedItem = await dataset.getItem({ itemId: 'item-id', version: 2 })
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: 'Identifiant unique de l’élément.',
  },
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description: 'Version du dataset à laquelle récupérer l’élément. Utilise la version la plus récente par défaut.',
  },
]}
/>

## Valeur renvoyée

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem | null>',
    description: 'Élément du dataset, ou null s’il est introuvable. Consultez dataset.addItem() pour connaître la structure de l’élément.',
  },
]}
/>
