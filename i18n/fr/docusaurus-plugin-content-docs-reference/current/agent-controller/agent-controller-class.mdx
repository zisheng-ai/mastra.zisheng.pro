---
title: "Référence : AgentController | AgentController"
description: "Référence de l’API AgentController, l’hôte partagé qui crée des sessions isolées et coordonne les Agents, le stockage, les outils, les Workspaces et les canaux."
packages:
  - "@mastra/core"
---

# AgentController

:::experimental

La fonctionnalité `AgentController` est en phase bêta et peut subir des changements incompatibles dans les versions mineures jusqu’à sa sortie de la phase bêta.

:::

La classe `AgentController` est un hôte partagé pour une ou plusieurs instances de [`Session`](/reference/agent-controller/session). Initialisez le contrôleur, créez une session, puis utilisez les API `session.*` pour gérer l’état de la conversation et contrôler l’exécution.

Pour une introduction guidée, consultez la [présentation d’AgentController](/docs/harness/agent-controller).

## Exemple d’utilisation {/* #usage-example */}

L’exemple suivant initialise un contrôleur et crée une session. Il s’abonne aux événements de la session avant d’envoyer un message.

```typescript title="src/mastra/agent-controller.ts"
import { Agent } from '@mastra/core/agent'
import { AgentController } from '@mastra/core/agent-controller'
import { Workspace } from '@mastra/core/workspace'

const agent = new Agent({
  id: 'coding-agent',
  name: 'Coding agent',
  instructions: 'Help with software engineering tasks.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
})

const controller = new AgentController({
  id: 'coding-controller',
  agent,
  workspace: new Workspace({ id: 'coding-workspace' }),
  modes: [{ id: 'build', name: 'Build', metadata: { default: true } }],
})

await controller.init()

const session = await controller.createSession({ resourceId: 'project-42' })
const unsubscribe = session.subscribe(event => {
  if (event.type === 'message_update') {
    console.log(event.message)
  }
})

await session.sendMessage({ content: 'Review the project structure.' })
unsubscribe()
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique du contrôleur. Il sert également d’identifiant par défaut pour la session et la ressource.',
  },
  {
    name: 'modes',
    type: 'AgentControllerMode[]',
    description: 'Définitions des modes disponibles pour chaque session. Au moins un mode est requis.',
    properties: [
      {
        type: 'AgentControllerMode',
        parameters: [
          { name: 'id', type: 'string', description: 'Identifiant unique du mode.' },
          { name: 'name', type: 'string', description: 'Nom affiché.', isOptional: true },
          {
            name: 'defaultModelId',
            type: 'string',
            description: 'Modèle sélectionné lorsqu’une session entre dans ce mode sans sélection enregistrée.',
            isOptional: true,
          },
          { name: 'description', type: 'string', description: 'Texte affiché dans les sélecteurs de mode.', isOptional: true },
          {
            name: 'instructions',
            type: 'string',
            description: 'Instructions ajoutées au-dessus de celles de l’Agent sous-jacent pour ce mode.',
            isOptional: true,
          },
          {
            name: 'transitionsTo',
            type: 'string',
            description: 'Mode activé après une suspension `submit_plan` approuvée.',
            isOptional: true,
          },
          {
            name: 'availableTools',
            type: 'string[]',
            description: 'Liste d’autorisation des noms d’outils exposés. Un tableau vide masque tous les outils dans ce mode.',
            isOptional: true,
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            description: 'Métadonnées du mode transmises telles quelles. `metadata.default: true` désigne le mode par défaut.',
            isOptional: true,
          },
          {
            name: 'tools',
            type: 'ToolsInput',
            description: 'Outils du mode. Mutuellement exclusif avec `additionalTools`.',
            isOptional: true,
          },
          {
            name: 'additionalTools',
            type: 'ToolsInput',
            description: 'Outils ajoutés à ceux de l’Agent sous-jacent. Mutuellement exclusif avec `tools`.',
            isOptional: true,
          },
          {
            name: 'agent',
            type: 'Agent',
            description: 'Agent propre au mode, désormais obsolète. Utilisez le paramètre `agent` de premier niveau.',
            isOptional: true,
          },
          {
            name: 'default',
            type: 'boolean',
            description: 'Marqueur par défaut obsolète. Utilisez `metadata.default` ou `defaultModeId`.',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'agent',
    type: 'Agent',
    description: 'Agent sous-jacent partagé utilisé par les modes configurés.',
    isOptional: true,
  },
  {
    name: 'resourceId',
    type: 'string',
    description: 'Identifiant de ressource par défaut pour les sessions et les threads. Sa valeur par défaut est `id`.',
    isOptional: true,
  },
  {
    name: 'storage',
    type: 'MastraCompositeStore',
    description: 'Stockage utilisé pour les threads persistants, les messages, les paramètres et les données d’exécution reprenables.',
    isOptional: true,
  },
  {
    name: 'stateSchema',
    type: 'PublicSchema<TState, any>',
    description: 'Schéma utilisé pour valider les mises à jour de `session.state`.',
    isOptional: true,
  },
  {
    name: 'initialState',
    type: 'Partial<TState>',
    description: 'État initial fusionné avec les valeurs par défaut du schéma pour chaque nouvelle session.',
    isOptional: true,
  },
  {
    name: 'memory',
    type: 'DynamicArgument<MastraMemory>',
    description: 'Instance de mémoire partagée avec les Agents sous-jacents qui ne définissent pas leur propre mémoire.',
    isOptional: true,
  },
  {
    name: 'defaultModeId',
    type: 'string',
    description: 'Identifiant du mode par défaut. Il prévaut sur les métadonnées du mode.',
    isOptional: true,
  },
  {
    name: 'instructions',
    type: 'string',
    description: 'Instructions du contrôleur combinées avec celles du mode actuel.',
    isOptional: true,
  },
  {
    name: 'tools',
    type: 'DynamicArgument<ToolsInput | undefined>',
    description: 'Outils partagés par les exécutions du contrôleur et accessibles aux sous-agents configurés.',
    isOptional: true,
  },
  {
    name: 'workspace',
    type: 'DynamicArgument<Workspace | undefined>',
    description: 'Workspace statique ou fabrique de Workspace par session. Une session doit résoudre un Workspace valide.',
    isOptional: true,
  },
  {
    name: 'browser',
    type: 'DynamicArgument<MastraBrowser | undefined>',
    description: 'Navigateur statique ou fabrique de navigateurs par session.',
    isOptional: true,
  },
  {
    name: 'channels',
    type: 'AgentControllerChannelsConfig',
    description: 'Configuration des canaux de chat utilisée pour acheminer les threads des canaux vers les sessions du contrôleur.',
    isOptional: true,
  },
  {
    name: 'intervalHandlers',
    type: 'IntervalHandler[]',
    description: 'Gestionnaires périodiques démarrés par `init()` et arrêtés par `stopIntervals()` ou `destroy()`.',
    isOptional: true,
  },
  {
    name: 'idGenerator',
    type: '() => string',
    description: 'Générateur d’identifiants personnalisé pour les threads, les messages et les signaux.',
    isOptional: true,
  },
  {
    name: 'modelUseCountProvider',
    type: 'ModelUseCountProvider',
    description: 'Renvoie les nombres d’utilisations des modèles servant à trier les modèles disponibles.',
    isOptional: true,
  },
  {
    name: 'modelUseCountTracker',
    type: 'ModelUseCountTracker',
    description: 'Enregistre une sélection de modèle après `session.model.switch()`.',
    isOptional: true,
  },
  {
    name: 'subagents',
    type: 'AgentControllerSubagent[]',
    description: 'Types de sous-agents exposés par l’outil `subagent` intégré.',
    isOptional: true,
    properties: [
      {
        type: 'AgentControllerSubagent',
        parameters: [
          { name: 'id', type: 'string', description: 'Identifiant unique du type de sous-agent.' },
          { name: 'name', type: 'string', description: 'Nom affiché.' },
          { name: 'description', type: 'string', description: 'Description utilisée par l’outil généré.' },
          { name: 'instructions', type: 'DynamicArgument<AgentInstructions>', description: 'Instructions du sous-agent.' },
          { name: 'tools', type: 'ToolsInput', description: 'Outils appartenant au sous-agent.', isOptional: true },
          {
            name: 'allowedControllerTools',
            type: 'string[]',
            description: 'ID des outils du contrôleur ajoutés aux outils du sous-agent.',
            isOptional: true,
          },
          {
            name: 'allowedWorkspaceTools',
            type: 'string[]',
            description: 'Noms des outils du Workspace visibles par le sous-agent.',
            isOptional: true,
          },
          { name: 'defaultModelId', type: 'string', description: 'Modèle par défaut du sous-agent.', isOptional: true },
          { name: 'maxSteps', type: 'number', description: 'Nombre maximal d’étapes d’exécution.', isOptional: true },
          { name: 'stopWhen', type: 'LoopOptions["stopWhen"]', description: 'Condition d’arrêt de la boucle.', isOptional: true },
          {
            name: 'forked',
            type: 'boolean',
            description: 'Indique si le sous-agent hérite par défaut d’un clone du thread parent.',
            isOptional: true,
            defaultValue: 'false',
          },
        ],
      },
    ],
  },
  {
    name: 'gateways',
    type: 'MastraModelGatewayInterface[]',
    description: 'Passerelles de modèles personnalisées fusionnées avec les passerelles intégrées.',
    isOptional: true,
  },
  {
    name: 'omConfig',
    type: 'AgentControllerOMConfig',
    description: 'Modèles et seuils par défaut de la mémoire observationnelle.',
    isOptional: true,
  },
  {
    name: 'disableBuiltinTools',
    type: 'BuiltinToolId[]',
    description: 'Outils intégrés du contrôleur à exclure des exécutions.',
    isOptional: true,
  },
  {
    name: 'toolCategoryResolver',
    type: '(toolName: string) => ToolCategory | null',
    description: 'Associe les noms d’outils à des catégories d’autorisation.',
    isOptional: true,
  },
  {
    name: 'pubsub',
    type: 'PubSub',
    description: 'Implémentation PubSub propagée aux Agents sous-jacents.',
    isOptional: true,
  },
  {
    name: 'threadLock',
    type: '{ acquire: (threadId: string) => void | Promise<void>; release: (threadId: string) => void | Promise<void> }',
    description: 'Implémentation du verrou utilisée pour coordonner la propriété des threads.',
    isOptional: true,
  },
  {
    name: 'observability',
    type: 'ObservabilityEntrypoint',
    description: 'Configuration de l’observabilité pour une instance Mastra autonome du contrôleur.',
    isOptional: true,
  },
]}
/>

## Propriétés {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant du contrôleur transmis au constructeur.',
  },
]}
/>

## Méthodes {/* #methods */}

### Sessions {/* #sessions */}

#### `createSession(options)` {/* #createsessionoptions */}

Obtient ou crée la session active enregistrée pour la paire `(resourceId, scope)`. Appelez `init()` avant cette méthode.

```typescript
const session = await controller.createSession({
  resourceId: 'project-42',
  scope: 'editor-window-1',
  threadId: 'thread-7',
})
```

Les mêmes `resourceId` et `scope` renvoient la même instance de `Session`. Un scope différent crée une session isolée pour la même ressource. Lorsque `threadId` est fourni, la méthode bascule une session mise en cache vers ce thread ou crée le thread s’il n’existe pas.

<PropertiesTable
  content={[
  {
    name: 'resourceId',
    type: 'string',
    description:
      'Ressource mémoire et clé du registre des sessions actives. Sa valeur par défaut est le `resourceId` configuré ou l’`id` du contrôleur.',
    isOptional: true,
  },
  {
    name: 'scope',
    type: 'string',
    description: 'Espace de noms facultatif du registre qui autorise plusieurs sessions actives pour une même ressource.',
    isOptional: true,
  },
  {
    name: 'threadId',
    type: 'string',
    description: 'Thread précis à associer. Les threads manquants sont créés avec cet identifiant.',
    isOptional: true,
  },
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant stable de la session. Sa valeur par défaut est l’`id` du contrôleur.',
    isOptional: true,
  },
  {
    name: 'ownerId',
    type: 'string',
    description: 'Identifiant stable du propriétaire de la session. Sa valeur par défaut est `id`.',
    isOptional: true,
  },
  {
    name: 'tags',
    type: 'Record<string, string>',
    description: 'Tags copiés dans les threads créés par la session.',
    isOptional: true,
  },
  { name: 'workspace', type: 'Workspace', description: 'Remplacement du Workspace pour cette session.', isOptional: true },
  { name: 'browser', type: 'MastraBrowser', description: 'Remplacement du navigateur pour cette session.', isOptional: true },
  {
    name: 'requestContext',
    type: 'RequestContext',
    description: 'Contexte utilisé pour résoudre les fabriques dynamiques de Workspace et de navigateurs.',
    isOptional: true,
  },
]}
/>

Renvoie : `Promise<Session<TState>>`

#### `getSessionByResource(resourceId, scope?)` {/* #getsessionbyresourceresourceid-scope */}

Renvoie la session active enregistrée pour une ressource et, facultativement, un scope.

```typescript
const session = await controller.getSessionByResource('project-42', 'editor-window-1')
```

Renvoie : `Promise<Session<TState> | undefined>`

#### `setResourceId(session, { resourceId })` {/* #setresourceidsession--resourceid- */}

Déplace une session active vers une autre ressource et supprime son association au thread actif.

```typescript
await controller.setResourceId(session, { resourceId: 'project-43' })
```

#### `getKnownResourceIds(session)` {/* #getknownresourceidssession */}

Répertorie les identifiants de ressources présents dans les threads stockés.

```typescript
const resourceIds = await controller.getKnownResourceIds(session)
```

Renvoie : `Promise<string[]>`

### Cycle de vie {/* #lifecycle */}

#### `init()` {/* #init */}

Initialise le stockage partagé, les services du Workspace et les gestionnaires périodiques configurés. Les appels répétés réutilisent la même promesse d’initialisation.

```typescript
await controller.init()
```

#### `destroy()` {/* #destroy */}

Arrête les gestionnaires périodiques appartenant au contrôleur. Cette opération ne détruit pas les Sessions créées par le contrôleur.

```typescript
await controller.destroy()
```

### Modes et Agents {/* #modes-and-agents */}

#### `listModes()` {/* #listmodes */}

Renvoie les définitions des modes configurés.

```typescript
const modes = controller.listModes()
```

Renvoie : `AgentControllerMode[]`

#### `getCurrentAgent(session)` {/* #getcurrentagentsession */}

Renvoie l’Agent sous-jacent du mode actif de la session.

```typescript
const agent = controller.getCurrentAgent(session)
```

Renvoie : `Agent`

### Workspace et navigateur {/* #workspace-and-browser */}

#### `hasWorkspace()` {/* #hasworkspace */}

Indique si le contrôleur possède une configuration de Workspace statique, dynamique ou fondée sur un objet.

```typescript
if (controller.hasWorkspace()) {
  console.log('Workspace configured')
}
```

Renvoie : `boolean`

#### `isWorkspaceReady()` {/* #isworkspaceready */}

Indique si le Workspace au niveau du contrôleur est prêt.

```typescript
const ready = controller.isWorkspaceReady()
```

Renvoie : `boolean`

#### `getWorkspace()` {/* #getworkspace */}

Renvoie un Workspace statique du contrôleur. Les fabriques dynamiques de Workspace renvoient `undefined` jusqu’à leur résolution.

```typescript
const workspace = controller.getWorkspace()
```

Renvoie : `Workspace | undefined`

#### `resolveWorkspace({ session, requestContext? })` {/* #resolveworkspace-session-requestcontext- */}

Résout un Workspace dynamique pour une session et met le résultat en cache dans le contrôleur.

```typescript
const workspace = await controller.resolveWorkspace({ session, requestContext })
```

Renvoie : `Promise<Workspace | undefined>`

#### `setBrowser(browser)` {/* #setbrowserbrowser */}

Remplace le navigateur du contrôleur et le propage aux Agents sous-jacents.

```typescript
controller.setBrowser(browser)
```

### Mastra et canaux {/* #mastra-and-channels */}

#### `getMastra()` {/* #getmastra */}

Renvoie l’instance Mastra parente ou l’instance interne créée par `init()`.

```typescript
const mastra = controller.getMastra()
```

Renvoie : `Mastra | undefined`

#### `getChannels()` {/* #getchannels */}

Renvoie l’intégration configurée des canaux de chat.

```typescript
const channels = controller.getChannels()
```

Renvoie : `AgentControllerChannels | null`

### Modèles {/* #models */}

#### `getCurrentModelAuthStatus(session)` {/* #getcurrentmodelauthstatussession */}

Renvoie l’état d’authentification du modèle sélectionné pour la session.

```typescript
const status = await controller.getCurrentModelAuthStatus(session)
```

Renvoie : `Promise<ModelAuthStatus>`

#### `listAvailableModels()` {/* #listavailablemodels */}

Répertorie les modèles issus des passerelles configurées et intégrées. Les résultats sont brièvement mis en cache et triés selon les données d’utilisation lorsque `modelUseCountProvider` est configuré.

```typescript
const models = await controller.listAvailableModels()
```

Renvoie : `Promise<AvailableModel[]>`

#### `invalidateAvailableModelsCache()` {/* #invalidateavailablemodelscache */}

Vide le cache des modèles disponibles.

```typescript
controller.invalidateAvailableModelsCache()
```

### Mémoire observationnelle et autorisations {/* #observational-memory-and-permissions */}

#### `loadOMProgress(session)` {/* #loadomprogresssession */}

Charge la progression stockée de la mémoire observationnelle pour le thread actif et émet un événement `om_status`.

```typescript
await controller.loadOMProgress(session)
```

#### `getObservationalMemoryRecord(session)` {/* #getobservationalmemoryrecordsession */}

Renvoie l’enregistrement de mémoire observationnelle du thread actif.

```typescript
const record = await controller.getObservationalMemoryRecord(session)
```

Renvoie : `Promise<ObservationalMemoryRecord | null>`

#### `getToolCategory({ toolName })` {/* #gettoolcategory-toolname- */}

Résout la catégorie d’autorisation d’un outil.

```typescript
const category = controller.getToolCategory({ toolName: 'execute_command' })
```

Renvoie : `ToolCategory | null`

### Intervalles {/* #intervals */}

#### `registerInterval(handler)` {/* #registerintervalhandler */}

Démarre ou remplace un gestionnaire périodique.

```typescript
controller.registerInterval({
  id: 'refresh',
  intervalMs: 60_000,
  handler: async () => refreshData(),
})
```

#### `removeInterval({ id })` {/* #removeinterval-id- */}

Arrête un intervalle et exécute sa fonction de rappel d’arrêt facultative.

```typescript
await controller.removeInterval({ id: 'refresh' })
```

#### `stopIntervals()` {/* #stopintervals */}

Arrête tous les intervalles et exécute leurs fonctions de rappel d’arrêt facultatives.

```typescript
await controller.stopIntervals()
```

## Voir aussi {/* #related */}

- [Guide d’AgentController](/docs/harness/agent-controller)
- [Référence de Session](/reference/agent-controller/session)
- [Agents](/docs/agents/overview)
- [Workspace](/docs/workspace/overview)
- [Canaux](/docs/capabilities/channels/overview)
