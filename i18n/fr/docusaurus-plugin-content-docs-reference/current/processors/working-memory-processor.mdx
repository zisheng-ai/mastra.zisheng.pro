---
title: "Référence : WorkingMemory | Processors"
description: "Documentation du Processor WorkingMemory de Mastra, qui injecte les données persistantes de l'utilisateur ou du contexte sous la forme d'instructions système."
packages:
  - "@mastra/core"
  - "@mastra/pg"
---

# WorkingMemory

`WorkingMemory` est un **Processor d'entrée** qui injecte les données de la mémoire de travail sous la forme d'un message système. Il récupère les informations persistantes dans le stockage et les met en forme comme instructions pour le LLM, ce qui permet à l'Agent de conserver le contexte relatif aux utilisateurs d'une conversation à l'autre.

## Exemple d'utilisation {/* #usage-example */}

```typescript
import { WorkingMemory } from '@mastra/core/processors'

const processor = new WorkingMemory({
  storage: memoryStorage,
  scope: 'resource',
  template: {
    format: 'markdown',
    content: `# User Profile
- **Name**:
- **Preferences**:
- **Goals**:
`,
  },
})
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'Options',
    description: 'Options de configuration du Processor de mémoire de travail',
    isOptional: false,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'storage',
            type: 'MemoryStorage',
            description: 'Instance de stockage permettant de récupérer les données de la mémoire de travail',
            isOptional: false,
          },
          {
            name: 'template',
            type: 'WorkingMemoryTemplate',
            description: 'Modèle qui définit le format et la structure de la mémoire de travail',
            isOptional: true,
            properties: [
              {
                type: 'WorkingMemoryTemplate',
                parameters: [
                  {
                    name: 'format',
                    type: "'markdown' | 'json'",
                    description: 'Format du contenu de la mémoire de travail',
                    isOptional: false,
                  },
                  {
                    name: 'content',
                    type: 'string',
                    description: 'Contenu du modèle qui définit la structure des données de la mémoire de travail',
                    isOptional: false,
                  },
                ],
              },
            ],
          },
          {
            name: 'scope',
            type: "'thread' | 'resource'",
            description:
              "Portée de la mémoire de travail. 'thread' la limite au thread actuel, tandis que 'resource' la partage entre tous les threads de la ressource",
            isOptional: true,
            default: "'resource'",
          },
          {
            name: 'useVNext',
            type: 'boolean',
            description: "Utilise le format d'instructions de nouvelle génération avec des recommandations améliorées",
            isOptional: true,
          },
          {
            name: 'readOnly',
            type: 'boolean',
            description:
              "Lorsque la valeur est true, la mémoire de travail est fournie comme contexte en lecture seule. Les données sont injectées dans la conversation, mais sans le Tool updateWorkingMemory ni les instructions de mise à jour. Utile pour les Agents qui doivent consulter la mémoire de travail sans la modifier.",
            isOptional: true,
            default: 'false',
          },
          {
            name: 'templateProvider',
            type: '{ getWorkingMemoryTemplate(args: { memoryConfig?: MemoryConfig }): Promise<WorkingMemoryTemplate | null> }',
            description: "Provider de modèle dynamique pour la résolution du modèle lors de l'exécution",
            isOptional: true,
          },
          {
            name: 'logger',
            type: 'IMastraLogger',
            description: 'Instance facultative du Logger pour une journalisation structurée',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "Identifiant du Processor défini sur 'working-memory'",
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom d'affichage du Processor défini sur 'WorkingMemory'",
    isOptional: false,
  },
  {
    name: 'defaultWorkingMemoryTemplate',
    type: 'string',
    description: "Modèle Markdown par défaut utilisé lorsqu'aucun modèle personnalisé n'est fourni",
    isOptional: false,
  },
  {
    name: 'processInput',
    type: '(args: { messages: MastraDBMessage[]; messageList: MessageList; abort: (reason?: string) => never; requestContext?: RequestContext }) => Promise<MessageList | MastraDBMessage[]>',
    description: 'Récupère la mémoire de travail et l’ajoute comme message système à la liste des messages',
    isOptional: false,
  },
]}
/>

## Exemple d'utilisation avancée {/* #extended-usage-example */}

```typescript title="src/mastra/agents/personalized-agent.ts"
import { Agent } from '@mastra/core/agent'
import { WorkingMemory, MessageHistory } from '@mastra/core/processors'
import { PostgresStorage } from '@mastra/pg'

const storage = new PostgresStorage({
  connectionString: process.env.DATABASE_URL,
})

export const agent = new Agent({
  id: 'personalized-agent',
  name: 'personalized-agent',
  instructions: 'You are a helpful assistant that remembers user preferences',
  model: '__GATEWAY_OPENAI_MODEL__',
  inputProcessors: [
    new WorkingMemory({
      storage,
      scope: 'resource',
      template: {
        format: 'markdown',
        content: `# User Information
- **Name**:
- **Location**:
- **Preferences**:
- **Communication Style**:
- **Current Projects**:
`,
      },
    }),
    new MessageHistory({ storage, lastMessages: 50 }),
  ],
  outputProcessors: [new MessageHistory({ storage })],
})
```

## Exemple au format JSON {/* #json-format-example */}

```typescript
import { WorkingMemory } from '@mastra/core/processors'

const processor = new WorkingMemory({
  storage: memoryStorage,
  scope: 'resource',
  template: {
    format: 'json',
    content: JSON.stringify({
      user: {
        name: { type: 'string' },
        preferences: { type: 'object' },
        goals: { type: 'array' },
      },
    }),
  },
})
```

## Comportement {/* #behavior */}

### Traitement de l'entrée {/* #input-processing */}

1. Récupère `threadId` et `resourceId` dans le contexte de requête
1. Selon la portée, récupère la mémoire de travail depuis :
   - Les métadonnées du thread (`scope: 'thread'`)
   - L'enregistrement de la ressource (`scope: 'resource'`)
1. Résout le modèle (à partir du Provider, des options ou de la valeur par défaut)
1. Génère les instructions système selon le mode :
   - **Mode normal** : inclut les recommandations pour stocker et mettre à jour les informations, la structure du modèle et les données actuelles
   - **Mode lecture seule** (`readOnly: true`) : inclut uniquement les données actuelles comme contexte, sans instructions de mise à jour
1. Ajoute l'instruction comme message système avec le tag `source: 'memory'`

### Mises à jour de la mémoire de travail {/* #working-memory-updates */}

Les mises à jour de la mémoire de travail s'effectuent au moyen du Tool `updateWorkingMemory` fourni par la classe Memory, et non au moyen de ce Processor. Le Processor se charge uniquement d'injecter dans les conversations l'état actuel de la mémoire de travail.

### Modèle par défaut {/* #default-template */}

Si aucun modèle n'est fourni, le Processor utilise un modèle Markdown par défaut avec des champs pour :

- Prénom, nom
- Lieu, profession
- Centres d'intérêt, objectifs
- Événements, faits, projets

## Voir aussi {/* #related */}

- [Garde-fous](/docs/agents/guardrails)
