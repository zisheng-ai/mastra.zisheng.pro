---
title: "Référence : SystemPromptScrubber | Processors"
description: "Documentation de SystemPromptScrubber dans Mastra, qui détecte et masque les prompts système dans les réponses de l'IA."
packages:
  - "@mastra/core"
---

# SystemPromptScrubber

`SystemPromptScrubber` est un **Processor de sortie** qui détecte et traite les prompts système, les instructions et les autres informations révélatrices susceptibles d'introduire des vulnérabilités de sécurité. Ce Processor contribue à préserver la sécurité en identifiant les types de prompts système et en proposant des stratégies flexibles pour les traiter, dont plusieurs méthodes de masquage qui garantissent un nettoyage approprié des informations sensibles.

## Exemple d'utilisation {/* #usage-example */}

```typescript
import { SystemPromptScrubber } from '@mastra/core/processors'

const processor = new SystemPromptScrubber({
  model: 'openrouter/openai/gpt-oss-safeguard-20b',
  strategy: 'redact',
  redactionMethod: 'mask',
  includeDetections: true,
  lastMessageOnly: true,
})
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'Options',
    description: 'Options de configuration pour la détection et le traitement des prompts système',
    isOptional: false,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'model',
            type: 'MastraModelConfig',
            description: "Configuration du modèle de l'Agent de détection",
            isOptional: false,
          },
          {
            name: 'strategy',
            type: "'block' | 'warn' | 'filter' | 'redact'",
            description:
              "Stratégie appliquée lorsque des prompts système sont détectés : 'block' rejette la réponse avec une erreur, 'warn' consigne un avertissement mais autorise la réponse, 'filter' supprime les messages signalés et 'redact' les remplace par des versions masquées",
            isOptional: true,
            default: "'redact'",
          },
          {
            name: 'customPatterns',
            type: 'string[]',
            description: 'Motifs personnalisés pour détecter les prompts système (chaînes contenant des expressions régulières)',
            isOptional: true,
            default: '[]',
          },
          {
            name: 'includeDetections',
            type: 'boolean',
            description: 'Indique si les avertissements doivent inclure les détails de la détection. Utile pour le débogage et la surveillance',
            isOptional: true,
            default: 'false',
          },
          {
            name: 'lastMessageOnly',
            type: 'boolean',
            description:
              "Indique s'il faut inspecter uniquement le message de sortie le plus récent du lot au lieu de vérifier chaque message. Utilisez cette option pour limiter le nettoyage fondé sur un LLM à la dernière réponse.",
            isOptional: true,
            default: 'false',
          },
          {
            name: 'instructions',
            type: 'string',
            description: "Instructions personnalisées pour l'Agent de détection. Si elles ne sont pas fournies, les instructions par défaut sont utilisées",
            isOptional: true,
            default: 'undefined',
          },
          {
            name: 'redactionMethod',
            type: "'mask' | 'placeholder' | 'remove'",
            description:
              "Méthode de masquage des prompts système : 'mask' les remplace par des astérisques, 'placeholder' les remplace par un texte générique et 'remove' les supprime entièrement",
            isOptional: true,
            default: "'mask'",
          },
          {
            name: 'placeholderText',
            type: 'string',
            description: "Texte générique personnalisé utilisé pour le masquage lorsque redactionMethod vaut 'placeholder'",
            isOptional: true,
            default: "'[SYSTEM_PROMPT]'",
          },
        ],
      },
    ],
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "Identifiant du Processor défini sur 'system-prompt-scrubber'",
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom d'affichage facultatif du Processor",
    isOptional: true,
  },
  {
    name: 'processOutputStream',
    type: '(args: { part: ChunkType; streamParts: ChunkType[]; state: Record<string, any>; abort: (reason?: string) => never; tracingContext?: TracingContext }) => Promise<ChunkType | null>',
    description: 'Traite les parties de sortie en streaming afin de détecter et de gérer les prompts système pendant le streaming',
    isOptional: false,
  },
  {
    name: 'processOutputResult',
    type: '(args: { messages: MastraDBMessage[]; abort: (reason?: string) => never }) => Promise<MastraDBMessage[]>',
    description: 'Traite les résultats de sortie finaux afin de détecter et de gérer les prompts système dans les scénarios sans streaming',
    isOptional: false,
  },
]}
/>

## Exemple d'utilisation avancée {/* #extended-usage-example */}

Lorsque vous utilisez `SystemPromptScrubber` comme Processor de sortie, il est recommandé de l'associer à `BatchPartsProcessor` pour optimiser les performances. `BatchPartsProcessor` regroupe les segments du flux en lots avant de les transmettre au Processor de nettoyage, ce qui réduit le nombre d'appels au LLM nécessaires à la détection.

```typescript title="src/mastra/agents/scrubbed-agent.ts"
import { Agent } from '@mastra/core/agent'
import { BatchPartsProcessor, SystemPromptScrubber } from '@mastra/core/processors'

export const agent = new Agent({
  id: 'scrubbed-agent',
  name: 'scrubbed-agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
  outputProcessors: [
    // Batch stream parts first to reduce LLM calls
    new BatchPartsProcessor({
      batchSize: 10,
    }),
    // Then apply system prompt detection on batched content
    new SystemPromptScrubber({
      model: 'openrouter/openai/gpt-oss-safeguard-20b',
      strategy: 'redact',
      customPatterns: ['system prompt', 'internal instructions'],
      includeDetections: true,
      redactionMethod: 'placeholder',
      placeholderText: '[REDACTED]',
    }),
  ],
})
```

## Voir aussi {/* #related */}

- [Garde-fous](/docs/agents/guardrails)
