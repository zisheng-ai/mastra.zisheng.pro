---
title: "Référence : SemanticRecall | Processors"
description: "Documentation du Processor SemanticRecall de Mastra, qui permet d’effectuer une recherche sémantique dans l’historique des conversations au moyen d’embeddings vectoriels."
packages:
  - "@mastra/core"
  - "@mastra/pg"
---

# SemanticRecall

`SemanticRecall` est un **Processor hybride** qui permet d’effectuer une recherche sémantique dans l’historique des conversations au moyen d’embeddings vectoriels. En entrée, il lance une recherche sémantique afin de trouver les anciens messages pertinents. En sortie, il crée des embeddings pour les nouveaux messages afin de permettre leur récupération sémantique ultérieure.

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { SemanticRecall } from '@mastra/core/processors'
import { openai } from '@ai-sdk/openai'

const processor = new SemanticRecall({
  storage: memoryStorage,
  vector: vectorStore,
  embedder: openai.embedding('text-embedding-3-small'),
  topK: 5,
  messageRange: 2,
  scope: 'resource',
})
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'SemanticRecallOptions',
    description: 'Options de configuration du Processor de rappel sémantique',
    isOptional: false,
    properties: [
      {
        type: 'SemanticRecallOptions',
        parameters: [
          {
            name: 'storage',
            type: 'MemoryStorage',
            description: 'Instance de stockage permettant de récupérer les messages',
            isOptional: false,
          },
          {
            name: 'vector',
            type: 'MastraVector',
            description: 'Stockage vectoriel utilisé pour la recherche sémantique',
            isOptional: false,
          },
          {
            name: 'embedder',
            type: 'MastraEmbeddingModel<string>',
            description: 'Embedder utilisé pour générer les embeddings des requêtes',
            isOptional: false,
          },
          {
            name: 'topK',
            type: 'number',
            description: 'Nombre de messages les plus similaires à récupérer',
            isOptional: true,
            default: '4',
          },
          {
            name: 'messageRange',
            type: 'number | { before: number; after: number }',
            description:
              'Nombre de messages de contexte à inclure avant et après chaque correspondance. Peut être un nombre unique (identique dans les deux directions) ou un objet contenant des valeurs distinctes',
            isOptional: true,
            default: '1',
          },
          {
            name: 'scope',
            type: "'thread' | 'resource'",
            description:
              "Portée de la recherche sémantique. 'thread' recherche uniquement dans le fil actuel. 'resource' recherche dans tous les fils de la ressource",
            isOptional: true,
            default: "'resource'",
          },
          {
            name: 'threshold',
            type: 'number',
            description: 'Seuil minimal du score de similarité (0 à 1). Les messages dont le score est inférieur à ce seuil sont filtrés',
            isOptional: true,
          },
          {
            name: 'indexName',
            type: 'string',
            description: 'Nom de l’index du stockage vectoriel. S’il n’est pas fourni, il est généré automatiquement à partir du modèle de l’embedder',
            isOptional: true,
          },
          {
            name: 'logger',
            type: 'IMastraLogger',
            description: 'Instance facultative du logger pour des logs structurés',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "Identifiant du Processor défini sur 'semantic-recall'",
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom d’affichage du Processor défini sur 'SemanticRecall'",
    isOptional: false,
  },
  {
    name: 'processInput',
    type: '(args: { messages: MastraDBMessage[]; messageList: MessageList; abort: (reason?: string) => never; tracingContext?: TracingContext; requestContext?: RequestContext }) => Promise<MessageList | MastraDBMessage[]>',
    description: 'Effectue une recherche sémantique dans les anciens messages et ajoute le contexte pertinent à la liste des messages',
    isOptional: false,
  },
  {
    name: 'processOutputResult',
    type: '(args: { messages: MastraDBMessage[]; messageList?: MessageList; abort: (reason?: string) => never; tracingContext?: TracingContext; requestContext?: RequestContext }) => Promise<MessageList | MastraDBMessage[]>',
    description: 'Crée des embeddings pour les nouveaux messages afin de permettre de futures recherches sémantiques',
    isOptional: false,
  },
]}
/>

## Exemple d’utilisation avancée {/* #extended-usage-example */}

```typescript title="src/mastra/agents/semantic-memory-agent.ts"
import { Agent } from '@mastra/core/agent'
import { SemanticRecall, MessageHistory } from '@mastra/core/processors'
import { PostgresStorage } from '@mastra/pg'
import { PgVector } from '@mastra/pg'
import { openai } from '@ai-sdk/openai'

const storage = new PostgresStorage({
  id: 'pg-storage',
  connectionString: process.env.DATABASE_URL,
})

const vector = new PgVector({
  id: 'pg-vector',
  connectionString: process.env.DATABASE_URL,
})

const semanticRecall = new SemanticRecall({
  storage,
  vector,
  embedder: openai.embedding('text-embedding-3-small'),
  topK: 5,
  messageRange: { before: 2, after: 1 },
  scope: 'resource',
  threshold: 0.7,
})

export const agent = new Agent({
  id: 'semantic-memory-agent',
  name: 'semantic-memory-agent',
  instructions: 'You are a helpful assistant with semantic memory recall',
  model: '__GATEWAY_OPENAI_MODEL__',
  inputProcessors: [semanticRecall, new MessageHistory({ storage, lastMessages: 50 })],
  outputProcessors: [semanticRecall, new MessageHistory({ storage })],
})
```

## Comportement {/* #behavior */}

### Traitement des entrées {/* #input-processing */}

1. Extrait la requête de l’utilisateur depuis son dernier message
1. Génère des embeddings pour la requête
1. Effectue une recherche vectorielle afin de trouver des messages sémantiquement similaires
1. Récupère les messages correspondants avec leur contexte environnant (selon `messageRange`)
1. Pour `scope: 'resource'`, formate les messages provenant d’autres fils comme un message système horodaté
1. Ajoute les messages rappelés avec le tag `source: 'memory'`

### Traitement des sorties {/* #output-processing */}

1. Extrait le contenu textuel des nouveaux messages de l’utilisateur et de l’assistant
1. Génère des embeddings pour chaque message
1. Stocke les embeddings dans le stockage vectoriel avec leurs métadonnées (ID du message, ID du fil, ID de la ressource, rôle, contenu et horodatage)
1. Utilise un cache LRU pour les embeddings afin d’éviter les appels redondants à l’API

### Rappel entre plusieurs fils {/* #cross-thread-recall */}

Lorsque `scope` vaut `'resource'`, le Processor peut rappeler des messages provenant d’autres fils. Ces messages sont formatés sous forme de message système avec des horodatages et des libellés de conversation, afin de préciser où et quand la conversation a eu lieu.

## Voir aussi {/* #related */}

- [Guardrails](/docs/agents/guardrails)
