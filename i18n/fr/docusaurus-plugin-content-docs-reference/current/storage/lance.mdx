---
title: "Référence : stockage LanceDB | Storage"
description: Documentation de l’implémentation du stockage LanceDB dans Mastra.
packages:
  - "@mastra/core"
  - "@mastra/lance"
---

# Stockage LanceDB

L’implémentation du stockage LanceDB fournit une solution de stockage hautes performances qui utilise le système de base de données LanceDB, particulièrement efficace pour le stockage traditionnel des données comme pour les opérations vectorielles.

:::warning[Observability non prise en charge]

Le stockage LanceDB **ne prend pas en charge le domaine Observability**. Les Traces de `MastraStorageExporter` ne peuvent pas être conservées dans LanceDB, et les fonctionnalités d’Observability de [Studio](/docs/studio/overview) ne fonctionneront pas si LanceDB est votre seul Provider de stockage. Pour activer Observability, utilisez un [stockage composite](/reference/storage/composite#specialized-storage-for-observability) afin d’acheminer les données d’Observability vers un Provider compatible tel que ClickHouse.

:::

## Installation {/* #installation */}

```bash npm2yarn
npm install @mastra/lance@latest
```

## Utilisation {/* #usage */}

### Utilisation de base du stockage {/* #basic-storage-usage */}

```typescript
import { LanceStorage } from '@mastra/lance'

// Connect to a local database
const storage = await LanceStorage.create('my-storage', '/path/to/db')

// Connect to a LanceDB cloud database
const storage = await LanceStorage.create('my-storage', 'db://host:port')

// Connect to a cloud database with custom options
const storage = await LanceStorage.create('my-storage', 's3://bucket/db', {
  storageOptions: { timeout: '60s' },
})
```

## Paramètres {/* #parameters */}

### `LanceStorage.create()` {/* #lancestoragecreate */}

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: 'Nom identifiant l’instance de stockage',
    isOptional: false,
  },
  {
    name: 'uri',
    type: 'string',
    description: 'URI de connexion à la base de données LanceDB. Peut être un chemin local, une URL de base de données cloud ou une URL de bucket S3',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'ConnectionOptions',
    description: 'Options de connexion à LanceDB, telles que le délai maximal, l’authentification, etc.',
    isOptional: true,
  },
]}
/>

## Remarques complémentaires {/* #additional-notes */}

### Gestion des schémas {/* #schema-management */}

L’implémentation LanceStorage gère automatiquement la création et la mise à jour des schémas. Elle associe les types de schéma Mastra aux types de données Apache Arrow utilisés en interne par LanceDB :

- `text`, `uuid` → Utf8
- `int`, `integer` → Int32
- `float` → Float32
- `jsonb`, `json` → Utf8 (serialized)
- `binary` → Binary

### Initialisation {/* #initialization */}

Lorsque vous transmettez le stockage à la classe Mastra, `init()` est appelée automatiquement avant toute opération de stockage :

```typescript
import { Mastra } from '@mastra/core'
import { LanceStorage } from '@mastra/lance'

const storage = await LanceStorage.create('my-storage', '/path/to/db')

const mastra = new Mastra({
  storage, // init() is called automatically
})
```

Si vous utilisez directement le stockage sans Mastra, vous devez appeler explicitement `init()` pour créer les tables :

```typescript
import { LanceStorage } from '@mastra/lance'

const storage = await LanceStorage.create('my-storage', '/path/to/db')

// Required when using storage directly
await storage.init()

// Access domain-specific stores via getStore()
const memoryStore = await storage.getStore('memory')
const thread = await memoryStore?.getThreadById({ threadId: '...' })
```

:::warning
Si `init()` n’est pas appelée, les tables ne seront pas créées et les opérations de stockage échoueront silencieusement ou lèveront des erreurs.
:::

### Options de déploiement {/* #deployment-options */}

Le stockage LanceDB peut être configuré pour différents scénarios de déploiement :

- **Développement local** : utilisez un chemin de fichier local pour le développement et les tests
  ```
  /path/to/db
  ```
- **Déploiement cloud** : connectez-vous à une instance LanceDB hébergée
  ```
  db://host:port
  ```
- **Stockage S3** : utilisez Amazon S3 pour un stockage cloud de grande capacité
  ```
  s3://bucket/db
  ```

### Gestion des tables {/* #table-management */}

LanceStorage fournit des méthodes pour gérer les tables :

- Créer des tables avec des schémas personnalisés
- Supprimer des tables
- Vider des tables en supprimant tous les enregistrements
- Charger des enregistrements par clé
- Insérer un seul enregistrement ou un lot d’enregistrements
