---
title: "Référence : Workflow.sleep() | Workflows"
description: Documentation de la méthode `Workflow.sleep()` des Workflows, qui suspend l’exécution pendant un nombre de millisecondes spécifié.
packages:
  - "@mastra/core"
---

# Workflow\.sleep()

La méthode `.sleep()` suspend l’exécution pendant un nombre de millisecondes spécifié. Elle accepte un nombre statique ou une fonction de rappel pour les délais définis au moment de l’exécution.

## Exemple d’utilisation {/* #usage-example */}

```typescript
workflow.sleep(5000)
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'milliseconds',
    type: 'number | ((context: { inputData: any }) => number | Promise<number>)',
    description: 'Nombre de millisecondes pendant lequel suspendre l’exécution, ou fonction de rappel qui renvoie le délai',
    isOptional: false,
  },
]}
/>

## Valeur renvoyée {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'Workflow',
    description: 'Instance de Workflow pour le chaînage de méthodes',
  },
]}
/>

## Exemple d’utilisation avancé {/* #extended-usage-example */}

```typescript prettier:false
import { createWorkflow, createStep } from "@mastra/core/workflows";

const step1 = createStep({...});
const step2 = createStep({...});

export const testWorkflow = createWorkflow({...})
  .then(step1)
  .sleep(async ({ inputData }) => {
    const { delayInMs } = inputData;
    return delayInMs;
  })
  .then(step2)
  .commit();
```
