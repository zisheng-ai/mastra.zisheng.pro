---
title: "Référence : Workflow.commit() | Workflows"
description: Documentation de the `Workflow.commit()` method in workflows, which finalizes the workflow and returns the final result.
packages:
  - "@mastra/core"
---

# Workflow\.commit()

The `.commit()` method finalizes the workflow and returns the final result.

## Exemple d’utilisation

```typescript
workflow.then(step1).commit()
```

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'Workflow',
    description: 'The workflow instance for method chaining',
  },
]}
/>

## Voir aussi

- [Flux de contrôle](/docs/workflows/control-flow)
