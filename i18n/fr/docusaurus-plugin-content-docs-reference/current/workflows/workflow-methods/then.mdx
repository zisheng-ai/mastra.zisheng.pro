---
title: "Référence : Workflow.then() | Workflows"
description: Documentation de the `Workflow.then()` method in workflows, which creates sequential dependencies between steps.
packages:
  - "@mastra/core"
---

# Workflow\.then()

The `.then()` method creates a sequential dependency between workflow steps, ensuring steps execute in a specific order.

## Exemple d’utilisation

```typescript
workflow.then(step1).then(step2)
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'step',
    type: 'Step',
    description: 'The step instance that should execute after the previous step completes',
    isOptional: false,
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'NewWorkflow',
    description: 'The workflow instance for method chaining',
  },
]}
/>

## Voir aussi

- [Control flow](/docs/workflows/control-flow)
