---
title: "Référence : Workflow.parallel() | Workflows"
description: Documentation de the `Workflow.parallel()` method in workflows, which executes multiple steps in parallel.
packages:
  - "@mastra/core"
---

# Workflow\.parallel()

The `.parallel()` method executes multiple steps in parallel.

## Exemple d’utilisation

```typescript
workflow.parallel([step1, step2])
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'steps',
    type: 'Step[]',
    description: 'The step instances to execute in parallel',
    isOptional: false,
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'Workflow',
    description: 'The workflow instance for method chaining',
  },
]}
/>

## Voir aussi

- [Simultaneous steps with parallel](/docs/workflows/control-flow#simultaneous-steps-with-parallel)
