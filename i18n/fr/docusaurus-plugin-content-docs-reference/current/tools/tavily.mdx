---
title: "Référence : Tools Tavily | Tools et MCP"
description: "Référence de l’API de @mastra/tavily, qui fournit des Tools de recherche Web, d’extraction, d’exploration et de cartographie aux Agents Mastra."
packages:
  - "@mastra/tavily"
---

# Tools Tavily

Le package `@mastra/tavily` encapsule l’API [Tavily](https://app.tavily.com) sous forme de Tools compatibles avec Mastra. Il expose des fonctions de fabrique pour la recherche, l’extraction, l’exploration et la cartographie. Chaque fonction renvoie un Tool créé avec [`createTool()`](./create-tool), accompagné de schémas Zod complets d’entrée et de sortie.

## Installation {/* #installation */}

```sh npm2yarn
npm install @mastra/tavily @tavily/core zod
```

## Démarrage rapide {/* #quick-start */}

Utilisez `createTavilyTools()` pour obtenir les quatre Tools avec une configuration partagée :

```typescript title="src/mastra/tools/index.ts"
import { createTavilyTools } from '@mastra/tavily'

const tools = createTavilyTools()
// Or pass an explicit API key:
// const tools = createTavilyTools({ apiKey: 'tvly-...' })
```

Chaque Tool peut également être créé séparément :

```typescript title="src/mastra/tools/index.ts"
import { createTavilySearchTool, createTavilyExtractTool } from '@mastra/tavily'

const searchTool = createTavilySearchTool()
const extractTool = createTavilyExtractTool({ apiKey: 'tvly-...' })
```

Par défaut, tous les Tools lisent `TAVILY_API_KEY` dans l’environnement. Vous pouvez transmettre explicitement `{ apiKey }` pour remplacer cette valeur.

## Configuration {/* #configuration */}

Toutes les fonctions de fabrique acceptent `TavilyClientOptions` provenant de `@tavily/core` :

<PropertiesTable
  content={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Clé d’API Tavily. Utilise la variable d’environnement `TAVILY_API_KEY` comme valeur de repli.',
    isOptional: true,
  },
  {
    name: 'clientName',
    type: 'string',
    description: 'Chaîne d’attribution envoyée avec chaque requête dans l’en-tête `X-Client-Name`.',
    isOptional: true,
    defaultValue: "'mastra'",
  },
  {
    name: 'apiBaseURL',
    type: 'string',
    description: 'URL de base de l’API Tavily.',
    isOptional: true,
  },
  {
    name: 'proxies',
    type: 'object',
    description: 'Configuration du proxy transmise au client HTTP sous-jacent.',
    isOptional: true,
  },
  {
    name: 'projectId',
    type: 'string',
    description: 'ID du projet Tavily servant à délimiter les requêtes.',
    isOptional: true,
  },
]}
/>

## `createTavilyTools()` {/* #createtavilytools */}

Renvoie un objet contenant les quatre Tools avec une configuration partagée.

```typescript
import { createTavilyTools } from '@mastra/tavily'

const tools = createTavilyTools({ apiKey: 'tvly-...' })
// tools.tavilySearch, tools.tavilyExtract, tools.tavilyCrawl, tools.tavilyMap
```

**Renvoie :** `{ tavilySearch, tavilyExtract, tavilyCrawl, tavilyMap }`

## `createTavilySearchTool()` {/* #createtavilysearchtool */}

Crée un Tool qui effectue des recherches sur le Web avec Tavily. Renvoie des résultats pertinents accompagnés d’extraits de contenu, de réponses facultatives générées par l’IA et d’images.

**ID du Tool :** `tavily-search`

```typescript
import { createTavilySearchTool } from '@mastra/tavily'

const searchTool = createTavilySearchTool()
```

### Entrée {/* #input */}

<PropertiesTable
  content={[
  {
    name: 'query',
    type: 'string',
    description: 'Requête de recherche.',
  },
  {
    name: 'searchDepth',
    type: "'basic' | 'advanced' | 'fast' | 'ultra-fast'",
    description:
      "Profondeur de recherche. Utilisez 'basic' pour des résultats standard, 'advanced' pour des résultats plus complets, ou 'fast'/'ultra-fast' pour une faible latence.",
    isOptional: true,
  },
  {
    name: 'maxResults',
    type: 'number',
    description: 'Nombre maximal de résultats à renvoyer (1 à 20).',
    isOptional: true,
  },
  {
    name: 'includeAnswer',
    type: "boolean | 'basic' | 'advanced'",
    description: 'Inclut un résumé de réponse généré par l’IA.',
    isOptional: true,
  },
  {
    name: 'includeImages',
    type: 'boolean',
    description: 'Inclut dans la réponse les images liées à la requête.',
    isOptional: true,
  },
  {
    name: 'includeImageDescriptions',
    type: 'boolean',
    description: 'Inclut une description des images renvoyées.',
    isOptional: true,
  },
  {
    name: 'includeRawContent',
    type: "false | 'markdown' | 'text'",
    description: "Inclut le contenu HTML nettoyé de chaque résultat. Transmettez false pour le désactiver, ou 'markdown'/'text' pour choisir le format.",
    isOptional: true,
  },
  {
    name: 'includeDomains',
    type: 'string[]',
    description: 'Limite les résultats à ces domaines.',
    isOptional: true,
  },
  {
    name: 'excludeDomains',
    type: 'string[]',
    description: 'Exclut les résultats provenant de ces domaines.',
    isOptional: true,
  },
  {
    name: 'timeRange',
    type: "'day' | 'week' | 'month' | 'year'",
    description: 'Filtre les résultats selon leur récence.',
    isOptional: true,
  },
]}
/>

### Sortie {/* #output */}

<PropertiesTable
  content={[
  {
    name: 'query',
    type: 'string',
    description: 'Requête de recherche d’origine.',
  },
  {
    name: 'answer',
    type: 'string',
    description: 'Résumé de réponse généré par l’IA.',
    isOptional: true,
  },
  {
    name: 'images',
    type: '{ url: string; description?: string }[]',
    description: 'Images associées.',
    isOptional: true,
  },
  {
    name: 'results',
    type: 'SearchResult[]',
    description: 'Tableau des résultats de recherche.',
    properties: [
      {
        type: 'SearchResult',
        parameters: [
          { name: 'title', type: 'string', description: 'Titre du résultat.' },
          { name: 'url', type: 'string', description: 'URL du résultat.' },
          { name: 'content', type: 'string', description: 'Extrait de contenu.' },
          { name: 'score', type: 'number', description: 'Score de pertinence.' },
          { name: 'rawContent', type: 'string', description: 'Contenu de la page entière (si demandé).', isOptional: true },
        ],
      },
    ],
  },
  {
    name: 'responseTime',
    type: 'number',
    description: 'Temps de réponse du serveur, en secondes.',
  },
]}
/>

## `createTavilyExtractTool()` {/* #createtavilyextracttool */}

Crée un Tool qui extrait le contenu d’une ou plusieurs URL. Renvoie le contenu brut des pages au format Markdown ou texte, avec un maximum de 20 URL par requête.

**ID du Tool :** `tavily-extract`

```typescript
import { createTavilyExtractTool } from '@mastra/tavily'

const extractTool = createTavilyExtractTool()
```

### Entrée {/* #input-1 */}

<PropertiesTable
  content={[
  {
    name: 'urls',
    type: 'string[]',
    description: 'URL dont extraire le contenu (1 à 20).',
  },
  {
    name: 'extractDepth',
    type: "'basic' | 'advanced'",
    description: "Profondeur d’extraction. Utilisez 'advanced' pour récupérer les tableaux et le contenu intégré.",
    isOptional: true,
  },
  {
    name: 'query',
    type: 'string',
    description: 'Intention de l’utilisateur servant à reclasser les fragments de contenu extrait selon leur pertinence.',
    isOptional: true,
  },
  {
    name: 'includeImages',
    type: 'boolean',
    description: 'Inclut les images extraites des pages.',
    isOptional: true,
  },
  {
    name: 'format',
    type: "'markdown' | 'text'",
    description: 'Format de sortie du contenu extrait.',
    isOptional: true,
    defaultValue: "'markdown'",
  },
]}
/>

### Sortie {/* #output-1 */}

<PropertiesTable
  content={[
  {
    name: 'results',
    type: 'ExtractResult[]',
    description: 'Pages extraites avec succès.',
    properties: [
      {
        type: 'ExtractResult',
        parameters: [
          { name: 'url', type: 'string', description: 'URL de la page.' },
          { name: 'rawContent', type: 'string', description: 'Contenu extrait de la page.' },
          { name: 'images', type: 'string[]', description: 'URL des images extraites.', isOptional: true },
        ],
      },
    ],
  },
  {
    name: 'failedResults',
    type: 'FailedResult[]',
    description: 'URL dont l’extraction a échoué.',
    properties: [
      {
        type: 'FailedResult',
        parameters: [
          { name: 'url', type: 'string', description: 'URL ayant échoué.' },
          { name: 'error', type: 'string', description: 'Message d’erreur.' },
        ],
      },
    ],
  },
  {
    name: 'responseTime',
    type: 'number',
    description: 'Temps de réponse du serveur, en secondes.',
  },
]}
/>

## `createTavilyCrawlTool()` {/* #createtavilycrawltool */}

Crée un Tool qui explore un site Web à partir d’une URL. Extrait le contenu des pages découvertes avec des contraintes configurables de profondeur, d’étendue et de domaine.

**ID du Tool :** `tavily-crawl`

```typescript
import { createTavilyCrawlTool } from '@mastra/tavily'

const crawlTool = createTavilyCrawlTool()
```

### Entrée {/* #input-2 */}

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: 'URL racine à partir de laquelle commencer l’exploration.',
  },
  {
    name: 'maxDepth',
    type: 'number',
    description: 'Profondeur maximale de l’exploration depuis l’URL de base.',
    isOptional: true,
  },
  {
    name: 'maxBreadth',
    type: 'number',
    description: 'Nombre maximal de liens à suivre par page.',
    isOptional: true,
  },
  {
    name: 'limit',
    type: 'number',
    description: 'Nombre total de pages traitées par l’explorateur avant son arrêt.',
    isOptional: true,
  },
  {
    name: 'instructions',
    type: 'string',
    description: 'Instructions en langage naturel destinées à l’explorateur.',
    isOptional: true,
  },
  {
    name: 'selectPaths',
    type: 'string[]',
    description: 'Expressions régulières permettant de sélectionner des chemins d’URL précis.',
    isOptional: true,
  },
  {
    name: 'selectDomains',
    type: 'string[]',
    description: 'Expressions régulières permettant de limiter l’exploration à certains domaines.',
    isOptional: true,
  },
  {
    name: 'excludePaths',
    type: 'string[]',
    description: 'Expressions régulières permettant d’exclure certains chemins d’URL.',
    isOptional: true,
  },
  {
    name: 'excludeDomains',
    type: 'string[]',
    description: 'Expressions régulières permettant d’exclure certains domaines.',
    isOptional: true,
  },
  {
    name: 'allowExternal',
    type: 'boolean',
    description: 'Indique s’il faut suivre les liens vers des domaines externes.',
    isOptional: true,
  },
  {
    name: 'extractDepth',
    type: "'basic' | 'advanced'",
    description: "Profondeur d’extraction. Utilisez 'advanced' pour récupérer les tableaux et le contenu intégré.",
    isOptional: true,
  },
  {
    name: 'includeImages',
    type: 'boolean',
    description: 'Inclut les images des pages explorées.',
    isOptional: true,
  },
  {
    name: 'format',
    type: "'markdown' | 'text'",
    description: 'Format de sortie du contenu extrait.',
    isOptional: true,
    defaultValue: "'markdown'",
  },
]}
/>

### Sortie {/* #output-2 */}

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: 'URL racine qui a été explorée.',
  },
  {
    name: 'results',
    type: 'CrawlResult[]',
    description: 'Contenu extrait des pages découvertes.',
    properties: [
      {
        type: 'CrawlResult',
        parameters: [
          { name: 'url', type: 'string', description: 'URL de la page.' },
          { name: 'rawContent', type: 'string', description: 'Contenu extrait de la page.' },
          { name: 'images', type: 'string[]', description: 'URL des images trouvées sur la page.', isOptional: true },
        ],
      },
    ],
  },
  {
    name: 'responseTime',
    type: 'number',
    description: 'Temps de réponse du serveur, en secondes.',
  },
]}
/>

## `createTavilyMapTool()` {/* #createtavilymaptool */}

Crée un Tool qui cartographie la structure d’un site Web à partir d’une URL. Découvre et renvoie une liste d’URL sans extraire le contenu des pages. Utilisez-le pour comprendre la structure du site avant une extraction ciblée.

**ID du Tool :** `tavily-map`

```typescript
import { createTavilyMapTool } from '@mastra/tavily'

const mapTool = createTavilyMapTool()
```

### Entrée {/* #input-3 */}

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: 'URL racine à partir de laquelle commencer la cartographie.',
  },
  {
    name: 'maxDepth',
    type: 'number',
    description: 'Profondeur maximale de la cartographie depuis l’URL de base.',
    isOptional: true,
  },
  {
    name: 'maxBreadth',
    type: 'number',
    description: 'Nombre maximal de liens à suivre par page.',
    isOptional: true,
  },
  {
    name: 'limit',
    type: 'number',
    description: 'Nombre total de liens traités par l’outil de cartographie avant son arrêt.',
    isOptional: true,
  },
  {
    name: 'instructions',
    type: 'string',
    description: 'Instructions en langage naturel destinées à l’outil de cartographie.',
    isOptional: true,
  },
  {
    name: 'selectPaths',
    type: 'string[]',
    description: 'Expressions régulières permettant de sélectionner des chemins d’URL précis.',
    isOptional: true,
  },
  {
    name: 'selectDomains',
    type: 'string[]',
    description: 'Expressions régulières permettant de limiter la cartographie à certains domaines.',
    isOptional: true,
  },
  {
    name: 'excludePaths',
    type: 'string[]',
    description: 'Expressions régulières permettant d’exclure certains chemins d’URL.',
    isOptional: true,
  },
  {
    name: 'excludeDomains',
    type: 'string[]',
    description: 'Expressions régulières permettant d’exclure certains domaines.',
    isOptional: true,
  },
  {
    name: 'allowExternal',
    type: 'boolean',
    description: 'Indique s’il faut inclure les liens vers des domaines externes.',
    isOptional: true,
  },
]}
/>

### Sortie {/* #output-3 */}

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: 'URL racine qui a été cartographiée.',
  },
  {
    name: 'results',
    type: 'string[]',
    description: 'URL découvertes.',
  },
  {
    name: 'responseTime',
    type: 'number',
    description: 'Temps de réponse du serveur, en secondes.',
  },
]}
/>

## Exemple d’Agent {/* #agent-example */}

L’exemple suivant présente un Agent de recherche qui combine la recherche et l’extraction :

```typescript title="src/mastra/agents/index.ts"
import { Agent } from '@mastra/core/agent'
import { createTavilySearchTool, createTavilyExtractTool } from '@mastra/tavily'

const agent = new Agent({
  id: 'web-search-agent',
  name: 'Web Search Agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  instructions:
    'You are a web search assistant. Use search tool to find relevant pages, then use extract tool to get full content from the best results.',
  tools: {
    search: createTavilySearchTool(),
    extract: createTavilyExtractTool(),
  },
})
```

## Variables d’environnement {/* #environment-variables */}

| Variable | Description |
| - | - |
| `TAVILY_API_KEY` | Votre clé d’API Tavily. Utilisée par défaut lorsque `apiKey` n’est pas transmis à une fonction de fabrique. |

## Ressources associées {/* #related */}

- [`createTool()`](./create-tool)
- [Documentation de l’API Tavily](https://docs.tavily.com)
