---
title: "Référence : Agent.listTools() | Agents"
description: "Documentation pour the `Agent.listTools()` method dans Mastra agents, which retrieves the tools that the agent peut use."
packages:
  - "@mastra/core"
---

# Agent.listTools()

The `.listTools()` method retrieves the tools configuré pour an agent, resolving them if they're a function. Ces tools extend the agent's capabilities, allowing it à perform specific actions ou access external systems.

## Exemple d’utilisation

```typescript
await agent.listTools()
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ requestContext?: RequestContext }',
    isOptional: true,
    defaultValue: '{}',
    description: 'Facultatif configuration objet containing requête context.',
    properties: [
      {
        type: '{ requestContext?: RequestContext }',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            defaultValue: 'new RequestContext()',
            description: 'Requête Contexte pour dependency injection et contextual information.',
          },
        ],
      },
    ],
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'tools',
    type: 'TTools | Promise<TTools>',
    description:
      'Le Tools configuré pour the agent, either comme a direct objet ou a promise that resolves à the tools.',
  },
]}
/>

## Extended usage example

```typescript
await agent.listTools({
  requestContext: new RequestContext(),
})
```

## Voir aussi

- [Using tools avec agents](/docs/agents/using-tools)
- [Creating tools](/docs/mcp/overview)
