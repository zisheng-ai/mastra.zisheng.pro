---
title: "Référence : Agent.getPar défautGenerateOptionsLegacy() | Agents"
description: "Documentation pour the `Agent.getDefaultGenerateOptionsLegacy()` method dans Mastra agents, which retrieves the par défaut options used pour legacy generation requests."
packages:
  - "@mastra/core"
---

# Agent.getPar défautGenerateOptionsLegacy()

:::warning

**Deprecated**: Ce method est deprecated et only works avec V1 models. For V2 models, utilise the nouveau [`.getDefaultOptions()`](./getDefaultOptions) method instead.

:::

Agents peut be configuré avec par défaut generation options pour controlling modèle behavior, output formatting et Tool et Workflow calls. The `.getDefaultGenerateOptionsLegacy()` method retrieves these defaults, resolving them if they're functions. Ces options apply à tous les `generateLegacy()` calls unless overridden et sont useful pour inspecting an agent’s unknown defaults.

## Exemple d’utilisation

```typescript
await agent.getDefaultGenerateOptionsLegacy()
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ requestContext?: RequestContext }',
    isOptional: true,
    defaultValue: '{}',
    description: 'Facultatif configuration objet containing requête context.',
    properties: [
      {
        type: '{ requestContext?: RequestContext }',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            defaultValue: 'new RequestContext()',
            description: 'Requête Contexte pour dependency injection et contextual information.',
          },
        ],
      },
    ],
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'defaultOptions',
    type: 'AgentGenerateOptions | Promise<AgentGenerateOptions>',
    description:
      'La valeur par défaut generation options configuré pour the agent, either comme a direct objet ou a promise that resolves à the options.',
  },
]}
/>

## Extended usage example

```typescript
await agent.getDefaultGenerateOptionsLegacy({
  requestContext: new RequestContext(),
})
```

## Voir aussi

- [Agent generation](/docs/agents/overview#use-your-agent)
- [Contextee de requête](/docs/server/request-context)
