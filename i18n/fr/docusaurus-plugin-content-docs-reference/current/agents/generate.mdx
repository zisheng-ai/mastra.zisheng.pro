---
title: "Référence : Agent.generate() | Agents"
description: "Documentation de la méthode `Agent.generate()` des agents Mastra, qui permet de générer des réponses sans streaming avec des fonctionnalités avancées."
packages:
  - "@mastra/core"
---

import { MODEL_SETTINGS_OBJECT } from "@site/src/components/ModelSettingsProperties";

# Agent.generate()

La méthode `.generate()` permet à un agent de générer des réponses sans streaming avec des fonctionnalités avancées. Elle accepte des messages et des options de génération facultatives.

## Exemple d’utilisation {/* #usage-example */}

Appelez l’agent avec un message pour générer une réponse :

```ts
const result = await agent.generate('message for agent')
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'string | string[] | CoreMessage[] | AiMessageType[] | UIMessageWithMetadata[]',
    description:
      'Les messages à envoyer à l’agent. Il peut s’agir d’une chaîne unique, d’un tableau de chaînes ou d’objets de message structurés.',
  },
  {
    name: 'options',
    type: 'AgentExecutionOptions<Output, Format>',
    isOptional: true,
    description: 'Configuration facultative du processus de génération.',
    properties: [
      {
        type: 'AgentExecutionOptions<Output, Format>',
        parameters: [
          {
            name: 'maxSteps',
            type: 'number',
            isOptional: true,
            description: 'Nombre maximal d’étapes à exécuter.',
          },
          {
            name: 'stopWhen',
            type: "LoopOptions['stopWhen']",
            isOptional: true,
            description: 'Conditions d’arrêt de l’exécution (par exemple, nombre d’étapes ou limite de tokens).',
          },
          {
            name: 'onIterationComplete',
            type: '(context: IterationCompleteContext) => { continue?: boolean; feedback?: string } | void | Promise<{ continue?: boolean; feedback?: string } | void>',
            isOptional: true,
            description:
              'Fonction de rappel appelée à la fin de chaque itération. Utilisez-la pour suivre la progression, fournir un retour afin de guider l’agent ou arrêter l’exécution de manière anticipée. Elle reçoit le contexte de l’itération, notamment le texte actuel, les appels d’outils et le motif de fin.',
            properties: [
              {
                type: 'IterationCompleteContext',
                parameters: [
                  {
                    name: 'context.iteration',
                    type: 'number',
                    description: 'Numéro de l’itération actuelle (à partir de 1).',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.maxIterations',
                    type: 'number | undefined',
                    description: 'Nombre maximal d’itérations autorisées (s’il est défini).',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.text',
                    type: 'string',
                    description: 'Réponse textuelle de cette itération.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.isFinal',
                    type: 'boolean',
                    description: 'Indique s’il s’agit de la dernière itération.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.finishReason',
                    type: 'string',
                    description: "Motif de fin de cette itération (par exemple, 'stop', 'length' ou 'tool-calls').",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.toolCalls',
                    type: 'ToolCall[]',
                    description: 'Appels d’outils effectués pendant cette itération.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.messages',
                    type: 'MastraDBMessage[]',
                    description: 'Tous les messages accumulés jusqu’ici.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'return.continue',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Définissez cette valeur sur false pour arrêter l’exécution de manière anticipée.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'return.feedback',
                    type: 'string',
                    isOptional: true,
                    description: 'Message de retour destiné à guider la prochaine itération de l’agent.',
                  },
                ],
              },
            ],
          },
          {
            name: 'isTaskComplete',
            type: 'IsTaskCompleteConfig',
            isOptional: true,
            description:
              'Configuration de l’évaluation de l’achèvement de la tâche, qui vérifie si celle-ci est terminée. Utilise les évaluateurs de Mastra pour vérifier automatiquement si la réponse de l’agent satisfait aux critères d’achèvement.',
            properties: [
              {
                type: 'IsTaskCompleteConfig',
                parameters: [
                  {
                    name: 'scorers',
                    type: 'MastraScorer[]',
                    description:
                      'Tableau d’évaluateurs qui mesurent l’achèvement de la tâche. Chaque évaluateur renvoie 0 (échec) ou 1 (réussite).',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'strategy',
                    type: "'all' | 'any'",
                    isOptional: true,
                    defaultValue: "'all'",
                    description:
                      "Stratégie de combinaison des résultats des évaluateurs. 'all' exige la réussite de tous les évaluateurs, tandis que 'any' en exige au moins un.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'onComplete',
                    type: '(result: IsTaskCompleteRunResult) => void | Promise<void>',
                    isOptional: true,
                    description:
                      'Fonction de rappel appelée lorsque la vérification de l’achèvement de la tâche se termine. Elle reçoit le résultat accompagné du score de chaque évaluateur.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'parallel',
                    type: 'boolean',
                    isOptional: true,
                    defaultValue: 'true',
                    description: 'Indique si les évaluateurs doivent être exécutés en parallèle.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'timeout',
                    type: 'number',
                    isOptional: true,
                    defaultValue: '600000',
                    description: 'Durée maximale, en millisecondes, d’attente de la fin de tous les évaluateurs.',
                  },
                ],
              },
            ],
          },
          {
            name: 'delegation',
            type: 'DelegationConfig',
            isOptional: true,
            description:
              'Configuration de la délégation aux sous-agents. Utilisez-la pour contrôler et surveiller la délégation de tâches par l’agent à d’autres agents, notamment pour modifier ou refuser les délégations et fournir un retour afin de guider le superviseur.',
            properties: [
              {
                type: 'DelegationConfig',
                parameters: [
                  {
                    name: 'onDelegationStart',
                    type: '(context: DelegationStartContext) => DelegationStartResult | void | Promise<DelegationStartResult | void>',
                    isOptional: true,
                    description:
                      'Appelée avant la délégation à un sous-agent. Utilisez-la pour modifier les paramètres de délégation, refuser entièrement la délégation ou modifier `context.requestContext` afin d’ajouter des entrées au contexte de requête de l’exécution du sous-agent.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'onDelegationComplete',
                    type: '(context: DelegationCompleteContext) => { feedback?: string } | void | Promise<{ feedback?: string } | void>',
                    isOptional: true,
                    description:
                      'Appelée lorsqu’une délégation à un sous-agent se termine. Le contexte comprend une méthode `bail()` permettant d’arrêter la suite de l’exécution, et vous pouvez renvoyer `{ feedback }` pour guider l’action suivante du superviseur. Le retour est enregistré dans la mémoire du superviseur sous forme de message de l’assistant.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'messageFilter',
                    type: '(context: MessageFilterContext) => MastraDBMessage[] | Promise<MastraDBMessage[]>',
                    isOptional: true,
                    description:
                      'Fonction de rappel appelée avant la délégation à un sous-agent. Utilisez-la pour filtrer les messages transmis au sous-agent.',
                  },
                ],
              },
            ],
          },
          {
            name: 'scorers',
            type: "MastraScorers | Record<string, { scorer: MastraScorer['name']; sampling?: ScoringSamplingConfig }>",
            isOptional: true,
            description: 'Évaluateurs à exécuter sur les résultats de l’exécution.',
            properties: [
              {
                type: "MastraScorers | Record<string, { scorer: MastraScorer['name']; sampling?: ScoringSamplingConfig }>",
                parameters: [
                  {
                    name: 'scorer',
                    type: 'string',
                    isOptional: false,
                    description: 'Nom de l’évaluateur à utiliser.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'sampling',
                    type: 'ScoringSamplingConfig',
                    isOptional: true,
                    description: 'Configuration de l’échantillonnage pour l’évaluateur.',
                    properties: [
                      {
                        type: 'ScoringSamplingConfig',
                        parameters: [
                          {
                            name: 'type',
                            type: "'none' | 'ratio'",
                            isOptional: false,
                            description:
                              "Type de stratégie d’échantillonnage. Utilisez 'none' pour désactiver l’échantillonnage ou 'ratio' pour un échantillonnage fondé sur un pourcentage.",
                          },
                        ],
                      },
                      {
                        parameters: [
                          {
                            name: 'rate',
                            type: 'number',
                            isOptional: true,
                            description: "Taux d’échantillonnage (0-1). Obligatoire lorsque le type est 'ratio'.",
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
          {
            name: 'returnScorerData',
            type: 'boolean',
            isOptional: true,
            description: 'Indique si la réponse doit inclure les données d’évaluation détaillées.',
          },
          {
            name: 'onChunk',
            type: '(chunk: ChunkType) => Promise<void> | void',
            isOptional: true,
            description: 'Fonction de rappel appelée pour chaque fragment pendant la génération.',
          },
          {
            name: 'onError',
            type: '({ error }: { error: Error | string }) => Promise<void> | void',
            isOptional: true,
            description: 'Fonction de rappel appelée lorsqu’une erreur survient pendant la génération.',
          },
          {
            name: 'onAbort',
            type: '(event: any) => Promise<void> | void',
            isOptional: true,
            description: 'Fonction de rappel appelée lorsque la génération est interrompue.',
          },
          {
            name: 'activeTools',
            type: 'Array<keyof ToolSet> | undefined',
            isOptional: true,
            description:
              'Tableau des noms d’outils qui doivent être actifs pendant l’exécution. Si la valeur est undefined, tous les outils disponibles sont actifs.',
          },
          {
            name: 'abortSignal',
            type: 'AbortSignal',
            isOptional: true,
            description:
              'Objet signal qui permet d’interrompre l’exécution de l’agent. Lorsque le signal est interrompu, toutes les opérations en cours prennent fin, y compris les exécutions de sous-agents en cours auxquelles l’agent a délégué des tâches.',
          },
          {
            name: 'prepareStep',
            type: 'PrepareStepFunction',
            isOptional: true,
            description: 'Fonction de rappel appelée avant chaque étape d’une exécution en plusieurs étapes.',
          },
          {
            name: 'requireToolApproval',
            type: 'boolean',
            isOptional: true,
            description:
              "Lorsque cette valeur est true, tous les appels d’outils nécessitent une approbation explicite avant leur exécution. La méthode generate() renvoie `finishReason: 'suspended'` et inclut un `suspendPayload` contenant les détails de l’appel d’outil (`toolCallId`, `toolName`, `args`). Utilisez `approveToolCallGenerate()` ou `declineToolCallGenerate()` pour poursuivre. Consultez [Approbation de l’agent](/docs/agents/agent-approval#tool-approval-with-generate) pour plus de détails.",
          },
          {
            name: 'autoResumeSuspendedTools',
            type: 'boolean',
            isOptional: true,
            description:
              'Lorsque cette valeur est true, reprend automatiquement les outils suspendus quand l’utilisateur envoie un nouveau message dans le même fil. L’agent extrait `resumeData` du message de l’utilisateur en fonction du `resumeSchema` de l’outil. Nécessite la configuration de la mémoire.',
          },
          {
            name: 'toolCallConcurrency',
            type: 'number',
            isOptional: true,
            description:
              'Nombre maximal d’appels d’outils à exécuter simultanément. La valeur par défaut est 1 lorsqu’une approbation peut être nécessaire, et 10 dans les autres cas.',
          },
          {
            name: 'context',
            type: 'ModelMessage[]',
            isOptional: true,
            description: 'Messages de contexte supplémentaires à fournir à l’agent.',
          },
          {
            name: 'structuredOutput',
            type: 'StructuredOutputOptions<S extends ZodTypeAny = ZodTypeAny>',
            isOptional: true,
            description: 'Options permettant d’affiner la génération de votre sortie structurée.',
            properties: [
              {
                type: 'StructuredOutputOptions<S extends ZodTypeAny = ZodTypeAny>',
                parameters: [
                  {
                    name: 'schema',
                    type: 'StandardJSONSchemaV1',
                    isOptional: false,
                    description: 'Schéma JSON standard définissant la structure de sortie attendue.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'model',
                    type: 'MastraLanguageModel',
                    isOptional: true,
                    description:
                      'Modèle de langage à utiliser pour générer la sortie structurée. S’il est fourni, il permet à l’agent de répondre en plusieurs étapes avec des appels d’outils, du texte et une sortie structurée',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'errorStrategy',
                    type: "'strict' | 'warn' | 'fallback'",
                    isOptional: true,
                    description:
                      "Stratégie de gestion des erreurs de validation du schéma. 'strict' lève des erreurs, 'warn' consigne des avertissements et 'fallback' utilise des valeurs de secours.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'fallbackValue',
                    type: '<S extends ZodTypeAny>',
                    isOptional: true,
                    description: "Valeur de secours à utiliser lorsque la validation du schéma échoue et que errorStrategy vaut 'fallback'.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'instructions',
                    type: 'string',
                    isOptional: true,
                    description: 'Instructions supplémentaires pour le modèle de sortie structurée.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'jsonPromptInjection',
                    type: "boolean | 'system' | 'inline' | 'auto'",
                    isOptional: true,
                    description:
                      "Contrôle la manière dont le schéma JSON est transmis au modèle. Définissez cette option sur 'auto' pour utiliser la sortie structurée native lorsqu’elle est prise en charge, et l’injection dans le prompt dans le cas contraire.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'logger',
                    type: 'IMastraLogger',
                    isOptional: true,
                    description: 'Instance facultative de journalisation structurée pendant la génération de la sortie.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'providerOptions',
                    type: 'ProviderOptions',
                    isOptional: true,
                    description:
                      "Options propres au fournisseur transmises à l’agent interne de structuration. Utilisez-les pour contrôler le comportement du modèle, comme l’effort de raisonnement des modèles de réflexion (par exemple, `{ openai: { reasoningEffort: 'low' } }`).",
                  },
                ],
              },
            ],
          },
          {
            name: 'outputProcessors',
            type: 'OutputProcessorOrWorkflow[]',
            isOptional: true,
            description: 'Processeurs de sortie à utiliser pour cette exécution (remplacent ceux par défaut de l’agent).',
          },
          {
            name: 'maxProcessorRetries',
            type: 'number',
            isOptional: true,
            description:
              'Nombre maximal de fois où les processeurs peuvent déclencher une nouvelle tentative pour cette génération. Remplace la valeur maxProcessorRetries par défaut de l’agent.',
          },
          {
            name: 'inputProcessors',
            type: 'InputProcessorOrWorkflow[]',
            isOptional: true,
            description: 'Processeurs d’entrée à utiliser pour cette exécution (remplacent ceux par défaut de l’agent).',
          },
          {
            name: 'instructions',
            type: 'string | string[] | CoreSystemMessage | SystemModelMessage | CoreSystemMessage[] | SystemModelMessage[]',
            isOptional: true,
            description:
              'Instructions personnalisées qui remplacent les instructions par défaut de l’agent pour cette exécution. Il peut s’agir d’une chaîne unique, d’un objet de message ou d’un tableau de l’un ou l’autre.',
          },
          {
            name: 'system',
            type: 'string | string[] | CoreSystemMessage | SystemModelMessage | CoreSystemMessage[] | SystemModelMessage[]',
            isOptional: true,
            description:
              'Messages système personnalisés à inclure dans le prompt. Il peut s’agir d’une chaîne unique, d’un objet de message ou d’un tableau de l’un ou l’autre. Les messages système fournissent du contexte ou des instructions de comportement supplémentaires qui complètent les instructions principales de l’agent.',
          },
          {
            name: 'output',
            type: 'Zod schema | JsonSchema7',
            isOptional: true,
            description:
              '**Obsolète.** Utilisez structuredOutput sans modèle pour obtenir le même résultat. Définit la structure attendue de la sortie. Peut être un objet JSON Schema ou un schéma Zod.',
          },
          {
            name: 'memory',
            type: 'object',
            isOptional: true,
            description: 'Configuration de la mémoire pour la persistance et la récupération des conversations.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'thread',
                    type: 'string | { id: string; metadata?: Record<string, any>, title?: string }',
                    isOptional: false,
                    description:
                      'Identifiant du fil assurant la continuité de la conversation. Peut être un ID sous forme de chaîne ou un objet contenant un ID ainsi que des métadonnées et un titre facultatifs.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'resource',
                    type: 'string',
                    isOptional: false,
                    description: 'Identifiant de ressource servant à organiser les conversations par utilisateur, session ou contexte.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'options',
                    type: 'MemoryConfig',
                    isOptional: true,
                    description:
                      'Options supplémentaires de configuration de la mémoire, notamment lastMessages, readOnly, semanticRecall, workingMemory et filterIncompleteToolCalls.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'onTitleGenerated',
                    type: '(title: string) => void | Promise<void>',
                    isOptional: true,
                    description:
                      'Fonction de rappel déclenchée de manière asynchrone lorsqu’un titre de fil est généré et conservé dans le stockage. La génération du titre s’exécute en arrière-plan et peut se terminer après le retour de `generate()`. Elle n’est déclenchée que lorsque `generateTitle` est activé dans les options de mémoire et que le fil ne possède aucun titre.',
                  },
                ],
              },
            ],
          },
          {
            name: 'onFinish',
            type: "LoopConfig['onFinish']",
            isOptional: true,
            description: 'Fonction de rappel déclenchée à la fin de la génération.',
          },
          {
            name: 'onStepFinish',
            type: "LoopConfig['onStepFinish']",
            isOptional: true,
            description: 'Fonction de rappel déclenchée après chaque étape de génération.',
          },
          {
            name: 'telemetry',
            type: 'TelemetrySettings',
            isOptional: true,
            description: 'Paramètres de collecte de la télémétrie OTLP pendant la génération (hors Tracing).',
            properties: [
              {
                type: 'TelemetrySettings',
                parameters: [
                  {
                    name: 'isEnabled',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Indique si la collecte de télémétrie est activée.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordInputs',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Indique si les données d’entrée doivent être enregistrées dans la télémétrie.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordOutputs',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Indique si les données de sortie doivent être enregistrées dans la télémétrie.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'functionId',
                    type: 'string',
                    isOptional: true,
                    description: 'Identifiant de la fonction en cours d’exécution.',
                  },
                ],
              },
            ],
          },
          MODEL_SETTINGS_OBJECT,
          {
            name: 'toolChoice',
            type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
            isOptional: true,
            description: 'Contrôle la manière dont les outils sont sélectionnés pendant la génération.',
            properties: [
              {
                type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
                parameters: [
                  {
                    name: "'auto'",
                    type: 'string',
                    isOptional: false,
                    description: 'Laisse le modèle décider quand utiliser des outils (par défaut).',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "'none'",
                    type: 'string',
                    isOptional: false,
                    description: 'Désactive complètement l’utilisation des outils.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "'required'",
                    type: 'string',
                    isOptional: false,
                    description: 'Force le modèle à utiliser au moins un outil.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "{ type: 'tool'; toolName: string }",
                    type: 'object',
                    isOptional: false,
                    description: 'Force le modèle à utiliser un outil précis.',
                  },
                ],
              },
            ],
          },
          {
            name: 'toolsets',
            type: 'ToolsetsInput',
            isOptional: true,
            description: 'Ensembles d’outils supplémentaires utilisables pour cette exécution.',
          },
          {
            name: 'clientTools',
            type: 'ToolsInput',
            isOptional: true,
            description: 'Outils côté client disponibles pendant l’exécution.',
          },
          {
            name: 'hooks',
            type: 'ToolHooks',
            isOptional: true,
            description:
              'Hooks propres à l’exécution, exécutés avant et après les appels d’outils. Ils remplacent, pour cette exécution, les hooks correspondants définis au niveau de l’agent. `beforeToolCall` peut renvoyer `{ proceed: false, output }` pour ignorer l’appel d’outil.',
          },
          {
            name: 'savePerStep',
            type: 'boolean',
            isOptional: true,
            description:
              'Enregistre progressivement les messages à la fin de chaque étape de génération (valeur par défaut : false). Désactivé en interne lorsque la mémoire observationnelle est activée.',
          },
          {
            name: 'providerOptions',
            type: 'Record<string, Record<string, JSONValue>>',
            isOptional: true,
            description: 'Options propres au fournisseur transmises au modèle de langage.',
            properties: [
              {
                type: 'Record<string, Record<string, JSONValue>>',
                parameters: [
                  {
                    name: 'openai',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Options propres à OpenAI, telles que reasoningEffort, responseFormat, etc.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'anthropic',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Options propres à Anthropic, telles que maxTokens, etc.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'google',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Options propres à Google.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: '[providerName]',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Toute option propre au fournisseur.',
                  },
                ],
              },
            ],
          },
          {
            name: 'runId',
            type: 'string',
            isOptional: true,
            description: 'Identifiant unique de cette exécution.',
          },
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            description: 'Contexte de requête contenant la configuration et l’état dynamiques.',
          },
          {
            name: 'tracingContext',
            type: 'TracingContext',
            isOptional: true,
            description:
              'Contexte de traçage permettant de créer des spans enfants et d’ajouter des métadonnées. Injecté automatiquement lors de l’utilisation du système de traçage de Mastra.',
            properties: [
              {
                type: 'TracingContext',
                parameters: [
                  {
                    name: 'currentSpan',
                    type: 'Span',
                    isOptional: true,
                    description:
                      'Span actuel permettant de créer des spans enfants et d’ajouter des métadonnées. Utilisez-le pour créer des spans enfants personnalisés ou mettre à jour les attributs des spans pendant l’exécution.',
                  },
                ],
              },
            ],
          },
          {
            name: 'tracingOptions',
            type: 'TracingOptions',
            isOptional: true,
            description: 'Options de configuration du Tracing.',
            properties: [
              {
                type: 'TracingOptions',
                parameters: [
                  {
                    name: 'metadata',
                    type: 'Record<string, any>',
                    isOptional: true,
                    description:
                      'Métadonnées à ajouter au span racine de la trace. Utiles pour ajouter des attributs personnalisés tels que des ID utilisateur, des ID de session ou des indicateurs de fonctionnalité.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'requestContextKeys',
                    type: 'string[]',
                    isOptional: true,
                    description:
                      "Clés RequestContext supplémentaires à extraire comme métadonnées pour cette trace. Prend en charge la notation à points pour les valeurs imbriquées (par exemple, 'user.id').",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'traceId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'ID de trace à utiliser pour cette exécution (1 à 32 caractères hexadécimaux). S’il est fourni, cette trace fera partie de la trace indiquée.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'parentSpanId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'ID du span parent à utiliser pour cette exécution (1 à 16 caractères hexadécimaux). S’il est fourni, le span racine sera créé en tant qu’enfant de ce span.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'tags',
                    type: 'string[]',
                    isOptional: true,
                    description: 'Tags à appliquer à cette trace. Libellés textuels permettant de classer et de filtrer les traces.',
                  },
                ],
              },
            ],
          },
          {
            name: 'versions',
            type: 'VersionOverrides',
            isOptional: true,
            description:
              'Remplacements de version propres à chaque appel pour la délégation aux sous-agents. Ils sont fusionnés par-dessus les versions définies au niveau de l’instance Mastra et propagés automatiquement dans les appels de sous-agents via requestContext. Nécessite le package editor. Consultez [Gestion des versions de l’éditeur](/docs/editor/overview#versioning).',
            properties: [
              {
                type: 'VersionOverrides',
                parameters: [
                  {
                    name: 'agents',
                    type: 'Record<string, VersionSelector>',
                    isOptional: true,
                    description: 'Table de correspondance entre les ID d’agents et leurs sélecteurs de version.',
                    properties: [
                      {
                        type: 'VersionSelector',
                        parameters: [
                          {
                            name: 'versionId',
                            type: 'string',
                            isOptional: true,
                            description: 'Cible une version précise par son ID.',
                          },
                        ],
                      },
                      {
                        parameters: [
                          {
                            name: 'status',
                            type: "'draft' | 'published'",
                            isOptional: true,
                            description: 'Cible la version la plus récente ayant ce statut de publication.',
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
          {
            name: 'includeRawChunks',
            type: 'boolean',
            isOptional: true,
            description: 'Indique si les fragments bruts doivent être inclus dans la sortie du stream. Cette option n’est pas disponible chez tous les fournisseurs de modèles.',
          },
        ],
      },
    ],
  },
]}
/>

## Structure de la réponse {/* #response-structure */}

`Agent.generate()` renvoie les données finales collectées pendant l’exécution. `steps` est un tableau d’objets représentant les étapes. Les tableaux d’outils du résultat, notamment `toolCalls` et `toolResults` au niveau supérieur ainsi que les tableaux imbriqués `step.toolCalls` et `step.toolResults`, utilisent le format de fragments de Mastra.

Cela signifie que les données des outils sont encapsulées dans `payload` :

```ts
const response = await agent.generate('Check the weather in Lagos')

for (const toolCall of response.toolCalls) {
  console.log(toolCall.type) // 'tool-call'
  console.log(toolCall.runId)
  console.log(toolCall.from)
  console.log(toolCall.payload.toolName)
  console.log(toolCall.payload.args)
}

for (const step of response.steps) {
  for (const toolResult of step.toolResults) {
    console.log(toolResult.type) // 'tool-result'
    console.log(toolResult.payload.toolName)
    console.log(toolResult.payload.result)
  }
}
```

Pour la version en streaming de ce même format de fragment, consultez la [référence ChunkType](/reference/streaming/ChunkType).

## Valeurs renvoyées {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: "Awaited<ReturnType<MastraModelOutput<Output>['getFullOutput']>>",
    description:
      'Renvoie la sortie complète du processus de génération, notamment le texte, l’objet (en cas de sortie structurée), les appels d’outils, leurs résultats, les statistiques d’utilisation et les informations sur les étapes.',
  },
  {
    name: 'text',
    type: 'string',
    description: 'Réponse textuelle générée par l’agent.',
  },
  {
    name: 'object',
    type: 'Output | undefined',
    isOptional: true,
    description: 'Objet de sortie structurée si structuredOutput a été fourni, validé par rapport au schéma.',
  },
  {
    name: 'toolCalls',
    type: 'ToolCallChunk[]',
    description: 'Tableau des fragments d’appels d’outils effectués pendant la génération.',
    properties: [
      {
        type: 'ToolCallChunk',
        parameters: [
          {
            name: 'type',
            type: "'tool-call'",
            description: 'Identifiant du type de fragment.',
          },
          {
            name: 'runId',
            type: 'string',
            description: 'Identifiant de l’exécution.',
          },
          {
            name: 'from',
            type: 'ChunkFrom',
            description: 'Source du fragment, par exemple AGENT ou WORKFLOW.',
          },
          {
            name: 'payload',
            type: 'ToolCallPayload',
            description: 'Données de l’appel d’outil.',
            properties: [
              {
                type: 'ToolCallPayload',
                parameters: [
                  {
                    name: 'toolCallId',
                    type: 'string',
                    description: 'Identifiant unique de l’appel d’outil.',
                  },
                  {
                    name: 'toolName',
                    type: 'string',
                    description: 'Nom de l’outil appelé.',
                  },
                  {
                    name: 'args',
                    type: 'Record<string, unknown>',
                    isOptional: true,
                    description: 'Arguments transmis à l’outil.',
                  },
                  {
                    name: 'providerExecuted',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Indique si le fournisseur du modèle a exécuté l’outil directement.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'toolResults',
    type: 'ToolResultChunk[]',
    description: 'Tableau des fragments de résultats issus des exécutions d’outils.',
    properties: [
      {
        type: 'ToolResultChunk',
        parameters: [
          {
            name: 'type',
            type: "'tool-result'",
            description: 'Identifiant du type de fragment.',
          },
          {
            name: 'runId',
            type: 'string',
            description: 'Identifiant de l’exécution.',
          },
          {
            name: 'from',
            type: 'ChunkFrom',
            description: 'Source du fragment, par exemple AGENT ou WORKFLOW.',
          },
          {
            name: 'payload',
            type: 'ToolResultPayload',
            description: 'Données du résultat de l’outil.',
            properties: [
              {
                type: 'ToolResultPayload',
                parameters: [
                  {
                    name: 'toolCallId',
                    type: 'string',
                    description: 'Identifiant unique de l’appel d’outil.',
                  },
                  {
                    name: 'toolName',
                    type: 'string',
                    description: 'Nom de l’outil qui a produit le résultat.',
                  },
                  {
                    name: 'result',
                    type: 'unknown',
                    description: 'Valeur renvoyée par l’outil.',
                  },
                  {
                    name: 'isError',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Indique si l’exécution de l’outil a échoué.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'usage',
    type: 'TokenUsage',
    description: 'Statistiques d’utilisation des tokens pour la génération.',
  },
  {
    name: 'steps',
    type: 'object[]',
    description: 'Tableau des étapes d’exécution, utile pour déboguer les générations en plusieurs étapes.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'text',
            type: 'string',
            description: 'Texte généré pendant cette étape.',
          },
          {
            name: 'toolCalls',
            type: 'ToolCallChunk[]',
            description: 'Appels d’outils émis pendant cette étape.',
          },
          {
            name: 'toolResults',
            type: 'ToolResultChunk[]',
            description: 'Résultats d’outils émis pendant cette étape.',
          },
          {
            name: 'finishReason',
            type: 'string',
            isOptional: true,
            description: 'Motif de fin de cette étape.',
          },
          {
            name: 'usage',
            type: 'LanguageModelUsage',
            description: 'Utilisation des tokens pour cette étape.',
          },
          {
            name: 'request',
            type: '{ body?: unknown }',
            description: 'Métadonnées de la requête pour cette étape.',
          },
          {
            name: 'response',
            type: 'object',
            description: 'Métadonnées de la réponse pour cette étape.',
          },
        ],
      },
    ],
  },
  {
    name: 'finishReason',
    type: 'string',
    description:
      "Motif de fin de la génération. Les valeurs possibles comprennent 'stop' (fin normale), 'tool-calls' (fin avec des appels d’outils), 'suspended' (en attente de l’approbation d’un outil) ou 'error' (une erreur s’est produite).",
  },
  {
    name: 'response',
    type: 'object',
    description: 'Métadonnées de réponse du fournisseur du modèle. Utiles pour accéder aux en-têtes de limite de débit et aux ID de requête.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'id',
            type: 'string',
            isOptional: true,
            description: 'ID de réponse du fournisseur du modèle.',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'timestamp',
            type: 'Date',
            isOptional: true,
            description: 'Horodatage de la génération de la réponse.',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'modelId',
            type: 'string',
            isOptional: true,
            description: 'Identifiant du modèle utilisé pour cette réponse.',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'headers',
            type: 'Record<string, string>',
            isOptional: true,
            description:
              'En-têtes de réponse HTTP du fournisseur du modèle. Contiennent des informations sur les limites de débit (par exemple, `anthropic-ratelimit-requests-remaining` et `x-ratelimit-remaining-tokens`) ainsi que d’autres métadonnées propres au fournisseur.',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'messages',
            type: 'ResponseMessage[]',
            isOptional: true,
            description: 'Messages de réponse au format du modèle.',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'uiMessages',
            type: 'UIMessage[]',
            isOptional: true,
            description: 'Messages de réponse au format de l’interface utilisateur, avec toutes les métadonnées ajoutées par les processeurs de sortie.',
          },
        ],
      },
    ],
  },
  {
    name: 'request',
    type: 'object',
    isOptional: true,
    description: 'Requête envoyée au modèle.',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'body',
            type: 'unknown',
            isOptional: true,
            description: 'Corps de la requête envoyé au fournisseur du modèle.',
          },
        ],
      },
    ],
  },
  {
    name: 'warnings',
    type: 'LanguageModelWarning[]',
    isOptional: true,
    description: 'Tous les avertissements émis par le fournisseur du modèle pendant la génération.',
  },
  {
    name: 'providerMetadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: 'Métadonnées propres au fournisseur renvoyées avec la réponse.',
  },
  {
    name: 'reasoning',
    type: 'ReasoningChunk[]',
    isOptional: true,
    description: 'Détails du raisonnement des modèles qui prennent en charge le raisonnement (par exemple, la série OpenAI o1).',
  },
  {
    name: 'reasoningText',
    type: 'string',
    isOptional: true,
    description: 'Texte de raisonnement combiné provenant des modèles de raisonnement.',
  },
  {
    name: 'sources',
    type: 'SourceChunk[]',
    isOptional: true,
    description: 'Sources citées par le modèle pendant la génération.',
  },
  {
    name: 'files',
    type: 'FileChunk[]',
    isOptional: true,
    description: 'Fichiers générés par le modèle.',
  },
  {
    name: 'suspendPayload',
    type: 'object',
    isOptional: true,
    description:
      "Présent lorsque `finishReason` vaut 'suspended'. Contient les détails de l’appel d’outil nécessaires pour approuver ou refuser l’appel en attente.",
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'toolCallId',
            type: 'string',
            description: 'Identifiant unique de l’appel d’outil en attente.',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'toolName',
            type: 'string',
            description: 'Nom de l’outil qui nécessite une approbation.',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'args',
            type: 'Record<string, any>',
            description: 'Arguments qui seront transmis à l’outil.',
          },
        ],
      },
    ],
  },
  {
    name: 'runId',
    type: 'string',
    isOptional: true,
    description:
      'Identifiant unique de cette exécution. Obligatoire lors de l’appel de `approveToolCallGenerate()` ou `declineToolCallGenerate()` pour reprendre une exécution suspendue.',
  },
  {
    name: 'traceId',
    type: 'string',
    isOptional: true,
    description:
      'ID de trace associé à cette exécution lorsque le Tracing est activé. Utilisez-le pour corréler les journaux et déboguer le flux d’exécution.',
  },
  {
    name: 'spanId',
    type: 'string',
    isOptional: true,
    description:
      'ID du span racine associé à cette exécution lorsque le Tracing est activé. Utilisez-le pour effectuer des recherches et des corrélations au niveau du span.',
  },
  {
    name: 'messages',
    type: 'MastraDBMessage[]',
    description: 'Tous les messages de cette exécution, notamment l’entrée, l’historique de la mémoire et la réponse.',
  },
  {
    name: 'rememberedMessages',
    type: 'MastraDBMessage[]',
    description: 'Uniquement les messages chargés depuis la mémoire (historique de la conversation).',
  },
  {
    name: 'error',
    type: 'Error',
    isOptional: true,
    description: 'Objet d’erreur si la génération a échoué.',
  },
  {
    name: 'tripwire',
    type: 'StepTripwireData',
    isOptional: true,
    description: 'Données du mécanisme de sécurité si le contenu a été bloqué par un processeur.',
  },
  {
    name: 'scoringData',
    type: 'object',
    isOptional: true,
    description: 'Données d’évaluation lorsque `returnScorerData` est activé.',
  },
]}
/>

## Autres exemples {/* #more-examples */}

### Avec les paramètres du modèle {/* #with-model-settings */}

Exemple de limitation des tokens de sortie et de réglage de la température :

```ts
const limitedResult = await agent.generate('Write a short poem about coding', {
  modelSettings: {
    maxOutputTokens: 50,
    temperature: 0.7,
  },
})
```

### Avec la mémoire {/* #with-memory */}

Donnez à votre agent accès à l’historique de la conversation et à sa persistance en configurant les options de mémoire. L’agent peut ainsi mémoriser les interactions précédentes et conserver le contexte d’un message à l’autre.

```ts
const memoryResult = await agent.generate('Remember my favorite color is blue', {
  memory: {
    thread: 'user-123-thread',
    resource: 'user-123',
  },
})
```

### Accéder aux en-têtes de réponse {/* #accessing-response-headers */}

Certains fournisseurs de modèles renvoient des informations utiles dans les en-têtes de réponse, telles que le nombre de tokens restants ou l’état de la limite de débit. Vous pouvez accéder à ces en-têtes depuis l’objet de résultat une fois la génération terminée.

```ts
const result = await agent.generate('Hello!')
const remainingRequests = result.response?.headers?.['anthropic-ratelimit-requests-remaining']
const remainingTokens = result.response?.headers?.['x-ratelimit-remaining-tokens']
console.log(`Remaining requests: ${remainingRequests}, Remaining tokens: ${remainingTokens}`)
```

### Analyser des images {/* #analyzing-images */}

Les agents peuvent analyser et décrire des images en traitant à la fois leur contenu visuel et le texte qu’elles contiennent. Pour activer l’analyse d’images, transmettez un objet avec `type: 'image'` et l’URL de l’image dans le tableau `content`. Vous pouvez associer le contenu de l’image à des prompts textuels pour guider l’analyse de l’agent.

```typescript
const response = await agent.generate([
  {
    role: 'user',
    content: [
      {
        type: 'image',
        image: 'https://placebear.com/cache/395-205.jpg',
        mimeType: 'image/jpeg',
      },
      {
        type: 'text',
        text: 'Describe the image in detail, and extract all the text in the image.',
      },
    ],
  },
])

console.log(response.text)
```

### Utiliser `maxSteps` {/* #using-maxsteps */}

Le paramètre `maxSteps` contrôle le nombre maximal d’appels séquentiels au LLM qu’un agent peut effectuer. Chaque étape génère une réponse et exécute les éventuels appels d’outils avant de traiter le résultat. Limiter le nombre d’étapes aide à éviter les boucles infinies et à réduire la latence. Cela permet également de contrôler l’utilisation des tokens par les agents qui utilisent des outils. La valeur par défaut est 5, mais elle peut être augmentée :

```typescript
const response = await agent.generate('Help me organize my day', {
  maxSteps: 10,
})

console.log(response.text)
```

### Utiliser `onStepFinish` {/* #using-onstepfinish */}

Vous pouvez suivre la progression des opérations en plusieurs étapes à l’aide de la fonction de rappel `onStepFinish`. Cela est utile pour le débogage ou pour communiquer la progression aux utilisateurs.

`onStepFinish` n’est disponible que lors du streaming ou de la génération de texte sans sortie structurée.

```typescript
const response = await agent.generate('Help me organize my day', {
  onStepFinish: ({ text, toolCalls, toolResults, finishReason, usage }) => {
    console.log({ text, toolCalls, toolResults, finishReason, usage })
  },
})
```

### Utiliser `onTitleGenerated` {/* #using-ontitlegenerated */}

Lorsque `generateTitle` est activé dans les options de mémoire, la génération du titre s’exécute de manière asynchrone une fois la réponse terminée. Utilisez `onTitleGenerated` pour réagir lorsque le titre est prêt, par exemple en l’envoyant au client via SSE.

```typescript
const response = await agent.generate('What is quantum computing?', {
  memory: {
    thread: threadId,
    resource: userId,
    onTitleGenerated: title => {
      console.log('Thread title:', title)
    },
  },
})
```
