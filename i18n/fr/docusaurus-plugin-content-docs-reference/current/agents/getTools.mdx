---
title: "Référence : Agent.getTools() | Agents"
description: "Documentation de la méthode `Agent.getTools()` dans les Agents Mastra, qui récupère les Tools que l’Agent peut utiliser."
packages:
  - "@mastra/core"
---

# Agent.getTools()

La méthode `.getTools()` récupère les Tools configurés pour un Agent et les résout s’ils sont définis par une fonction. Ces Tools étendent les capacités de l’Agent, ce qui lui permet d’effectuer des actions spécifiques ou d’accéder à des systèmes externes.

## Exemple d’utilisation

```typescript
await agent.getTools()
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ requestContext?: RequestContext }',
    isOptional: true,
    defaultValue: '{}',
    description: 'Objet de configuration facultatif contenant le contexte d’exécution.',
    properties: [
      {
        type: '{ requestContext?: RequestContext }',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            defaultValue: 'new RequestContext()',
            description: 'Contexte d’exécution pour l’injection de dépendances et les informations contextuelles.',
          },
        ],
      },
    ],
  },
]}
/>

## Valeur renvoyée

<PropertiesTable
  content={[
  {
    name: 'tools',
    type: 'TTools | Promise<TTools>',
    description:
      'Tools configurés pour l’Agent, sous la forme d’un objet direct ou d’une promesse qui se résout en Tools.',
  },
]}
/>

## Exemple d’utilisation étendu

```typescript
await agent.getTools({
  requestContext: new RequestContext(),
})
```
