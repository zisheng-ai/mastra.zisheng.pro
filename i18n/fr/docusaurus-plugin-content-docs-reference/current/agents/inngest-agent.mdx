---
title: "Référence : createInngestAgent() | Agents"
description: "Référence de l'API de createInngestAgent(), qui enveloppe un Agent avec une exécution durable fondée sur Inngest et des streams reprenables."
packages:
  - "@mastra/inngest"
---

# `createInngestAgent()`

`createInngestAgent()` enveloppe un [`Agent`](/reference/agents/agent) existant avec une exécution durable fondée sur [Inngest](https://www.inngest.com/docs). Comme [`createDurableAgent()`](/reference/agents/durable-agent#createdurableagentoptions), il diffuse les événements via [PubSub](/docs/server/pubsub) et prend en charge les streams reprenables, mais exécute la boucle agentique sur le moteur d'exécution d'Inngest plutôt que dans le processus. Utilisez-le lorsqu'une exécution doit survivre au redémarrage des processus ou s'exécuter dans un environnement distribué.

Pour une exécution durable dans le processus, utilisez [`createDurableAgent()`](/reference/agents/durable-agent#createdurableagentoptions). Pour une exécution sans attente sur le moteur de Workflow intégré, utilisez [`createEventedAgent()`](/reference/agents/durable-agent#createeventedagentoptions).

## Exemple d'utilisation {/* #usage-example */}

Configurez le client Inngest, enveloppez un Agent, enregistrez-le auprès de Mastra et exposez l'endpoint de service Inngest :

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { Agent } from '@mastra/core/agent'
import { createInngestAgent, serve as inngestServe } from '@mastra/inngest'
import { Inngest } from 'inngest'

const inngest = new Inngest({ id: 'my-app' })

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
})

const durableAgent = createInngestAgent({ agent, inngest })

export const mastra = new Mastra({
  agents: { myAgent: durableAgent },
  server: {
    apiRoutes: [
      {
        path: '/inngest/api',
        method: 'ALL',
        createHandler: async ({ mastra }) => inngestServe({ mastra, inngest }),
      },
    ],
  },
})
```

Diffusez une réponse et lisez le résultat :

```typescript
const { output, runId, cleanup } = await durableAgent.stream('Hello!')

const text = await output.text

cleanup()
```

## `createInngestAgent(options)` {/* #createinngestagentoptions */}

Enveloppe un `Agent` avec une exécution durable fondée sur Inngest et des streams reprenables.

```typescript
import { createInngestAgent } from '@mastra/inngest'

const durableAgent = createInngestAgent({ agent, inngest })
```

Renvoie : [`InngestAgent`](#inngestagent-interface)

### Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'Agent',
    isOptional: false,
    description:
      "Agent à envelopper avec l'exécution durable Inngest. Les méthodes non implémentées par `InngestAgent`, telles que `listTools()` et `getMemory()`, délèguent à cet Agent via un Proxy.",
  },
  {
    name: 'inngest',
    type: 'Inngest',
    isOptional: false,
    description:
      "Instance du client Inngest. Utilisée pour envoyer les événements de Workflow et, dans le SDK v4, publier les événements du stream en temps réel.",
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: "Remplacement de l'identifiant.",
    defaultValue: 'agent.id',
  },
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: 'Remplacement du nom.',
    defaultValue: 'agent.name',
  },
  {
    name: 'pubsub',
    type: 'PubSub',
    isOptional: true,
    description:
      "Instance PubSub destinée au streaming des événements. L'instance `InngestPubSub` par défaut utilise Inngest Realtime, qui fonctionne entre les processus.",
    defaultValue: 'InngestPubSub',
  },
  {
    name: 'cache',
    type: 'MastraServerCache',
    isOptional: true,
    description:
      "Cache des événements de stream stockés, qui permet de reprendre les streams. Lorsqu'il est fourni, PubSub est automatiquement enveloppé avec `CachingPubSub`. S'il est omis, l'Agent hérite du cache de l'instance Mastra.",
  },
  {
    name: 'mastra',
    type: 'Mastra',
    isOptional: true,
    description: "Instance Mastra destinée à l'Observability. Définie automatiquement lorsque l'Agent est enregistré auprès de Mastra.",
  },
]}
/>

## Interface `InngestAgent` {/* #inngestagent-interface */}

Objet renvoyé par `createInngestAgent()`. Il fournit les méthodes d'exécution durable ci-dessous. Toute propriété ou méthode non définie explicitement, telle que `listTools()` et `getMemory()`, est transmise à l'Agent sous-jacent via un Proxy.

### Propriétés {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "Identifiant de l'Agent.",
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom de l'Agent.",
  },
  {
    name: 'agent',
    type: 'Agent',
    description: 'Agent Mastra sous-jacent.',
  },
  {
    name: 'inngest',
    type: 'Inngest',
    description: 'Client Inngest.',
  },
  {
    name: 'cache',
    type: 'MastraServerCache | undefined',
    description: 'Instance de cache résolue, si les streams reprenables sont activés.',
  },
  {
    name: 'pubsub',
    type: 'PubSub',
    description: 'Instance PubSub utilisée pour le streaming des événements.',
  },
]}
/>

## Méthodes {/* #methods */}

### Exécution {/* #execution */}

#### `stream(messages, options?)` {/* #streammessages-options */}

Diffuse une réponse au moyen du moteur d'exécution durable d'Inngest. Le Workflow est déclenché via un événement Inngest après l'établissement de l'abonnement PubSub.

```typescript
const { output, runId, cleanup } = await durableAgent.stream('Hello!', {
  onChunk: chunk => console.log(chunk),
  onFinish: result => console.log('done', result),
})

const text = await output.text
cleanup()
```

Renvoie : [`Promise<InngestAgentStreamResult>`](#inngestagentstreamresult)

#### `resume(runId, resumeData, options?)` {/* #resumerunid-resumedata-options */}

Reprend une exécution Inngest suspendue, par exemple après l'approbation d'un Tool. Charge le snapshot du Workflow depuis le stockage, recherche l'étape suspendue et envoie un événement de reprise à Inngest.

```typescript
const { output, cleanup } = await durableAgent.resume(
  runId,
  {
    approved: true,
  },
  { threadId: 'thread-1', resourceId: 'user-1' },
)

await output.text
cleanup()
```

Le troisième argument accepte `threadId` et `resourceId` en plus des callbacks du cycle de vie :

<PropertiesTable
  content={[
  {
    name: 'threadId',
    type: 'string',
    isOptional: true,
    description: "Identifiant du fil de discussion à associer à l'exécution reprise.",
  },
  {
    name: 'resourceId',
    type: 'string',
    isOptional: true,
    description: "Identifiant de la ressource à associer à l'exécution reprise.",
  },
  {
    name: 'onChunk',
    type: '(chunk: ChunkType) => void | Promise<void>',
    isOptional: true,
    description: 'Appelé pour chaque fragment diffusé.',
  },
  {
    name: 'onStepFinish',
    type: '(result: AgentStepFinishEventData) => void | Promise<void>',
    isOptional: true,
    description: 'Appelé lorsqu’une étape de la boucle agentique se termine.',
  },
  {
    name: 'onFinish',
    type: '(result: AgentFinishEventData) => void | Promise<void>',
    isOptional: true,
    description: "Appelé lorsque l'exécution se termine.",
  },
  {
    name: 'onError',
    type: '(error: Error) => void | Promise<void>',
    isOptional: true,
    description: "Appelé lorsque l'exécution rencontre une erreur.",
  },
  {
    name: 'onSuspended',
    type: '(data: AgentSuspendedEventData) => void | Promise<void>',
    isOptional: true,
    description: "Appelé lorsque l'exécution est suspendue.",
  },
]}
/>

Renvoie : [`Promise<InngestAgentStreamResult>`](#inngestagentstreamresult)

#### `generate(messages, options?)` {/* #generatemessages-options */}

Exécute une réponse sur le moteur d'exécution durable d'Inngest et la résout en un unique `FullOutput`. Si l'exécution est suspendue, `generate()` est résolue avec `finishReason: 'suspended'`. L'option `runId` est facultative. Si vous l'omettez, `generate()` crée un identifiant d'exécution et le renvoie dans `result.runId`. Poursuivez l'exécution avec [`resumeGenerate()`](#resumegeneraterunid-resumedata-options). Utilisez [`stream()`](#streammessages-options) avec `onSuspended` lorsque l'appelant a besoin d'un callback de suspension.

```typescript
const result = await durableAgent.generate('Delete the old records', {
  requireToolApproval: true,
})

result.runId // Generated automatically
result.finishReason // 'suspended' when approval is required
```

Renvoie : `Promise<FullOutput<TOutput>>`

#### `resumeGenerate(runId, resumeData, options?)` {/* #resumegeneraterunid-resumedata-options */}

Reprend une exécution `generate()` suspendue et la résout en un unique `FullOutput`.

```typescript
if (!result.runId) {
  throw new Error('Run ID is missing')
}

const resumedResult = await durableAgent.resumeGenerate(result.runId, { approved: true })
```

Renvoie : `Promise<FullOutput<TOutput>>`

#### `observe(runId, options?)` {/* #observerunid-options */}

Se reconnecte à une exécution existante, en rejouant les événements mis en cache avant de transmettre les événements en direct. Utilisez cette méthode après une déconnexion réseau. Transmettez `offset` pour commencer la relecture depuis une position connue.

```typescript
const { output, cleanup } = await durableAgent.observe(runId, {
  offset: 0,
  onChunk: chunk => console.log(chunk),
})

await output.text
```

Le résultat de `observe()` n'inclut ni `threadId` ni `resourceId`.

Renvoie : `Promise<Omit<InngestAgentStreamResult, 'threadId' | 'resourceId'>>`

:::warning

Le `cleanup()` renvoyé par `observe()` détruit les entrées du registre et les événements en cache de l'exécution. Appelez-le uniquement lorsque vous avez terminé cette exécution. Si elle est suspendue et que vous comptez la reprendre plus tard, n'appelez pas `cleanup()`.

:::

#### `prepare(messages, options?)` {/* #preparemessages-options */}

Prépare une exécution durable sans la déclencher. Renvoie l'entrée sérialisée du Workflow qui permet de déclencher manuellement l'événement de Workflow Inngest.

```typescript
const { runId, messageId, workflowInput, threadId, resourceId } = await durableAgent.prepare(
  'Summarize the document',
  {
    memory: { threadId: 'thread-1', resourceId: 'user-1' },
  },
)
```

Renvoie :

```typescript
interface PrepareResult {
  runId: string
  messageId: string
  workflowInput: any
  threadId?: string
  resourceId?: string
}
```

### Introspection {/* #introspection */}

#### `isInngestAgent(obj)` {/* #isinngestagentobj */}

Garde de type qui vérifie si un objet est un `InngestAgent`.

```typescript
import { isInngestAgent } from '@mastra/inngest'

if (isInngestAgent(agent)) {
  // agent is InngestAgent
}
```

Renvoie : `boolean`

## Options du stream {/* #stream-options */}

`stream()` accepte un objet `InngestAgentStreamOptions`. Il prend en charge les mêmes options d'exécution d'Agent que [`DurableAgent.stream()`](/reference/agents/durable-agent#stream-options), ainsi que les callbacks du cycle de vie.

<PropertiesTable
  content={[
  {
    name: 'runId',
    type: 'string',
    isOptional: true,
    description: "Identifiant unique de cette exécution. Utilisez-le ensuite avec `resume()` ou `observe()`.",
  },
  {
    name: 'instructions',
    type: "AgentExecutionOptions['instructions']",
    isOptional: true,
    description: "Remplace les instructions par défaut de l'Agent pour cette exécution.",
  },
  {
    name: 'context',
    type: 'ModelMessage[]',
    isOptional: true,
    description: "Messages de contexte supplémentaires à fournir à l'Agent.",
  },
  {
    name: 'memory',
    type: 'object',
    isOptional: true,
    description: 'Configuration de la mémoire pour la persistance et la récupération des conversations.',
  },
  {
    name: 'requestContext',
    type: 'RequestContext',
    isOptional: true,
    description: "Contexte de requête contenant la configuration dynamique et l'état de cette exécution.",
  },
  {
    name: 'maxSteps',
    type: 'number',
    isOptional: true,
    description: "Nombre maximal d'étapes à exécuter.",
  },
  {
    name: 'toolsets',
    type: 'object',
    isOptional: true,
    description: 'Ensembles de Tools supplémentaires disponibles pour cette exécution.',
  },
  {
    name: 'clientTools',
    type: 'object',
    isOptional: true,
    description: "Tools côté client disponibles pendant l'exécution.",
  },
  {
    name: 'toolChoice',
    type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
    isOptional: true,
    description: 'Stratégie de sélection des Tools.',
  },
  {
    name: 'modelSettings',
    type: 'object',
    isOptional: true,
    description: 'Paramètres propres au modèle, tels que la température.',
  },
  {
    name: 'requireToolApproval',
    type: 'boolean',
    isOptional: true,
    description: "Exige l'approbation de tous les appels de Tool, ce qui suspend l'exécution jusqu'à sa reprise.",
  },
  {
    name: 'autoResumeSuspendedTools',
    type: 'boolean',
    isOptional: true,
    description: "Reprend automatiquement les Tools suspendus au lieu d'attendre un appel externe à `resume()`.",
  },
  {
    name: 'toolCallConcurrency',
    type: 'number',
    isOptional: true,
    description: "Nombre maximal d'appels de Tool à exécuter simultanément.",
  },
  {
    name: 'includeRawChunks',
    type: 'boolean',
    isOptional: true,
    description: 'Inclut les fragments bruts du Provider dans la sortie du stream.',
  },
  {
    name: 'maxProcessorRetries',
    type: 'number',
    isOptional: true,
    description: 'Nombre maximal de nouvelles tentatives des Processors par génération.',
  },
  {
    name: 'untilIdle',
    type: 'boolean | { maxIdleMs?: number }',
    isOptional: true,
    description:
      "Lorsque cette option est définie, maintient le stream ouvert pendant les continuations des tâches en arrière-plan jusqu'à ce que l'Agent soit inactif. Transmettez `true` pour le délai d'inactivité par défaut de 5 minutes, ou `{ maxIdleMs }` pour le personnaliser.",
  },
  {
    name: 'onChunk',
    type: '(chunk: ChunkType) => void | Promise<void>',
    isOptional: true,
    description: 'Appelé pour chaque fragment diffusé.',
  },
  {
    name: 'onStepFinish',
    type: '(result: AgentStepFinishEventData) => void | Promise<void>',
    isOptional: true,
    description: 'Appelé lorsqu’une étape de la boucle agentique se termine.',
  },
  {
    name: 'onFinish',
    type: '(result: AgentFinishEventData) => void | Promise<void>',
    isOptional: true,
    description: "Appelé lorsque l'exécution se termine.",
  },
  {
    name: 'onError',
    type: '(error: Error) => void | Promise<void>',
    isOptional: true,
    description: "Appelé lorsque l'exécution rencontre une erreur.",
  },
  {
    name: 'onSuspended',
    type: '(data: AgentSuspendedEventData) => void | Promise<void>',
    isOptional: true,
    description: "Appelé lorsque l'exécution est suspendue, par exemple pour l'approbation d'un Tool.",
  },
]}
/>

`observe()` accepte les callbacks du cycle de vie (`onChunk`, `onStepFinish`, `onFinish`, `onError`, `onSuspended`), ainsi qu'un `offset` contrôlant le point de départ de la relecture.

## `InngestAgentStreamResult` {/* #inngestagentstreamresult */}

Objet renvoyé par `stream()` et `resume()`. La méthode `observe()` renvoie la même forme, mais omet `threadId` et `resourceId`.

```typescript
interface InngestAgentStreamResult<OUTPUT = undefined> {
  output: MastraModelOutput<OUTPUT>
  readonly fullStream: ReadableStream<any>
  runId: string
  threadId?: string
  resourceId?: string
  cleanup: () => void
}
```

<PropertiesTable
  content={[
  {
    name: 'output',
    type: 'MastraModelOutput',
    description: 'Sortie en streaming. Attendez `output.text` pour obtenir le texte complet, ou consommez `output.fullStream`.',
  },
  {
    name: 'fullStream',
    type: 'ReadableStream',
    description: "Stream complet d'événements, qui délègue à `output.fullStream`.",
  },
  {
    name: 'runId',
    type: 'string',
    description: "Identifiant unique de l'exécution. Transmettez-le à `resume()` ou `observe()` pour vous reconnecter.",
  },
  {
    name: 'threadId',
    type: 'string',
    isOptional: true,
    description: "Identifiant du fil de discussion lors de l'utilisation de la mémoire.",
  },
  {
    name: 'resourceId',
    type: 'string',
    isOptional: true,
    description: "Identifiant de la ressource lors de l'utilisation de la mémoire.",
  },
  {
    name: 'cleanup',
    type: '() => void',
    description: "Se désabonne de PubSub et efface les entrées du registre de l'exécution. Appelez cette fonction lorsque vous avez terminé l'exécution.",
  },
]}
/>

## Mise à disposition des fonctions Inngest {/* #serving-inngest-functions */}

Le package `@mastra/inngest` fournit `serve()` et `createServe()` pour enregistrer les fonctions de Workflow Inngest auprès de votre framework HTTP.

### `serve(options)` {/* #serveoptions */}

Met à disposition les Workflows Mastra avec Hono, le framework par défaut. Collecte tous les Workflows Mastra fondés sur Inngest et les enregistre comme fonctions Inngest.

```typescript
import { serve } from '@mastra/inngest'

app.use('/inngest/api', async c => {
  return serve({ mastra, inngest })(c)
})
```

### `createServe(adapter)` {/* #createserveadapter */}

Factory qui accepte n'importe quel adaptateur de service Inngest (`inngest/express`, `inngest/fastify`, `inngest/next`, etc.) et renvoie une fonction de service pour ce framework.

```typescript title="Express"
import { createServe } from '@mastra/inngest'
import { serve } from 'inngest/express'

const serveExpress = createServe(serve)
app.use('/inngest/api', serveExpress({ mastra, inngest }))
```

```typescript title="Next.js"
import { createServe } from '@mastra/inngest'
import { serve } from 'inngest/next'

const serveNext = createServe(serve)
export const { GET, POST, PUT } = serveNext({ mastra, inngest })
```

### Options de service {/* #serve-options */}

<PropertiesTable
  content={[
  {
    name: 'mastra',
    type: 'Mastra',
    isOptional: false,
    description: "Instance Mastra contenant les Agents et Workflows enregistrés.",
  },
  {
    name: 'inngest',
    type: 'Inngest',
    isOptional: false,
    description: 'Instance du client Inngest.',
  },
  {
    name: 'functions',
    type: 'InngestFunction.Like[]',
    isOptional: true,
    description: 'Fonctions Inngest supplémentaires à mettre à disposition avec les Workflows Mastra.',
  },
  {
    name: 'registerOptions',
    type: 'RegisterOptions',
    isOptional: true,
    description: "Options transmises au gestionnaire d'enregistrement Inngest.",
  },
]}
/>

## Voir aussi {/* #related */}

- [Référence de DurableAgent](/reference/agents/durable-agent)
- [Classe Agent](/reference/agents/agent)
- [Guide de déploiement Inngest](/guides/deployment/inngest)
- [PubSub](/docs/server/pubsub)
