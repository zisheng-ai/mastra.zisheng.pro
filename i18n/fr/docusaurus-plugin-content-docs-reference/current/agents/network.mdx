---
title: "Référence : Agent.network() | Agents"
description: "Documentation pour the `Agent.network()` method dans Mastra agents, which enables multi-agent collaboration et routing."
packages:
  - "@mastra/core"
---

import { MODEL_SETTINGS_OBJECT } from "@site/src/components/ModelSettingsProperties";

# Agent.network()

The `.network()` method enables multi-agent collaboration et routing. Ce method accepts messages et facultatif execution options.

:::warning[Deprecated]

The `.network()` primitive possède been deprecated et va be removed dans a future major release. Use [supervisor agents](/docs/capabilities/subagents) avec `agent.stream()` ou `agent.generate()` instead. Consultez the [migration guide](/guides/migrations/network-to-supervisor) à upgrade.

:::

## Exemple d’utilisation

```typescript
import { Agent } from '@mastra/core/agent'
import { agent1, agent2 } from './agents'
import { workflow1 } from './workflows'
import { tool1, tool2 } from './tools'

const agent = new Agent({
  id: 'network-agent',
  name: 'Network Agent',
  instructions: 'You are a network agent that can help users with a variety of tasks.',
  model: '__GATEWAY_OPENAI_MODEL__',
  agents: {
    agent1,
    agent2,
  },
  workflows: {
    workflow1,
  },
  tools: {
    tool1,
    tool2,
  },
})

await agent.network(`
  Find me the weather in Tokyo.
  Based on the weather, plan an activity for me.
`)
```

## Paramètres

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'string | string[] | CoreMessage[] | AiMessageType[] | UIMessageWithMetadata[]',
    description:
      'The messages à envoie à the agent. Can be a unique string, tableau de strings, ou structured message objects.',
  },
  {
    name: 'options',
    type: 'MultiPrimitiveExecutionOptions',
    isOptional: true,
    description: 'Facultatif configuration pour the network process.',
    properties: [
      {
        type: 'MultiPrimitiveExecutionOptions',
        parameters: [
          {
            name: 'maxSteps',
            type: 'number',
            isOptional: true,
            description: 'Maximum nombre de steps à exécute during execution.',
          },
          {
            name: 'abortSignal',
            type: 'AbortSignal',
            isOptional: true,
            description:
              'Signal à abort the network execution. Lorsqu’unborted, the network stops routing, cancels tout in-progress sub-agent, tool, ou Workflow execution, et skips saving partial results à memory.',
          },
          {
            name: 'onAbort',
            type: '(event: { primitiveType: string; primitiveId: string; iteration: nombre }) => void | Promise<void>',
            isOptional: true,
            description:
              'Callback fired when the network est aborted. Receives an event avec the type et ID de the primitive that était executing when the abort occurred.',
          },
          {
            name: 'memory',
            type: 'object',
            isOptional: true,
            description: 'Configuration pour memory. Ce est the preferred way à manage memory.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'thread',
                    type: 'string | { id: string; metadata?: Record<string, any>, title?: chaîne }',
                    isOptional: false,
                    description:
                      'The conversation thread, comme a chaîne ID ou an objet avec an `id` et facultatif `metadata`.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'resource',
                    type: 'string',
                    isOptional: false,
                    description: 'Identifiant pour the utilisateur ou resource associated avec the thread.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'options',
                    type: 'MemoryConfig',
                    isOptional: true,
                    description: 'Configuration pour memory behavior, like message history et semantic recall.',
                  },
                ],
              },
            ],
          },
          {
            name: 'tracingContext',
            type: 'TracingContext',
            isOptional: true,
            description:
              "Tracing context pour creating child spans et adding metadata. Automatically injected when using Mastra's tracing system.",
            properties: [
              {
                type: 'TracingContext',
                parameters: [
                  {
                    name: 'currentSpan',
                    type: 'Span',
                    isOptional: true,
                    description:
                      'Current span pour creating child spans et adding metadata. Utilisez ce à crée personnalisé child spans ou met à jour span attributes during execution.',
                  },
                ],
              },
            ],
          },
          {
            name: 'tracingOptions',
            type: 'TracingOptions',
            isOptional: true,
            description: 'Options pour Tracing configuration.',
            properties: [
              {
                type: 'TracingOptions',
                parameters: [
                  {
                    name: 'metadata',
                    type: 'Record<string, any>',
                    isOptional: true,
                    description:
                      'Metadata à ajoute à the root trace span. Useful pour adding personnalisé attributes like utilisateur IDs, session IDs, ou feature flags.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'requestContextKeys',
                    type: 'string[]',
                    isOptional: true,
                    description:
                      "Supplémentaire RequêteContexte keys à extract comme metadata pour this trace. Prend en charge dot notation pour nested values (e.g., 'user.id').",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'traceId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'Trace ID à utilise pour this execution (1-32 hexadecimal characters). If provided, this trace va be part de the specified trace.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'parentSpanId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'Parent span ID à utilise pour this execution (1-16 hexadecimal characters). If provided, the root span va be created comme a child de this span.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'tags',
                    type: 'string[]',
                    isOptional: true,
                    description: 'Tags à apply à this trace. String labels pour categorizing et filtering traces.',
                  },
                ],
              },
            ],
          },
          {
            name: 'telemetry',
            type: 'TelemetrySettings',
            isOptional: true,
            description: 'Settings pour OTLP telemetry collection during streaming (not Tracing).',
            properties: [
              {
                type: 'TelemetrySettings',
                parameters: [
                  {
                    name: 'isEnabled',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Actifr ou désactive telemetry. Désactivé par par défaut tandis que experimental.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordInputs',
                    type: 'boolean',
                    isOptional: true,
                    description:
                      'Actifr ou désactive input recording. Activé par default. You might want à désactive input recording à avoid recording sensitive information.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordOutputs',
                    type: 'boolean',
                    isOptional: true,
                    description:
                      'Actifr ou désactive output recording. Activé par default. You might want à désactive output recording à avoid recording sensitive information.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'functionId',
                    type: 'string',
                    isOptional: true,
                    description: 'Identifiant pour this function. Used à group telemetry données par function.',
                  },
                ],
              },
            ],
          },
          MODEL_SETTINGS_OBJECT,
          {
            name: 'structuredOutput',
            type: 'StructuredOutputOptions',
            isOptional: true,
            description: 'Configuration pour generating a typed structured output depuis the network result.',
            properties: [
              {
                type: 'StructuredOutputOptions',
                parameters: [
                  {
                    name: 'schema',
                    type: 'ZodSchema | JSONSchema7',
                    isOptional: false,
                    description: 'The schema à validate the output against. Can be a Zod schema ou JSON Schema.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'model',
                    type: 'MastraModelConfig',
                    isOptional: true,
                    description: "Modèle à utilise pour generating the structured output. Par défauts à the agent's model.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'instructions',
                    type: 'string',
                    isOptional: true,
                    description: 'Personnalisé instructions pour generating the structured output.',
                  },
                ],
              },
            ],
          },
          {
            name: 'runId',
            type: 'string',
            isOptional: true,
            description: 'Unique ID pour this generation run. Useful pour tracking et debugging purposes.',
          },
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            description: 'Requête Contexte pour dependency injection et contextual information.',
          },
          {
            name: 'traceId',
            type: 'string',
            isOptional: true,
            description:
              'The trace ID associated avec this execution when Tracing est enabled. Utilisez ce à correlate logs et debug execution flow.',
          },
          {
            name: 'spanId',
            type: 'string',
            isOptional: true,
            description:
              'The root span ID associated avec this execution when Tracing est enabled. Utilisez ce pour span-level lookup et correlation.',
          },
          {
            name: 'onStepFinish',
            type: '(event: any) => Promise<void> | void',
            isOptional: true,
            description:
              'Callback fired après chaque LLM step within a sub-agent execution. Receives step details including finish reason et token usage.',
          },
          {
            name: 'onError',
            type: '({ error }: { error: Error | chaîne }) => Promise<void> | void',
            isOptional: true,
            description: 'Callback fired when an error occurs during sub-agent execution.',
          },
        ],
      },
    ],
  },
]}
/>

## Renvoie

<PropertiesTable
  content={[
  {
    name: 'stream',
    type: 'MastraAgentNetworkStream<NetworkChunkType>',
    description:
      'Un personnalisé stream that extends ReadableFlux<NetworkChunkType> avec additional network-specific properties',
  },
  {
    name: 'status',
    type: 'Promise<RunStatus>',
    description: 'Un promise that resolves à the actuel Workflow exécute status',
  },
  {
    name: 'result',
    type: 'Promise<WorkflowResult<TState, TOutput, TSteps>>',
    description: 'Un promise that resolves à the final Workflow result',
  },
  {
    name: 'usage',
    type: 'Promise<{ promptTokens: number; completionTokens: number; totalTokens: nombre }>',
    description: 'Un promise that resolves à token usage statistics',
  },
  {
    name: 'object',
    type: 'Promise<OUTPUT | undefined>',
    description:
      'Un promise that resolves à the structured output object. Only disponible when structuredSortie option est provided. Resolves à undefined if aucun schema était specified.',
  },
  {
    name: 'objectStream',
    type: 'ReadableStream<Partial<OUTPUT>>',
    description:
      "Un stream de partial objects during structured output generation. Useful pour streaming partial results comme they're being generated.",
  },
]}
/>

## Structured output

Lorsque vous need typed, validated results depuis your network, utilise the `structuredOutput` option. The network va generate a réponse matching your schema après task completion.

```typescript
import { z } from 'zod'

const resultSchema = z.object({
  summary: z.string().describe('A brief summary of the findings'),
  recommendations: z.array(z.string()).describe('List of recommendations'),
  confidence: z.number().min(0).max(1).describe('Confidence score'),
})

const stream = await agent.network('Research AI trends and summarize', {
  structuredOutput: {
    schema: resultSchema,
  },
})

// Consume the stream
for await (const chunk of stream) {
  // Handle streaming events
}

// Get the typed result
const result = await stream.object
// result is typed as { summary: string; recommendations: string[]; confidence: number }
console.log(result?.summary)
console.log(result?.recommendations)
```

### Fluxing Partial Objects

Vous pouvez également stream partial objects comme they're being generated:

```typescript
const stream = await agent.network('Analyze data', {
  structuredOutput: { schema: resultSchema },
})

// Stream partial objects
for await (const partial of stream.objectStream) {
  console.log('Partial result:', partial)
}

// Get final result
const final = await stream.object
```

### Chunk Types

Lorsque vous utilisez structured output, additional chunk types sont emitted:

- `network-object`: Emitted avec partial objects during streaming
- `network-object-result`: Emitted avec the final structured object

## Aborting a network

Use `abortSignal` à cancel a running network. Lorsqu’unborted, the network stops routing, cancels tout in-progress sub-agent, tool, ou Workflow execution, et doesn't enregistre partial results à memory.

```typescript
const controller = new AbortController()

// Abort after 30 seconds
setTimeout(() => controller.abort(), 30_000)

const stream = await agent.network('Research this topic thoroughly', {
  abortSignal: controller.signal,
  onAbort: ({ primitiveType, primitiveId, iteration }) => {
    console.log(`Aborted ${primitiveType} "${primitiveId}" at iteration ${iteration}`)
  },
})

for await (const chunk of stream) {
  if (
    chunk.type === 'routing-agent-abort' ||
    chunk.type === 'agent-execution-abort' ||
    chunk.type === 'tool-execution-abort' ||
    chunk.type === 'workflow-execution-abort'
  ) {
    console.log('Network was aborted')
  }
}
```
