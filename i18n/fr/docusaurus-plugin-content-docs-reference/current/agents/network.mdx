---
title: "Référence : Agent.network() | Agents"
description: "Documentation de la méthode `Agent.network()` des Agents Mastra, qui permet la collaboration et le routage multi-Agents."
packages:
  - "@mastra/core"
---

import { MODEL_SETTINGS_OBJECT } from "@site/src/components/ModelSettingsProperties";

# Agent.network()

La méthode `.network()` permet la collaboration et le routage multi-Agents. Elle accepte des messages et des options d’exécution facultatives.

:::warning[Obsolète]

La primitive `.network()` est obsolète et sera supprimée dans une prochaine version majeure. Utilisez plutôt des [Agents superviseurs](/docs/capabilities/subagents) avec `agent.stream()` ou `agent.generate()`. Consultez le [guide de migration](/guides/migrations/network-to-supervisor) pour effectuer la mise à niveau.

:::

## Exemple d’utilisation {/* #usage-example */}

```typescript
import { Agent } from '@mastra/core/agent'
import { agent1, agent2 } from './agents'
import { workflow1 } from './workflows'
import { tool1, tool2 } from './tools'

const agent = new Agent({
  id: 'network-agent',
  name: 'Network Agent',
  instructions: 'You are a network agent that can help users with a variety of tasks.',
  model: '__GATEWAY_OPENAI_MODEL__',
  agents: {
    agent1,
    agent2,
  },
  workflows: {
    workflow1,
  },
  tools: {
    tool1,
    tool2,
  },
})

await agent.network(`
  Find me the weather in Tokyo.
  Based on the weather, plan an activity for me.
`)
```

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'string | string[] | CoreMessage[] | AiMessageType[] | UIMessageWithMetadata[]',
    description:
      'Messages à envoyer à l’Agent. Il peut s’agir d’une chaîne, d’un tableau de chaînes ou d’objets de message structurés.',
  },
  {
    name: 'options',
    type: 'MultiPrimitiveExecutionOptions',
    isOptional: true,
    description: 'Configuration facultative du processus réseau.',
    properties: [
      {
        type: 'MultiPrimitiveExecutionOptions',
        parameters: [
          {
            name: 'maxSteps',
            type: 'number',
            isOptional: true,
            description: 'Nombre maximal d’étapes à exécuter.',
          },
          {
            name: 'abortSignal',
            type: 'AbortSignal',
            isOptional: true,
            description:
              'Signal permettant d’interrompre l’exécution du réseau. Lors de l’interruption, le réseau arrête le routage, annule toute exécution en cours de sous-Agent, de Tool ou de Workflow et n’enregistre pas les résultats partiels dans la Memory.',
          },
          {
            name: 'onAbort',
            type: '(event: { primitiveType: string; primitiveId: string; iteration: number }) => void | Promise<void>',
            isOptional: true,
            description:
              'Callback déclenché lorsque le réseau est interrompu. Reçoit un événement contenant le type et l’ID de la primitive en cours d’exécution au moment de l’interruption.',
          },
          {
            name: 'memory',
            type: 'object',
            isOptional: true,
            description: 'Configuration de la Memory. Il s’agit de la méthode recommandée pour la gérer.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'thread',
                    type: 'string | { id: string; metadata?: Record<string, any>, title?: string }',
                    isOptional: false,
                    description:
                      'Fil de conversation, sous forme d’ID de chaîne ou d’objet contenant un `id` et des `metadata` facultatives.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'resource',
                    type: 'string',
                    isOptional: false,
                    description: 'Identifiant de l’utilisateur ou de la ressource associé au fil.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'options',
                    type: 'MemoryConfig',
                    isOptional: true,
                    description: 'Configuration du comportement de la Memory, notamment l’historique des messages et le rappel sémantique.',
                  },
                ],
              },
            ],
          },
          {
            name: 'tracingContext',
            type: 'TracingContext',
            isOptional: true,
            description:
              "Contexte de traçage permettant de créer des spans enfants et d’ajouter des métadonnées. Injecté automatiquement lors de l’utilisation du système de traçage de Mastra.",
            properties: [
              {
                type: 'TracingContext',
                parameters: [
                  {
                    name: 'currentSpan',
                    type: 'Span',
                    isOptional: true,
                    description:
                      'Span actuel permettant de créer des spans enfants et d’ajouter des métadonnées. Utilisez-le pour créer des spans enfants personnalisés ou mettre à jour leurs attributs pendant l’exécution.',
                  },
                ],
              },
            ],
          },
          {
            name: 'tracingOptions',
            type: 'TracingOptions',
            isOptional: true,
            description: 'Options de configuration du traçage.',
            properties: [
              {
                type: 'TracingOptions',
                parameters: [
                  {
                    name: 'metadata',
                    type: 'Record<string, any>',
                    isOptional: true,
                    description:
                      'Métadonnées à ajouter au span racine de la trace. Utiles pour ajouter des attributs personnalisés tels que des ID utilisateur, des ID de session ou des indicateurs de fonctionnalité.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'requestContextKeys',
                    type: 'string[]',
                    isOptional: true,
                    description:
                      "Clés RequestContext supplémentaires à extraire comme métadonnées de cette trace. Prend en charge la notation par points pour les valeurs imbriquées (par exemple 'user.id').",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'traceId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'ID de trace à utiliser pour cette exécution (1 à 32 caractères hexadécimaux). S’il est fourni, cette trace fera partie de la trace indiquée.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'parentSpanId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'ID du span parent à utiliser pour cette exécution (1 à 16 caractères hexadécimaux). S’il est fourni, le span racine sera créé comme enfant de ce span.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'tags',
                    type: 'string[]',
                    isOptional: true,
                    description: 'Tags à appliquer à cette trace. Libellés de chaîne permettant de catégoriser et de filtrer les traces.',
                  },
                ],
              },
            ],
          },
          {
            name: 'telemetry',
            type: 'TelemetrySettings',
            isOptional: true,
            description: 'Paramètres de collecte de la télémétrie OTLP pendant le streaming (et non du traçage).',
            properties: [
              {
                type: 'TelemetrySettings',
                parameters: [
                  {
                    name: 'isEnabled',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Active ou désactive la télémétrie. Désactivée par défaut pendant la phase expérimentale.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordInputs',
                    type: 'boolean',
                    isOptional: true,
                    description:
                      'Active ou désactive l’enregistrement des entrées. Activé par défaut. Vous pouvez le désactiver afin de ne pas enregistrer d’informations sensibles.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordOutputs',
                    type: 'boolean',
                    isOptional: true,
                    description:
                      'Active ou désactive l’enregistrement des sorties. Activé par défaut. Vous pouvez le désactiver afin de ne pas enregistrer d’informations sensibles.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'functionId',
                    type: 'string',
                    isOptional: true,
                    description: 'Identifiant de cette fonction. Sert à regrouper les données de télémétrie par fonction.',
                  },
                ],
              },
            ],
          },
          MODEL_SETTINGS_OBJECT,
          {
            name: 'structuredOutput',
            type: 'StructuredOutputOptions',
            isOptional: true,
            description: 'Configuration permettant de générer une sortie structurée typée à partir du résultat du réseau.',
            properties: [
              {
                type: 'StructuredOutputOptions',
                parameters: [
                  {
                    name: 'schema',
                    type: 'ZodSchema | JSONSchema7',
                    isOptional: false,
                    description: 'Schéma par rapport auquel valider la sortie. Peut être un schéma Zod ou JSON Schema.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'model',
                    type: 'MastraModelConfig',
                    isOptional: true,
                    description: "Modèle à utiliser pour générer la sortie structurée. Utilise par défaut le modèle de l’Agent.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'instructions',
                    type: 'string',
                    isOptional: true,
                    description: 'Instructions personnalisées pour générer la sortie structurée.',
                  },
                ],
              },
            ],
          },
          {
            name: 'runId',
            type: 'string',
            isOptional: true,
            description: 'ID unique de cette exécution de génération. Utile pour le suivi et le débogage.',
          },
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            description: 'Request Context destiné à l’injection de dépendances et aux informations contextuelles.',
          },
          {
            name: 'traceId',
            type: 'string',
            isOptional: true,
            description:
              'ID de trace associé à cette exécution lorsque le traçage est activé. Utilisez-le pour corréler les logs et déboguer le flux d’exécution.',
          },
          {
            name: 'spanId',
            type: 'string',
            isOptional: true,
            description:
              'ID du span racine associé à cette exécution lorsque le traçage est activé. Utilisez-le pour la recherche et la corrélation au niveau du span.',
          },
          {
            name: 'onStepFinish',
            type: '(event: any) => Promise<void> | void',
            isOptional: true,
            description:
              'Callback déclenché après chaque étape du LLM au cours de l’exécution d’un sous-Agent. Reçoit les détails de l’étape, notamment le motif de fin et l’utilisation des tokens.',
          },
          {
            name: 'onError',
            type: '({ error }: { error: Error | string }) => Promise<void> | void',
            isOptional: true,
            description: 'Callback déclenché lorsqu’une erreur survient pendant l’exécution d’un sous-Agent.',
          },
        ],
      },
    ],
  },
]}
/>

## Valeurs renvoyées {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'stream',
    type: 'MastraAgentNetworkStream<NetworkChunkType>',
    description:
      'Flux personnalisé qui étend ReadableStream<NetworkChunkType> avec des propriétés supplémentaires propres au réseau',
  },
  {
    name: 'status',
    type: 'Promise<RunStatus>',
    description: 'Promise résolue avec l’état actuel d’exécution du Workflow',
  },
  {
    name: 'result',
    type: 'Promise<WorkflowResult<TState, TOutput, TSteps>>',
    description: 'Promise résolue avec le résultat final du Workflow',
  },
  {
    name: 'usage',
    type: 'Promise<{ promptTokens: number; completionTokens: number; totalTokens: number }>',
    description: 'Promise résolue avec les statistiques d’utilisation des tokens',
  },
  {
    name: 'object',
    type: 'Promise<OUTPUT | undefined>',
    description:
      'Promise résolue avec l’objet de sortie structurée. Disponible uniquement lorsque l’option structuredOutput est fournie. Est résolue avec undefined si aucun schéma n’a été indiqué.',
  },
  {
    name: 'objectStream',
    type: 'ReadableStream<Partial<OUTPUT>>',
    description:
      "Flux d’objets partiels pendant la génération de la sortie structurée. Utile pour diffuser les résultats partiels à mesure qu’ils sont générés.",
  },
]}
/>

## Sortie structurée {/* #structured-output */}

Lorsque vous avez besoin de résultats typés et validés provenant de votre réseau, utilisez l’option `structuredOutput`. Le réseau génère une réponse correspondant à votre schéma une fois la tâche terminée.

```typescript
import { z } from 'zod'

const resultSchema = z.object({
  summary: z.string().describe('A brief summary of the findings'),
  recommendations: z.array(z.string()).describe('List of recommendations'),
  confidence: z.number().min(0).max(1).describe('Confidence score'),
})

const stream = await agent.network('Research AI trends and summarize', {
  structuredOutput: {
    schema: resultSchema,
  },
})

// Consume the stream
for await (const chunk of stream) {
  // Handle streaming events
}

// Get the typed result
const result = await stream.object
// result is typed as { summary: string; recommendations: string[]; confidence: number }
console.log(result?.summary)
console.log(result?.recommendations)
```

### Streaming d’objets partiels {/* #streaming-partial-objects */}

Vous pouvez également diffuser les objets partiels à mesure qu’ils sont générés :

```typescript
const stream = await agent.network('Analyze data', {
  structuredOutput: { schema: resultSchema },
})

// Stream partial objects
for await (const partial of stream.objectStream) {
  console.log('Partial result:', partial)
}

// Get final result
const final = await stream.object
```

### Types de fragments {/* #chunk-types */}

Lors de l’utilisation d’une sortie structurée, des types de fragments supplémentaires sont émis :

- `network-object` : émis avec les objets partiels pendant le streaming
- `network-object-result` : émis avec l’objet structuré final

## Interrompre un réseau {/* #aborting-a-network */}

Utilisez `abortSignal` pour annuler un réseau en cours d’exécution. Lors de l’interruption, le réseau arrête le routage, annule toute exécution en cours de sous-Agent, de Tool ou de Workflow et n’enregistre pas les résultats partiels dans la Memory.

```typescript
const controller = new AbortController()

// Abort after 30 seconds
setTimeout(() => controller.abort(), 30_000)

const stream = await agent.network('Research this topic thoroughly', {
  abortSignal: controller.signal,
  onAbort: ({ primitiveType, primitiveId, iteration }) => {
    console.log(`Aborted ${primitiveType} "${primitiveId}" at iteration ${iteration}`)
  },
})

for await (const chunk of stream) {
  if (
    chunk.type === 'routing-agent-abort' ||
    chunk.type === 'agent-execution-abort' ||
    chunk.type === 'tool-execution-abort' ||
    chunk.type === 'workflow-execution-abort'
  ) {
    console.log('Network was aborted')
  }
}
```
