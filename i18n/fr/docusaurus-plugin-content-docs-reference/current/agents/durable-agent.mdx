---
title: 'Référence : DurableAgent | Agents'
description: 'Référence de l’API DurableAgent, qui enveloppe un Agent pour offrir une exécution durable et des flux pouvant être repris.'
packages:
  - '@mastra/core'
---

# DurableAgent

`DurableAgent` enveloppe un [`Agent`](/reference/agents/agent) existant pour lui fournir une exécution durable et des flux pouvant être repris. Il exécute la boucle de l’Agent afin qu’un client puisse se déconnecter puis se reconnecter sans manquer d’événements, et diffuse ces événements via [PubSub](/docs/server/pubsub). Utilisez-le lorsqu’une exécution doit se poursuivre au-delà d’une seule requête ou survivre à une perte de connexion.

Créez-en un avec la fabrique [`createDurableAgent`](#createdurableagentoptions), ou utilisez [`createEventedAgent`](#createeventedagentoptions) pour une exécution sans attente de résultat sur le moteur de Workflow intégré. Pour une exécution propulsée par Inngest, utilisez [`createInngestAgent`](/reference/agents/inngest-agent) depuis `@mastra/inngest`.

## Exemple d’utilisation {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { Agent } from '@mastra/core/agent'
import { createDurableAgent } from '@mastra/core/agent/durable'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
})

const durableAgent = createDurableAgent({ agent })

export const mastra = new Mastra({
  agents: { myAgent: durableAgent },
})
```

Diffusez une réponse et lisez le résultat. La fonction `cleanup` se désabonne de PubSub lorsque vous avez terminé l’exécution :

```typescript
const { output, runId, cleanup } = await durableAgent.stream('Hello!')

const text = await output.text

cleanup()
```

### Utiliser l’option de configuration `durable` {/* #using-the-durable-config-flag */}

Définissez `durable: true` dans `AgentConfig` : l’Agent est alors automatiquement enveloppé avec `createDurableAgent` lorsqu’il est rattaché à une instance de `Mastra`. Utilisez un objet pour transmettre des options avancées telles que `cache`, `pubsub`, `maxSteps` ou `cleanupTimeoutMs`.

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { Agent } from '@mastra/core/agent'

const myAgent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
  durable: true, // or: { maxSteps: 10, cleanupTimeoutMs: 60_000 }
})

export const mastra = new Mastra({
  agents: { myAgent },
})
```

`mastra.getAgent('myAgent')` renvoie le `DurableAgent` enveloppé. Les Agents autonomes (construits mais jamais enregistrés auprès d’une instance de `Mastra`) ne deviennent pas durables. L’enveloppement est effectué lors de l’enregistrement.

## `createDurableAgent(options)` {/* #createdurableagentoptions */}

Enveloppe un `Agent` pour lui fournir une exécution durable et des flux pouvant être repris. C’est la méthode recommandée pour créer un `DurableAgent`.

```typescript
import { createDurableAgent } from '@mastra/core/agent/durable'

const durableAgent = createDurableAgent({ agent })
```

Renvoie : `DurableAgent`

### Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'Agent',
    isOptional: false,
    description: 'L’Agent à envelopper avec des capacités d’exécution durable. Les méthodes de l’Agent délèguent leurs opérations à cet Agent.',
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: 'Remplacement de l’ID.',
    defaultValue: 'agent.id',
  },
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: 'Remplacement du nom.',
    defaultValue: 'agent.name',
  },
  {
    name: 'cache',
    type: 'MastraServerCache | false',
    isOptional: true,
    description:
      'Cache des événements de flux stockés, qui permet de reprendre les flux. Si cette option est omise, l’Agent hérite du cache de l’instance Mastra ou utilise un InMemoryServerCache. Définissez-la sur `false` pour désactiver la mise en cache, ce qui empêche la reprise des flux.',
  },
  {
    name: 'pubsub',
    type: 'PubSub',
    isOptional: true,
    description: 'Instance PubSub utilisée pour diffuser les événements.',
    defaultValue: 'EventEmitterPubSub',
  },
  {
    name: 'maxSteps',
    type: 'number',
    isOptional: true,
    description: 'Nombre maximal d’étapes de la boucle de l’Agent.',
  },
]}
/>

## `createEventedAgent(options)` {/* #createeventedagentoptions */}

Enveloppe un `Agent` afin d’assurer une exécution durable sans attente de résultat sur le moteur de Workflow intégré. Comme `createDurableAgent`, cette fonction renvoie un résultat à partir duquel diffuser le flux, mais le Workflow sous-jacent s’exécute de manière non bloquante (via `startAsync`) au lieu d’aller jusqu’à son terme avant que le flux ne soit raccordé. Utilisez-la lorsque l’exécution doit progresser indépendamment de l’appelant. Elle n’accepte pas le remplacement de `id` ni de `name`.

```typescript
import { createEventedAgent } from '@mastra/core/agent/durable'

const eventedAgent = createEventedAgent({ agent })
```

Renvoie : `EventedAgent` (une sous-classe de `DurableAgent`)

### Paramètres {/* #parameters-1 */}

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'Agent',
    isOptional: false,
    description: 'L’Agent à envelopper avec des capacités d’exécution durable pilotée par événements.',
  },
  {
    name: 'cache',
    type: 'MastraServerCache | false',
    isOptional: true,
    description:
      'Cache des événements de flux stockés, qui permet de reprendre les flux. Si cette option est omise, l’Agent hérite du cache de l’instance Mastra ou utilise un InMemoryServerCache. Définissez-la sur `false` pour désactiver la mise en cache.',
  },
  {
    name: 'pubsub',
    type: 'PubSub',
    isOptional: true,
    description: 'Instance PubSub utilisée pour diffuser les événements.',
    defaultValue: 'EventEmitterPubSub',
  },
  {
    name: 'maxSteps',
    type: 'number',
    isOptional: true,
    description: 'Nombre maximal d’étapes de la boucle de l’Agent.',
  },
]}
/>

## Paramètres du constructeur {/* #constructor-parameters */}

La classe `DurableAgent` accepte les mêmes options que `createDurableAgent`, auxquelles s’ajoute `cleanupTimeoutMs`. Privilégiez la fabrique, sauf si vous devez créer une sous-classe.

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'Agent',
    isOptional: false,
    description: 'L’Agent à envelopper avec des capacités d’exécution durable.',
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: 'Remplacement de l’ID.',
    defaultValue: 'agent.id',
  },
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: 'Remplacement du nom.',
    defaultValue: 'agent.name',
  },
  {
    name: 'cache',
    type: 'MastraServerCache | false',
    isOptional: true,
    description:
      'Cache des événements de flux stockés. Si cette option est omise, il est hérité de l’instance Mastra ou un InMemoryServerCache est utilisé. Définissez-la sur `false` pour désactiver la mise en cache.',
  },
  {
    name: 'pubsub',
    type: 'PubSub',
    isOptional: true,
    description: 'Instance PubSub utilisée pour diffuser les événements.',
    defaultValue: 'EventEmitterPubSub',
  },
  {
    name: 'maxSteps',
    type: 'number',
    isOptional: true,
    description: 'Nombre maximal d’étapes de la boucle de l’Agent.',
  },
  {
    name: 'cleanupTimeoutMs',
    type: 'number',
    isOptional: true,
    description:
      'Délai de grâce, en millisecondes, avant le nettoyage automatique des entrées du registre lorsqu’un flux se termine ou rencontre une erreur. Définissez cette valeur sur 0 pour désactiver le nettoyage automatique et exiger un appel manuel à `cleanup()`. Le nettoyage automatique ne se déclenche pas pour les événements suspendus.',
    defaultValue: '30000',
  },
]}
/>

## Méthodes {/* #methods */}

### Exécution {/* #execution */}

#### `stream(messages, options?)` {/* #streammessages-options */}

Diffuse une réponse au moyen d’une exécution durable. Renvoie immédiatement un résultat dont la propriété `output` produit des événements à mesure que l’exécution progresse.

```typescript
const { output, runId, cleanup } = await durableAgent.stream('Hello!', {
  onChunk: chunk => console.log(chunk),
  onFinish: result => console.log('done', result),
})

const text = await output.text
cleanup()
```

Renvoie : [`Promise<DurableAgentStreamResult>`](#durableagentstreamresult)

#### `resume(runId, resumeData, options?)` {/* #resumerunid-resumedata-options */}

Reprend une exécution suspendue, par exemple après l’approbation d’un Tool. Transmettez le `runId` du flux d’origine ainsi que les données attendues par l’exécution. Lève une exception si aucune entrée de registre n’existe pour cette exécution.

```typescript
const { output, cleanup } = await durableAgent.resume(runId, {
  approved: true,
})

await output.text
cleanup()
```

Renvoie : [`Promise<DurableAgentStreamResult>`](#durableagentstreamresult)

#### `observe(runId, options?)` {/* #observerunid-options */}

Se reconnecte à une exécution existante en rejouant les événements mis en cache avant de transmettre les événements en direct. Utilisez cette méthode après une déconnexion réseau. Transmettez `offset` pour commencer la relecture à partir d’une position connue.

```typescript
const { output, cleanup } = await durableAgent.observe(runId, {
  offset: 0,
  onChunk: chunk => console.log(chunk),
})

await output.text
```

Par défaut, `observe()` attend indéfiniment les événements. Si le processus qui exécute l’opération s’arrête de façon inattendue, celle-ci cesse de produire des événements sans jamais émettre d’événement de fin ; le flux observé attendrait donc indéfiniment. Transmettez `idleTimeoutMs` pour limiter cette attente : après ce nombre de millisecondes sans activité, le flux prend fin. Une vérification facultative `isAlive` est d’abord consultée. Renvoyez `true` tant que l’exécution est toujours en cours de traitement (par exemple lors d’un appel de Tool de longue durée ou d’une exécution mise en pause dans l’attente d’une intervention humaine) afin de continuer à attendre. Renvoyer `false`, ou omettre `isAlive`, met fin au flux avec une erreur. Une exception temporaire levée par `isAlive` est interprétée comme « toujours active » ; l’échec ponctuel d’une vérification ne met donc jamais fin à un flux actif.

```typescript
const { output } = await durableAgent.observe(runId, {
  idleTimeoutMs: 30_000,
  isAlive: () => runHeartbeat.isFresh(runId),
})
```

Mettre fin à une exécution après expiration du délai d’inactivité déclenche le même nettoyage qu’une exécution qui rencontre une erreur (voir l’avertissement ci-dessous) : son état mis en cache est donc libéré au lieu d’être conservé. Ces deux options sont facultatives. Omettez-les pour conserver le comportement antérieur d’attente indéfinie.

Renvoie : `Promise<DurableAgentStreamResult>`

:::warning

La fonction `cleanup()` renvoyée par `observe()` détruit les entrées de registre et les événements mis en cache de l’exécution. Appelez-la uniquement lorsque vous avez terminé cette exécution. Si celle-ci est suspendue et que vous prévoyez de la reprendre plus tard, n’appelez pas `cleanup()`. Laissez le minuteur de nettoyage automatique s’en charger une fois l’exécution terminée ou en erreur. Le nettoyage automatique ne se déclenche pas pour les événements suspendus.

:::

#### `prepare(messages, options?)` {/* #preparemessages-options */}

Prépare une exécution durable sans la démarrer. Enregistre l’exécution dans le registre interne et renvoie l’entrée sérialisée du Workflow. Utilisez cette méthode lorsque vous devez contrôler le moment et la manière dont le Workflow est déclenché.

```typescript
const { runId, messageId, workflowInput, threadId, resourceId } = await durableAgent.prepare(
  'Summarize the document',
  {
    memory: { threadId: 'thread-1', resourceId: 'user-1' },
  },
)
```

Renvoie :

```typescript
interface PrepareResult {
  runId: string
  messageId: string
  workflowInput: any
  registryEntry: object
  threadId?: string
  resourceId?: string
}
```

### Récupération {/* #recovery */}

#### `recoverActiveRuns(options?)` {/* #recoveractiverunsoptions */}

Détecte les exécutions de cet Agent bloquées avec le statut `running` et les relance à partir du dernier instantané persistant. Récupère jusqu’à `options.limit` exécutions (valeur par défaut : 100). Renvoie un récapitulatif des éléments récupérés.

```typescript
const result = await durableAgent.recoverActiveRuns()
// { recovered: [{ runId, status }], succeeded: 2, failed: 0 }
```

Transmettez un `runId` pour récupérer une seule exécution connue :

```typescript
await durableAgent.recoverActiveRuns({ runId: 'run-abc-123' })
```

Renvoie :

```typescript
interface DurableAgentRecoverActiveRunsResult {
  recovered: Array<{ runId: string; status: 'success' | 'failed'; error?: Error }>
  succeeded: number
  failed: number
}
```

<PropertiesTable
  content={[
  {
    name: 'options.runId',
    type: 'string',
    isOptional: true,
    description: 'Récupère une exécution particulière à partir de son ID. Lorsque cette option est définie, les filtres de détection sont ignorés.',
  },
  {
    name: 'options.limit',
    type: 'number',
    isOptional: true,
    description: 'Nombre maximal d’exécutions actives à détecter. La valeur par défaut est 100.',
  },
  {
    name: 'options.createdBefore',
    type: 'Date',
    isOptional: true,
    description: 'Récupère uniquement les exécutions créées avant cette date.',
  },
]}
/>

#### `recover(runId, options?)` {/* #recoverrunid-options */}

Récupère une seule exécution à partir de son ID. Renvoie un résultat diffusable ayant la même structure que `stream()`. Utilisez cette méthode lorsque vous devez observer le flux de récupération en temps réel.

```typescript
const { output, cleanup } = await durableAgent.recover('run-abc-123', {
  onChunk: chunk => console.log(chunk),
  onError: ({ error }) => console.error(error),
})

await output.text
cleanup()
```

Renvoie : [`Promise<DurableAgentStreamResult>`](#durableagentstreamresult)

## Options de flux {/* #stream-options */}

`stream()` accepte un objet `DurableAgentStreamOptions`. Il prend en charge les options d’exécution de l’Agent ci-dessous, ainsi que les callbacks de cycle de vie.

<PropertiesTable
  content={[
  {
    name: 'runId',
    type: 'string',
    isOptional: true,
    description: 'Identifiant unique de cette exécution. Utilisez-le ensuite avec `resume()` ou `observe()`.',
  },
  {
    name: 'instructions',
    type: "AgentExecutionOptions['instructions']",
    isOptional: true,
    description:
      "Remplace les instructions par défaut de l’Agent pour cette exécution. Accepte une chaîne statique ou la même valeur d’instructions dynamiques que celle prise en charge par l’Agent.",
  },
  {
    name: 'context',
    type: 'ModelMessage[]',
    isOptional: true,
    description: 'Messages de contexte supplémentaires à fournir à l’Agent.',
  },
  {
    name: 'memory',
    type: 'object',
    isOptional: true,
    description: 'Configuration de Memory pour la persistance et la récupération des conversations.',
  },
  {
    name: 'requestContext',
    type: 'RequestContext',
    isOptional: true,
    description: 'Contexte de requête transportant la configuration dynamique et l’état de cette exécution.',
  },
  {
    name: 'maxSteps',
    type: 'number',
    isOptional: true,
    description: 'Nombre maximal d’étapes à exécuter pour ce flux.',
  },
  {
    name: 'toolsets',
    type: 'object',
    isOptional: true,
    description: 'Ensembles de Tools supplémentaires disponibles pour cette exécution.',
  },
  {
    name: 'clientTools',
    type: 'object',
    isOptional: true,
    description: 'Tools côté client disponibles pendant l’exécution.',
  },
  {
    name: 'toolChoice',
    type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
    isOptional: true,
    description: 'Stratégie de sélection des Tools.',
  },
  {
    name: 'activeTools',
    type: 'string[]',
    isOptional: true,
    description: "Limite l’exécution au sous-ensemble nommé des Tools de l’Agent.",
  },
  {
    name: 'modelSettings',
    type: 'object',
    isOptional: true,
    description:
      'Paramètres propres au modèle, tels que la température. Les en-têtes contenant des identifiants (`Authorization`, `X-Api-Key` et similaires) sont retirés de l’instantané sérialisé avant son passage d’un processus à un autre.',
  },
  {
    name: 'stopWhen',
    type: "AgentExecutionOptions['stopWhen']",
    isOptional: true,
    description:
      'Prédicat ou composition qui met fin de manière anticipée à la boucle de l’Agent. La closure est conservée dans le registre des exécutions du processus ; les reprises entre processus se rabattent uniquement sur `maxSteps`.',
  },
  {
    name: 'system',
    type: 'string | string[]',
    isOptional: true,
    description: 'Message système supplémentaire ajouté après les instructions de l’Agent et avant les messages utilisateur.',
  },
  {
    name: 'requireToolApproval',
    type: 'boolean | ((args: { toolName: string; args: unknown; requestContext: RequestContext; workspace?: string }) => boolean | Promise<boolean>)',
    isOptional: true,
    description:
      'Exige une approbation pour les appels de Tool. Transmettez `true` ou `false` pour les soumettre tous à approbation ou n’en soumettre aucun, ou une fonction pour définir une stratégie par appel. Les stratégies sous forme de fonction résident dans le registre des exécutions du processus ; les reprises entre processus se rabattent sur une copie `true`.',
  },
  {
    name: 'autoResumeSuspendedTools',
    type: 'boolean',
    isOptional: true,
    description: 'Reprend automatiquement les Tools suspendus au lieu d’attendre un appel externe à `resume()`.',
  },
  {
    name: 'toolCallConcurrency',
    type: 'number',
    isOptional: true,
    description: 'Nombre maximal d’appels de Tool à exécuter simultanément.',
  },
  {
    name: 'includeRawChunks',
    type: 'boolean',
    isOptional: true,
    description: 'Inclut les fragments bruts du Provider dans la sortie du flux.',
  },
  {
    name: 'maxProcessorRetries',
    type: 'number',
    isOptional: true,
    description: 'Nombre maximal de nouvelles tentatives du processeur par génération.',
  },
  {
    name: 'structuredOutput',
    type: 'object',
    isOptional: true,
    description: 'Configuration de la sortie structurée.',
  },
  {
    name: 'untilIdle',
    type: 'boolean | { maxIdleMs?: number }',
    isOptional: true,
    description:
      'Lorsque cette option est définie, maintient le flux ouvert pendant les continuations des tâches en arrière-plan jusqu’à ce que l’Agent soit inactif. Transmettez `true` pour appliquer le délai d’inactivité par défaut de 5 minutes, ou `{ maxIdleMs }` pour le personnaliser. Équivaut à la méthode obsolète `streamUntilIdle()`. Également pris en charge par `resume()`.',
  },
  {
    name: 'disableBackgroundTasks',
    type: 'boolean',
    isOptional: true,
    description: 'Désactive la répartition des tâches en arrière-plan pour cette exécution. Les Tools éligibles à l’arrière-plan sont alors exécutés directement.',
  },
  {
    name: 'tracingOptions',
    type: "AgentExecutionOptions['tracingOptions']",
    isOptional: true,
    description:
      'Métadonnées de traçage, tags, ID de trace, ID du span parent et `requestContextKeys` transmis aux spans de l’Agent et du modèle. Entièrement sérialisable en JSON.',
  },
  {
    name: 'actor',
    type: "AgentExecutionOptions['actor']",
    isOptional: true,
    description: 'Signal d’acteur propre à chaque appel, transmis aux vérifications FGA et à l’exécution des Tools.',
  },
  {
    name: 'transform',
    type: "AgentExecutionOptions['transform']",
    isOptional: true,
    description:
      'Stratégie de transformation de la charge utile du Tool propre à chaque invocation. La closure `transformToolPayload` réside dans le registre des exécutions du processus ; seule la copie `targets`, compatible avec JSON, est sérialisée.',
  },
  {
    name: 'prepareStep',
    type: "AgentExecutionOptions['prepareStep']",
    isOptional: true,
    description:
      'Hook de préparation propre à chaque étape, invoqué comme `PrepareStepProcessor` au début de chaque itération. Uniquement sous forme de closure — stocké dans le registre des exécutions du processus. Les reprises entre processus perdent ce hook.',
  },
  {
    name: 'isTaskComplete',
    type: "AgentExecutionOptions['isTaskComplete']",
    isOptional: true,
    description:
      'Stratégie d’achèvement propre à chaque appel. Les instances de Scorer et `onComplete` résident dans le registre des exécutions du processus ; les primitives compatibles avec JSON (`strategy`, `timeout`, `parallel`, `suppressFeedback`, `scorerNames`) sont sérialisées pour permettre l’observabilité entre processus.',
  },
  {
    name: 'delegation',
    type: "AgentExecutionOptions['delegation']",
    isOptional: true,
    description:
      'Hooks de délégation aux sous-Agents (`onDelegationStart`, `onDelegationComplete`, `messageFilter`). Les callbacks sont intégrés aux enveloppes de Tool des sous-Agents lors de la préparation. Les reprises entre processus perdent ces callbacks.',
  },
  {
    name: 'versions',
    type: 'object',
    isOptional: true,
    description: 'Remplacements de version pour la délégation aux sous-Agents.',
  },
  {
    name: 'abortSignal',
    type: 'AbortSignal',
    isOptional: true,
    description:
      "Signal d’annulation externe. Transmis à l’`AbortController` interne de l’exécution durable, afin que l’une ou l’autre source puisse annuler l’exécution. Les reprises entre processus ne peuvent pas récupérer ce signal — transmettez-en un nouveau à `resume()` si vous devez pouvoir annuler après la reprise.",
  },
  {
    name: 'onChunk',
    type: '(chunk: ChunkType) => void | Promise<void>',
    isOptional: true,
    description: 'Appelé pour chaque fragment diffusé.',
  },
  {
    name: 'onStepFinish',
    type: '(result: AgentStepFinishEventData) => void | Promise<void>',
    isOptional: true,
    description: 'Appelé lorsqu’une étape de la boucle de l’Agent se termine.',
  },
  {
    name: 'onFinish',
    type: '(result: AgentFinishEventData) => void | Promise<void>',
    isOptional: true,
    description: 'Appelé lorsque l’exécution se termine.',
  },
  {
    name: 'onError',
    type: '(error: Error) => void | Promise<void>',
    isOptional: true,
    description: 'Appelé lorsque l’exécution rencontre une erreur.',
  },
  {
    name: 'onSuspended',
    type: '(data: AgentSuspendedEventData) => void | Promise<void>',
    isOptional: true,
    description: 'Appelé lorsque l’exécution est suspendue, par exemple pour l’approbation d’un Tool.',
  },
  {
    name: 'onAbort',
    type: "AgentExecutionOptions['onAbort']",
    isOptional: true,
    description: 'Appelé lorsque l’exécution est annulée via `abortSignal` ou `result.abort()`.',
  },
  {
    name: 'onIterationComplete',
    type: "AgentExecutionOptions['onIterationComplete']",
    isOptional: true,
    description:
      'Appelé après chaque itération de la boucle de l’Agent avec les dernières valeurs de `messageList`, `finishReason` et de l’indicateur `isFinal`. Sert uniquement à l’observation sur les Agents durables : renvoyer `continue: false` ou un feedback n’influence pas la boucle.',
  },
]}
/>

`resume()` et `observe()` acceptent les mêmes callbacks de cycle de vie (`onChunk`, `onStepFinish`, `onFinish`, `onError`, `onSuspended`). `observe()` accepte également un `offset` pour définir le point de départ de la relecture.

## DurableAgentStreamResult {/* #durableagentstreamresult */}

Objet renvoyé par `stream()`, `resume()`, `observe()` et `recover()`.

```typescript
interface DurableAgentStreamResult<OUTPUT = undefined> {
  output: MastraModelOutput<OUTPUT>
  readonly fullStream: ReadableStream<any>
  runId: string
  threadId?: string
  resourceId?: string
  cleanup: () => void
  abort: () => void
}
```

<PropertiesTable
  content={[
  {
    name: 'output',
    type: 'MastraModelOutput',
    description: 'Sortie diffusée en continu. Attendez `output.text` pour obtenir le texte complet, ou consommez `output.fullStream`.',
  },
  {
    name: 'fullStream',
    type: 'ReadableStream',
    description: 'Flux complet des événements, délégué à `output.fullStream`.',
  },
  {
    name: 'runId',
    type: 'string',
    description: 'ID unique de l’exécution. Transmettez-le à `resume()` ou `observe()` pour vous reconnecter.',
  },
  {
    name: 'threadId',
    type: 'string',
    isOptional: true,
    description: 'ID du thread lors de l’utilisation de Memory.',
  },
  {
    name: 'resourceId',
    type: 'string',
    isOptional: true,
    description: 'ID de la ressource lors de l’utilisation de Memory.',
  },
  {
    name: 'cleanup',
    type: '() => void',
    description: 'Se désabonne de PubSub et efface les entrées de registre de l’exécution. Appelez cette fonction lorsque vous avez terminé l’exécution.',
  },
  {
    name: 'abort',
    type: '() => void',
    description:
      'Annule l’exécution en faisant basculer l’`AbortController` interne. Se manifeste sous la forme d’une `AbortError` dans l’étape d’exécution durable du LLM et déclenche le callback `onAbort`. Peut être appelée sans risque après la fin de l’exécution — elle est alors sans effet.',
  },
]}
/>

## Voir aussi {/* #related */}

- [`createInngestAgent()`](/reference/agents/inngest-agent)
- [Classe Agent](/reference/agents/agent)
- [PubSub](/docs/server/pubsub)
- [`.getMemory()`](/reference/agents/getMemory)
