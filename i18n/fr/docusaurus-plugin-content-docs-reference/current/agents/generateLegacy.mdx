---
title: "Référence : Agent.generateLegacy() (ancienne API) | Agents"
description: "Documentation de l'ancienne méthode `Agent.generateLegacy()` des Agents Mastra. Cette méthode est obsolète et sera supprimée dans une version future."
packages:
  - "@mastra/core"
---

# Agent.generateLegacy() (ancienne API)

:::warning

**Obsolète** : cette méthode est obsolète et fonctionne uniquement avec les anciens adaptateurs de modèle. Pour les adaptateurs actuels, utilisez plutôt [`.generate()`](./generate).

:::

La méthode `.generateLegacy()` est l'ancienne version de l'API de génération des Agents. Elle s'utilise avec les anciens adaptateurs de modèle pour produire du texte ou des réponses structurées. Cette méthode accepte des messages et des options de génération facultatives.

## Exemple d'utilisation {/* #usage-example */}

```typescript
await agent.generateLegacy('message for agent')
```

## Prise en charge des nouvelles tentatives par les Processors {/* #processor-retry-support */}

`generateLegacy()` n'exécute ni les Processors d'erreur ni `maxProcessorRetries`. Elle utilise à la place l'ancien chemin de génération du SDK AI.

Les juges des Scorers dotés d'anciens adaptateurs de modèle appellent `generateLegacy()`. Ils ne bénéficient pas du budget coordonné de `StreamErrorRetryProcessor` disponible pour les juges qui utilisent l'API de génération actuelle de Mastra. Utilisez un adaptateur actuel lorsque vous avez besoin des nouvelles tentatives déclenchées par les Processors d'erreur. L'ancienne option `maxRetries` reste distincte et utilise `2` par défaut.

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'string | string[] | CoreMessage[] | AiMessageType[] | UIMessageWithMetadata[]',
    description:
      "Messages à envoyer à l'Agent. Il peut s'agir d'une chaîne, d'un tableau de chaînes ou d'objets de message structurés au contenu multimodal (texte, images, etc.).",
  },
  {
    name: 'options',
    type: 'AgentGenerateOptions',
    isOptional: true,
    description: 'Configuration facultative du processus de génération.',
    properties: [
      {
        type: 'AgentGenerateOptions',
        parameters: [
          {
            name: 'abortSignal',
            type: 'AbortSignal',
            isOptional: true,
            description:
              "Objet signal permettant d'annuler l'exécution de l'Agent. Lorsque le signal est déclenché, toutes les opérations en cours sont interrompues.",
          },
          {
            name: 'context',
            type: 'CoreMessage[]',
            isOptional: true,
            description: "Messages de contexte supplémentaires à fournir à l'Agent.",
          },
          {
            name: 'structuredOutput',
            type: 'StructuredOutputOptions<S extends ZodTypeAny = ZodTypeAny>',
            isOptional: true,
            description:
              'Active la génération de sorties structurées avec une meilleure expérience de développement. Crée et utilise automatiquement un StructuredOutputProcessor en interne.',
            properties: [
              {
                type: 'StructuredOutputOptions<S extends ZodTypeAny = ZodTypeAny>',
                parameters: [
                  {
                    name: 'schema',
                    type: 'z.ZodSchema<S>',
                    isOptional: false,
                    description: 'Schéma Zod servant à valider la sortie.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'model',
                    type: 'MastraLanguageModel',
                    isOptional: false,
                    description: "Modèle à utiliser pour l'Agent de structuration interne.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'errorStrategy',
                    type: "'strict' | 'warn' | 'fallback'",
                    isOptional: true,
                    description: "Stratégie appliquée en cas d'échec de l'analyse ou de la validation. Utilise 'strict' par défaut.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'fallbackValue',
                    type: '<S extends ZodTypeAny>',
                    isOptional: true,
                    description: "Valeur de repli lorsque errorStrategy vaut 'fallback'.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'instructions',
                    type: 'string',
                    isOptional: true,
                    description: "Instructions personnalisées de l'Agent de structuration.",
                  },
                ],
              },
            ],
          },
          {
            name: 'outputProcessors',
            type: 'Processor[]',
            isOptional: true,
            description:
              "Remplace les Processors de sortie définis sur l'Agent. Ces Processors peuvent modifier ou valider les messages de l'Agent avant leur renvoi à l'utilisateur. Ils doivent implémenter l'une des fonctions `processOutputResult` ou `processOutputStream`, ou les deux.",
          },
          {
            name: 'inputProcessors',
            type: 'Processor[]',
            isOptional: true,
            description:
              "Remplace les Processors d'entrée définis sur l'Agent. Ces Processors peuvent modifier ou valider les messages avant leur traitement par l'Agent. Ils doivent implémenter la fonction `processInput`.",
          },
          {
            name: 'experimental_output',
            type: 'Zod schema | JsonSchema7',
            isOptional: true,
            description:
              "Remarque : il est préférable d'utiliser la propriété `structuredOutput`. Active la génération de sorties structurées avec la génération de texte et les appels de Tool. Le modèle génère des réponses conformes au schéma fourni.",
          },
          {
            name: 'instructions',
            type: 'string',
            isOptional: true,
            description:
              "Instructions personnalisées qui remplacent celles par défaut de l'Agent pour cette génération. Utiles pour modifier dynamiquement le comportement de l'Agent sans créer de nouvelle instance.",
          },
          {
            name: 'output',
            type: 'Zod schema | JsonSchema7',
            isOptional: true,
            description: 'Définit la structure attendue de la sortie. Peut être un objet JSON Schema ou un schéma Zod.',
          },
          {
            name: 'memory',
            type: 'object',
            isOptional: true,
            description: 'Configuration de la mémoire. Il s’agit du moyen recommandé pour la gérer.',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'thread',
                    type: 'string | { id: string; metadata?: Record<string, any>, title?: string }',
                    isOptional: false,
                    description:
                      'Fil de conversation, sous forme d’identifiant textuel ou d’objet contenant un `id` et des `metadata` facultatives.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'resource',
                    type: 'string',
                    isOptional: false,
                    description: "Identifiant de l'utilisateur ou de la ressource associée au fil.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'options',
                    type: 'MemoryConfig',
                    isOptional: true,
                    description:
                      "Configuration du comportement de la mémoire, comme l'historique des messages et le rappel sémantique. Consultez `MemoryConfig` ci-dessous.",
                  },
                ],
              },
            ],
          },
          {
            name: 'maxSteps',
            type: 'number',
            isOptional: true,
            defaultValue: '5',
            description: "Nombre maximal d'étapes d'exécution autorisées.",
          },
          {
            name: 'maxRetries',
            type: 'number',
            isOptional: true,
            defaultValue: '2',
            description: 'Nombre maximal de nouvelles tentatives. Définissez 0 pour les désactiver.',
          },
          {
            name: 'onStepFinish',
            type: 'GenerateTextOnStepFinishCallback<any> | never',
            isOptional: true,
            description:
              "Fonction de rappel appelée après chaque étape d'exécution. Elle reçoit les détails de l'étape sous forme de chaîne JSON. Indisponible pour les sorties structurées",
          },
          {
            name: 'runId',
            type: 'string',
            isOptional: true,
            description: "Identifiant unique de cette exécution de génération. Utile pour le suivi et le débogage.",
          },
          {
            name: 'telemetry',
            type: 'TelemetrySettings',
            isOptional: true,
            description: 'Paramètres de collecte de la télémétrie pendant la génération.',
            properties: [
              {
                type: 'TelemetrySettings',
                parameters: [
                  {
                    name: 'isEnabled',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Active ou désactive la télémétrie. Désactivée par défaut pendant la phase expérimentale.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordInputs',
                    type: 'boolean',
                    isOptional: true,
                    description:
                      "Active ou désactive l'enregistrement des entrées. Activé par défaut. Vous pouvez le désactiver afin d'éviter d'enregistrer des informations sensibles.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordOutputs',
                    type: 'boolean',
                    isOptional: true,
                    description:
                      "Active ou désactive l'enregistrement des sorties. Activé par défaut. Vous pouvez le désactiver afin d'éviter d'enregistrer des informations sensibles.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'functionId',
                    type: 'string',
                    isOptional: true,
                    description: 'Identifiant de cette fonction. Utilisé pour regrouper les données de télémétrie par fonction.',
                  },
                ],
              },
            ],
          },
          {
            name: 'temperature',
            type: 'number',
            isOptional: true,
            description:
              "Contrôle le caractère aléatoire de la sortie du modèle. Des valeurs élevées (par exemple 0,8) la rendent plus aléatoire, tandis que des valeurs faibles (par exemple 0,2) la rendent plus ciblée et déterministe.",
          },
          {
            name: 'toolChoice',
            type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
            isOptional: true,
            defaultValue: "'auto'",
            description: "Contrôle la manière dont l'Agent utilise les Tools pendant la génération.",
            properties: [
              {
                type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
                parameters: [
                  {
                    name: "'auto'",
                    type: 'string',
                    description: "Laisse le modèle décider s'il doit utiliser des Tools (par défaut).",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "'none'",
                    type: 'string',
                    description: "N'utilise aucun Tool.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "'required'",
                    type: 'string',
                    description: "Exige que le modèle utilise au moins un Tool.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "{ type: 'tool'; toolName: string }",
                    type: 'object',
                    description: "Exige que le modèle utilise un Tool précis, désigné par son nom.",
                  },
                ],
              },
            ],
          },
          {
            name: 'toolsets',
            type: 'ToolsetsInput',
            isOptional: true,
            description: "Ensembles de Tools supplémentaires à mettre à la disposition de l'Agent pendant la génération.",
          },
          {
            name: 'clientTools',
            type: 'ToolsInput',
            isOptional: true,
            description:
              "Tools exécutés côté 'client' de la requête. La fonction d'exécution ne figure pas dans leur définition.",
          },
          {
            name: 'hooks',
            type: 'ToolHooks',
            isOptional: true,
            description:
              "Hooks propres à l'exécution, lancés avant et après les appels de Tool. Remplace pour cette exécution les hooks correspondants au niveau de l'Agent. `beforeToolCall` peut renvoyer `{ proceed: false, output }` pour ignorer l'appel de Tool.",
          },
          {
            name: 'savePerStep',
            type: 'boolean',
            isOptional: true,
            description:
              "Enregistre les messages de manière incrémentielle après chaque étape de génération (par défaut : false). Désactivé en interne lorsque la mémoire observationnelle est activée.",
          },
          {
            name: 'providerOptions',
            type: 'Record<string, Record<string, JSONValue>>',
            isOptional: true,
            description:
              "Options supplémentaires propres au Provider, transmises au Provider LLM sous-jacent. La structure est `{ providerName: { optionKey: value } }`. Mastra étendant le SDK AI, consultez la [documentation du SDK AI](https://sdk.vercel.ai/docs/providers/ai-sdk-providers) pour découvrir toutes les options des Providers.",
            properties: [
              {
                type: 'Record<string, Record<string, JSONValue>>',
                parameters: [
                  {
                    name: 'openai',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: "Options propres à OpenAI. Exemple : `{ reasoningEffort: 'high' }`",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'anthropic',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Options propres à Anthropic. Exemple : `{ maxTokens: 1000 }`',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'google',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Options propres à Google. Exemple : `{ safetySettings: [...] }`',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: '[providerName]',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description:
                      "Autres options propres au Provider. La clé est le nom du Provider et la valeur est un enregistrement d'options propres à celui-ci.",
                  },
                ],
              },
            ],
          },
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            description: "Contexte de requête destiné à l'injection de dépendances et aux informations contextuelles.",
          },
          {
            name: 'maxTokens',
            type: 'number',
            isOptional: true,
            description: 'Nombre maximal de tokens à générer.',
          },
          {
            name: 'topP',
            type: 'number',
            isOptional: true,
            description:
              "Échantillonnage par noyau. Il s'agit d'un nombre compris entre 0 et 1. Il est recommandé de définir soit `temperature`, soit `topP`, mais pas les deux.",
          },
          {
            name: 'topK',
            type: 'number',
            isOptional: true,
            description:
              "Échantillonne uniquement parmi les K meilleures options pour chaque token suivant. Permet d'éliminer les réponses de faible probabilité de la 'longue traîne'.",
          },
          {
            name: 'presencePenalty',
            type: 'number',
            isOptional: true,
            description:
              'Paramètre de pénalité de présence. Influe sur la probabilité que le modèle répète des informations déjà présentes dans le prompt. Nombre entre -1 (augmente les répétitions) et 1 (pénalité maximale, réduit les répétitions).',
          },
          {
            name: 'frequencyPenalty',
            type: 'number',
            isOptional: true,
            description:
              'Paramètre de pénalité de fréquence. Influe sur la probabilité que le modèle emploie plusieurs fois les mêmes mots ou expressions. Nombre entre -1 (augmente les répétitions) et 1 (pénalité maximale, réduit les répétitions).',
          },
          {
            name: 'stopSequences',
            type: 'string[]',
            isOptional: true,
            description:
              "Séquences d'arrêt. Si elles sont définies, le modèle cesse de générer du texte dès que l'une d'elles est produite.",
          },
          {
            name: 'seed',
            type: 'number',
            isOptional: true,
            description:
              "Graine entière à utiliser pour l'échantillonnage aléatoire. Si elle est définie et prise en charge par le modèle, les appels produisent des résultats déterministes.",
          },
          {
            name: 'headers',
            type: 'Record<string, string | undefined>',
            isOptional: true,
            description:
              'En-têtes HTTP supplémentaires à envoyer avec la requête. Uniquement applicable aux Providers accessibles via HTTP.',
          },
        ],
      },
    ],
  },
]}
/>

## Valeurs renvoyées {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'text',
    type: 'string',
    isOptional: true,
    description: "Réponse textuelle générée. Présente lorsque la sortie est 'text' (aucun schéma fourni).",
  },
  {
    name: 'object',
    type: 'object',
    isOptional: true,
    description:
      'Réponse structurée générée. Présente lorsqu\'un schéma est fourni via `output`, `structuredOutput` ou `experimental_output`.',
  },
  {
    name: 'toolCalls',
    type: 'Array<ToolCall>',
    isOptional: true,
    description: "Appels de Tool effectués pendant la génération. Présents dans les modes texte et objet.",
    properties: [
      {
        type: 'Array<ToolCall>',
        parameters: [
          {
            name: 'toolName',
            type: 'string',
            required: true,
            description: 'Nom du Tool invoqué.',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'args',
            type: 'any',
            required: true,
            description: 'Arguments transmis au Tool.',
          },
        ],
      },
    ],
  },
]}
/>

## Migration vers la nouvelle API {/* #migration-to-new-api */}

:::info

La nouvelle méthode `.generate()` offre des fonctionnalités améliorées, notamment la compatibilité avec le SDK AI v5+, une meilleure gestion des sorties structurées et une meilleure prise en charge de la diffusion en continu. Consultez le [guide de migration](/guides/migrations/vnext-to-standard-apis) pour obtenir des instructions détaillées.

:::

### Exemple de migration rapide {/* #quick-migration-example */}

#### Avant (ancienne API) {/* #before-legacy */}

```typescript
const result = await agent.generateLegacy('message', {
  temperature: 0.7,
  maxSteps: 3,
})
```

#### Après (nouvelle API) {/* #after-new-api */}

```typescript
const result = await agent.generate('message', {
  modelSettings: {
    temperature: 0.7,
  },
  maxSteps: 3,
})
```

## Exemple d'utilisation avancé {/* #extended-usage-example */}

```typescript
import { z } from 'zod'
import { ModerationProcessor, TokenLimiterProcessor } from '@mastra/core/processors'

await agent.generateLegacy(
  [
    { role: 'user', content: 'message for agent' },
    {
      role: 'user',
      content: [
        {
          type: 'text',
          text: 'message for agent',
        },
        {
          type: 'image',
          imageUrl: 'https://example.com/image.jpg',
          mimeType: 'image/jpeg',
        },
      ],
    },
  ],
  {
    temperature: 0.7,
    maxSteps: 3,
    memory: {
      thread: 'user-123',
      resource: 'test-app',
    },
    toolChoice: 'auto',
    providerOptions: {
      openai: {
        reasoningEffort: 'high',
      },
    },
    // Structured output with better DX
    structuredOutput: {
      schema: z.object({
        sentiment: z.enum(['positive', 'negative', 'neutral']),
        confidence: z.number(),
      }),
      model: '__GATEWAY_OPENAI_MODEL__',
      errorStrategy: 'warn',
    },
    // Output processors for response validation
    outputProcessors: [
      new ModerationProcessor({ model: '__GATEWAY_OPENAI_MODEL_MINI__' }),
      new TokenLimiterProcessor({ maxTokens: 1000 }),
    ],
  },
)
```

## Voir aussi {/* #related */}

- [Guide de migration](/guides/migrations/vnext-to-standard-apis)
- [Nouvelle méthode .generate()](./generate)
- [Génération de réponses](/docs/agents/overview#use-your-agent)
- [Diffusion des réponses en continu](/docs/agents/overview#use-your-agent)
