---
title: "Référence : magasin de vecteurs Pinecone | Vecteurs"
description: Documentation de la classe PineconeVector de Mastra, qui fournit une interface avec la base de données vectorielle de Pinecone.
packages:
  - "@mastra/pinecone"
---

# Magasin de vecteurs Pinecone

La classe PineconeVector fournit une interface avec la base de données vectorielle de [Pinecone](https://www.pinecone.io/).
Elle permet la recherche vectorielle en temps réel, avec des fonctionnalités telles que la recherche hybride, le filtrage de métadonnées et la gestion des espaces de noms.

## Options du constructeur {/* #constructor-options */}

Le constructeur accepte toutes les [options de configuration de Pinecone](https://docs.pinecone.io/reference/typescript-sdk), ainsi que des champs propres à Mastra.

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique de cette instance de magasin de vecteurs',
  },
  {
    name: 'apiKey',
    type: 'string',
    description: "Clé d'API Pinecone",
  },
  {
    name: 'controllerHostUrl',
    type: 'string',
    isOptional: true,
    description: "URL d'hôte personnalisée du contrôleur Pinecone",
  },
  {
    name: 'additionalHeaders',
    type: 'Record<string, string>',
    isOptional: true,
    description: 'En-têtes HTTP supplémentaires à inclure dans les requêtes',
  },
  {
    name: 'sourceTag',
    type: 'string',
    isOptional: true,
    description: 'Balise de source pour le suivi des requêtes',
  },
  {
    name: 'cloud',
    type: "'aws' | 'gcp' | 'azure'",
    isOptional: true,
    defaultValue: 'aws',
    description: 'Fournisseur cloud pour la création de nouveaux index',
  },
  {
    name: 'region',
    type: 'string',
    isOptional: true,
    defaultValue: 'us-east-1',
    description: 'Région pour la création de nouveaux index',
  },
]}
/>

## Méthodes {/* #methods */}

### `createIndex()` {/* #createindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à créer",
  },
  {
    name: 'dimension',
    type: 'number',
    description: "Dimension du vecteur (doit correspondre à votre modèle d'embedding)",
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: "Métrique de distance pour la recherche de similarité. Utilisez 'dotproduct' si vous prévoyez d'utiliser la recherche hybride.",
  },
]}
/>

### `upsert()` {/* #upsert */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de votre index Pinecone',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: "Tableau de vecteurs d'embedding denses",
  },
  {
    name: 'sparseVectors',
    type: '{ indices: number[], values: number[] }[]',
    isOptional: true,
    description: 'Tableau de vecteurs creux pour la recherche hybride. Chaque vecteur doit comporter des tableaux indices et values correspondants.',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: 'Métadonnées de chaque vecteur',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: 'ID de vecteurs facultatifs (générés automatiquement s’ils ne sont pas fournis)',
  },
  {
    name: 'namespace',
    type: 'string',
    isOptional: true,
    description:
      'Espace de noms facultatif où stocker les vecteurs. Les vecteurs de différents espaces de noms sont isolés les uns des autres.',
  },
]}
/>

### `query()` {/* #query */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à interroger",
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: 'Vecteur de requête dense pour trouver des vecteurs similaires',
  },
  {
    name: 'sparseVector',
    type: '{ indices: number[], values: number[] }',
    isOptional: true,
    description: 'Vecteur creux facultatif pour la recherche hybride. Il doit comporter des tableaux indices et values correspondants.',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: 'Nombre de résultats à renvoyer',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtres de métadonnées pour la requête',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: "Indique s'il faut inclure le vecteur dans le résultat",
  },
  {
    name: 'namespace',
    type: 'string',
    isOptional: true,
    description: "Espace de noms facultatif depuis lequel interroger les vecteurs. Renvoie uniquement les résultats de l'espace de noms spécifié.",
  },
]}
/>

### `listIndexes()` {/* #listindexes */}

Renvoie un tableau de noms d'index sous forme de chaînes.

### `describeIndex()` {/* #describeindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à décrire",
  },
]}
/>

Renvoie :

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()` {/* #deleteindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à supprimer",
  },
]}
/>

### `updateVector()` {/* #updatevector */}

Met à jour un seul vecteur par ID ou par filtre de métadonnées. Vous devez fournir `id` ou `filter`, mais pas les deux.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index qui contient le vecteur",
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: 'ID du vecteur à mettre à jour (mutuellement exclusif avec filter)',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtre de métadonnées pour identifier le ou les vecteurs à mettre à jour (mutuellement exclusif avec id)',
  },
  {
    name: 'namespace',
    type: 'string',
    isOptional: true,
    description: "Espace de noms facultatif pour l'opération de mise à jour",
  },
  {
    name: 'update',
    type: 'object',
    description: 'Paramètres de mise à jour',
  },
  {
    name: 'update.vector',
    type: 'number[]',
    isOptional: true,
    description: 'Nouvelles valeurs de vecteur à appliquer',
  },
  {
    name: 'update.metadata',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Nouvelles métadonnées à appliquer',
  },
]}
/>

### `deleteVector()` {/* #deletevector */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index qui contient le vecteur",
  },
  {
    name: 'id',
    type: 'string',
    description: 'ID du vecteur à supprimer',
  },
]}
/>

### `deleteVectors()` {/* #deletevectors */}

Supprime plusieurs vecteurs par ID ou par filtre de métadonnées. Vous devez fournir `ids` ou `filter`, mais pas les deux.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index qui contient les vecteurs à supprimer",
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: 'Tableau des ID de vecteurs à supprimer (mutuellement exclusif avec filter)',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtre de métadonnées pour identifier les vecteurs à supprimer (mutuellement exclusif avec ids)',
  },
  {
    name: 'namespace',
    type: 'string',
    isOptional: true,
    description: "Espace de noms facultatif pour l'opération de suppression",
  },
]}
/>

## Types de réponse {/* #response-types */}

Les résultats de requête sont renvoyés dans ce format :

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
}
```

## Gestion des erreurs {/* #error-handling */}

Le magasin lève des erreurs typées qui peuvent être interceptées :

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

### Variables d'environnement {/* #environment-variables */}

Variables d'environnement requises :

- `PINECONE_API_KEY` : votre clé d'API Pinecone

## Recherche hybride {/* #hybrid-search */}

Pinecone prend en charge la recherche hybride en combinant des vecteurs denses et creux. Pour utiliser la recherche hybride :

1. Créez un index avec `metric: 'dotproduct'`
1. Lors de l'upsert, fournissez des vecteurs creux avec le paramètre `sparseVectors`
1. Lors de la requête, fournissez un vecteur creux avec le paramètre `sparseVector`

## Ressources associées {/* #related */}

- [Filtres de métadonnées](../rag/metadata-filters)
