---
title: "Référence : base vectorielle Lance | Vecteurs"
description: "Documentation de la classe LanceVectorStore dans Mastra, qui fournit une recherche vectorielle avec LanceDB, une base vectorielle embarquée fondée sur le format colonnaire Lance."
packages:
  - "@mastra/lance"
---

# Base vectorielle Lance

La classe LanceVectorStore fournit une recherche vectorielle avec [LanceDB](https://lancedb.github.io/lancedb/), une base vectorielle embarquée fondée sur le format colonnaire Lance. Elle offre un stockage efficace et une recherche de similarité rapide, aussi bien pour le développement local que pour les déploiements en production.

## Méthode de fabrique {/* #factory-method */}

LanceVectorStore utilise un modèle de fabrique pour sa création. Utilisez la méthode statique `create()` plutôt que le constructeur directement.

<PropertiesTable
  content={[
  {
    name: 'uri',
    type: 'string',
    description: 'Chemin de la base de données LanceDB ou URI des déploiements cloud',
  },
  {
    name: 'options',
    type: 'ConnectionOptions',
    description: 'Options de connexion supplémentaires pour LanceDB',
    isOptional: true,
  },
]}
/>

## Exemples de construction {/* #constructor-examples */}

Vous pouvez créer une instance de `LanceVectorStore` à l’aide de la méthode statique create :

```ts
import { LanceVectorStore } from '@mastra/lance'

// Connect to a local database
const vectorStore = await LanceVectorStore.create('/path/to/db')

// Connect to a LanceDB cloud database
const cloudStore = await LanceVectorStore.create('db://host:port')

// Connect to a cloud database with options
const s3Store = await LanceVectorStore.create('s3://bucket/db', {
  storageOptions: { timeout: '60s' },
})
```

## Méthodes {/* #methods */}

### `createIndex()` {/* #createindex */}

<PropertiesTable
  content={[
  {
    name: 'tableName',
    type: 'string',
    description: 'Nom de la table dans laquelle créer l’index',
  },
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index (nom de colonne) à créer',
  },
  {
    name: 'dimension',
    type: 'number',
    description: 'Dimension du vecteur (doit correspondre à votre modèle d’embedding)',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: 'Métrique de distance pour la recherche de similarité',
  },
  {
    name: 'indexConfig',
    type: 'LanceIndexConfig',
    isOptional: true,
    defaultValue: "{ type: 'hnsw' }",
    description: 'Configuration de l’index',
  },
]}
/>

#### `LanceIndexConfig` {/* #lanceindexconfig */}

<PropertiesTable
  content={[
  {
    name: 'type',
    type: "'ivfflat' | 'hnsw'",
    description: 'Type d’index',
    defaultValue: 'hnsw',
    properties: [
      {
        type: 'string',
        parameters: [
          {
            name: 'ivfflat',
            type: 'ivfflat',
            description: 'Regroupe les vecteurs en listes pour la recherche approximative.',
          },
          {
            name: 'hnsw',
            type: 'hnsw',
            description: 'Index fondé sur un graphe offrant des recherches rapides et un rappel élevé.',
          },
        ],
      },
    ],
  },
  {
    name: 'numPartitions',
    type: 'number',
    isOptional: true,
    defaultValue: '128',
    description: 'Nombre de partitions des index IVF',
  },
  {
    name: 'numSubVectors',
    type: 'number',
    isOptional: true,
    defaultValue: '16',
    description: 'Nombre de sous-vecteurs pour la quantification de produit',
  },
  {
    name: 'hnsw',
    type: 'HNSWConfig',
    isOptional: true,
    description: 'Configuration HNSW',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'm',
            type: 'number',
            description: 'Nombre maximal de connexions par nœud (valeur par défaut : 16)',
            isOptional: true,
          },
          {
            name: 'efConstruction',
            type: 'number',
            description: 'Complexité lors de la construction (valeur par défaut : 100)',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

### `createTable()` {/* #createtable */}

<PropertiesTable
  content={[
  {
    name: 'tableName',
    type: 'string',
    description: 'Nom de la table à créer',
  },
  {
    name: 'data',
    type: 'Record<string, unknown>[] | TableLike',
    description: 'Données initiales de la table',
  },
  {
    name: 'options',
    type: 'Partial<CreateTableOptions>',
    isOptional: true,
    description: 'Options supplémentaires de création de la table',
  },
]}
/>

### `upsert()` {/* #upsert */}

<PropertiesTable
  content={[
  {
    name: 'tableName',
    type: 'string',
    description: 'Nom de la table dans laquelle upserter les vecteurs',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: 'Tableau de vecteurs d’embedding',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: 'Métadonnées de chaque vecteur',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: 'ID de vecteurs facultatifs (générés automatiquement s’ils ne sont pas fournis)',
  },
]}
/>

### `query()` {/* #query */}

<PropertiesTable
  content={[
  {
    name: 'tableName',
    type: 'string',
    description: 'Nom de la table à interroger',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: 'Vecteur de requête',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: 'Nombre de résultats à renvoyer',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtres de métadonnées',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: 'Indique s’il faut inclure le vecteur dans le résultat',
  },
  {
    name: 'columns',
    type: 'string[]',
    isOptional: true,
    defaultValue: '[]',
    description: 'Colonnes précises à inclure dans le résultat',
  },
  {
    name: 'includeAllColumns',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: 'Indique s’il faut inclure toutes les colonnes dans le résultat',
  },
]}
/>

### `listTables()` {/* #listtables */}

Renvoie un tableau de noms de tables sous forme de chaînes.

```typescript
const tables = await vectorStore.listTables()
// ['my_vectors', 'embeddings', 'documents']
```

### `getTableSchema()` {/* #gettableschema */}

<PropertiesTable
  content={[
  {
    name: 'tableName',
    type: 'string',
    description: 'Nom de la table à décrire',
  },
]}
/>

Renvoie le schéma de la table indiquée.

### `deleteTable()` {/* #deletetable */}

<PropertiesTable
  content={[
  {
    name: 'tableName',
    type: 'string',
    description: 'Nom de la table à supprimer',
  },
]}
/>

### `deleteAllTables()` {/* #deletealltables */}

Supprime toutes les tables de la base de données.

### `listIndexes()` {/* #listindexes */}

Renvoie un tableau de noms d’index sous forme de chaînes.

### `describeIndex()` {/* #describeindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à décrire',
  },
]}
/>

Renvoie des informations sur l’index :

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
  type: 'ivfflat' | 'hnsw'
  config: {
    m?: number
    efConstruction?: number
    numPartitions?: number
    numSubVectors?: number
  }
}
```

### `deleteIndex()` {/* #deleteindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à supprimer',
  },
]}
/>

### `updateVector()` {/* #updatevector */}

Met à jour un seul vecteur par ID ou par filtre de métadonnées. Vous devez fournir `id` ou `filter`, mais pas les deux.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index contenant le vecteur',
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: 'ID du vecteur à mettre à jour (mutuellement exclusif avec filter)',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtre de métadonnées permettant d’identifier le ou les vecteurs à mettre à jour (mutuellement exclusif avec id)',
  },
  {
    name: 'update',
    type: '{ vector?: number[]; metadata?: Record<string, any>; }',
    description: 'Objet contenant le vecteur et/ou les métadonnées à mettre à jour',
  },
]}
/>

### `deleteVector()` {/* #deletevector */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index contenant le vecteur',
  },
  {
    name: 'id',
    type: 'string',
    description: 'ID du vecteur à supprimer',
  },
]}
/>

### `deleteVectors()` {/* #deletevectors */}

Supprime plusieurs vecteurs par ID ou par filtre de métadonnées. Vous devez fournir `ids` ou `filter`, mais pas les deux.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index contenant les vecteurs à supprimer',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: 'Tableau des ID de vecteurs à supprimer (mutuellement exclusif avec filter)',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtre de métadonnées permettant d’identifier les vecteurs à supprimer (mutuellement exclusif avec ids)',
  },
]}
/>

### `close()` {/* #close */}

Ferme la connexion à la base de données.

## Types de réponse {/* #response-types */}

Les résultats de la requête sont renvoyés dans ce format :

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
  document?: string // Document text if available
}
```

## Gestion des erreurs {/* #error-handling */}

La base vectorielle déclenche des erreurs typées qui peuvent être interceptées :

```typescript
try {
  await store.query({
    tableName: 'my_vectors',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof Error) {
    console.log(error.message)
  }
}
```

## Bonnes pratiques {/* #best-practices */}

- Utilisez le type d’index adapté à votre cas d’utilisation :
  - HNSW pour un meilleur rappel et de meilleures performances lorsque la mémoire n’est pas limitée
  - IVF pour une meilleure efficacité mémoire avec de grands jeux de données
- Pour des performances optimales avec de grands jeux de données, envisagez d’ajuster les valeurs `numPartitions` et `numSubVectors`
- Utilisez la méthode `close()` pour fermer correctement les connexions lorsque vous avez fini d’utiliser la base de données
- Stockez les métadonnées selon un schéma cohérent afin de simplifier les opérations de filtrage

## Ressources associées {/* #related */}

- [Filtres de métadonnées](../rag/metadata-filters)
