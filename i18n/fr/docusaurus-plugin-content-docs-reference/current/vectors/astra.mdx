---
title: "Référence : stockage vectoriel Astra | Vectors"
description: Documentation de la classe AstraVector dans Mastra, qui fournit une recherche vectorielle avec DataStax Astra DB.
packages:
  - "@mastra/astra"
---

# Stockage vectoriel Astra

La classe AstraVector fournit une recherche vectorielle à l'aide de [DataStax Astra DB](https://www.datastax.com/products/datastax-astra), une base de données cloud-native et serverless fondée sur Apache Cassandra.
Elle offre des capacités de recherche vectorielle adaptées aux entreprises et une haute disponibilité.

## Options du constructeur {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'token',
    type: 'string',
    description: "Jeton de l'API Astra DB",
  },
  {
    name: 'endpoint',
    type: 'string',
    description: "Endpoint de l'API Astra DB",
  },
  {
    name: 'keyspace',
    type: 'string',
    isOptional: true,
    description: 'Nom facultatif du keyspace',
  },
]}
/>

## Méthodes {/* #methods */}

### `createIndex()` {/* #createindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à créer",
  },
  {
    name: 'dimension',
    type: 'number',
    description: "Dimension du vecteur (doit correspondre à votre modèle d'embedding)",
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: 'Métrique de distance pour la recherche par similarité (correspond à dot_product pour dotproduct)',
  },
]}
/>

### `upsert()` {/* #upsert */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index dans lequel effectuer l'upsert",
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: "Tableau de vecteurs d'embedding",
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: 'Métadonnées de chaque vecteur',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: "Identifiants facultatifs des vecteurs (générés automatiquement s'ils ne sont pas fournis)",
  },
]}
/>

### `query()` {/* #query */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à interroger",
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: 'Vecteur de requête permettant de trouver des vecteurs similaires',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: 'Nombre de résultats à renvoyer',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtres de métadonnées de la requête',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: 'Indique si les vecteurs doivent être inclus dans les résultats',
  },
]}
/>

### `listIndexes()` {/* #listindexes */}

Renvoie un tableau de noms d'index sous forme de chaînes.

### `describeIndex()` {/* #describeindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à décrire",
  },
]}
/>

Renvoie :

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()` {/* #deleteindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à supprimer",
  },
]}
/>

### `updateVector()` {/* #updatevector */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index contenant le vecteur",
  },
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant du vecteur à mettre à jour',
  },
  {
    name: 'update',
    type: 'object',
    description: 'Objet de mise à jour contenant les modifications du vecteur ou des métadonnées, ou les deux',
    properties: [
      {
        name: 'vector',
        type: 'number[]',
        isOptional: true,
        description: 'Nouvelles valeurs du vecteur',
      },
      {
        name: 'metadata',
        type: 'Record<string, any>',
        isOptional: true,
        description: 'Nouvelles valeurs des métadonnées',
      },
    ],
  },
]}
/>

### `deleteVector()` {/* #deletevector */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index contenant le vecteur",
  },
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant du vecteur à supprimer',
  },
]}
/>

## Types de réponse {/* #response-types */}

Les résultats de la requête sont renvoyés dans ce format :

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
}
```

## Gestion des erreurs {/* #error-handling */}

Le stockage lève des erreurs typées qui peuvent être interceptées :

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

## Variables d'environnement {/* #environment-variables */}

Variables d'environnement requises :

- `ASTRA_DB_TOKEN` : votre jeton de l'API Astra DB
- `ASTRA_DB_ENDPOINT` : votre endpoint de l'API Astra DB

## Ressources associées {/* #related */}

- [Filtres de métadonnées](../rag/metadata-filters)
