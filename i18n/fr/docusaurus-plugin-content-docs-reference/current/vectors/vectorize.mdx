---
title: "Référence : stockage vectoriel Cloudflare | Vectors"
description: Documentation de la classe CloudflareVector dans Mastra, qui fournit une recherche vectorielle avec Cloudflare Vectorize.
packages:
  - "@mastra/vectorize"
---

# Stockage vectoriel Cloudflare

La classe CloudflareVector fournit une recherche vectorielle avec [Cloudflare Vectorize](https://developers.cloudflare.com/vectorize/), un service de base de données vectorielle intégré au réseau edge de Cloudflare.

## Options du constructeur {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'accountId',
    type: 'string',
    description: 'ID du compte Cloudflare',
  },
  {
    name: 'apiToken',
    type: 'string',
    description: 'Token API Cloudflare doté des autorisations Vectorize',
  },
]}
/>

## Méthodes {/* #methods */}

### `createIndex()` {/* #createindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à créer',
  },
  {
    name: 'dimension',
    type: 'number',
    description: 'Dimension du vecteur (doit correspondre à votre modèle d’embedding)',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: 'Métrique de distance de la recherche par similarité (dotproduct correspond au produit scalaire)',
  },
]}
/>

### `upsert()` {/* #upsert */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index dans lequel effectuer l’upsert',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: 'Tableau de vecteurs d’embedding',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: 'Métadonnées de chaque vecteur',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: 'ID de vecteurs facultatifs (générés automatiquement s’ils ne sont pas fournis)',
  },
]}
/>

### `query()` {/* #query */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à interroger',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: 'Vecteur de requête permettant de trouver des vecteurs similaires',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: 'Nombre de résultats à renvoyer',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtres de métadonnées de la requête',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: 'Indique s’il faut inclure les vecteurs dans les résultats',
  },
]}
/>

### `listIndexes()` {/* #listindexes */}

Renvoie un tableau de noms d'index sous forme de chaînes.

### `describeIndex()` {/* #describeindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à décrire',
  },
]}
/>

Renvoie :

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()` {/* #deleteindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à supprimer',
  },
]}
/>

### `createMetadataIndex()` {/* #createmetadataindex */}

Crée un index sur un champ de métadonnées afin d'activer le filtrage.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index contenant le champ de métadonnées',
  },
  {
    name: 'propertyName',
    type: 'string',
    description: 'Nom du champ de métadonnées à indexer',
  },
  {
    name: 'indexType',
    type: "'string' | 'number' | 'boolean'",
    description: 'Type du champ de métadonnées',
  },
]}
/>

### `deleteMetadataIndex()` {/* #deletemetadataindex */}

Supprime l'index associé à un champ de métadonnées.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index contenant le champ de métadonnées',
  },
  {
    name: 'propertyName',
    type: 'string',
    description: 'Nom du champ de métadonnées dont il faut supprimer l’indexation',
  },
]}
/>

### `listMetadataIndexes()` {/* #listmetadataindexes */}

Répertorie tous les index de champs de métadonnées d'un index.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index dont il faut répertorier les index de métadonnées',
  },
]}
/>

### `updateVector()` {/* #updatevector */}

Met à jour un vecteur ou des métadonnées pour un ID donné dans un index.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index contenant l’ID à mettre à jour',
  },
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique du vecteur ou des métadonnées à mettre à jour',
  },
  {
    name: 'update',
    type: '{ vector?: number[]; metadata?: Record<string, any>; }',
    description: 'Objet contenant le vecteur et/ou les métadonnées à mettre à jour',
  },
]}
/>

### `deleteVector()` {/* #deletevector */}

Supprime un vecteur et les métadonnées qui lui sont associées pour un ID donné dans un index.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index contenant l’ID à supprimer',
  },
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique du vecteur et des métadonnées à supprimer',
  },
]}
/>

## Types de réponse {/* #response-types */}

Les résultats de requête sont renvoyés au format suivant :

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[]
}
```

## Gestion des erreurs {/* #error-handling */}

Le stockage lève des erreurs typées qui peuvent être interceptées :

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

## Variables d'environnement {/* #environment-variables */}

Variables d'environnement requises :

- `CLOUDFLARE_ACCOUNT_ID` : ID de votre compte Cloudflare
- `CLOUDFLARE_API_TOKEN` : token API Cloudflare doté des autorisations Vectorize

## Ressources associées {/* #related */}

- [Filtres de métadonnées](../rag/metadata-filters)
