---
title: "Référence : magasin de vecteurs Qdrant | Vecteurs"
description: Documentation sur l’intégration de Qdrant à Mastra, un moteur de recherche de similarité vectorielle permettant de gérer des vecteurs et des charges utiles.
packages:
  - "@mastra/qdrant"
---

# Magasin de vecteurs Qdrant

La classe QdrantVector propose une recherche vectorielle à l’aide de [Qdrant](https://qdrant.tech/), un moteur de recherche de similarité vectorielle.
Elle fournit un service prêt pour la production avec une API pratique pour stocker, rechercher et gérer des vecteurs, ainsi qu’une prise en charge supplémentaire des charges utiles et des filtres avancés.

## Options du constructeur {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: 'URL REST de l’instance Qdrant. Par exemple : https://xyz-example.eu-central.aws.cloud.qdrant.io:6333',
  },
  {
    name: 'apiKey',
    type: 'string',
    description: 'Clé API Qdrant facultative',
  },
  {
    name: 'https',
    type: 'boolean',
    description: 'Indique s’il faut utiliser TLS lors de la configuration de la connexion. Recommandé.',
  },
]}
/>

## Méthodes {/* #methods */}

### `createIndex()` {/* #createindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à créer',
  },
  {
    name: 'dimension',
    type: 'number',
    description: 'Dimension du vecteur (doit correspondre à votre modèle d’embedding). Obligatoire pour les collections à vecteur unique.',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: 'Métrique de distance pour la recherche de similarité',
  },
  {
    name: 'namedVectors',
    type: "Record<string, { size: number; distance: 'cosine' | 'euclidean' | 'dotproduct' }>",
    isOptional: true,
    description:
      'Configuration des espaces vectoriels nommés. Lorsqu’elle est fournie, crée une collection avec plusieurs champs vectoriels nommés.',
  },
]}
/>

#### Création d’une collection de vecteurs nommés {/* #creating-a-named-vectors-collection */}

```typescript
// Create a collection with multiple named vector spaces
await store.createIndex({
  indexName: 'multi_modal',
  dimension: 768, // fallback
  namedVectors: {
    text: { size: 768, distance: 'cosine' },
    image: { size: 512, distance: 'euclidean' },
  },
})
```

### `upsert()` {/* #upsert */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index dans lequel effectuer l’upsert',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: 'Tableau de vecteurs d’embedding',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: 'Métadonnées de chaque vecteur',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: 'ID de vecteur facultatifs (générés automatiquement s’ils ne sont pas fournis)',
  },
  {
    name: 'vectorName',
    type: 'string',
    isOptional: true,
    description: 'Nom de l’espace vectoriel dans lequel effectuer l’upsert lors de l’utilisation de vecteurs nommés.',
  },
]}
/>

#### Upsert dans des espaces vectoriels nommés {/* #upserting-into-named-vector-spaces */}

```typescript
// Upsert into the "text" vector space
await store.upsert({
  indexName: 'multi_modal',
  vectors: textEmbeddings,
  metadata: textMetadata,
  vectorName: 'text',
})

// Upsert into the "image" vector space
await store.upsert({
  indexName: 'multi_modal',
  vectors: imageEmbeddings,
  metadata: imageMetadata,
  vectorName: 'image',
})
```

### `query()` {/* #query */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à interroger',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: 'Vecteur de requête pour trouver des vecteurs similaires',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: 'Nombre de résultats à renvoyer',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtres de métadonnées pour la requête',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: 'Indique s’il faut inclure les vecteurs dans les résultats',
  },
  {
    name: 'using',
    type: 'string',
    isOptional: true,
    description:
      'Nom du champ vectoriel à interroger lors de l’utilisation de vecteurs nommés. Utilisez cette option lorsque votre collection comporte plusieurs champs vectoriels nommés.',
  },
]}
/>

#### Vecteurs nommés {/* #named-vectors */}

Qdrant prend en charge [plusieurs vecteurs par collection](https://qdrant.tech/documentation/concepts/vectors/#named-vectors), avec un nom attribué à chaque champ vectoriel. Utilisez le paramètre `using` pour sélectionner le champ vectoriel à interroger :

```typescript
const results = await store.query({
  indexName: 'my_index',
  queryVector: embedding,
  topK: 10,
  using: 'title_embedding', // Query against a specific named vector
})
```

### `listIndexes()` {/* #listindexes */}

Renvoie un tableau de noms d’index sous forme de chaînes.

### `describeIndex()` {/* #describeindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à décrire',
  },
]}
/>

Renvoie :

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()` {/* #deleteindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à supprimer',
  },
]}
/>

### `updateVector()` {/* #updatevector */}

Met à jour un seul vecteur par ID ou par filtre de métadonnées. Vous devez fournir `id` ou `filter`, mais pas les deux.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index à mettre à jour',
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: 'ID du vecteur à mettre à jour (s’exclut mutuellement avec filter)',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtre de métadonnées pour identifier le ou les vecteurs à mettre à jour (s’exclut mutuellement avec id)',
  },
  {
    name: 'update',
    type: '{ vector?: number[]; metadata?: Record<string, any>; }',
    description: 'Objet contenant le vecteur et/ou les métadonnées à mettre à jour',
  },
]}
/>

Met à jour un vecteur et/ou ses métadonnées dans l’index indiqué. Si le vecteur et les métadonnées sont tous deux fournis, les deux sont mis à jour. Fournir une seule valeur met uniquement cette valeur à jour.

### `deleteVector()` {/* #deletevector */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index duquel supprimer le vecteur',
  },
  {
    name: 'id',
    type: 'string',
    description: 'ID du vecteur à supprimer',
  },
]}
/>

Supprime un vecteur de l’index indiqué à partir de son ID.

### `deleteVectors()` {/* #deletevectors */}

Supprime plusieurs vecteurs par ID ou par filtre de métadonnées. Vous devez fournir `ids` ou `filter`, mais pas les deux.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de l’index contenant les vecteurs à supprimer',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: 'Tableau des ID de vecteurs à supprimer (s’exclut mutuellement avec filter)',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtre de métadonnées pour identifier les vecteurs à supprimer (s’exclut mutuellement avec ids)',
  },
]}
/>

### `createPayloadIndex()` {/* #createpayloadindex */}

Crée un index de charge utile (métadonnées) sur un champ de collection pour permettre un filtrage efficace. Cette opération est **obligatoire** pour Qdrant Cloud et pour toute instance Qdrant avec `strict_mode_config = true`.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de la collection sur laquelle créer l’index de charge utile',
  },
  {
    name: 'fieldName',
    type: 'string',
    description: 'Nom du champ de charge utile à indexer',
  },
  {
    name: 'fieldSchema',
    type: "'keyword' | 'integer' | 'float' | 'geo' | 'text' | 'bool' | 'datetime' | 'uuid'",
    description: 'Type de schéma du champ de charge utile',
  },
  {
    name: 'wait',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'true',
    description: 'Indique s’il faut attendre la fin de l’opération',
  },
]}
/>

```typescript
// Create a keyword index for filtering by source
await store.createPayloadIndex({
  indexName: 'my_index',
  fieldName: 'source',
  fieldSchema: 'keyword',
})

const results = await store.query({
  indexName: 'my_index',
  queryVector: queryVector,
  filter: { source: 'document-a' },
})
```

### `deletePayloadIndex()` {/* #deletepayloadindex */}

Supprime un index de charge utile d’un champ de collection.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: 'Nom de la collection dont supprimer l’index de charge utile',
  },
  {
    name: 'fieldName',
    type: 'string',
    description: 'Nom de l’index de champ de charge utile à supprimer',
  },
  {
    name: 'wait',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'true',
    description: 'Indique s’il faut attendre la fin de l’opération',
  },
]}
/>

## Types de réponse {/* #response-types */}

Les résultats de requête sont renvoyés dans ce format :

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
}
```

## Gestion des erreurs {/* #error-handling */}

Le magasin lève des erreurs typées que vous pouvez intercepter :

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

## Pages associées {/* #related */}

- [Filtres de métadonnées](../rag/metadata-filters)
