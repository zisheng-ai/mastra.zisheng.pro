---
title: "Référence : stockage vectoriel Elasticsearch | Vectors"
description: Documentation de la classe ElasticSearchVector de Mastra, qui fournit une recherche vectorielle au moyen d'Elasticsearch.
packages:
  - "@mastra/elasticsearch"
---

# Stockage vectoriel Elasticsearch

La classe `ElasticSearchVector` fournit une recherche vectorielle au moyen d'[Elasticsearch](https://www.elastic.co/elasticsearch/), avec son type de champ `dense_vector` et ses fonctionnalités de recherche k-NN.
Elle fait partie du package `@mastra/elasticsearch`.

## Installation {/* #installation */}

```bash npm2yarn
npm install @mastra/elasticsearch@latest
```

## Utilisation {/* #usage */}

```typescript prettier:false
import { ElasticSearchVector } from "@mastra/elasticsearch";

const store = new ElasticSearchVector({
  id: "elasticsearch-vector",
  url: process.env.ELASTICSEARCH_URL,
});

// Create an index
await store.createIndex({
  indexName: "my-collection",
  dimension: 1536,
});

// Add vectors with metadata
const vectors = [[0.1, 0.2, ...], [0.3, 0.4, ...]];
const metadata = [
  { text: "first document", category: "A" },
  { text: "second document", category: "B" }
];
await store.upsert({
  indexName: "my-collection",
  vectors,
  metadata,
});

// Query similar vectors
const results = await store.query({
  indexName: "my-collection",
  queryVector: [0.1, 0.2, ...],
  topK: 10,
  filter: { category: "A" },
});
```

## Options du constructeur {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique de cette instance de stockage vectoriel',
  },
  {
    name: 'url',
    type: 'string',
    description: "URL de connexion Elasticsearch (par exemple, 'http://localhost:9200')",
  },
]}
/>

## Méthodes {/* #methods */}

### `createIndex()` {/* #createindex */}

Crée un index avec la configuration indiquée.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à créer",
  },
  {
    name: 'dimension',
    type: 'number',
    description: "Dimension du vecteur (doit correspondre à votre modèle d'embedding)",
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: 'Métrique de distance pour la recherche de similarité',
  },
]}
/>

### `upsert()` {/* #upsert */}

Ajoute ou met à jour les vecteurs et leurs métadonnées dans l'index.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index dans lequel effectuer l'insertion",
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: "Tableau de vecteurs d'embedding",
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: 'Métadonnées de chaque vecteur',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: "Identifiants facultatifs des vecteurs (générés automatiquement s'ils ne sont pas fournis)",
  },
]}
/>

### `query()` {/* #query */}

Recherche des vecteurs similaires avec un filtrage facultatif des métadonnées.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index dans lequel effectuer la recherche",
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: 'Vecteur de requête pour lequel rechercher des vecteurs similaires',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: 'Nombre de résultats à renvoyer',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtres de métadonnées',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: 'Indique si les données vectorielles doivent être incluses dans les résultats',
  },
]}
/>

### `describeIndex()` {/* #describeindex */}

Récupère les informations relatives à un index.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à décrire",
  },
]}
/>

Renvoie :

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()` {/* #deleteindex */}

Supprime un index et toutes ses données.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index à supprimer",
  },
]}
/>

### `listIndexes()` {/* #listindexes */}

Répertorie tous les index vectoriels.

Renvoie : `Promise<string[]>`

### `updateVector()` {/* #updatevector */}

Met à jour un seul vecteur à partir de son identifiant ou d'un filtre de métadonnées. Vous devez fournir `id` ou `filter`, mais pas les deux.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index contenant le vecteur",
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: 'Identifiant du vecteur à mettre à jour (incompatible avec filter)',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtre de métadonnées permettant d’identifier le ou les vecteurs à mettre à jour (incompatible avec id)',
  },
  {
    name: 'update',
    type: 'object',
    description: 'Données de mise à jour contenant le vecteur ou les métadonnées, ou les deux',
  },
  {
    name: 'update.vector',
    type: 'number[]',
    isOptional: true,
    description: 'Nouvelles données vectorielles',
  },
  {
    name: 'update.metadata',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Nouvelles métadonnées',
  },
]}
/>

### `deleteVector()` {/* #deletevector */}

Supprime un seul vecteur à partir de son identifiant.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index contenant le vecteur",
  },
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant du vecteur à supprimer',
  },
]}
/>

### `deleteVectors()` {/* #deletevectors */}

Supprime plusieurs vecteurs à partir de leurs identifiants ou d'un filtre de métadonnées. Vous devez fournir `ids` ou `filter`, mais pas les deux.

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: "Nom de l'index contenant les vecteurs à supprimer",
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: 'Tableau des identifiants de vecteurs à supprimer (incompatible avec filter)',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: 'Filtre de métadonnées permettant d’identifier les vecteurs à supprimer (incompatible avec ids)',
  },
]}
/>

## Types de réponse {/* #response-types */}

Les résultats des requêtes sont renvoyés au format suivant :

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
}
```

## Voir aussi {/* #related */}

- [Filtres de métadonnées](../rag/metadata-filters)
