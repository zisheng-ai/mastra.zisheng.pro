---
title: "Référence : BlobStoreFournisseur | Éditeur"
description: "Reference pour BlobStoreFournisseur, the interface that registers a blob store implementation avec MastraÉditeur."
packages:
  - "@mastra/core"
---

# BlobStoreFournisseur

`BlobStoreProvider` est the interface a package implements à register a blob store avec [MastraÉditeur class](/reference/editor/mastra-editor). Blob stores hold content-addressable skill blobs et autre large artifacts produced par the editor.

The built-in `storage` provider uses the configuré storage backend's `blobs` domain. Externe providers (for example, S3) sont supplied via `MastraEditorConfig.blobStores`.

## Exemple d’utilisation

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'
import { s3BlobStoreProvider } from '@mastra/s3'

new MastraEditor({
  blobStores: {
    [s3BlobStoreProvider.id]: s3BlobStoreProvider,
  },
})
```

## Propriétés

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Unique provider identifiant (for example, `"storage"`, `"s3"`).',
  },
  {
    name: 'name',
    type: 'string',
    description: 'Human-readable nom pour UI display.',
  },
  {
    name: 'description',
    type: 'string',
    description: 'Short description.',
    isOptional: true,
  },
  {
    name: 'configSchema',
    type: 'Record<string, unknown>',
    description: 'JSON Schema describing provider-specific configuration. Used par the UI à affiche config forms.',
    isOptional: true,
  },
  {
    name: 'createBlobStore',
    type: '(config: TConfig) => BlobStore | Promise<BlobStore>',
    description:
      'Créer a runtime `BlobStore` instance depuis the stored config. Called when the editor resolves a blob store via `MastraEditor.resolveBlobStore(providerId, config)`.',
  },
]}
/>

## Implementing a provider

```typescript title="src/providers/my-blob-store.ts"
import type { BlobStore, BlobStoreProvider } from '@mastra/core/editor'

export const myBlobStoreProvider: BlobStoreProvider<{ bucket: string; region: string }> = {
  id: 'my-blob-store',
  name: 'My Blob Store',
  description: 'Object-store-backed blob storage.',
  configSchema: {
    type: 'object',
    required: ['bucket', 'region'],
    properties: {
      bucket: { type: 'string' },
      region: { type: 'string' },
    },
  },
  async createBlobStore(config): Promise<BlobStore> {
    return createMyBlobStore({ bucket: config.bucket, region: config.region })
  },
}
```

When aucun `providerId` est passed à `MastraEditor.resolveBlobStore()`, the editor falls back à the storage backend's `blobs` domain.

## Voir aussi

- [MastraÉditeur class](/reference/editor/mastra-editor): Fournisseur registry et `resolveBlobStore()` method.
