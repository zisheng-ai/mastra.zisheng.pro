---
title: "Référence : SandboxProcessManager | Workspace"
description: "Référence de l'API de SandboxProcessManager et ProcessHandle, les classes de base pour la gestion des processus en arrière-plan dans les Sandboxes."
packages:
  - "@mastra/core"
---

# SandboxProcessManager

**Ajouté dans :** `@mastra/core@1.7.0`

Classe de base abstraite permettant de gérer les processus en arrière-plan dans les Sandboxes. Elle fournit des méthodes pour lancer les processus, les répertorier, récupérer leurs handles à partir du PID et les arrêter.

[`BlaxelSandbox`](/reference/workspace/blaxel-sandbox), [`DaytonaSandbox`](/reference/workspace/daytona-sandbox), [`E2BSandbox`](/reference/workspace/e2b-sandbox), [`ModalSandbox`](/reference/workspace/modal-sandbox) et [`LocalSandbox`](/reference/workspace/local-sandbox) comprennent tous un gestionnaire de processus intégré. Vous n'avez pas besoin d'instancier directement cette classe, sauf si vous créez un Provider de Sandbox personnalisé.

## Exemple d'utilisation {/* #usage-example */}

Accédez au gestionnaire de processus au moyen de la propriété `processes` de la Sandbox :

```typescript title="src/mastra/index.ts"
import { LocalSandbox } from '@mastra/core/workspace'

const sandbox = new LocalSandbox({ workingDirectory: './workspace' })
await sandbox.start()

// Spawn a background process
const handle = await sandbox.processes.spawn('node server.js', {
  env: { PORT: '3000' },
  onStdout: data => console.log(data),
})

// List all tracked processes
const procs = await sandbox.processes.list()

// Get a handle by PID
const proc = await sandbox.processes.get(handle.pid)

// Kill a process
await sandbox.processes.kill(handle.pid)
```

## Méthodes {/* #methods */}

### `spawn(command, options?)` {/* #spawncommand-options */}

Lance un processus en arrière-plan. Renvoie immédiatement un `ProcessHandle` sans attendre la fin du processus.

```typescript
const handle = await sandbox.processes.spawn('npm run dev', {
  cwd: '/app',
  env: { NODE_ENV: 'development' },
  onStdout: data => console.log(data),
})
```

**Paramètres :**

<PropertiesTable
  content={[
  {
    name: 'command',
    type: 'string',
    description: 'Commande à exécuter. Interprétée par le shell.',
  },
  {
    name: 'options',
    type: 'SpawnProcessOptions',
    description: 'Paramètres facultatifs du processus lancé.',
    isOptional: true,
    properties: [
      {
        type: 'SpawnProcessOptions',
        parameters: [
          {
            name: 'timeout',
            type: 'number',
            description: "Délai d'expiration en millisecondes. Arrête le processus en cas de dépassement.",
            isOptional: true,
          },
          {
            name: 'env',
            type: 'NodeJS.ProcessEnv',
            description: "Variables d'environnement du processus.",
            isOptional: true,
          },
          {
            name: 'cwd',
            type: 'string',
            description: 'Répertoire de travail du processus.',
            isOptional: true,
          },
          {
            name: 'onStdout',
            type: '(data: string) => void',
            description: "Callback pour les fragments de stdout. Appelé à mesure que les données arrivent.",
            isOptional: true,
          },
          {
            name: 'onStderr',
            type: '(data: string) => void',
            description: "Callback pour les fragments de stderr. Appelé à mesure que les données arrivent.",
            isOptional: true,
          },
          {
            name: 'abortSignal',
            type: 'AbortSignal',
            description: "Signal permettant d'abandonner le processus. Lorsqu'il est déclenché, le processus est arrêté.",
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

**Renvoie :** `Promise<ProcessHandle>`

### `list()` {/* #list */}

Répertorie tous les processus suivis. Renvoie des informations sur chaque processus, notamment son PID, son état d'exécution et son code de sortie.

```typescript
const procs = await sandbox.processes.list()
for (const proc of procs) {
  console.log(proc.pid, proc.running, proc.exitCode)
}
```

**Renvoie :** `Promise<ProcessInfo[]>`

### `get(pid)` {/* #getpid */}

Récupère le handle d'un processus à partir de son PID. Renvoie `undefined` si le processus est introuvable ou a déjà été retiré.

```typescript
const handle = await sandbox.processes.get(1234)
if (handle) {
  console.log(handle.stdout)
  await handle.kill()
}
```

**Renvoie :** `Promise<ProcessHandle | undefined>`

### `kill(pid)` {/* #killpid */}

Arrête un processus à partir de son PID. Attend la fin du processus avant de renvoyer le résultat. Renvoie `true` si le processus a été arrêté et `false` s'il est introuvable.

```typescript
const killed = await sandbox.processes.kill(handle.pid)
```

**Renvoie :** `Promise<boolean>`

## `ProcessInfo` {/* #processinfo */}

Informations sur un processus suivi, renvoyées par `list()`.

<PropertiesTable
  content={[
  {
    name: 'pid',
    type: 'number',
    description: 'Identifiant du processus.',
  },
  {
    name: 'command',
    type: 'string',
    description: 'Commande qui a été exécutée.',
    isOptional: true,
  },
  {
    name: 'running',
    type: 'boolean',
    description: "Indique si le processus est toujours en cours d'exécution.",
  },
  {
    name: 'exitCode',
    type: 'number',
    description: 'Code de sortie si le processus est terminé.',
    isOptional: true,
  },
]}
/>

---

## `ProcessHandle` {/* #processhandle */}

Handle d'un processus lancé en arrière-plan. Fournit des méthodes pour lire la sortie, envoyer des données sur stdin, attendre la fin et arrêter le processus.

Vous ne créez pas directement d'instances de `ProcessHandle`. Elles sont renvoyées par `spawn()` et `get()`.

### Exemple d'utilisation {/* #usage-example-1 */}

```typescript
const handle = await sandbox.processes.spawn('npm run dev', {
  onStdout: data => console.log(data),
})

// Read accumulated output
console.log(handle.pid)
console.log(handle.stdout)
console.log(handle.stderr)
console.log(handle.exitCode) // undefined while running

// Wait for completion
const result = await handle.wait()

// Send stdin
await handle.sendStdin('input data\n')

// Kill the process
await handle.kill()
```

### Propriétés {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'pid',
    type: 'number',
    description: 'Identifiant du processus.',
  },
  {
    name: 'stdout',
    type: 'string',
    description: 'Sortie stdout accumulée jusque-là.',
  },
  {
    name: 'stderr',
    type: 'string',
    description: 'Sortie stderr accumulée jusque-là.',
  },
  {
    name: 'exitCode',
    type: 'number | undefined',
    description: "Code de sortie. undefined tant que le processus est en cours d'exécution.",
  },
  {
    name: 'command',
    type: 'string | undefined',
    description: 'Commande qui a été lancée. Définie automatiquement par le gestionnaire de processus.',
  },
  {
    name: 'reader',
    type: 'Readable',
    description: 'Stream lisible de stdout. Utile pour les protocoles tels que LSP ou JSON-RPC qui communiquent via stdio.',
  },
  {
    name: 'writer',
    type: 'Writable',
    description: 'Stream inscriptible vers stdin. Utile pour les protocoles tels que LSP ou JSON-RPC qui communiquent via stdio.',
  },
]}
/>

### Méthodes {/* #methods-1 */}

#### `wait(options?)` {/* #waitoptions */}

Attend la fin du processus et renvoie le résultat. Vous pouvez transmettre des callbacks `onStdout`/`onStderr` pour diffuser la sortie pendant l'attente. Les callbacks sont automatiquement supprimés lorsque `wait()` est résolue.

```typescript
// Simple wait
const result = await handle.wait()
console.log(result.success, result.exitCode, result.stdout)

// Wait with streaming
const result = await handle.wait({
  onStdout: data => process.stdout.write(data),
  onStderr: data => process.stderr.write(data),
})
```

**Paramètres :**

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'WaitOptions',
    description: "Paramètres facultatifs de l'attente.",
    isOptional: true,
    properties: [
      {
        type: 'WaitOptions',
        parameters: [
          {
            name: 'onStdout',
            type: '(data: string) => void',
            description: "Callback pour les fragments de stdout pendant l'attente.",
            isOptional: true,
          },
          {
            name: 'onStderr',
            type: '(data: string) => void',
            description: "Callback pour les fragments de stderr pendant l'attente.",
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

**Renvoie :** `Promise<CommandResult>`

L'objet `CommandResult` contient :

<PropertiesTable
  content={[
  {
    name: 'success',
    type: 'boolean',
    description: 'true si le code de sortie est 0.',
  },
  {
    name: 'exitCode',
    type: 'number',
    description: 'Code de sortie numérique.',
  },
  {
    name: 'stdout',
    type: 'string',
    description: 'Sortie stdout complète.',
  },
  {
    name: 'stderr',
    type: 'string',
    description: 'Sortie stderr complète.',
  },
  {
    name: 'executionTimeMs',
    type: 'number',
    description: "Durée d'exécution en millisecondes.",
  },
  {
    name: 'timedOut',
    type: 'boolean',
    description: "true si le processus a été arrêté en raison d'un délai d'expiration.",
    isOptional: true,
  },
  {
    name: 'killed',
    type: 'boolean',
    description: 'true si le processus a été arrêté par un signal.',
    isOptional: true,
  },
]}
/>

#### `kill()` {/* #kill */}

Arrête le processus. Renvoie `true` si le processus a été arrêté et `false` s'il était déjà terminé.

```typescript
const killed = await handle.kill()
```

**Renvoie :** `Promise<boolean>`

#### `sendStdin(data)` {/* #sendstdindata */}

Envoie des données sur le stdin du processus. Lève une erreur si le processus est déjà terminé ou si stdin n'est pas disponible.

```typescript
await handle.sendStdin('console.log("hello")\n')
```

**Renvoie :** `Promise<void>`

## Interopérabilité des streams {/* #stream-interop */}

`ProcessHandle` expose les propriétés `reader` et `writer` pour l'intégration avec des protocoles Node.js fondés sur des streams, tels que LSP ou JSON-RPC :

```typescript
import {
  createMessageConnection,
  StreamMessageReader,
  StreamMessageWriter,
} from 'vscode-jsonrpc/node'

const handle = await sandbox.processes.spawn('typescript-language-server --stdio')

const connection = createMessageConnection(
  new StreamMessageReader(handle.reader),
  new StreamMessageWriter(handle.writer),
)
connection.listen()
```

## Création d'un gestionnaire de processus personnalisé {/* #building-a-custom-process-manager */}

Pour créer un gestionnaire de processus destiné à un Provider de Sandbox personnalisé, étendez `SandboxProcessManager` et implémentez `spawn()` et `list()`. La classe de base enveloppe automatiquement vos méthodes avec `ensureRunning()` afin que la Sandbox démarre avant toute opération sur un processus.

```typescript
import { SandboxProcessManager, ProcessHandle } from '@mastra/core/workspace'
import type { ProcessInfo, SpawnProcessOptions } from '@mastra/core/workspace'

class MyProcessManager extends SandboxProcessManager<MySandbox> {
  async spawn(command: string, options: SpawnProcessOptions = {}): Promise<ProcessHandle> {
    // Your spawn implementation
    const handle = new MyProcessHandle(/* ... */)
    this._tracked.set(handle.pid, handle)
    return handle
  }

  async list(): Promise<ProcessInfo[]> {
    return Array.from(this._tracked.values()).map(handle => ({
      pid: handle.pid,
      running: handle.exitCode === undefined,
      exitCode: handle.exitCode,
    }))
  }
}
```

Transmettez le gestionnaire de processus à votre Sandbox au moyen de l'option `processes` de `MastraSandbox` :

```typescript
class MySandbox extends MastraSandbox {
  constructor() {
    super({
      name: 'MySandbox',
      processes: new MyProcessManager(),
    })
  }
}
```

Lorsqu'un gestionnaire de processus est fourni, `MastraSandbox` crée automatiquement une implémentation par défaut de `executeCommand` qui utilise `spawn()` + `wait()` ; vous n'avez donc pas besoin d'implémenter les deux.

## Voir aussi {/* #related */}

- [Sandbox](/docs/workspace/sandbox)
- [Interface WorkspaceSandbox](/reference/workspace/sandbox)
- [Référence de LocalSandbox](/reference/workspace/local-sandbox)
- [Référence d'E2BSandbox](/reference/workspace/e2b-sandbox)
- [Référence de DaytonaSandbox](/reference/workspace/daytona-sandbox)
