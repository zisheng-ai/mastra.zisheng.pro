---
title: "Référence : BlaxelSandbox | Workspace"
description: "Documentation du Provider BlaxelSandbox pour l'exécution isolée de code dans le cloud."
packages:
  - "@mastra/blaxel"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# BlaxelSandbox

Exécute des commandes dans des Sandboxes cloud [Blaxel](https://blaxel.ai/) isolés. Fournit des environnements sécurisés et isolés d'exécution de code qui prennent en charge le montage de stockages cloud (S3, GCS) au moyen de FUSE. Pour en savoir plus sur l'interface, consultez l'[interface WorkspaceSandbox](/reference/workspace/sandbox).

## Installation {/* #installation */}

```bash npm2yarn
npm install @mastra/blaxel
```

## Utilisation {/* #usage */}

Ajoutez un `BlaxelSandbox` à un Workspace et attribuez-le à un Agent :

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { BlaxelSandbox } from '@mastra/blaxel'

const workspace = new Workspace({
  sandbox: new BlaxelSandbox(),
})

const agent = new Agent({
  id: 'code-agent',
  name: 'Code Agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})
```

### Image personnalisée avec des ressources {/* #custom-image-with-resources */}

Utilisez une image Docker personnalisée avec de la mémoire supplémentaire :

```typescript
const workspace = new Workspace({
  sandbox: new BlaxelSandbox({
    image: 'node:20-slim',
    memory: 8192,
    region: 'auto',
    timeout: '10m',
  }),
})
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique de cette instance de Sandbox.',
    isOptional: true,
    defaultValue: 'Généré automatiquement',
  },
  {
    name: 'image',
    type: 'string',
    description:
      'Image Docker à utiliser pour le Sandbox. Les images basées sur Debian prennent en charge les montages S3 et GCS. Les images basées sur Alpine prennent uniquement en charge les montages S3.',
    isOptional: true,
    defaultValue: "'blaxel/ts-app:latest'",
  },
  {
    name: 'memory',
    type: 'number',
    description: 'Mémoire allouée en Mo.',
    isOptional: true,
    defaultValue: '4096',
  },
  {
    name: 'timeout',
    type: 'string',
    description: "Délai d'expiration de l'exécution sous la forme d'une chaîne de durée (par exemple, '5m' ou '1h'). Correspond à la durée de vie du Sandbox Blaxel.",
    isOptional: true,
  },
  {
    name: 'region',
    type: 'string',
    description:
      "Région Blaxel dans laquelle le Sandbox doit être créé. Utilisez 'auto' pour choisir automatiquement une région, ou définissez une région précise telle que 'us-pdx-1'.",
    isOptional: true,
    defaultValue: "process.env.BL_REGION || 'auto'",
  },
  {
    name: 'env',
    type: 'Record<string, string>',
    description: "Variables d'environnement à définir dans le Sandbox.",
    isOptional: true,
  },
  {
    name: 'labels',
    type: 'Record<string, string>',
    description: 'Étiquettes personnalisées du Sandbox.',
    isOptional: true,
  },
  {
    name: 'runtimes',
    type: 'SandboxRuntime[]',
    description:
      "Environnements d'exécution pris en charge. Valeurs valides : 'node', 'python', 'bash', 'ruby', 'go', 'rust', 'java', 'cpp', 'r'.",
    isOptional: true,
    defaultValue: "['node', 'python', 'bash']",
  },
  {
    name: 'ports',
    type: "Array<{ name?: string; target: number; protocol?: 'HTTP' | 'TCP' | 'UDP' }>",
    description: 'Ports à exposer depuis le Sandbox.',
    isOptional: true,
  },
]}
/>

## Propriétés {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "Identifiant de l'instance du Sandbox.",
  },
  {
    name: 'name',
    type: 'string',
    description: "'BlaxelSandbox'",
  },
  {
    name: 'provider',
    type: 'string',
    description: "'blaxel'",
  },
  {
    name: 'status',
    type: 'ProviderStatus',
    description: "'pending' | 'initializing' | 'running' | 'stopped' | 'error'",
  },
  {
    name: 'instance',
    type: 'SandboxInstance',
    description:
      "SandboxInstance Blaxel sous-jacente permettant d'accéder directement aux API Blaxel. Lève SandboxNotReadyError si le Sandbox n'a pas été démarré.",
  },
  {
    name: 'processes',
    type: 'BlaxelProcessManager',
    description:
      'Gestionnaire des processus en arrière-plan. Consultez la [référence de SandboxProcessManager](/reference/workspace/process-manager).',
  },
]}
/>

## Processus en arrière-plan {/* #background-processes */}

`BlaxelSandbox` comprend un gestionnaire de processus intégré permettant de lancer et de gérer des processus en arrière-plan.

```typescript
const sandbox = new BlaxelSandbox({ id: 'dev-sandbox' })
await sandbox.start()

// Spawn a background process
const handle = await sandbox.processes.spawn('node server.js', {
  env: { PORT: '3000' },
  onStdout: data => console.log(data),
})

// Interact with the process
console.log(handle.stdout)
await handle.kill()
```

:::note
Les Sandboxes Blaxel ne prennent pas en charge stdin. L'appel à `handle.sendStdin()` lève une erreur.
:::

Consultez la [référence de `SandboxProcessManager`](/reference/workspace/process-manager) pour découvrir l'API complète.

## Montage d'un stockage cloud {/* #mounting-cloud-storage */}

Les Sandboxes Blaxel peuvent monter des systèmes de fichiers S3 ou GCS, ce qui rend le stockage cloud accessible sous la forme d'un répertoire local dans le Sandbox.

### S3 {/* #s3 */}

```typescript
import { Workspace } from '@mastra/core/workspace'
import { S3Filesystem } from '@mastra/s3'
import { BlaxelSandbox } from '@mastra/blaxel'

const workspace = new Workspace({
  mounts: {
    '/data': new S3Filesystem({
      bucket: 'my-bucket',
      region: 'us-east-1',
      accessKeyId: process.env.AWS_ACCESS_KEY_ID,
      secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY,
    }),
  },
  sandbox: new BlaxelSandbox(),
})
```

### GCS {/* #gcs */}

```typescript
import { Workspace } from '@mastra/core/workspace'
import { GCSFilesystem } from '@mastra/gcs'
import { BlaxelSandbox } from '@mastra/blaxel'

const workspace = new Workspace({
  mounts: {
    '/data': new GCSFilesystem({
      bucket: 'my-bucket',
      credentials: JSON.parse(process.env.GCS_SERVICE_ACCOUNT_KEY!),
    }),
  },
  sandbox: new BlaxelSandbox(),
})
```
