---
title: "Référence : VercelSandbox | Workspace"
description: "Référence de l’API de VercelSandbox, un fournisseur de sandbox reposant sur le produit de microVM Firecracker éphémère Vercel Sandbox."
packages:
  - "@mastra/vercel"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# VercelSandbox

Exécute des commandes dans [Vercel Sandbox](https://vercel.com/docs/vercel-sandbox), une microVM [Firecracker](https://firecracker-microvm.github.io/) éphémère exécutant Amazon Linux 2023. Fournit un système de fichiers persistant pendant la session, un accès `sudo`, des ports exposés et des processus en arrière-plan. Pour en savoir plus sur l’interface, consultez l’[interface WorkspaceSandbox](/reference/workspace/sandbox).

:::note

Cette solution est distincte de [`VercelServerlessSandbox`](/reference/workspace/vercel-serverless), qui exécute les commandes sous forme de **fonctions** serverless Vercel sans état. `VercelSandbox` exécute une microVM Linux complète avec un système de fichiers persistant et des processus de longue durée.

:::

## Installation {/* #installation */}

```bash npm2yarn
npm install @mastra/vercel
```

## Authentification {/* #authentication */}

Le SDK `@vercel/sandbox` utilise automatiquement un jeton OIDC Vercel lorsqu’aucun identifiant explicite n’est fourni. Si vous fournissez `token`, `teamId` ou `projectId`, fournissez les trois valeurs ensemble.

<Tabs>
  <TabItem value="oidc" label="OIDC (recommandé)">
    Pour le développement local, liez le projet et récupérez un jeton de développement :

    ```bash
    vercel link
    vercel env pull
    ```

    Sur Vercel, l’authentification est gérée automatiquement ; aucune configuration n’est nécessaire.
  </TabItem>

  <TabItem value="env-file" label="Jeton d’accès (.env)">
    Pour les environnements sans OIDC, fournissez les trois valeurs ensemble :

    ```bash
    VERCEL_TOKEN=your-token
    VERCEL_TEAM_ID=your-team-id
    VERCEL_PROJECT_ID=your-project-id
    ```
  </TabItem>

  <TabItem value="constructor" label="Constructeur">
    ```typescript
    new VercelSandbox({
      token: 'your-token',
      teamId: 'your-team-id',
      projectId: 'your-project-id',
    })
    ```
  </TabItem>
</Tabs>

## Utilisation {/* #usage */}

Ajoutez un `VercelSandbox` à un workspace et assignez-le à un agent :

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { VercelSandbox } from '@mastra/vercel'

const workspace = new Workspace({
  sandbox: new VercelSandbox({
    runtime: 'node24',
    timeout: 600_000,
  }),
})

const agent = new Agent({
  id: 'code-agent',
  name: 'Code Agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})

const response = await agent.generate('Print "Hello, world!" and show the Node.js version.')

console.log(response.text)
```

### Ressources et ports exposés {/* #resources-and-exposed-ports */}

Allouez des vCPU (2 048 Mo de mémoire par vCPU) et exposez des ports pour atteindre les services réseau exécutés dans la sandbox :

```typescript
const sandbox = new VercelSandbox({
  runtime: 'node24',
  resources: { vcpus: 4 },
  ports: [3000],
})

const workspace = new Workspace({ sandbox })
await sandbox.start()

// The public HTTPS domain for an exposed port is available via getInfo()
const { metadata } = sandbox.getInfo()
console.log(metadata?.domains) // { 3000: 'https://....vercel.run' }
```

### Sortie en streaming {/* #streaming-output */}

Diffusez la sortie des commandes en temps réel via les rappels `onStdout` et `onStderr` :

```typescript
await sandbox.executeCommand('sh', ['-c', 'for i in 1 2 3; do echo "line $i"; sleep 1; done'], {
  onStdout: chunk => process.stdout.write(chunk),
  onStderr: chunk => process.stderr.write(chunk),
})
```

Les deux rappels sont facultatifs et peuvent être utilisés indépendamment.

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique de cette instance de sandbox.',
    isOptional: true,
    defaultValue: 'Généré automatiquement',
  },
  {
    name: 'sandboxName',
    type: 'string',
    description: 'Nom facultatif transmis à l’API Vercel. Généré automatiquement s’il est omis.',
    isOptional: true,
  },
  {
    name: 'token',
    type: 'string',
    description: 'Jeton de l’API Vercel. Utilise à défaut la variable d’environnement VERCEL_TOKEN. Omettez-le pour utiliser le jeton OIDC.',
    isOptional: true,
  },
  {
    name: 'teamId',
    type: 'string',
    description: 'ID de l’équipe Vercel. Utilise à défaut la variable d’environnement VERCEL_TEAM_ID.',
    isOptional: true,
  },
  {
    name: 'projectId',
    type: 'string',
    description: 'ID du projet Vercel. Utilise à défaut la variable d’environnement VERCEL_PROJECT_ID.',
    isOptional: true,
  },
  {
    name: 'runtime',
    type: "'node24' | 'node22' | 'node26' | 'python3.13'",
    description: 'Environnement d’exécution de la sandbox.',
    isOptional: true,
    defaultValue: "'node24'",
  },
  {
    name: 'timeout',
    type: 'number',
    description: 'Délai en millisecondes avant l’arrêt automatique de la sandbox.',
    isOptional: true,
    defaultValue: '300000 (5 minutes)',
  },
  {
    name: 'resources',
    type: '{ vcpus?: number }',
    description: 'Allocation des ressources. Chaque vCPU dispose de 2 048 Mo de mémoire.',
    isOptional: true,
  },
  {
    name: 'ports',
    type: 'number[]',
    description:
      'Ports à exposer depuis la sandbox (jusqu’à 15). Les domaines HTTPS publics sont disponibles via getInfo().metadata.domains.',
    isOptional: true,
  },
  {
    name: 'env',
    type: 'Record<string, string>',
    description: 'Variables d’environnement par défaut héritées par toutes les commandes.',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'Métadonnées personnalisées exposées via getInfo().',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'instructions',
    type: 'string | ((opts) => string)',
    description:
      'Remplace les instructions par défaut renvoyées par getInstructions(). Fournissez une chaîne pour les remplacer ou une fonction pour étendre les valeurs par défaut.',
    isOptional: true,
  },
  {
    name: 'onStart',
    type: 'SandboxLifecycleHook',
    description: 'Hook de cycle de vie appelé lorsque la sandbox atteint l’état `running`.',
    isOptional: true,
  },
  {
    name: 'onStop',
    type: 'SandboxLifecycleHook',
    description: 'Hook de cycle de vie appelé avant l’arrêt de la sandbox.',
    isOptional: true,
  },
  {
    name: 'onDestroy',
    type: 'SandboxLifecycleHook',
    description: 'Hook de cycle de vie appelé avant la destruction de la sandbox.',
    isOptional: true,
  },
]}
/>

## Propriétés {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant de l’instance de sandbox.',
  },
  {
    name: 'name',
    type: "'VercelSandbox'",
    description: 'Nom lisible par les humains.',
  },
  {
    name: 'provider',
    type: "'vercel-sandbox'",
    description: 'Identifiant du type de fournisseur.',
  },
  {
    name: 'status',
    type: 'ProviderStatus',
    description: "'pending' | 'starting' | 'running' | 'stopping' | 'stopped' | 'destroying' | 'destroyed' | 'error'",
  },
  {
    name: 'sandbox',
    type: 'Sandbox',
    description:
      'L’instance Sandbox sous-jacente de @vercel/sandbox. Déclenche SandboxNotReadyError si la sandbox n’a pas été démarrée.',
  },
  {
    name: 'processes',
    type: 'VercelSandboxProcessManager',
    description:
      'Gestionnaire des processus en arrière-plan. Consultez la [référence de SandboxProcessManager](/reference/workspace/process-manager).',
  },
]}
/>

## Processus en arrière-plan {/* #background-processes */}

`VercelSandbox` inclut un gestionnaire permettant de lancer et de gérer des processus en arrière-plan. Chaque processus lancé s’exécute sous la forme d’une commande détachée dans la microVM, dont la sortie est diffusée dans les journaux de commandes.

```typescript
const sandbox = new VercelSandbox({ runtime: 'node24', ports: [3000] })
await sandbox.start()

const handle = await sandbox.processes.spawn('node server.js', {
  env: { PORT: '3000' },
  onStdout: data => console.log(data),
})

console.log(handle.stdout)
await handle.kill()
```

Consultez la [référence de `SandboxProcessManager`](/reference/workspace/process-manager) pour l’API complète.

:::note

Le SDK Vercel Sandbox n’expose pas de canal stdin pour les commandes en cours d’exécution ; `handle.sendStdin()` déclenche donc une erreur. Le montage du système de fichiers (FUSE) n’est pas non plus pris en charge par ce fournisseur.

:::

## Limites {/* #limits */}

- Jusqu’à 32 vCPU, avec 2 048 Mo de mémoire par vCPU.
- Jusqu’à 15 ports exposés.
- Le système de fichiers est éphémère. Il persiste uniquement pendant la session et est perdu lorsque la sandbox s’arrête.
- La durée d’exécution maximale dépend de l’offre (45 minutes avec Hobby, jusqu’à 24 heures avec Pro et Enterprise) ; la valeur par défaut est de 5 minutes.

Consultez la [documentation Vercel Sandbox](https://vercel.com/docs/vercel-sandbox) pour connaître les limites et les tarifs actuels.
