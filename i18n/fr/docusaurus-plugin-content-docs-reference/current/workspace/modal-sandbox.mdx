---
title: "Référence : ModalSandbox | Workspace"
description: "Documentation du Provider ModalSandbox pour l'exécution isolée de code dans le cloud avec Modal."
packages:
  - "@mastra/modal"
---

# ModalSandbox

Exécute des commandes dans des sandboxes cloud [Modal](https://modal.com) isolées. Fournit des environnements sécurisés et éphémères reposant sur l'infrastructure de Modal. Pour plus de détails sur l'interface, consultez l'[interface WorkspaceSandbox](/reference/workspace/sandbox).

## Installation {/* #installation */}

```bash npm2yarn
npm install @mastra/modal
```

## Utilisation {/* #usage */}

Ajoutez une `ModalSandbox` à un Workspace et attribuez-la à un agent :

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { ModalSandbox } from '@mastra/modal'

const workspace = new Workspace({
  sandbox: new ModalSandbox({
    id: 'dev-sandbox',
    baseImage: 'ubuntu:22.04',
    timeoutMs: 60_000,
  }),
})

const agent = new Agent({
  id: 'dev-agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  workspace,
})
```

## Authentification {/* #authentication */}

Définissez les identifiants Modal à l'aide de variables d'environnement ou des options du constructeur :

```bash
MODAL_TOKEN_ID=ak-...
MODAL_TOKEN_SECRET=as-...
```

Ou transmettez-les directement :

```typescript
const sandbox = new ModalSandbox({
  tokenId: process.env.MODAL_TOKEN_ID,
  tokenSecret: process.env.MODAL_TOKEN_SECRET,
})
```

Obtenez vos identifiants depuis le [tableau de bord Modal](https://modal.com/settings/tokens).

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description:
      "Identifiant ou nom unique de cette sandbox. Utilisé comme nom de sandbox Modal afin de pouvoir s'y reconnecter lors des appels ultérieurs à start().",
    isOptional: true,
    defaultValue: 'Généré automatiquement',
  },
  {
    name: 'appName',
    type: 'string',
    description: "Nom de l'application Modal à associer aux sandboxes.",
    isOptional: true,
    defaultValue: "'mastra'",
  },
  {
    name: 'baseImage',
    type: 'string',
    description: 'Image Docker à utiliser pour la sandbox.',
    isOptional: true,
    defaultValue: "'ubuntu:22.04'",
  },
  {
    name: 'timeoutMs',
    type: 'number',
    description:
      "Durée de vie maximale en temps réel, en millisecondes. La sandbox est arrêtée lorsque ce délai expire, quelle que soit son activité. Le maximum autorisé par Modal est de 24 heures (86_400_000).",
    isOptional: true,
    defaultValue: '300000 (5 minutes)',
  },
  {
    name: 'env',
    type: 'Record<string, string>',
    description: "Variables d'environnement intégrées à la sandbox lors de sa création.",
    isOptional: true,
  },
  {
    name: 'workdir',
    type: 'string',
    description: 'Répertoire de travail par défaut dans la sandbox.',
    isOptional: true,
  },
  {
    name: 'tokenId',
    type: 'string',
    description: "Identifiant du jeton Modal. Utilise la variable d'environnement MODAL_TOKEN_ID à défaut.",
    isOptional: true,
  },
  {
    name: 'tokenSecret',
    type: 'string',
    description: "Secret du jeton Modal. Utilise la variable d'environnement MODAL_TOKEN_SECRET à défaut.",
    isOptional: true,
  },
  {
    name: 'instructions',
    type: 'string | function',
    description:
      'Instructions personnalisées renvoyées par getInstructions(). Transmettez une chaîne pour remplacer entièrement les instructions par défaut, ou une fonction pour les étendre.',
    isOptional: true,
  },
  {
    name: 'onStart',
    type: 'function',
    description: "Hook de cycle de vie appelé une fois que la sandbox atteint l'état d'exécution.",
    isOptional: true,
  },
  {
    name: 'onStop',
    type: 'function',
    description: "Hook de cycle de vie appelé avant l'arrêt de la sandbox.",
    isOptional: true,
  },
  {
    name: 'onDestroy',
    type: 'function',
    description: 'Hook de cycle de vie appelé avant la destruction de la sandbox.',
    isOptional: true,
  },
]}
/>

## Propriétés {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "Identifiant de l'instance de sandbox.",
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom du Provider ('ModalSandbox')",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Identifiant du Provider ('modal')",
  },
  {
    name: 'status',
    type: 'ProviderStatus',
    description: "'pending' | 'starting' | 'running' | 'stopping' | 'stopped' | 'destroying' | 'destroyed' | 'error'",
  },
  {
    name: 'modal',
    type: 'Sandbox',
    description:
      "Instance Modal Sandbox sous-jacente. Lève SandboxNotReadyError si la sandbox n'a pas été démarrée.",
  },
  {
    name: 'processes',
    type: 'ModalProcessManager',
    description:
      "Gestionnaire de processus d'arrière-plan. Consultez la [référence de SandboxProcessManager](/reference/workspace/process-manager).",
  },
]}
/>

## Cycle de vie de la sandbox {/* #sandbox-lifecycle */}

- **`_start()`** : tente de se reconnecter à une sandbox en cours d'exécution. Si aucune n'est trouvée, crée une sandbox à partir du snapshot le plus récent (s'il en existe un provenant d'un précédent appel à `_stop()`), ou à partir de baseImage.
- **`_stop()`** : crée un snapshot du système de fichiers, puis arrête la sandbox. Le snapshot est conservé en mémoire dans la même instance pour les prochains démarrages.
- **`_destroy()`** : arrête la sandbox et supprime tout snapshot.

```typescript
const sandbox = new ModalSandbox({
  id: 'dev-sandbox',
  baseImage: 'ubuntu:22.04',
  timeoutMs: 300_000,
})

await sandbox._start()
await sandbox.processes.spawn('npm install')
await sandbox._stop()
await sandbox._start()
```

## Processus d'arrière-plan {/* #background-processes */}

`ModalSandbox` comprend un gestionnaire de processus intégré permettant de lancer et de gérer des processus d'arrière-plan. Chaque appel à `spawn()` crée un nouveau `ContainerProcess` par l'intermédiaire de l'API `Sandbox.exec()` du SDK Modal.

```typescript
const sandbox = new ModalSandbox({ id: 'dev-sandbox' })
await sandbox._start()

// Spawn a background process
const handle = await sandbox.processes.spawn('node script.js', {
  env: { PORT: '3000' },
  onStdout: data => console.log(data),
})

// Wait for the process to complete
const result = await handle.wait()
console.log(result.exitCode)

// Kill the process
await handle.kill()
```

:::note

`sendStdin()` n'est pas pris en charge. Le SDK JS de Modal n'expose pas stdin sur `Sandbox.exec()`.

:::

Consultez la [référence de `SandboxProcessManager`](/reference/workspace/process-manager) pour découvrir l'API complète.

## Ressources associées {/* #related */}

- [Référence de SandboxProcessManager](/reference/workspace/process-manager)
- [Interface WorkspaceSandbox](/reference/workspace/sandbox)
- [Référence d'E2BSandbox](/reference/workspace/e2b-sandbox)
- [Référence de DaytonaSandbox](/reference/workspace/daytona-sandbox)
- [Présentation de Workspace](/docs/workspace/overview)
