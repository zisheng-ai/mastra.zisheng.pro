---
title: "Référence : AzureBlobFilesystem | Workspace"
description: "Documentation du Provider AzureBlobFilesystem pour Azure Blob Storage."
packages:
  - "@mastra/azure"
---

# AzureBlobFilesystem

Stocke les fichiers dans des conteneurs Azure Blob Storage. Pour en savoir plus sur l'interface, consultez l'[interface WorkspaceFilesystem](/reference/workspace/filesystem).

## Installation {/* #installation */}

```bash npm2yarn
npm install @mastra/azure
```

## Utilisation {/* #usage */}

Ajoutez un `AzureBlobFilesystem` à un Workspace et attribuez-le à un agent :

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const workspace = new Workspace({
  filesystem: new AzureBlobFilesystem({
    container: 'my-container',
    connectionString: process.env.AZURE_STORAGE_CONNECTION_STRING,
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  workspace,
})
```

### Clé de compte {/* #account-key */}

Utilisez un nom de compte et une clé de compte si vous ne souhaitez pas transmettre une chaîne de connexion complète :

```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  accountKey: process.env.AZURE_STORAGE_ACCOUNT_KEY,
})
```

### Token de signature d'accès partagé {/* #shared-access-signature-token */}

Utilisez un token de signature d'accès partagé (SAS) avec `accountName` ou `endpoint` :

```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  sasToken: process.env.AZURE_STORAGE_SAS_TOKEN,
})
```

### DefaultAzureCredential {/* #defaultazurecredential */}

Utilisez `DefaultAzureCredential` lorsque votre environnement fournit des identifiants Azure Identity :

```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  useDefaultCredential: true,
})
```

Installez `@azure/identity` lorsque vous utilisez `DefaultAzureCredential` :

```bash npm2yarn
npm install @azure/identity
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'container',
    type: 'string',
    description: 'Nom du conteneur Azure Blob Storage',
  },
  {
    name: 'connectionString',
    type: 'string',
    description: 'Chaîne de connexion Azure Storage complète. Prévaut sur les autres options d’authentification.',
    isOptional: true,
  },
  {
    name: 'accountName',
    type: 'string',
    description: 'Nom du compte Azure Storage. Requis sauf si vous utilisez connectionString ou endpoint.',
    isOptional: true,
  },
  {
    name: 'accountKey',
    type: 'string',
    description: 'Clé du compte Azure Storage.',
    isOptional: true,
  },
  {
    name: 'sasToken',
    type: 'string',
    description: 'Token de signature d’accès partagé. Nécessite accountName ou endpoint.',
    isOptional: true,
  },
  {
    name: 'useDefaultCredential',
    type: 'boolean',
    description: 'Utilise DefaultAzureCredential fourni par @azure/identity.',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'endpoint',
    type: 'string',
    description: 'URL personnalisée du point de terminaison du service Blob. Utilisée pour le développement local avec Azurite.',
    isOptional: true,
  },
  {
    name: 'prefix',
    type: 'string',
    description: 'Préfixe facultatif pour toutes les clés (agit comme un sous-répertoire).',
    isOptional: true,
  },
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique de cette instance de système de fichiers.',
    isOptional: true,
    defaultValue: 'Généré automatiquement',
  },
  {
    name: 'displayName',
    type: 'string',
    description: 'Nom d’affichage lisible dans l’interface utilisateur.',
    isOptional: true,
  },
  {
    name: 'icon',
    type: 'FilesystemIcon',
    description: 'Identifiant de l’icône dans l’interface utilisateur.',
    isOptional: true,
  },
  {
    name: 'description',
    type: 'string',
    description: 'Courte description de ce système de fichiers dans l’interface utilisateur.',
    isOptional: true,
  },
  {
    name: 'readOnly',
    type: 'boolean',
    description: 'Lorsque cette valeur est true, toutes les opérations d’écriture sont bloquées.',
    isOptional: true,
    defaultValue: 'false',
  },
]}
/>

## Propriétés {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant de l’instance du système de fichiers.',
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom du Provider ('AzureBlobFilesystem').",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Identifiant du Provider ('azure-blob').",
  },
  {
    name: 'readOnly',
    type: 'boolean | undefined',
    description: 'Indique si le système de fichiers est en mode lecture seule.',
  },
]}
/>

## Méthodes {/* #methods */}

AzureBlobFilesystem implémente l'[interface WorkspaceFilesystem](/reference/workspace/filesystem) et fournit toutes les méthodes standard d'un système de fichiers :

- `readFile(path, options?)` : lit le contenu d'un fichier
- `writeFile(path, content, options?)` : écrit du contenu dans un fichier
- `appendFile(path, content)` : ajoute du contenu à un fichier
- `deleteFile(path, options?)` : supprime un fichier
- `copyFile(src, dest, options?)` : copie un fichier
- `moveFile(src, dest, options?)` : déplace ou renomme un fichier
- `mkdir(path, options?)` : crée un répertoire
- `rmdir(path, options?)` : supprime un répertoire
- `readdir(path, options?)` : répertorie le contenu d'un répertoire
- `exists(path)` : vérifie si un chemin existe
- `stat(path)` : récupère les métadonnées d'un fichier ou d'un répertoire

### `init()` {/* #init */}

Initialise le système de fichiers. Vérifie l'accès au conteneur et les identifiants.

```typescript
await filesystem.init()
```

### `getInfo()` {/* #getinfo */}

Renvoie les métadonnées de cette instance de système de fichiers.

```typescript
const info = filesystem.getInfo()
// { id: '...', name: 'AzureBlobFilesystem', provider: 'azure-blob', status: 'ready' }
```

### `getContainer()` {/* #getcontainer */}

Renvoie le `ContainerClient` Azure Blob Storage sous-jacent.

```typescript
const container = await filesystem.getContainer()
```

### `getMountConfig()` {/* #getmountconfig */}

Renvoie la configuration de montage des Providers de Sandbox qui prennent en charge les montages de systèmes de fichiers Azure Blob.

```typescript
const config = filesystem.getMountConfig()
// { type: 'azure-blob', container: 'my-container', ... }
```

:::note

Le montage d'Azure Blob dans une Sandbox dépend de la prise en charge des configurations de montage `azure-blob` par la Sandbox. Utilisez `filesystem` pour les opérations directes sur les fichiers du Workspace lorsque votre Provider de Sandbox ne prend pas en charge les montages Azure Blob.

:::

## Ressources associées {/* #related */}

- [Interface WorkspaceFilesystem](/reference/workspace/filesystem)
- [Référence S3Filesystem](/reference/workspace/s3-filesystem)
- [Référence GCSFilesystem](/reference/workspace/gcs-filesystem)
- [Présentation du Workspace](/docs/workspace/overview)
