---
title: "Référence : AppleContainerSandbox | Workspace"
description: "Référence de l’API AppleContainerSandbox, un Sandbox local basé sur l’interface en ligne de commande de conteneurs Apple pour l’exécution de commandes dans Workspace."
packages:
  - "@mastra/apple-container"
---

# AppleContainerSandbox

Exécute des commandes dans des conteneurs Linux OCI locaux via l’interface en ligne de commande [`container`](https://github.com/apple/container) d’Apple. Le Provider démarre un conteneur de longue durée et utilise `container exec` pour les commandes de Workspace. Pour en savoir plus sur l’interface, consultez [l’interface WorkspaceSandbox](/reference/workspace/sandbox).

## Installation {/* #installation */}

```bash npm2yarn
npm install @mastra/apple-container
```

Nécessite un Mac Apple silicon exécutant macOS 26 ou une version ultérieure, avec l’interface en ligne de commande `container` d’Apple installée. Démarrez le système de conteneurs avant d’utiliser le Provider :

```bash
container system start
```

## Utilisation {/* #usage */}

Ajoutez un `AppleContainerSandbox` à un Workspace et attribuez-le à un Agent :

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { AppleContainerSandbox } from '@mastra/apple-container'

const workspace = new Workspace({
  sandbox: new AppleContainerSandbox({
    image: 'node:22-slim',
    volumes: {
      '/Users/me/project': '/workspace',
    },
    workingDir: '/workspace',
  }),
})

const agent = new Agent({
  id: 'dev-agent',
  name: 'Dev Agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})

const response = await agent.generate('Run `node --version`.')
console.log(response.text)
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique de cette instance de Sandbox.',
    isOptional: true,
    defaultValue: 'Généré automatiquement',
  },
  {
    name: 'name',
    type: 'string',
    description:
      'Nom du conteneur Apple transmis à `container run --name`. Les caractères hors de `[a-zA-Z0-9_.-]` sont remplacés par `-` et le résultat reçoit un préfixe s’il ne commence pas par un caractère alphanumérique.',
    isOptional: true,
    defaultValue: 'L’`id` du Sandbox',
  },
  {
    name: 'image',
    type: 'string',
    description: 'Image OCI à utiliser pour le conteneur.',
    isOptional: true,
    defaultValue: "'node:22-slim'",
  },
  {
    name: 'command',
    type: 'string[]',
    description: 'Commande d’initialisation du conteneur. Elle doit maintenir le conteneur actif pour l’exécution de commandes basée sur exec.',
    isOptional: true,
    defaultValue: "['sleep', 'infinity']",
  },
  {
    name: 'env',
    type: 'Record<string, string>',
    description: 'Variables d’environnement à définir dans le conteneur et lors des exécutions de commandes.',
    isOptional: true,
  },
  {
    name: 'volumes',
    type: 'Record<string, string>',
    description: 'Montages liés de l’hôte vers le conteneur. Les clés sont des chemins hôte, les valeurs sont des chemins de conteneur.',
    isOptional: true,
  },
  {
    name: 'mounts',
    type: 'string[]',
    description: 'Spécifications brutes de `container run --mount`.',
    isOptional: true,
  },
  {
    name: 'network',
    type: 'string',
    description: 'Spécification d’attachement réseau du conteneur Apple.',
    isOptional: true,
  },
  {
    name: 'publishedPorts',
    type: 'string[]',
    description: 'Spécifications de publication de ports transmises avec `--publish`.',
    isOptional: true,
  },
  {
    name: 'publishedSockets',
    type: 'string[]',
    description: 'Spécifications de publication de sockets transmises avec `--publish-socket`.',
    isOptional: true,
  },
  {
    name: 'cpus',
    type: 'number | string',
    description: 'Nombre de processeurs à allouer.',
    isOptional: true,
  },
  {
    name: 'memory',
    type: 'string',
    description: "Allocation de mémoire, par exemple '1G'.",
    isOptional: true,
  },
  {
    name: 'platform',
    type: 'string',
    description: "Plateforme OCI, par exemple 'linux/arm64'.",
    isOptional: true,
  },
  {
    name: 'arch',
    type: 'string',
    description: 'Architecture de l’image lors de la sélection d’une image multi-architecture.',
    isOptional: true,
  },
  {
    name: 'os',
    type: 'string',
    description: 'Système d’exploitation de l’image lors de la sélection d’une image multiplateforme.',
    isOptional: true,
  },
  {
    name: 'rosetta',
    type: 'boolean',
    description: 'Active Rosetta dans le conteneur.',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'readonlyRootfs',
    type: 'boolean',
    description: 'Monte le système de fichiers racine du conteneur en lecture seule.',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'ssh',
    type: 'boolean',
    description: 'Transmet le socket de l’agent SSH de l’hôte.',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'init',
    type: 'boolean',
    description: "Active le processus init d’Apple dans le conteneur.",
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'virtualization',
    type: 'boolean',
    description: 'Expose les capacités de virtualisation au conteneur.',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'capAdd',
    type: 'string[]',
    description: 'Capacités Linux à ajouter.',
    isOptional: true,
  },
  {
    name: 'capDrop',
    type: 'string[]',
    description: 'Capacités Linux à supprimer.',
    isOptional: true,
  },
  {
    name: 'tmpfs',
    type: 'string[]',
    description: 'Chemins de destination tmpfs transmis avec `--tmpfs`, par exemple `/tmp`.',
    isOptional: true,
  },
  {
    name: 'dns',
    type: 'string[]',
    description: 'Adresses IP des serveurs de noms DNS.',
    isOptional: true,
  },
  {
    name: 'dnsSearch',
    type: 'string[]',
    description: 'Domaines de recherche DNS.',
    isOptional: true,
  },
  {
    name: 'noDns',
    type: 'boolean',
    description: 'Ne configure pas DNS dans le conteneur.',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'labels',
    type: 'Record<string, string>',
    description: 'Étiquettes de conteneur supplémentaires. Les étiquettes Mastra (mastra.sandbox, mastra.sandbox.id) sont toujours incluses.',
    isOptional: true,
  },
  {
    name: 'workingDir',
    type: 'string',
    description: 'Répertoire de travail dans le conteneur.',
    isOptional: true,
    defaultValue: "'/workspace'",
  },
  {
    name: 'timeout',
    type: 'number',
    description: 'Délai d’expiration par défaut des commandes, en millisecondes.',
    isOptional: true,
    defaultValue: '300000 (5 minutes)',
  },
  {
    name: 'deleteOnDestroy',
    type: 'boolean',
    description:
      'Supprime le conteneur Apple lorsque le Sandbox est détruit. Lorsque cette valeur est false, destroy arrête le conteneur à la place.',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'containerBinary',
    type: 'string',
    description: 'Chemin ou nom de l’interface en ligne de commande de conteneurs Apple.',
    isOptional: true,
    defaultValue: "'container'",
  },
  {
    name: 'instructions',
    type: 'string | function',
    description:
      'Instructions personnalisées qui remplacent les instructions par défaut renvoyées par getInstructions(). Transmettez une chaîne vide pour supprimer les instructions.',
    isOptional: true,
  },
]}
/>

## Propriétés {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant de l’instance de Sandbox.',
  },
  {
    name: 'name',
    type: 'string',
    description: "Nom du Provider ('AppleContainerSandbox').",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Identifiant du Provider ('apple-container').",
  },
  {
    name: 'status',
    type: 'ProviderStatus',
    description: "'pending' | 'starting' | 'running' | 'stopping' | 'stopped' | 'destroying' | 'destroyed' | 'error'",
  },
  {
    name: 'containerId',
    type: 'string',
    description: 'ID du conteneur Apple lorsqu’il est connu ; sinon, nom de conteneur configuré.',
  },
]}
/>

## Variables d’environnement {/* #environment-variables */}

Définissez des variables d’environnement au niveau du conteneur avec `env`. Des variables d’environnement par commande peuvent également être transmises via les options de `executeCommand` :

```typescript
const sandbox = new AppleContainerSandbox({
  image: 'node:22-slim',
  env: {
    NODE_ENV: 'development',
  },
})

await sandbox.executeCommand('node', ['-e', 'console.log(process.env.TASK_ID)'], {
  env: { TASK_ID: '42' },
})
```

## Montages liés {/* #bind-mounts */}

Montez des répertoires hôte dans le conteneur à l’aide de l’option `volumes` :

```typescript
const sandbox = new AppleContainerSandbox({
  image: 'node:22-slim',
  volumes: {
    '/Users/me/project': '/workspace/project',
    '/Users/me/.npm': '/root/.npm',
  },
})
```

Les montages liés sont appliqués lors de la création du conteneur. Les chemins hôte doivent exister avant le démarrage du Sandbox.

## Options de ressources et de plateforme {/* #resource-and-platform-options */}

Les options de l’interface en ligne de commande de conteneurs Apple peuvent être transmises au constructeur :

```typescript
const sandbox = new AppleContainerSandbox({
  image: 'node:22-slim',
  volumes: {
    '/Users/me/project': '/workspace',
  },
  cpus: 2,
  memory: '2G',
  platform: 'linux/arm64',
  readonlyRootfs: true,
  tmpfs: ['/tmp'],
})
```

Ces options ne sont appliquées que lors de la création d’un nouveau conteneur. Si le Sandbox se reconnecte à un conteneur existant portant le même nom, détruisez puis recréez le Sandbox pour appliquer les options d’exécution modifiées.
L’option `--tmpfs` d’Apple n’accepte que les chemins de conteneur, tels que `/tmp` ; elle n’accepte pas les spécifications d’options de style Docker comme `/tmp:rw,size=256m`.
Lorsque `readonlyRootfs` est activé, assurez-vous que `workingDir` pointe vers un chemin fourni par l’image ou par un montage lié. Un tmpfs accessible en écriture est également pris en charge.

## Modèle de sécurité {/* #security-model */}

`AppleContainerSandbox` exécute des conteneurs locaux via le service `container` Apple de l’hôte. Considérez les options du constructeur comme une configuration côté serveur de confiance :

- `volumes`, `mounts` et `publishedSockets` peuvent exposer des chemins hôte au code conteneurisé.
- `publishedPorts` peut exposer des services du conteneur sur l’hôte ou le réseau. Liez-les à `127.0.0.1` lorsque seul un accès local est prévu.
- `ssh` transmet le socket de l’agent SSH de l’hôte.
- `capAdd` et `virtualization` peuvent étendre les capacités du code conteneurisé.
- `containerBinary` est une porte de sortie réservée au constructeur pour du code de confiance et ne fait pas partie du schéma sérialisable du Provider d’éditeur.

Utilisez les montages et capacités les plus restreints dont votre charge de travail a besoin. Les conteneurs existants ne sont reconnectés que s’ils portent des étiquettes Mastra de propriété correspondant à l’ID du Sandbox. Les conteneurs créés par ce Provider incluent également une étiquette de hachage de configuration ; lorsque cette étiquette est présente, la reconnexion échoue si des options d’exécution immuables, telles que l’image, la commande, les montages, les ports, les capacités ou le répertoire de travail, ont changé.

## Limitations {/* #limitations */}

`AppleContainerSandbox` implémente l’exécution de commandes Workspace au premier plan avec `executeCommand()`. Il n’expose pas encore de `SandboxProcessManager` pour les processus d’arrière-plan ou les sessions LSP.

Les délais d’expiration des commandes sont appliqués à l’intérieur du conteneur afin que les commandes expirées soient nettoyées par l’environnement d’exécution du conteneur. Les signaux d’abandon annulent le chemin d’attente de l’interface en ligne de commande de l’hôte et ne doivent pas remplacer les délais d’expiration des commandes lorsque le nettoyage dans le conteneur est important.

## Reconnexion {/* #reconnection */}

`AppleContainerSandbox` se reconnecte en inspectant un conteneur portant le nom configuré. Lorsque `start()` est appelé :

- Un conteneur en cours d’exécution est réutilisé.
- Un conteneur arrêté est redémarré.
- Un conteneur absent est créé à partir de l’image configurée.
- Un conteneur portant le nom configuré, mais dépourvu d’étiquettes Mastra de propriété correspondantes, échoue au lieu d’être géré.
- Un conteneur appartenant à Mastra dont l’étiquette de hachage de configuration ne correspond pas aux options d’exécution immuables échoue au lieu d’être réutilisé.

```typescript
const sandbox = new AppleContainerSandbox({ id: 'persistent-sandbox' })
await sandbox.start()

const sandbox2 = new AppleContainerSandbox({ id: 'persistent-sandbox' })
await sandbox2.start()
```

## Provider d’éditeur {/* #editor-provider */}

Enregistrez le Provider auprès de `MastraEditor` pour hydrater les configurations de Sandbox stockées :

```typescript
import { MastraEditor } from '@mastra/editor'
import { appleContainerSandboxProvider } from '@mastra/apple-container'

const editor = new MastraEditor({
  sandboxes: {
    [appleContainerSandboxProvider.id]: appleContainerSandboxProvider,
  },
})
```

## Pages associées {/* #related */}

- [Interface WorkspaceSandbox](/reference/workspace/sandbox)
- [Référence DockerSandbox](/reference/workspace/docker-sandbox)
- [Référence LocalSandbox](/reference/workspace/local-sandbox)
- [Présentation de Workspace](/docs/workspace/overview)
