---
title: "Référence : Agent.stream() | Streaming"
description: "Documentation de la méthode `Agent.stream()` des agents Mastra, qui permet de diffuser les réponses en temps réel avec des fonctionnalités avancées."
packages:
  - "@mastra/ai-sdk"
---

import { MODEL_SETTINGS_OBJECT } from "@site/src/components/ModelSettingsProperties";

# Agent.stream()

La méthode `.stream()` permet de diffuser en temps réel les réponses d’un agent, avec des fonctionnalités avancées et une grande souplesse de format. Cette méthode accepte des messages et des options de streaming facultatives, offrant une expérience de streaming moderne compatible à la fois avec le format natif de Mastra et avec AI SDK v5+.

## Exemple d’utilisation {/* #usage-example */}

```ts
const stream = await agent.stream('message for agent')
```

:::info

**Compatibilité des modèles** : cette méthode est conçue pour les modèles V2. Les modèles V1 doivent utiliser la méthode [`.streamLegacy()`](./streamLegacy). Mastra détecte automatiquement la version de votre modèle et génère une erreur en cas d’incompatibilité.

:::

## Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'string | string[] | CoreMessage[] | AiMessageType[] | UIMessageWithMetadata[]',
    description:
      'Messages à envoyer à l’agent. Il peut s’agir d’une chaîne unique, d’un tableau de chaînes ou d’objets de message structurés.',
  },
  {
    name: 'options',
    type: 'AgentExecutionOptions<Output, Format>',
    isOptional: true,
    description: 'Configuration facultative du processus de streaming.',
    properties: [
      {
        type: 'AgentExecutionOptions<Output, Format>',
        parameters: [
          {
            name: 'maxSteps',
            type: 'number',
            isOptional: true,
            description: 'Nombre maximal d’étapes à exécuter.',
          },
          {
            name: 'scorers',
            type: "MastraScorers | Record<string, { scorer: MastraScorer['name']; sampling?: ScoringSamplingConfig }>",
            isOptional: true,
            description: 'Évaluateurs à exécuter sur les résultats.',
            properties: [
              {
                parameters: [
                  {
                    name: 'scorer',
                    type: 'string',
                    isOptional: false,
                    description: 'Nom de l’évaluateur à utiliser.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'sampling',
                    type: 'ScoringSamplingConfig',
                    isOptional: true,
                    description: 'Configuration de l’échantillonnage de l’évaluateur.',
                    properties: [
                      {
                        parameters: [
                          {
                            name: 'type',
                            type: "'none' | 'ratio'",
                            isOptional: false,
                            description:
                              "Type de stratégie d’échantillonnage. Utilisez 'none' pour désactiver l’échantillonnage ou 'ratio' pour un échantillonnage fondé sur un pourcentage.",
                          },
                        ],
                      },
                      {
                        parameters: [
                          {
                            name: 'rate',
                            type: 'number',
                            isOptional: true,
                            description: "Taux d’échantillonnage (0-1). Requis lorsque le type est 'ratio'.",
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
          {
            name: 'onIterationComplete',
            type: '(context: IterationCompleteContext) => { continue?: boolean; feedback?: string } | void | Promise<{ continue?: boolean; feedback?: string } | void>',
            isOptional: true,
            description:
              'Fonction de rappel appelée à la fin de chaque itération. Utilisez-la pour suivre la progression, fournir un retour afin de guider l’agent ou arrêter l’exécution de manière anticipée. Elle reçoit le contexte de l’itération, notamment le texte actuel, les appels d’outils et le motif de fin.',
            properties: [
              {
                parameters: [
                  {
                    name: 'context.iteration',
                    type: 'number',
                    description: 'Numéro de l’itération actuelle (à partir de 1).',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.maxIterations',
                    type: 'number | undefined',
                    description: 'Nombre maximal d’itérations autorisées (s’il est défini).',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.text',
                    type: 'string',
                    description: 'Réponse textuelle de cette itération.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.isFinal',
                    type: 'boolean',
                    description: 'Indique s’il s’agit de la dernière itération.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.finishReason',
                    type: 'string',
                    description: "Motif de fin de cette itération (par ex. 'stop', 'length', 'tool-calls').",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.toolCalls',
                    type: 'ToolCall[]',
                    description: 'Appels d’outils effectués pendant cette itération.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.messages',
                    type: 'MastraDBMessage[]',
                    description: 'Tous les messages accumulés jusqu’à présent.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'return.continue',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Définissez cette valeur sur false pour arrêter l’exécution de manière anticipée.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'return.feedback',
                    type: 'string',
                    isOptional: true,
                    description: "Message de retour destiné à guider l’itération suivante de l’agent.",
                  },
                ],
              },
            ],
          },
          {
            name: 'isTaskComplete',
            type: 'IsTaskCompleteConfig',
            isOptional: true,
            description:
              "Configuration de l’évaluation d’achèvement qui vérifie si la tâche est terminée. Utilise les évaluateurs de Mastra pour vérifier automatiquement si la réponse de l’agent satisfait aux critères d’achèvement.",
            properties: [
              {
                parameters: [
                  {
                    name: 'scorers',
                    type: 'MastraScorer[]',
                    description:
                      'Tableau d’évaluateurs qui déterminent si la tâche est achevée. Chaque évaluateur renvoie 0 (échec) ou 1 (réussite).',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'strategy',
                    type: "'all' | 'any'",
                    isOptional: true,
                    defaultValue: "'all'",
                    description:
                      "Stratégie de combinaison des résultats des évaluateurs. 'all' exige la réussite de tous les évaluateurs, tandis que 'any' en exige au moins un.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'onComplete',
                    type: '(result: IsTaskCompleteRunResult) => void | Promise<void>',
                    isOptional: true,
                    description:
                      'Fonction de rappel appelée à la fin de la vérification de l’achèvement de la tâche. Elle reçoit le résultat avec le score de chaque évaluateur.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'parallel',
                    type: 'boolean',
                    isOptional: true,
                    defaultValue: 'true',
                    description: 'Indique si les évaluateurs doivent être exécutés en parallèle.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'timeout',
                    type: 'number',
                    isOptional: true,
                    defaultValue: '600000',
                    description: 'Durée maximale, en millisecondes, d’attente de la fin de tous les évaluateurs.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'suppressFeedback',
                    type: 'boolean',
                    isOptional: true,
                    defaultValue: 'false',
                    description:
                      'Lorsque cette valeur est true, marque le retour de la vérification d’achèvement afin que les consommateurs puissent le masquer dans la sortie affichée. Ce retour n’est ajouté à la conversation qu’en cas d’échec de la vérification, afin de guider l’itération suivante.',
                  },
                ],
              },
            ],
          },
          {
            name: 'delegation',
            type: 'DelegationConfig',
            isOptional: true,
            description:
              'Configuration de la délégation aux sous-agents. Utilisez-la pour contrôler et surveiller la délégation de tâches à d’autres agents, notamment pour modifier ou refuser les délégations et fournir un retour afin de guider le superviseur.',
            properties: [
              {
                parameters: [
                  {
                    name: 'onDelegationStart',
                    type: '(context: DelegationStartContext) => DelegationStartResult | void | Promise<DelegationStartResult | void>',
                    isOptional: true,
                    description:
                      "Appelée avant la délégation à un sous-agent. Utilisez-la pour modifier les paramètres de délégation, refuser entièrement la délégation ou modifier `context.requestContext` afin d’ajouter des entrées au contexte de requête de l’exécution du sous-agent.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'onDelegationComplete',
                    type: '(context: DelegationCompleteContext) => { feedback?: string } | void | Promise<{ feedback?: string } | void>',
                    isOptional: true,
                    description:
                      "Appelée une fois la délégation à un sous-agent terminée. Le contexte comprend une méthode `bail()` permettant d’arrêter la suite de l’exécution, et vous pouvez renvoyer `{ feedback }` pour guider l’action suivante du superviseur. Le retour est enregistré dans la mémoire du superviseur sous forme de message de l’assistant.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'messageFilter',
                    type: '(context: MessageFilterContext) => MastraDBMessage[] | Promise<MastraDBMessage[]>',
                    isOptional: true,
                    description:
                      'Fonction de rappel appelée avant la délégation à un sous-agent. Utilisez-la pour filtrer les messages transmis au sous-agent.',
                  },
                ],
              },
            ],
          },
          {
            name: 'tracingContext',
            type: 'TracingContext',
            isOptional: true,
            description: 'Contexte de traçage pour la hiérarchie des spans et les métadonnées.',
          },
          {
            name: 'returnScorerData',
            type: 'boolean',
            isOptional: true,
            description: 'Indique si des données d’évaluation détaillées doivent être renvoyées dans la réponse.',
          },
          {
            name: 'onChunk',
            type: '(chunk: ChunkType) => Promise<void> | void',
            isOptional: true,
            description: 'Fonction de rappel appelée pour chaque fragment pendant le streaming.',
          },
          {
            name: 'onError',
            type: '({ error }: { error: Error | string }) => Promise<void> | void',
            isOptional: true,
            description: 'Fonction de rappel appelée lorsqu’une erreur survient pendant le streaming.',
          },
          {
            name: 'onAbort',
            type: '(event: any) => Promise<void> | void',
            isOptional: true,
            description: 'Fonction de rappel appelée lorsque le flux est interrompu.',
          },
          {
            name: 'abortSignal',
            type: 'AbortSignal',
            isOptional: true,
            description:
              "Objet signal qui permet d’interrompre l’exécution de l’agent. Lorsque le signal est interrompu, toutes les opérations en cours prennent fin, y compris les exécutions de sous-agents en cours que l’agent a déléguées.",
          },
          {
            name: 'activeTools',
            type: 'Array<keyof ToolSet> | undefined',
            isOptional: true,
            description: 'Tableau des noms d’outils actifs pouvant être utilisés pendant l’exécution.',
          },
          {
            name: 'prepareStep',
            type: 'PrepareStepFunction<any>',
            isOptional: true,
            description: 'Fonction de rappel appelée avant chaque étape d’une exécution en plusieurs étapes.',
          },
          {
            name: 'context',
            type: 'ModelMessage[]',
            isOptional: true,
            description: 'Messages de contexte supplémentaires à fournir à l’agent.',
          },
          {
            name: 'structuredOutput',
            type: 'StructuredOutputOptions<S extends ZodTypeAny = ZodTypeAny>',
            isOptional: true,
            description: 'Options permettant d’affiner la génération de la sortie structurée.',
            properties: [
              {
                parameters: [
                  {
                    name: 'schema',
                    type: 'StandardJSONSchemaV1',
                    isOptional: false,
                    description: 'Schéma JSON standard définissant la structure de sortie attendue.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'model',
                    type: 'MastraLanguageModel',
                    isOptional: true,
                    description:
                      'Modèle de langage à utiliser pour générer la sortie structurée. Lorsqu’il est fourni, il permet à l’agent de répondre en plusieurs étapes avec des appels d’outils, du texte et une sortie structurée.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'errorStrategy',
                    type: "'strict' | 'warn' | 'fallback'",
                    isOptional: true,
                    description:
                      "Stratégie de gestion des erreurs de validation du schéma. 'strict' génère des erreurs, 'warn' consigne des avertissements et 'fallback' utilise des valeurs de repli.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'fallbackValue',
                    type: '<S extends ZodTypeAny>',
                    isOptional: true,
                    description: "Valeur de repli à utiliser lorsque la validation du schéma échoue et que errorStrategy vaut 'fallback'.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'instructions',
                    type: 'string',
                    isOptional: true,
                    description: 'Instructions supplémentaires pour le modèle de sortie structurée.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'jsonPromptInjection',
                    type: "boolean | 'system' | 'inline' | 'auto'",
                    isOptional: true,
                    description:
                      "Contrôle la façon dont le schéma JSON est transmis au modèle. Définissez cette valeur sur 'auto' pour utiliser la sortie structurée native lorsqu’elle est prise en charge, et injecter le schéma directement dans le prompt dans le cas contraire.",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'providerOptions',
                    type: 'ProviderOptions',
                    isOptional: true,
                    description:
                      "Options propres au fournisseur transmises à l’agent interne de structuration. Utilisez-les pour contrôler le comportement du modèle, comme l’effort de raisonnement des modèles de réflexion (par ex. `{ openai: { reasoningEffort: 'low' } }`).",
                  },
                ],
              },
            ],
          },
          {
            name: 'outputProcessors',
            type: 'Processor[]',
            isOptional: true,
            description:
              'Remplace les processeurs de sortie définis sur l’agent. Ces processeurs peuvent modifier ou valider les messages de l’agent avant leur renvoi à l’utilisateur. Ils doivent implémenter l’une des fonctions `processOutputResult` et `processOutputStream`, ou les deux.',
          },
          {
            name: 'includeRawChunks',
            type: 'boolean',
            isOptional: true,
            description: 'Indique si les fragments bruts doivent être inclus dans la sortie du flux (option non disponible chez tous les fournisseurs de modèles).',
          },
          {
            name: 'inputProcessors',
            type: 'Processor[]',
            isOptional: true,
            description:
              'Remplace les processeurs d’entrée définis sur l’agent. Ces processeurs peuvent modifier ou valider les messages avant leur traitement par l’agent. Ils doivent implémenter la fonction `processInput`.',
          },
          {
            name: 'instructions',
            type: 'string',
            isOptional: true,
            description:
              "Instructions personnalisées qui remplacent les instructions par défaut de l’agent pour cette génération précise. Elles permettent de modifier dynamiquement le comportement de l’agent sans créer une nouvelle instance.",
          },
          {
            name: 'system',
            type: 'string | string[] | CoreSystemMessage | SystemModelMessage | CoreSystemMessage[] | SystemModelMessage[]',
            isOptional: true,
            description:
              "Messages système personnalisés à inclure dans le prompt. Il peut s’agir d’une chaîne unique, d’un objet message ou d’un tableau contenant l’un ou l’autre. Les messages système apportent du contexte ou des instructions de comportement supplémentaires qui complètent les instructions principales de l’agent.",
          },
          {
            name: 'output',
            type: 'Zod schema | JsonSchema7',
            isOptional: true,
            description:
              '**Obsolète.** Utilisez structuredOutput sans modèle pour obtenir le même résultat. Définit la structure attendue de la sortie. Peut être un objet JSON Schema ou un schéma Zod.',
          },
          {
            name: 'memory',
            type: 'object',
            isOptional: true,
            description: 'Configuration de la mémoire. Il s’agit de la méthode recommandée pour gérer la mémoire.',
            properties: [
              {
                parameters: [
                  {
                    name: 'thread',
                    type: 'string | { id: string; metadata?: Record<string, any>, title?: string }',
                    isOptional: false,
                    description:
                      'Thread de conversation, sous forme d’un ID chaîne ou d’un objet comportant un `id` et des `metadata` facultatives.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'resource',
                    type: 'string',
                    isOptional: false,
                    description: 'Identifiant de l’utilisateur ou de la ressource associés au thread.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'options',
                    type: 'MemoryConfig',
                    isOptional: true,
                    description:
                      'Configuration du comportement de la mémoire, notamment lastMessages, readOnly, semanticRecall, workingMemory et filterIncompleteToolCalls.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'onTitleGenerated',
                    type: '(title: string) => void | Promise<void>',
                    isOptional: true,
                    description:
                      'Fonction de rappel déclenchée de manière asynchrone lorsqu’un titre de thread est généré et conservé dans le stockage. La génération du titre s’effectue en arrière-plan et peut se terminer après la fin du flux. Elle ne se déclenche que lorsque `generateTitle` est activé dans les options de mémoire et que le thread ne possède pas encore de titre.',
                  },
                ],
              },
            ],
          },
          {
            name: 'onFinish',
            type: 'StreamTextOnFinishCallback<any> | StreamObjectOnFinishCallback<OUTPUT>',
            isOptional: true,
            description: 'Fonction de rappel appelée à la fin du streaming. Elle reçoit le résultat final.',
          },
          {
            name: 'onStepFinish',
            type: 'StreamTextOnStepFinishCallback<any> | never',
            isOptional: true,
            description:
              'Fonction de rappel appelée après chaque étape d’exécution. Elle reçoit les détails de l’étape sous forme de chaîne JSON. Non disponible pour les sorties structurées.',
          },
          {
            name: 'telemetry',
            type: 'TelemetrySettings',
            isOptional: true,
            description: 'Paramètres de collecte de la télémétrie OTLP pendant le streaming (hors Tracing).',
            properties: [
              {
                parameters: [
                  {
                    name: 'isEnabled',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Active ou désactive la télémétrie. Désactivée par défaut pendant la phase expérimentale.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordInputs',
                    type: 'boolean',
                    isOptional: true,
                    description:
                      'Active ou désactive l’enregistrement des entrées. Activé par défaut. Vous pouvez désactiver cet enregistrement afin d’éviter de consigner des informations sensibles.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordOutputs',
                    type: 'boolean',
                    isOptional: true,
                    description:
                      'Active ou désactive l’enregistrement des sorties. Activé par défaut. Vous pouvez désactiver cet enregistrement afin d’éviter de consigner des informations sensibles.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'functionId',
                    type: 'string',
                    isOptional: true,
                    description: 'Identifiant de cette fonction. Utilisé pour regrouper les données de télémétrie par fonction.',
                  },
                ],
              },
            ],
          },
          MODEL_SETTINGS_OBJECT,
          {
            name: 'toolChoice',
            type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
            isOptional: true,
            defaultValue: "'auto'",
            description: 'Contrôle la manière dont l’agent utilise les outils pendant le streaming.',
            properties: [
              {
                parameters: [
                  {
                    name: "'auto'",
                    type: 'string',
                    description: 'Laisse le modèle décider s’il doit utiliser des outils (par défaut).',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "'none'",
                    type: 'string',
                    description: 'N’utilise aucun outil.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "'required'",
                    type: 'string',
                    description: 'Exige que le modèle utilise au moins un outil.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "{ type: 'tool'; toolName: string }",
                    type: 'object',
                    description: 'Exige que le modèle utilise un outil précis, désigné par son nom.',
                  },
                ],
              },
            ],
          },
          {
            name: 'toolsets',
            type: 'ToolsetsInput',
            isOptional: true,
            description: 'Ensembles d’outils supplémentaires à mettre à la disposition de l’agent pendant le streaming.',
          },
          {
            name: 'clientTools',
            type: 'ToolsInput',
            isOptional: true,
            description:
              "Outils exécutés côté 'client' de la requête. La définition de ces outils ne comporte aucune fonction execute.",
          },
          {
            name: 'hooks',
            type: 'ToolHooks',
            isOptional: true,
            description:
              'Hooks propres à l’exécution, lancés avant et après les appels d’outils. Remplace, pour cette exécution, les hooks correspondants définis au niveau de l’agent. `beforeToolCall` peut renvoyer `{ proceed: false, output }` pour ignorer l’appel d’outil.',
          },
          {
            name: 'savePerStep',
            type: 'boolean',
            isOptional: true,
            description: 'Enregistre progressivement les messages après chaque étape terminée du flux (par défaut : false).',
          },
          {
            name: 'requireToolApproval',
            type: 'boolean',
            isOptional: true,
            description:
              'Lorsque cette valeur est true, tous les appels d’outils exigent une approbation explicite avant leur exécution. Le flux émet des fragments `tool-call-approval` et se met en pause jusqu’à l’appel de `approveToolCall()` ou `declineToolCall()`.',
          },
          {
            name: 'autoResumeSuspendedTools',
            type: 'boolean',
            isOptional: true,
            description:
              "Lorsque cette valeur est true, reprend automatiquement les outils suspendus quand l’utilisateur envoie un nouveau message dans le même thread. L’agent extrait `resumeData` du message de l’utilisateur selon le `resumeSchema` de l’outil. Nécessite la configuration de la mémoire.",
          },
          {
            name: 'toolCallConcurrency',
            type: 'number',
            isOptional: true,
            description:
              'Nombre maximal d’appels d’outils à exécuter simultanément. La valeur par défaut est 1 lorsqu’une approbation peut être requise, et 10 dans le cas contraire.',
          },
          {
            name: 'providerOptions',
            type: 'Record<string, Record<string, JSONValue>>',
            isOptional: true,
            description:
              "Options supplémentaires propres au fournisseur, transmises au fournisseur LLM sous-jacent. La structure est `{ providerName: { optionKey: value } }`. Par exemple : `{ openai: { reasoningEffort: 'high' }, anthropic: { maxTokens: 1000 } }`.",
            properties: [
              {
                parameters: [
                  {
                    name: 'openai',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: "Options propres à OpenAI. Exemple : `{ reasoningEffort: 'high' }`",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'anthropic',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Options propres à Anthropic. Exemple : `{ maxTokens: 1000 }`',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'google',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Options propres à Google. Exemple : `{ safetySettings: [...] }`',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: '[providerName]',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description:
                      'Autres options propres au fournisseur. La clé correspond au nom du fournisseur et la valeur est un enregistrement d’options propres à celui-ci.',
                  },
                ],
              },
            ],
          },
          {
            name: 'runId',
            type: 'string',
            isOptional: true,
            description: 'ID unique de cette exécution de génération. Utile pour le suivi et le débogage.',
          },
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            description: 'Contexte de requête destiné à l’injection de dépendances et aux informations contextuelles.',
          },
          {
            name: 'tracingContext',
            type: 'TracingContext',
            isOptional: true,
            description:
              "Contexte de traçage permettant de créer des spans enfants et d’ajouter des métadonnées. Injecté automatiquement lors de l’utilisation du système de traçage de Mastra.",
            properties: [
              {
                parameters: [
                  {
                    name: 'currentSpan',
                    type: 'Span',
                    isOptional: true,
                    description:
                      'Span actuel servant à créer des spans enfants et à ajouter des métadonnées. Utilisez-le pour créer des spans enfants personnalisés ou mettre à jour les attributs d’un span pendant l’exécution.',
                  },
                ],
              },
            ],
          },
          {
            name: 'tracingOptions',
            type: 'TracingOptions',
            isOptional: true,
            description: 'Options de configuration du traçage.',
            properties: [
              {
                parameters: [
                  {
                    name: 'metadata',
                    type: 'Record<string, any>',
                    isOptional: true,
                    description:
                      'Métadonnées à ajouter au span racine de la trace. Utiles pour ajouter des attributs personnalisés comme des ID utilisateur, des ID de session ou des indicateurs de fonctionnalité.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'requestContextKeys',
                    type: 'string[]',
                    isOptional: true,
                    description:
                      "Clés RequestContext supplémentaires à extraire en tant que métadonnées de cette trace. Prend en charge la notation par points pour les valeurs imbriquées (par ex. 'user.id').",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'traceId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'ID de trace à utiliser pour cette exécution (1 à 32 caractères hexadécimaux). Lorsqu’il est fourni, cette trace appartient à la trace indiquée.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'parentSpanId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'ID du span parent à utiliser pour cette exécution (1 à 16 caractères hexadécimaux). Lorsqu’il est fourni, le span racine est créé comme enfant de ce span.',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'tags',
                    type: 'string[]',
                    isOptional: true,
                    description: 'Tags à appliquer à cette trace. Libellés sous forme de chaînes permettant de classer et de filtrer les traces.',
                  },
                ],
              },
            ],
          },
          {
            name: 'versions',
            type: 'VersionOverrides',
            isOptional: true,
            description:
              'Remplacements de version propres à chaque invocation pour la délégation aux sous-agents. Ils sont fusionnés avec les versions définies au niveau de l’instance Mastra et propagés automatiquement dans les appels de sous-agents via requestContext. Nécessite le package editor. Consultez [la gestion des versions dans Editor](/docs/editor/overview#versioning).',
            properties: [
              {
                type: 'VersionOverrides',
                parameters: [
                  {
                    name: 'agents',
                    type: 'Record<string, VersionSelector>',
                    isOptional: true,
                    description: 'Correspondance entre les ID d’agents et leurs sélecteurs de version.',
                    properties: [
                      {
                        type: 'VersionSelector',
                        parameters: [
                          {
                            name: 'versionId',
                            type: 'string',
                            isOptional: true,
                            description: 'Cible une version précise par son ID.',
                          },
                        ],
                      },
                      {
                        parameters: [
                          {
                            name: 'status',
                            type: "'draft' | 'published'",
                            isOptional: true,
                            description: 'Cible la dernière version possédant ce statut de publication.',
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
          {
            name: 'untilIdle',
            type: 'boolean | { maxIdleMs?: number }',
            isOptional: true,
            description:
              'Lorsque cette option est définie, maintient le flux ouvert pendant les continuations de tâches en arrière-plan. L’agent invoque automatiquement le LLM de nouveau lorsque les tâches en arrière-plan se terminent, et diffuse les tours de continuation dans le même `fullStream`. Transmettez `true` pour utiliser les paramètres par défaut (délai d’inactivité de 5 min), ou un objet avec `maxIdleMs` pour les configurer. Nécessite la mémoire. Remplace la méthode autonome `streamUntilIdle()`.',
            properties: [
              {
                parameters: [
                  {
                    name: 'maxIdleMs',
                    type: 'number',
                    isOptional: true,
                    defaultValue: '300000',
                    description:
                      'Ferme le flux externe après ce nombre de millisecondes d’inactivité entre les tours. Le minuteur ne s’exécute que lorsque le composant d’encapsulation se trouve entre deux tours. Valeur par défaut : 5 minutes.',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## Valeurs renvoyées {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'stream',
    type: 'MastraModelOutput<Output>',
    description: 'Renvoie une instance MastraModelOutput qui donne accès à la sortie diffusée en streaming.',
  },
  {
    name: 'traceId',
    type: 'string',
    isOptional: true,
    description:
      'ID de trace associé à cette exécution lorsque le traçage est activé. Utilisez-le pour corréler les journaux et déboguer le flux d’exécution.',
  },
  {
    name: 'spanId',
    type: 'string',
    isOptional: true,
    description:
      'ID du span racine associé à cette exécution lorsque le traçage est activé. Utilisez-le pour les recherches et les corrélations au niveau du span.',
  },
]}
/>

## Exemple d’utilisation détaillé {/* #extended-usage-example */}

### Format Mastra (par défaut) {/* #mastra-format-default */}

```ts title="index.ts"
import { stepCountIs } from 'ai-v5'

const stream = await agent.stream('Tell me a story', {
  stopWhen: stepCountIs(3), // Stop after 3 steps
  modelSettings: {
    temperature: 0.7,
  },
})

// Access text stream
for await (const chunk of stream.textStream) {
  console.log(chunk)
}

// or access full stream
for await (const chunk of stream.fullStream) {
  console.log(chunk)
}

// Get full text after streaming
const fullText = await stream.text
```

### Format AI SDK v5+ {/* #ai-sdk-v5-format */}

Pour utiliser le flux avec AI SDK v5 (et les versions ultérieures), vous pouvez le convertir à l’aide de notre fonction utilitaire `toAISdkStream`.

```ts title="index.ts"
import { stepCountIs, createUIMessageStreamResponse } from 'ai'
import { toAISdkStream } from '@mastra/ai-sdk'

const stream = await agent.stream('Tell me a story', {
  stopWhen: stepCountIs(3), // Stop after 3 steps
  modelSettings: {
    temperature: 0.7,
  },
})

// In an API route for frontend integration
return createUIMessageStreamResponse({
  stream: toAISdkStream(stream, { from: 'agent' }),
})
```

### Utilisation des fonctions de rappel {/* #using-callbacks */}

Toutes les fonctions de rappel sont désormais disponibles en tant que propriétés de premier niveau, pour une utilisation plus claire de l’API.

```ts title="index.ts"
const stream = await agent.stream('Tell me a story', {
  onFinish: result => {
    console.log('Streaming finished:', result)
  },
  onStepFinish: step => {
    console.log('Step completed:', step)
  },
  onChunk: chunk => {
    console.log('Received chunk:', chunk)
  },
  onError: ({ error }) => {
    console.error('Streaming error:', error)
  },
  onAbort: event => {
    console.log('Stream aborted:', event)
  },
})

// Process the stream
for await (const chunk of stream.textStream) {
  console.log(chunk)
}
```

### Exemple avancé avec des options {/* #advanced-example-with-options */}

```ts title="index.ts"
import { z } from 'zod'
import { stepCountIs } from 'ai'

await agent.stream('message for agent', {
  stopWhen: stepCountIs(3), // Stop after 3 steps
  modelSettings: {
    temperature: 0.7,
  },
  memory: {
    thread: 'user-123',
    resource: 'test-app',
  },
  toolChoice: 'auto',
  // Structured output with better DX
  structuredOutput: {
    schema: z.object({
      sentiment: z.enum(['positive', 'negative', 'neutral']),
      confidence: z.number(),
    }),
    model: '__GATEWAY_OPENAI_MODEL__',
    errorStrategy: 'warn',
  },
  // Output processors for streaming response validation
  outputProcessors: [
    new ModerationProcessor({ model: 'openrouter/openai/gpt-oss-safeguard-20b' }),
    new BatchPartsProcessor({ maxBatchSize: 3, maxWaitTime: 100 }),
  ],
})
```

## Transport WebSocket de Responses {/* #responses-websocket-transport */}

Activez le streaming WebSocket de Responses avec les options du fournisseur. Cela ne s’applique qu’aux appels en streaming et est pris en charge pour les modèles OpenAI directs ainsi que pour les déploiements Responses d’Azure OpenAI. Si le streaming WebSocket n’est pas disponible, Mastra utilise le streaming HTTP comme solution de repli. Par défaut, Mastra ferme le WebSocket à la fin du flux.

```ts title="index.ts"
const stream = await agent.stream('Hello', {
  providerOptions: {
    openai: {
      transport: 'websocket', // 'websocket' | 'fetch' | 'auto'
      websocket: {
        url: 'wss://api.openai.com/v1/responses',
        closeOnFinish: true, // default
      },
    },
  },
})
```

Pour Azure OpenAI, configurez la passerelle avec `useResponsesAPI: true`, puis utilisez `providerOptions.azure.transport`.

```ts title="index.ts"
const stream = await agent.stream('Hello', {
  providerOptions: {
    azure: {
      transport: 'websocket',
      store: false,
      websocket: { closeOnFinish: true },
    },
  },
})
```

Pour garder la connexion ouverte après la fin du flux, définissez `closeOnFinish: false` et fermez-la manuellement.

```ts title="index.ts"
const stream = await agent.stream('Hello', {
  providerOptions: {
    openai: {
      transport: 'websocket',
      websocket: { closeOnFinish: false },
    },
  },
})

// Later, when you're done with the connection:
stream.transport?.close()
```

Les connexions WebSocket de Responses traitent une seule réponse à la fois. Mastra rejette les requêtes de continuation qui se chevauchent et incluent `previous_response_id` sur le même transport WebSocket. Attendez la fin du flux actif avant d’envoyer le tour suivant de la chaîne de réponses.

## Voir aussi {/* #related */}

- [Génération de réponses](/docs/agents/overview#use-your-agent)
- [Streaming des réponses](/docs/agents/overview#use-your-agent)
- [Approbation des agents](/docs/agents/agent-approval)
