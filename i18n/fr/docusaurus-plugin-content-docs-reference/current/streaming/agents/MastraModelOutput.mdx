---
title: "Référence : MastraModelOutput | Streaming"
description: "Référence complète de MastraModelOutput, l'objet stream renvoyé par agent.stream() qui donne accès aux sorties du modèle par streaming et par Promise."
packages:
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# MastraModelOutput

La classe `MastraModelOutput` est renvoyée par [.stream()](./stream) et donne accès aux sorties du modèle à la fois par streaming et par Promise. Elle prend en charge la génération de sorties structurées, les appels de Tool, le raisonnement et le suivi détaillé de l'utilisation.

```typescript
// MastraModelOutput is returned by agent.stream()
const stream = await agent.stream('Hello world')
```

Pour la configuration et l'utilisation de base, consultez la documentation de la méthode [.stream()](./stream).

## Propriétés de streaming {/* #streaming-properties */}

Ces propriétés donnent accès aux sorties du modèle en temps réel, à mesure de leur génération :

<PropertiesTable
  content={[
  {
    name: 'fullStream',
    type: 'ReadableStream<ChunkType<OUTPUT>>',
    description:
      "Stream complet de tous les types de fragments, notamment le texte, les appels de Tool, le raisonnement, les métadonnées et les fragments de contrôle. Fournit un accès précis à chaque aspect de la réponse du modèle.",
    properties: [
      {
        type: 'ReadableStream',
        parameters: [
          {
            name: 'ChunkType',
            type: 'ChunkType<OUTPUT>',
            description: 'Tous les types de fragments pouvant être émis pendant le streaming',
          },
        ],
      },
    ],
  },
  {
    name: 'textStream',
    type: 'ReadableStream<string>',
    description:
      'Stream contenant uniquement le texte incrémentiel. Filtre toutes les métadonnées, les appels de Tool et les fragments de contrôle afin de fournir seulement le texte en cours de génération.',
  },
  {
    name: 'objectStream',
    type: 'ReadableStream<Partial<OUTPUT>>',
    description:
      "Stream de mises à jour progressives de l'objet structuré lors de l'utilisation de schémas de sortie. Émet des objets partiels à mesure de leur construction, ce qui permet de visualiser en temps réel la génération des données structurées.",
    properties: [
      {
        type: 'ReadableStream',
        parameters: [
          {
            name: 'PartialSchemaOutput',
            type: 'Partial<OUTPUT>',
            description: 'Objet partiellement terminé correspondant au schéma défini',
          },
        ],
      },
    ],
  },
  {
    name: 'elementStream',
    type: 'ReadableStream<OUTPUT extends (infer T)[] ? T : never>',
    description:
      "Stream des différents éléments du tableau lorsque le schéma de sortie définit un type tableau. Chaque élément est émis dès qu'il est terminé, sans attendre le tableau complet.",
  },
]}
/>

## Propriétés fondées sur des Promises {/* #promise-based-properties */}

Ces propriétés sont résolues avec les valeurs finales une fois le stream terminé :

<PropertiesTable
  content={[
  {
    name: 'text',
    type: 'Promise<string>',
    description: 'Réponse textuelle complète et concaténée du modèle. Résolue une fois la génération du texte terminée.',
  },
  {
    name: 'object',
    type: 'Promise<OUTPUT>',
    description:
      "Réponse complète sous forme d'objet structuré lors de l'utilisation de schémas de sortie. Validée par rapport au schéma avant sa résolution. Rejetée si la validation échoue.",
    properties: [
      {
        type: 'Promise',
        parameters: [
          {
            name: 'InferSchemaOutput',
            type: 'OUTPUT',
            description: 'Objet entièrement typé correspondant exactement à la définition du schéma',
          },
        ],
      },
    ],
  },
  {
    name: 'reasoning',
    type: 'Promise<string>',
    description:
      "Texte de raisonnement complet pour les modèles prenant en charge le raisonnement, tels que la série o1 d'OpenAI. Renvoie une chaîne vide pour les modèles dépourvus de cette fonctionnalité.",
  },
  {
    name: 'reasoningText',
    type: 'Promise<string | undefined>',
    description:
      "Autre moyen d'accéder au contenu du raisonnement. Peut être undefined pour les modèles qui ne prennent pas en charge le raisonnement, tandis que 'reasoning' renvoie une chaîne vide.",
  },
  {
    name: 'toolCalls',
    type: 'Promise<ToolCallChunk[]>',
    description:
      "Tableau de tous les fragments d'appel de Tool créés pendant l'exécution. Chaque fragment contient les métadonnées du Tool et les détails de l'exécution.",
    properties: [
      {
        type: 'ToolCallChunk',
        parameters: [
          {
            name: 'type',
            type: "'tool-call'",
            description: 'Identifiant du type de fragment',
          },
          {
            name: 'runId',
            type: 'string',
            description: "Identifiant de l'exécution",
          },
          {
            name: 'from',
            type: 'ChunkFrom',
            description: 'Source du fragment (AGENT, WORKFLOW, etc.)',
          },
          {
            name: 'payload',
            type: 'ToolCallPayload',
            description: "Données de l'appel de Tool, notamment toolCallId, toolName, args et les détails de l'exécution",
          },
        ],
      },
    ],
  },
  {
    name: 'toolResults',
    type: 'Promise<ToolResultChunk[]>',
    description:
      "Tableau de tous les fragments de résultat de Tool correspondant aux appels de Tool. Contient les résultats d'exécution et les informations sur les erreurs.",
    properties: [
      {
        type: 'ToolResultChunk',
        parameters: [
          {
            name: 'type',
            type: "'tool-result'",
            description: 'Identifiant du type de fragment',
          },
          {
            name: 'runId',
            type: 'string',
            description: "Identifiant de l'exécution",
          },
          {
            name: 'from',
            type: 'ChunkFrom',
            description: 'Source du fragment (AGENT, WORKFLOW, etc.)',
          },
          {
            name: 'payload',
            type: 'ToolResultPayload',
            description: "Données du résultat du Tool, notamment toolCallId, toolName, result et l'état d'erreur",
          },
        ],
      },
    ],
  },
  {
    name: 'usage',
    type: 'Promise<LanguageModelUsage>',
    description:
      "Statistiques d'utilisation des tokens, notamment les tokens d'entrée, de sortie, totaux et de raisonnement pour les modèles de raisonnement.",
    properties: [
      {
        type: 'Record',
        parameters: [
          {
            name: 'inputTokens',
            type: 'number',
            description: "Tokens consommés par le prompt d'entrée",
          },
          {
            name: 'outputTokens',
            type: 'number',
            description: 'Tokens générés dans la réponse',
          },
          {
            name: 'totalTokens',
            type: 'number',
            description: "Somme des tokens d'entrée et de sortie",
          },
          {
            name: 'reasoningTokens',
            type: 'number',
            isOptional: true,
            description: 'Tokens de raisonnement masqués pour les modèles de raisonnement',
          },
          {
            name: 'cachedInputTokens',
            type: 'number',
            isOptional: true,
            description: "Nombre de tokens d'entrée trouvés dans le cache",
          },
        ],
      },
    ],
  },
  {
    name: 'finishReason',
    type: 'Promise<string | undefined>',
    description:
      "Raison de l'arrêt de la génération (par exemple, 'stop', 'length', 'tool_calls', 'content_filter'). Undefined si le stream n'est pas terminé.",
    properties: [
      {
        type: 'enum',
        parameters: [
          {
            name: 'stop',
            type: "'stop'",
            description: 'Le modèle a terminé naturellement',
          },
          {
            name: 'length',
            type: "'length'",
            description: 'La limite maximale de tokens a été atteinte',
          },
          {
            name: 'tool_calls',
            type: "'tool_calls'",
            description: 'Le modèle a appelé des Tools',
          },
          {
            name: 'content_filter',
            type: "'content_filter'",
            description: 'Le contenu a été filtré',
          },
        ],
      },
    ],
  },
  {
    name: 'response',
    type: 'Promise<Response>',
    description: 'Métadonnées et messages de réponse du Provider de modèle.',
    properties: [
      {
        type: 'Response',
        parameters: [
          {
            name: 'id',
            type: 'string',
            isOptional: true,
            description: 'Identifiant de réponse du Provider de modèle',
          },
          {
            name: 'timestamp',
            type: 'Date',
            isOptional: true,
            description: 'Horodatage de la réponse',
          },
          {
            name: 'modelId',
            type: 'string',
            isOptional: true,
            description: 'Identifiant du modèle utilisé pour cette réponse',
          },
          {
            name: 'headers',
            type: 'Record<string, string>',
            isOptional: true,
            description: 'En-têtes de réponse du Provider de modèle',
          },
          {
            name: 'messages',
            type: 'ResponseMessage[]',
            isOptional: true,
            description: 'Messages de réponse au format du modèle',
          },
          {
            name: 'uiMessages',
            type: 'UIMessage[]',
            isOptional: true,
            description: 'Messages de réponse au format de l’interface utilisateur, avec toutes les métadonnées ajoutées par les Processors de sortie',
          },
        ],
      },
    ],
  },
]}
/>

## Propriétés d'erreur {/* #error-properties */}

<PropertiesTable
  content={[
  {
    name: 'error',
    type: 'string | Error | { message: string; stack: string; } | undefined',
    description:
      "Informations sur l'erreur si le stream en a rencontré une. Undefined si aucune erreur ne s'est produite. Il peut s'agir d'un message sous forme de chaîne, d'un objet Error ou d'une erreur sérialisée avec sa stack trace.",
  },
]}
/>

## Méthodes {/* #methods */}

<PropertiesTable
  content={[
  {
    name: 'getFullOutput',
    type: '() => Promise<FullOutput>',
    description:
      "Renvoie un objet de sortie complet contenant tous les résultats : texte, objet structuré, appels de Tool, statistiques d'utilisation, raisonnement et métadonnées. Cette méthode unique permet d'accéder facilement à tous les résultats du stream.",
    properties: [
      {
        type: 'FullOutput',
        parameters: [
          {
            name: 'text',
            type: 'string',
            description: 'Réponse textuelle complète',
          },
          {
            name: 'object',
            type: 'OUTPUT',
            isOptional: true,
            description: 'Sortie structurée si un schéma a été fourni',
          },
          {
            name: 'toolCalls',
            type: 'ToolCallChunk[]',
            description: "Tous les fragments d'appel de Tool créés",
          },
          {
            name: 'toolResults',
            type: 'ToolResultChunk[]',
            description: 'Tous les fragments de résultat de Tool',
          },
          {
            name: 'usage',
            type: 'Record<string, number>',
            description: "Statistiques d'utilisation des tokens",
          },
          {
            name: 'reasoning',
            type: 'string',
            isOptional: true,
            description: 'Texte de raisonnement, si disponible',
          },
          {
            name: 'finishReason',
            type: 'string',
            isOptional: true,
            description: "Raison de l'arrêt de la génération",
          },
          {
            name: 'response',
            type: 'Response',
            description: 'Métadonnées et messages de réponse du Provider de modèle',
          },
        ],
      },
    ],
  },
  {
    name: 'consumeStream',
    type: '(options?: ConsumeStreamOptions) => Promise<void>',
    description:
      "Consomme manuellement l'intégralité du stream sans traiter les fragments. Utile lorsque vous avez seulement besoin des résultats finaux fondés sur des Promises et souhaitez déclencher la consommation du stream.",
    properties: [
      {
        type: 'ConsumeStreamOptions',
        parameters: [
          {
            name: 'onError',
            type: '(error: Error) => void',
            isOptional: true,
            description: 'Callback permettant de gérer les erreurs du stream',
          },
        ],
      },
    ],
  },
]}
/>

## Exemples d'utilisation {/* #usage-examples */}

### Streaming de texte simple {/* #basic-text-streaming */}

```typescript
const stream = await agent.stream('Write a haiku')

// Stream text as it's generated
for await (const text of stream.textStream) {
  process.stdout.write(text)
}

// Or get the complete text
const fullText = await stream.text
console.log(fullText)
```

### Streaming de sortie structurée {/* #structured-output-streaming */}

```typescript
const stream = await agent.stream('Generate user data', {
  structuredOutput: {
    schema: z.object({
      name: z.string(),
      age: z.number(),
      email: z.string(),
    }),
  },
})

// Stream partial objects
for await (const partial of stream.objectStream) {
  console.log('Progress:', partial) // { name: "John" }, { name: "John", age: 30 }, ...
}

// Get final validated object
const user = await stream.object
console.log('Final:', user) // { name: "John", age: 30, email: "john@example.com" }
```

````

### Tool Calls and Results

```typescript
const stream = await agent.stream("What's the weather in NYC?", {
  tools: { weather: weatherTool }
});

// Monitor tool calls
const toolCalls = await stream.toolCalls;
const toolResults = await stream.toolResults;

console.log("Tools called:", toolCalls);
console.log("Results:", toolResults);
````

### Accès à la sortie complète {/* #complete-output-access */}

```typescript
const stream = await agent.stream('Analyze this data')

const output = await stream.getFullOutput()
console.log({
  text: output.text,
  usage: output.usage,
  reasoning: output.reasoning,
  finishReason: output.finishReason,
})
```

### Traitement du stream complet {/* #full-stream-processing */}

```typescript
const stream = await agent.stream('Complex task')

for await (const chunk of stream.fullStream) {
  switch (chunk.type) {
    case 'text-delta':
      process.stdout.write(chunk.payload.text)
      break
    case 'tool-call':
      console.log(`Calling ${chunk.payload.toolName}...`)
      break
    case 'reasoning-delta':
      console.log(`Reasoning: ${chunk.payload.text}`)
      break
    case 'finish':
      console.log(`Done! Reason: ${chunk.payload.stepResult.reason}`)
      // Access response messages with any metadata added by output processors
      const uiMessages = chunk.payload.response?.uiMessages
      if (uiMessages) {
        console.log('Response messages:', uiMessages)
      }
      break
  }
}
```

### Gestion des erreurs {/* #error-handling */}

```typescript
const stream = await agent.stream('Analyze this data')

try {
  // Option 1: Handle errors in consumeStream
  await stream.consumeStream({
    onError: error => {
      console.error('Stream error:', error)
    },
  })

  const result = await stream.text
} catch (error) {
  console.error('Failed to get result:', error)
}

// Option 2: Check error property
const result = await stream.getFullOutput()
if (stream.error) {
  console.error('Stream had errors:', stream.error)
}
```

## Types associés {/* #related-types */}

- [.stream()](./stream) : méthode qui renvoie MastraModelOutput
- [ChunkType](../ChunkType) : tous les types de fragments possibles dans le stream complet
