---
title: "Référence : GraphRAG | RAG"
description: Documentation de la classe GraphRAG de Mastra, qui met en œuvre une approche de la génération augmentée par récupération fondée sur un graphe.
packages:
  - "@mastra/rag"
---

# GraphRAG

La classe `GraphRAG` met en œuvre une approche de la génération augmentée par récupération fondée sur un graphe. Elle crée un graphe de connaissances à partir de segments de documents, dans lequel les nœuds représentent les documents et les arêtes leurs relations sémantiques. Elle permet ainsi à la fois la recherche directe de similarités et la découverte de contenus associés par parcours du graphe.

## Utilisation de base {/* #basic-usage */}

```typescript
import { GraphRAG } from '@mastra/rag'

const graphRag = new GraphRAG({
  dimension: 1536,
  threshold: 0.7,
})

// Create the graph from chunks and embeddings
graphRag.createGraph(documentChunks, embeddings)

// Query the graph with embedding
const results = await graphRag.query({
  query: queryEmbedding,
  topK: 10,
  randomWalkSteps: 100,
  restartProb: 0.15,
})
```

## Paramètres du constructeur {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'dimension',
    type: 'number',
    description: "Dimension des vecteurs d'embedding",
    isOptional: true,
    defaultValue: '1536',
  },
  {
    name: 'threshold',
    type: 'number',
    description: 'Seuil de similarité pour la création des arêtes entre les nœuds (0-1)',
    isOptional: true,
    defaultValue: '0.7',
  },
]}
/>

## Méthodes {/* #methods */}

### `createGraph` {/* #creategraph */}

Crée un graphe de connaissances à partir de segments de documents et de leurs embeddings.

```typescript prettier:false
createGraph(chunks: GraphChunk[], embeddings: GraphEmbedding[]): void
```

#### Paramètres {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'chunks',
    type: 'GraphChunk[]',
    description: 'Tableau de segments de documents contenant du texte et des métadonnées',
    isOptional: false,
  },
  {
    name: 'embeddings',
    type: 'GraphEmbedding[]',
    description: 'Tableau des embeddings correspondant aux segments',
    isOptional: false,
  },
]}
/>

### query {/* #query */}

Effectue une recherche fondée sur un graphe en combinant la similarité vectorielle et le parcours du graphe.

```typescript prettier:false
query({
  query,
  topK = 10,
  randomWalkSteps = 100,
  restartProb = 0.15
}: {
  query: number[];
  topK?: number;
  randomWalkSteps?: number;
  restartProb?: number;
}): RankedNode[]
```

#### Paramètres {/* #parameters-1 */}

<PropertiesTable
  content={[
  {
    name: 'query',
    type: 'number[]',
    description: "Vecteur d'embedding de la requête",
    isOptional: false,
  },
  {
    name: 'topK',
    type: 'number',
    description: 'Nombre de résultats à renvoyer',
    isOptional: true,
    defaultValue: '10',
  },
  {
    name: 'randomWalkSteps',
    type: 'number',
    description: "Nombre d'étapes de la marche aléatoire",
    isOptional: true,
    defaultValue: '100',
  },
  {
    name: 'restartProb',
    type: 'number',
    description: 'Probabilité de reprendre la marche à partir du nœud de la requête',
    isOptional: true,
    defaultValue: '0.15',
  },
]}
/>

#### Valeur renvoyée {/* #returns */}

Renvoie un tableau d'objets `RankedNode`, dans lequel chaque nœud contient :

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Identifiant unique du nœud',
  },
  {
    name: 'content',
    type: 'string',
    description: 'Contenu textuel du segment de document',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>',
    description: 'Métadonnées supplémentaires associées au segment',
  },
  {
    name: 'score',
    type: 'number',
    description: 'Score de pertinence combiné issu du parcours du graphe',
  },
]}
/>

## Exemple avancé {/* #advanced-example */}

```typescript
const graphRag = new GraphRAG({
  dimension: 1536,
  threshold: 0.8, // Stricter similarity threshold
})

// Create graph from chunks and embeddings
graphRag.createGraph(documentChunks, embeddings)

// Query with custom parameters
const results = await graphRag.query({
  query: queryEmbedding,
  topK: 5,
  randomWalkSteps: 200,
  restartProb: 0.2,
})
```

## Voir aussi {/* #related */}

- [createGraphRAGTool](../tools/graph-rag-tool)
