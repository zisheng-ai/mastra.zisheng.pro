---
title: "Référence : ExtractParams | RAG"
description: Documentation de la configuration de l'extraction de métadonnées dans Mastra.
packages:
  - "@mastra/rag"
---

# ExtractParams

ExtractParams configure l'extraction de métadonnées à partir de chunks de documents au moyen d'une analyse par LLM.

## Exemple {/* #example */}

```typescript
import { MDocument } from '@mastra/rag'

const doc = MDocument.fromText(text)
const chunks = await doc.chunk({
  extract: {
    title: true, // Extract titles using default settings
    summary: true, // Generate summaries using default settings
    keywords: true, // Extract keywords using default settings
  },
})

// Example output:
// chunks[0].metadata = {
//   documentTitle: "AI Systems Overview",
//   sectionSummary: "Overview of artificial intelligence concepts and applications",
//   excerptKeywords: "KEYWORDS: AI, machine learning, algorithms"
// }
```

## Paramètres {/* #parameters */}

Le paramètre `extract` accepte les champs suivants :

<PropertiesTable
  content={[
  {
    name: 'title',
    type: 'boolean | TitleExtractorsArgs',
    isOptional: true,
    description: "Active l'extraction du titre. Définissez la valeur sur true pour utiliser les paramètres par défaut, ou fournissez une configuration personnalisée.",
  },
  {
    name: 'summary',
    type: 'boolean | SummaryExtractArgs',
    isOptional: true,
    description: "Active l'extraction du résumé. Définissez la valeur sur true pour utiliser les paramètres par défaut, ou fournissez une configuration personnalisée.",
  },
  {
    name: 'questions',
    type: 'boolean | QuestionAnswerExtractArgs',
    isOptional: true,
    description: 'Active la génération de questions. Définissez la valeur sur true pour utiliser les paramètres par défaut, ou fournissez une configuration personnalisée.',
  },
  {
    name: 'keywords',
    type: 'boolean | KeywordExtractArgs',
    isOptional: true,
    description: "Active l'extraction des mots-clés. Définissez la valeur sur true pour utiliser les paramètres par défaut, ou fournissez une configuration personnalisée.",
  },
  {
    name: 'schema',
    type: 'SchemaExtractArgs',
    isOptional: true,
    description: "Active l'extraction structurée des métadonnées à l'aide d'un schéma Zod.",
  },
]}
/>

## Arguments des extracteurs {/* #extractor-arguments */}

### `TitleExtractorsArgs` {/* #titleextractorsargs */}

<PropertiesTable
  content={[
  {
    name: 'llm',
    type: 'MastraLanguageModel',
    isOptional: true,
    description: "Modèle de langage AI SDK à utiliser pour l'extraction du titre",
  },
  {
    name: 'nodes',
    type: 'number',
    isOptional: true,
    description: 'Nombre de nœuds de titre à extraire',
  },
  {
    name: 'nodeTemplate',
    type: 'string',
    isOptional: true,
    description: "Modèle de prompt personnalisé pour extraire les nœuds de titre. Doit inclure l'espace réservé {context}",
  },
  {
    name: 'combineTemplate',
    type: 'string',
    isOptional: true,
    description: "Modèle de prompt personnalisé pour combiner les titres. Doit inclure l'espace réservé {context}",
  },
]}
/>

### `SummaryExtractArgs` {/* #summaryextractargs */}

<PropertiesTable
  content={[
  {
    name: 'llm',
    type: 'MastraLanguageModel',
    isOptional: true,
    description: "Modèle de langage AI SDK à utiliser pour l'extraction du résumé",
  },
  {
    name: 'summaries',
    type: "('self' | 'prev' | 'next')[]",
    isOptional: true,
    description:
      "Liste des types de résumés à générer. Peut uniquement inclure 'self' (chunk actuel), 'prev' (chunk précédent) ou 'next' (chunk suivant)",
  },
  {
    name: 'promptTemplate',
    type: 'string',
    isOptional: true,
    description: "Modèle de prompt personnalisé pour générer le résumé. Doit inclure l'espace réservé {context}",
  },
]}
/>

### `QuestionAnswerExtractArgs` {/* #questionanswerextractargs */}

<PropertiesTable
  content={[
  {
    name: 'llm',
    type: 'MastraLanguageModel',
    isOptional: true,
    description: 'Modèle de langage AI SDK à utiliser pour générer les questions',
  },
  {
    name: 'questions',
    type: 'number',
    isOptional: true,
    description: 'Nombre de questions à générer',
  },
  {
    name: 'promptTemplate',
    type: 'string',
    isOptional: true,
    description:
      'Modèle de prompt personnalisé pour générer les questions. Doit inclure les espaces réservés {context} et {numQuestions}',
  },
  {
    name: 'embeddingOnly',
    type: 'boolean',
    isOptional: true,
    description: 'Si la valeur est true, génère uniquement des embeddings, sans questions',
  },
]}
/>

### `KeywordExtractArgs` {/* #keywordextractargs */}

<PropertiesTable
  content={[
  {
    name: 'llm',
    type: 'MastraLanguageModel',
    isOptional: true,
    description: "Modèle de langage AI SDK à utiliser pour l'extraction des mots-clés",
  },
  {
    name: 'keywords',
    type: 'number',
    isOptional: true,
    description: 'Nombre de mots-clés à extraire',
  },
  {
    name: 'promptTemplate',
    type: 'string',
    isOptional: true,
    description:
      "Modèle de prompt personnalisé pour extraire les mots-clés. Doit inclure les espaces réservés {context} et {maxKeywords}",
  },
]}
/>

### `SchemaExtractArgs` {/* #schemaextractargs */}

<PropertiesTable
  content={[
  {
    name: 'schema',
    type: 'ZodType',
    isOptional: false,
    description: 'Schéma Zod définissant la structure des données à extraire.',
  },
  {
    name: 'llm',
    type: 'MastraLanguageModel',
    isOptional: true,
    description: "Modèle de langage AI SDK à utiliser pour l'extraction.",
  },
  {
    name: 'instructions',
    type: 'string',
    isOptional: true,
    description: 'Instructions indiquant au LLM les éléments à extraire.',
  },
  {
    name: 'metadataKey',
    type: 'string',
    isOptional: true,
    description: "Clé sous laquelle imbriquer les résultats de l'extraction. En cas d'omission, les résultats sont répartis dans l'objet de métadonnées.",
  },
]}
/>

## Exemple avancé {/* #advanced-example */}

```typescript
import { MDocument } from '@mastra/rag'

const doc = MDocument.fromText(text)
const chunks = await doc.chunk({
  extract: {
    // Title extraction with custom settings
    title: {
      nodes: 2, // Extract 2 title nodes
      nodeTemplate: 'Generate a title for this: {context}',
      combineTemplate: 'Combine these titles: {context}',
    },

    // Summary extraction with custom settings
    summary: {
      summaries: ['self'], // Generate summaries for current chunk
      promptTemplate: 'Summarize this: {context}',
    },

    // Question generation with custom settings
    questions: {
      questions: 3, // Generate 3 questions
      promptTemplate: 'Generate {numQuestions} questions about: {context}',
      embeddingOnly: false,
    },

    // Keyword extraction with custom settings
    keywords: {
      keywords: 5, // Extract 5 keywords
      promptTemplate: 'Extract {maxKeywords} key terms from: {context}',
    },

    // Schema extraction with Zod
    schema: {
      schema: z.object({
        productName: z.string(),
        category: z.enum(['electronics', 'clothing']),
      }),
      instructions: 'Extract product information.',
      metadataKey: 'product',
    },
  },
})

// Example output:
// chunks[0].metadata = {
//   documentTitle: "AI in Modern Computing",
//   sectionSummary: "Overview of AI concepts and their applications in computing",
//   questionsThisExcerptCanAnswer: "1. What is machine learning?\n2. How do neural networks work?",
//   excerptKeywords: "1. Machine learning\n2. Neural networks\n3. Training data",
//   product: {
//     productName: "Neural Net 2000",
//     category: "electronics"
//   }
// }
```

## Regroupement de documents pour l'extraction du titre {/* #document-grouping-for-title-extraction */}

Lorsque vous utilisez `TitleExtractor`, vous pouvez regrouper plusieurs chunks pour l'extraction du titre en indiquant un `docId` commun dans le champ `metadata` de chaque chunk. Tous les chunks ayant le même `docId` recevront le même titre extrait. Si aucun `docId` n'est défini, chaque chunk est traité comme un document distinct pour l'extraction du titre.

**Exemple :**

```ts
import { MDocument } from '@mastra/rag'

const doc = new MDocument({
  docs: [
    { text: 'chunk 1', metadata: { docId: 'docA' } },
    { text: 'chunk 2', metadata: { docId: 'docA' } },
    { text: 'chunk 3', metadata: { docId: 'docB' } },
  ],
  type: 'text',
})

await doc.extractMetadata({ title: true })
// The first two chunks will share a title, while the third chunk will be assigned a separate title.
```
