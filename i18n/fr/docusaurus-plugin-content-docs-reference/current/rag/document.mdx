---
title: "Référence : MDocument | Traitement des documents | RAG"
description: Documentation de la classe MDocument de Mastra, qui assure le traitement et le découpage des documents.
packages:
  - "@mastra/rag"
---

# MDocument

La classe MDocument traite les documents destinés aux applications RAG. Ses principales méthodes sont `.chunk()` et `.extractMetadata()`.

## Constructor {/* #constructor */}

<PropertiesTable
  content={[
  {
    name: 'docs',
    type: 'Array<{ text: string, metadata?: Record<string, any> }>',
    description: 'Tableau de fragments de document avec leur contenu textuel et des métadonnées facultatives',
  },
  {
    name: 'type',
    type: "'text' | 'html' | 'markdown' | 'json' | 'latex'",
    description: 'Type de contenu du document',
  },
]}
/>

## Méthodes statiques {/* #static-methods */}

### `fromText()` {/* #fromtext */}

Crée un document à partir de contenu en texte brut.

```typescript prettier:false
static fromText(text: string, metadata?: Record<string, any>): MDocument
```

### `fromHTML()` {/* #fromhtml */}

Crée un document à partir de contenu HTML.

```typescript prettier:false
static fromHTML(html: string, metadata?: Record<string, any>): MDocument
```

### `fromMarkdown()` {/* #frommarkdown */}

Crée un document à partir de contenu Markdown.

```typescript prettier:false
static fromMarkdown(markdown: string, metadata?: Record<string, any>): MDocument
```

### `fromJSON()` {/* #fromjson */}

Crée un document à partir de contenu JSON.

```typescript prettier:false
static fromJSON(json: string, metadata?: Record<string, any>): MDocument
```

## Méthodes d’instance {/* #instance-methods */}

### `chunk()` {/* #chunk */}

Découpe le document en fragments et extrait facultativement des métadonnées.

```typescript prettier:false
async chunk(params?: ChunkParams): Promise<Chunk[]>
```

Consultez la [référence de chunk()](./chunk) pour connaître les options détaillées.

### `getDocs()` {/* #getdocs */}

Renvoie un tableau de fragments de document traités.

```typescript prettier:false
getDocs(): Chunk[]
```

### `getText()` {/* #gettext */}

Renvoie un tableau de chaînes de texte provenant des fragments.

```typescript prettier:false
getText(): string[]
```

### `getMetadata()` {/* #getmetadata */}

Renvoie un tableau d’objets de métadonnées provenant des fragments.

```typescript prettier:false
getMetadata(): Record<string, any>[]
```

### `extractMetadata()` {/* #extractmetadata */}

Extrait des métadonnées à l’aide des extracteurs indiqués. Consultez la [référence d’ExtractParams](./extract-params) pour plus de détails.

```typescript prettier:false
async extractMetadata(params: ExtractParams): Promise<MDocument>
```

## Exemples {/* #examples */}

```typescript
import { MDocument } from '@mastra/rag'

// Create document from text
const doc = MDocument.fromText('Your content here')

// Split into chunks with metadata extraction
const chunks = await doc.chunk({
  strategy: 'markdown',
  headers: [
    ['#', 'title'],
    ['##', 'section'],
  ],
  extract: {
    summary: true, // Extract summaries with default settings
    keywords: true, // Extract keywords with default settings
  },
})

// Get processed chunks
const docs = doc.getDocs()
const texts = doc.getText()
const metadata = doc.getMetadata()
```
