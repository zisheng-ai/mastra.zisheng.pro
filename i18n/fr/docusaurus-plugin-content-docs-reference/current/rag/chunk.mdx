---
title: "Référence : .chunk() | RAG"
description: Documentation de la fonction chunk de Mastra, qui divise les documents en segments plus petits au moyen de différentes stratégies.
packages:
  - "@mastra/rag"
---

# .chunk()

La fonction `.chunk()` divise les documents en segments plus petits au moyen de stratégies et d'options.

## Exemple {/* #example */}

```typescript
import { MDocument } from '@mastra/rag'

const doc = MDocument.fromMarkdown(`
# Introduction
This is a sample document that we want to split into chunks.

## Section 1
Here is the first section with some content.

## Section 2 
Here is another section with different content.
`)

// Basic chunking with defaults
const chunks = await doc.chunk()

// Markdown-specific chunking with header extraction
const chunksWithMetadata = await doc.chunk({
  strategy: 'markdown',
  headers: [
    ['#', 'title'],
    ['##', 'section'],
  ],
  extract: {
    summary: true, // Extract summaries with default settings
    keywords: true, // Extract keywords with default settings
  },
})
```

## Paramètres {/* #parameters */}

Les paramètres suivants sont disponibles pour toutes les stratégies de découpage.
Chaque stratégie n'utilise que le sous-ensemble de paramètres adapté à son cas d'utilisation.

<PropertiesTable
  content={[
  {
    name: 'strategy',
    type: "'recursive' | 'character' | 'token' | 'markdown' | 'semantic-markdown' | 'html' | 'json' | 'latex' | 'sentence'",
    isOptional: true,
    description:
      "Stratégie de découpage à utiliser. Si elle n'est pas précisée, la valeur par défaut dépend du type de document. Selon la stratégie de découpage, des options supplémentaires sont disponibles. Valeurs par défaut : fichiers .md → 'markdown', .html/.htm → 'html', .json → 'json', .tex → 'latex', autres → 'recursive'",
  },
  {
    name: 'maxSize',
    type: 'number',
    isOptional: true,
    defaultValue: '4000',
    description:
      'Taille maximale de chaque segment. Certaines configurations de stratégie (markdown avec des en-têtes, HTML avec des en-têtes) ignorent ce paramètre.',
  },
  {
    name: 'overlap',
    type: 'number',
    isOptional: true,
    defaultValue: '50',
    description: 'Nombre de caractères ou de tokens qui se chevauchent entre les segments.',
  },
  {
    name: 'lengthFunction',
    type: '(text: string) => number',
    isOptional: true,
    description: 'Fonction permettant de calculer la longueur du texte. Utilise par défaut le nombre de caractères.',
  },
  {
    name: 'separatorPosition',
    type: "'start' | 'end'",
    isOptional: true,
    description:
      "Emplacement du séparateur dans les segments. 'start' le rattache au début du segment suivant, tandis que 'end' le rattache à la fin du segment actuel. Si aucune valeur n'est précisée, les séparateurs sont supprimés.",
  },
  {
    name: 'addStartIndex',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: "Indique s'il faut ajouter aux segments les métadonnées de l'index de départ.",
  },
  {
    name: 'stripWhitespace',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'true',
    description: "Indique s'il faut supprimer les espaces des segments.",
  },
  {
    name: 'extract',
    type: 'ExtractParams',
    isOptional: true,
    description: "Configuration de l'extraction des métadonnées.",
  },
]}
/>

Consultez la [référence d'ExtractParams](/reference/rag/extract-params) pour plus de détails sur le paramètre `extract`.

## Options propres aux stratégies {/* #strategy-specific-options */}

Les options propres à une stratégie sont transmises comme paramètres de premier niveau avec le paramètre de stratégie. Par exemple :

```typescript
// Character strategy example
const chunks = await doc.chunk({
  strategy: 'character',
  separator: '.', // Character-specific option
  isSeparatorRegex: false, // Character-specific option
  maxSize: 300, // general option
})

// Recursive strategy example
const chunks = await doc.chunk({
  strategy: 'recursive',
  separators: ['\n\n', '\n', ' '], // Recursive-specific option
  language: 'markdown', // Recursive-specific option
  maxSize: 500, // general option
})

// Sentence strategy example
const chunks = await doc.chunk({
  strategy: 'sentence',
  maxSize: 450, // Required for sentence strategy
  minSize: 50, // Sentence-specific option
  sentenceEnders: ['.'], // Sentence-specific option
  fallbackToCharacters: false, // Sentence-specific option
})

// HTML strategy example
const chunks = await doc.chunk({
  strategy: 'html',
  headers: [
    ['h1', 'title'],
    ['h2', 'subtitle'],
  ], // HTML-specific option
})

// Markdown strategy example
const chunks = await doc.chunk({
  strategy: 'markdown',
  headers: [
    ['#', 'title'],
    ['##', 'section'],
  ], // Markdown-specific option
  stripHeaders: true, // Markdown-specific option
})

// Semantic Markdown strategy example
const chunks = await doc.chunk({
  strategy: 'semantic-markdown',
  joinThreshold: 500, // Semantic Markdown-specific option
  modelName: 'gpt-3.5-turbo', // Semantic Markdown-specific option
})

// Token strategy example
const chunks = await doc.chunk({
  strategy: 'token',
  encodingName: 'gpt2', // Token-specific option
  modelName: 'gpt-3.5-turbo', // Token-specific option
  maxSize: 1000, // general option
})
```

Les options décrites ci-dessous sont transmises directement au premier niveau de l'objet de configuration, et non imbriquées dans un objet d'options distinct.

### Character {/* #character */}

<PropertiesTable
  content={[
  {
    name: 'separators',
    type: 'string[]',
    isOptional: true,
    description:
      "Tableau de séparateurs à essayer par ordre de préférence. La stratégie tente d'effectuer le découpage sur le premier séparateur, puis utilise les suivants comme solutions de repli.",
  },
  {
    name: 'isSeparatorRegex',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: 'Indique si le séparateur est un motif regex',
  },
]}
/>

### Recursive {/* #recursive */}

<PropertiesTable
  content={[
  {
    name: 'separators',
    type: 'string[]',
    isOptional: true,
    description:
      "Tableau de séparateurs à essayer par ordre de préférence. La stratégie tente d'effectuer le découpage sur le premier séparateur, puis utilise les suivants comme solutions de repli.",
  },
  {
    name: 'isSeparatorRegex',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: 'Indique si les séparateurs sont des motifs regex',
  },
  {
    name: 'language',
    type: 'Language',
    isOptional: true,
    description:
      "Langage de programmation ou de balisage utilisé pour un comportement de découpage propre au langage. Consultez l'enum Language pour connaître les valeurs prises en charge.",
  },
]}
/>

### Sentence {/* #sentence */}

<PropertiesTable
  content={[
  {
    name: 'maxSize',
    type: 'number',
    description: 'Taille maximale de chaque segment (obligatoire pour la stratégie sentence)',
  },
  {
    name: 'minSize',
    type: 'number',
    isOptional: true,
    defaultValue: '50',
    description:
      'Taille minimale de chaque segment. Dans la mesure du possible, les segments plus petits sont fusionnés avec les segments adjacents.',
  },
  {
    name: 'targetSize',
    type: 'number',
    isOptional: true,
    description:
      'Taille cible privilégiée des segments. Utilise par défaut 80 % de maxSize. La stratégie tente de créer des segments proches de cette taille.',
  },
  {
    name: 'sentenceEnders',
    type: 'string[]',
    isOptional: true,
    defaultValue: "['.', '!', '?']",
    description: 'Tableau de caractères marquant les fins de phrase utilisées comme limites de découpage.',
  },
  {
    name: 'fallbackToWords',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'true',
    description: 'Indique si un découpage au niveau des mots doit être utilisé pour les phrases qui dépassent maxSize.',
  },
  {
    name: 'fallbackToCharacters',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'true',
    description:
      'Indique si un découpage au niveau des caractères doit être utilisé pour les mots qui dépassent maxSize. Ne s\'applique que si fallbackToWords est activé.',
  },
]}
/>

### HTML {/* #html */}

<PropertiesTable
  content={[
  {
    name: 'headers',
    type: 'Array<[string, string]>',
    description: "Tableau de paires [sélecteur, clé de métadonnées] pour le découpage fondé sur les en-têtes",
  },
  {
    name: 'sections',
    type: 'Array<[string, string]>',
    description: 'Tableau de paires [sélecteur, clé de métadonnées] pour le découpage fondé sur les sections',
  },
  {
    name: 'returnEachLine',
    type: 'boolean',
    isOptional: true,
    description: 'Indique si chaque ligne doit être renvoyée comme un segment distinct',
  },
]}
/>

Lorsque vous utilisez la stratégie HTML, toutes les options générales sont ignorées. Utilisez `headers` pour le découpage fondé sur les en-têtes ou `sections` pour celui fondé sur les sections. Si les deux sont utilisés, `sections` est ignoré.

### Markdown {/* #markdown */}

<PropertiesTable
  content={[
  {
    name: 'headers',
    type: 'Array<[string, string]>',
    isOptional: true,
    description: "Tableau de paires [niveau d'en-tête, clé de métadonnées]",
  },
  {
    name: 'stripHeaders',
    type: 'boolean',
    isOptional: true,
    description: 'Indique si les en-têtes doivent être supprimés de la sortie',
  },
  {
    name: 'returnEachLine',
    type: 'boolean',
    isOptional: true,
    description: 'Indique si chaque ligne doit être renvoyée comme un segment distinct',
  },
]}
/>

Lorsque vous utilisez l'option `headers`, la stratégie markdown ignore toutes les options générales et découpe le contenu selon la structure des en-têtes markdown. Pour utiliser avec markdown un découpage fondé sur la taille, omettez le paramètre `headers`.

### Semantic Markdown {/* #semantic-markdown */}

<PropertiesTable
  content={[
  {
    name: 'joinThreshold',
    type: 'number',
    isOptional: true,
    defaultValue: '500',
    description:
      'Nombre maximal de tokens pour fusionner les sections associées. Les sections qui dépassent individuellement cette limite restent intactes, tandis que les plus petites sont fusionnées avec leurs sections sœurs ou parentes si leur taille combinée demeure sous ce seuil.',
  },
  {
    name: 'modelName',
    type: 'string',
    isOptional: true,
    description:
      "Nom du modèle utilisé pour la tokenisation. S'il est fourni, son `encodingName` de tokenisation sous-jacent est utilisé.",
  },
  {
    name: 'encodingName',
    type: 'string',
    isOptional: true,
    defaultValue: 'cl100k_base',
    description: "Nom de l'encodage des tokens à utiliser. Dérivé de `modelName` lorsqu'il est disponible.",
  },
  {
    name: 'allowedSpecial',
    type: "Set<string> | 'all'",
    isOptional: true,
    description: "Ensemble des tokens spéciaux autorisés pendant la tokenisation, ou 'all' pour tous les autoriser",
  },
  {
    name: 'disallowedSpecial',
    type: "Set<string> | 'all'",
    isOptional: true,
    defaultValue: 'all',
    description: "Ensemble des tokens spéciaux à interdire pendant la tokenisation, ou 'all' pour tous les interdire",
  },
]}
/>

### Token {/* #token */}

<PropertiesTable
  content={[
  {
    name: 'encodingName',
    type: 'string',
    isOptional: true,
    description: "Nom de l'encodage des tokens à utiliser",
  },
  {
    name: 'modelName',
    type: 'string',
    isOptional: true,
    description: 'Nom du modèle utilisé pour la tokenisation',
  },
  {
    name: 'allowedSpecial',
    type: "Set<string> | 'all'",
    isOptional: true,
    description: "Ensemble des tokens spéciaux autorisés pendant la tokenisation, ou 'all' pour tous les autoriser",
  },
  {
    name: 'disallowedSpecial',
    type: "Set<string> | 'all'",
    isOptional: true,
    description: "Ensemble des tokens spéciaux à interdire pendant la tokenisation, ou 'all' pour tous les interdire",
  },
]}
/>

### JSON {/* #json */}

<PropertiesTable
  content={[
  {
    name: 'maxSize',
    type: 'number',
    description: 'Taille maximale de chaque segment',
  },
  {
    name: 'minSize',
    type: 'number',
    isOptional: true,
    description: 'Taille minimale de chaque segment',
  },
  {
    name: 'ensureAscii',
    type: 'boolean',
    isOptional: true,
    description: "Indique s'il faut garantir l'encodage ASCII",
  },
  {
    name: 'convertLists',
    type: 'boolean',
    isOptional: true,
    description: 'Indique si les listes doivent être converties dans le JSON',
  },
]}
/>

### Latex {/* #latex */}

La stratégie Latex utilise uniquement les options générales de découpage répertoriées ci-dessus. Elle fournit un découpage tenant compte de LaTeX et optimisé pour les documents mathématiques et universitaires.

## Valeur de retour {/* #return-value */}

Renvoie une instance de `MDocument` contenant les documents découpés. Chaque segment comprend :

```typescript
interface DocumentNode {
  text: string
  metadata: Record<string, any>
  embedding?: number[]
}
```
