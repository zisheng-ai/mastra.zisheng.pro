---
title: "AI-ROUTER | 模型"
description: "在 Mastra 中使用 AI-ROUTER 模型。共有 5 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/ai-router.svg" alt="AI-ROUTER 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />AI-ROUTER

透過 Mastra 的模型路由器存取 5 個 AI-ROUTER 模型。系統會自動使用 `AI_ROUTER_API_KEY` 環境變數完成身分驗證。

如需進一步了解，請參閱[AI-ROUTER 文件](https://ai-router.dev/openai-compatible-api-gateway/)。

```bash title=".env"
AI_ROUTER_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'ai-router/gpt-5.4',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` endpoint。部分 Provider 特有功能可能無法使用，詳情請參閱[AI-ROUTER 文件](https://ai-router.dev/openai-compatible-api-gateway/)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'ai-router/gpt-5.4',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1050000,
    maxOutput: 128000,
    inputCost: 2.5,
    outputCost: 15,
  },
  {
    model: 'ai-router/gpt-5.5',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1050000,
    maxOutput: 128000,
    inputCost: 5,
    outputCost: 30,
  },
  {
    model: 'ai-router/gpt-5.6-luna',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1050000,
    maxOutput: 128000,
    inputCost: 1,
    outputCost: 6,
  },
  {
    model: 'ai-router/gpt-5.6-sol',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1050000,
    maxOutput: 128000,
    inputCost: 5,
    outputCost: 30,
  },
  {
    model: 'ai-router/gpt-5.6-terra',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1050000,
    maxOutput: 128000,
    inputCost: 2.5,
    outputCost: 15,
  },
]}
/>

## 進階設定 {/* #advanced-configuration */}

### 自訂標頭 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://api.ai-router.dev/v1',
    id: 'ai-router/gpt-5.4',
    apiKey: process.env.AI_ROUTER_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動態選擇模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'ai-router/gpt-5.6-terra' : 'ai-router/gpt-5.4'
  },
})
```
