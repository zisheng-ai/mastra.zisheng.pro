---
title: "Upstage | 模型"
description: "在 Mastra 中使用 Upstage 模型。共有 4 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/upstage.svg" alt="Upstage 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Upstage

透過 Mastra 的模型路由器存取 4 個 Upstage 模型。系統會自動使用 `UPSTAGE_API_KEY` 環境變數完成身分驗證。

如需進一步了解，請參閱[Upstage 文件](https://developers.upstage.ai)。

```bash title=".env"
UPSTAGE_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "upstage/solar-mini"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` endpoint。部分 Provider 特有功能可能無法使用，詳情請參閱[Upstage 文件](https://developers.upstage.ai)。

:::

## 模型

<ProviderModelsTable
  models={[
  {
    model: 'upstage/solar-mini',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 32768,
    maxOutput: 4096,
    inputCost: 0.15,
    outputCost: 0.15,
  },
  {
    model: 'upstage/solar-pro2',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 65536,
    maxOutput: 8192,
    inputCost: 0.25,
    outputCost: 0.25,
  },
  {
    model: 'upstage/solar-pro3',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 8192,
    inputCost: 0.25,
    outputCost: 0.25,
  },
  {
    model: 'upstage/solar-pro4',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 524288,
    maxOutput: 131072,
    inputCost: 0.3,
    outputCost: 1.2,
  },
]}
/>

## 進階設定

### 自訂標頭

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.upstage.ai/v1/solar",
    id: "upstage/solar-mini",
    apiKey: process.env.UPSTAGE_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動態選擇模型

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "upstage/solar-pro4"
      : "upstage/solar-mini";
  }
});
```
