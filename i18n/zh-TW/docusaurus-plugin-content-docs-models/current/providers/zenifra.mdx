---
title: "Zenifra | 模型"
description: "在 Mastra 中使用 Zenifra 模型。共有 1 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/zenifra.svg" alt="Zenifra 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Zenifra

透過 Mastra 的模型路由器存取 1 個 Zenifra 模型。系統會自動使用 `ZENIFRA_AI_KEY` 環境變數完成身分驗證。

如需進一步了解，請參閱[Zenifra 文件](https://docs.zenifra.com)。

```bash title=".env"
ZENIFRA_AI_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'zenifra/alibaba/qwen3.6-35b-a3b',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` endpoint。部分 Provider 特有功能可能無法使用，詳情請參閱[Zenifra 文件](https://docs.zenifra.com)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'zenifra/alibaba/qwen3.6-35b-a3b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 65536,
    inputCost: 0.19,
    outputCost: 0.48,
  },
]}
/>

## 進階設定 {/* #advanced-configuration */}

### 自訂標頭 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://ai.zenifra.com/v1',
    id: 'zenifra/alibaba/qwen3.6-35b-a3b',
    apiKey: process.env.ZENIFRA_AI_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動態選擇模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'zenifra/alibaba/qwen3.6-35b-a3b' : 'zenifra/alibaba/qwen3.6-35b-a3b'
  },
})
```
