---
title: "AKI.IO | 模型"
description: "在 Mastra 中使用 AKI.IO 模型。共有 6 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/aki-io.svg" alt="AKI.IO 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />AKI.IO

透過 Mastra 的模型路由器存取 6 個 AKI.IO 模型。系統會自動使用 `AKI_IO_API_KEY` 環境變數完成身分驗證。

如需進一步了解，請參閱[AKI.IO 文件](https://aki.io/docs/)。

```bash title=".env"
AKI_IO_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "aki-io/gemma4-26b"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` endpoint。部分 Provider 特有功能可能無法使用，詳情請參閱[AKI.IO 文件](https://aki.io/docs/)。

:::

## 模型

<ProviderModelsTable
  models={[
  {
    model: 'aki-io/gemma4-26b',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 32768,
    inputCost: 0.1,
    outputCost: 0.5,
  },
  {
    model: 'aki-io/gpt-oss-120b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 128000,
    maxOutput: 32768,
    inputCost: 0.15,
    outputCost: 0.55,
  },
  {
    model: 'aki-io/kimi-k2.7-code-1100b',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 81920,
    inputCost: 0.86,
    outputCost: 3,
  },
  {
    model: 'aki-io/minimax-m2.5-230b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 196608,
    maxOutput: 65536,
    inputCost: 0.25,
    outputCost: 1.2,
  },
  {
    model: 'aki-io/mistral4-119b',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 81920,
    inputCost: 0.2,
    outputCost: 0.6,
  },
  {
    model: 'aki-io/qwen3.6-35b',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 32768,
    inputCost: 0.15,
    outputCost: 0.5,
  },
]}
/>

## 進階設定

### 自訂標頭

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://aki.io/v1",
    id: "aki-io/gemma4-26b",
    apiKey: process.env.AKI_IO_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動態選擇模型

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "aki-io/qwen3.6-35b"
      : "aki-io/gemma4-26b";
  }
});
```
