---
title: "Cerebras | 模型"
description: "在 Mastra 中使用 Cerebras 模型。共有 3 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/cerebras.svg" alt="Cerebras 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Cerebras

透過 Mastra 的模型路由器存取 3 個 Cerebras 模型。系統會自動使用 `CEREBRAS_API_KEY` 環境變數完成身分驗證。

如需進一步了解，請參閱[Cerebras 文件](https://inference-docs.cerebras.ai/models/overview)。

```bash title=".env"
CEREBRAS_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "cerebras/gemma-4-31b"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

## 模型

<ProviderModelsTable
  models={[
  {
    model: 'cerebras/gemma-4-31b',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 40960,
    inputCost: 0.99,
    outputCost: 1.49,
  },
  {
    model: 'cerebras/gpt-oss-120b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 40960,
    inputCost: 0.35,
    outputCost: 0.75,
  },
  {
    model: 'cerebras/zai-glm-4.7',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 40960,
    inputCost: 2.25,
    outputCost: 2.75,
  },
]}
/>

## 進階設定

### 自訂標頭

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    id: "cerebras/gemma-4-31b",
    apiKey: process.env.CEREBRAS_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動態選擇模型

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "cerebras/zai-glm-4.7"
      : "cerebras/gemma-4-31b";
  }
});
```

## 直接安裝 Provider

你也可以直接將此 Provider 安裝為獨立套件，用它取代 Mastra 模型路由字串。詳情請參閱[套件文件](https://www.npmjs.com/package/@ai-sdk/cerebras)。

```bash npm2yarn
npm install @ai-sdk/cerebras
```

如需此 Provider 的詳細說明，請參閱 [AI SDK Cerebras Provider 文件](https://ai-sdk.dev/providers/ai-sdk-providers/cerebras)。
