---
title: "參考：BatchPartsProcessor | 處理器"
description: "Mastra BatchPartsProcessor 的文件；此處理器會將多個串流部分批次合併，以降低發送頻率。"
packages:
  - "@mastra/core"
---

# BatchPartsProcessor

`BatchPartsProcessor` 是一種**輸出處理器**，會將多個串流部分批次合併，以降低串流期間的發送頻率。此處理器能減少網路負擔、將零碎的文字區塊合併以改善使用者體驗，並透過控制各部分傳送至使用者端的時機來最佳化串流效能。

## 使用範例 {/* #usage-example */}

```typescript
import { BatchPartsProcessor } from '@mastra/core/processors'

const processor = new BatchPartsProcessor({
  batchSize: 5,
  maxWaitTime: 100,
  emitOnNonText: true,
})
```

## 建構函式參數 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'Options',
    description: '用於批次合併串流部分的設定選項',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'batchSize',
            type: 'number',
            description: '發送前要合併成一個批次的部分數量',
            isOptional: true,
            default: '5',
          },
          {
            name: 'maxWaitTime',
            type: 'number',
            description: '發送批次前的最長等待時間（毫秒）。若已設定，即使目前批次尚未達到 batchSize，也會將其發送',
            isOptional: true,
            default: 'undefined (no timeout)',
          },
          {
            name: 'emitOnNonText',
            type: 'boolean',
            description: '遇到非文字部分時是否立即發送',
            isOptional: true,
            default: 'true',
          },
        ],
      },
    ],
  },
]}
/>

## 回傳值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "設為 'batch-parts' 的處理器識別碼",
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: '選用的處理器顯示名稱',
    isOptional: true,
  },
  {
    name: 'processOutputStream',
    type: '(args: { part: ChunkType; streamParts: ChunkType[]; state: Record<string, any>; abort: (reason?: string) => never }) => Promise<ChunkType | null>',
    description: '處理串流輸出部分，以將其批次合併',
    isOptional: false,
  },
  {
    name: 'flush',
    type: '(state?: BatchPartsState) => ChunkType | null',
    description: '串流結束時，強制送出批次中所有剩餘的部分',
    isOptional: false,
  },
]}
/>

## 進階使用範例 {/* #extended-usage-example */}

```typescript title="src/mastra/agents/batched-agent.ts"
import { Agent } from '@mastra/core/agent'
import { BatchPartsProcessor } from '@mastra/core/processors'

export const agent = new Agent({
  id: 'batched-agent',
  name: 'batched-agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
  outputProcessors: [
    new BatchPartsProcessor({
      batchSize: 5,
      maxWaitTime: 100,
      emitOnNonText: true,
    }),
  ],
})
```

## 相關資源 {/* #related */}

- [防護機制](/docs/agents/guardrails)
