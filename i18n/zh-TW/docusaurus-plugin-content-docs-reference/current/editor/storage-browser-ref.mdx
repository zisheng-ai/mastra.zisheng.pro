---
title: "參考：StorageBrowserRef | Editor"
description: "StorageBrowserRef 與 StorageBrowserConfig 的參考文件；這是附加至已儲存 Agent 的內嵌瀏覽器設定。"
packages:
  - "@mastra/core"
---

# StorageBrowserRef

`StorageBrowserRef` 是附加至已儲存 Agent 的內嵌瀏覽器設定。具現化時，會使用在 [`MastraEditor.browsers`](/reference/editor/mastra-editor) 上註冊的 [`BrowserProvider`](/reference/editor/browser-provider) 解析 `provider` ID。

這是 [`BuilderAgentDefaults.browser`](https://agent-builder.mastra.ai/reference/builder-agent-defaults) 與已儲存 Agent 記錄所使用的類型。

## 使用範例

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'
import { stagehandBrowserProvider } from '@mastra/stagehand'

new MastraEditor({
  browsers: { [stagehandBrowserProvider.id]: stagehandBrowserProvider },
  builder: {
    enabled: true,
    configuration: {
      agent: {
        browser: {
          type: 'inline',
          config: {
            provider: 'stagehand',
            headless: true,
            viewport: { width: 1280, height: 720 },
          },
        },
      },
    },
  },
})
```

## 類型

```typescript
type StorageBrowserRef = { type: 'inline'; config: StorageBrowserConfig }
```

瀏覽器不提供 `{ type: 'id' }` 變體，一律使用內嵌設定。

## 屬性

<PropertiesTable
  content={[
  {
    name: 'type',
    type: "'inline'",
    description: '判別欄位。必須是常值 `"inline"`。',
  },
  {
    name: 'config',
    type: 'StorageBrowserConfig',
    description: 'Provider ID 與各執行個體的瀏覽器設定。請參閱下一節。',
  },
]}
/>

## `StorageBrowserConfig`

內嵌於 `config` 下的資料結構，定義於 `@mastra/core/storage`。

<PropertiesTable
  content={[
  {
    name: 'provider',
    type: 'string',
    description:
      'Provider 識別碼（例如 `"stagehand"`）。必須與 `MastraEditor.browsers` 上註冊的 `BrowserProvider.id` 相符。目前沒有內建 Provider。',
  },
  {
    name: 'headless',
    type: 'boolean',
    description: '以 headless 模式執行瀏覽器（不顯示 UI）。',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'viewport',
    type: '{ width: number; height: number }',
    description: '瀏覽器檢視區尺寸，控制視窗大小與頁面呈現方式。',
    isOptional: true,
  },
  {
    name: 'timeout',
    type: 'number',
    description: '瀏覽器操作的預設逾時時間，以毫秒為單位。',
    isOptional: true,
    defaultValue: '10000',
  },
  {
    name: 'screencast',
    type: 'ScreencastOptions',
    description: '將瀏覽器畫面串流至 UI 的螢幕錄製選項。',
    isOptional: true,
    properties: [
      {
        type: 'ScreencastOptions',
        parameters: [
          {
            name: 'format',
            type: "'jpeg' | 'png'",
            description: '圖片格式。',
            isOptional: true,
            defaultValue: "'jpeg'",
          },
          {
            name: 'quality',
            type: 'number',
            description: 'JPEG 品質，範圍為 0–100。',
            isOptional: true,
            defaultValue: '80',
          },
          {
            name: 'maxWidth',
            type: 'number',
            description: '最大寬度，以像素為單位。',
            isOptional: true,
            defaultValue: '1280',
          },
          {
            name: 'maxHeight',
            type: 'number',
            description: '最大高度，以像素為單位。',
            isOptional: true,
            defaultValue: '720',
          },
          {
            name: 'everyNthFrame',
            type: 'number',
            description: '每 N 個影格擷取一次。',
            isOptional: true,
            defaultValue: '1',
          },
        ],
      },
    ],
  },
]}
/>

## 具現化 {#hydration}

`StorageBrowserRef` 會在 `mastra.editor.agent.getById()` 中延遲解析。Editor 會在 `MastraEditor.browsers` 上查詢 `config.provider`，並呼叫 `provider.createBrowser(config)`。如果未註冊該 Provider，Editor 會記錄警告並傳回 `undefined`：Agent 仍會載入，但不含瀏覽器。

## 相關內容

- [Browser](https://agent-builder.mastra.ai/browser)：概念與完整範例。
- [BrowserProvider](/reference/editor/browser-provider)：供實作者使用的 Provider 介面。
- [BuilderAgentDefaults](https://agent-builder.mastra.ai/reference/builder-agent-defaults)：將此類型固定為 Builder 預設值的位置。
- [StorageWorkspaceRef](/reference/editor/storage-workspace-ref)：Workspace 設定的同類參照類型。
