---
title: "參考：StorageWorkspaceRef | Editor"
description: "StorageWorkspaceRef 的參考文件；這是將已儲存 Agent 指向 Workspace 的判別聯集。"
packages:
  - "@mastra/core"
---

# StorageWorkspaceRef

`StorageWorkspaceRef` 是用於將 Workspace 附加至已儲存 Agent 的判別聯集。它可以依 ID 指向在 Mastra 執行階段註冊的 Workspace，也可以直接內嵌 Workspace 快照。

這是 [`BuilderAgentDefaults.workspace`](https://agent-builder.mastra.ai/reference/builder-agent-defaults) 與已儲存 Agent 記錄所使用的類型。

## 使用範例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'

// Variant 1 — reference a runtime workspace by id
new MastraEditor({
  builder: {
    enabled: true,
    configuration: {
      agent: {
        workspace: { type: 'id', workspaceId: 'builder-workspace' },
      },
    },
  },
})

// Variant 2 — inline a workspace snapshot
new MastraEditor({
  builder: {
    enabled: true,
    configuration: {
      agent: {
        workspace: {
          type: 'inline',
          config: {
            name: 'inline-builder-workspace',
            filesystem: {
              provider: 'local',
              config: { basePath: './agent-files' },
            },
          },
        },
      },
    },
  },
})
```

## 變體 {/* #variants */}

<PropertiesTable
  content={[
  {
    name: "{ type: 'id'; workspaceId: string }",
    type: 'IdRef',
    description:
      '參照透過 `new Mastra({ workspace })` 或 `mastra.addWorkspace()` 在 Mastra 執行階段註冊的 Workspace。此 ID 是管理員設定、已儲存 Agent 記錄與具現化之間的連接鍵。',
    properties: [
      {
        type: "{ type: 'id'; workspaceId: string }",
        parameters: [
          {
            name: 'type',
            type: "'id'",
            description: '判別欄位。必須是常值 `"id"`。',
          },
          {
            name: 'workspaceId',
            type: 'string',
            description:
              '在 Mastra 執行階段註冊的 Workspace ID。Builder 會使用此值擷取快照、保存 Workspace，並於稍後進行具現化。',
          },
        ],
      },
    ],
  },
  {
    name: "{ type: 'inline'; config: StorageWorkspaceSnapshotType }",
    type: 'InlineRef',
    description:
      '直接內嵌 Workspace 快照。Builder 會衍生格式為 `inline-<sha256(config)[:12]>` 的確定性 ID 並保存快照，因此 Agent 之間相同的內嵌設定會去除重複。',
    properties: [
      {
        type: "{ type: 'inline'; config: StorageWorkspaceSnapshotType }",
        parameters: [
          {
            name: 'type',
            type: "'inline'",
            description: '判別欄位。必須是常值 `"inline"`。',
          },
          {
            name: 'config',
            type: 'StorageWorkspaceSnapshotType',
            description:
              '序列化的 Workspace 設定（檔案系統、Sandbox、掛載、搜尋、Skill、Tool）。欄位清單請參閱下方的 `StorageWorkspaceSnapshotType` 一節。',
          },
        ],
      },
    ],
  },
]}
/>

## `StorageWorkspaceSnapshotType` {/* #storageworkspacesnapshottype */}

內嵌於 `{ type: 'inline', config }` 下的資料結構，定義於 `@mastra/core/storage`。

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: 'Workspace 的顯示名稱。',
  },
  {
    name: 'description',
    type: 'string',
    description: 'Workspace 清單中顯示的用途說明。',
    isOptional: true,
  },
  {
    name: 'filesystem',
    type: 'StorageFilesystemConfig',
    description:
      '主要檔案系統設定。`provider` 必須與 `MastraEditor.filesystems` 上註冊的 ID 相符（內建：`local`）。',
    isOptional: true,
  },
  {
    name: 'sandbox',
    type: 'StorageSandboxConfig',
    description:
      'Sandbox 設定。`provider` 必須與 `MastraEditor.sandboxes` 上註冊的 ID 相符（內建：`local`）。',
    isOptional: true,
  },
  {
    name: 'mounts',
    type: 'Record<string, StorageFilesystemConfig>',
    description: '掛載至 Workspace 的其他檔案系統，以掛載路徑作為鍵。',
    isOptional: true,
  },
  {
    name: 'search',
    type: 'StorageSearchConfig',
    description: '搜尋設定（向量 Provider、embedder、BM25 設定）。',
    isOptional: true,
  },
  {
    name: 'skills',
    type: 'string[]',
    description: '指派給此 Workspace 的 Skill 實體 ID。',
    isOptional: true,
  },
  {
    name: 'tools',
    type: 'StorageWorkspaceToolsConfig',
    description: 'Workspace Tool 設定（允許清單、預設值）。',
    isOptional: true,
  },
  {
    name: 'autoSync',
    type: 'boolean',
    description: '自動同步檔案系統與 Sandbox。',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'operationTimeout',
    type: 'number',
    description: '個別操作的逾時時間，以毫秒為單位。',
    isOptional: true,
  },
]}
/>

## 具現化 {/* #hydration */}

`StorageWorkspaceRef` 會在 `mastra.editor.agent.getById()` 中延遲解析，而不是在 `list()` 中解析。Editor 會在 `MastraEditor.filesystems` / `MastraEditor.sandboxes` 上查詢檔案系統與 Sandbox Provider，並具現化執行階段 `Workspace`。缺少 Provider 時會顯示警告，並省略 Workspace。

## 相關內容 {/* #related */}

- [Workspace](https://agent-builder.mastra.ai/workspace)：概念與完整範例。
- [BuilderAgentDefaults](https://agent-builder.mastra.ai/reference/builder-agent-defaults)：將此類型固定為 Builder 預設值的位置。
- [MastraEditor 類別](/reference/editor/mastra-editor)：註冊檔案系統與 Sandbox Provider。
- [StorageBrowserRef](/reference/editor/storage-browser-ref)：瀏覽器設定的同類參照類型。
