---
title: "參考：Mastra.getAgentById() | Core"
description: "關於 `Mastra.getAgentById()` 方法的說明文件，該方法依 ID 取得 Agent。"
packages:
  - "@mastra/core"
---

# Mastra.getAgentById()

`.getAgentById()` 方法依 `id` 取得已註冊的 Agent。它首先依 `agent.id` 搜尋已註冊的 Agent。如果沒有相符項，則改用 [`Mastra.getAgent()`](/reference/core/getAgent)，並將提供的值視為 Agent 註冊鍵。

使用 `mastra.getAgentById(id)` 取得程式碼中定義的 Agent。使用 `await mastra.getAgentById(id, version)` 取得版本化 Agent。

## 使用範例 {/* #usage-example */}

以下範例註冊一個 Agent，依 ID 取得它，然後使用傳回的 Agent。

```typescript title="src/mastra/index.ts"
import { Agent } from '@mastra/core/agent'
import { Mastra } from '@mastra/core/mastra'

const supportAgent = new Agent({
  id: 'support-agent-id',
  name: 'Support Agent',
  instructions: 'Answer support questions clearly and concisely.',
  model: '__GATEWAY_OPENAI_MODEL__',
})

export const mastra = new Mastra({
  agents: {
    supportAgent,
  },
})

const agent = mastra.getAgentById('support-agent-id')

const response = await agent.generate('How can I reset my password?')
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description:
      '要取得的 Agent ID。Mastra 首先依 `agent.id` 搜尋已註冊的 Agent。如果沒有相符項，則將此值用作 Agent 註冊鍵並呼叫 `getAgent()`。',
  },
  {
    name: 'version',
    type: "{ versionId: string } | { status?: 'draft' | 'published' }",
    description: '用於取得版本化 Agent 的選用版本選擇器。提供該參數時，此方法傳回 Promise。',
    isOptional: true,
    properties: [
      {
        type: 'VersionSelector',
        parameters: [
          {
            name: 'versionId',
            type: 'string',
            description: '要取得的特定 Agent 版本的 ID。',
          },
          {
            name: 'status',
            type: "'draft' | 'published'",
            description: '選擇具有此發布狀態的最新 Agent 版本。',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'TAgents[TAgentName]',
    description: '省略 `version` 時，傳回具有指定 ID 的 Agent 執行個體。',
  },
  {
    name: 'agent',
    type: 'Promise<TAgents[TAgentName]>',
    description: '提供 `version` 時，傳回解析為版本化 Agent 執行個體的 Promise。',
  },
]}
/>

依 ID 或註冊鍵找不到 Agent 時，會擲回 `MastraError`。

## 相關內容 {/* #related */}

- [Mastra.getAgent()](/reference/core/getAgent)
- [Agent 概覽](/docs/agents/overview)
