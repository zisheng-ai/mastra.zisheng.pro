---
title: "參考：Mastra.listWorkflows() | Core"
description: "關於 `Mastra.listWorkflows()` 方法的說明文件，該方法取得所有已設定的 Workflow。"
packages:
  - "@mastra/core"
---

# Mastra.listWorkflows()

`.listWorkflows()` 方法用於取得 Mastra 執行個體中已設定的所有 Workflow。此方法接受一個選用的 options 物件。

## 使用範例

```typescript
mastra.listWorkflows()
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ serialized?: boolean }',
    description:
      '選用設定物件。當 `serialized` 為 true 時，傳回僅包含 name 屬性的簡化 Workflow 物件，而非完整的 Workflow 執行個體。',
    optional: true,
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'workflows',
    type: 'Record<string, Workflow>',
    description:
      '包含所有已設定 Workflow 的記錄，其中鍵為 Workflow ID，值為 Workflow 執行個體（如果 serialized 為 true，則為簡化物件）。',
  },
]}
/>

## 相關內容

- [Workflow 概覽](/docs/workflows/overview)
