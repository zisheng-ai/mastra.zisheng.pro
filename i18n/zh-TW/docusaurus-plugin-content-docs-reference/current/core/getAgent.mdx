---
title: "參考：Mastra.getAgent() | Core"
description: "關於 `Agent.getAgent()` 方法的說明文件，該方法依名稱取得 Agent。"
packages:
  - "@mastra/core"
---

# Mastra.getAgent()

`.getAgent()` 方法用於取得 Agent。此方法接受一個 `string` 參數，用於指定 Agent 名稱。

## 使用範例

```typescript
mastra.getAgent('testAgent')
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'TAgentName extends keyof TAgents',
    description: '要取得的 Agent 名稱。必須是 Mastra 設定中存在的有效 Agent 名稱。',
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'TAgents[TAgentName]',
    description: '具有指定名稱的 Agent 實例。如果找不到該 Agent，則擲回錯誤。',
  },
]}
/>

## 相關內容

- [Agent 概覽](/docs/agents/overview)
