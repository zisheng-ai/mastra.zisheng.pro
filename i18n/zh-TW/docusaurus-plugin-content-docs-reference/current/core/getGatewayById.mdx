---
title: "參考：Mastra.getGatewayById() | Core"
description: "依唯一 ID 取得已註冊的 gateway"
packages:
  - "@mastra/core"
---

# Mastra.getGatewayById()

依唯一 ID（gateway 執行個體的 `id` 屬性）取得 gateway。需要依本身的識別碼而非註冊鍵尋找 gateway 時，此方法非常有用。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({
  gateways: {
    customKey: new MyCustomGateway(), // id = 'my-custom-gateway'
  },
})

// Retrieve by ID (not registration key)
const gateway = mastra.getGatewayById('my-custom-gateway')
console.log(gateway.name) // 'My Custom Gateway'
```

## 使用情境 {/* #use-cases */}

- Gateway 版本控制：不同版本使用不同的唯一 ID (`'gateway-v1'`, `'gateway-v2'`)
- 在註冊鍵未知時尋找 gateway
- 跨不同 Mastra 執行個體識別 gateway

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'gateway 的唯一 ID（gateway.id 屬性）',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'gateway',
    type: 'MastraModelGateway | undefined',
    description: 'gateway 執行個體；如果找不到，則為 undefined',
  },
]}
/>

## 相關內容 {/* #related */}

- [Mastra.getGateway()](/reference/core/getGateway): 依註冊鍵取得 gateway
- [Mastra.listGateways()](/reference/core/listGateways): 列出所有 gateway
- [Mastra.addGateway()](/reference/core/addGateway): 新增 gateway
- [MastraModelGateway](/reference/core/mastra-model-gateway): Gateway 基類
- [自訂 Gateway 指南](/models/gateways/custom-gateways): 建立自訂 gateway
