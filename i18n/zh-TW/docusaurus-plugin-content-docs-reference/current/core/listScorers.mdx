---
title: "參考：listScorers() | Core"
description: "關於 `listScorers()` 方法的說明文件，該方法傳回用於評估 AI 輸出的所有已註冊 Scorer。"
packages:
  - "@mastra/core"
---

# listScorers()

`listScorers()` 方法傳回 Mastra 執行個體中註冊的所有 Scorer。Scorer 用於評估 AI 輸出，並可在 Agent 產生或 Workflow 執行期間覆寫預設 Scorer。

## 使用範例

```typescript
import { mastra } from './mastra'

// Get all registered scorers
const allScorers = mastra.listScorers()

// Access a specific scorer
const myScorer = allScorers.relevancyScorer
```

## 參數

此方法不接受任何參數。

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'scorers',
    type: 'Record<string, MastraScorer> | undefined',
    description:
      '包含所有已註冊 Scorer 的物件，其中鍵為 Scorer 名稱，值為 MastraScorer 執行個體。如果未註冊 Scorer，則傳回 undefined。',
  },
]}
/>

## 相關內容

- [getScorer()](/reference/core/getScorer): 依鍵取得指定 Scorer
- [getScorerById()](/reference/core/getScorerById): 依 id 屬性取得 Scorer
- [Scorer 概覽](/docs/evals/overview): 瞭解如何建立和使用 Scorer
