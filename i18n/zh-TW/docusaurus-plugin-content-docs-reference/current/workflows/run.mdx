---
title: "參考：Run 類別 | Workflows"
description: Mastra 中 Run 類別的文件，此類別代表 Workflow 執行個體。
packages:
  - "@mastra/core"
---

# Run 類別

`Run` 類別代表 Workflow 執行個體，提供啟動、繼續、串流及監控 Workflow 執行的方法。

## 使用範例

```typescript
const run = await workflow.createRun()

const result = await run.start({
  inputData: { value: 'initial data' },
})

if (result.status === 'suspended') {
  const resumedResult = await run.resume({
    resumeData: { value: 'resume data' },
  })
}
```

## Run 方法

<PropertiesTable
  content={[
  {
    name: 'start',
    type: '(options?: StartOptions) => Promise<WorkflowResult>',
    description: '以輸入資料啟動 Workflow 執行',
    required: true,
  },
  {
    name: 'resume',
    type: '(options?: ResumeOptions) => Promise<WorkflowResult>',
    description: '從特定步驟繼續已暫停的 Workflow',
    required: true,
  },
  {
    name: 'stream',
    type: '(options?: StreamOptions) => MastraWorkflowStream',
    description: '以事件串流監控 Workflow 執行，並提供增強的串流功能',
    required: true,
  },
  {
    name: 'resumeStream',
    type: '(options?: ResumeStreamOptions) => MastraWorkflowStream',
    description: '以串流支援繼續已暫停的 Workflow',
    required: true,
  },
  {
    name: 'cancel',
    type: '() => Promise<{ message: string }>',
    description: '取消 Workflow 執行、停止所有執行中的步驟，並阻止後續步驟執行',
    required: true,
  },
  {
    name: 'restart',
    type: '(options?: RestartOptions) => Promise<WorkflowResult>',
    description: '從最後一個作用中步驟重新啟動 Workflow 執行',
    required: true,
  },
  {
    name: 'timeTravel',
    type: '(options?: TimeTravelOptions) => Promise<WorkflowResult>',
    description: '使用已儲存的快照資料或你提供的自訂情境，從任一特定步驟開始重新執行 Workflow。',
    required: true,
  },
  {
    name: 'timeTravelStream',
    type: '(options?: TimeTravelOptions) => MastraWorkflowStream',
    description: '以串流支援對 Workflow 執行進行 time travel',
    required: true,
  },
]}
/>

## Run 狀態

Workflow run 的 `status` 代表目前的執行狀態。可能的值如下：

<PropertiesTable
  content={[
  {
    name: 'success',
    type: 'string',
    description: '所有步驟都已成功執行完畢，並產生有效的結果輸出',
  },
  {
    name: 'failed',
    type: 'string',
    description: 'Workflow 執行期間發生錯誤，並可取得錯誤詳細資料',
  },
  {
    name: 'suspended',
    type: 'string',
    description: 'Workflow 執行已暫停並等待繼續，且包含已暫停步驟的資訊',
  },
  {
    name: 'canceled',
    type: 'string',
    description: 'Workflow 執行已透過 cancel() 方法取消，所有執行中的步驟均已停止，後續步驟也不會執行',
  },
]}
/>

## 相關內容

- [Run.start()](./run-methods/start)
- [Run.resume()](./run-methods/resume)
- [Run.cancel()](./run-methods/cancel)
- [Run.restart()](./run-methods/restart)
- [Run.timeTravel()](./run-methods/timeTravel)
- [Run.stream()](/docs/workflows/overview#inspecting-workflow-stream-payloads)
- [Run.timeTravelStream()](../streaming/workflows/timeTravelStream)
