---
title: "參考：Workflow.then() | Workflows"
description: Workflow 中 `Workflow.then()` 方法的文件，此方法會在步驟之間建立循序相依關係。
packages:
  - "@mastra/core"
---

# Workflow\.then()

`.then()` 方法會在 Workflow 步驟之間建立循序相依關係，確保步驟依指定順序執行。

## 使用範例

```typescript
workflow.then(step1).then(step2)
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'step',
    type: 'Step',
    description: '應在前一個步驟完成後執行的步驟執行個體',
    isOptional: false,
  },
]}
/>

## 回傳值

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'NewWorkflow',
    description: '可供方法鏈接使用的 Workflow 執行個體',
  },
]}
/>

## 相關內容

- [控制流程](/docs/workflows/control-flow)
