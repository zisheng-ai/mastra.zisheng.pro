---
title: "參考：Workflow.createRun() | Workflows"
description: Workflow 中 `Workflow.createRun()` 方法的文件，此方法會建立新的 Workflow run 執行個體。
packages:
  - "@mastra/core"
---

# Workflow\.createRun()

`.createRun()` 方法會建立新的 Workflow run 執行個體，讓你能以特定輸入資料執行 Workflow。這是目前會回傳 `Run` 執行個體的 API。

## 使用範例

```typescript
await workflow.createRun()
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'runId',
    type: 'string',
    description: 'Workflow run 的選用自訂識別碼',
    isOptional: true,
  },
  {
    name: 'resourceId',
    type: 'string',
    description:
      '選用識別碼，用來將 Workflow run 與特定資源（例如使用者 ID、租戶 ID）建立關聯。此值會隨 Workflow run 一併保存，並可用來篩選及查詢 run。',
    isOptional: true,
  },
  {
    name: 'disableScorers',
    type: 'boolean',
    description: '是否停用此 Workflow run 的 scorer，為選用旗標',
    isOptional: true,
  },
]}
/>

## 回傳值

<PropertiesTable
  content={[
  {
    name: 'run',
    type: 'Run',
    description: '可用來執行 Workflow 的新 Workflow run 執行個體',
  },
]}
/>

## 延伸使用範例

```typescript
const workflow = mastra.getWorkflow('workflow')

const run = await workflow.createRun()

const result = await run.start({
  inputData: {
    value: 10,
  },
})
```

## 使用 `resourceId`

`resourceId` 參數會將 Workflow run 與特定資源（例如使用者或租戶）建立關聯。這對多租戶應用程式，或需要追蹤由哪位使用者啟動 Workflow 的情境很有幫助。

```typescript
const workflow = mastra.getWorkflow('workflow')

// Create a run associated with a specific user
const run = await workflow.createRun({
  resourceId: 'user-123',
})

const result = await run.start({
  inputData: {
    value: 10,
  },
})

// Later, retrieve the run and access the resourceId
const storedRun = await workflow.getWorkflowRunById(run.runId)
console.log(storedRun.resourceId) // "user-123"
```

## 相關內容

- [Run 類別](../run)
- [Workflows 概觀](/docs/workflows/overview)
