---
title: "參考：toAISdkV5Messages() | AI SDK"
description: toAISdkV5Messages() 的 API 參考；此函式可將 Mastra 訊息轉換為 AI SDK v5 UI 訊息。
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdkV5Messages()

將輸入格式的訊息轉換為 AI SDK V5（及更新版本）的 UI 訊息格式。此函式接受多種格式的訊息（字串、AI SDK V4/V5 訊息、Mastra 資料庫訊息等），並將其正規化為 AI SDK V5+ `UIMessage` 格式，適合搭配 `useChat()` 等 AI SDK UI 元件使用。

## 使用範例

```typescript title="app/chat/page.tsx"
import { toAISdkV5Messages } from '@mastra/ai-sdk/ui'
import { useChat } from 'ai/react'

// Stored messages from your database, memory or API
const storedMessages = [
  { id: '1', role: 'user', content: 'Hello', parts: [{ type: 'text', text: 'Hello' }] },
  {
    id: '2',
    role: 'assistant',
    content: 'Hi there!',
    parts: [{ type: 'text', text: 'Hi there!' }],
  },
]

export default function Chat() {
  const { messages } = useChat({
    initialMessages: toAISdkV5Messages(storedMessages),
  })

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role}: {message.parts.map(part => (part.type === 'text' ? part.text : null))}
        </div>
      ))}
    </div>
  )
}
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'MessageListInput',
    description:
      '要轉換的訊息。可以是字串、字串陣列、單一訊息物件，或任何受支援格式的訊息物件陣列。',
    isOptional: false,
  },
]}
/>

## 傳回值

傳回一個 AI SDK V5+ `UIMessage` 物件陣列，其結構如下：

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '唯一的訊息識別碼。',
  },
  {
    name: 'role',
    type: "'user' | 'assistant' | 'system'",
    description: '訊息傳送者的角色。',
  },
  {
    name: 'parts',
    type: 'UIMessagePart[]',
    description: 'UI 部分的陣列，包含文字、Tool 結果、檔案、推理、來源及步驟標記。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: '選用的中繼資料，包含 createdAt、threadId、resourceId 及自訂欄位。',
    isOptional: true,
  },
]}
/>

## 範例

### 轉換文字訊息

```typescript
import { toAISdkV5Messages } from '@mastra/ai-sdk/ui'

const messages = toAISdkV5Messages(['Hello', 'How can I help you today?'])
// Returns array of UIMessage objects with user role
```

### 使用 Mastra 使用者端載入訊息

```typescript
import { MastraClient } from '@mastra/client-js'
import { toAISdkV5Messages } from '@mastra/ai-sdk/ui'

const client = new MastraClient()

const { messages } = await client.listThreadMessages('thread-id', { agentId: 'myAgent' })
const uiMessages = toAISdkV5Messages(messages)
```
