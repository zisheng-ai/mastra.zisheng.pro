---
title: "參考：toAISdkV4Messages() | AI SDK"
description: toAISdkV4Messages() 的 API 參考；此函式可將 Mastra 訊息轉換為 AI SDK v4 UI 訊息。
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdkV4Messages()

將輸入格式的訊息轉換為 AI SDK V4 UI 訊息格式。此函式接受多種格式的訊息（字串、AI SDK V4/V5 訊息、Mastra 資料庫訊息等），並將其正規化為 AI SDK V4 `UIMessage` 格式，適合搭配 `useChat()` 等 AI SDK UI 元件使用。

## 使用範例 {/* #usage-example */}

```typescript title="app/chat/page.tsx"
import { toAISdkV4Messages } from '@mastra/ai-sdk'
import { useChat } from 'ai/react' // AI SDK V4

// Stored messages from your database, memory or API
const storedMessages = [
  { id: '1', role: 'user', parts: [{ type: 'text', text: 'Hello' }] },
  { id: '2', role: 'assistant', parts: [{ type: 'text', text: 'Hi there!' }] },
]

export default function Chat() {
  const { messages } = useChat({
    initialMessages: toAISdkV4Messages(storedMessages),
  })

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role}: {message.content}
        </div>
      ))}
    </div>
  )
}
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'MessageListInput',
    description:
      '要轉換的訊息。可以是字串、字串陣列、單一訊息物件，或任何受支援格式的訊息物件陣列。',
    isOptional: false,
  },
]}
/>

## 傳回值 {/* #returns */}

傳回一個 AI SDK V4 `UIMessage` 物件陣列，其結構如下：

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '唯一的訊息識別碼。',
  },
  {
    name: 'role',
    type: "'user' | 'assistant' | 'system'",
    description: '訊息傳送者的角色。',
  },
  {
    name: 'content',
    type: 'string',
    description: '訊息的文字內容。',
  },
  {
    name: 'parts',
    type: 'UIMessagePart[]',
    description: 'UI 部分的陣列，包含文字、Tool 呼叫、檔案、推理、來源及步驟標記。',
  },
  {
    name: 'createdAt',
    type: 'Date',
    description: '訊息的建立時間戳記。',
  },
  {
    name: 'toolInvocations',
    type: 'ToolInvocation[]',
    description: 'assistant 訊息中的 Tool 呼叫陣列。',
    isOptional: true,
  },
  {
    name: 'experimental_attachments',
    type: 'Attachment[]',
    description: '訊息上的檔案附件。',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: '附加至訊息的自訂中繼資料。',
    isOptional: true,
  },
]}
/>

## 範例 {/* #examples */}

### 轉換文字訊息 {/* #converting-text-messages */}

```typescript
import { toAISdkV4Messages } from '@mastra/ai-sdk'

const messages = toAISdkV4Messages(['Hello', 'How can I help you today?'])
// Returns array of UIMessage objects with user role and content string
```

### 使用 Mastra 使用者端載入訊息 {/* #loading-messages-with-mastra-client */}

```typescript
import { MastraClient } from '@mastra/client-js'
import { toAISdkV4Messages } from '@mastra/ai-sdk'

const client = new MastraClient()

const { messages } = await client.listThreadMessages('thread-id', { agentId: 'myAgent' })
const uiMessages = toAISdkV4Messages(messages)
```
