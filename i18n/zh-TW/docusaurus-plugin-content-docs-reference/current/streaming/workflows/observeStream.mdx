---
title: "參考：Run.observeStream() | 串流"
description: Workflow 中 `Run.observeStream()` 方法的文件；此方法可重新開啟已在執行中的 Workflow 執行串流。
packages:
  - "@mastra/core"
---

# Run.observeStream()

`.observeStream()` 方法會為目前正在執行的 Workflow 開啟新的 `ReadableStream`。若原始串流已無法使用，你仍可透過這個新串流觀察事件。

## 使用範例

```typescript
const run = await workflow.createRun()

run.stream({
  inputData: {
    value: 'initial data',
  },
})

const stream = await run.observeStream()

for await (const chunk of stream) {
  console.log(chunk)
}
```

## 回傳值

`ReadableStream<ChunkType>`

## 串流事件

串流會在 Workflow 執行期間輸出各種事件類型。每個事件都有 `type` 欄位，以及包含相關資料的 `payload`：

- **`workflow-start`**：Workflow 開始執行
- **`workflow-step-start`**：步驟開始執行
- **`workflow-step-output`**：步驟的自訂輸出
- **`workflow-step-result`**：步驟執行完成並產生結果
- **`workflow-finish`**：Workflow 執行完成，並提供用量統計資料。若執行成功，`payload.finalWorkflowResult` 會包含 Workflow 的最終結果

## 相關內容

- [Workflow 概觀](/docs/workflows/overview#running-workflows)
- [Workflow.createRun()](/reference/workflows/workflow-methods/create-run)
- [Run.stream()](./stream)
- [Run.resumeStream()](./resumeStream)
