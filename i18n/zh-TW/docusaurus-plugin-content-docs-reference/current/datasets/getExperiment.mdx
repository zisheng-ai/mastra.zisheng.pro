---
title: "參考：dataset.getExperiment() | 資料集"
description: "`dataset.getExperiment()` 方法的說明文件，該方法依 ID 取得特定實驗。"
packages:
  - "@mastra/core"
---

# dataset.getExperiment()

**新增於：** `@mastra/core@1.4.0`

依 ID 取得特定實驗（執行）。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const experiment = await dataset.getExperiment({ experimentId: 'exp-id' })

console.log(`Status: ${experiment.status}`)
console.log(`${experiment.succeededCount}/${experiment.totalItems} succeeded`)
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: '要取得的實驗 ID。',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Experiment>',
    description: '實驗記錄。完整結構請參閱 dataset.listExperiments()。',
  },
]}
/>
