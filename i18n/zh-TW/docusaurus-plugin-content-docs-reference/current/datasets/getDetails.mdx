---
title: "參考：dataset.getDetails() | 資料集"
description: "`dataset.getDetails()` 方法的說明文件，該方法從儲存中取得完整的資料集記錄。"
packages:
  - "@mastra/core"
---

# dataset.getDetails()

**新增於：** `@mastra/core@1.4.0`

從儲存中取得完整的資料集記錄，包括中繼資料、schema 和版本資訊。

## 使用範例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Get dataset details
const details = await dataset.getDetails()
console.log(details.name, details.version)
```

## 參數

此方法不接受任何參數。

## 傳回值

傳回 `Promise<DatasetRecord>`。如果找不到資料集，則擲回 `MastraError`。

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '資料集的唯一識別碼。',
  },
  {
    name: 'name',
    type: 'string',
    description: '資料集的顯示名稱。',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: '資料集的描述。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '與資料集關聯的任意中繼資料。',
  },
  {
    name: 'inputSchema',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '項目輸入的 JSON Schema。',
  },
  {
    name: 'groundTruthSchema',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '項目 ground truth 的 JSON Schema。',
  },
  {
    name: 'version',
    type: 'number',
    description: '資料集的目前版本號。',
  },
  {
    name: 'createdAt',
    type: 'Date',
    description: '資料集的建立時間。',
  },
  {
    name: 'updatedAt',
    type: 'Date',
    description: '資料集的最後更新時間。',
  },
]}
/>
