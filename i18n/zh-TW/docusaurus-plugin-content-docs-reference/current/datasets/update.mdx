---
title: "參考：dataset.update() | 資料集"
description: "`dataset.update()` 方法的說明文件，該方法更新資料集中繼資料和 schema。"
packages:
  - "@mastra/core"
---

# dataset.update()

**新增於：** `@mastra/core@1.4.0`

更新資料集的中繼資料、名稱、描述和/或 schema。使用 Standard JSON Schema（Zod、Valibot、ArkType 等）定義 schema。

## 使用範例

```typescript
import { Mastra } from '@mastra/core'
import { z } from 'zod'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Update with plain metadata
const updated = await dataset.update({
  name: 'Updated QA pairs',
  description: 'Revised evaluation set',
})

// Update with Zod schema (library that supports Standard JSON Schema)
const updated2 = await dataset.update({
  inputSchema: z.object({
    question: z.string(),
    context: z.string().optional(),
  }),
})
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: '新的顯示名稱。',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: '新的描述。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '更新後的中繼資料。',
  },
  {
    name: 'inputSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: '項目輸入的 Standard JSON Schema。',
  },
  {
    name: 'groundTruthSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: '項目 ground truth 的 Standard JSON Schema。',
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetRecord>',
    description: '更新後的資料集記錄。完整結構請參閱 dataset.getDetails()。',
  },
]}
/>
