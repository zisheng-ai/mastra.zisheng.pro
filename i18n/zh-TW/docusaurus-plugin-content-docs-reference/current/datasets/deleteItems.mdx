---
title: "參考：dataset.deleteItems() | 資料集"
description: "`dataset.deleteItems()` 方法的說明文件，該方法從資料集中批次刪除多個項目。"
packages:
  - "@mastra/core"
---

# dataset.deleteItems()

**新增於：** `@mastra/core@1.4.0`

透過一次批次操作從資料集中刪除多個項目。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItems({
  itemIds: ['item-1', 'item-2', 'item-3'],
})
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemIds',
    type: 'string[]',
    description: '要刪除的項目 ID 陣列。',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: '所有項目刪除後，Promise 完成。',
  },
]}
/>
