---
title: "參考：dataset.listItems() | 資料集"
description: "`dataset.listItems()` 方法的說明文件，該方法列出資料集中的項目，並支援選用的分頁和篩選。"
packages:
  - "@mastra/core"
---

# dataset.listItems()

**新增於：** `@mastra/core@1.4.0`

列出資料集中的項目。僅提供 `version` 時，傳回該版本中所有項目的 `DatasetItem[]` 純陣列快照。其他所有情況（不傳參數，或提供 `search` / `page` / `perPage`，無論是否同時提供 `version`）均傳回分頁的 `{ items, pagination }` 結構。

## 使用範例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Paginated list (default: page 0, 20 per page)
const result = await dataset.listItems()

// List with search
const filtered = await dataset.listItems({ search: 'TypeScript', page: 0, perPage: 10 })

// Paginated list scoped to a specific version
const versionedPage = await dataset.listItems({ version: 2, page: 0, perPage: 20 })

// Version-only snapshot returns a bare DatasetItem[] (deprecated form —
// prefer passing page/perPage to receive the paginated shape).
const versionedItems = await dataset.listItems({ version: 2 })
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description:
      '要列出項目時對應的資料集版本。單獨傳入時，傳回該版本所有項目的 `DatasetItem[]` 純陣列快照。將 `search` / `page` / `perPage` 與 `version` 一起傳入時，則改為傳回分頁結構。',
  },
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: '分頁頁碼。預設為 `0`。',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: '每頁的項目數。預設為 `20`。',
  },
  {
    name: 'search',
    type: 'string',
    isOptional: true,
    description: '用於篩選項目的搜尋字串。',
  },
]}
/>

## 傳回值

僅提供 `version`（不提供 `search`、`page` 或 `perPage`）時：

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description:
      '指定資料集版本中所有項目的裸陣列。保留此結構是為了回溯相容，但它已棄用——請傳入 `page` / `perPage`（或 `search`），以始終接收下方的分頁結構。',
  },
]}
/>

<br />

其他所有情況（不傳參數，或提供 `search` / `page` / `perPage`，無論是否同時提供 `version`）：

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: '分頁回應。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'items',
            type: 'DatasetItem[]',
            description: '目前頁面的項目陣列。',
          },
          {
            name: 'pagination',
            type: 'object',
            description: '分頁中繼資料。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'total',
                    type: 'number',
                    description: '項目總數。',
                  },
                  {
                    name: 'page',
                    type: 'number',
                    description: '目前頁碼。',
                  },
                  {
                    name: 'perPage',
                    type: 'number | false',
                    description: '每頁項目數；如果未分頁則為 `false`。',
                  },
                  {
                    name: 'hasMore',
                    type: 'boolean',
                    description: '是否還有更多頁面。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>
