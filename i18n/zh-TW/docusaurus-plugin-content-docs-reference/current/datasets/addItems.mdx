---
title: "參考：dataset.addItems() | 資料集"
description: "`dataset.addItems()` 方法的說明文件，該方法向資料集批次新增多個項目。"
packages:
  - "@mastra/core"
---

# dataset.addItems()

**新增於：** `@mastra/core@1.4.0`

透過一次批次操作向資料集新增多個項目。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const items = await dataset.addItems({
  items: [
    { input: { question: 'What is AI?' }, groundTruth: { answer: 'Artificial Intelligence' } },
    { input: { question: 'What is ML?' }, groundTruth: { answer: 'Machine Learning' } },
    { input: { question: 'What is DL?' }, metadata: { category: 'deep-learning' } },
  ],
})

console.log(`Added ${items.length} items`)
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'items',
    type: 'Array<object>',
    description: '要新增的項目陣列。每個項目的結構都與 dataset.addItem() 的輸入相同。',
    properties: [
      {
        type: 'DatasetItem',
        parameters: [
          {
            name: 'input',
            type: 'unknown',
            description: '項目的輸入資料。',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: '預期輸出或 ground truth。',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: '任意中繼資料。',
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description: '已建立的資料集項目陣列。項目結構請參閱 dataset.addItem()。',
  },
]}
/>
