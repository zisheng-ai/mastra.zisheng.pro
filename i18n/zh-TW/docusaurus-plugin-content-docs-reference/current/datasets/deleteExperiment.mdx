---
title: "參考：dataset.deleteExperiment() | 資料集"
description: "`dataset.deleteExperiment()` 方法的說明文件，該方法依 ID 刪除實驗。"
packages:
  - "@mastra/core"
---

# dataset.deleteExperiment()

**新增於：** `@mastra/core@1.4.0`

依 ID 刪除實驗（執行），包括所有關聯結果。

## 使用範例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteExperiment({ experimentId: 'exp-id' })
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: '要刪除的實驗 ID。',
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: '實驗及其結果刪除後，Promise 完成。',
  },
]}
/>
