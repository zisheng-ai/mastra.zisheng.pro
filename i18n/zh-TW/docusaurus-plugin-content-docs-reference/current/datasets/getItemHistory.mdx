---
title: "參考：dataset.getItemHistory() | 資料集"
description: "`dataset.getItemHistory()` 方法的說明文件，該方法取得資料集項目的完整 SCD-2 變更歷史記錄。"
packages:
  - "@mastra/core"
---

# dataset.getItemHistory()

**新增於：** `@mastra/core@1.4.0`

取得特定項目在所有資料集版本中的完整 SCD-2（緩慢變更維度類型 2）歷史記錄。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const history = await dataset.getItemHistory({ itemId: 'item-id' })

for (const row of history) {
  console.log(`Version ${row.datasetVersion}: ${row.isDeleted ? 'deleted' : 'active'}`)
}
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: '要取得歷史記錄的項目 ID。',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItemRow[]>',
    description: '依版本排序的項目版本記錄陣列。',
    properties: [
      {
        type: 'DatasetItemRow',
        parameters: [
          {
            name: 'id',
            type: 'string',
            description: '記錄的唯一識別碼。',
          },
          {
            name: 'datasetId',
            type: 'string',
            description: '父資料集的 ID。',
          },
          {
            name: 'datasetVersion',
            type: 'number',
            description: '此記錄對應的資料集版本。',
          },
          {
            name: 'validTo',
            type: 'number | null',
            description: '此記錄被取代時的版本；如果仍為目前記錄，則為 `null`。',
          },
          {
            name: 'isDeleted',
            type: 'boolean',
            description: '此記錄是否表示刪除操作。',
          },
          {
            name: 'input',
            type: 'unknown',
            description: '此版本的輸入資料。',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: '此版本的 ground truth。',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: '此版本的中繼資料。',
          },
          {
            name: 'createdAt',
            type: 'Date',
            description: '此記錄的建立時間。',
          },
          {
            name: 'updatedAt',
            type: 'Date',
            description: '此記錄的最後更新時間。',
          },
        ],
      },
    ],
  },
]}
/>
