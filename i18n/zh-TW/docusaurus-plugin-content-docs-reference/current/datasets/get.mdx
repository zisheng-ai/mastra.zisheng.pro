---
title: "參考：DatasetsManager.get() | 資料集"
description: "`DatasetsManager.get()` 方法的說明文件，該方法依 ID 取得現有資料集。"
packages:
  - "@mastra/core"
---

# DatasetsManager.get()

**新增於：** `@mastra/core@1.4.0`

依 ID 取得現有資料集。如果資料集不存在，則擲回 `MastraError`。

## 使用範例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Now use the dataset
const details = await dataset.getDetails()
console.log(details.name)
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '資料集的唯一識別碼。',
  },
]}
/>

## 傳回值

如果找不到資料集，則擲回 `MastraError`。

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Dataset>',
    description: 'Dataset 實例。',
  },
]}
/>
