---
title: "參考：BraintrustExporter | 可觀測性"
description: 用於 Tracing 的 Braintrust exporter
packages:
  - "@mastra/braintrust"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# BraintrustExporter

將 Tracing 資料傳送至 Braintrust，以進行 eval 與可觀測性分析。

## 建構函式 {/* #constructor */}

```typescript prettier:false
new BraintrustExporter(config: BraintrustExporterConfig)
```

## `BraintrustExporterConfig` {/* #braintrustexporterconfig */}

```typescript
interface BraintrustExporterConfig extends BaseExporterConfig {
  apiKey?: string
  endpoint?: string
  projectName?: string
  braintrustLogger?: Logger<true>
  currentSpan?: () => Span | undefined
  tuningParameters?: Record<string, any>
}
```

擴充 `BaseExporterConfig`，其中包括：

- `logger?: IMastraLogger` - Logger 執行個體
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - Log 層級（預設：INFO）

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Braintrust API key。若未設定，則使用 BRAINTRUST_API_KEY 環境變數。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: '自訂 Braintrust endpoint。若未設定，則使用 BRAINTRUST_ENDPOINT 環境變數。',
    required: false,
  },
  {
    name: 'projectName',
    type: 'string',
    description: "專案名稱（預設：'mastra-tracing'）",
    required: false,
  },
  {
    name: 'braintrustLogger',
    type: 'Logger<true>',
    description: '用於 eval 與 traced span context 整合的 Braintrust logger 執行個體。',
    required: false,
  },
  {
    name: 'currentSpan',
    type: '() => Span | undefined',
    description:
      '來自應用程式 Braintrust package 執行個體的 resolver。當 Eval() 或 logger.traced() span 是由另一份已安裝的 Braintrust SDK 建立時，請與 braintrustLogger 搭配使用。',
    required: false,
  },
  {
    name: 'logLevel',
    type: "'debug' | 'info' | 'warn' | 'error'",
    description: "Logger 層級（預設：'warn'）",
    required: false,
  },
  {
    name: 'tuningParameters',
    type: 'Record<string, any>',
    description: 'Braintrust 的調校參數',
    required: false,
  },
]}
/>

## 方法 {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

將 tracing 事件匯出至 Braintrust。

### export {/* #export */}

```typescript prettier:false
async export(spans: ReadOnlySpan[]): Promise<void>
```

將 span 批次匯出至 Braintrust。

### flush {/* #flush */}

```typescript prettier:false
async flush(): Promise<void>
```

強制將所有緩衝的 span flush 至 Braintrust，而不關閉 exporter。這在 serverless 環境中特別實用，可確保 runtime 結束前已匯出 span。

### shutdown {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

Flush 待處理資料並關閉 client。

## 使用方式 {/* #usage */}

### 零設定（使用環境變數） {/* #zero-config-using-environment-variables */}

```typescript
import { BraintrustExporter } from '@mastra/braintrust'

// Reads from BRAINTRUST_API_KEY, BRAINTRUST_ENDPOINT
const exporter = new BraintrustExporter()
```

### 明確設定 {/* #explicit-configuration */}

```typescript
import { BraintrustExporter } from '@mastra/braintrust'

const exporter = new BraintrustExporter({
  apiKey: process.env.BRAINTRUST_API_KEY,
  projectName: 'my-ai-project',
})
```

## Span 類型對應 {/* #span-type-mapping */}

| Span 類型 | Braintrust 類型 |
| - | - |
| `MODEL_GENERATION` | `llm` |
| `MODEL_CHUNK` | `llm` |
| `TOOL_CALL` | `tool` |
| `MCP_TOOL_CALL` | `tool` |
| `PROVIDER_TOOL_CALL` | `tool` |
| `WORKFLOW_CONDITIONAL_EVAL` | `function` |
| `WORKFLOW_WAIT_EVENT` | `function` |
| 所有其他類型 | `task` |
