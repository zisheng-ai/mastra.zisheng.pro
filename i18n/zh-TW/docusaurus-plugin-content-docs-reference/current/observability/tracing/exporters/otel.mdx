---
title: "參考：OtelExporter | 可觀測性"
description: 用於 trace 與 log 的 OpenTelemetry exporter
packages:
  - "@mastra/otel-exporter"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# OtelExporter

將 trace 與 log 傳送至任何與 OpenTelemetry 相容的可觀測性平台。Trace 使用標準化 GenAI semantic convention；log 保留原始 severity 與 message body，並透過 OTEL log record 的原生 trace context，以及 `mastra.traceId`／`mastra.spanId` 屬性與 trace 建立關聯。

## 建構函式 {/* #constructor */}

```typescript prettier:false
new OtelExporter(config: OtelExporterConfig)
```

## `OtelExporterConfig` {/* #otelexporterconfig */}

```typescript
interface OtelExporterConfig {
  provider?: ProviderConfig
  signals?: {
    traces?: boolean
    logs?: boolean
  }
  timeout?: number
  batchSize?: number
  logLevel?: 'debug' | 'info' | 'warn' | 'error'
}
```

<PropertiesTable
  props={[
  {
    name: 'provider',
    type: 'ProviderConfig',
    description: 'Provider 專屬設定（請見下方）',
    required: true,
  },
  {
    name: 'signals',
    type: '{ traces?: boolean; logs?: boolean }',
    description:
      '個別 signal 開關。兩者預設皆為 true。將任一值設為 false 即可停用該 signal。匯出 log 也需要安裝所選 protocol 對應的 `@opentelemetry/exporter-logs-otlp-*` package。',
    required: false,
  },
  {
    name: 'timeout',
    type: 'number',
    description: '匯出逾時（毫秒，預設：30000）',
    required: false,
  },
  {
    name: 'batchSize',
    type: 'number',
    description: '每批 span／log 的數量（預設：512）',
    required: false,
  },
  {
    name: 'logLevel',
    type: "'debug' | 'info' | 'warn' | 'error'",
    description: "Logger 層級（預設：'warn'）",
    required: false,
  },
  {
    name: 'resourceAttributes',
    type: 'DetectedResourceAttributes',
    description: '選用的 OpenTelemetry Resource Attribute（此處的值會覆寫任何預設值）',
    required: false,
  },
]}
/>

## Provider 設定 {/* #provider-configurations */}

### `Dash0Config` {/* #dash0config */}

```typescript
interface Dash0Config {
  apiKey?: string
  endpoint?: string
  dataset?: string
}
```

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Dash0 API key。若未設定，則使用 DASH0_API_KEY 環境變數。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: 'Dash0 ingress endpoint。若未設定，則使用 DASH0_ENDPOINT 環境變數。',
    required: false,
  },
  {
    name: 'dataset',
    type: 'string',
    description: 'Dataset 名稱。若未設定，則使用 DASH0_DATASET 環境變數。',
    required: false,
  },
]}
/>

### `SignozConfig` {/* #signozconfig */}

```typescript
interface SignozConfig {
  apiKey?: string
  region?: 'us' | 'eu' | 'in'
  endpoint?: string
}
```

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'SigNoz ingestion key。若未設定，則使用 SIGNOZ_API_KEY 環境變數。',
    required: false,
  },
  {
    name: 'region',
    type: "'us' | 'eu' | 'in'",
    description: "SigNoz cloud region。若未設定，則使用 SIGNOZ_REGION 環境變數。（預設：'us'）",
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: '自訂 endpoint。若未設定，則使用 SIGNOZ_ENDPOINT 環境變數。',
    required: false,
  },
]}
/>

### `NewRelicConfig` {/* #newrelicconfig */}

```typescript
interface NewRelicConfig {
  apiKey?: string
  endpoint?: string
}
```

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'New Relic license key。若未設定，則使用 NEW_RELIC_LICENSE_KEY 環境變數。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description:
      '自訂 endpoint。若未設定，則使用 NEW_RELIC_ENDPOINT 環境變數。（預設：https://otlp.nr-data.net:443/v1/traces）',
    required: false,
  },
]}
/>

### `TraceloopConfig` {/* #traceloopconfig */}

```typescript
interface TraceloopConfig {
  apiKey?: string
  destinationId?: string
  endpoint?: string
}
```

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Traceloop API key。若未設定，則使用 TRACELOOP_API_KEY 環境變數。',
    required: false,
  },
  {
    name: 'destinationId',
    type: 'string',
    description: '目的地識別碼。若未設定，則使用 TRACELOOP_DESTINATION_ID 環境變數。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description:
      '自訂 endpoint。若未設定，則使用 TRACELOOP_ENDPOINT 環境變數。（預設：https://api.traceloop.com/v1/traces）',
    required: false,
  },
]}
/>

### `LaminarConfig` {/* #laminarconfig */}

```typescript
interface LaminarConfig {
  apiKey?: string
  endpoint?: string
}
```

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Laminar 專案 API key。若未設定，則使用 LMNR_PROJECT_API_KEY 環境變數。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: '自訂 endpoint。若未設定，則使用 LAMINAR_ENDPOINT 環境變數。（預設：https://api.lmnr.ai/v1/traces）',
    required: false,
  },
]}
/>

### `CustomConfig` {/* #customconfig */}

```typescript
interface CustomConfig {
  endpoint: string
  protocol?: 'http/json' | 'http/protobuf' | 'grpc' | 'zipkin'
  headers?: Record<string, string>
}
```

<PropertiesTable
  props={[
  {
    name: 'endpoint',
    type: 'string',
    description: 'OTEL collector endpoint URL',
    required: true,
  },
  {
    name: 'protocol',
    type: "'http/json' | 'http/protobuf' | 'grpc' | 'zipkin'",
    description: "匯出 protocol（預設：'http/json'）",
    required: false,
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: '用於驗證的自訂 header',
    required: false,
  },
]}
/>

## 方法 {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

將 tracing 事件匯出至設定的 OTEL 後端。

### flush {/* #flush */}

```typescript prettier:false
async flush(): Promise<void>
```

強制將所有緩衝的 span flush 至 OTEL 後端，而不關閉 exporter。這在 serverless 環境中特別實用，可確保 runtime 結束前已匯出 span。

### shutdown {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

Flush 待處理 trace 並關閉 exporter。

## 使用範例 {/* #usage-examples */}

### 零設定（使用環境變數） {/* #zero-config-using-environment-variables */}

```typescript
import { OtelExporter } from '@mastra/otel-exporter'

// Set SIGNOZ_API_KEY, SIGNOZ_REGION environment variables
const exporter = new OtelExporter({ provider: { signoz: {} } })

// Or for other providers:
// Set DASH0_API_KEY, DASH0_ENDPOINT for Dash0
// Set NEW_RELIC_LICENSE_KEY for New Relic
// Set TRACELOOP_API_KEY for Traceloop
// Set LMNR_PROJECT_API_KEY for Laminar
```

### 明確設定 {/* #explicit-configuration */}

```typescript
import { OtelExporter } from '@mastra/otel-exporter'

const exporter = new OtelExporter({
  provider: {
    signoz: {
      apiKey: process.env.SIGNOZ_API_KEY,
      region: 'us',
    },
  },
})
```

### 使用自訂 Endpoint {/* #with-custom-endpoint */}

```typescript
const exporter = new OtelExporter({
  provider: {
    custom: {
      endpoint: 'https://my-collector.example.com/v1/traces',
      protocol: 'http/protobuf',
      headers: {
        'x-api-key': process.env.API_KEY,
      },
    },
  },
  timeout: 60000,
  logLevel: 'debug',
})
```

## Protocol 需求 {/* #protocol-requirements */}

不同 Provider 需要不同的 OTEL exporter package。Trace 與 log 匯出彼此獨立：若只需要 trace，只需安裝 trace package；若也需要匯出 log，請同時安裝所選 protocol 的 trace 與 log package。Zipkin 不支援 OTLP log。

| Protocol | Trace package | Log package |
| - | - | - |
| gRPC | `@opentelemetry/exporter-trace-otlp-grpc` (+ `@grpc/grpc-js`) | `@opentelemetry/exporter-logs-otlp-grpc` (+ `@grpc/grpc-js`) |
| HTTP/Protobuf | `@opentelemetry/exporter-trace-otlp-proto` | `@opentelemetry/exporter-logs-otlp-proto` |
| HTTP/JSON | `@opentelemetry/exporter-trace-otlp-http` | `@opentelemetry/exporter-logs-otlp-http` |
| Zipkin | `@opentelemetry/exporter-zipkin` | 不支援 |

## 標籤支援 {/* #tags-support */}

OtelExporter 支援以 trace 標籤進行分類與篩選。標籤只會套用至根 span，並儲存為 `mastra.tags` 屬性。

### 使用方式 {/* #usage */}

```typescript
const result = await agent.generate('Hello', {
  tracingOptions: {
    tags: ['production', 'experiment-v2', 'user-request'],
  },
})
```

### 標籤的儲存方式 {/* #how-tags-are-stored */}

為了達到最高的後端相容性，標籤會以 JSON 字串化 array 的形式儲存於 `mastra.tags` span 屬性中：

```json
{
  "mastra.tags": "[\"production\",\"experiment-v2\",\"user-request\"]"
}
```

:::note
雖然 OpenTelemetry 規格支援原生 array 屬性，但許多後端（Jaeger、Zipkin、Tempo）對 array 的支援有限。JSON 字串可確保在所有可觀測性平台上有一致的行為。
:::

## 相關內容 {/* #related */}

- [OtelExporter 指南](/docs/observability/integrations/exporters/otel)：包含 Provider 設定的建置指南
- [OtelBridge](/docs/observability/integrations/bridges/otel)：用於雙向 OTEL context 整合
- [Tracing 概覽](/docs/observability/tracing/overview)：一般 tracing 概念
