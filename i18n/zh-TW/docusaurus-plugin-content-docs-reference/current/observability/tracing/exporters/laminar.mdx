---
title: "參考：LaminarExporter | 可觀測性"
description: 用於 Tracing 的 Laminar exporter
packages:
  - "@mastra/laminar"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# LaminarExporter

透過 OTLP/HTTP（protobuf）將 Tracing 資料傳送至 Laminar。

## 建構函式

```typescript prettier:false
new LaminarExporter(config?: LaminarExporterConfig)
```

## `LaminarExporterConfig`

```typescript
interface LaminarExporterConfig extends BaseExporterConfig {
  apiKey?: string
  baseUrl?: string
  endpoint?: string
  headers?: Record<string, string>
  realtime?: boolean
  disableBatch?: boolean
  batchSize?: number
  timeoutMillis?: number
}
```

擴充 `BaseExporterConfig`，其中包括：

- `logger?: IMastraLogger` - Logger 執行個體
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - Log 層級（預設：INFO）

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Laminar 專案 API key。若未設定，則使用 LMNR_PROJECT_API_KEY 環境變數。',
    required: false,
  },
  {
    name: 'baseUrl',
    type: 'string',
    description:
      'Laminar base URL（用於匯出 trace 與評分）。若未設定，則使用 LMNR_BASE_URL 環境變數（預設：https://api.lmnr.ai）。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description:
      'OTLP/HTTP trace endpoint（以 /v1/traces 結尾）。若未設定，則使用 LAMINAR_ENDPOINT 環境變數（預設：`${baseUrl}/v1/traces`）。',
    required: false,
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: '要納入 OTLP 請求的其他 header（會與 Authorization 合併）。',
    required: false,
  },
  {
    name: 'realtime',
    type: 'boolean',
    description: '每個 span 後立即 flush，以便即時查看（預設：false）。',
    required: false,
  },
  {
    name: 'disableBatch',
    type: 'boolean',
    description: '停用批次處理（SimpleSpanProcessor）。預設：false。',
    required: false,
  },
  {
    name: 'batchSize',
    type: 'number',
    description: '每批最多可包含的 span 數量（BatchSpanProcessor）。預設：512。',
    required: false,
  },
  {
    name: 'timeoutMillis',
    type: 'number',
    description: 'OTLP 匯出逾時（毫秒）。預設：30000。',
    required: false,
  },
]}
/>

## 方法

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

將 tracing 事件匯出至 Laminar。

### flush

```typescript prettier:false
async flush(): Promise<void>
```

強制將所有緩衝的 span flush 至 Laminar，而不關閉 exporter。這在 serverless 環境中特別實用，可確保 runtime 結束前已匯出 span。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

Flush 待處理資料並關閉 exporter。

## 使用方式

### 零設定（使用環境變數）

```typescript
import { LaminarExporter } from '@mastra/laminar'

// Reads from LMNR_PROJECT_API_KEY, LMNR_BASE_URL, LAMINAR_ENDPOINT
const exporter = new LaminarExporter()
```

### 明確設定

```typescript
import { LaminarExporter } from '@mastra/laminar'

const exporter = new LaminarExporter({
  apiKey: process.env.LMNR_PROJECT_API_KEY,
  baseUrl: 'https://api.lmnr.ai',
  realtime: true,
})
```
