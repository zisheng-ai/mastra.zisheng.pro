---
title: "參考：設定 | 可觀測性"
description: Tracing 設定型別與 registry 函式
packages:
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# 設定

## `ObservabilityRegistryConfig` {/* #observabilityregistryconfig */}

```typescript
interface ObservabilityRegistryConfig {
  default?: { enabled?: boolean }
  configs?: Record<string, Omit<ObservabilityInstanceConfig, 'name'> | ObservabilityInstance>
  configSelector?: ConfigSelector
  sensitiveDataFilter?: boolean | SensitiveDataFilterOptions
}
```

<PropertiesTable
  content={[
  {
    name: 'default',
    type: '{ enabled?: boolean }',
    description: '啟用包含 MastraStorageExporter 與 MastraPlatformExporter 的預設設定',
    required: false,
  },
  {
    name: 'configs',
    type: "Record<string, Omit<ObservabilityInstanceConfig, 'name'> | ObservabilityInstance>",
    description: '具名可觀測性執行個體設定或預先執行個體化的執行個體',
    required: false,
  },
  {
    name: 'configSelector',
    type: 'ConfigSelector',
    description: 'Runtime 設定 selector 函式',
    required: false,
  },
  {
    name: 'sensitiveDataFilter',
    type: 'boolean | SensitiveDataFilterOptions',
    description:
      '控制是否將 [SensitiveDataFilter](/reference/observability/tracing/processors/sensitive-data-filter) 自動套用至每個已設定的執行個體。預設為 `true`。傳入 `false` 可選擇停用，或傳入 `SensitiveDataFilterOptions` 物件以自訂欄位、遮蔽 token 或遮蔽樣式。如果某設定的 `spanOutputProcessors` 已包含 `SensitiveDataFilter`，則不會重複新增。預先執行個體化的執行個體不會修改。',
    required: false,
  },
]}
/>

## `ObservabilityInstanceConfig` {/* #observabilityinstanceconfig */}

```typescript
interface ObservabilityInstanceConfig {
  name: string
  serviceName: string
  sampling?: SamplingStrategy
  exporters?: ObservabilityExporter[]
  spanOutputProcessors?: SpanOutputProcessor[]
  includeInternalSpans?: boolean
  excludeSpanTypes?: SpanType[]
  spanFilter?: (span: AnyExportedSpan) => boolean
  requestContextKeys?: string[]
  serializationOptions?: SerializationOptions
  logging?: { enabled?: boolean; level?: LogLevel }
}
```

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: '設定識別碼',
    required: true,
  },
  {
    name: 'serviceName',
    type: 'string',
    description: 'Trace 中的服務名稱',
    required: true,
  },
  {
    name: 'sampling',
    type: 'SamplingStrategy',
    description: '取樣設定（預設為 ALWAYS）',
    required: false,
  },
  {
    name: 'exporters',
    type: 'ObservabilityExporter[]',
    description: 'Trace 資料 exporter',
    required: false,
  },
  {
    name: 'spanOutputProcessors',
    type: 'SpanOutputProcessor[]',
    description: 'Span 輸出 processor',
    required: false,
  },
  {
    name: 'includeInternalSpans',
    type: 'boolean',
    description: '包含 Mastra 操作的內部 span',
    required: false,
  },
  {
    name: 'excludeSpanTypes',
    type: 'SpanType[]',
    description:
      '要從匯出中排除的 span 類型。適合用於降低以 span 計費平台的雜訊與費用。如需詳細資訊，請參閱 [span 篩選參考](/reference/observability/tracing/span-filtering)。',
    required: false,
  },
  {
    name: 'spanFilter',
    type: '(span: AnyExportedSpan) => boolean',
    description:
      '控制匯出哪些 span 的 filter 函式。傳回 true 以保留，傳回 false 以捨棄。在 excludeSpanTypes 與 spanOutputProcessors 之後執行。如需詳細資訊，請參閱 [span 篩選參考](/reference/observability/tracing/span-filtering)。',
    required: false,
  },
  {
    name: 'requestContextKeys',
    type: 'string[]',
    description: '要擷取為 metadata 的 RequestContext key（支援點記法）',
    required: false,
  },
  {
    name: 'serializationOptions',
    type: 'SerializationOptions',
    description: '控制 span 資料（input／output／attributes）序列化的選項',
    required: false,
    properties: [
      {
        type: 'SerializationOptions',
        parameters: [
          {
            name: 'maxStringLength',
            type: 'number',
            description: '字串值的長度上限（預設：1024）',
            isOptional: true,
          },
          {
            name: 'maxDepth',
            type: 'number',
            description: '巢狀物件的深度上限（預設：6）',
            isOptional: true,
          },
          {
            name: 'maxArrayLength',
            type: 'number',
            description: 'Array 的項目數上限（預設：50）',
            isOptional: true,
          },
          {
            name: 'maxObjectKeys',
            type: 'number',
            description: '物件的 key 數量上限（預設：50）',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'logging',
    type: '{ enabled?: boolean; level?: LogLevel }',
    description:
      '控制將 log 轉送至可觀測性儲存空間。詳情請參閱[記錄至可觀測性儲存空間](/docs/observability/logging#logging-to-observability-storage)。',
    required: false,
    properties: [
      {
        type: '{ enabled?: boolean; level?: LogLevel }',
        parameters: [
          {
            name: 'enabled',
            type: 'boolean',
            description: '設為 false 以停用將 log 轉送至可觀測性儲存空間（預設：true）',
            isOptional: true,
          },
          {
            name: 'level',
            type: 'LogLevel',
            description: "要轉送的最低 log 層級。可為 'debug'、'info'、'warn'、'error'、'fatal' 之一（預設：'debug'）",
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

## `SamplingStrategy` {/* #samplingstrategy */}

```typescript
type SamplingStrategy =
  | { type: 'always' }
  | { type: 'never' }
  | { type: 'ratio'; probability: number }
  | { type: 'custom'; sampler: (options?: TracingOptions) => boolean }
```

## `ConfigSelector` {/* #configselector */}

```typescript
type ConfigSelector = (
  options: ConfigSelectorOptions,
  availableConfigs: ReadonlyMap<string, ObservabilityInstance>,
) => string | undefined
```

## `ConfigSelectorOptions` {/* #configselectoroptions */}

```typescript
interface ConfigSelectorOptions {
  requestContext?: RequestContext
}
```

## Registry 方法 {/* #registry-methods */}

Observability 類別提供用於管理可觀測性執行個體的方法：

### `registerInstance` {/* #registerinstance */}

```typescript prettier:false
registerInstance(
  name: string,
  instance: ObservabilityInstance,
  isDefault?: boolean
): void;
```

在 registry 中註冊可觀測性執行個體。

### `getInstance` {/* #getinstance */}

```typescript prettier:false
getInstance(name: string): ObservabilityInstance | undefined;
```

依名稱擷取可觀測性執行個體。

### `getDefaultInstance` {/* #getdefaultinstance */}

```typescript prettier:false
getDefaultInstance(): ObservabilityInstance | undefined;
```

傳回預設可觀測性執行個體。

### `getSelectedInstance` {/* #getselectedinstance */}

```typescript prettier:false
getSelectedInstance(
  options: ConfigSelectorOptions
): ObservabilityInstance | undefined;
```

傳回由 config selector 選取的可觀測性執行個體，若未選取則傳回預設執行個體。

### `listInstances` {/* #listinstances */}

```typescript prettier:false
listInstances(): ReadonlyMap<string, ObservabilityInstance>;
```

傳回所有已註冊的可觀測性執行個體。

### `hasInstance` {/* #hasinstance */}

```typescript prettier:false
hasInstance(name: string): boolean;
```

檢查可觀測性執行個體是否存在。

### `setConfigSelector` {/* #setconfigselector */}

```typescript prettier:false
setConfigSelector(selector: ConfigSelector): void;
```

設定 config selector 函式。

### `unregisterInstance` {/* #unregisterinstance */}

```typescript prettier:false
unregisterInstance(name: string): boolean;
```

從 registry 移除可觀測性執行個體。

### clear {/* #clear */}

```typescript prettier:false
clear(): void;
```

清除所有執行個體，但不將其關閉。

### shutdown {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>;
```

關閉所有可觀測性執行個體，並清除 registry。

## 另請參閱 {/* #see-also */}

### 文件 {/* #documentation */}

- [Tracing 概覽](/docs/observability/tracing/overview)：概念與使用指南
- [取樣策略](/docs/observability/tracing/overview#sampling-strategies)：取樣設定詳細資料
- [多設定建置](/docs/observability/overview#multi-config-setup)：使用多個設定

### 參考 {/* #reference */}

- [Tracing 類別](/reference/observability/tracing/instances)：核心 tracing 類別
- [介面](/reference/observability/tracing/interfaces)：型別定義
- [Span 參考](/reference/observability/tracing/spans)：span 生命週期

### Exporter {/* #exporters */}

- [MastraStorageExporter](/reference/observability/tracing/exporters/mastra-storage-exporter)：將可觀測性事件持久化至 Mastra Storage
- [MastraPlatformExporter](/reference/observability/tracing/exporters/mastra-platform-exporter)：將可觀測性事件傳送至 Mastra platform
- [Braintrust](/reference/observability/tracing/exporters/braintrust)：Braintrust 整合
- [Langfuse](/reference/observability/tracing/exporters/langfuse)：Langfuse 整合
- [LangSmith](/reference/observability/tracing/exporters/langsmith)：LangSmith 整合
- [Span 篩選](/reference/observability/tracing/span-filtering)：選擇性排除 span
