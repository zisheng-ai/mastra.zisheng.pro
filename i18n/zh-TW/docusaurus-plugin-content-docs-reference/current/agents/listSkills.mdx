---
title: "參考：.listSkills() | Agent"
description: "Agent .listSkills() 方法的 API 參考文件；此方法會回傳所有已設定 Skill 的 metadata。"
packages:
  - "@mastra/core"
---

# `.listSkills()`

回傳 Agent 可用的所有 Skill metadata，包括 Agent 層級與 Workspace Skill。

## 使用範例 {/* #usage-example */}

```typescript title="src/routes/skills.ts"
import { agent } from '../mastra/agents'

const skills = await agent.listSkills()

for (const skill of skills) {
  console.log(`${skill.name}: ${skill.description}`)
}
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'object',
    isOptional: true,
    description: 'Skill 解析選項。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            description: '傳給動態 Skill resolver 的 request context。',
          },
        ],
      },
    ],
  },
]}
/>

## 回傳值 {/* #return-value */}

回傳 `Promise<SkillMetadata[]>`。

每個項目都包含已探索 Skill 的 metadata：

```typescript
interface SkillMetadata {
  name: string
  description: string
  path: string
  source: { type: string; projectPath: string }
  references: string[]
  scripts: string[]
  assets: string[]
  license?: string
  compatibility?: string[]
  'user-invocable'?: boolean
  metadata?: Record<string, unknown>
}
```

## 合併行為 {/* #merging-behavior */}

同時設定 Agent 層級 Skill 與 Workspace Skill 時，`.listSkills()` 會回傳合併後的集合。若名稱衝突，Agent 層級 Skill 優先。如果兩者都定義名為 `code-review` 的 Skill，只會回傳 Agent 層級版本。

## 相關內容 {/* #related */}

- [Agent Skill](/docs/agents/skills)
- [`.getSkill()` 參考文件](/reference/agents/getSkill)
- [`createSkill()` 參考文件](/reference/agents/createSkill)
