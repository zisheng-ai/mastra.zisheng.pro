---
title: "參考：Agent.getDefaultOptions() | Agent"
description: "Mastra Agent 中 `Agent.getDefaultOptions()` 方法的文件；此方法會取得 stream 與 generate 呼叫所使用的預設選項。"
packages:
  - "@mastra/core"
---

# Agent.getDefaultOptions()

您可以為 Agent 設定記憶體使用方式與輸出格式的預設選項，也可以設定反覆運算步驟。`.getDefaultOptions()` 方法會回傳這些預設值；若值為函式，也會解析函式。除非另行覆寫，這些選項會套用至所有 `stream()` 與 `generate()` 呼叫，並可用來檢視事先不確定的 Agent 預設值。

## 使用範例 {/* #usage-example */}

```typescript
await agent.getDefaultOptions()
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ requestContext?: RequestContext }',
    isOptional: true,
    defaultValue: '{}',
    description: '包含 request context 的選用設定物件。',
    properties: [
      {
        type: '{ requestContext?: RequestContext }',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            defaultValue: 'new RequestContext()',
            description: '用於相依性注入與情境資訊的 Request Context。',
          },
        ],
      },
    ],
  },
]}
/>

## 回傳值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'defaultOptions',
    type: 'AgentExecutionOptions<Output> | Promise<AgentExecutionOptions<Output>>',
    description: '為 Agent 設定的預設串流選項；可能是直接提供的物件，也可能是解析為這些選項的 promise。',
  },
]}
/>

## 延伸使用範例 {/* #extended-usage-example */}

```typescript
await agent.getDefaultOptions({
  requestContext: new RequestContext(),
})
```

## 相關內容 {/* #related */}

- [使用 Agent 串流](/guides/concepts/streaming#streaming-with-agents)
- [Request Context](/docs/server/request-context)
