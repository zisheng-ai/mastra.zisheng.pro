---
title: "參考：Astra 向量儲存 | Vectors"
description: Mastra 中 AstraVector 類別的文件，此類別使用 DataStax Astra DB 提供向量搜尋。
packages:
  - "@mastra/astra"
---

# Astra 向量儲存

AstraVector 類別使用 [DataStax Astra DB](https://www.datastax.com/products/datastax-astra) 提供向量搜尋；Astra DB 是以 Apache Cassandra 為基礎建置的雲端原生無伺服器資料庫。
它提供企業級容量與高可用性的向量搜尋功能。

## 建構函式選項 {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'token',
    type: 'string',
    description: 'Astra DB API 權杖',
  },
  {
    name: 'endpoint',
    type: 'string',
    description: 'Astra DB API 端點',
  },
  {
    name: 'keyspace',
    type: 'string',
    isOptional: true,
    description: '選用的 keyspace 名稱',
  },
]}
/>

## 方法 {/* #methods */}

### `createIndex()` {/* #createindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要建立的索引名稱',
  },
  {
    name: 'dimension',
    type: 'number',
    description: '向量維度（必須與嵌入模型相符）',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: '相似度搜尋使用的距離度量（dotproduct 會對應至 dot_product）',
  },
]}
/>

### `upsert()` {/* #upsert */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要 upsert 資料的索引名稱',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: '嵌入向量陣列',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: '每個向量的中繼資料',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '選用的向量 ID（未提供時會自動產生）',
  },
]}
/>

### `query()` {/* #query */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要查詢的索引名稱',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: '用於尋找相似向量的查詢向量',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: '要傳回的結果數量',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '查詢的中繼資料篩選條件',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: '結果是否包含向量',
  },
]}
/>

### `listIndexes()` {/* #listindexes */}

以字串陣列傳回索引名稱。

### `describeIndex()` {/* #describeindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要描述的索引名稱',
  },
]}
/>

傳回：

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()` {/* #deleteindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要刪除的索引名稱',
  },
]}
/>

### `updateVector()` {/* #updatevector */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含該向量的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要更新的向量 ID',
  },
  {
    name: 'update',
    type: 'object',
    description: '包含向量及／或中繼資料變更的更新物件',
    properties: [
      {
        name: 'vector',
        type: 'number[]',
        isOptional: true,
        description: '新的向量值',
      },
      {
        name: 'metadata',
        type: 'Record<string, any>',
        isOptional: true,
        description: '新的中繼資料值',
      },
    ],
  },
]}
/>

### `deleteVector()` {/* #deletevector */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含該向量的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要刪除的向量 ID',
  },
]}
/>

## 回應型別 {/* #response-types */}

查詢結果會以下列格式傳回：

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
}
```

## 錯誤處理 {/* #error-handling */}

此儲存會擲回可攔截的具型別錯誤：

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

## 環境變數 {/* #environment-variables */}

必要的環境變數：

- `ASTRA_DB_TOKEN`：你的 Astra DB API 權杖
- `ASTRA_DB_ENDPOINT`：你的 Astra DB API 端點

## 相關內容 {/* #related */}

- [中繼資料篩選條件](../rag/metadata-filters)
