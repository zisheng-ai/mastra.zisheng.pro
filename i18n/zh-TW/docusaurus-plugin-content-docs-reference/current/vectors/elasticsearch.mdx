---
title: "參考：Elasticsearch 向量儲存 | Vectors"
description: Mastra 中 ElasticSearchVector 類別的文件，此類別使用 Elasticsearch 提供向量搜尋。
packages:
  - "@mastra/elasticsearch"
---

# Elasticsearch 向量儲存

`ElasticSearchVector` 類別使用 [Elasticsearch](https://www.elastic.co/elasticsearch/) 的 `dense_vector` 欄位型別與 k-NN 搜尋功能提供向量搜尋。
它是 `@mastra/elasticsearch` 套件的一部分。

## 安裝 {/* #installation */}

```bash npm2yarn
npm install @mastra/elasticsearch@latest
```

## 使用方式 {/* #usage */}

```typescript prettier:false
import { ElasticSearchVector } from "@mastra/elasticsearch";

const store = new ElasticSearchVector({
  id: "elasticsearch-vector",
  url: process.env.ELASTICSEARCH_URL,
});

// Create an index
await store.createIndex({
  indexName: "my-collection",
  dimension: 1536,
});

// Add vectors with metadata
const vectors = [[0.1, 0.2, ...], [0.3, 0.4, ...]];
const metadata = [
  { text: "first document", category: "A" },
  { text: "second document", category: "B" }
];
await store.upsert({
  indexName: "my-collection",
  vectors,
  metadata,
});

// Query similar vectors
const results = await store.query({
  indexName: "my-collection",
  queryVector: [0.1, 0.2, ...],
  topK: 10,
  filter: { category: "A" },
});
```

## 建構函式選項 {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '此向量儲存執行個體的不重複識別碼',
  },
  {
    name: 'url',
    type: 'string',
    description: "Elasticsearch 連線 URL（例如 'http://localhost:9200'）",
  },
]}
/>

## 方法 {/* #methods */}

### `createIndex()` {/* #createindex */}

使用指定的設定建立新索引。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要建立的索引名稱',
  },
  {
    name: 'dimension',
    type: 'number',
    description: '向量維度（必須與嵌入模型相符）',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: '相似度搜尋使用的距離度量',
  },
]}
/>

### `upsert()` {/* #upsert */}

在索引中新增或更新向量及其中繼資料。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要插入資料的索引名稱',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: '嵌入向量陣列',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: '每個向量的中繼資料',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '選用的向量 ID（未提供時會自動產生）',
  },
]}
/>

### `query()` {/* #query */}

搜尋相似向量，並可選擇套用中繼資料篩選。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要搜尋的索引名稱',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: '用於尋找相似向量的查詢向量',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: '要傳回的結果數量',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '中繼資料篩選條件',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: '結果是否包含向量資料',
  },
]}
/>

### `describeIndex()` {/* #describeindex */}

取得索引的相關資訊。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要描述的索引名稱',
  },
]}
/>

傳回：

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()` {/* #deleteindex */}

刪除索引及其中所有資料。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要刪除的索引名稱',
  },
]}
/>

### `listIndexes()` {/* #listindexes */}

列出所有向量索引。

傳回：`Promise<string[]>`

### `updateVector()` {/* #updatevector */}

依 ID 或中繼資料篩選條件更新單一向量。必須提供 `id` 或 `filter` 其中一項，但不能同時提供兩者。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含該向量的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: '要更新的向量 ID（不可與 filter 同時使用）',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '用於識別待更新向量的中繼資料篩選條件（不可與 id 同時使用）',
  },
  {
    name: 'update',
    type: 'object',
    description: '包含向量及／或中繼資料的更新資料',
  },
  {
    name: 'update.vector',
    type: 'number[]',
    isOptional: true,
    description: '新的向量資料',
  },
  {
    name: 'update.metadata',
    type: 'Record<string, any>',
    isOptional: true,
    description: '新的中繼資料',
  },
]}
/>

### `deleteVector()` {/* #deletevector */}

依 ID 刪除單一向量。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含該向量的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要刪除的向量 ID',
  },
]}
/>

### `deleteVectors()` {/* #deletevectors */}

依 ID 或中繼資料篩選條件刪除多個向量。必須提供 `ids` 或 `filter` 其中一項，但不能同時提供兩者。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含待刪除向量的索引名稱',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '要刪除的向量 ID 陣列（不可與 filter 同時使用）',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '用於識別待刪除向量的中繼資料篩選條件（不可與 ids 同時使用）',
  },
]}
/>

## 回應型別 {/* #response-types */}

查詢結果會以下列格式傳回：

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
}
```

## 相關內容 {/* #related */}

- [中繼資料篩選條件](../rag/metadata-filters)
