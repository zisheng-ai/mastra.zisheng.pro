---
title: "參考：Cloudflare 向量儲存 | Vectors"
description: Mastra 中 CloudflareVector 類別的文件，此類別使用 Cloudflare Vectorize 提供向量搜尋。
packages:
  - "@mastra/vectorize"
---

# Cloudflare 向量儲存

CloudflareVector 類別使用與 Cloudflare 邊緣網路整合的向量資料庫服務 [Cloudflare Vectorize](https://developers.cloudflare.com/vectorize/) 提供向量搜尋。

## 建構函式選項 {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'accountId',
    type: 'string',
    description: 'Cloudflare 帳戶 ID',
  },
  {
    name: 'apiToken',
    type: 'string',
    description: '具有 Vectorize 權限的 Cloudflare API 權杖',
  },
]}
/>

## 方法 {/* #methods */}

### `createIndex()` {/* #createindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要建立的索引名稱',
  },
  {
    name: 'dimension',
    type: 'number',
    description: '向量維度（必須與嵌入模型相符）',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: '相似度搜尋使用的距離度量（dotproduct 對應至 dot-product）',
  },
]}
/>

### `upsert()` {/* #upsert */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要 upsert 資料的索引名稱',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: '嵌入向量陣列',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: '每個向量的中繼資料',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '選用的向量 ID（未提供時會自動產生）',
  },
]}
/>

### `query()` {/* #query */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要查詢的索引名稱',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: '用於尋找相似向量的查詢向量',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: '要傳回的結果數量',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '查詢的中繼資料篩選條件',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: '結果是否包含向量',
  },
]}
/>

### `listIndexes()` {/* #listindexes */}

以字串陣列傳回索引名稱。

### `describeIndex()` {/* #describeindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要描述的索引名稱',
  },
]}
/>

傳回：

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()` {/* #deleteindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要刪除的索引名稱',
  },
]}
/>

### `createMetadataIndex()` {/* #createmetadataindex */}

在中繼資料欄位上建立索引，以啟用篩選。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含中繼資料欄位的索引名稱',
  },
  {
    name: 'propertyName',
    type: 'string',
    description: '要建立索引的中繼資料欄位名稱',
  },
  {
    name: 'indexType',
    type: "'string' | 'number' | 'boolean'",
    description: '中繼資料欄位的型別',
  },
]}
/>

### `deleteMetadataIndex()` {/* #deletemetadataindex */}

從中繼資料欄位移除索引。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含中繼資料欄位的索引名稱',
  },
  {
    name: 'propertyName',
    type: 'string',
    description: '要移除索引的中繼資料欄位名稱',
  },
]}
/>

### `listMetadataIndexes()` {/* #listmetadataindexes */}

列出某個索引的所有中繼資料欄位索引。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要列出其中繼資料索引的索引名稱',
  },
]}
/>

### `updateVector()` {/* #updatevector */}

更新索引中特定 ID 的向量或中繼資料。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含待更新 ID 的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要更新的向量或中繼資料不重複識別碼',
  },
  {
    name: 'update',
    type: '{ vector?: number[]; metadata?: Record<string, any>; }',
    description: '包含待更新向量及／或中繼資料的物件',
  },
]}
/>

### `deleteVector()` {/* #deletevector */}

刪除索引中特定 ID 的向量及其關聯中繼資料。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含待刪除 ID 的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要刪除的向量與中繼資料不重複識別碼',
  },
]}
/>

## 回應型別 {/* #response-types */}

查詢結果會以下列格式傳回：

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[]
}
```

## 錯誤處理 {/* #error-handling */}

此儲存會擲回可攔截的具型別錯誤：

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

## 環境變數 {/* #environment-variables */}

必要的環境變數：

- `CLOUDFLARE_ACCOUNT_ID`：你的 Cloudflare 帳戶 ID
- `CLOUDFLARE_API_TOKEN`：具有 Vectorize 權限的 Cloudflare API 權杖

## 相關內容 {/* #related */}

- [中繼資料篩選條件](../rag/metadata-filters)
