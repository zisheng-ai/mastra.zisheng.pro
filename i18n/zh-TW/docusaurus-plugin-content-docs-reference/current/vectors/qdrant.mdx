---
title: "參考：Qdrant 向量儲存 | Vectors"
description: 將 Qdrant 與 Mastra 整合的文件；Qdrant 是用於管理向量與承載資料的向量相似度搜尋引擎。
packages:
  - "@mastra/qdrant"
---

# Qdrant 向量儲存

QdrantVector 類別使用向量相似度搜尋引擎 [Qdrant](https://qdrant.tech/) 提供向量搜尋。
它提供可用於正式環境的服務，並具備方便的 API，可儲存、搜尋和管理向量，且支援額外承載資料與擴充篩選功能。

## 建構函式選項

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: 'Qdrant 執行個體的 REST URL。例如 https://xyz-example.eu-central.aws.cloud.qdrant.io:6333',
  },
  {
    name: 'apiKey',
    type: 'string',
    description: '選用的 Qdrant API 金鑰',
  },
  {
    name: 'https',
    type: 'boolean',
    description: '設定連線時是否使用 TLS。建議使用。',
  },
]}
/>

## 方法

### `createIndex()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要建立的索引名稱',
  },
  {
    name: 'dimension',
    type: 'number',
    description: '向量維度（必須與嵌入模型相符）。單一向量集合必須提供。',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: '相似度搜尋使用的距離度量',
  },
  {
    name: 'namedVectors',
    type: "Record<string, { size: number; distance: 'cosine' | 'euclidean' | 'dotproduct' }>",
    isOptional: true,
    description: '具名向量空間的設定。提供此項時，會建立具有多個具名向量欄位的集合。',
  },
]}
/>

#### 建立具名向量集合

```typescript
// Create a collection with multiple named vector spaces
await store.createIndex({
  indexName: 'multi_modal',
  dimension: 768, // fallback
  namedVectors: {
    text: { size: 768, distance: 'cosine' },
    image: { size: 512, distance: 'euclidean' },
  },
})
```

### `upsert()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要 upsert 資料的索引名稱',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: '嵌入向量陣列',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: '每個向量的中繼資料',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '選用的向量 ID（未提供時會自動產生）',
  },
  {
    name: 'vectorName',
    type: 'string',
    isOptional: true,
    description: '使用具名向量時，要 upsert 資料的向量空間名稱。',
  },
]}
/>

#### Upsert 至具名向量空間

```typescript
// Upsert into the "text" vector space
await store.upsert({
  indexName: 'multi_modal',
  vectors: textEmbeddings,
  metadata: textMetadata,
  vectorName: 'text',
})

// Upsert into the "image" vector space
await store.upsert({
  indexName: 'multi_modal',
  vectors: imageEmbeddings,
  metadata: imageMetadata,
  vectorName: 'image',
})
```

### `query()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要查詢的索引名稱',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: '用於尋找相似向量的查詢向量',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: '要傳回的結果數量',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '查詢的中繼資料篩選條件',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: '結果是否包含向量',
  },
  {
    name: 'using',
    type: 'string',
    isOptional: true,
    description: '使用具名向量時要查詢的向量欄位名稱。集合有多個具名向量欄位時使用此選項。',
  },
]}
/>

#### 具名向量

Qdrant 支援[每個集合使用多個向量](https://qdrant.tech/documentation/concepts/vectors/#named-vectors)，並為每個向量欄位指派名稱。使用 `using` 參數選擇要查詢的向量欄位：

```typescript
const results = await store.query({
  indexName: 'my_index',
  queryVector: embedding,
  topK: 10,
  using: 'title_embedding', // Query against a specific named vector
})
```

### `listIndexes()`

以字串陣列傳回索引名稱。

### `describeIndex()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要描述的索引名稱',
  },
]}
/>

傳回：

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要刪除的索引名稱',
  },
]}
/>

### `updateVector()`

依 ID 或中繼資料篩選條件更新單一向量。必須提供 `id` 或 `filter` 其中一項，但不能同時提供兩者。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要更新的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: '要更新的向量 ID（不可與 filter 同時使用）',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '用於識別待更新向量的中繼資料篩選條件（不可與 id 同時使用）',
  },
  {
    name: 'update',
    type: '{ vector?: number[]; metadata?: Record<string, any>; }',
    description: '包含待更新向量及／或中繼資料的物件',
  },
]}
/>

更新指定索引中的向量及／或其中繼資料。如果同時提供向量與中繼資料，兩者都會更新。只提供其中一個值時，僅更新該值。

### `deleteVector()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要從中刪除向量的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要刪除的向量 ID',
  },
]}
/>

依 ID 從指定索引刪除向量。

### `deleteVectors()`

依 ID 或中繼資料篩選條件刪除多個向量。必須提供 `ids` 或 `filter` 其中一項，但不能同時提供兩者。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含待刪除向量的索引名稱',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '要刪除的向量 ID 陣列（不可與 filter 同時使用）',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '用於識別待刪除向量的中繼資料篩選條件（不可與 ids 同時使用）',
  },
]}
/>

### `createPayloadIndex()`

在集合欄位上建立承載資料（中繼資料）索引，以啟用高效率篩選。Qdrant Cloud 以及任何設定 `strict_mode_config = true` 的 Qdrant 執行個體都**必須**執行此操作。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要建立承載資料索引的集合名稱',
  },
  {
    name: 'fieldName',
    type: 'string',
    description: '要建立索引的承載資料欄位名稱',
  },
  {
    name: 'fieldSchema',
    type: "'keyword' | 'integer' | 'float' | 'geo' | 'text' | 'bool' | 'datetime' | 'uuid'",
    description: '承載資料欄位的 schema 型別',
  },
  {
    name: 'wait',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'true',
    description: '是否等待操作完成',
  },
]}
/>

```typescript
// Create a keyword index for filtering by source
await store.createPayloadIndex({
  indexName: 'my_index',
  fieldName: 'source',
  fieldSchema: 'keyword',
})

const results = await store.query({
  indexName: 'my_index',
  queryVector: queryVector,
  filter: { source: 'document-a' },
})
```

### `deletePayloadIndex()`

從集合欄位移除承載資料索引。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要從中刪除承載資料索引的集合名稱',
  },
  {
    name: 'fieldName',
    type: 'string',
    description: '要刪除的承載資料欄位索引名稱',
  },
  {
    name: 'wait',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'true',
    description: '是否等待操作完成',
  },
]}
/>

## 回應型別

查詢結果會以下列格式傳回：

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
}
```

## 錯誤處理

此儲存會擲回可攔截的具型別錯誤：

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

## 相關內容

- [中繼資料篩選條件](../rag/metadata-filters)
