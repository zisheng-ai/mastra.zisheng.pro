---
title: "參考：AzureBlobFilesystem | Workspace"
description: "Azure Blob Storage 的 AzureBlobFilesystem Provider 文件。"
packages:
  - "@mastra/azure"
---

# AzureBlobFilesystem

將檔案儲存在 Azure Blob Storage 容器中。介面詳情請參閱 [WorkspaceFilesystem 介面](/reference/workspace/filesystem)。

## 安裝 {/* #installation */}

```bash npm2yarn
npm install @mastra/azure
```

## 使用方式 {/* #usage */}

將 `AzureBlobFilesystem` 加入 Workspace，並指派給 Agent：

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const workspace = new Workspace({
  filesystem: new AzureBlobFilesystem({
    container: 'my-container',
    connectionString: process.env.AZURE_STORAGE_CONNECTION_STRING,
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  workspace,
})
```

### 帳戶金鑰 {/* #account-key */}

如果不想傳入完整連線字串，請使用帳戶名稱與帳戶金鑰：

```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  accountKey: process.env.AZURE_STORAGE_ACCOUNT_KEY,
})
```

### 共用存取簽章 Token {/* #shared-access-signature-token */}

將共用存取簽章（SAS）Token 與 `accountName` 或 `endpoint` 搭配使用：

```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  sasToken: process.env.AZURE_STORAGE_SAS_TOKEN,
})
```

### DefaultAzureCredential {/* #defaultazurecredential */}

如果環境提供 Azure 身分認證，請使用 `DefaultAzureCredential`：

```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  useDefaultCredential: true,
})
```

使用 `DefaultAzureCredential` 時，請安裝 `@azure/identity`：

```bash npm2yarn
npm install @azure/identity
```

## 建構函式參數 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'container',
    type: 'string',
    description: 'Azure Blob Storage 容器名稱',
  },
  {
    name: 'connectionString',
    type: 'string',
    description: '完整的 Azure Storage 連線字串。優先於其他驗證選項。',
    isOptional: true,
  },
  {
    name: 'accountName',
    type: 'string',
    description: 'Azure Storage 帳戶名稱。除非使用 connectionString 或 endpoint，否則為必填。',
    isOptional: true,
  },
  {
    name: 'accountKey',
    type: 'string',
    description: 'Azure Storage 帳戶金鑰。',
    isOptional: true,
  },
  {
    name: 'sasToken',
    type: 'string',
    description: '共用存取簽章 Token。需要 accountName 或 endpoint。',
    isOptional: true,
  },
  {
    name: 'useDefaultCredential',
    type: 'boolean',
    description: '使用 @azure/identity 的 DefaultAzureCredential。',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'endpoint',
    type: 'string',
    description: '自訂 Blob 服務端點 URL。用於搭配 Azurite 進行本機開發。',
    isOptional: true,
  },
  {
    name: 'prefix',
    type: 'string',
    description: '所有鍵可使用的選用前綴（作用如同子目錄）。',
    isOptional: true,
  },
  {
    name: 'id',
    type: 'string',
    description: '此檔案系統執行個體的唯一識別碼。',
    isOptional: true,
    defaultValue: 'Auto-generated',
  },
  {
    name: 'displayName',
    type: 'string',
    description: '在 UI 中顯示的易讀名稱。',
    isOptional: true,
  },
  {
    name: 'icon',
    type: 'FilesystemIcon',
    description: 'UI 使用的圖示識別碼。',
    isOptional: true,
  },
  {
    name: 'description',
    type: 'string',
    description: '在 UI 中顯示的檔案系統簡短說明。',
    isOptional: true,
  },
  {
    name: 'readOnly',
    type: 'boolean',
    description: '設為 true 時，會封鎖所有寫入操作。',
    isOptional: true,
    defaultValue: 'false',
  },
]}
/>

## 屬性 {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '檔案系統執行個體識別碼。',
  },
  {
    name: 'name',
    type: 'string',
    description: "Provider 名稱（'AzureBlobFilesystem'）。",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Provider 識別碼（'azure-blob'）。",
  },
  {
    name: 'readOnly',
    type: 'boolean | undefined',
    description: '檔案系統是否處於唯讀模式。',
  },
]}
/>

## 方法 {/* #methods */}

AzureBlobFilesystem 實作 [WorkspaceFilesystem 介面](/reference/workspace/filesystem)，並提供所有標準檔案系統方法：

- `readFile(path, options?)` - 讀取檔案內容
- `writeFile(path, content, options?)` - 將內容寫入檔案
- `appendFile(path, content)` - 將內容附加至檔案
- `deleteFile(path, options?)` - 刪除檔案
- `copyFile(src, dest, options?)` - 複製檔案
- `moveFile(src, dest, options?)` - 移動檔案或重新命名
- `mkdir(path, options?)` - 建立目錄
- `rmdir(path, options?)` - 移除目錄
- `readdir(path, options?)` - 列出目錄內容
- `exists(path)` - 檢查路徑是否存在
- `stat(path)` - 取得檔案或目錄的中繼資料

### `init()` {/* #init */}

初始化檔案系統。驗證容器存取權與認證。

```typescript
await filesystem.init()
```

### `getInfo()` {/* #getinfo */}

傳回此檔案系統執行個體的中繼資料。

```typescript
const info = filesystem.getInfo()
// { id: '...', name: 'AzureBlobFilesystem', provider: 'azure-blob', status: 'ready' }
```

### `getContainer()` {/* #getcontainer */}

傳回底層 Azure Blob Storage `ContainerClient`。

```typescript
const container = await filesystem.getContainer()
```

### `getMountConfig()` {/* #getmountconfig */}

傳回支援 Azure Blob 檔案系統掛載之 Sandbox Provider 的掛載設定。

```typescript
const config = filesystem.getMountConfig()
// { type: 'azure-blob', container: 'my-container', ... }
```

:::note

Azure Blob Sandbox 掛載取決於 Sandbox 是否支援 `azure-blob` 掛載設定。當 Sandbox Provider 不支援 Azure Blob 掛載時，請使用 `filesystem` 直接執行 Workspace 檔案操作。

:::

## 相關內容 {/* #related */}

- [WorkspaceFilesystem 介面](/reference/workspace/filesystem)
- [S3Filesystem 參考](/reference/workspace/s3-filesystem)
- [GCSFilesystem 參考](/reference/workspace/gcs-filesystem)
- [Workspace 概觀](/docs/workspace/overview)
