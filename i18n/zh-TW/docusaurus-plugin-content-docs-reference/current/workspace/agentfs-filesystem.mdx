---
title: "參考：AgentFSFilesystem | Workspace"
description: "AgentFSFilesystem Provider 的文件；此 Provider 使用 Turso/SQLite 後端儲存空間。"
packages:
  - "@mastra/agentfs"
---

# AgentFSFilesystem

透過 [AgentFS](https://github.com/nichochar/agentfs) SDK 將檔案儲存在 Turso/SQLite 資料庫中。檔案會跨工作階段持久保存於本機 SQLite 資料庫，讓 Agent 無須外部雲端服務也能使用持久儲存空間。介面詳情請參閱 [WorkspaceFilesystem 介面](/reference/workspace/filesystem)。

## 安裝 {/* #installation */}

```bash npm2yarn
npm install @mastra/agentfs
```

## 使用方式 {/* #usage */}

將 `AgentFSFilesystem` 加入 Workspace，並指派給 Agent。建立 `AgentFSFilesystem` 類別的執行個體時，必須至少提供 `agentId`、`path` 或 `agent` 其中之一。

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { AgentFSFilesystem } from '@mastra/agentfs'

const workspace = new Workspace({
  filesystem: new AgentFSFilesystem({
    agentId: 'my-agent',
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_OPENAI_MODEL__',
  workspace,
})
```

### 使用明確的資料庫路徑 {/* #using-an-explicit-database-path */}

預設情況下，資料庫會儲存在 `.agentfs` 目錄中，並以 `agentId` 作為檔名。你也可以指定自訂資料庫路徑：

```typescript
import { AgentFSFilesystem } from '@mastra/agentfs'

const filesystem = new AgentFSFilesystem({
  path: '/data/my-agent.db',
})
```

### 使用預先開啟的 AgentFS 執行個體 {/* #using-a-pre-opened-agentfs-instance */}

如果需要自行管理 AgentFS 的生命週期：

```typescript
import { AgentFS } from 'agentfs-sdk'
import { AgentFSFilesystem } from '@mastra/agentfs'

const agent = await AgentFS.open({ id: 'my-agent' })

const filesystem = new AgentFSFilesystem({
  agent, // caller manages open/close
})
```

## 建構函式參數 {/* #constructor-parameters */}

你必須至少提供 `agentId`、`path` 或 `agent` 其中之一。

<PropertiesTable
  content={[
  {
    name: 'agentId',
    type: 'string',
    description: 'Agent ID；會在 `.agentfs/<agentId>.db` 建立資料庫',
    isOptional: true,
  },
  {
    name: 'path',
    type: 'string',
    description: '明確的資料庫檔案路徑（可取代 agentId）',
    isOptional: true,
  },
  {
    name: 'agent',
    type: 'AgentFS',
    description: '預先開啟的 AgentFS 執行個體。提供此值時，由呼叫端管理生命週期（開啟／關閉）。',
    isOptional: true,
  },
  {
    name: 'id',
    type: 'string',
    description: '此檔案系統執行個體的唯一識別碼',
    isOptional: true,
    defaultValue: '自動產生',
  },
  {
    name: 'displayName',
    type: 'string',
    description: '在 UI 中顯示的易讀名稱',
    isOptional: true,
    defaultValue: "'AgentFS'",
  },
  {
    name: 'icon',
    type: 'FilesystemIcon',
    description: 'UI 使用的圖示識別碼',
    isOptional: true,
    defaultValue: "'database'",
  },
  {
    name: 'description',
    type: 'string',
    description: '在 UI 中顯示的檔案系統簡短說明',
    isOptional: true,
  },
  {
    name: 'readOnly',
    type: 'boolean',
    description: '設為 true 時，會封鎖所有寫入操作',
    isOptional: true,
    defaultValue: 'false',
  },
]}
/>

## 屬性 {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '檔案系統執行個體識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: "Provider 名稱（'AgentFSFilesystem'）",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Provider 識別碼（'agentfs'）",
  },
  {
    name: 'readOnly',
    type: 'boolean | undefined',
    description: '檔案系統是否處於唯讀模式',
  },
]}
/>
