---
title: "參考文件：createACPTool() | ACP"
description: "createACPTool() 的 API 參考文件。此函式會將 ACP 相容的 Coding Agent 包裝為 Mastra Tool。"
packages:
  - "@mastra/acp"
  - "@mastra/core"
---

# createACPTool()

`createACPTool()` 函式會建立 Mastra Tool，將 `task` 字串傳送給 Agent Client Protocol（ACP）相容的 Coding Agent，並以 `output` 傳回最終 ACP 回應。當上層 Agent 應決定何時將 ACP Agent 當作 Tool 呼叫時，可使用此函式。

若要改將 ACP Agent 註冊為 Mastra subagent，請使用 [`AcpAgent` 類別](/reference/acp/acp-agent)。

## 使用範例

建立程式碼編輯 Tool，並在上層 Agent 上註冊：

```typescript title="src/mastra/agents/code-supervisor.ts"
import { createACPTool } from '@mastra/acp'
import { Agent } from '@mastra/core/agent'

const codeAgentTool = createACPTool({
  id: 'code-agent',
  description: 'Use an ACP-compatible coding agent to inspect and edit code',
  command: 'acp-agent',
  args: ['--stdio'],
  cwd: process.cwd(),
})

export const codeSupervisor = new Agent({
  id: 'code-supervisor',
  name: 'Code Supervisor',
  instructions: 'Use the code-agent tool when a task requires repository inspection or code edits.',
  model: '__GATEWAY_OPENAI_MODEL__',
  tools: {
    codeAgentTool,
  },
})
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Mastra Tool 的唯一識別碼。',
  },
  {
    name: 'description',
    type: 'string',
    description: '模型可呼叫此 Tool 時顯示的說明。',
  },
  {
    name: 'command',
    type: 'string',
    description: '要產生的 ACP Agent 可執行檔。',
  },
  {
    name: 'args',
    type: 'string[]',
    description: '傳給 ACP Agent 可執行檔的引數。',
    isOptional: true,
    defaultValue: '[]',
  },
  {
    name: 'env',
    type: 'Record<string, string>',
    description: '產生 ACP 處理程序時，要與目前處理程序環境合併的環境變數。',
    isOptional: true,
  },
  {
    name: 'cwd',
    type: 'string',
    description: 'ACP 處理程序與 ACP session 的工作目錄，也會作為預設的本機檔案系統基底路徑。',
    isOptional: true,
    defaultValue: 'process.cwd()',
  },
  {
    name: 'session',
    type: 'Partial<NewSessionRequest>',
    description: 'ACP session 建立選項。預設使用 `cwd` 或 `process.cwd()`，並使用空的 MCP 伺服器清單。',
    isOptional: true,
  },
  {
    name: 'initialize',
    type: 'Partial<InitializeRequest>',
    description: 'ACP 初始化選項。預設使用 Mastra 用戶端資訊、目前 ACP 通訊協定版本，以及檔案系統讀寫功能。',
    isOptional: true,
  },
  {
    name: 'authMethodId',
    type: 'string',
    description: '初始化後、建立 session 前要叫用的 ACP 驗證方法 ID。',
    isOptional: true,
  },
  {
    name: 'persistSession',
    type: 'boolean',
    description:
      '為執行 Tool 建立的 ACP connection 是否在 prompt 後中斷連線。設為 `false` 可在每個 prompt 完成後停止處理程序。',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'onPermissionRequest',
    type: '(request: RequestPermissionRequest) => Promise<RequestPermissionResponse>',
    description: 'ACP Agent 要求權限時叫用的 callback。預設會選取第一個權限選項；沒有可用選項時則取消。',
    isOptional: true,
  },
  {
    name: 'createClient',
    type: '(defaultClient: Client) => Client',
    description:
      '自訂用來回應 Agent 要求的 ACP 用戶端。此函式會接收預設用戶端，以便加以包裝或擴充，例如加入 `extMethod` 與 `extNotification` handler。',
    isOptional: true,
  },
  {
    name: 'workspace',
    type: 'Workspace',
    description:
      '共用 ACP connection 選項中的 Workspace 選項。執行 Tool 時，若執行 context 中有目前的 Mastra Workspace，`createACPTool()` 會將其傳入；否則 ACP connection 會退回使用本機檔案系統 Workspace。若需要提供明確的 Workspace 執行個體，請使用 `AcpAgent`。',
    isOptional: true,
  },
  {
    name: 'model',
    type: 'ModelId',
    description: '建立 ACP session 後，使用 ACP `session/set_model` 方法選取的模型 ID。',
    isOptional: true,
  },
]}
/>

## 輸入 schema

<PropertiesTable
  content={[
  {
    name: 'task',
    type: 'string',
    description: '要傳送給 ACP Agent 的工作。',
  },
]}
/>

## 輸出 schema

<PropertiesTable
  content={[
  {
    name: 'output',
    type: 'string',
    description: 'ACP Agent 傳回的最終文字輸出。',
  },
]}
/>

## Suspend 與 resume schema

`createACPTool()` 會為權限要求 payload 定義 suspend 與 resume schema。權限決策會透過 `onPermissionRequest` 傳回；`@mastra/acp` 預設會選取 ACP Agent 傳回的第一個選項，沒有可用選項時則取消。

### Suspend payload

<PropertiesTable
  content={[
  {
    name: 'permissionRequest',
    type: '{ title: string; options: { optionId: string; name: string }[] }',
    description: 'ACP Agent 傳回的權限要求標題與可選選項。',
  },
]}
/>

### Resume payload

<PropertiesTable
  content={[
  {
    name: 'optionId',
    type: 'string',
    description: '以 `outcome: "selected"` 繼續時，要選取的權限選項 ID。',
    isOptional: true,
  },
  {
    name: 'outcome',
    type: '"selected" | "cancelled"',
    description: '用於繼續或取消 ACP 要求的權限決策。',
    isOptional: true,
  },
]}
/>

## Session 生命週期

每次執行 Tool 都會建立 ACP connection，並啟動所設定的 `command`。它會先初始化 ACP 用戶端並建立 ACP session，再透過 ACP `session/prompt` 傳送 `task`。

執行 Tool 時建立的 ACP connection，其 `persistSession` 預設為 `true`。若 ACP 處理程序應在該 prompt 完成後立即停止，請設定 `persistSession: false`。

若需要在多次呼叫之間重複使用 ACP subagent 執行個體，並明確控制 session 生命週期，請使用 [`AcpAgent`](/reference/acp/acp-agent#session-lifecycle)。

## 權限處理

ACP Agent 可能會要求用戶端先選擇權限選項，才繼續執行。`@mastra/acp` 預設會選取 ACP Agent 傳回的第一個選項；沒有可用選項時則取消。

傳入 `onPermissionRequest` 可檢查要求並傳回自己的權限回應：

```typescript title="src/mastra/agents/code-agent.ts"
import { createACPTool } from '@mastra/acp'

export const codeAgentTool = createACPTool({
  id: 'code-agent',
  description: 'Use an ACP-compatible coding agent',
  command: 'acp-agent',
  args: ['--stdio'],
  async onPermissionRequest(request) {
    const allowOption = request.options.find(option => option.name === 'Allow')

    if (!allowOption) {
      return { outcome: { outcome: 'cancelled' } }
    }

    return {
      outcome: {
        outcome: 'selected',
        optionId: allowOption.optionId,
      },
    }
  },
})
```

可使用此 callback 強制執行本機 policy，或檢查權限標題，也可以將決策導向自己的核准流程。

## 擴充方法

某些 ACP Agent 會在標準 ACP 要求集合之外，呼叫用戶端上的自訂擴充方法。預設用戶端會以「Method not found」錯誤拒絕未知方法，這可能會中止 Agent 的 turn。

傳入 `createClient` 可擴充或取代預設用戶端。callback 會接收預設用戶端，並傳回 connection 使用的用戶端：

```typescript title="src/mastra/agents/code-agent.ts"
import { createACPTool } from '@mastra/acp'

export const codeAgentTool = createACPTool({
  id: 'code-agent',
  description: 'Use an ACP-compatible coding agent',
  command: 'acp-agent',
  args: ['--stdio'],
  createClient: defaultClient =>
    Object.assign(defaultClient, {
      async extMethod(method: string, params: Record<string, unknown>) {
        return {}
      },
      async extNotification(method: string, params: Record<string, unknown>) {},
    }),
})
```

若也需要變更標準 handler，請傳回完全自訂的 `Client` 實作。`Client` 型別會從 `@mastra/acp` 重新匯出。

## 相關內容

- [Agent Client Protocol 文件](/docs/agents/acp)
- [AcpAgent 類別參考文件](/reference/acp/acp-agent)
- [Tool 參考文件](/reference/tools/create-tool)
- [Agent Client Protocol 簡介](https://agentclientprotocol.com/overview/introduction)
- [Agent Client Protocol schema](https://agentclientprotocol.com/protocol/schema)
