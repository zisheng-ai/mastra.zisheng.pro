---
title: "參考：Observability API | Client SDK"
description: 瞭解如何使用 client-js SDK 取得 Trace、監控應用效能、為 Trace 評分並記錄意見回饋。
packages:
  - "@mastra/client-js"
---

# Observability API

Observability API 提供用於取得 Trace、監控應用效能、為 Trace 評分以進行評估，以及記錄與 Trace 和 span 關聯的意見回饋的方法。

## 取得特定 Trace {/* #getting-a-specific-trace */}

透過 ID 取得特定 Trace，包括其所有 span 和詳細資訊：

```typescript
const trace = await mastraClient.getTrace('trace-id-123')
```

## 篩選並取得 Trace {/* #getting-traces-with-filtering */}

取得選用篩選的 Trace 根 span 分頁清單：

```typescript
const traces = await mastraClient.getTraces({
  pagination: {
    page: 1,
    perPage: 20,
    dateRange: {
      start: new Date('2024-01-01'),
      end: new Date('2024-01-31'),
    },
  },
  filters: {
    name: 'weather-agent', // Filter by trace name
    spanType: 'agent', // Filter by span type
    entityId: 'weather-agent-id', // Filter by entity ID
    entityType: 'agent', // Filter by entity type
  },
})

console.log(`Found ${traces.spans.length} root spans`)
console.log(`Total pages: ${traces.pagination.totalPages}`)

// To get the complete trace with all spans, use getTrace
const completeTrace = await mastraClient.getTrace(traces.spans[0].traceId)
```

## 為清單視圖列出 Trace {/* #listing-traces-for-a-list-view */}

`listTracesLight()` 傳回與 Trace 清單相同的行，但不包含 `input`、`output` 和 `attributes` payload。每一行改為攜帶簡短的 `inputPreview` 字串，因此清單不必傳輸完整 prompt 即可呈現預覽列。

同時呈現大量 Trace 時應優先使用它，且僅在打開某一行時取得完整記錄：

```typescript
const list = await mastraClient.listTracesLight({
  pagination: { page: 0, perPage: 25 },
  filters: { entityType: 'agent' },
})

for (const span of list.spans) {
  console.log(span.name, span.inputPreview)
}

// Fetch the full payload only for the trace the user selects
const selected = await mastraClient.getTrace(list.spans[0].traceId)
```

它接受與 `listTraces()` 相同的篩選、排序和增量輪詢參數。確實需要完整 span payload 時，請使用 `listTraces()`。

## 為 Trace 評分 {/* #scoring-traces */}

使用已註冊的 Scorer 為特定 Trace 評分，以進行評估：

```typescript
const result = await mastraClient.score({
  scorerName: 'answer-relevancy',
  targets: [
    { traceId: 'trace-1', spanId: 'span-1' }, // Score specific span
    { traceId: 'trace-2' }, // Score specific span which defaults to the parent span
  ],
})
```

## 依 span 取得評分 {/* #getting-scores-by-span */}

取得 Trace 中特定 span 的評分：

```typescript
const scores = await mastraClient.listScoresBySpan({
  traceId: 'trace-123',
  spanId: 'span-456',
  page: 1,
  perPage: 20,
})
```

## 意見回饋 {/* #feedback */}

意見回饋方法用於建立、列出和查詢人工參與的訊號，例如評分、讚／倒讚、評論和糾正。有關範例，請參閱[意見回饋指南](/docs/observability/feedback)；有關完整 schema，請參閱[意見回饋參考](/reference/observability/feedback)。

### 建立意見回饋 {/* #creating-feedback */}

建立與 Trace 或 span 關聯的意見回饋記錄：

```typescript
const response = await mastraClient.createFeedback({
  feedback: {
    traceId: 'trace-123',
    spanId: 'span-456',
    feedbackSource: 'user',
    feedbackType: 'rating',
    value: 1,
    comment: 'Helpful answer.',
  },
})
```

### 列出意見回饋 {/* #listing-feedback */}

取得選用篩選的意見回饋記錄分頁清單：

```typescript
const feedback = await mastraClient.listFeedback({
  filters: {
    feedbackType: 'rating',
    feedbackSource: 'studio',
  },
  pagination: { page: 0, perPage: 20 },
  orderBy: { field: 'timestamp', direction: 'DESC' },
})
```

### 彙總意見回饋 {/* #aggregating-feedback */}

彙總數值型意見回饋值，例如評分或以 `1` 和 `-1` 編碼的讚／倒讚：

```typescript
const averageRating = await mastraClient.getFeedbackAggregate({
  feedbackType: 'rating',
  aggregation: 'avg',
  comparePeriod: 'previous_day',
})
```

### 對意見回饋分組 {/* #grouping-feedback */}

依可觀測性維度對數值型意見回饋分組：

```typescript
const ratingsByAgent = await mastraClient.getFeedbackBreakdown({
  feedbackType: 'rating',
  groupBy: ['entityName'],
  aggregation: 'avg',
})
```

### 依時間查詢意見回饋 {/* #querying-feedback-over-time */}

依時間間隔對數值型意見回饋分組：

```typescript
const ratingsOverTime = await mastraClient.getFeedbackTimeSeries({
  feedbackType: 'rating',
  interval: '1h',
  aggregation: 'avg',
  groupBy: ['feedbackSource'],
})
```

### 查詢意見回饋百分位數 {/* #querying-feedback-percentiles */}

傳回數值型意見回饋值的百分位數序列：

```typescript
const ratingPercentiles = await mastraClient.getFeedbackPercentiles({
  feedbackType: 'rating',
  percentiles: [0.5, 0.95],
  interval: '1d',
})
```

## 相關內容 {/* #related */}

- [意見回饋指南](/docs/observability/feedback)：瞭解如何建立、列出和查詢意見回饋
- [意見回饋參考](/reference/observability/feedback)：查看意見回饋 schema 和 HTTP 路由
- [Agents API](./agents)：瞭解會產生 Trace 的 Agent 互動
- [Workflows API](./workflows)：瞭解 Workflow 執行監控
