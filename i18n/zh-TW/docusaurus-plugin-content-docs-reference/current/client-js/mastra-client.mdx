---
title: "參考：Mastra client SDK | Client SDK"
description: 瞭解如何使用 client-js SDK 與 Mastra 互動。
packages:
  - "@mastra/client-js"
---

# Mastra client SDK

Mastra Client SDK 提供類型安全的介面，用於從使用者端環境與 [Mastra Server](/docs/deployment/mastra-server) 互動。

## 使用範例

```typescript title="lib/mastra/mastra-client.ts"
import { MastraClient } from '@mastra/client-js'

export const mastraClient = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})
```

## `RequestContext`

在 client SDK 中使用 `RequestContext` 時，請從 `@mastra/client-js` 匯入。

```typescript
import { MastraClient, RequestContext } from '@mastra/client-js'

const client = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})

const requestContext = new RequestContext()
requestContext.set('userId', 'user-123')

const agent = client.getAgent('support-agent')

const response = await agent.generate('Summarize this ticket', {
  requestContext,
})
```

你也可以將 `requestContext` 作為 `Record<string, any>` 傳入。

## 參數

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: 'Mastra API 的基礎 URL。所有請求都將相對於此 URL 傳送。',
    isOptional: false,
  },
  {
    name: 'retries',
    type: 'number',
    description: '請求失敗後、擲回錯誤前的重試次數。',
    isOptional: true,
    defaultValue: '3',
  },
  {
    name: 'backoffMs',
    type: 'number',
    description: '重試失敗請求前的初始延遲（以毫秒為單位）。每次重試時，此值都會加倍（指數退避）。',
    isOptional: true,
    defaultValue: '300',
  },
  {
    name: 'maxBackoffMs',
    type: 'number',
    description: '最大退避時間（以毫秒為單位）。防止兩次嘗試之間等待過久。',
    isOptional: true,
    defaultValue: '5000',
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: '包含自訂 HTTP 標頭的物件，這些標頭會隨每個請求一起傳送。',
    isOptional: true,
  },
  {
    name: 'credentials',
    type: '"omit" | "same-origin" | "include"',
    description:
      '請求的認證資訊模式。更多資訊請參閱 https://developer.mozilla.org/en-US/docs/Web/API/Request/credentials。',
    isOptional: true,
  },
]}
/>

## 方法

<PropertiesTable
  content={[
  {
    name: 'listAgents()',
    type: 'Promise<Record<string, GetAgentResponse>>',
    description: '傳回所有可用的 Agent 執行個體。',
  },
  {
    name: 'getAgent(agentId)',
    type: 'Agent',
    description: '透過 ID 取得特定 Agent 執行個體。',
  },
  {
    name: 'listMemoryThreads(params)',
    type: 'Promise<StorageThreadType[]>',
    description: '取得指定資源和 Agent 的 memory thread。需要 `resourceId` 和 `agentId`。',
  },
  {
    name: 'createMemoryThread(params)',
    type: 'Promise<MemoryThread>',
    description: '使用指定參數建立新的 memory thread。',
  },
  {
    name: 'getMemoryThread({ threadId, agentId })',
    type: 'MemoryThread',
    description: '透過 ID 取得特定 memory thread。',
  },
  {
    name: 'saveMessageToMemory(params)',
    type: 'Promise<{ messages: (MastraMessageV1 | MastraDBMessage)[] }>',
    description: '將一條或多條訊息儲存到 memory 系統。傳回已儲存的訊息。',
  },
  {
    name: 'getMemoryStatus()',
    type: 'Promise<MemoryStatus>',
    description: '傳回 memory 系統的目前狀態。',
  },
  {
    name: 'listTools()',
    type: 'Record<string, Tool>',
    description: '傳回所有可用的 Tool。',
  },
  {
    name: 'getTool(toolId)',
    type: 'Tool',
    description: '透過 ID 取得特定 Tool 執行個體。',
  },
  {
    name: 'listWorkflows()',
    type: 'Record<string, Workflow>',
    description: '傳回所有可用的 Workflow 執行個體。',
  },
  {
    name: 'getWorkflow(workflowId)',
    type: 'Workflow',
    description: '透過 ID 取得特定 Workflow 執行個體。',
  },
  {
    name: 'getAgentBuilderActions()',
    type: 'Promise<Record<string, WorkflowInfo>>',
    description:
      '傳回所有可用的 Agent Builder action。請參閱 [Agent Builder API](https://agent-builder.mastra.ai/reference/client)。',
  },
  {
    name: 'getAgentBuilderAction(actionId)',
    type: 'AgentBuilder',
    description:
      '透過 ID 取得 Agent Builder action。請參閱 [Agent Builder API](https://agent-builder.mastra.ai/reference/client)。',
  },
  {
    name: 'responses',
    type: 'Responses',
    description: '提供 OpenAI 風格的 Responses API 輔助方法，包括 `create()`、`retrieve()`、`stream()` 和 `delete()`。',
  },
  {
    name: 'conversations',
    type: 'Conversations',
    description: '提供對話輔助方法，包括 `create()`、`retrieve()`、`delete()` 和 `items.list()`。',
  },
  {
    name: 'getVector(vectorName)',
    type: 'MastraVector',
    description: '依名稱傳回向量儲存執行個體。',
  },
  {
    name: 'listLogs(params)',
    type: 'Promise<LogEntry[]>',
    description: '取得與所提供篩選條件相符的系統記錄。',
  },
  {
    name: 'getLog(params)',
    type: 'Promise<LogEntry>',
    description: '透過 ID 或篩選條件取得特定記錄項目。',
  },
  {
    name: 'listLogTransports()',
    type: 'string[]',
    description: '傳回已設定記錄 transport 類型的清單。',
  },
  {
    name: 'getTrace(traceId)',
    type: 'Promise<TraceRecord>',
    description: '透過 ID 取得特定 Trace，包括其所有 span 和詳細資訊。',
  },
  {
    name: 'getTraces(params)',
    type: 'Promise<GetTracesResponse>',
    description: '取得選用篩選的 Trace 根 span 分頁清單。使用 getTrace() 取得包含所有 span 的完整 Trace。',
  },
]}
/>
