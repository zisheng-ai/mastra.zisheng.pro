---
title: "參考：錯誤處理 | Client SDK"
description: 瞭解 Mastra client-js SDK 的內建重試機制和錯誤處理能力。
packages:
  - "@mastra/client-js"
---

# 錯誤處理

Mastra Client SDK 包含內建的重試機制和錯誤處理能力。

## 錯誤處理

所有 API 方法都可能擲回錯誤，你可以捕獲並處理這些錯誤：

```typescript
try {
  const agent = mastraClient.getAgent('agent-id')
  const response = await agent.generate('Hello')
} catch (error) {
  console.error('An error occurred:', error.message)
}
```
