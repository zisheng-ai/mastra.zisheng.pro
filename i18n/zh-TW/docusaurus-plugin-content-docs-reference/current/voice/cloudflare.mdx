---
title: "參考：Cloudflare | Voice"
description: "CloudflareVoice 類別的說明文件，透過 Cloudflare Workers AI 提供文字轉語音功能。"
packages:
  - "@mastra/voice-cloudflare"
---

# Cloudflare

Mastra 的 CloudflareVoice 類別透過 Cloudflare Workers AI 提供文字轉語音功能。此 Provider 專注於高效率、低延遲的語音合成，適合邊緣運算環境。

## 使用範例 {/* #usage-example */}

```typescript
import { CloudflareVoice } from '@mastra/voice-cloudflare'

// Initialize with configuration
const voice = new CloudflareVoice({
  speechModel: {
    name: '@cf/meta/m2m100-1.2b',
    apiKey: 'your-cloudflare-api-token',
    accountId: 'your-cloudflare-account-id',
  },
  speaker: 'en-US-1', // Default voice
})

// Convert text to speech
const audioStream = await voice.speak('Hello, how can I help you?', {
  speaker: 'en-US-2', // Override default voice
})

// Get available voices
const speakers = await voice.getSpeakers()
console.log(speakers)
```

## 設定 {/* #configuration */}

### 建構函式選項 {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'CloudflareSpeechConfig',
    description: '文字轉語音合成的設定。',
    isOptional: true,
    properties: [
      {
        type: 'CloudflareSpeechConfig',
        parameters: [
          {
            name: 'name',
            type: 'string',
            description: 'TTS 使用的模型名稱。',
            isOptional: true,
            defaultValue: "'@cf/meta/m2m100-1.2b'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description:
              '具備 Workers AI 存取權限的 Cloudflare API 權杖。若未提供，則使用 CLOUDFLARE_API_TOKEN 環境變數。',
            isOptional: true,
          },
          {
            name: 'accountId',
            type: 'string',
            description: 'Cloudflare 帳戶 ID。若未提供，則使用 CLOUDFLARE_ACCOUNT_ID 環境變數。',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: '語音合成的預設語音 ID。',
    isOptional: true,
    defaultValue: "'en-US-1'",
  },
]}
/>

## 方法 {/* #methods */}

### `speak()` {/* #speak */}

使用 Cloudflare 的文字轉語音服務將文字轉換為語音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要轉換為語音的文字或文字資料流。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'Options',
    description: '設定選項。',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '語音合成使用的語音 ID。',
            isOptional: true,
            defaultValue: '建構函式的 speaker 值',
          },
          {
            name: 'format',
            type: 'string',
            description: '輸出音訊格式。',
            isOptional: true,
            defaultValue: "'mp3'",
          },
        ],
      },
    ],
  },
]}
/>

回傳：`Promise<NodeJS.ReadableStream>`

### `getSpeakers()` {/* #getspeakers */}

回傳可用語音選項的陣列，其中每個節點包含：

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: "語音的唯一識別碼（例如 'en-US-1'）",
    isOptional: false,
  },
  {
    name: 'language',
    type: 'string',
    description: "語音的語言程式碼（例如 'en-US'）",
    isOptional: false,
  },
]}
/>

## 注意事項 {/* #notes */}

- API 權杖可透過建構函式選項或環境變數（CLOUDFLARE\_API\_TOKEN 與 CLOUDFLARE\_ACCOUNT\_ID）提供
- Cloudflare Workers AI 針對低延遲的邊緣運算進行最佳化
- 此 Provider 僅支援文字轉語音（TTS）功能，不支援語音轉文字（STT）
- 此服務能與其他 Cloudflare Workers 產品良好整合
- 在正式環境使用時，請確認您的 Cloudflare 帳戶具備適當的 Workers AI 訂閱方案
- 相較於其他部分 Provider，可選語音較少，但邊緣效能極佳

## 相關 Provider {/* #related-providers */}

如果除了文字轉語音之外也需要語音轉文字功能，請考慮使用下列 Provider：

- [OpenAI](./openai)：同時提供 TTS 與 STT
- [Google](./google)：同時提供 TTS 與 STT
- [Azure](./azure)：同時提供 TTS 與 STT
