---
title: "參考：Deepgram | Voice"
description: "Deepgram Voice 實作的說明文件，提供支援多種語音模型與語言的文字轉語音及語音轉文字功能。"
packages:
  - "@mastra/voice-deepgram"
---

# Deepgram

Mastra 的 Deepgram Voice 實作透過 Deepgram API 提供文字轉語音（TTS）與語音轉文字（STT）功能。它支援多種語音模型與語言，並可設定語音合成與轉錄選項。

## 使用範例

```typescript
import { DeepgramVoice } from '@mastra/voice-deepgram'

// Initialize with default configuration (uses DEEPGRAM_API_KEY environment variable)
const voice = new DeepgramVoice()

// Initialize with custom configuration
const voice = new DeepgramVoice({
  speechModel: {
    name: 'aura',
    apiKey: 'your-api-key',
  },
  listeningModel: {
    name: 'nova-2',
    apiKey: 'your-api-key',
  },
  speaker: 'asteria-en',
})

// Text-to-Speech
const audioStream = await voice.speak('Hello, world!')

// Speech-to-Text
const transcript = await voice.listen(audioStream)
```

## 建構函式參數

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'DeepgramVoiceConfig',
    description: '文字轉語音功能的設定。',
    isOptional: true,
    defaultValue: "{ name: 'aura' }",
    properties: [
      {
        type: 'DeepgramVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'DeepgramModel',
            description: '要使用的 Deepgram 模型',
            isOptional: true,
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Deepgram API 金鑰。若未提供，則使用 DEEPGRAM_API_KEY 環境變數',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'Record<string, any>',
            description: '要傳遞給 Deepgram API 的其他屬性',
            isOptional: true,
          },
          {
            name: 'language',
            type: 'string',
            description: '模型的語言程式碼',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'listeningModel',
    type: 'DeepgramVoiceConfig',
    description: '語音轉文字功能的設定。',
    isOptional: true,
    defaultValue: "{ name: 'nova' }",
    properties: [
      {
        type: 'DeepgramVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'DeepgramModel',
            description: '要使用的 Deepgram 模型',
            isOptional: true,
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Deepgram API 金鑰。若未提供，則使用 DEEPGRAM_API_KEY 環境變數',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'Record<string, any>',
            description: '要傳遞給 Deepgram API 的其他屬性',
            isOptional: true,
          },
          {
            name: 'language',
            type: 'string',
            description: '模型的語言程式碼',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'DeepgramVoiceId',
    description: '文字轉語音使用的預設語音',
    isOptional: true,
    defaultValue: "'asteria-en'",
  },
]}/>

## 方法

### `speak()`

使用已設定的語音模型與語音，將文字轉換為語音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要轉換為語音的文字。若提供資料流，會先將其轉換為文字。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '語音合成的其他選項',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '針對此請求覆寫預設語音',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}/>

回傳：`Promise<NodeJS.ReadableStream>`

### `listen()`

使用已設定的聆聽模型，將語音轉換為文字。

<PropertiesTable
  content={[
  {
    name: 'audioStream',
    type: 'NodeJS.ReadableStream',
    description: '要轉錄的音訊資料流',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '要傳遞給 Deepgram API 的其他選項',
    isOptional: true,
  },
]}/>

回傳：`Promise<string>`

### `getSpeakers()`

回傳可用的語音選項清單。

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
    isOptional: false,
  },
]}/>
