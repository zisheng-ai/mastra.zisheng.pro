---
title: "參考：PlayAI | Voice"
description: "PlayAI 語音實作的文件，提供文字轉語音功能。"
packages:
  - "@mastra/voice-playai"
---

# PlayAI

Mastra 的 PlayAI 語音實作透過 PlayAI API 提供文字轉語音功能。

## 用法範例

```typescript
import { PlayAIVoice } from '@mastra/voice-playai'

// Initialize with default configuration (uses PLAYAI_API_KEY environment variable and PLAYAI_USER_ID environment variable)
const voice = new PlayAIVoice()

// Initialize with default configuration
const voice = new PlayAIVoice({
  speechModel: {
    name: 'PlayDialog',
    apiKey: process.env.PLAYAI_API_KEY,
    userId: process.env.PLAYAI_USER_ID,
  },
  speaker: 'Angelo', // Default voice
})

// Convert text to speech with a specific voice
const audioStream = await voice.speak('Hello, world!', {
  speaker:
    's3://voice-cloning-zero-shot/b27bc13e-996f-4841-b584-4d35801aea98/original/manifest.json', // Dexter voice
})
```

## 建構函式參數

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'PlayAIConfig',
    description: '文字轉語音功能的設定',
    isOptional: true,
    defaultValue: "{ name: 'PlayDialog' }",
    properties: [
      {
        type: 'PlayAIConfig',
        parameters: [
          {
            name: 'name',
            type: "'PlayDialog' | 'Play3.0-mini'",
            description: '要使用的 PlayAI 模型',
            isOptional: true,
            defaultValue: "'PlayDialog'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'PlayAI API 金鑰。若未提供，則使用 PLAYAI_API_KEY 環境變數',
            isOptional: true,
          },
          {
            name: 'userId',
            type: 'string',
            description: 'PlayAI 使用者 ID。若未提供，則使用 PLAYAI_USER_ID 環境變數',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: '語音合成所使用的預設語音 ID',
    isOptional: true,
    defaultValue: '第一個可用的語音 ID',
  },
]}
/>

## 方法

### `speak()`

使用設定的語音模型與語音，將文字轉換成語音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要轉換成語音的文字。若提供串流，會先將其轉換成文字。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'Options',
    description: '設定選項。',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '覆寫這次請求的預設 speaker',
            isOptional: true,
            defaultValue: '建構函式的 speaker 值',
          },
        ],
      },
    ],
  },
]}
/>

回傳：`Promise<NodeJS.ReadableStream>`。

### `getSpeakers()`

回傳可用語音選項的陣列，其中每個節點包含：

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: '語音名稱',
    isOptional: false,
  },
  {
    name: 'accent',
    type: 'string',
    description: "語音的口音（例如 'US'、'British'、'Australian'）",
    isOptional: false,
  },
  {
    name: 'gender',
    type: "'M' | 'F'",
    description: '語音的性別',
    isOptional: false,
  },
  {
    name: 'age',
    type: "'Young' | 'Middle' | 'Old'",
    description: '語音的年齡類別',
    isOptional: false,
  },
  {
    name: 'style',
    type: "'Conversational' | 'Narrative'",
    description: '語音的說話風格',
    isOptional: false,
  },
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
    isOptional: false,
  },
]}
/>

### `listen()`

PlayAI 不支援此方法，呼叫時會擲回錯誤。PlayAI 不提供語音轉文字功能。

## 注意事項

- PlayAI 驗證需要 API 金鑰與使用者 ID
- 此服務提供兩種模型：'PlayDialog' 與 'Play3.0-mini'
- 每個語音都有唯一的 S3 manifest ID，發出 API 呼叫時必須使用此 ID
