---
title: "參考：voice.getSpeakers() | Voice Providers"
description: "說明語音 Provider 所提供的 getSpeakers() 方法；此方法會取得可用的語音選項。"
packages:
  - "@mastra/voice-elevenlabs"
  - "@mastra/voice-openai"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# voice.getSpeakers()

`getSpeakers()` 方法會從語音 Provider 取得可用的語音選項（speaker）清單。應用程式可以向用戶提供語音選項，或以程式方式依不同情境選擇最合適的語音。

## 使用範例

```typescript
import { OpenAIVoice } from '@mastra/voice-openai'
import { ElevenLabsVoice } from '@mastra/voice-elevenlabs'

// Initialize voice providers
const openaiVoice = new OpenAIVoice()
const elevenLabsVoice = new ElevenLabsVoice({
  apiKey: process.env.ELEVENLABS_API_KEY,
})

// Get available speakers from OpenAI
const openaiSpeakers = await openaiVoice.getSpeakers()
console.log('OpenAI voices:', openaiSpeakers)
// Example output: [{ voiceId: "alloy" }, { voiceId: "echo" }, { voiceId: "fable" }, ...]

// Get available speakers from ElevenLabs
const elevenLabsSpeakers = await elevenLabsVoice.getSpeakers()
console.log('ElevenLabs voices:', elevenLabsSpeakers)
// Example output: [{ voiceId: "21m00Tcm4TlvDq8ikWAM", name: "Rachel" }, ...]

// Use a specific voice for speech
const text = 'Hello, this is a test of different voices.'
await openaiVoice.speak(text, { speaker: openaiSpeakers[2].voiceId })
await elevenLabsVoice.speak(text, { speaker: elevenLabsSpeakers[0].voiceId })
```

## 參數

此方法不接受任何參數。

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'Promise<Array<{ voiceId: string } & TSpeakerMetadata>>',
    type: 'Promise',
    description:
      '此 Promise 會 resolve 為語音選項陣列。每個選項至少包含 voiceId 屬性，也可能包含其他 Provider 特定的中繼資料。',
  },
]}
/>

## Provider 特定的中繼資料

不同語音 Provider 會為其語音傳回不同的中繼資料：

<Tabs>
  <TabItem value="OpenAI">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: "語音的唯一識別碼（例如 'alloy'、'echo'、'fable'、'onyx'、'nova'、'shimmer'）",
  },
]}
    />
  </TabItem>

  <TabItem value="OpenAI Realtime">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: "語音的唯一識別碼（例如 'alloy'、'echo'、'fable'、'onyx'、'nova'、'shimmer'）",
  },
]}
    />
  </TabItem>

  <TabItem value="Deepgram">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'language',
    type: 'string',
    description: "嵌入語音 ID 中的語言代碼（例如 'en'）",
  },
]}
    />
  </TabItem>

  <TabItem value="ElevenLabs">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: '方便閱讀的語音名稱',
  },
  {
    name: 'category',
    type: 'string',
    description: "語音類別（例如 'premade'、'cloned'）",
  },
]}
    />
  </TabItem>

  <TabItem value="Google">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'languageCodes',
    type: 'string[]',
    description: "語音支援的語言代碼陣列（例如 ['en-US']）",
  },
]}
    />
  </TabItem>

  <TabItem value="Azure">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'language',
    type: 'string',
    description: "從語音 ID 擷取的語言代碼（例如 'en'）",
  },
  {
    name: 'region',
    type: 'string',
    description: "從語音 ID 擷取的區域代碼（例如 'US'）",
  },
]}
    />
  </TabItem>

  <TabItem value="Murf">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: '語音名稱（與 voiceId 相同）',
  },
  {
    name: 'language',
    type: 'string',
    description: "從語音 ID 擷取的語言代碼（例如 'en'）",
  },
  {
    name: 'gender',
    type: 'string',
    description: "語音的性別（目前實作一律為 'neutral'）",
  },
]}
    />
  </TabItem>

  <TabItem value="PlayAI">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼（指向 manifest.json 的 S3 URL）',
  },
  {
    name: 'name',
    type: 'string',
    description: "方便閱讀的語音名稱（例如 'Angelo'、'Arsenio'）",
  },
  {
    name: 'accent',
    type: 'string',
    description: "語音的口音（例如 'US'、'Irish'、'US African American'）",
  },
  {
    name: 'gender',
    type: 'string',
    description: "語音的性別（'M' 或 'F'）",
  },
  {
    name: 'age',
    type: 'string',
    description: "語音的年齡類別（例如 'Young'、'Middle'）",
  },
  {
    name: 'style',
    type: 'string',
    description: "語音的說話風格（例如 'Conversational'）",
  },
]}
    />
  </TabItem>

  <TabItem value="Speechify">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: '方便閱讀的語音名稱',
  },
  {
    name: 'language',
    type: 'string',
    description: "語音的語言代碼（例如 'en-US'）",
  },
]}
    />
  </TabItem>

  <TabItem value="Sarvam">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: '方便閱讀的語音名稱',
  },
  {
    name: 'language',
    type: 'string',
    description: "語音的語言（例如 'english'、'hindi'）",
  },
  {
    name: 'gender',
    type: 'string',
    description: "語音的性別（'male' 或 'female'）",
  },
]}
    />
  </TabItem>
</Tabs>

## 注意事項

- 不同 Provider 提供的語音可能有顯著差異
- 部分 Provider 可能需要驗證，才能取得完整的語音清單
- 如果 Provider 不支援此方法，預設實作會傳回空陣列
- 若需要頻繁顯示清單，基於效能考量，請考慮快取結果
- 所有 Provider 都保證提供 `voiceId` 屬性，但其他中繼資料會因 Provider 而異
