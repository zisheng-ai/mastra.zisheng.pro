---
title: "參考：ElevenLabs | Voice"
description: "ElevenLabs Voice 實作的說明文件，提供支援多種語音模型與自然語音合成的高品質文字轉語音功能。"
packages:
  - "@mastra/voice-elevenlabs"
---

# ElevenLabs

Mastra 的 ElevenLabs Voice 實作透過 ElevenLabs API 提供高品質的文字轉語音（TTS）與語音轉文字（STT）功能。

## 使用範例

```typescript
import { ElevenLabsVoice } from '@mastra/voice-elevenlabs'

// Initialize with default configuration (uses ELEVENLABS_API_KEY environment variable)
const voice = new ElevenLabsVoice()

// Initialize with custom configuration
const voice = new ElevenLabsVoice({
  speechModel: {
    name: 'eleven_multilingual_v2',
    apiKey: 'your-api-key',
  },
  speaker: 'custom-speaker-id',
})

// Text-to-Speech
const audioStream = await voice.speak('Hello, world!')

// Get available speakers
const speakers = await voice.getSpeakers()
```

## 建構函式參數

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'ElevenLabsVoiceConfig',
    description: '文字轉語音功能的設定。',
    isOptional: true,
    defaultValue: "{ name: 'eleven_multilingual_v2' }",
    properties: [
      {
        type: 'ElevenLabsVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'ElevenLabsModel',
            description: '要使用的 ElevenLabs 模型',
            isOptional: true,
            defaultValue: "'eleven_multilingual_v2'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'ElevenLabs API 金鑰。若未提供，則使用 ELEVENLABS_API_KEY 環境變數',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: '文字轉語音使用的語音 ID',
    isOptional: true,
    defaultValue: "'9BWtsMINqrJLrRacOk9x'（Aria 語音）",
  },
]}/>

## 方法

### `speak()`

使用已設定的語音模型與語音，將文字轉換為語音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要轉換為語音的文字。若提供資料流，會先將其轉換為文字。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '語音合成的其他選項',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '針對此請求覆寫預設語音 ID',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}/>

回傳：`Promise<NodeJS.ReadableStream>`

### `getSpeakers()`

回傳可用語音選項的陣列，其中每個節點包含：

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: '語音的顯示名稱',
    isOptional: false,
  },
  {
    name: 'language',
    type: 'string',
    description: '語音的語言程式碼',
    isOptional: false,
  },
  {
    name: 'gender',
    type: 'string',
    description: '語音的性別',
    isOptional: false,
  },
]}/>

### `listen()`

使用 ElevenLabs Speech-to-Text API 將輸入音訊轉換為文字。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'NodeJS.ReadableStream',
    description: '包含要轉錄之音訊資料的可讀資料流',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '轉錄的設定選項',
    isOptional: true,
  },
]}/>

options 物件支援下列屬性：

<PropertiesTable
  content={[
  {
    name: 'language_code',
    type: 'string',
    description: "ISO 語言程式碼（例如 'en'、'fr'、'es'）",
    isOptional: true,
  },
  {
    name: 'tag_audio_events',
    type: 'boolean',
    description: '是否標記 [MUSIC]、[LAUGHTER] 等音訊事件',
    isOptional: true,
  },
  {
    name: 'num_speakers',
    type: 'number',
    description: '要在音訊中偵測的說話者人數',
    isOptional: true,
  },
  {
    name: 'filetype',
    type: 'string',
    description: "音訊檔案格式（例如 'mp3'、'wav'、'ogg'）",
    isOptional: true,
  },
  {
    name: 'timeoutInSeconds',
    type: 'number',
    description: '請求逾時秒數',
    isOptional: true,
  },
  {
    name: 'maxRetries',
    type: 'number',
    description: '重試次數上限',
    isOptional: true,
  },
  {
    name: 'abortSignal',
    type: 'AbortSignal',
    description: '中止請求的訊號',
    isOptional: true,
  },
]}/>

回傳：`Promise<string>` — 解析為轉錄文字的 Promise

## 重要事項

1. 必須提供 ElevenLabs API 金鑰。可透過 `ELEVENLABS_API_KEY` 環境變數設定，或傳入建構函式。
1. 預設語音設為 Aria（ID：'9BWtsMINqrJLrRacOk9x'）。
1. ElevenLabs 不支援語音轉文字功能。
1. 可使用 `getSpeakers()` 方法取得可用語音；該方法會回傳每個語音的詳細資訊，包括語言與性別。
