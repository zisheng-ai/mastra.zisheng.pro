---
title: "參考：Murf | 語音"
description: "Murf 語音實作的文件，說明文字轉語音功能。"
packages:
  - "@mastra/voice-murf"
---

# Murf

Mastra 的 Murf 語音實作使用 Murf 的 AI 語音服務，提供文字轉語音 (TTS) 功能。它支援不同語言的多種語音。

## 使用範例

```typescript
import { MurfVoice } from '@mastra/voice-murf'

// Initialize with default configuration (uses MURF_API_KEY environment variable)
const voice = new MurfVoice()

// Initialize with custom configuration
const voice = new MurfVoice({
  speechModel: {
    name: 'GEN2',
    apiKey: 'your-api-key',
    properties: {
      format: 'MP3',
      rate: 1.0,
      pitch: 1.0,
      sampleRate: 48000,
      channelType: 'STEREO',
    },
  },
  speaker: 'en-US-cooper',
})

// Text-to-Speech with default settings
const audioStream = await voice.speak('Hello, world!')

// Text-to-Speech with custom properties
const audioStream = await voice.speak('Hello, world!', {
  speaker: 'en-UK-hazel',
  properties: {
    format: 'WAV',
    rate: 1.2,
    style: 'casual',
  },
})

// Get available voices
const voices = await voice.getSpeakers()
```

## 建構函式參數

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'MurfConfig',
    description: '文字轉語音功能的設定',
    isOptional: true,
    defaultValue: "{ name: 'GEN2' }",
    properties: [
      {
        type: 'MurfConfig',
        parameters: [
          {
            name: 'name',
            type: "'GEN1' | 'GEN2'",
            description: '要使用的 Murf 模型世代',
            isOptional: false,
            defaultValue: "'GEN2'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Murf API 金鑰。未提供時會改用 MURF_API_KEY 環境變數',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'object',
            description: '所有語音合成請求的預設屬性',
            isOptional: true,
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'style',
                    type: 'string',
                    description: '語音的說話風格',
                    isOptional: true,
                  },
                  {
                    name: 'rate',
                    type: 'number',
                    description: '說話速度倍數',
                    isOptional: true,
                  },
                  {
                    name: 'pitch',
                    type: 'number',
                    description: '語音音高調整',
                    isOptional: true,
                  },
                  {
                    name: 'sampleRate',
                    type: '8000 | 24000 | 44100 | 48000',
                    description: '音訊取樣率（單位：Hz）',
                    isOptional: true,
                  },
                  {
                    name: 'format',
                    type: "'MP3' | 'WAV' | 'FLAC' | 'ALAW' | 'ULAW'",
                    description: '輸出音訊格式',
                    isOptional: true,
                  },
                  {
                    name: 'channelType',
                    type: "'STEREO' | 'MONO'",
                    description: '音訊聲道設定',
                    isOptional: true,
                  },
                  {
                    name: 'pronunciationDictionary',
                    type: 'Record<string, string>',
                    description: '自訂發音對應',
                    isOptional: true,
                  },
                  {
                    name: 'encodeAsBase64',
                    type: 'boolean',
                    description: '是否將音訊編碼為 Base64',
                    isOptional: true,
                  },
                  {
                    name: 'variation',
                    type: 'number',
                    description: '語音變化參數',
                    isOptional: true,
                  },
                  {
                    name: 'audioDuration',
                    type: 'number',
                    description: '目標音訊長度（秒）',
                    isOptional: true,
                  },
                  {
                    name: 'multiNativeLocale',
                    type: 'string',
                    description: '多語言支援使用的語系',
                    isOptional: true,
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: '文字轉語音預設使用的語音 ID',
    isOptional: true,
    defaultValue: "'en-UK-hazel'",
  },
]}
/>

## 方法

### `speak()`

使用 Murf 的 API 將文字轉換成語音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要轉換成語音的文字。若提供串流，會先將其轉換成文字。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '語音合成選項',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '覆寫此請求的預設說話者',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'object',
            description: '覆寫此請求的預設語音屬性',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

傳回：`Promise<NodeJS.ReadableStream>`

### `getSpeakers()`

傳回可用的語音選項陣列，其中每個節點包含：

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: '語音的顯示名稱',
    isOptional: false,
  },
  {
    name: 'language',
    type: 'string',
    description: '語音的語言代碼',
    isOptional: false,
  },
  {
    name: 'gender',
    type: 'string',
    description: '語音的性別',
    isOptional: false,
  },
]}
/>

### `listen()`

Murf 不支援此方法，呼叫時會擲回錯誤。Murf 不提供語音轉文字功能。

## 重要注意事項

1. 必須提供 Murf API 金鑰。請透過 `MURF_API_KEY` 環境變數設定，或傳入建構函式。
1. 此服務預設使用 GEN2 模型版本。
1. 語音屬性可在建構函式層級設定，並可針對個別請求覆寫。
1. 此服務可透過格式、取樣率與聲道類型等屬性，提供廣泛的音訊自訂功能。
1. 不支援語音轉文字功能。
