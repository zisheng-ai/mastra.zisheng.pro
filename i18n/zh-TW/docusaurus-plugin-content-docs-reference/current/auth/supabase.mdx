---
title: "參考：MastraAuthSupabase 類別 | 身分驗證"
description: "MastraAuthSupabase 類別的 API 參考文件。此類別使用 Supabase Auth 驗證 Mastra 應用程式。"
packages:
  - "@mastra/auth-supabase"
  - "@mastra/core"
---

# MastraAuthSupabase 類別

`MastraAuthSupabase` 類別使用 Supabase Auth 為 Mastra 提供身分驗證。它會使用 Supabase 的身分驗證系統驗證傳入要求，並透過 `auth` 選項與 Mastra 伺服器整合。

## 使用範例

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthSupabase } from '@mastra/auth-supabase'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthSupabase({
      url: process.env.SUPABASE_URL,
      anonKey: process.env.SUPABASE_ANON_KEY,
    }),
  },
})
```

## 建構函式參數

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: '你的 Supabase 專案 URL。可在 Supabase 專案設定中找到。',
    isOptional: true,
    defaultValue: 'process.env.SUPABASE_URL',
  },
  {
    name: 'anonKey',
    type: 'string',
    description: '你的 Supabase 專案匿名／公開 key。用於用戶端身分驗證。',
    isOptional: true,
    defaultValue: 'process.env.SUPABASE_ANON_KEY',
  },
  {
    name: 'name',
    type: 'string',
    description: '身分驗證 Provider 執行個體的自訂名稱。',
    isOptional: true,
  },
  {
    name: 'authorizeUser',
    type: '(user: User, request: HoneRequest) => Promise<boolean> | boolean',
    description:
      "自訂授權函式，用於判斷是否應授予使用者存取權。此函式會在 token 驗證後呼叫。預設會檢查 'users' table 中的 'isAdmin' column。",
    isOptional: true,
  },
]}
/>

## 相關內容

[MastraAuthSupabase](/docs/server/auth/supabase)
