---
title: "參考：MastraJwtAuth 類別 | 身分驗證"
description: "MastraJwtAuth 類別的 API 參考文件。此類別使用 JSON Web Token 驗證 Mastra 應用程式。"
packages:
  - "@mastra/auth"
  - "@mastra/core"
---

# MastraJwtAuth 類別

`MastraJwtAuth` 類別使用 JSON Web Token（JWT），為 Mastra 提供輕量的身分驗證機制。它會根據共享 secret 驗證傳入要求，並透過 `auth` 選項與 Mastra 伺服器整合。

## 使用範例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraJwtAuth } from '@mastra/auth'

export const mastra = new Mastra({
  server: {
    auth: new MastraJwtAuth({
      secret: '<your-secret>',
    }),
  },
})
```

## 建構函式參數 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'secret',
    type: 'string',
    description: '用於簽署及驗證 JSON Web Token（JWT）的唯一字串，以便對傳入要求進行身分驗證。',
    isOptional: false,
  },
]}
/>

## 相關內容 {/* #related */}

- [JSON Web Token](/docs/server/auth/jwt)
