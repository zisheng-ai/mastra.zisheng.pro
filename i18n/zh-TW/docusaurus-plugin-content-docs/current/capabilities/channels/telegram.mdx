---
title: "Telegram | 頻道"
description: "設定 Mastra Agent，透過頻道 adapter 接收 Telegram 訊息。"
packages:
  - "@mastra/core"
---

# Telegram

Telegram 頻道可讓 Mastra Agent 接收來自 Telegram 的私訊、群組訊息與機器人提及。Mastra 會處理 Agent 串接與 webhook 路由；Chat SDK Telegram adapter 文件則涵蓋機器人設定、webhook 註冊與輪詢行為。

## 安裝 adapter

從 Chat SDK 安裝 Telegram adapter：

```bash npm2yarn
npm install @chat-adapter/telegram
```

## Agent 設定

將 `createTelegramAdapter()` 加入 Agent 的 `channels.adapters` 物件：

```typescript title="src/mastra/agents/telegram-agent.ts"
import { Agent } from '@mastra/core/agent'
import { createTelegramAdapter } from '@chat-adapter/telegram'

export const telegramAgent = new Agent({
  id: 'telegram-agent',
  name: 'Telegram Agent',
  instructions: 'Answer questions and help with tasks in Telegram.',
  model: '__GATEWAY_OPENAI_MODEL__',
  channels: {
    adapters: {
      telegram: createTelegramAdapter(),
    },
  },
})
```

在 Mastra instance 上註冊 Agent：

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { telegramAgent } from './agents/telegram-agent'

export const mastra = new Mastra({
  agents: { telegramAgent },
})
```

## Adapter 設定

請依照 [Chat SDK Telegram adapter 文件](https://chat-sdk.dev/adapters/official/telegram)完成 Telegram 專屬設定，包括 BotFather、webhook 註冊、秘密 token 與輪詢模式。

Adapter 會從下列環境變數讀取 Telegram 憑證：

```bash title=".env"
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_WEBHOOK_SECRET_TOKEN=your-webhook-secret-token
TELEGRAM_BOT_USERNAME=your_bot_username
```

## Webhook URL

Mastra 會根據 Agent ID 與 adapter key 產生 Telegram webhook 路由：

```text
/api/agents/telegram-agent/channels/telegram/webhook
```

使用公開 Mastra 伺服器 URL 作為基底 URL：

```text
https://your-app.example.com/api/agents/telegram-agent/channels/telegram/webhook
```

當 Telegram adapter 文件要求填入 webhook URL 時，請使用此 URL。Telegram 也支援輪詢模式；若想在本機開發時使用輪詢，請依照 Chat SDK adapter 文件操作。

## 相關內容

- [頻道總覽](/docs/capabilities/channels/overview)
- [更多平台](/docs/capabilities/channels/other-adapters)
