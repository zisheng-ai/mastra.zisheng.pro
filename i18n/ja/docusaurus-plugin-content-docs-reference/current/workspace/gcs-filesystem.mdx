---
title: "リファレンス：GCSFilesystem | Workspace"
description: "Google Cloud Storage 向け GCSFilesystem Provider のドキュメントです。"
packages:
  - "@mastra/gcs"
---

# GCSFilesystem {/* #gcsfilesystem */}

Google Cloud Storage にファイルを保存します。インターフェースの詳細は、[WorkspaceFilesystem インターフェース](/reference/workspace/filesystem)を参照してください。

## インストール {/* #installation */}

```bash npm2yarn
npm install @mastra/gcs
```

## 使用方法 {/* #usage */}

Workspace に `GCSFilesystem` を追加して Agent に割り当てます。

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { GCSFilesystem } from '@mastra/gcs'

const workspace = new Workspace({
  filesystem: new GCSFilesystem({
    bucket: 'my-gcs-bucket',
    projectId: 'my-project-id',
    credentials: JSON.parse(process.env.GCS_SERVICE_ACCOUNT_KEY),
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  workspace,
})
```

### Application Default Credentials を使用する {/* #using-application-default-credentials */}

Google Cloud 上で実行する場合や `gcloud` CLI が設定済みの場合は、認証情報を省略できます。

```typescript
import { GCSFilesystem } from '@mastra/gcs'

const filesystem = new GCSFilesystem({
  bucket: 'my-gcs-bucket',
  // Uses Application Default Credentials automatically
})
```

[Application Default Credentials（ADC）](https://cloud.google.com/docs/authentication/application-default-credentials)は、次の順序で認証情報を自動検出します。

1. 環境変数 `GOOGLE_APPLICATION_CREDENTIALS`（サービスアカウントキーファイルへのパス）
1. GCP（Compute Engine、Cloud Run、GKE など）で実行する場合のデフォルトサービスアカウント
1. `gcloud auth application-default login` で取得したユーザー認証情報（ローカル開発向け）

### キーファイルのパスを使用する {/* #using-a-key-file-path */}

サービスアカウントキーファイルへのパスを渡すこともできます。

```typescript
import { GCSFilesystem } from '@mastra/gcs'

const filesystem = new GCSFilesystem({
  bucket: 'my-gcs-bucket',
  projectId: 'my-project-id',
  credentials: '/path/to/service-account-key.json',
})
```

## コンストラクターパラメーター {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'bucket',
    type: 'string',
    description: 'GCS バケット名。',
  },
  {
    name: 'projectId',
    type: 'string',
    description: 'GCS プロジェクト ID。サービスアカウント認証情報を使用する場合は必須です。',
    isOptional: true,
  },
  {
    name: 'credentials',
    type: 'object | string',
    description:
      'サービスアカウントキーの JSON オブジェクトまたはキーファイルへのパス。未指定の場合は Application Default Credentials を使用します。',
    isOptional: true,
  },
  {
    name: 'prefix',
    type: 'string',
    description: 'すべてのキーに付加する任意のプレフィックス（サブディレクトリとして機能します）。',
    isOptional: true,
  },
  {
    name: 'id',
    type: 'string',
    description: 'この Filesystem インスタンスの一意な識別子。',
    isOptional: true,
    defaultValue: 'Auto-generated',
  },
  {
    name: 'displayName',
    type: 'string',
    description: 'UI に表示するわかりやすい名前。',
    isOptional: true,
  },
  {
    name: 'icon',
    type: 'FilesystemIcon',
    description: 'UI に表示するアイコンの識別子。',
    isOptional: true,
  },
  {
    name: 'description',
    type: 'string',
    description: 'UI に表示する Filesystem の短い説明。',
    isOptional: true,
  },
  {
    name: 'readOnly',
    type: 'boolean',
    description: 'true の場合、すべての書き込み操作を禁止します。',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'endpoint',
    type: 'string',
    description: 'カスタム API エンドポイント URL。エミュレーターを使用するローカル開発向けです。',
    isOptional: true,
  },
]}
/>

## プロパティ {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Filesystem インスタンスの識別子。',
  },
  {
    name: 'name',
    type: 'string',
    description: "Provider 名（'GCSFilesystem'）。",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Provider 識別子（'gcs'）。",
  },
  {
    name: 'bucket',
    type: 'string',
    description: 'GCS バケット名。',
  },
  {
    name: 'readOnly',
    type: 'boolean | undefined',
    description: 'Filesystem が読み取り専用モードかどうか。',
  },
]}
/>

## メソッド {/* #methods */}

GCSFilesystem は [WorkspaceFilesystem インターフェース](/reference/workspace/filesystem)を実装し、標準の Filesystem メソッドをすべて提供します。

- `readFile(path, options?)` - ファイルの内容を読み取ります
- `writeFile(path, content, options?)` - ファイルに内容を書き込みます
- `appendFile(path, content)` - ファイルに内容を追記します
- `deleteFile(path, options?)` - ファイルを削除します
- `copyFile(src, dest, options?)` - ファイルをコピーします
- `moveFile(src, dest, options?)` - ファイルを移動または名前変更します
- `mkdir(path, options?)` - ディレクトリを作成します
- `rmdir(path, options?)` - ディレクトリを削除します
- `readdir(path, options?)` - ディレクトリの内容を一覧表示します
- `exists(path)` - パスが存在するか確認します
- `stat(path)` - ファイルまたはディレクトリのメタデータを取得します

### `init()` {/* #init */}

Filesystem を初期化し、バケットへのアクセスと認証情報を検証します。

```typescript
await filesystem.init()
```

### `getInfo()` {/* #getinfo */}

この Filesystem インスタンスのメタデータを返します。

```typescript
const info = filesystem.getInfo()
// { id: '...', name: 'GCSFilesystem', provider: 'gcs', status: 'ready' }
```

### `getMountConfig()` {/* #getmountconfig */}

この種類の Filesystem のマウントに対応する Sandbox 向けに、マウント設定を返します。

```typescript
const config = filesystem.getMountConfig()
// { type: 'gcs', bucket: 'my-bucket', ... }
```

## E2B Sandbox にマウントする {/* #mounting-in-e2b-sandboxes */}

GCSFilesystem を E2B Sandbox にマウントすると、バケットへローカルディレクトリとしてアクセスできます。

```typescript
import { Workspace } from '@mastra/core/workspace'
import { GCSFilesystem } from '@mastra/gcs'
import { E2BSandbox } from '@mastra/e2b'

const workspace = new Workspace({
  mounts: {
    '/data': new GCSFilesystem({
      bucket: 'my-gcs-bucket',
      projectId: 'my-project-id',
      credentials: JSON.parse(process.env.GCS_SERVICE_ACCOUNT_KEY),
    }),
  },
  sandbox: new E2BSandbox({ id: 'dev-sandbox' }),
})
```

マウントの詳細は、[E2BSandbox リファレンス](/reference/workspace/e2b-sandbox)を参照してください。

## 関連項目 {/* #related */}

- [WorkspaceFilesystem インターフェース](/reference/workspace/filesystem)
- [ArchilFilesystem リファレンス](/reference/workspace/archil-filesystem)
- [S3Filesystem リファレンス](/reference/workspace/s3-filesystem)
- [AzureBlobFilesystem リファレンス](/reference/workspace/azure-blob-filesystem)
- [E2BSandbox リファレンス](/reference/workspace/e2b-sandbox)
- [Workspace の概要](/docs/workspace/overview)
