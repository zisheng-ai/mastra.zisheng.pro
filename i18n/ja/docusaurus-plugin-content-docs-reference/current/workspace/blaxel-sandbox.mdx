---
title: "リファレンス：BlaxelSandbox | Workspace"
description: "分離されたクラウドコード実行環境向け BlaxelSandbox Provider のドキュメントです。"
packages:
  - "@mastra/blaxel"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# BlaxelSandbox

分離された [Blaxel](https://blaxel.ai/) クラウド Sandbox でコマンドを実行します。FUSE によるクラウドストレージ（S3、GCS）のマウントに対応した、安全で分離されたコード実行環境を提供します。インターフェースの詳細は、[WorkspaceSandbox インターフェース](/reference/workspace/sandbox)を参照してください。

## インストール {/* #installation */}

```bash npm2yarn
npm install @mastra/blaxel
```

## 使用方法 {/* #usage */}

Workspace に `BlaxelSandbox` を追加して Agent に割り当てます。

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { BlaxelSandbox } from '@mastra/blaxel'

const workspace = new Workspace({
  sandbox: new BlaxelSandbox(),
})

const agent = new Agent({
  id: 'code-agent',
  name: 'Code Agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})
```

### リソースを指定したカスタムイメージ {/* #custom-image-with-resources */}

追加メモリを指定してカスタム Docker イメージを使用します。

```typescript
const workspace = new Workspace({
  sandbox: new BlaxelSandbox({
    image: 'node:20-slim',
    memory: 8192,
    region: 'auto',
    timeout: '10m',
  }),
})
```

## コンストラクターパラメーター {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'この Sandbox インスタンスの一意な識別子。',
    isOptional: true,
    defaultValue: 'Auto-generated',
  },
  {
    name: 'image',
    type: 'string',
    description:
      'Sandbox で使用する Docker イメージ。Debian ベースのイメージは S3 と GCS のマウント、Alpine ベースのイメージは S3 のマウントに対応します。',
    isOptional: true,
    defaultValue: "'blaxel/ts-app:latest'",
  },
  {
    name: 'memory',
    type: 'number',
    description: '割り当てるメモリ容量（MB）。',
    isOptional: true,
    defaultValue: '4096',
  },
  {
    name: 'timeout',
    type: 'string',
    description: "期間文字列で指定する実行タイムアウト（例：'5m'、'1h'）。Blaxel Sandbox の TTL に対応します。",
    isOptional: true,
  },
  {
    name: 'region',
    type: 'string',
    description:
      "Sandbox を作成する Blaxel リージョン。リージョンを自動選択するには 'auto'、明示するには 'us-pdx-1' などを指定します。",
    isOptional: true,
    defaultValue: "process.env.BL_REGION || 'auto'",
  },
  {
    name: 'env',
    type: 'Record<string, string>',
    description: 'Sandbox に設定する環境変数。',
    isOptional: true,
  },
  {
    name: 'labels',
    type: 'Record<string, string>',
    description: 'Sandbox のカスタムラベル。',
    isOptional: true,
  },
  {
    name: 'runtimes',
    type: 'SandboxRuntime[]',
    description: "対応ランタイム。有効な値：'node'、'python'、'bash'、'ruby'、'go'、'rust'、'java'、'cpp'、'r'。",
    isOptional: true,
    defaultValue: "['node', 'python', 'bash']",
  },
  {
    name: 'ports',
    type: "Array<{ name?: string; target: number; protocol?: 'HTTP' | 'TCP' | 'UDP' }>",
    description: 'Sandbox から公開するポート。',
    isOptional: true,
  },
]}
/>

## プロパティ {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Sandbox インスタンスの識別子。',
  },
  {
    name: 'name',
    type: 'string',
    description: "'BlaxelSandbox'",
  },
  {
    name: 'provider',
    type: 'string',
    description: "'blaxel'",
  },
  {
    name: 'status',
    type: 'ProviderStatus',
    description: "'pending' | 'initializing' | 'running' | 'stopped' | 'error'",
  },
  {
    name: 'instance',
    type: 'SandboxInstance',
    description:
      'Blaxel API へ直接アクセスするための基盤となる Blaxel SandboxInstance。Sandbox が起動していない場合は SandboxNotReadyError をスローします。',
  },
  {
    name: 'processes',
    type: 'BlaxelProcessManager',
    description:
      'バックグラウンドプロセスマネージャー。[SandboxProcessManager リファレンス](/reference/workspace/process-manager)を参照してください。',
  },
]}
/>

## バックグラウンドプロセス {/* #background-processes */}

`BlaxelSandbox` には、バックグラウンドプロセスを起動・管理するプロセスマネージャーが組み込まれています。

```typescript
const sandbox = new BlaxelSandbox({ id: 'dev-sandbox' })
await sandbox.start()

// Spawn a background process
const handle = await sandbox.processes.spawn('node server.js', {
  env: { PORT: '3000' },
  onStdout: data => console.log(data),
})

// Interact with the process
console.log(handle.stdout)
await handle.kill()
```

:::note
Blaxel Sandbox は stdin に対応していません。`handle.sendStdin()` を呼び出すとエラーがスローされます。
:::

完全な API は、[`SandboxProcessManager` リファレンス](/reference/workspace/process-manager)を参照してください。

## クラウドストレージのマウント {/* #mounting-cloud-storage */}

Blaxel Sandbox は S3 または GCS Filesystem をマウントし、クラウドストレージを Sandbox 内のローカルディレクトリとして利用できます。

### S3 {/* #s3 */}

```typescript
import { Workspace } from '@mastra/core/workspace'
import { S3Filesystem } from '@mastra/s3'
import { BlaxelSandbox } from '@mastra/blaxel'

const workspace = new Workspace({
  mounts: {
    '/data': new S3Filesystem({
      bucket: 'my-bucket',
      region: 'us-east-1',
      accessKeyId: process.env.AWS_ACCESS_KEY_ID,
      secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY,
    }),
  },
  sandbox: new BlaxelSandbox(),
})
```

### GCS {/* #gcs */}

```typescript
import { Workspace } from '@mastra/core/workspace'
import { GCSFilesystem } from '@mastra/gcs'
import { BlaxelSandbox } from '@mastra/blaxel'

const workspace = new Workspace({
  mounts: {
    '/data': new GCSFilesystem({
      bucket: 'my-bucket',
      credentials: JSON.parse(process.env.GCS_SERVICE_ACCOUNT_KEY!),
    }),
  },
  sandbox: new BlaxelSandbox(),
})
```
