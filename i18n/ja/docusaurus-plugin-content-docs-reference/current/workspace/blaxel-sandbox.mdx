---
title: "リファレンス：BlaxelSandbox | Workspace"
description: "分離されたクラウドコード実行環境向け BlaxelSandbox Provider のドキュメントです。"
packages:
  - "@mastra/blaxel"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# BlaxelSandbox

分離された [Blaxel](https://blaxel.ai/) クラウド Sandbox でコマンドを実行します。FUSE によるクラウドストレージ（S3、GCS）のマウントに対応した、安全で分離されたコード実行環境を提供します。インターフェースの詳細は、[WorkspaceSandbox インターフェース](/reference/workspace/sandbox)を参照してください。

## インストール

```bash npm2yarn
npm install @mastra/blaxel
```

## 使用方法

Workspace に `BlaxelSandbox` を追加して Agent に割り当てます。

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { BlaxelSandbox } from '@mastra/blaxel'

const workspace = new Workspace({
  sandbox: new BlaxelSandbox(),
})

const agent = new Agent({
  id: 'code-agent',
  name: 'Code Agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})
```

### Custom image with resources

Use a custom Docker image with additional memory:

```typescript
const workspace = new Workspace({
  sandbox: new BlaxelSandbox({
    image: 'node:20-slim',
    memory: 8192,
    region: 'auto',
    timeout: '10m',
  }),
})
```

## コンストラクターパラメーター

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'この Sandbox インスタンスの一意な識別子。',
    isOptional: true,
    defaultValue: 'Auto-generated',
  },
  {
    name: 'image',
    type: 'string',
    description:
      'Docker image to use for the sandbox. Debian-based images support both S3 and GCS mounts. Alpine-based images support S3 mounts only.',
    isOptional: true,
    defaultValue: "'blaxel/ts-app:latest'",
  },
  {
    name: 'memory',
    type: 'number',
    description: '割り当てるメモリ容量（MB）。',
    isOptional: true,
    defaultValue: '4096',
  },
  {
    name: 'timeout',
    type: 'string',
    description: "Execution timeout as a duration string (e.g. '5m', '1h'). Maps to the Blaxel sandbox TTL.",
    isOptional: true,
  },
  {
    name: 'region',
    type: 'string',
    description:
      "Blaxel region where the sandbox should be created. Use 'auto' to choose a region automatically, or set a concrete region like 'us-pdx-1'.",
    isOptional: true,
    defaultValue: "process.env.BL_REGION || 'auto'",
  },
  {
    name: 'env',
    type: 'Record<string, string>',
    description: 'Sandbox に設定する環境変数。',
    isOptional: true,
  },
  {
    name: 'labels',
    type: 'Record<string, string>',
    description: 'Sandbox のカスタムラベル。',
    isOptional: true,
  },
  {
    name: 'runtimes',
    type: 'SandboxRuntime[]',
    description:
      "Supported runtimes. Valid values: 'node', 'python', 'bash', 'ruby', 'go', 'rust', 'java', 'cpp', 'r'.",
    isOptional: true,
    defaultValue: "['node', 'python', 'bash']",
  },
  {
    name: 'ports',
    type: "Array<{ name?: string; target: number; protocol?: 'HTTP' | 'TCP' | 'UDP' }>",
    description: 'Sandbox から公開するポート。',
    isOptional: true,
  },
]}
/>

## プロパティ

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Sandbox インスタンスの識別子。',
  },
  {
    name: 'name',
    type: 'string',
    description: "'BlaxelSandbox'",
  },
  {
    name: 'provider',
    type: 'string',
    description: "'blaxel'",
  },
  {
    name: 'status',
    type: 'ProviderStatus',
    description: "'pending' | 'initializing' | 'running' | 'stopped' | 'error'",
  },
  {
    name: 'instance',
    type: 'SandboxInstance',
    description:
      'The underlying Blaxel SandboxInstance for direct access to Blaxel APIs. Throws SandboxNotReadyError if the sandbox has not been started.',
  },
  {
    name: 'processes',
    type: 'BlaxelProcessManager',
    description:
      'Background process manager. See [SandboxProcessManager reference](/reference/workspace/process-manager).',
  },
]}
/>

## バックグラウンドプロセス

`BlaxelSandbox` includes a built-in process manager for spawning and managing background processes.

```typescript
const sandbox = new BlaxelSandbox({ id: 'dev-sandbox' })
await sandbox.start()

// Spawn a background process
const handle = await sandbox.processes.spawn('node server.js', {
  env: { PORT: '3000' },
  onStdout: data => console.log(data),
})

// Interact with the process
console.log(handle.stdout)
await handle.kill()
```

:::note
Blaxel sandboxes don't support stdin. Calling `handle.sendStdin()` throws an error.
:::

See [`SandboxProcessManager` reference](/reference/workspace/process-manager) for the full API.

## クラウドストレージのマウント

Blaxel sandboxes can mount S3 or GCS filesystems, making cloud storage accessible as a local directory inside the sandbox.

### S3

```typescript
import { Workspace } from '@mastra/core/workspace'
import { S3Filesystem } from '@mastra/s3'
import { BlaxelSandbox } from '@mastra/blaxel'

const workspace = new Workspace({
  mounts: {
    '/data': new S3Filesystem({
      bucket: 'my-bucket',
      region: 'us-east-1',
      accessKeyId: process.env.AWS_ACCESS_KEY_ID,
      secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY,
    }),
  },
  sandbox: new BlaxelSandbox(),
})
```

### GCS

```typescript
import { Workspace } from '@mastra/core/workspace'
import { GCSFilesystem } from '@mastra/gcs'
import { BlaxelSandbox } from '@mastra/blaxel'

const workspace = new Workspace({
  mounts: {
    '/data': new GCSFilesystem({
      bucket: 'my-bucket',
      credentials: JSON.parse(process.env.GCS_SERVICE_ACCOUNT_KEY!),
    }),
  },
  sandbox: new BlaxelSandbox(),
})
```
