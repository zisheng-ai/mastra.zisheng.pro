---
title: "リファレンス：ArchilFilesystem | Workspace"
description: "Archil の伸縮可能なサーバーレス Filesystem 向け ArchilFilesystem Provider の API リファレンスです。"
packages:
  - "@mastra/archil"
---

# ArchilFilesystem {/* #archilfilesystem */}

[Archil](https://docs.archil.com) の伸縮可能なサーバーレスディスクにファイルを保存します。高速な読み書きに対応する S3 互換オブジェクト API、POSIX シェル操作用の `exec()`、サーバー側並列検索用の `grep()` を組み合わせています。インターフェースの詳細は、[WorkspaceFilesystem インターフェース](/reference/workspace/filesystem)を参照してください。

## インストール {/* #installation */}

```bash npm2yarn
npm install @mastra/archil
```

## 使用方法 {/* #usage */}

Workspace に `ArchilFilesystem` を追加して Agent に割り当てます。

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { ArchilFilesystem } from '@mastra/archil'

const workspace = new Workspace({
  filesystem: new ArchilFilesystem({
    diskId: 'dsk-0123456789abcdef',
    apiKey: process.env.ARCHIL_API_KEY,
    region: 'aws-us-east-1',
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  workspace,
})
```

### 初期化時に新しいディスクを作成する {/* #creating-a-new-disk-on-init */}

既存のディスクがない場合は、`createDiskOptions` を指定すると自動的に作成できます。

```typescript
import { ArchilFilesystem } from '@mastra/archil'

const filesystem = new ArchilFilesystem({
  apiKey: process.env.ARCHIL_API_KEY,
  region: 'aws-us-east-1',
  createDiskOptions: {
    name: 'my-agent-workspace',
  },
})

await filesystem.init()
// filesystem.diskId now contains the newly created disk ID
```

### 環境変数へのフォールバック {/* #environment-variable-fallbacks */}

`apiKey` と `region` を省略すると、Provider は環境変数から値を読み取ります。

- `ARCHIL_API_KEY` - API キー
- `ARCHIL_REGION` - リージョン（例：`aws-us-east-1`）
- `ARCHIL_S3_BASE_URL` - カスタム S3 エンドポイント（任意）

```typescript
import { ArchilFilesystem } from '@mastra/archil'

// Uses ARCHIL_API_KEY and ARCHIL_REGION from environment
const filesystem = new ArchilFilesystem({
  diskId: 'dsk-0123456789abcdef',
})
```

## コンストラクターパラメーター {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'diskId',
    type: 'string',
    description:
      '既存の Archil ディスク ID（例："dsk-0123456789abcdef"）。`createDiskOptions` とは同時に指定できません。',
    isOptional: true,
  },
  {
    name: 'createDiskOptions',
    type: 'CreateDiskRequest',
    description: '初期化時に新しいディスクを作成するためのオプション。`diskId` とは同時に指定できません。',
    isOptional: true,
  },
  {
    name: 'apiKey',
    type: 'string',
    description: 'Archil API キー。未指定の場合は環境変数 `ARCHIL_API_KEY` を使用します。',
    isOptional: true,
  },
  {
    name: 'region',
    type: 'string',
    description: 'Archil リージョン（例："aws-us-east-1"）。未指定の場合は環境変数 `ARCHIL_REGION` を使用します。',
    isOptional: true,
  },
  {
    name: 'baseUrl',
    type: 'string',
    description: 'カスタム Archil コントロールプレーン URL（テストまたはセルフホスト環境向け）。',
    isOptional: true,
  },
  {
    name: 's3BaseUrl',
    type: 'string',
    description: 'カスタム S3 互換 API URL。未指定の場合は環境変数 `ARCHIL_S3_BASE_URL` を使用します。',
    isOptional: true,
  },
  {
    name: 'id',
    type: 'string',
    description: 'この Filesystem インスタンスの一意な識別子。',
    isOptional: true,
    defaultValue: 'Auto-generated',
  },
  {
    name: 'displayName',
    type: 'string',
    description: 'UI に表示するわかりやすい名前。',
    isOptional: true,
  },
  {
    name: 'icon',
    type: 'FilesystemIcon',
    description: 'UI に表示するアイコンの識別子。',
    isOptional: true,
  },
  {
    name: 'description',
    type: 'string',
    description: 'UI に表示する Filesystem の短い説明。',
    isOptional: true,
  },
  {
    name: 'readOnly',
    type: 'boolean',
    description: 'true の場合、すべての書き込み操作を禁止します。',
    isOptional: true,
    defaultValue: 'false',
  },
]}
/>

## プロパティ {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Filesystem インスタンスの識別子。',
  },
  {
    name: 'name',
    type: 'string',
    description: "Provider 名（'ArchilFilesystem'）。",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Provider 識別子（'archil'）。",
  },
  {
    name: 'diskId',
    type: 'string | undefined',
    description: 'Archil ディスク ID（初期化後に使用可能）。',
  },
  {
    name: 'readOnly',
    type: 'boolean | undefined',
    description: 'Filesystem が読み取り専用モードかどうか。',
  },
]}
/>

## メソッド {/* #methods */}

ArchilFilesystem は [WorkspaceFilesystem インターフェース](/reference/workspace/filesystem)を実装し、標準の Filesystem メソッドをすべて提供します。

- `readFile(path, options?)` - ファイルの内容を読み取ります
- `writeFile(path, content, options?)` - ファイルに内容を書き込みます
- `appendFile(path, content)` - ファイルに内容を追記します
- `deleteFile(path, options?)` - ファイルを削除します
- `copyFile(src, dest, options?)` - ファイルをコピーします
- `moveFile(src, dest, options?)` - ファイルを移動または名前変更します
- `mkdir(path, options?)` - ディレクトリを作成します
- `rmdir(path, options?)` - ディレクトリを削除します
- `readdir(path, options?)` - ディレクトリの内容を一覧表示します
- `exists(path)` - パスが存在するか確認します
- `stat(path)` - ファイルまたはディレクトリのメタデータを取得します

### `init()` {/* #init */}

Filesystem を初期化します。既存のディスクに接続するか、新しいディスクを作成します。

```typescript
await filesystem.init()
```

### `getInfo()` {/* #getinfo */}

この Filesystem インスタンスのメタデータを返します。

```typescript
const info = filesystem.getInfo()
// { id: '...', name: 'ArchilFilesystem', provider: 'archil', status: 'ready' }
```

### Archil 固有のメソッド {/* #archil-specific-methods */}

#### `exec(command)` {/* #execcommand */}

ディスク上でシェルコマンドを実行し、終了コード、stdout、stderr を返します。

```typescript
const result = await filesystem.exec('ls -la /data')
// { exitCode: 0, stdout: '...', stderr: '' }
```

#### `grep(options)` {/* #grepoptions */}

ディスク上のファイルを対象に、サーバー側で並列検索を実行します。

```typescript
const results = await filesystem.grep({
  directory: '/logs',
  pattern: 'ERROR',
  recursive: true,
  glob: '*.log',
})
// { matches: [...], stoppedReason: null }
```

#### `share(path, options?)` {/* #sharepath-options */}

ファイル共有用の署名付き URL を生成します。

```typescript
const { url } = await filesystem.share('/reports/output.pdf', {
  expiresIn: 3600,
})
```

## 関連項目 {/* #related */}

- [WorkspaceFilesystem インターフェース](/reference/workspace/filesystem)
- [S3Filesystem リファレンス](/reference/workspace/s3-filesystem)
- [GCSFilesystem リファレンス](/reference/workspace/gcs-filesystem)
- [AzureBlobFilesystem リファレンス](/reference/workspace/azure-blob-filesystem)
- [Archil ドキュメント](https://docs.archil.com)
