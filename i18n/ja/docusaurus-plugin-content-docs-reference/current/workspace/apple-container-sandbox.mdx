---
title: "リファレンス：AppleContainerSandbox | Workspace"
description: "Workspace のコマンド実行に使用するローカル Apple container CLI Sandbox、AppleContainerSandbox の API リファレンスです。"
packages:
  - "@mastra/apple-container"
---

# AppleContainerSandbox

Apple の [`container`](https://github.com/apple/container) CLI を介して、ローカル OCI Linux コンテナ内でコマンドを実行します。Provider は長時間稼働するコンテナを起動し、Workspace のコマンドに `container exec` を使用します。インターフェースの詳細は、[WorkspaceSandbox インターフェース](/reference/workspace/sandbox)を参照してください。

## インストール

```bash npm2yarn
npm install @mastra/apple-container
```

Apple シリコン搭載 Mac、macOS 26 以降、Apple の `container` CLI が必要です。Provider を使用する前にコンテナシステムを起動してください。

```bash
container system start
```

## 使用方法

Workspace に `AppleContainerSandbox` を追加して Agent に割り当てます。

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { AppleContainerSandbox } from '@mastra/apple-container'

const workspace = new Workspace({
  sandbox: new AppleContainerSandbox({
    image: 'node:22-slim',
    volumes: {
      '/Users/me/project': '/workspace',
    },
    workingDir: '/workspace',
  }),
})

const agent = new Agent({
  id: 'dev-agent',
  name: 'Dev Agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})

const response = await agent.generate('Run `node --version`.')
console.log(response.text)
```

## コンストラクターパラメーター

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'この Sandbox インスタンスの一意な識別子。',
    isOptional: true,
    defaultValue: 'Auto-generated',
  },
  {
    name: 'name',
    type: 'string',
    description:
      '`container run --name` に渡す Apple コンテナ名。`[a-zA-Z0-9_.-]` 以外の文字は `-` に置換され、先頭が英数字でない場合は接頭辞が付加されます。',
    isOptional: true,
    defaultValue: 'The sandbox `id`',
  },
  {
    name: 'image',
    type: 'string',
    description: 'コンテナで使用する OCI イメージ。',
    isOptional: true,
    defaultValue: "'node:22-slim'",
  },
  {
    name: 'command',
    type: 'string[]',
    description:
      'コンテナの初期化コマンド。exec ベースでコマンドを実行できるよう、コンテナを稼働状態に保つ必要があります。',
    isOptional: true,
    defaultValue: "['sleep', 'infinity']",
  },
  {
    name: 'env',
    type: 'Record<string, string>',
    description: 'コンテナ内およびコマンド実行時に設定する環境変数。',
    isOptional: true,
  },
  {
    name: 'volumes',
    type: 'Record<string, string>',
    description: 'ホストからコンテナへのバインドマウント。キーはホストパス、値はコンテナパスです。',
    isOptional: true,
  },
  {
    name: 'mounts',
    type: 'string[]',
    description: '`container run --mount` の未加工の指定。',
    isOptional: true,
  },
  {
    name: 'network',
    type: 'string',
    description: 'Apple コンテナのネットワーク接続指定。',
    isOptional: true,
  },
  {
    name: 'publishedPorts',
    type: 'string[]',
    description: '`--publish` として渡すポート公開指定。',
    isOptional: true,
  },
  {
    name: 'publishedSockets',
    type: 'string[]',
    description: '`--publish-socket` として渡すソケット公開指定。',
    isOptional: true,
  },
  {
    name: 'cpus',
    type: 'number | string',
    description: '割り当てる CPU 数。',
    isOptional: true,
  },
  {
    name: 'memory',
    type: 'string',
    description: "メモリ割り当て（例：'1G'）。",
    isOptional: true,
  },
  {
    name: 'platform',
    type: 'string',
    description: "OCI プラットフォーム（例：'linux/arm64'）。",
    isOptional: true,
  },
  {
    name: 'arch',
    type: 'string',
    description: 'マルチアーキテクチャイメージを選択する際のイメージアーキテクチャ。',
    isOptional: true,
  },
  {
    name: 'os',
    type: 'string',
    description: 'マルチプラットフォームイメージを選択する際のイメージ OS。',
    isOptional: true,
  },
  {
    name: 'rosetta',
    type: 'boolean',
    description: 'コンテナ内で Rosetta を有効にするかどうか。',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'readonlyRootfs',
    type: 'boolean',
    description: 'コンテナのルート Filesystem を読み取り専用でマウントするかどうか。',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'ssh',
    type: 'boolean',
    description: 'ホストの SSH Agent ソケットを転送するかどうか。',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'init',
    type: 'boolean',
    description: 'コンテナ内で Apple の init プロセスを有効にするかどうか。',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'virtualization',
    type: 'boolean',
    description: 'コンテナに仮想化機能を公開するかどうか。',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'capAdd',
    type: 'string[]',
    description: '追加する Linux capability。',
    isOptional: true,
  },
  {
    name: 'capDrop',
    type: 'string[]',
    description: '削除する Linux capability。',
    isOptional: true,
  },
  {
    name: 'tmpfs',
    type: 'string[]',
    description: '`--tmpfs` として渡す tmpfs の宛先パス（例：`/tmp`）。',
    isOptional: true,
  },
  {
    name: 'dns',
    type: 'string[]',
    description: 'DNS ネームサーバーの IP。',
    isOptional: true,
  },
  {
    name: 'dnsSearch',
    type: 'string[]',
    description: 'DNS 検索ドメイン。',
    isOptional: true,
  },
  {
    name: 'noDns',
    type: 'boolean',
    description: 'コンテナ内で DNS を設定しないかどうか。',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'labels',
    type: 'Record<string, string>',
    description: '追加のコンテナラベル。Mastra ラベル（mastra.sandbox、mastra.sandbox.id）は常に含まれます。',
    isOptional: true,
  },
  {
    name: 'workingDir',
    type: 'string',
    description: 'コンテナ内の作業ディレクトリ。',
    isOptional: true,
    defaultValue: "'/workspace'",
  },
  {
    name: 'timeout',
    type: 'number',
    description: 'デフォルトのコマンドタイムアウト（ミリ秒）。',
    isOptional: true,
    defaultValue: '300000 (5 minutes)',
  },
  {
    name: 'deleteOnDestroy',
    type: 'boolean',
    description: 'Sandbox の破棄時に Apple コンテナを削除するかどうか。false の場合、代わりにコンテナを停止します。',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'containerBinary',
    type: 'string',
    description: 'Apple container CLI のパスまたは名前。',
    isOptional: true,
    defaultValue: "'container'",
  },
  {
    name: 'instructions',
    type: 'string | function',
    description:
      'getInstructions() が返すデフォルトの指示を上書きするカスタム指示。指示を非表示にするには空文字列を渡します。',
    isOptional: true,
  },
]}
/>

## プロパティ

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Sandbox インスタンスの識別子。',
  },
  {
    name: 'name',
    type: 'string',
    description: "Provider 名（'AppleContainerSandbox'）。",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Provider 識別子（'apple-container'）。",
  },
  {
    name: 'status',
    type: 'ProviderStatus',
    description: "'pending' | 'starting' | 'running' | 'stopping' | 'stopped' | 'destroying' | 'destroyed' | 'error'",
  },
  {
    name: 'containerId',
    type: 'string',
    description: '判明している場合は Apple コンテナ ID、それ以外は設定されたコンテナ名。',
  },
]}
/>

## 環境変数

コンテナレベルの環境変数は `env` で設定します。コマンドごとの環境変数は `executeCommand` のオプションでも渡せます。

```typescript
const sandbox = new AppleContainerSandbox({
  image: 'node:22-slim',
  env: {
    NODE_ENV: 'development',
  },
})

await sandbox.executeCommand('node', ['-e', 'console.log(process.env.TASK_ID)'], {
  env: { TASK_ID: '42' },
})
```

## バインドマウント

`volumes` オプションを使用して、ホストのディレクトリをコンテナにマウントします。

```typescript
const sandbox = new AppleContainerSandbox({
  image: 'node:22-slim',
  volumes: {
    '/Users/me/project': '/workspace/project',
    '/Users/me/.npm': '/root/.npm',
  },
})
```

バインドマウントはコンテナ作成時に適用されます。ホストパスは Sandbox の起動前に存在している必要があります。

## リソースとプラットフォームのオプション

Apple container CLI のオプションはコンストラクターから渡せます。

```typescript
const sandbox = new AppleContainerSandbox({
  image: 'node:22-slim',
  volumes: {
    '/Users/me/project': '/workspace',
  },
  cpus: 2,
  memory: '2G',
  platform: 'linux/arm64',
  readonlyRootfs: true,
  tmpfs: ['/tmp'],
})
```

これらのオプションは、新しいコンテナの作成時にのみ適用されます。Sandbox が同名の既存コンテナへ再接続する場合、変更したランタイムオプションを適用するには Sandbox を破棄して再作成してください。
Apple の `--tmpfs` は `/tmp` などのコンテナパスのみを受け付け、`/tmp:rw,size=256m` のような Docker 形式のオプション指定には対応していません。
`readonlyRootfs` を有効にする場合、`workingDir` がイメージまたはバインドマウントで提供されるパスを指すようにしてください。書き込み可能な tmpfs も使用できます。

## セキュリティモデル

`AppleContainerSandbox` は、ホストの Apple `container` サービスを介してローカルコンテナを実行します。コンストラクターオプションは、信頼できるサーバー側設定として扱ってください。

- `volumes`、`mounts`、`publishedSockets` は、コンテナ内のコードにホストパスを公開する可能性があります。
- `publishedPorts` は、コンテナ内のサービスをホストまたはネットワークに公開する可能性があります。ローカルアクセスのみを想定する場合は `127.0.0.1` にバインドしてください。
- `ssh` はホストの SSH Agent ソケットを転送します。
- `capAdd` と `virtualization` は、コンテナ内のコードが実行できる操作を拡大する可能性があります。
- `containerBinary` は信頼できるコード向けのコンストラクター専用エスケープハッチであり、シリアライズ可能な Editor Provider スキーマには含まれません。

ワークロードに必要な最小限のマウントと capability を使用してください。既存コンテナに再接続できるのは、その Sandbox ID に対応する Mastra の所有権ラベルが付いている場合だけです。この Provider が作成したコンテナには設定ハッシュラベルも含まれます。このラベルがある場合、イメージ、コマンド、マウント、ポート、capability、作業ディレクトリなどの変更不可のランタイムオプションが変わっていると、再接続は失敗します。

## 制限事項

`AppleContainerSandbox` は `executeCommand()` によるフォアグラウンドの Workspace コマンド実行に対応しています。バックグラウンドプロセスや LSP セッション用の `SandboxProcessManager` はまだ公開していません。

コマンドのタイムアウトはコンテナ内で適用されるため、タイムアウトしたコマンドはコンテナランタイムによってクリーンアップされます。中断シグナルはホスト CLI の待機処理を取り消すだけです。コンテナ内のクリーンアップが重要な場合、コマンドタイムアウトの代わりに使用しないでください。

## 再接続

`AppleContainerSandbox` は、設定された名前のコンテナを調べて再接続します。`start()` の呼び出し時には、次のように動作します。

- 実行中のコンテナは再利用されます。
- 停止中のコンテナは再起動されます。
- コンテナが存在しない場合は、設定されたイメージから作成されます。
- 設定された名前のコンテナに対応する Mastra の所有権ラベルがない場合は、管理せずに失敗します。
- Mastra が所有するコンテナでも、設定ハッシュラベルが変更不可のランタイムオプションと一致しない場合は、再利用せずに失敗します。

```typescript
const sandbox = new AppleContainerSandbox({ id: 'persistent-sandbox' })
await sandbox.start()

const sandbox2 = new AppleContainerSandbox({ id: 'persistent-sandbox' })
await sandbox2.start()
```

## Editor Provider への登録

保存された Sandbox 設定を復元できるよう、Provider を `MastraEditor` に登録します。

```typescript
import { MastraEditor } from '@mastra/editor'
import { appleContainerSandboxProvider } from '@mastra/apple-container'

const editor = new MastraEditor({
  sandboxes: {
    [appleContainerSandboxProvider.id]: appleContainerSandboxProvider,
  },
})
```

## 関連項目

- [WorkspaceSandbox インターフェース](/reference/workspace/sandbox)
- [DockerSandbox リファレンス](/reference/workspace/docker-sandbox)
- [LocalSandbox リファレンス](/reference/workspace/local-sandbox)
- [Workspace の概要](/docs/workspace/overview)
