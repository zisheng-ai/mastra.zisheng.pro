---
title: "リファレンス：AzureBlobFilesystem | Workspace"
description: "Azure Blob Storage 向け AzureBlobFilesystem Provider のドキュメントです。"
packages:
  - "@mastra/azure"
---

# AzureBlobFilesystem {/* #azureblobfilesystem */}

Azure Blob Storage コンテナにファイルを保存します。インターフェースの詳細は、[WorkspaceFilesystem インターフェース](/reference/workspace/filesystem)を参照してください。

## インストール {/* #installation */}

```bash npm2yarn
npm install @mastra/azure
```

## 使用方法 {/* #usage */}

Workspace に `AzureBlobFilesystem` を追加して Agent に割り当てます。

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const workspace = new Workspace({
  filesystem: new AzureBlobFilesystem({
    container: 'my-container',
    connectionString: process.env.AZURE_STORAGE_CONNECTION_STRING,
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  workspace,
})
```

### アカウントキー {/* #account-key */}

完全な接続文字列を渡さない場合は、アカウント名とアカウントキーを使用します。

```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  accountKey: process.env.AZURE_STORAGE_ACCOUNT_KEY,
})
```

### Shared Access Signature トークン {/* #shared-access-signature-token */}

Shared Access Signature（SAS）トークンは、`accountName` または `endpoint` と組み合わせて使用します。

```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  sasToken: process.env.AZURE_STORAGE_SAS_TOKEN,
})
```

### DefaultAzureCredential {/* #defaultazurecredential */}

環境から Azure ID の認証情報を取得できる場合は、`DefaultAzureCredential` を使用します。

```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  useDefaultCredential: true,
})
```

`DefaultAzureCredential` を使用する場合は、`@azure/identity` をインストールします。

```bash npm2yarn
npm install @azure/identity
```

## コンストラクターパラメーター {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'container',
    type: 'string',
    description: 'Azure Blob Storage のコンテナ名。',
  },
  {
    name: 'connectionString',
    type: 'string',
    description: '完全な Azure Storage 接続文字列。他の認証オプションより優先されます。',
    isOptional: true,
  },
  {
    name: 'accountName',
    type: 'string',
    description: 'Azure Storage アカウント名。connectionString または endpoint を使用しない場合は必須です。',
    isOptional: true,
  },
  {
    name: 'accountKey',
    type: 'string',
    description: 'Azure Storage アカウントキー。',
    isOptional: true,
  },
  {
    name: 'sasToken',
    type: 'string',
    description: 'Shared Access Signature トークン。accountName または endpoint が必要です。',
    isOptional: true,
  },
  {
    name: 'useDefaultCredential',
    type: 'boolean',
    description: '@azure/identity の DefaultAzureCredential を使用するかどうか。',
    isOptional: true,
    defaultValue: 'false',
  },
  {
    name: 'endpoint',
    type: 'string',
    description: 'カスタム Blob サービスのエンドポイント URL。Azurite を使ったローカル開発に使用します。',
    isOptional: true,
  },
  {
    name: 'prefix',
    type: 'string',
    description: 'すべてのキーに付加する任意のプレフィックス（サブディレクトリとして機能します）。',
    isOptional: true,
  },
  {
    name: 'id',
    type: 'string',
    description: 'この Filesystem インスタンスの一意な識別子。',
    isOptional: true,
    defaultValue: 'Auto-generated',
  },
  {
    name: 'displayName',
    type: 'string',
    description: 'UI に表示するわかりやすい名前。',
    isOptional: true,
  },
  {
    name: 'icon',
    type: 'FilesystemIcon',
    description: 'UI に表示するアイコンの識別子。',
    isOptional: true,
  },
  {
    name: 'description',
    type: 'string',
    description: 'UI に表示する Filesystem の短い説明。',
    isOptional: true,
  },
  {
    name: 'readOnly',
    type: 'boolean',
    description: 'true の場合、すべての書き込み操作を禁止します。',
    isOptional: true,
    defaultValue: 'false',
  },
]}
/>

## プロパティ {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Filesystem インスタンスの識別子。',
  },
  {
    name: 'name',
    type: 'string',
    description: "Provider 名（'AzureBlobFilesystem'）。",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Provider 識別子（'azure-blob'）。",
  },
  {
    name: 'readOnly',
    type: 'boolean | undefined',
    description: 'Filesystem が読み取り専用モードかどうか。',
  },
]}
/>

## メソッド {/* #methods */}

AzureBlobFilesystem は [WorkspaceFilesystem インターフェース](/reference/workspace/filesystem)を実装し、標準の Filesystem メソッドをすべて提供します。

- `readFile(path, options?)` - ファイルの内容を読み取ります
- `writeFile(path, content, options?)` - ファイルに内容を書き込みます
- `appendFile(path, content)` - ファイルに内容を追記します
- `deleteFile(path, options?)` - ファイルを削除します
- `copyFile(src, dest, options?)` - ファイルをコピーします
- `moveFile(src, dest, options?)` - ファイルを移動または名前変更します
- `mkdir(path, options?)` - ディレクトリを作成します
- `rmdir(path, options?)` - ディレクトリを削除します
- `readdir(path, options?)` - ディレクトリの内容を一覧表示します
- `exists(path)` - パスが存在するか確認します
- `stat(path)` - ファイルまたはディレクトリのメタデータを取得します

### `init()` {/* #init */}

Filesystem を初期化し、コンテナへのアクセスと認証情報を検証します。

```typescript
await filesystem.init()
```

### `getInfo()` {/* #getinfo */}

この Filesystem インスタンスのメタデータを返します。

```typescript
const info = filesystem.getInfo()
// { id: '...', name: 'AzureBlobFilesystem', provider: 'azure-blob', status: 'ready' }
```

### `getContainer()` {/* #getcontainer */}

基盤となる Azure Blob Storage の `ContainerClient` を返します。

```typescript
const container = await filesystem.getContainer()
```

### `getMountConfig()` {/* #getmountconfig */}

Azure Blob Filesystem のマウントに対応する Sandbox Provider 向けのマウント設定を返します。

```typescript
const config = filesystem.getMountConfig()
// { type: 'azure-blob', container: 'my-container', ... }
```

:::note

Azure Blob を Sandbox にマウントするには、Sandbox が `azure-blob` マウント設定に対応している必要があります。Sandbox Provider が Azure Blob のマウントに対応していない場合、Workspace からファイルを直接操作するには `filesystem` を使用してください。

:::

## 関連項目 {/* #related */}

- [WorkspaceFilesystem インターフェース](/reference/workspace/filesystem)
- [S3Filesystem リファレンス](/reference/workspace/s3-filesystem)
- [GCSFilesystem リファレンス](/reference/workspace/gcs-filesystem)
- [Workspace の概要](/docs/workspace/overview)
