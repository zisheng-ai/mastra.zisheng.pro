---
title: "リファレンス: FilesystemProvider | Editor"
description: "ファイルシステム実装を MastraEditor に登録するインターフェース、FilesystemProvider のリファレンス。"
packages:
  - "@mastra/core"
---

# FilesystemProvider

`FilesystemProvider` は、パッケージが [`MastraEditor`](/reference/editor/mastra-editor) にファイルシステムを登録するために実装するインターフェースです。Editor は Workspace のハイドレーション時に `createFilesystem(config)` を呼び出します。その際、`provider` ID を検索キーとして保存済みの [`StorageWorkspaceRef`](/reference/editor/storage-workspace-ref) のファイルシステム構成から取得します。

組み込みの `local` Filesystem Provider は自動的に登録されます。外部 Provider（S3 や GCS など）は `MastraEditorConfig.filesystems` を介して指定します。

## 使用例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'
import { s3FilesystemProvider } from '@mastra/s3'

new MastraEditor({
  filesystems: {
    [s3FilesystemProvider.id]: s3FilesystemProvider,
  },
})
```

## プロパティ {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description:
      '一意の Provider 識別子（例: `"local"`、`"s3"`）。この Provider を使用するすべての保存済み Workspace で、`StorageFilesystemConfig.provider` と一致する必要があります。',
  },
  {
    name: 'name',
    type: 'string',
    description: 'UI に表示する、人が読める名前。',
  },
  {
    name: 'description',
    type: 'string',
    description: 'Workspace のファイルシステム選択欄に表示される簡潔な説明。',
    isOptional: true,
  },
  {
    name: 'configSchema',
    type: 'Record<string, unknown>',
    description: 'Provider 固有の構成を記述する JSON Schema。UI で構成フォームをレンダリングするために使用されます。',
    isOptional: true,
  },
  {
    name: 'createFilesystem',
    type: '(config: TConfig) => WorkspaceFilesystem | Promise<WorkspaceFilesystem>',
    description:
      '保存済みの構成から、実行時の `WorkspaceFilesystem` インスタンスを作成します。Workspace のハイドレーション時に呼び出されます。',
  },
]}
/>

## Provider の実装 {/* #implementing-a-provider */}

```typescript title="src/providers/my-filesystem.ts"
import type { FilesystemProvider, WorkspaceFilesystem } from '@mastra/core/editor'

export const myFilesystemProvider: FilesystemProvider<{ bucket: string; region: string }> = {
  id: 'my-fs',
  name: 'My Filesystem',
  description: 'Object-store-backed filesystem.',
  configSchema: {
    type: 'object',
    required: ['bucket', 'region'],
    properties: {
      bucket: { type: 'string' },
      region: { type: 'string' },
    },
  },
  async createFilesystem(config): Promise<WorkspaceFilesystem> {
    return createMyFilesystem({ bucket: config.bucket, region: config.region })
  },
}
```

登録後、管理者はインラインの Workspace 構成または保存済みの [`StorageWorkspaceRef`](/reference/editor/storage-workspace-ref) から、この Provider を参照できます。

## 関連項目 {/* #related */}

- [Workspace](https://agent-builder.mastra.ai/workspace): 概念と実践的な例。
- [StorageWorkspaceRef](/reference/editor/storage-workspace-ref): `createFilesystem` が使用する保存済み構成。
- [SandboxProvider](/reference/editor/sandbox-provider): コマンド実行用の関連 Provider。
- [MastraEditor クラス](/reference/editor/mastra-editor): Provider レジストリ。
