---
title: "リファレンス: ProcessorProvider | Editor"
description: "設定可能な Processor を MastraEditor に登録するインターフェース、ProcessorProvider のリファレンス。"
packages:
  - "@mastra/core"
---

# ProcessorProvider

`ProcessorProvider` は、設定可能な Processor を [`MastraEditor`](/reference/editor/mastra-editor) に登録するためにパッケージが実装するインターフェースです。Processor Provider には、次の2つの役割があります。

1. **検出:** UI は `info`、`configSchema`、`availablePhases` を使用して設定フォームをレンダリングします。
1. **ランタイム:** Editor は Agent のハイドレーション時に `createProcessor(config)` を呼び出し、保存された設定から Processor をインスタンス化します。

Provider は `MastraEditorConfig.processorProviders` を介して指定します。

## 使用例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'
import { moderationProcessorProvider } from '@mastra/processors/moderation'

new MastraEditor({
  processorProviders: {
    moderation: moderationProcessorProvider,
  },
})
```

## プロパティ {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'info',
    type: 'ProcessorProviderInfo',
    description: 'Provider に関するメタデータ。',
    properties: [
      {
        parameters: [
          {
            name: 'id',
            type: 'string',
            description: '一意の識別子（例: `"moderation"`、`"token-limiter"`）。',
          },
          {
            name: 'name',
            type: 'string',
            description: '人が読みやすい名前。',
          },
          {
            name: 'description',
            type: 'string',
            description: 'Provider の簡潔な説明。',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'configSchema',
    type: 'ZodSchema',
    description:
      'この Provider が受け入れる設定を記述する Zod スキーマ。UI がフォームをレンダリングするために使用します。検証済みの設定オブジェクトは `createProcessor()` に渡されます。',
  },
  {
    name: 'availablePhases',
    type: 'ProcessorPhase[]',
    description:
      "この Provider の Processor がサポートするフェーズ。`'processInput'`、`'processInputStep'`、`'processOutputStream'`、`'processOutputResult'`、`'processOutputStep'` のうち1つ以上。",
  },
  {
    name: 'createProcessor',
    type: '(config: Record<string, unknown>) => Processor',
    description:
      '検証済みの設定から `Processor` インスタンスを作成します。Agent のハイドレーション時に呼び出され、保存された Processor 設定を実際のインスタンスに解決します。',
  },
]}
/>

## Provider を実装する {/* #implementing-a-provider */}

```typescript title="src/providers/my-processor.ts"
import type { ProcessorProvider } from '@mastra/core/processor-provider'
import { z } from 'zod'
import { MyProcessor } from './my-processor-impl'

export const myProcessorProvider: ProcessorProvider = {
  info: {
    id: 'my-processor',
    name: 'My Processor',
    description: 'Filters output for sensitive content.',
  },
  configSchema: z.object({
    threshold: z.number().min(0).max(1),
  }),
  availablePhases: ['processOutputStream', 'processOutputResult'],
  createProcessor(config) {
    return new MyProcessor(config as { threshold: number })
  },
}
```

## 関連情報 {/* #related */}

- [MastraEditor クラス](/reference/editor/mastra-editor): Provider レジストリ。
