---
title: "リファレンス: createNotificationInboxTool() | Signal"
description: "通知受信ボックスのレコードを読み取り、管理する Tool を作成する createNotificationInboxTool() の API リファレンスです。"
packages:
  - "@mastra/core"
---

# createNotificationInboxTool()

**追加バージョン:** `@mastra/core@1.39.0`

`createNotificationInboxTool()` は、現在の Thread の通知受信ボックスレコードを Agent が確認、管理できる Mastra Tool を作成します。

この Tool は Durable Notification Signal とともに使用します。通知レコードの送信については、[`Agent.sendNotificationSignal()`](/reference/agents/agent#sendnotificationsignalnotification-options) を参照してください。

## 使用例 {/* #usage-example */}

次の例では、受信ボックス Tool を Agent に追加します。

```typescript title="src/mastra/agents/support-agent.ts"
import { Agent } from '@mastra/core/agent'
import { createNotificationInboxTool } from '@mastra/core/notifications'

const notificationsStorage = await storage.getStore('notifications')

export const supportAgent = new Agent({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'Help the user triage updates.',
  model: '__GATEWAY_OPENAI_MODEL__',
  tools: {
    notificationInbox: createNotificationInboxTool({ storage: notificationsStorage }),
  },
})
```

## パラメーター {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'object',
    description: 'Tool の設定。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'storage',
            type: 'NotificationsStorage',
            description: "`storage.getStore('notifications')` が返す通知ストレージドメイン。",
          },
        ],
      },
    ],
  },
]}
/>

## Tool の入力 {/* #tool-input */}

生成される Tool の ID は `notification-inbox` で、次の入力フィールドを受け取ります。

<PropertiesTable
  content={[
  {
    name: 'action',
    type: "'list' | 'read' | 'markSeen' | 'dismiss' | 'archive' | 'search'",
    description: '実行する受信ボックス Action。',
  },
  {
    name: 'threadId',
    type: 'string',
    isOptional: true,
    description: '確認する Thread ID。利用可能な場合は、現在の Agent Thread がデフォルトになります。',
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: '通知 ID。`markSeen`、`dismiss`、`archive` では必須です。`read` では省略できます。',
  },
  {
    name: 'status',
    type: "'pending' | 'delivered' | 'seen' | 'dismissed' | 'archived' | 'discarded'",
    isOptional: true,
    description: 'list、read、search Action のステータスフィルター。',
  },
  {
    name: 'priority',
    type: "'low' | 'medium' | 'high' | 'urgent'",
    isOptional: true,
    description: 'list、read、search Action の優先度フィルター。',
  },
  {
    name: 'source',
    type: 'string',
    isOptional: true,
    description: 'list、read、search Action の Source フィルター。',
  },
  {
    name: 'query',
    type: 'string',
    isOptional: true,
    description: '検索クエリ。`action` が `search` の場合は必須です。',
  },
  {
    name: 'limit',
    type: 'number',
    isOptional: true,
    description: '返す通知の最大数。正の整数である必要があります。',
  },
]}
/>

## Action {/* #actions */}

| Action | 説明 |
| - | - |
| `list` | 条件に一致する通知を返します。 |
| `read` | 読み取り可能な通知内容を Signal として配信し、配信済みのレコードを確認済みにします。 |
| `markSeen` | 1 件の通知を確認済みにします。 |
| `dismiss` | 1 件の通知を破棄済みにします。 |
| `archive` | 1 件の通知をアーカイブ済みにします。 |
| `search` | クエリと省略可能なフィルターを使用して通知を検索します。 |

`read` が保留中または配信済みの通知を検出すると、通常の Tool 出力として全内容を返す代わりに、その内容を Notification Signal として配信します。Agent が `<notification-summary>` Signal の背後にある完全なレコードを必要とする場合に、その Signal の後で使用してください。
