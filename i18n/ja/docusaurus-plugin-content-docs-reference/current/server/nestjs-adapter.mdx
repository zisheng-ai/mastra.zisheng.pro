---
title: "リファレンス：NestJS Adapter | Server"
description: "@mastra/nestjs サーバーアダプターの API リファレンス。"
packages:
  - "@mastra/nestjs"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# NestJS Adapter

`@mastra/nestjs` パッケージは、Express ベースの NestJS プラットフォームで Mastra を実行するための NestJS モジュールを提供します。

v1 では意図的に Express のみをサポートしています。別の HTTP アダプターで Nest をブートストラップした場合、部分的な統合を試みる代わりに、`MastraModule` が起動時にエラーをスローします。アダプター全般の概念については、[Server Adapters](/docs/server/server-adapters) を参照してください。

## インストール {/* #installation */}

NestJS アダプターをインストールし、アプリが Express プラットフォームを使用していることを確認します。

```bash npm2yarn
npm install @mastra/nestjs@latest
```

## 使用例 {/* #usage-example */}

```typescript title="src/app.module.ts"
import { Module } from '@nestjs/common'
import { MastraModule } from '@mastra/nestjs'
import { mastra } from './mastra'

@Module({
  imports: [
    MastraModule.register({
      mastra,
    }),
  ],
})
export class AppModule {}
```

:::note

`MastraModule` はキャッチオールコントローラー（`@All('*')`）を登録します。アプリのモジュールより先にインポートすると、関係のないルートをインターセプトして 404 を返す可能性があります。競合を避けるには、`MastraModule` を最後にインポートするか、専用のプレフィックス（例：`/api/v1/mastra`）配下にマウントしてください。

:::

```typescript title="src/main.ts"
import { NestFactory } from '@nestjs/core'
import { AppModule } from './app.module'

async function bootstrap() {
  const app = await NestFactory.create(AppModule)
  await app.listen(3000)
}

bootstrap()
```

デフォルトでは、Mastra のルートは `/api` 配下にマウントされます。変更するには `prefix` を使用します。

## モジュールオプション {/* #module-options */}

<PropertiesTable
  content={[
  {
    name: 'mastra',
    type: 'Mastra',
    description: 'Mastra インスタンス',
    isOptional: false,
  },
  {
    name: 'prefix',
    type: 'string',
    description: 'ルートパスのプレフィックス（例：`/api/v2`）',
    isOptional: true,
    defaultValue: '`/api`',
  },
  {
    name: 'rateLimitOptions',
    type: '{ enabled?: boolean; defaultLimit?: number; windowMs?: number; generateLimit?: number }',
    description: 'レート制限の設定（デフォルトで有効）',
    isOptional: true,
  },
  {
    name: 'shutdownOptions',
    type: '{ timeoutMs?: number; notifyClients?: boolean }',
    description: 'グレースフルシャットダウンの設定',
    isOptional: true,
  },
  {
    name: 'bodyLimitOptions',
    type: '{ maxSize?: number; maxFileSize?: number; tempDir?: string; allowedMimeTypes?: string[] }',
    description: 'リクエストボディのサイズ制限',
    isOptional: true,
  },
  {
    name: 'streamOptions',
    type: '{ redact?: boolean; heartbeatMs?: number }',
    description: 'ストリーミングの設定',
    isOptional: true,
  },
  {
    name: 'tracingOptions',
    type: '{ enabled?: boolean; serviceName?: string }',
    description: 'OpenTelemetry トレーシングの設定',
    isOptional: true,
  },
  {
    name: 'contextOptions',
    type: '{ strict?: boolean; logWarnings?: boolean }',
    description: 'リクエストコンテキスト解析の設定',
    isOptional: true,
  },
  {
    name: 'customRouteAuthConfig',
    type: 'Map<string, boolean>',
    description: 'ルート単位の認証オーバーライド。キーは `METHOD:PATH` です。',
    isOptional: true,
  },
  {
    name: 'tools',
    type: 'Record<string, Tool>',
    description: 'サーバーに登録された Tool',
    isOptional: true,
  },
  {
    name: 'taskStore',
    type: 'InMemoryTaskStore',
    description: 'A2A（Agent-to-Agent）操作用のタスクストア',
    isOptional: true,
  },
  {
    name: 'mcpOptions',
    type: '{ serverless?: boolean; sessionIdGenerator?: () => string }',
    description: 'MCP トランスポートのオプション',
    isOptional: true,
  },
  {
    name: 'auth',
    type: '{ enabled?: boolean; allowQueryApiKey?: boolean }',
    description:
      'Mastra のトークン認証を有効にします。デフォルトでは無効です。ほとんどの NestJS アプリは独自の認証ガードを使用します。後方互換性のため、クエリ文字列の `apiKey` 認証はオプトインです。',
    isOptional: true,
    defaultValue: '`{ enabled: false }`',
  },
]}
/>

## 非同期登録 {/* #async-registration */}

```typescript title="src/app.module.ts"
import { Module } from '@nestjs/common'
import { ConfigModule, ConfigService } from '@nestjs/config'
import { MastraModule } from '@mastra/nestjs'
import { Mastra } from '@mastra/core/mastra'

@Module({
  imports: [
    ConfigModule.forRoot(),
    MastraModule.registerAsync({
      imports: [ConfigModule],
      useFactory: (config: ConfigService) => ({
        mastra: new Mastra({
          agents: {
            greeter: {
              name: 'greeter',
              description: 'Greets the user',
              model: config.get('MASTRA_MODEL', '__GATEWAY_OPENAI_MODEL_MINI__'),
            },
          },
        }),
        prefix: config.get('MASTRA_PREFIX', '/api'),
      }),
      inject: [ConfigService],
    }),
  ],
})
export class AppModule {}
```

## Mastra へのアクセス {/* #accessing-mastra */}

サービス内で `MASTRA` トークンまたは `MastraService` を使用します。

```typescript
import { Injectable, Inject } from '@nestjs/common'
import { MASTRA, MastraService } from '@mastra/nestjs'
import type { Mastra } from '@mastra/core/mastra'

@Injectable()
export class AgentService {
  constructor(@Inject(MASTRA) private readonly mastra: Mastra) {}
}

@Injectable()
export class WorkflowService {
  constructor(private readonly mastraService: MastraService) {}
}
```

## MCP ルート {/* #mcp-routes */}

MCP エンドポイントは API プレフィックス配下で公開されます。

- `POST /api/mcp/:serverId/mcp`
- `GET /api/mcp/:serverId/sse`
- `POST /api/mcp/:serverId/messages`

## ヘルスチェックルート {/* #health-routes */}

インフラストラクチャとの互換性を保つため、運用エンドポイントには意図的にプレフィックスが付きません。

- `GET /health`
- `GET /ready`
- `GET /info`

## 関連項目 {/* #related */}

- [Server Adapters](/docs/server/server-adapters)
- [MastraServer リファレンス](/reference/server/mastra-server)
