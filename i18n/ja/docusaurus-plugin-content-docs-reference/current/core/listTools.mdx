---
title: "リファレンス: Mastra.listTools() | Core"
description: "Mastra レベルの Tool レジストリを返す `Mastra.listTools()` メソッドのドキュメント。"
packages:
  - "@mastra/core"
---

# Mastra.listTools()

`.listTools()` メソッドは、Mastra インスタンスに設定されている Tool を返します。返されるレコードでは Mastra の登録キーがキー、Tool インスタンスが値になります。

## 使用例

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core/mastra'
import { weatherTool } from './tools/weather-tool'

export const mastra = new Mastra({
  tools: {
    weather: weatherTool,
  },
})

const tools = mastra.listTools()
const weather = tools?.weather
```

## パラメーター

このメソッドはパラメーターを受け取りません。

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'tools',
    type: 'TTools | undefined',
    description:
      'Mastra レベルの Tool レジストリ。各キーは登録キー、各値は Tool インスタンスです。レジストリがない場合は `undefined` を返します。',
  },
]}
/>

このメソッドが一覧表示するのは Mastra レベルのレジストリです。関数形式の Agent `tools` コールバック内にのみ存在する Tool は含まれません。

## 関連項目

- [Agent 間で Tool を共有](/docs/agents/using-tools#share-tools-across-agents)
- [Mastra クラス](/reference/core/mastra-class)
- [Mastra.getTool()](/reference/core/getTool)
- [Mastra.getToolById()](/reference/core/getToolById)
