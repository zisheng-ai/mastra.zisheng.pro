---
title: "リファレンス: Mastra.getAgent() | Core"
description: "名前で Agent を取得する Mastra の `Agent.getAgent()` メソッドのドキュメント。"
packages:
  - "@mastra/core"
---

# Mastra.getAgent()

`.getAgent()` メソッドは Agent を取得します。Agent 名を表す単一の `string` パラメータを受け取ります。

## 使用例

```typescript
mastra.getAgent('testAgent')
```

## パラメータ

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'TAgentName extends keyof TAgents',
    description: '取得する Agent の名前。Mastra 設定に存在する有効な Agent 名を指定する必要があります。',
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'TAgents[TAgentName]',
    description: '指定した名前の Agent インスタンス。Agent が見つからない場合はエラーをスローします。',
  },
]}
/>

## 関連項目

- [Agent の概要](/docs/agents/overview)
