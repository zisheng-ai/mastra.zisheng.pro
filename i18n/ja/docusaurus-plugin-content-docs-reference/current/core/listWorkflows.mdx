---
title: "リファレンス: Mastra.listWorkflows() | Core"
description: "設定済みのすべての Workflow を取得する Mastra の `Mastra.listWorkflows()` メソッドのドキュメント。"
packages:
  - "@mastra/core"
---

# Mastra.listWorkflows()

`.listWorkflows()` メソッドは、Mastra インスタンスに設定されているすべての Workflow を取得します。任意の options オブジェクトを受け取ります。

## 使用例

```typescript
mastra.listWorkflows()
```

## パラメーター

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ serialized?: boolean }',
    description:
      '任意の設定オブジェクト。`serialized` が true の場合、Workflow インスタンス全体ではなく name プロパティだけを持つ簡略化された Workflow オブジェクトを返します。',
    optional: true,
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'workflows',
    type: 'Record<string, Workflow>',
    description:
      '設定済みのすべての Workflow のレコード。キーは Workflow ID、値は Workflow インスタンスです（serialized が true の場合は簡略化されたオブジェクト）。',
  },
]}
/>

## 関連項目

- [Workflow の概要](/docs/workflows/overview)
