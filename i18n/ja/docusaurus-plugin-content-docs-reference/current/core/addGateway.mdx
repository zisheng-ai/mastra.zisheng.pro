---
title: "リファレンス: Mastra.addGateway() | Core"
description: "Mastra インスタンスに Gateway を追加します"
packages:
  - "@mastra/core"
---

# Mastra.addGateway()

Mastra インスタンスに Gateway を動的に追加します。初期化中にも、初期化後にも使用できます。

## 使用例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra()

// Add with explicit key
mastra.addGateway(new MyCustomGateway(), 'myGateway')

// Add using gateway's ID as the key
mastra.addGateway(new MyCustomGateway())
// Stored with key 'my-custom-gateway' (the gateway's id)
```

## パラメータ

<PropertiesTable
  content={[
  {
    name: 'gateway',
    type: 'MastraModelGateway',
    description: '追加する Gateway インスタンス',
  },
  {
    name: 'key',
    type: 'string',
    isOptional: true,
    description: '任意の登録キー。指定しない場合は gateway.getId() を使用します',
  },
]}
/>

## 戻り値

なし。Gateway が内部レジストリに追加されます。

## 動作

- 同じキーの Gateway がすでに存在する場合、追加はスキップされます
- 開発モード（`MASTRA_DEV=true`）では、カスタム Gateway の型生成が自動的に開始されます
- Gateway はモデル解決にすぐ使用できるようになります

## 関連項目

- [Mastra.getGateway()](/reference/core/getGateway): 登録キーで Gateway を取得
- [Mastra.getGatewayById()](/reference/core/getGatewayById): ID で Gateway を取得
- [Mastra.listGateways()](/reference/core/listGateways): すべての Gateway を一覧表示
- [MastraModelGateway](/reference/core/mastra-model-gateway): Gateway の基底クラス
- [カスタム Gateway ガイド](/models/gateways/custom-gateways): カスタム Gateway の作成方法
