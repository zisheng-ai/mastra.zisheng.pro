---
title: "リファレンス: Mastra.getEditor() | Core"
description: "設定済みの Editor インスタンスを取得する Mastra の `Mastra.getEditor()` メソッドのドキュメント。"
packages:
  - "@mastra/core"
  - "@mastra/editor"
---

# Mastra.getEditor()

`.getEditor()` メソッドは、Mastra インスタンスに設定されている Editor インスタンスを取得します。Editor は、保存済みの Agent、プロンプトブロック、MCP クライアント、Scorer、Skill、Workspace をプログラムから管理するための CRUD 名前空間を公開します。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'
import { MastraEditor } from '@mastra/editor'

const mastra = new Mastra({
  editor: new MastraEditor(),
})

const editor = mastra.getEditor()

const agent = await editor?.agent.create({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'Help customers with their questions.',
})
```

## パラメーター {/* #parameters */}

このメソッドはパラメーターを受け取りません。

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'editor',
    type: 'MastraEditor | undefined',
    description: '設定済みの Editor インスタンス。Editor が設定されていない場合は undefined。',
  },
]}
/>

## 関連項目 {/* #related */}

- [Editor](/docs/editor/overview)
- [MastraEditor リファレンス](/reference/editor/mastra-editor)
