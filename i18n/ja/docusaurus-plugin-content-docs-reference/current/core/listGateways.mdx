---
title: "リファレンス: Mastra.listGateways() | Core"
description: "登録済みのすべての Gateway を一覧表示します"
packages:
  - "@mastra/core"
---

# Mastra.listGateways()

登録キーでインデックス付けされた、登録済みのすべての Gateway のレコードを返します。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({
  gateways: {
    myGateway: new MyCustomGateway(),
    anotherGateway: new AnotherGateway(),
  },
})

const gateways = mastra.listGateways()
console.log(Object.keys(gateways)) // ['myGateway', 'anotherGateway']

// Iterate over all gateways
for (const [key, gateway] of Object.entries(gateways)) {
  console.log(`${key}: ${gateway.name}`)
}
```

## パラメーター {/* #parameters */}

ありません。

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'gateways',
    type: 'Record<string, MastraModelGateway>',
    description: '登録キーでインデックス付けされた Gateway インスタンスのレコード',
  },
]}
/>

## 関連項目 {/* #related */}

- [Mastra.getGateway()](/reference/core/getGateway): 登録キーで Gateway を取得
- [Mastra.getGatewayById()](/reference/core/getGatewayById): ID で Gateway を取得
- [Mastra.addGateway()](/reference/core/addGateway): Gateway を追加
- [MastraModelGateway](/reference/core/mastra-model-gateway): Gateway の基底クラス
- [カスタム Gateway ガイド](/models/gateways/custom-gateways): カスタム Gateway の作成方法
