---
title: "リファレンス: Mastra.getTool() | Core"
description: "Mastra の登録キーで Tool を取得する `Mastra.getTool()` メソッドのドキュメント。"
packages:
  - "@mastra/core"
---

# Mastra.getTool()

`.getTool()` メソッドは、Mastra レベルのレジストリから登録キーで Tool を取得します。

## 使用例 {/* #usage-example */}

`weather` キーで Tool を登録し、そのキーを使って取得します。

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core/mastra'
import { weatherTool } from './tools/weather-tool'

export const mastra = new Mastra({
  tools: {
    weather: weatherTool,
  },
})

const tool = mastra.getTool('weather')
```

## パラメーター {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'TToolName extends keyof TTools',
    description: 'Mastra の `tools` レジストリにある Tool の登録キー。',
  },
]}
/>

## 関連項目 {/* #related */}

- [Agent 間で Tool を共有](/docs/agents/using-tools#share-tools-across-agents)
- [Mastra クラス](/reference/core/mastra-class)
- [Mastra.getToolById()](/reference/core/getToolById)
- [Mastra.listTools()](/reference/core/listTools)
