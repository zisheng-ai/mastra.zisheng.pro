---
title: 'リファレンス：Agent.getMetadata() | Agents'
description: "Agent に設定されたメタデータを取得する Mastra Agent の `Agent.getMetadata()` メソッドのドキュメントです。"
packages:
  - '@mastra/core'
---

# Agent.getMetadata()

`.getMetadata()` メソッドは、Agent に設定されたメタデータを取得し、関数の場合は解決します。Agent の ID や名前へデータを組み込まずにクライアント上で Agent を分類するには、メタデータを使います。

## 使用例

```typescript
await agent.getMetadata()
```

## パラメータ

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ requestContext?: RequestContext }',
    isOptional: true,
    defaultValue: '{}',
    description: 'リクエストコンテキストを含む任意の設定オブジェクト。',
    properties: [
      {
        type: '{ requestContext?: RequestContext }',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            defaultValue: 'undefined',
            description: '動的なメタデータ関数へ渡される Request Context。メタデータが静的な場合は無視されます。',
          },
        ],
      },
    ],
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'metadata',
    type: 'Record<string, unknown> | Promise<Record<string, unknown>>',
    description:
      'Agent に設定されたメタデータ。メタデータが設定されていない場合は `undefined` です。直接返されるか、関数として定義されている場合はメタデータへ解決される Promise として返されます。',
  },
]}
/>

## 詳細な使用例

静的なメタデータ：

```typescript title="src/mastra/agents/support-agent.ts"
import { Agent } from '@mastra/core/agent'

export const supportAgent = new Agent({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'You help customers with support requests.',
  model: '__GATEWAY_OPENAI_MODEL__',
  metadata: { type: 'support' },
})

const metadata = supportAgent.getMetadata()
```

リクエストコンテキストから解決される動的なメタデータ：

```typescript title="src/mastra/agents/support-agent.ts"
import { Agent } from '@mastra/core/agent'

export const supportAgent = new Agent({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'You help customers with support requests.',
  model: '__GATEWAY_OPENAI_MODEL__',
  metadata: ({ requestContext }) => ({
    type: 'support',
    tenant: requestContext.get('tenant'),
  }),
})

const metadata = await supportAgent.getMetadata({ requestContext })
```

## 関連項目

- [Agent の概要](/docs/agents/overview)
- [Request Context](/docs/server/request-context)
