---
title: "リファレンス：Agent.network() | Agents"
description: "複数 Agent の連携とルーティングを可能にする、Mastra Agent の `Agent.network()` メソッドに関するドキュメントです。"
packages:
  - "@mastra/core"
---

import { MODEL_SETTINGS_OBJECT } from "@site/src/components/ModelSettingsProperties";

# Agent.network() {/* #agentnetwork */}

`.network()` メソッドは、複数 Agent の連携とルーティングを可能にします。このメソッドは Message と、省略可能な実行オプションを受け取ります。

:::warning[非推奨]

`.network()` Primitive は非推奨となり、将来のメジャーリリースで削除される予定です。代わりに、`agent.stream()` または `agent.generate()` とともに [Supervisor Agent](/docs/capabilities/subagents) を使用してください。移行方法については、[移行ガイド](/guides/migrations/network-to-supervisor)を参照してください。

:::

## 使用例 {/* #usage-example */}

```typescript
import { Agent } from '@mastra/core/agent'
import { agent1, agent2 } from './agents'
import { workflow1 } from './workflows'
import { tool1, tool2 } from './tools'

const agent = new Agent({
  id: 'network-agent',
  name: 'Network Agent',
  instructions: 'You are a network agent that can help users with a variety of tasks.',
  model: '__GATEWAY_OPENAI_MODEL__',
  agents: {
    agent1,
    agent2,
  },
  workflows: {
    workflow1,
  },
  tools: {
    tool1,
    tool2,
  },
})

await agent.network(`
  Find me the weather in Tokyo.
  Based on the weather, plan an activity for me.
`)
```

## パラメーター {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'string | string[] | CoreMessage[] | AiMessageType[] | UIMessageWithMetadata[]',
    description:
      'Agent に送信する Message。単一の文字列、文字列の配列、または構造化された Message オブジェクトを指定できます。',
  },
  {
    name: 'options',
    type: 'MultiPrimitiveExecutionOptions',
    isOptional: true,
    description: 'Network プロセスの省略可能な設定。',
    properties: [
      {
        type: 'MultiPrimitiveExecutionOptions',
        parameters: [
          {
            name: 'maxSteps',
            type: 'number',
            isOptional: true,
            description: '実行中の最大 Step 数。',
          },
          {
            name: 'abortSignal',
            type: 'AbortSignal',
            isOptional: true,
            description:
              'Network の実行を中止する Signal。中止すると、Network はルーティングを停止し、実行中の Subagent、Tool、Workflow をすべてキャンセルして、途中結果を Memory に保存しません。',
          },
          {
            name: 'onAbort',
            type: '(event: { primitiveType: string; primitiveId: string; iteration: number }) => void | Promise<void>',
            isOptional: true,
            description:
              'Network が中止されたときに呼び出される Callback。中止時に実行されていた Primitive の種類と ID を含むイベントを受け取ります。',
          },
          {
            name: 'memory',
            type: 'object',
            isOptional: true,
            description: 'Memory の設定。Memory の管理にはこの方法を推奨します。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'thread',
                    type: 'string | { id: string; metadata?: Record<string, any>, title?: string }',
                    isOptional: false,
                    description:
                      'Conversation Thread。文字列 ID、または `id` と省略可能な `metadata` を含むオブジェクトとして指定します。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'resource',
                    type: 'string',
                    isOptional: false,
                    description: 'Thread に関連付けられたユーザーまたは Resource の識別子。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'options',
                    type: 'MemoryConfig',
                    isOptional: true,
                    description: 'Message History や Semantic Recall など、Memory の動作に関する設定。',
                  },
                ],
              },
            ],
          },
          {
            name: 'tracingContext',
            type: 'TracingContext',
            isOptional: true,
            description:
              'Child Span の作成とメタデータの追加に使用する Tracing Context。Mastra の Tracing システムを使用すると自動的に注入されます。',
            properties: [
              {
                type: 'TracingContext',
                parameters: [
                  {
                    name: 'currentSpan',
                    type: 'Span',
                    isOptional: true,
                    description:
                      'Child Span の作成とメタデータの追加に使用する現在の Span。実行中に独自の Child Span を作成したり、Span 属性を更新したりする場合に使用します。',
                  },
                ],
              },
            ],
          },
          {
            name: 'tracingOptions',
            type: 'TracingOptions',
            isOptional: true,
            description: 'Tracing 設定のオプション。',
            properties: [
              {
                type: 'TracingOptions',
                parameters: [
                  {
                    name: 'metadata',
                    type: 'Record<string, any>',
                    isOptional: true,
                    description:
                      'Root Trace Span に追加するメタデータ。ユーザー ID、Session ID、Feature Flag などのカスタム属性を追加する場合に役立ちます。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'requestContextKeys',
                    type: 'string[]',
                    isOptional: true,
                    description:
                      "この Trace のメタデータとして抽出する追加の RequestContext Key。ネストされた値には Dot Notation（例：'user.id'）を使用できます。",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'traceId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'この実行で使用する Trace ID（1〜32文字の16進数）。指定すると、この Trace は指定した Trace の一部になります。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'parentSpanId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'この実行で使用する Parent Span ID（1〜16文字の16進数）。指定すると、Root Span はこの Span の Child として作成されます。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'tags',
                    type: 'string[]',
                    isOptional: true,
                    description: 'この Trace に適用する Tag。Trace の分類と絞り込みに使用する文字列 Label です。',
                  },
                ],
              },
            ],
          },
          {
            name: 'telemetry',
            type: 'TelemetrySettings',
            isOptional: true,
            description: 'ストリーミング中に OTLP Telemetry を収集するための設定（Tracing ではありません）。',
            properties: [
              {
                type: 'TelemetrySettings',
                parameters: [
                  {
                    name: 'isEnabled',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Telemetry を有効または無効にします。実験的機能のため、デフォルトでは無効です。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordInputs',
                    type: 'boolean',
                    isOptional: true,
                    description:
                      '入力の記録を有効または無効にします。デフォルトでは有効です。機密情報を記録しないように、入力の記録を無効にすることもできます。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordOutputs',
                    type: 'boolean',
                    isOptional: true,
                    description:
                      '出力の記録を有効または無効にします。デフォルトでは有効です。機密情報を記録しないように、出力の記録を無効にすることもできます。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'functionId',
                    type: 'string',
                    isOptional: true,
                    description:
                      'この Function の識別子。Function ごとに Telemetry Data をグループ化するために使用します。',
                  },
                ],
              },
            ],
          },
          MODEL_SETTINGS_OBJECT,
          {
            name: 'structuredOutput',
            type: 'StructuredOutputOptions',
            isOptional: true,
            description: 'Network の結果から型付きの構造化出力を生成するための設定。',
            properties: [
              {
                type: 'StructuredOutputOptions',
                parameters: [
                  {
                    name: 'schema',
                    type: 'ZodSchema | JSONSchema7',
                    isOptional: false,
                    description: '出力の検証に使用する Schema。Zod Schema または JSON Schema を指定できます。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'model',
                    type: 'MastraModelConfig',
                    isOptional: true,
                    description: '構造化出力の生成に使用する Model。デフォルトでは Agent の Model を使用します。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'instructions',
                    type: 'string',
                    isOptional: true,
                    description: '構造化出力を生成するためのカスタム instructions。',
                  },
                ],
              },
            ],
          },
          {
            name: 'runId',
            type: 'string',
            isOptional: true,
            description: 'この Generation Run の一意の ID。追跡や Debug に役立ちます。',
          },
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            description: '依存性注入とコンテキスト情報に使用する Request Context。',
          },
          {
            name: 'traceId',
            type: 'string',
            isOptional: true,
            description:
              'Tracing が有効な場合に、この実行に関連付けられる Trace ID。Log の関連付けや実行フローの Debug に使用します。',
          },
          {
            name: 'spanId',
            type: 'string',
            isOptional: true,
            description:
              'Tracing が有効な場合に、この実行に関連付けられる Root Span ID。Span 単位の検索と関連付けに使用します。',
          },
          {
            name: 'onStepFinish',
            type: '(event: any) => Promise<void> | void',
            isOptional: true,
            description:
              'Subagent の実行中に各 LLM Step が完了した後で呼び出される Callback。終了理由や Token 使用量など、Step の詳細を受け取ります。',
          },
          {
            name: 'onError',
            type: '({ error }: { error: Error | string }) => Promise<void> | void',
            isOptional: true,
            description: 'Subagent の実行中にエラーが発生したときに呼び出される Callback。',
          },
        ],
      },
    ],
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'stream',
    type: 'MastraAgentNetworkStream<NetworkChunkType>',
    description: 'ReadableStream<NetworkChunkType> を Network 固有の追加プロパティで拡張したカスタム Stream',
  },
  {
    name: 'status',
    type: 'Promise<RunStatus>',
    description: '現在の Workflow Run Status に解決される Promise',
  },
  {
    name: 'result',
    type: 'Promise<WorkflowResult<TState, TOutput, TSteps>>',
    description: '最終的な Workflow の結果に解決される Promise',
  },
  {
    name: 'usage',
    type: 'Promise<{ promptTokens: number; completionTokens: number; totalTokens: number }>',
    description: 'Token 使用量の統計に解決される Promise',
  },
  {
    name: 'object',
    type: 'Promise<OUTPUT | undefined>',
    description:
      '構造化出力オブジェクトに解決される Promise。structuredOutput オプションを指定した場合のみ利用できます。Schema が指定されていない場合は undefined に解決されます。',
  },
  {
    name: 'objectStream',
    type: 'ReadableStream<Partial<OUTPUT>>',
    description:
      '構造化出力の生成中に Partial Object を配信する Stream。生成途中の結果をストリーミングする場合に役立ちます。',
  },
]}
/>

## 構造化出力 {/* #structured-output */}

Network から型付きで検証済みの結果を取得するには、`structuredOutput` オプションを使用します。Network はタスクの完了後、Schema に一致するレスポンスを生成します。

```typescript
import { z } from 'zod'

const resultSchema = z.object({
  summary: z.string().describe('A brief summary of the findings'),
  recommendations: z.array(z.string()).describe('List of recommendations'),
  confidence: z.number().min(0).max(1).describe('Confidence score'),
})

const stream = await agent.network('Research AI trends and summarize', {
  structuredOutput: {
    schema: resultSchema,
  },
})

// Consume the stream
for await (const chunk of stream) {
  // Handle streaming events
}

// Get the typed result
const result = await stream.object
// result is typed as { summary: string; recommendations: string[]; confidence: number }
console.log(result?.summary)
console.log(result?.recommendations)
```

### Partial Object のストリーミング {/* #streaming-partial-objects */}

生成中の Partial Object をストリーミングすることもできます。

```typescript
const stream = await agent.network('Analyze data', {
  structuredOutput: { schema: resultSchema },
})

// Stream partial objects
for await (const partial of stream.objectStream) {
  console.log('Partial result:', partial)
}

// Get final result
const final = await stream.object
```

### Chunk の種類 {/* #chunk-types */}

構造化出力を使用すると、次の種類の Chunk も出力されます。

- `network-object`：ストリーミング中に Partial Object とともに出力されます
- `network-object-result`：最終的な構造化オブジェクトとともに出力されます

## Network の中止 {/* #aborting-a-network */}

実行中の Network をキャンセルするには、`abortSignal` を使用します。中止すると、Network はルーティングを停止し、実行中の Subagent、Tool、Workflow をすべてキャンセルして、途中結果を Memory に保存しません。

```typescript
const controller = new AbortController()

// Abort after 30 seconds
setTimeout(() => controller.abort(), 30_000)

const stream = await agent.network('Research this topic thoroughly', {
  abortSignal: controller.signal,
  onAbort: ({ primitiveType, primitiveId, iteration }) => {
    console.log(`Aborted ${primitiveType} "${primitiveId}" at iteration ${iteration}`)
  },
})

for await (const chunk of stream) {
  if (
    chunk.type === 'routing-agent-abort' ||
    chunk.type === 'agent-execution-abort' ||
    chunk.type === 'tool-execution-abort' ||
    chunk.type === 'workflow-execution-abort'
  ) {
    console.log('Network was aborted')
  }
}
```
