---
title: "リファレンス：Agent.getInstructions() | Agents"
description: "Agent の動作を導く instructions を取得する Mastra Agent の `Agent.getInstructions()` メソッドのドキュメントです。"
packages:
  - "@mastra/core"
---

# Agent.getInstructions()

`.getInstructions()` メソッドは、Agent に設定された instructions を取得し、関数の場合は解決します。これらの instructions は Agent の動作を導き、その機能と制約を定義します。

## 使用例

```typescript
await agent.getInstructions()
```

## パラメータ

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ requestContext?: RequestContext }',
    isOptional: true,
    defaultValue: '{}',
    description: 'リクエストコンテキストを含む任意の設定オブジェクト。',
    properties: [
      {
        type: '{ requestContext?: RequestContext }',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            defaultValue: 'undefined',
            description: '依存性注入とコンテキスト情報に使う Request Context。',
          },
        ],
      },
    ],
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'instructions',
    type: 'SystemMessage | Promise<SystemMessage>',
    description:
      'Agent に設定された instructions。SystemMessage には string | string[] | CoreSystemMessage | CoreSystemMessage[] | SystemModelMessage | SystemModelMessage[] を指定できます。instructions は直接返されるか、instructions へ解決される Promise として返されます。',
  },
]}
/>

## 詳細な使用例

```typescript
await agent.getInstructions({
  requestContext: new RequestContext(),
})
```

## 関連項目

- [Agent の概要](/docs/agents/overview)
- [Request Context](/docs/server/request-context)
