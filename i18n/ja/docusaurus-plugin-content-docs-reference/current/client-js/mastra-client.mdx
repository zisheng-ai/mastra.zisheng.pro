---
title: "リファレンス：Mastra client SDK | Client SDK"
description: client-js SDK を使用して Mastra と連携する方法を説明します。
packages:
  - "@mastra/client-js"
---

# Mastra client SDK

Mastra Client SDK は、クライアント環境から [Mastra Server](/docs/deployment/mastra-server) と連携するための型安全なインターフェースを提供します。

## 使用例 {/* #usage-example */}

```typescript title="lib/mastra/mastra-client.ts"
import { MastraClient } from '@mastra/client-js'

export const mastraClient = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})
```

## `RequestContext` {/* #requestcontext */}

client SDK で `RequestContext` を使用する場合は、`@mastra/client-js` からインポートします。

```typescript
import { MastraClient, RequestContext } from '@mastra/client-js'

const client = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})

const requestContext = new RequestContext()
requestContext.set('userId', 'user-123')

const agent = client.getAgent('support-agent')

const response = await agent.generate('Summarize this ticket', {
  requestContext,
})
```

`requestContext` は `Record<string, any>` として渡すこともできます。

## パラメーター {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: 'Mastra API のベース URL。すべてのリクエストはこの URL を基準に送信されます。',
    isOptional: false,
  },
  {
    name: 'retries',
    type: 'number',
    description: 'リクエストが失敗した場合、エラーをスローするまでに再試行する回数。',
    isOptional: true,
    defaultValue: '3',
  },
  {
    name: 'backoffMs',
    type: 'number',
    description:
      '失敗したリクエストを再試行するまでの初回待機時間（ミリ秒）。この値は再試行のたびに倍増します（指数バックオフ）。',
    isOptional: true,
    defaultValue: '300',
  },
  {
    name: 'maxBackoffMs',
    type: 'number',
    description: 'バックオフ時間の上限（ミリ秒）。再試行間の待機時間が長くなりすぎるのを防ぎます。',
    isOptional: true,
    defaultValue: '5000',
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: 'すべてのリクエストに含めるカスタム HTTP ヘッダーのオブジェクト。',
    isOptional: true,
  },
  {
    name: 'credentials',
    type: '"omit" | "same-origin" | "include"',
    description:
      'リクエストの credentials モード。詳細は https://developer.mozilla.org/en-US/docs/Web/API/Request/credentials を参照してください。',
    isOptional: true,
  },
]}
/>

## メソッド {/* #methods */}

<PropertiesTable
  content={[
  {
    name: 'listAgents()',
    type: 'Promise<Record<string, GetAgentResponse>>',
    description: '利用可能なすべての Agent インスタンスを返します。',
  },
  {
    name: 'getAgent(agentId)',
    type: 'Agent',
    description: 'ID を指定して特定の Agent インスタンスを取得します。',
  },
  {
    name: 'listMemoryThreads(params)',
    type: 'Promise<StorageThreadType[]>',
    description: '指定したリソースと Agent のメモリスレッドを取得します。`resourceId` と `agentId` が必要です。',
  },
  {
    name: 'createMemoryThread(params)',
    type: 'Promise<MemoryThread>',
    description: '指定したパラメーターで新しいメモリスレッドを作成します。',
  },
  {
    name: 'getMemoryThread({ threadId, agentId })',
    type: 'MemoryThread',
    description: 'ID を指定して特定のメモリスレッドを取得します。',
  },
  {
    name: 'saveMessageToMemory(params)',
    type: 'Promise<{ messages: (MastraMessageV1 | MastraDBMessage)[] }>',
    description: '1 件以上のメッセージをメモリシステムに保存します。保存したメッセージを返します。',
  },
  {
    name: 'getMemoryStatus()',
    type: 'Promise<MemoryStatus>',
    description: 'メモリシステムの現在の状態を返します。',
  },
  {
    name: 'listTools()',
    type: 'Record<string, Tool>',
    description: '利用可能なすべての Tool を返します。',
  },
  {
    name: 'getTool(toolId)',
    type: 'Tool',
    description: 'ID を指定して特定の Tool インスタンスを取得します。',
  },
  {
    name: 'listWorkflows()',
    type: 'Record<string, Workflow>',
    description: '利用可能なすべての Workflow インスタンスを返します。',
  },
  {
    name: 'getWorkflow(workflowId)',
    type: 'Workflow',
    description: 'ID を指定して特定の Workflow インスタンスを取得します。',
  },
  {
    name: 'getAgentBuilderActions()',
    type: 'Promise<Record<string, WorkflowInfo>>',
    description:
      '利用可能なすべての Agent Builder アクションを返します。[Agent Builder API](https://agent-builder.mastra.ai/reference/client) を参照してください。',
  },
  {
    name: 'getAgentBuilderAction(actionId)',
    type: 'AgentBuilder',
    description:
      'ID を指定して Agent Builder アクションを取得します。[Agent Builder API](https://agent-builder.mastra.ai/reference/client) を参照してください。',
  },
  {
    name: 'responses',
    type: 'Responses',
    description:
      '`create()`、`retrieve()`、`stream()`、`delete()` を備えた OpenAI スタイルの Responses API ヘルパーを提供します。',
  },
  {
    name: 'conversations',
    type: 'Conversations',
    description: '`create()`、`retrieve()`、`delete()`、`items.list()` を備えた会話ヘルパーを提供します。',
  },
  {
    name: 'getVector(vectorName)',
    type: 'MastraVector',
    description: '名前を指定してベクトルストアのインスタンスを返します。',
  },
  {
    name: 'listLogs(params)',
    type: 'Promise<LogEntry[]>',
    description: '指定したフィルターに一致するシステムログを取得します。',
  },
  {
    name: 'getLog(params)',
    type: 'Promise<LogEntry>',
    description: 'ID またはフィルターを指定して特定のログエントリを取得します。',
  },
  {
    name: 'listLogTransports()',
    type: 'string[]',
    description: '設定済みのログトランスポート種別の一覧を返します。',
  },
  {
    name: 'getTrace(traceId)',
    type: 'Promise<TraceRecord>',
    description: 'すべての span と詳細を含む、特定の Trace を ID で取得します。',
  },
  {
    name: 'getTraces(params)',
    type: 'Promise<GetTracesResponse>',
    description:
      'オプションのフィルタリングを使用して、Trace のルート span をページネーション付きで取得します。すべての span を含む完全な Trace を取得するには getTrace() を使用します。',
  },
]}
/>
