---
title: "リファレンス：エラー処理 | Client SDK"
description: Mastra client-js SDK に組み込まれている再試行メカニズムとエラー処理機能について説明します。
packages:
  - "@mastra/client-js"
---

# エラー処理

Mastra Client SDK には、再試行メカニズムとエラー処理機能が組み込まれています。

## エラー処理 {/* #error-handling-1 */}

すべての API メソッドはエラーをスローする可能性があり、エラーをキャッチして処理できます。

```typescript
try {
  const agent = mastraClient.getAgent('agent-id')
  const response = await agent.generate('Hello')
} catch (error) {
  console.error('An error occurred:', error.message)
}
```
