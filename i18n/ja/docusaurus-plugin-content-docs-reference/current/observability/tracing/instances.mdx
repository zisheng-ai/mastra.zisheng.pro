---
title: "リファレンス：Tracing | Observability"
description: Tracing の主要なクラスとメソッド
packages:
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# Observability インスタンス {/* #observability-instances */}

## `DefaultObservabilityInstance` {/* #defaultobservabilityinstance */}

ObservabilityInstance インターフェースのデフォルト実装です。

### コンストラクター {/* #constructor */}

```typescript prettier:false
new DefaultObservabilityInstance(config: ObservabilityInstanceConfig)
```

指定した設定で新しい DefaultObservabilityInstance を作成します。

### プロパティ {/* #properties */}

BaseObservabilityInstance のすべてのプロパティとメソッドを継承します。

## `BaseObservabilityInstance` {/* #baseobservabilityinstance */}

カスタム ObservabilityInstance 実装の基底クラスです。

### メソッド {/* #methods */}

#### `getConfig` {/* #getconfig */}

```typescript prettier:false
getConfig(): Readonly<Required<ObservabilityInstanceConfig>>
```

現在の observability 設定を返します。

#### `getExporters` {/* #getexporters */}

```typescript prettier:false
getExporters(): readonly ObservabilityExporter[]
```

設定済みのすべての exporter を返します。

#### `getSpanOutputProcessors` {/* #getspanoutputprocessors */}

```typescript prettier:false
getSpanOutputProcessors(): readonly SpanOutputProcessor[]
```

設定済みのすべての span output processor を返します。

#### `getLogger` {/* #getlogger */}

```typescript prettier:false
getLogger(): IMastraLogger
```

exporter やその他のコンポーネントで使用する logger インスタンスを返します。

#### `startSpan` {/* #startspan */}

```typescript prettier:false
startSpan<TType extends SpanType>(
  options: StartSpanOptions<TType>
): Span<TType>
```

指定した SpanType の新しい span を開始します。親が指定されていない場合は、Trace のルート span を作成します。

<PropertiesTable
  props={[
  {
    name: 'type',
    type: 'SpanType',
    description: '作成する span の型',
    required: true,
  },
  {
    name: 'name',
    type: 'string',
    description: 'span の名前',
    required: true,
  },
  {
    name: 'parent',
    type: 'AnySpan',
    description: '親 span（ルートでない場合）',
    required: false,
  },
  {
    name: 'attributes',
    type: 'SpanTypeMap[TType]',
    description: '型固有の属性',
    required: false,
  },
  {
    name: 'metadata',
    type: 'Record<string, any>',
    description: 'ユーザー定義のメタデータ',
    required: false,
  },
  {
    name: 'input',
    type: 'any',
    description: '初期入力データ',
    required: false,
  },
  {
    name: 'customSamplerOptions',
    type: 'CustomSamplerOptions',
    description: 'カスタム sampler のオプション',
    required: false,
  },
]}
/>

#### shutdown {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

すべての exporter と processor を停止し、リソースを解放します。

## カスタム実装 {/* #custom-implementation */}

カスタム ObservabilityInstance 実装を作成するには、BaseObservabilityInstance を継承します。

```typescript
class CustomObservabilityInstance extends BaseObservabilityInstance {
  constructor(config: ObservabilityInstanceConfig) {
    super(config)
    // Custom initialization
  }

  // Override methods as needed
  startSpan<TType extends SpanType>(options: StartSpanOptions<TType>): Span<TType> {
    // Custom span creation logic
    return super.startSpan(options)
  }
}
```

## 関連項目 {/* #see-also */}

### ドキュメント {/* #documentation */}

- [Tracing の概要](/docs/observability/tracing/overview)：概念と使用方法
- [設定リファレンス](/reference/observability/tracing/configuration)：設定オプション
- [インターフェースリファレンス](/reference/observability/tracing/interfaces)：型定義
- [Span リファレンス](/reference/observability/tracing/spans)：span のライフサイクルとメソッド

### Exporters {/* #exporters */}

- [MastraStorageExporter](/reference/observability/tracing/exporters/mastra-storage-exporter)：ストレージへの永続化
- [MastraPlatformExporter](/reference/observability/tracing/exporters/mastra-platform-exporter)：Mastra プラットフォームとの統合
- [ConsoleExporter](/reference/observability/tracing/exporters/console-exporter)：デバッグ出力
