---
title: "リファレンス：LaminarExporter | Observability"
description: Tracing 用 Laminar exporter
packages:
  - "@mastra/laminar"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# LaminarExporter {/* #laminarexporter */}

Tracing データを OTLP/HTTP（protobuf）経由で Laminar へ送信します。

## コンストラクター {/* #constructor */}

```typescript prettier:false
new LaminarExporter(config?: LaminarExporterConfig)
```

## `LaminarExporterConfig` {/* #laminarexporterconfig */}

```typescript
interface LaminarExporterConfig extends BaseExporterConfig {
  apiKey?: string
  baseUrl?: string
  endpoint?: string
  headers?: Record<string, string>
  realtime?: boolean
  disableBatch?: boolean
  batchSize?: number
  timeoutMillis?: number
}
```

`BaseExporterConfig` を拡張し、次の項目を含みます。

- `logger?: IMastraLogger` - Logger インスタンス
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - ログレベル（デフォルト：INFO）

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Laminar プロジェクトの API キー。未指定の場合は LMNR_PROJECT_API_KEY 環境変数を使用します。',
    required: false,
  },
  {
    name: 'baseUrl',
    type: 'string',
    description:
      'Laminar のベース URL（Trace のエクスポートとスコアリングに使用）。未指定の場合は LMNR_BASE_URL 環境変数を使用します（デフォルト：https://api.lmnr.ai）。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description:
      'OTLP/HTTP の Trace エンドポイント（/v1/traces で終わる URL）。未指定の場合は LAMINAR_ENDPOINT 環境変数を使用します（デフォルト：`${baseUrl}/v1/traces`）。',
    required: false,
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: 'OTLP リクエストに含める追加ヘッダー（Authorization とマージされます）。',
    required: false,
  },
  {
    name: 'realtime',
    type: 'boolean',
    description: '即時に表示できるよう、各 Span の後にフラッシュします（デフォルト：false）。',
    required: false,
  },
  {
    name: 'disableBatch',
    type: 'boolean',
    description: 'バッチ処理を無効にします（SimpleSpanProcessor）。デフォルト：false。',
    required: false,
  },
  {
    name: 'batchSize',
    type: 'number',
    description: 'バッチあたりの最大 Span 数（BatchSpanProcessor）。デフォルト：512。',
    required: false,
  },
  {
    name: 'timeoutMillis',
    type: 'number',
    description: 'OTLP エクスポートのタイムアウト（ミリ秒）。デフォルト：30000。',
    required: false,
  },
]}
/>

## メソッド {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

Tracing イベントを Laminar へエクスポートします。

### flush {/* #flush */}

```typescript prettier:false
async flush(): Promise<void>
```

exporter をシャットダウンせず、バッファー内のすべての Span を Laminar へ強制的にフラッシュします。ランタイム終了前に Span が確実にエクスポートされるようにする必要がある、サーバーレス環境で役立ちます。

### shutdown {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

保留中のデータをフラッシュし、exporter をシャットダウンします。

## 使用方法 {/* #usage */}

### ゼロコンフィグ（環境変数を使用） {/* #zero-config-using-environment-variables */}

```typescript
import { LaminarExporter } from '@mastra/laminar'

// Reads from LMNR_PROJECT_API_KEY, LMNR_BASE_URL, LAMINAR_ENDPOINT
const exporter = new LaminarExporter()
```

### 明示的な設定 {/* #explicit-configuration */}

```typescript
import { LaminarExporter } from '@mastra/laminar'

const exporter = new LaminarExporter({
  apiKey: process.env.LMNR_PROJECT_API_KEY,
  baseUrl: 'https://api.lmnr.ai',
  realtime: true,
})
```
