---
title: "リファレンス：ConsoleExporter | Observability"
description: ConsoleExporter の API リファレンス
packages:
  - "@mastra/core"
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# ConsoleExporter

デバッグと開発のために Trace イベントをコンソールへ出力します。

## コンストラクター {/* #constructor */}

```typescript prettier:false
new ConsoleExporter(config?: BaseExporterConfig)
```

<PropertiesTable
  content={[
  {
    name: 'config',
    type: 'BaseExporterConfig',
    description: '設定オプション',
    isOptional: true,
    properties: [
      {
        type: 'BaseExporterConfig',
        parameters: [
          {
            name: 'logger',
            type: 'IMastraLogger',
            description: 'Logger インスタンス（未指定の場合は INFO レベルの ConsoleLogger を使用）',
            isOptional: true,
          },
          {
            name: 'logLevel',
            type: "LogLevel | 'debug' | 'info' | 'warn' | 'error'",
            description: 'exporter のログレベル（デフォルト：INFO）',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

## プロパティ {/* #properties */}

```typescript prettier:false
readonly name = 'tracing-console-exporter';
```

## メソッド {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

Tracing イベントをコンソールへエクスポートします。

<PropertiesTable
  props={[
  {
    name: 'event',
    type: 'TracingEvent',
    description: 'エクスポートする Tracing イベント',
    required: true,
  },
]}
/>

### shutdown {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

シャットダウンメッセージをログに記録します。

## 出力形式 {/* #output-format */}

exporter はイベントタイプに応じて異なる形式で出力します。

### SPAN\_STARTED {/* #span_started */}

```
🚀 SPAN_STARTED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Trace ID: [trace id]
   Input: [formatted input]
   Attributes: [formatted attributes]
────────────────────────────────────────
```

### SPAN\_ENDED {/* #span_ended */}

```
✅ SPAN_ENDED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Duration: [duration]ms
   Trace ID: [trace id]
   Input: [formatted input]
   Output: [formatted output]
   Error: [formatted error if present]
   Attributes: [formatted attributes]
────────────────────────────────────────
```

### SPAN\_UPDATED {/* #span_updated */}

```
📝 SPAN_UPDATED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Trace ID: [trace id]
   Input: [formatted input]
   Output: [formatted output]
   Error: [formatted error if present]
   Updated Attributes: [formatted attributes]
────────────────────────────────────────
```

## 使用方法 {/* #usage */}

```typescript
import { ConsoleExporter } from '@mastra/observability'
import { ConsoleLogger, LogLevel } from '@mastra/core/logger'

// Use default logger (INFO level)
const exporter = new ConsoleExporter()

// Use custom log level
const exporter = new ConsoleExporter({
  logLevel: 'debug',
})

// Use custom logger instance
const customLogger = new ConsoleLogger({ level: LogLevel.DEBUG })
const exporterWithLogger = new ConsoleExporter({
  logger: customLogger,
})
```

## 実装の詳細 {/* #implementation-details */}

- 属性を 2 スペースのインデント付き JSON として整形します
- Span の所要時間をミリ秒単位で計算して表示します
- シリアライズエラーを適切に処理します
- 未実装のイベントタイプを警告としてログに記録します
- イベント間に 80 文字の区切り線を使用します

## 関連項目 {/* #see-also */}

### ドキュメント {/* #documentation */}

- [Tracing の概要](/docs/observability/tracing/overview)：完全ガイド
- [Exporters](/docs/observability/integrations/overview#exporters)：exporter の概念

### その他の Exporter {/* #other-exporters */}

- [MastraStorageExporter](/reference/observability/tracing/exporters/mastra-storage-exporter)：ストレージへの永続化
- [MastraPlatformExporter](/reference/observability/tracing/exporters/mastra-platform-exporter)：Mastra プラットフォーム
- [Langfuse](/reference/observability/tracing/exporters/langfuse)：Langfuse との統合
- [Braintrust](/reference/observability/tracing/exporters/braintrust)：Braintrust との統合

### リファレンス {/* #reference */}

- [設定](/reference/observability/tracing/configuration)：設定オプション
- [インターフェース](/reference/observability/tracing/interfaces)：型定義
