---
title: "リファレンス: ArizeExporter | Observability"
description: OpenInference を使用する Tracing 用 Arize exporter
packages:
  - "@mastra/arize"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# ArizeExporter

Tracing データを Arize Phoenix、Arize AX、または OpenInference セマンティック規約をサポートする OpenTelemetry 互換の Observability プラットフォームへ送信します。

## コンストラクター {/* #constructor */}

```typescript prettier:false
new ArizeExporter(config: ArizeExporterConfig)
```

## `ArizeExporterConfig` {/* #arizeexporterconfig */}

```typescript
type ArizeExporterConfig = Omit<OtelExporterConfig, 'provider'> & {
  // Phoenix / OpenTelemetry configuration
  endpoint?: string
  apiKey?: string

  // Arize AX configuration
  spaceId?: string

  // Common configuration
  projectName?: string
  headers?: Record<string, string>
}
```

`OtelExporterConfig`（`provider` を除く）を継承し、次の項目を含みます。

- `timeout?: number` - エクスポートのタイムアウト（ミリ秒、デフォルト：30000）
- `batchSize?: number` - バッチあたりの Span 数（デフォルト：512）
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - ログレベル（デフォルト：WARN）
- `resourceAttributes?: Record<string, any>` - カスタムリソース属性

### メタデータの引き渡し {/* #metadata-passthrough */}

予約されていない Span 属性は OpenInference の `metadata` ペイロードにシリアライズされます。`tracingOptions.metadata`（例：`companyId`、`tier`）から追加します。`input`、`output`、`sessionId`、thread/user ID、OpenInference ID などの予約フィールドは自動的に除外されます。

<PropertiesTable
  props={[
  {
    name: 'endpoint',
    type: 'string',
    description:
      'Trace エクスポート用 Collector エンドポイント。未指定の場合は PHOENIX_COLLECTOR_ENDPOINT 環境変数を使用します。Phoenix では必須です。',
    required: false,
  },
  {
    name: 'apiKey',
    type: 'string',
    description: '認証用 API キー。未指定の場合は ARIZE_API_KEY または PHOENIX_API_KEY 環境変数を使用します。',
    required: false,
  },
  {
    name: 'spaceId',
    type: 'string',
    description:
      'Arize AX の space 識別子。未指定の場合は ARIZE_SPACE_ID 環境変数を使用します。Arize AX では必須です。',
    required: false,
  },
  {
    name: 'projectName',
    type: 'string',
    description: 'プロジェクト名。未指定の場合は ARIZE_PROJECT_NAME または PHOENIX_PROJECT_NAME 環境変数を使用します。',
    required: false,
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: 'OTLP リクエストに追加するヘッダー',
    required: false,
  },
  {
    name: 'timeout',
    type: 'number',
    description: 'Span をエクスポートするまでのタイムアウト（ミリ秒、デフォルト：30000）',
    required: false,
  },
  {
    name: 'batchSize',
    type: 'number',
    description: 'エクスポート前にバッチ化する Span 数（デフォルト：512）',
    required: false,
  },
  {
    name: 'logLevel',
    type: "'debug' | 'info' | 'warn' | 'error'",
    description: "Logger のレベル（デフォルト：'warn'）",
    required: false,
  },
  {
    name: 'resourceAttributes',
    type: 'Record<string, any>',
    description: '各 Span に追加するカスタムリソース属性',
    required: false,
  },
]}
/>

## メソッド {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

Tracing イベントを設定済みのエンドポイントへエクスポートします。

### export {/* #export */}

```typescript prettier:false
async export(spans: ReadOnlySpan[]): Promise<void>
```

OpenInference セマンティック規約に従い、OpenTelemetry を使用して Span をバッチエクスポートします。

### flush {/* #flush */}

```typescript prettier:false
async flush(): Promise<void>
```

exporter をシャットダウンせず、バッファー内のすべての Span を設定済みエンドポイントへ強制的にフラッシュします。ランタイム終了前に Span が確実にエクスポートされるようにする必要がある、サーバーレス環境で役立ちます。

### shutdown {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

保留中のデータをフラッシュし、クライアントをシャットダウンします。

## 使用方法 {/* #usage */}

### ゼロコンフィグ（環境変数を使用） {/* #zero-config-using-environment-variables */}

```typescript
import { ArizeExporter } from '@mastra/arize'

// For Phoenix: Set PHOENIX_COLLECTOR_ENDPOINT, PHOENIX_API_KEY, PHOENIX_PROJECT_NAME
// For Arize AX: Set ARIZE_SPACE_ID, ARIZE_API_KEY, ARIZE_PROJECT_NAME
const exporter = new ArizeExporter()
```

### Phoenix の設定 {/* #phoenix-configuration */}

```typescript
import { ArizeExporter } from '@mastra/arize'

const exporter = new ArizeExporter({
  endpoint: 'http://localhost:6006/v1/traces',
  apiKey: process.env.PHOENIX_API_KEY, // Optional for local Phoenix
  projectName: 'my-ai-project',
})
```

### Arize AX の設定 {/* #arize-ax-configuration */}

```typescript
import { ArizeExporter } from '@mastra/arize'

const exporter = new ArizeExporter({
  spaceId: process.env.ARIZE_SPACE_ID!,
  apiKey: process.env.ARIZE_API_KEY!,
  projectName: 'my-ai-project',
})
```

## `OpenInference` セマンティック規約 {/* #openinference-semantic-conventions */}

ArizeExporter は [OpenInference セマンティック規約](https://github.com/Arize-ai/openinference/tree/main/spec) を生成 AI アプリケーション向けに実装し、異なる Observability プラットフォーム間で標準化された Trace 構造を提供します。

## タグのサポート {/* #tags-support */}

ArizeExporter は分類とフィルタリングのための Trace タグ付けをサポートします。タグはルート Span にのみ適用され、OpenInference ネイティブの `tag.tags` セマンティック規約にマッピングされます。

### 使用方法 {/* #usage-1 */}

```typescript
const result = await agent.generate('Hello', {
  tracingOptions: {
    tags: ['production', 'experiment-v2', 'user-request'],
  },
})
```

### タグの保存方法 {/* #how-tags-are-stored */}

タグは OpenInference の `tag.tags` 属性を使用して保存されます：

```json
{
  "tag.tags": ["production", "experiment-v2", "user-request"]
}
```

## 関連項目 {/* #related */}

- [ArizeExporter ドキュメント](/docs/observability/integrations/exporters/arize)
- [Phoenix ドキュメント](https://docs.arize.com/phoenix)
- [Arize AX ドキュメント](https://docs.arize.com/)
