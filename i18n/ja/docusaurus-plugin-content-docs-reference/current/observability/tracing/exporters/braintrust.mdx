---
title: "リファレンス：BraintrustExporter | Observability"
description: Tracing 用 Braintrust exporter
packages:
  - "@mastra/braintrust"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# BraintrustExporter

評価と Observability のために Tracing データを Braintrust へ送信します。

## コンストラクター

```typescript prettier:false
new BraintrustExporter(config: BraintrustExporterConfig)
```

## `BraintrustExporterConfig`

```typescript
interface BraintrustExporterConfig extends BaseExporterConfig {
  apiKey?: string
  endpoint?: string
  projectName?: string
  braintrustLogger?: Logger<true>
  currentSpan?: () => Span | undefined
  tuningParameters?: Record<string, any>
}
```

`BaseExporterConfig` を拡張し、次の項目を含みます。

- `logger?: IMastraLogger` - Logger インスタンス
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - ログレベル（デフォルト：INFO）

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Braintrust API キー。未指定の場合は BRAINTRUST_API_KEY 環境変数を使用します。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: 'カスタム Braintrust エンドポイント。未指定の場合は BRAINTRUST_ENDPOINT 環境変数を使用します。',
    required: false,
  },
  {
    name: 'projectName',
    type: 'string',
    description: "プロジェクト名（デフォルト：'mastra-tracing'）",
    required: false,
  },
  {
    name: 'braintrustLogger',
    type: 'Logger<true>',
    description: '評価およびトレース対象 Span のコンテキストを統合する Braintrust logger インスタンス。',
    required: false,
  },
  {
    name: 'currentSpan',
    type: '() => Span | undefined',
    description:
      'アプリ側の Braintrust パッケージインスタンスからのリゾルバー。Eval() または logger.traced() の Span が、別途インストールされた Braintrust SDK のコピーによって作成される場合に braintrustLogger とともに使用します。',
    required: false,
  },
  {
    name: 'logLevel',
    type: "'debug' | 'info' | 'warn' | 'error'",
    description: "Logger のレベル（デフォルト：'warn'）",
    required: false,
  },
  {
    name: 'tuningParameters',
    type: 'Record<string, any>',
    description: 'Braintrust のチューニングパラメーター',
    required: false,
  },
]}
/>

## メソッド

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

Tracing イベントを Braintrust へエクスポートします。

### export

```typescript prettier:false
async export(spans: ReadOnlySpan[]): Promise<void>
```

Span を Braintrust へバッチエクスポートします。

### flush

```typescript prettier:false
async flush(): Promise<void>
```

exporter をシャットダウンせず、バッファー内のすべての Span を Braintrust へ強制的にフラッシュします。ランタイム終了前に Span が確実にエクスポートされるようにする必要がある、サーバーレス環境で役立ちます。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

保留中のデータをフラッシュし、クライアントをシャットダウンします。

## 使用方法

### ゼロコンフィグ（環境変数を使用）

```typescript
import { BraintrustExporter } from '@mastra/braintrust'

// Reads from BRAINTRUST_API_KEY, BRAINTRUST_ENDPOINT
const exporter = new BraintrustExporter()
```

### 明示的な設定

```typescript
import { BraintrustExporter } from '@mastra/braintrust'

const exporter = new BraintrustExporter({
  apiKey: process.env.BRAINTRUST_API_KEY,
  projectName: 'my-ai-project',
})
```

## Span タイプのマッピング

| Span タイプ | Braintrust タイプ |
| - | - |
| `MODEL_GENERATION` | `llm` |
| `MODEL_CHUNK` | `llm` |
| `TOOL_CALL` | `tool` |
| `MCP_TOOL_CALL` | `tool` |
| `PROVIDER_TOOL_CALL` | `tool` |
| `WORKFLOW_CONDITIONAL_EVAL` | `function` |
| `WORKFLOW_WAIT_EVENT` | `function` |
| その他すべて | `task` |
