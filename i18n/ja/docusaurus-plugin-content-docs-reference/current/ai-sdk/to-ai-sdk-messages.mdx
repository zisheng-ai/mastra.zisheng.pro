---
title: "リファレンス：toAISdkMessages() | AI SDK"
description: Mastra のメッセージを AI SDK v5 または v6 の UI メッセージに変換する関数、toAISdkMessages() の API リファレンスです。
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdkMessages() {/* #toaisdkmessages */}

各種入力形式のメッセージを AI SDK の UI メッセージ形式に変換します。`toAISdkMessages()` では、既存の AI SDK v5 のデフォルト動作が維持されます。アプリが AI SDK v6 に対して型付けされている場合は、`version: 'v6'` を渡してください。

`version: 'v6'` では、MessageList の AI SDK v6 UI 出力パスが使用されます。MessageList の入力検出と取り込みは変更されません。

## 使用例 {/* #usage-example */}

```typescript title="app/chat/page.tsx"
import { toAISdkMessages } from '@mastra/ai-sdk/ui'
import { useChat } from 'ai/react'

// Stored messages from your database, memory, or API
const storedMessages = [
  { id: '1', role: 'user', content: 'Hello', parts: [{ type: 'text', text: 'Hello' }] },
  {
    id: '2',
    role: 'assistant',
    content: 'Hi there!',
    parts: [{ type: 'text', text: 'Hi there!' }],
  },
]

export default function Chat() {
  const { messages } = useChat({
    initialMessages: toAISdkMessages(storedMessages, { version: 'v6' }),
  })

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role}: {message.parts.map(part => (part.type === 'text' ? part.text : null))}
        </div>
      ))}
    </div>
  )
}
```

## パラメーター {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'MessageListInput',
    description:
      '変換するメッセージ。文字列、文字列の配列、単一のメッセージオブジェクト、またはサポートされている任意の形式のメッセージオブジェクト配列を指定できます。',
    isOptional: false,
  },
  {
    name: 'options.version',
    type: "'v5' | 'v6'",
    description:
      "返す AI SDK のメッセージ型を選択します。既存のデフォルト動作を使用するには、省略するか `'v5'` を渡します。アプリが AI SDK v6 の `useChat()` メッセージ型に対して型付けされている場合は、`'v6'` を渡します。",
    isOptional: true,
  },
]}
/>

## 戻り値 {/* #returns */}

選択したバージョンに対応する型の AI SDK `UIMessage` オブジェクトの配列を返します。

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'メッセージの一意な識別子。',
  },
  {
    name: 'role',
    type: "'user' | 'assistant' | 'system'",
    description: 'メッセージ送信者のロール。',
  },
  {
    name: 'parts',
    type: 'UIMessagePart[]',
    description: 'テキスト、Tool の結果、ファイル、推論、ソース、ステップマーカーを含む UI パーツの配列。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'createdAt、threadId、resourceId、カスタムフィールドを含む任意のメタデータ。',
    isOptional: true,
  },
]}
/>

## 例 {/* #examples */}

### デフォルトの AI SDK v5 型を使用する {/* #using-the-default-ai-sdk-v5-types */}

```typescript
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const messages = toAISdkMessages(['Hello', 'How can I help you today?'])
```

### AI SDK v6 のメッセージ型を返す {/* #returning-ai-sdk-v6-message-types */}

```typescript
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const messages = toAISdkMessages(['Hello', 'How can I help you today?'], {
  version: 'v6',
})
```

### Mastra クライアントでメッセージを読み込む {/* #loading-messages-with-mastra-client */}

```typescript
import { MastraClient } from '@mastra/client-js'
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const client = new MastraClient()

const { messages } = await client.listThreadMessages('thread-id', { agentId: 'myAgent' })
const uiMessages = toAISdkMessages(messages)
```

## 関連項目 {/* #related */}

- [`toAISdkStream()`](/reference/ai-sdk/to-ai-sdk-stream)
- [`toAISdkV5Messages()`](/reference/ai-sdk/to-ai-sdk-v5-messages)
- [`toAISdkV4Messages()`](/reference/ai-sdk/to-ai-sdk-v4-messages)
