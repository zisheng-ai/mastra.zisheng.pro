---
title: "リファレンス: withMastra() | AI SDK"
description: AI SDK で Mastra の機能を使用するための関数、withMastra() の API リファレンスです。
packages:
  - "@mastra/ai-sdk"
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# withMastra() {/* #withmastra */}

AI SDK モデルを Mastra の Processor や Memory でラップします。

## 使用例 {/* #usage-example */}

```typescript title="src/example.ts"
import { openai } from '@ai-sdk/openai'
import { generateText } from 'ai'
import { withMastra } from '@mastra/ai-sdk'
import type { Processor } from '@mastra/core/processors'

const loggingProcessor: Processor<'logger'> = {
  id: 'logger',
  async processInput({ messages }) {
    console.log('Input:', messages.length, 'messages')
    return messages
  },
}

const model = withMastra(openai('gpt-5.4'), {
  inputProcessors: [loggingProcessor],
})

const { text } = await generateText({
  model,
  prompt: 'What is 2 + 2?',
})
```

## パラメーター {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'model',
    type: 'LanguageModelV2 | LanguageModelV3',
    description: "任意の AI SDK v5 または v6 言語モデル（例: `openai('gpt-5.4')`、`anthropic('claude-opus-4-6')`）。",
    isOptional: false,
  },
  {
    name: 'options',
    type: 'WithMastraOptions',
    description: 'Processor と Memory の設定オブジェクト。',
    isOptional: true,
    properties: [
      {
        type: 'WithMastraOptions',
        parameters: [
          {
            name: 'inputProcessors',
            type: 'InputProcessor[]',
            description: 'LLM の呼び出し前に実行する入力 Processor。',
            isOptional: true,
          },
          {
            name: 'outputProcessors',
            type: 'OutputProcessor[]',
            description: 'LLM のレスポンスに対して実行する出力 Processor。',
            isOptional: true,
          },
          {
            name: 'memory',
            type: 'WithMastraMemoryOptions',
            description: 'Memory の設定。メッセージ履歴の自動永続化を有効にします。',
            isOptional: true,
            properties: [
              {
                type: 'WithMastraMemoryOptions',
                parameters: [
                  {
                    name: 'storage',
                    type: 'MemoryStorage',
                    description:
                      "メッセージを永続化するための Memory ストレージドメイン。複合ストアから `await storage.getStore('memory')` を使用して取得します。",
                    isOptional: false,
                  },
                  {
                    name: 'threadId',
                    type: 'string',
                    description: '会話を永続化するためのスレッド ID。',
                    isOptional: false,
                  },
                  {
                    name: 'resourceId',
                    type: 'string',
                    description: 'リソース ID（ユーザーまたはセッションの識別子）。',
                    isOptional: true,
                  },
                  {
                    name: 'lastMessages',
                    type: 'number | false',
                    description: '取得する直近のメッセージ数。無効にする場合は false。',
                    isOptional: true,
                  },
                  {
                    name: 'semanticRecall',
                    type: 'WithMastraSemanticRecallOptions',
                    description: 'セマンティックリコールの設定（RAG ベースの Memory 取得）。',
                    isOptional: true,
                  },
                  {
                    name: 'workingMemory',
                    type: "MemoryConfig['workingMemory']",
                    description: 'Working Memory の設定（永続化されるユーザーデータ）。',
                    isOptional: true,
                  },
                  {
                    name: 'readOnly',
                    type: 'boolean',
                    description: '読み取り専用モード。新しいメッセージの保存を防ぎます。',
                    isOptional: true,
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## 戻り値 {/* #returns */}

`generateText`、`streamText`、`generateObject`、`streamObject` と互換性のある、ラップされたモデル。

## ストリーミングの動作 {/* #streaming-behavior */}

`processOutputResult` を実装する出力 Processor は、ストリームの完了後に実行されます。メッセージ履歴とセマンティックリコールを永続化するには、ストリームを最後まで消費してください。
