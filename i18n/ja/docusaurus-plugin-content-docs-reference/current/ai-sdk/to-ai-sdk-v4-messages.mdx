---
title: "リファレンス：toAISdkV4Messages() | AI SDK"
description: Mastra のメッセージを AI SDK v4 の UI メッセージに変換する関数、toAISdkV4Messages() の API リファレンスです。
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdkV4Messages()

各種入力形式のメッセージを AI SDK V4 の UI メッセージ形式に変換します。この関数は、複数の形式（文字列、AI SDK V4/V5 メッセージ、Mastra DB メッセージなど）のメッセージを受け取り、`useChat()` などの AI SDK UI コンポーネントでの使用に適した AI SDK V4 の `UIMessage` 形式に正規化します。

## 使用例

```typescript title="app/chat/page.tsx"
import { toAISdkV4Messages } from '@mastra/ai-sdk'
import { useChat } from 'ai/react' // AI SDK V4

// Stored messages from your database, memory or API
const storedMessages = [
  { id: '1', role: 'user', parts: [{ type: 'text', text: 'Hello' }] },
  { id: '2', role: 'assistant', parts: [{ type: 'text', text: 'Hi there!' }] },
]

export default function Chat() {
  const { messages } = useChat({
    initialMessages: toAISdkV4Messages(storedMessages),
  })

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role}: {message.content}
        </div>
      ))}
    </div>
  )
}
```

## パラメーター

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'MessageListInput',
    description:
      '変換するメッセージ。文字列、文字列の配列、単一のメッセージオブジェクト、またはサポートされている任意の形式のメッセージオブジェクト配列を指定できます。',
    isOptional: false,
  },
]}
/>

## 戻り値

次の構造を持つ AI SDK V4 `UIMessage` オブジェクトの配列を返します。

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'メッセージの一意な識別子。',
  },
  {
    name: 'role',
    type: "'user' | 'assistant' | 'system'",
    description: 'メッセージ送信者のロール。',
  },
  {
    name: 'content',
    type: 'string',
    description: 'メッセージのテキストコンテンツ。',
  },
  {
    name: 'parts',
    type: 'UIMessagePart[]',
    description: 'テキスト、Tool 呼び出し、ファイル、推論、ソース、ステップマーカーを含む UI パーツの配列。',
  },
  {
    name: 'createdAt',
    type: 'Date',
    description: 'メッセージの作成日時。',
  },
  {
    name: 'toolInvocations',
    type: 'ToolInvocation[]',
    description: 'アシスタントメッセージに含まれる Tool 呼び出しの配列。',
    isOptional: true,
  },
  {
    name: 'experimental_attachments',
    type: 'Attachment[]',
    description: 'メッセージに添付されたファイル。',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'メッセージに付加されたカスタムメタデータ。',
    isOptional: true,
  },
]}
/>

## 例

### テキストメッセージを変換する

```typescript
import { toAISdkV4Messages } from '@mastra/ai-sdk'

const messages = toAISdkV4Messages(['Hello', 'How can I help you today?'])
// Returns array of UIMessage objects with user role and content string
```

### Mastra クライアントでメッセージを読み込む

```typescript
import { MastraClient } from '@mastra/client-js'
import { toAISdkV4Messages } from '@mastra/ai-sdk'

const client = new MastraClient()

const { messages } = await client.listThreadMessages('thread-id', { agentId: 'myAgent' })
const uiMessages = toAISdkV4Messages(messages)
```
