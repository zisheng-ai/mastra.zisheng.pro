---
title: "リファレンス：Memory.deleteMessages() | Memory"
description: "複数のメッセージを ID で削除する、Mastra の `Memory.deleteMessages()` メソッドのドキュメントです。"
packages:
  - "@mastra/core"
---

# Memory.deleteMessages()

`.deleteMessages()` メソッドは、複数のメッセージを ID で削除します。

## 使用例

```typescript
await memory?.deleteMessages(['671ae63f-3a91-4082-a907-fe7de78e10ec'])
```

## パラメーター

<PropertiesTable
  content={[
  {
    name: 'messageIds',
    type: 'string[]',
    description: '削除するメッセージ ID の配列',
    isOptional: false,
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'void',
    type: 'Promise<void>',
    description: 'すべてのメッセージが削除されると解決する Promise',
  },
]}
/>

## 詳細な使用例

```typescript title="src/test-memory.ts"
import { mastra } from './mastra'
import { MastraDBMessage } from '@mastra/core'

const agent = mastra.getAgent('agent')
const memory = await agent.getMemory()

const { messages } = await memory!.recall({ threadId: 'thread-123' })

const messageIds = messages.map((message: MastraDBMessage) => message.id)
await memory?.deleteMessages([...messageIds])
```

## 関連情報

- [Memory クラスのリファレンス](/reference/memory/memory-class)
- [recall](/reference/memory/recall)
- [Memory の概要](/docs/memory/overview)
