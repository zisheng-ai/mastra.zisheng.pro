---
title: "リファレンス：Memory.createThread() | Memory"
description: "Memory システムに新しい会話 Thread を作成する、Mastra の `Memory.createThread()` メソッドのドキュメントです。"
packages:
  - "@mastra/memory"
---

# Memory.createThread()

`.createThread()` メソッドは、Memory システムに新しい会話 Thread を作成します。各 Thread は個別の会話またはコンテキストを表し、複数のメッセージを含めることができます。

## 使用例

```typescript
await memory?.createThread({ resourceId: 'user-123' })
```

## パラメーター

<PropertiesTable
  content={[
  {
    name: 'resourceId',
    type: 'string',
    description: 'この Thread が属するリソースの識別子（例：ユーザー ID、プロジェクト ID）',
    isOptional: false,
  },
  {
    name: 'threadId',
    type: 'string',
    description: 'Thread に使用する省略可能なカスタム ID。指定しない場合は生成されます。',
    isOptional: true,
  },
  {
    name: 'title',
    type: 'string',
    description: 'Thread の省略可能なタイトル',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'Thread に関連付ける省略可能なメタデータ',
    isOptional: true,
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '作成された Thread の一意の識別子',
  },
  {
    name: 'resourceId',
    type: 'string',
    description: 'Thread に関連付けられたリソース ID',
  },
  {
    name: 'title',
    type: 'string',
    description: 'Thread のタイトル（指定した場合）',
  },
  {
    name: 'createdAt',
    type: 'Date',
    description: 'Thread が作成された日時',
  },
  {
    name: 'updatedAt',
    type: 'Date',
    description: 'Thread が最後に更新された日時',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: 'Thread に関連付けられた追加のメタデータ',
  },
]}
/>

## 詳細な使用例

```typescript title="src/test-memory.ts"
import { mastra } from './mastra'

const agent = mastra.getAgent('agent')
const memory = await agent.getMemory()

const thread = await memory?.createThread({
  resourceId: 'user-123',
  title: 'Memory Test Thread',
  metadata: {
    source: 'test-script',
    purpose: 'memory-testing',
  },
})

const response = await agent.generate('message for agent', {
  memory: {
    thread: thread!.id,
    resource: thread!.resourceId,
  },
})

console.log(response.text)
```

### 関連情報

- [Memory クラスのリファレンス](/reference/memory/memory-class)
- [Memory の概要](/docs/memory/overview)（Thread の概念について説明します）
- [getThreadById](/reference/memory/getThreadById)
- [listThreads](/reference/memory/listThreads)
- [recall](/reference/memory/recall)
