---
title: "リファレンス：Inworld | Voice"
description: "Inworld AI を使用したストリーミング Text-to-Speech およびバッチ Speech-to-Text 機能を提供する InworldVoice クラスのドキュメントです。"
packages:
  - "@mastra/voice-inworld"
---

# Inworld

Mastra の Inworld Voice 実装は、Inworld AI API を使用したストリーミング Text-to-Speech（TTS）およびバッチ Speech-to-Text（STT）機能を提供します。複数の TTS・STT モデル、設定可能な音声エンコーディング、プログレッシブ音声ストリーミングをサポートします。

リアルタイムの全二重音声間通信向けに、同じパッケージから [`InworldRealtimeVoice`](/reference/voice/inworld-realtime) もエクスポートされています。

## 使用例 {/* #usage-example */}

```typescript
import { InworldVoice } from '@mastra/voice-inworld'

// Initialize with default configuration (uses INWORLD_API_KEY environment variable)
const voice = new InworldVoice()

// Initialize with custom configuration
const voice = new InworldVoice({
  speechModel: {
    name: 'inworld-tts-2',
    apiKey: 'your-api-key',
  },
  listeningModel: {
    name: 'groq/whisper-large-v3',
    apiKey: 'your-api-key',
  },
  speaker: 'Dennis',
})

// Text-to-Speech (streaming)
const audioStream = await voice.speak('Hello, world!')

// Speech-to-Text
const transcript = await voice.listen(audioStream)
```

## コンストラクターパラメーター {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'InworldVoiceConfig',
    description: 'Text-to-Speech 機能の設定。',
    isOptional: true,
    defaultValue: "{ name: 'inworld-tts-2' }",
    properties: [
      {
        type: 'InworldVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: "'inworld-tts-2' | 'inworld-tts-1.5-max' | 'inworld-tts-1.5-mini'",
            description: '使用する Inworld TTS モデル。',
            isOptional: true,
            defaultValue: "'inworld-tts-2'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Inworld API キー。未指定の場合は INWORLD_API_KEY 環境変数を使用します。',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'listeningModel',
    type: 'InworldListeningConfig',
    description: 'Speech-to-Text 機能の設定。',
    isOptional: true,
    defaultValue: "{ name: 'groq/whisper-large-v3' }",
    properties: [
      {
        type: 'InworldListeningConfig',
        parameters: [
          {
            name: 'name',
            type: "'groq/whisper-large-v3'",
            description: '使用する Inworld STT モデル。',
            isOptional: true,
            defaultValue: "'groq/whisper-large-v3'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Inworld API キー。未指定の場合は INWORLD_API_KEY 環境変数を使用します。',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: 'Text-to-Speech に使用するデフォルトの Voice ID。',
    isOptional: true,
    defaultValue: "'Dennis'",
  },
  {
    name: 'audioEncoding',
    type: "'LINEAR16' | 'MP3' | 'OGG_OPUS' | 'ALAW' | 'MULAW' | 'FLAC' | 'PCM' | 'WAV'",
    description: 'TTS 出力のデフォルト音声エンコーディング。',
    isOptional: true,
    defaultValue: "'MP3'",
  },
  {
    name: 'sampleRateHertz',
    type: 'number',
    description: 'TTS 出力のデフォルトサンプルレート。',
    isOptional: true,
    defaultValue: '48000',
  },
  {
    name: 'language',
    type: 'string',
    description: 'STT のデフォルト BCP-47 言語コード。',
    isOptional: true,
    defaultValue: "'en-US'",
  },
]}
/>

## メソッド {/* #methods */}

### `speak(input, options?)` {/* #speakinput-options */}

Inworld のストリーミング TTS エンドポイントを使用してテキストを音声に変換します。到着した音声チャンクを順次送出する読み取り可能なストリームを返します。

```typescript
const audioStream = await voice.speak('Hello, world!', {
  speaker: 'Olivia',
  audioEncoding: 'WAV',
  sampleRateHertz: 24000,
  speakingRate: 1.2,
  temperature: 0.8,
})
```

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '音声に変換するテキスト。ストリームを指定した場合は、先にテキストへ変換されます。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'InworldSpeakOptions',
    description: '音声合成の追加オプション。',
    isOptional: true,
    properties: [
      {
        type: 'InworldSpeakOptions',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: 'このリクエストでデフォルトの Speaker を上書きします。',
            isOptional: true,
          },
          {
            name: 'audioEncoding',
            type: 'AudioEncoding',
            description: 'デフォルトの音声エンコーディングを上書きします。',
            isOptional: true,
          },
          {
            name: 'sampleRateHertz',
            type: 'number',
            description: 'デフォルトのサンプルレートを上書きします。',
            isOptional: true,
          },
          {
            name: 'speakingRate',
            type: 'number',
            description: '発話速度を調整します。',
            isOptional: true,
          },
          {
            name: 'temperature',
            type: 'number',
            description:
              'Voice の変動性を制御します。`inworld-tts-1.5-*` モデルでは有効ですが、`inworld-tts-2` では無視されます。',
            isOptional: true,
          },
          {
            name: 'deliveryMode',
            type: "'STABLE' | 'BALANCED' | 'CREATIVE'",
            description: '表現スタイルを調整する制御。`inworld-tts-2` だけで有効です。',
            isOptional: true,
          },
          {
            name: 'language',
            type: 'string',
            description: 'このリクエストの BCP-47 言語コード。省略すると自動検出されます。',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

**戻り値：** `Promise<NodeJS.ReadableStream>`

### `listen(input, options?)` {/* #listeninput-options */}

Inworld のバッチ STT エンドポイントを使用して音声をテキストに変換します。

```typescript
const transcript = await voice.listen(audioStream, {
  audioEncoding: 'MP3',
  sampleRateHertz: 44100,
  language: 'ja-JP',
})
```

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'NodeJS.ReadableStream',
    description: '文字起こしする音声ストリーム。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'InworldListenOptions',
    description: '文字起こしの追加オプション。',
    isOptional: true,
    properties: [
      {
        type: 'InworldListenOptions',
        parameters: [
          {
            name: 'audioEncoding',
            type: "'LINEAR16' | 'MP3' | 'OGG_OPUS' | 'FLAC' | 'AUTO_DETECT'",
            description: '入力ストリームの音声エンコーディング。',
            isOptional: true,
            defaultValue: "'AUTO_DETECT'",
          },
          {
            name: 'sampleRateHertz',
            type: 'number',
            description: '入力音声のサンプルレート。',
            isOptional: true,
            defaultValue: '16000',
          },
          {
            name: 'language',
            type: 'string',
            description: '文字起こしの BCP-47 言語コード。',
            isOptional: true,
            defaultValue: "'en-US'",
          },
          {
            name: 'numberOfChannels',
            type: 'number',
            description: '入力の音声チャンネル数。',
            isOptional: true,
            defaultValue: '1',
          },
        ],
      },
    ],
  },
]}
/>

**戻り値：** `Promise<string>`

### `getSpeakers()` {/* #getspeakers */}

Inworld API から使用可能な Voice の一覧を返します。

```typescript
const speakers = await voice.getSpeakers()
// [{ voiceId: 'Dennis', name: 'Dennis', language: 'en', description: '...', tags: ['friendly'], source: 'SYSTEM' }, ...]
```

**戻り値：** `Promise<Array<{ voiceId: string; name: string; language: string; description: string; tags: string[]; source: string }>>`

## 注意事項 {/* #notes */}

- TTS エンドポイントはプログレッシブ NDJSON ストリーミングを使用するため、完全な応答を受信する前に音声の再生を開始できます。
- API キーは `speechModel` または `listeningModel` の設定、あるいは `INWORLD_API_KEY` 環境変数で指定できます。TTS と STT のキーは個別に解決されます。`speechModel.apiKey` と `listeningModel.apiKey` に異なる値を渡すと、各サービスで独自の認証情報を使用できます。一方だけを指定した場合は、環境変数を参照する前のフォールバックとして両方のサービスで再利用されます。
- `inworld-tts-2` はデフォルトのフラッグシップモデルです。このモデルの表現スタイルを調整するには、`deliveryMode`（`STABLE` | `BALANCED` | `CREATIVE`）を使用します。`temperature` オプションは `inworld-tts-2` では無視されます。
- `inworld-tts-1.5-mini` モデルは、`inworld-tts-1.5-max` と比べて Voice 品質が低下する代わりに、より低いレイテンシーを実現します。
