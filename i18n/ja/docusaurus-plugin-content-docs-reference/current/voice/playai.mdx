---
title: "リファレンス：PlayAI | Voice"
description: "Text-to-Speech 機能を提供する PlayAI Voice 実装のドキュメントです。"
packages:
  - "@mastra/voice-playai"
---

# PlayAI

Mastra の PlayAI Voice 実装は、PlayAI API を使用した Text-to-Speech 機能を提供します。

## 使用例

```typescript
import { PlayAIVoice } from '@mastra/voice-playai'

// Initialize with default configuration (uses PLAYAI_API_KEY environment variable and PLAYAI_USER_ID environment variable)
const voice = new PlayAIVoice()

// Initialize with default configuration
const voice = new PlayAIVoice({
  speechModel: {
    name: 'PlayDialog',
    apiKey: process.env.PLAYAI_API_KEY,
    userId: process.env.PLAYAI_USER_ID,
  },
  speaker: 'Angelo', // Default voice
})

// Convert text to speech with a specific voice
const audioStream = await voice.speak('Hello, world!', {
  speaker:
    's3://voice-cloning-zero-shot/b27bc13e-996f-4841-b584-4d35801aea98/original/manifest.json', // Dexter voice
})
```

## コンストラクターパラメーター

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'PlayAIConfig',
    description: 'Text-to-Speech 機能の設定',
    isOptional: true,
    defaultValue: "{ name: 'PlayDialog' }",
    properties: [
      {
        type: 'PlayAIConfig',
        parameters: [
          {
            name: 'name',
            type: "'PlayDialog' | 'Play3.0-mini'",
            description: '使用する PlayAI モデル',
            isOptional: true,
            defaultValue: "'PlayDialog'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'PlayAI API キー。未指定の場合は PLAYAI_API_KEY 環境変数を使用します',
            isOptional: true,
          },
          {
            name: 'userId',
            type: 'string',
            description: 'PlayAI ユーザー ID。未指定の場合は PLAYAI_USER_ID 環境変数を使用します',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: '音声合成に使用するデフォルトの Voice ID',
    isOptional: true,
    defaultValue: 'First available voice ID',
  },
]}
/>

## メソッド

### `speak()`

設定された音声モデルと Voice を使用してテキストを音声に変換します。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '音声に変換するテキスト。ストリームを指定した場合は、先にテキストへ変換されます。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'Options',
    description: '設定オプション。',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: 'このリクエストでデフォルトの Speaker を上書きします',
            isOptional: true,
            defaultValue: "Constructor's speaker value",
          },
        ],
      },
    ],
  },
]}
/>

戻り値：`Promise<NodeJS.ReadableStream>`。

### `getSpeakers()`

使用可能な Voice オプションの配列を返します。各要素には次の値が含まれます。

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: 'Voice 名',
    isOptional: false,
  },
  {
    name: 'accent',
    type: 'string',
    description: "Voice のアクセント（例：'US'、'British'、'Australian'）",
    isOptional: false,
  },
  {
    name: 'gender',
    type: "'M' | 'F'",
    description: 'Voice の性別',
    isOptional: false,
  },
  {
    name: 'age',
    type: "'Young' | 'Middle' | 'Old'",
    description: 'Voice の年齢カテゴリー',
    isOptional: false,
  },
  {
    name: 'style',
    type: "'Conversational' | 'Narrative'",
    description: 'Voice の発話スタイル',
    isOptional: false,
  },
  {
    name: 'voiceId',
    type: 'string',
    description: 'Voice の一意な識別子',
    isOptional: false,
  },
]}
/>

### `listen()`

このメソッドは PlayAI でサポートされておらず、エラーをスローします。PlayAI は Speech-to-Text 機能を提供しません。

## 注意事項

- PlayAI の認証には API キーとユーザー ID の両方が必要です
- このサービスは、'PlayDialog' と 'Play3.0-mini' の2つのモデルを提供します
- 各 Voice には一意な S3 マニフェスト ID があり、API 呼び出しで使用する必要があります
