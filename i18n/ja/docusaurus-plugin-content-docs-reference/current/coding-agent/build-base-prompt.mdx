---
title: "リファレンス: buildBasePrompt() | Coding Agent"
description: "PromptContext から Coding Agent 共通のベースシステムプロンプトを構築する buildBasePrompt() の API リファレンスです。"
packages:
  - "@mastra/core"
---

# buildBasePrompt()

`buildBasePrompt()` は、Coding Agent を優れたコーディングアシスタントとして動作させるための指示である、共通のベースシステムプロンプトを構築します。現在の環境（プロジェクト、プラットフォーム、Git ブランチ、モード、モデル）を記述する `PromptContext` を受け取り、プロンプトを文字列として返します。

製品固有の文字列は `productName`、`coAuthorName`、`coAuthorEmail` によってパラメーター化されているため、フォークせずにプロンプトやコミットトレーラーのブランドを変更できます。デフォルトは `"Mastra Code"` / `"noreply@mastra.ai"` なので、既存の呼び出し元では同一の出力が維持されます。

Agent の指示を実行時に定義して構築する場合は、[`createCodingAgent()`](/reference/coding-agent/create-coding-agent) と組み合わせて使用します。

## 使用例

`PromptContext` からベースプロンプトを構築します。

```typescript title="src/mastra/prompt.ts"
import { buildBasePrompt } from '@mastra/core/coding-agent'

const prompt = buildBasePrompt({
  projectPath: process.cwd(),
  projectName: 'my-app',
  gitBranch: 'main',
  platform: process.platform,
  date: new Date().toDateString(),
  mode: 'build',
  modelId: '__GATEWAY_OPENAI_MODEL_BASE__',
  toolGuidance: '',
})
```

プロンプトのブランドを変更するには、製品と共同作成者のフィールドを渡します。

```typescript
const prompt = buildBasePrompt({
  // ...environment fields
  productName: 'Acme Coder',
  coAuthorName: 'Acme Bot',
  coAuthorEmail: 'bot@acme.dev',
})
```

## パラメーター

`buildBasePrompt()` は 1 つの `PromptContext` オブジェクトを受け取ります。

<PropertiesTable
  content={[
  {
    name: 'projectPath',
    type: 'string',
    description: 'プロジェクトルートの絶対パス。',
  },
  {
    name: 'projectName',
    type: 'string',
    description: 'プロジェクトの表示名。',
  },
  {
    name: 'gitBranch',
    type: 'string',
    description: 'プロジェクトが Git リポジトリの場合の、現在の Git ブランチ。',
    isOptional: true,
  },
  {
    name: 'platform',
    type: 'string',
    description: 'オペレーティングシステムのプラットフォーム（例: process.platform の値）。',
  },
  {
    name: 'commonBinaries',
    type: '{ name: string; path: string | null }[]',
    description:
      'システムで検出された一般的なバイナリ。それぞれに名前と解決済みのパス（見つからない場合は null）が含まれます。',
    isOptional: true,
  },
  {
    name: 'date',
    type: 'string',
    description: 'プロンプトに挿入する現在の日付文字列。',
  },
  {
    name: 'mode',
    type: 'string',
    description: 'アクティブな Agent モード（例: "build"、"plan"）。',
  },
  {
    name: 'modelId',
    type: 'string',
    description: 'アクティブなモデルの識別子。指定すると、コミットの Co-Authored-By 行に含まれます。',
    isOptional: true,
  },
  {
    name: 'activePlan',
    type: '{ title: string; plan: string; approvedAt: string } | null',
    description: '現在承認されている計画（存在する場合）。',
    isOptional: true,
  },
  {
    name: 'toolGuidance',
    type: 'string',
    description: 'プロンプトに追加する Tool 使用ガイダンスのセクション。',
  },
  {
    name: 'productName',
    type: 'string',
    description: 'プロンプトのヘッダーで使用する表示名。',
    isOptional: true,
    defaultValue: 'Mastra Code',
  },
  {
    name: 'coAuthorName',
    type: 'string',
    description: 'コミットの Co-Authored-By 行で使用する名前。',
    isOptional: true,
    defaultValue: 'Mastra Code',
  },
  {
    name: 'coAuthorEmail',
    type: 'string',
    description: 'コミットの Co-Authored-By 行で使用するメールアドレス。',
    isOptional: true,
    defaultValue: 'noreply@mastra.ai',
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'prompt',
    type: 'string',
    description: 'Coding Agent のベースシステムプロンプト。',
  },
]}
/>

## 関連項目

- [`createCodingAgent()`](/reference/coding-agent/create-coding-agent)
