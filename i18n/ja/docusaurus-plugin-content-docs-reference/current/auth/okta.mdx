---
title: "リファレンス：MastraAuthOkta と MastraRBACOkta | Auth"
description: "Okta を使用して Mastra アプリケーションの認証と認可を行う MastraAuthOkta クラスと MastraRBACOkta クラスの API リファレンスです。"
packages:
  - "@mastra/auth-okta"
  - "@mastra/core"
---

# MastraAuthOkta クラスと MastraRBACOkta クラス

## MastraAuthOkta クラス {/* #mastraauthokta-class */}

`MastraAuthOkta` クラスは、Okta を使用して Mastra の認証を提供します。暗号化されたセッション Cookie を使用する OAuth 2.0 / OIDC ログインフローを実装し、`auth` オプションを介して Mastra サーバーと統合します。

### 使用例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthOkta } from '@mastra/auth-okta'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthOkta({
      domain: process.env.OKTA_DOMAIN,
      clientId: process.env.OKTA_CLIENT_ID,
      clientSecret: process.env.OKTA_CLIENT_SECRET,
      redirectUri: process.env.OKTA_REDIRECT_URI,
    }),
  },
})
```

:::note

必要な環境変数が設定されている場合は、コンストラクターパラメーターを省略できます。その場合は、引数なしで `new MastraAuthOkta()` を使用します。

:::

### コンストラクターパラメーター {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'domain',
    type: 'string',
    description: 'Okta ドメイン（例：`dev-123456.okta.com`）。issuer URL と API エンドポイントの構築に使用されます。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_DOMAIN',
  },
  {
    name: 'clientId',
    type: 'string',
    description: 'Okta アプリケーションの OAuth クライアント ID。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_CLIENT_ID',
  },
  {
    name: 'clientSecret',
    type: 'string',
    description: 'OAuth クライアントシークレット。SSO 認可コードフローに必要です。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_CLIENT_SECRET',
  },
  {
    name: 'issuer',
    type: 'string',
    description: 'トークン発行者の URL。カスタム認可サーバーを使用する場合は、この値を上書きします。',
    isOptional: true,
    defaultValue: '`https://{domain}/oauth2/default`',
  },
  {
    name: 'redirectUri',
    type: 'string',
    description:
      'SSO コールバック用の OAuth リダイレクト URI。Okta アプリケーションに設定したリダイレクト URI と一致する必要があります。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_REDIRECT_URI',
  },
  {
    name: 'scopes',
    type: 'string[]',
    description: 'ログインフロー中に要求する OAuth スコープ。',
    isOptional: true,
    defaultValue: "['openid', 'profile', 'email', 'groups']",
  },
  {
    name: 'apiToken',
    type: 'string',
    description:
      'Users API を介したユーザー検索用の Okta API トークン。`getUser()` が ID に基づいてユーザーデータを返すために必要です。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_API_TOKEN',
  },
  {
    name: 'session',
    type: 'OktaSessionOptions',
    description: 'セッション Cookie の設定。',
    isOptional: true,
    properties: [
      {
        type: 'OktaSessionOptions',
        parameters: [
          {
            name: 'cookieName',
            type: 'string',
            description: 'セッション Cookie の名前。',
            isOptional: true,
            defaultValue: "'okta_session'",
          },
          {
            name: 'cookieMaxAge',
            type: 'number',
            description: 'Cookie の最大有効期間（秒単位）。',
            isOptional: true,
            defaultValue: '86400 (24 hours)',
          },
          {
            name: 'cookiePassword',
            type: 'string',
            description:
              'セッション Cookie を暗号化するためのパスワード。32 文字以上である必要があります。設定しない場合は自動生成された値が使用されますが、再起動後には保持されません。',
            isOptional: true,
            defaultValue: 'process.env.OKTA_COOKIE_PASSWORD',
          },
          {
            name: 'secureCookies',
            type: 'boolean',
            description: 'セッション Cookie に `Secure` フラグを設定します。',
            isOptional: true,
            defaultValue: 'true in production, false otherwise',
          },
        ],
      },
    ],
  },
  {
    name: 'name',
    type: 'string',
    description: 'Auth Provider インスタンスのカスタム名。',
    isOptional: true,
    defaultValue: "'okta'",
  },
]}
/>

### 環境変数 {/* #environment-variables */}

コンストラクターオプションが指定されていない場合、次の環境変数が自動的に使用されます。

<PropertiesTable
  content={[
  {
    name: 'OKTA_DOMAIN',
    type: 'string',
    description: 'Okta ドメイン（例：`dev-123456.okta.com`）。Okta 管理コンソールで確認できます。',
  },
  {
    name: 'OKTA_CLIENT_ID',
    type: 'string',
    description: 'Okta アプリケーションの OAuth クライアント ID。',
  },
  {
    name: 'OKTA_CLIENT_SECRET',
    type: 'string',
    description: 'Okta アプリケーションの OAuth クライアントシークレット。',
  },
  {
    name: 'OKTA_ISSUER',
    type: 'string',
    description: 'トークン発行者の URL。設定されていない場合、デフォルトは `https://{domain}/oauth2/default` です。',
    isOptional: true,
  },
  {
    name: 'OKTA_REDIRECT_URI',
    type: 'string',
    description: 'SSO コールバック用の OAuth リダイレクト URI。',
  },
  {
    name: 'OKTA_COOKIE_PASSWORD',
    type: 'string',
    description: 'セッション Cookie を暗号化するためのパスワード。32 文字以上である必要があります。',
    isOptional: true,
  },
  {
    name: 'OKTA_API_TOKEN',
    type: 'string',
    description: 'ユーザー検索と RBAC グループ解決用の Okta API トークン。',
    isOptional: true,
  },
]}
/>

### 認証フロー {/* #authentication-flow */}

`MastraAuthOkta` は、次の順序でリクエストを認証します。

1. **セッション Cookie**：暗号化されたセッション Cookie を読み取り、復号します。セッションが有効で期限切れでない場合、ユーザーは認証されます。
1. **JWT フォールバック**：セッション Cookie が存在しない場合、`Authorization` ヘッダーのトークンを Okta の JWKS エンドポイントに対して検証します。

認証後、`authorizeUser` はユーザーが有効な `oktaId` を持っていることを確認します。追加のロジックを実装するには、カスタム `authorizeUser` 関数を指定します。

### `OktaUser` 型 {/* #oktauser-type */}

`OktaUser` 型は、基本の `EEUser` インターフェースを Okta 固有のフィールドで拡張します。

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'ユーザー識別子（`sub` クレームに対応）。',
  },
  {
    name: 'oktaId',
    type: 'string',
    description: 'Okta ユーザー ID（`id` と同じ）。',
  },
  {
    name: 'email',
    type: 'string',
    description: 'ユーザーのメールアドレス。',
    isOptional: true,
  },
  {
    name: 'name',
    type: 'string',
    description: 'トークンクレームから構築されるユーザーの表示名。',
    isOptional: true,
  },
  {
    name: 'avatarUrl',
    type: 'string',
    description: 'ユーザーのプロフィール画像の URL。',
    isOptional: true,
  },
  {
    name: 'groups',
    type: 'string[]',
    description: 'ユーザーが所属する Okta グループ。`groups` クレームから設定されます。',
    isOptional: true,
  },
]}
/>

## MastraRBACOkta クラス {/* #mastrarbacokta-class */}

`MastraRBACOkta` クラスは、Okta グループを Mastra 権限にマッピングします。Okta API からユーザーグループを取得し、設定可能なロールマッピングに照らして解決します。`MastraAuthOkta` またはその他の Auth Provider と組み合わせて使用します。

:::note

RBAC には、有効な Enterprise Edition ライセンスが必要です。開発環境ではライセンスなしで動作するためローカルで試せますが、本番環境ではライセンスが必要です。詳細については、[営業担当者にお問い合わせください](https://mastra.ai/contact)。

:::

### 使用例 {/* #usage-example-1 */}

`MastraRBACOkta` を `rbac` オプションに渡し、Auth Provider と組み合わせて使用します。

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthOkta, MastraRBACOkta } from '@mastra/auth-okta'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthOkta(),
    rbac: new MastraRBACOkta({
      roleMapping: {
        Admin: ['*'],
        Engineering: ['agents:*', 'workflows:*', 'tools:*'],
        Viewer: ['agents:read', 'workflows:read'],
        _default: [],
      },
    }),
  },
})
```

別の Auth Provider で Okta RBAC を使用するには、他の Provider のユーザーオブジェクトから Okta ユーザー ID を解決する `getUserId` 関数を渡します。

```typescript title="src/mastra/index.ts"
import { MastraAuthAuth0 } from '@mastra/auth-auth0'
import { MastraRBACOkta } from '@mastra/auth-okta'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthAuth0(),
    rbac: new MastraRBACOkta({
      getUserId: user => user.metadata?.oktaUserId || user.email,
      roleMapping: {
        Engineering: ['agents:*', 'workflows:*'],
        Admin: ['*'],
        _default: [],
      },
    }),
  },
})
```

### コンストラクターパラメーター {/* #constructor-parameters-1 */}

<PropertiesTable
  content={[
  {
    name: 'roleMapping',
    type: 'RoleMapping',
    description:
      "Okta グループ名を Mastra 権限文字列の配列にマッピングします。どのグループにも一致しないユーザーに権限を割り当てるには `'_default'` を使用します。`'*'`（フルアクセス）や `'agents:*'`（Agent に対するすべての操作）のようなワイルドカードをサポートします。",
    isOptional: false,
  },
  {
    name: 'domain',
    type: 'string',
    description: 'Okta ドメイン。Okta 管理 SDK の初期化に使用されます。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_DOMAIN',
  },
  {
    name: 'apiToken',
    type: 'string',
    description: '管理 SDK 用の Okta API トークン。Okta API からユーザーグループを取得するために必要です。',
    isOptional: true,
    defaultValue: 'process.env.OKTA_API_TOKEN',
  },
  {
    name: 'getUserId',
    type: '(user: unknown) => string | undefined',
    description:
      'ユーザーオブジェクトから Okta ユーザー ID を抽出します。Okta RBAC を別の Auth Provider と組み合わせる場合に使用します。指定されていない場合は、ユーザーオブジェクトの `oktaId` または `id` にフォールバックします。',
    isOptional: true,
  },
  {
    name: 'cache',
    type: 'PermissionCacheOptions',
    description: 'グループ検索用の LRU キャッシュを設定します。',
    isOptional: true,
    properties: [
      {
        type: 'PermissionCacheOptions',
        parameters: [
          {
            name: 'maxSize',
            type: 'number',
            description: 'キャッシュするユーザーの最大数。',
            isOptional: true,
            defaultValue: '1000',
          },
          {
            name: 'ttlMs',
            type: 'number',
            description: '有効期間（ミリ秒単位）。',
            isOptional: true,
            defaultValue: '60000',
          },
        ],
      },
    ],
  },
]}
/>
