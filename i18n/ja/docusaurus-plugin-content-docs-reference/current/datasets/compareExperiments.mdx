---
title: "リファレンス：DatasetsManager.compareExperiments() | Datasets"
description: "`DatasetsManager.compareExperiments()` メソッドのドキュメントです。このメソッドは2つ以上の Experiment を比較します。"
packages:
  - "@mastra/core"
---

# DatasetsManager.compareExperiments()

**追加バージョン：** `@mastra/core@1.4.0`

2つ以上の Experiment を比較し、項目別および Scorer 別の比較結果を生成します。2つ以上の Experiment ID が必要です。

## 使用例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const comparison = await mastra.datasets.compareExperiments({
  experimentIds: ['exp-baseline', 'exp-new'],
  baselineId: 'exp-baseline',
})

console.log(`Baseline: ${comparison.baselineId}`)

for (const item of comparison.items) {
  console.log(`Item ${item.itemId}:`)
  console.log(`  Input: ${JSON.stringify(item.input)}`)

  for (const [expId, result] of Object.entries(item.results)) {
    if (result) {
      console.log(
        `  ${expId}: output=${JSON.stringify(result.output)}, scores=${JSON.stringify(result.scores)}`,
      )
    }
  }
}
```

## パラメータ

<PropertiesTable
  content={[
  {
    name: 'experimentIds',
    type: 'string[]',
    description: '比較する Experiment ID の配列。2つ以上必要です。',
  },
  {
    name: 'baselineId',
    type: 'string',
    isOptional: true,
    description: 'ベースライン Experiment の ID。デフォルトは `experimentIds` の最初の ID です。',
  },
]}
/>

## 戻り値

指定した Experiment ID が2つ未満の場合は `MastraError` をスローします。

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: '比較結果。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'baselineId',
            type: 'string',
            description: '比較に使用するベースライン Experiment の ID。',
          },
          {
            name: 'items',
            type: 'Array<object>',
            description: '項目別の比較データ。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'itemId',
                    type: 'string',
                    description: 'データセット項目の ID。',
                  },
                  {
                    name: 'input',
                    type: 'unknown | null',
                    description: '項目の入力データ。',
                  },
                  {
                    name: 'groundTruth',
                    type: 'unknown | null',
                    description: '項目の正解データ。',
                  },
                  {
                    name: 'results',
                    type: 'Record<string, { output: unknown; scores: Record<string, number | null> } | null>',
                    description:
                      'Experiment ID をキーとする結果。各エントリには、その Experiment の出力と Scorer の結果が含まれます。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## 関連項目

- [dataset.startExperiment()](/reference/datasets/startExperiment)
- [dataset.listExperiments()](/reference/datasets/listExperiments)
