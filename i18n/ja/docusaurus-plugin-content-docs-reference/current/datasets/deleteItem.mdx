---
title: "リファレンス：dataset.deleteItem() | Datasets"
description: "データセットから1つの項目を削除する `dataset.deleteItem()` メソッドのドキュメントです。"
packages:
  - "@mastra/core"
---

# dataset.deleteItem() {/* #datasetdeleteitem */}

**追加バージョン：** `@mastra/core@1.4.0`

ID を指定して、データセットから1つの項目を削除します。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItem({ itemId: 'item-id' })
```

## パラメータ {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: '削除する項目の ID。',
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: '項目が削除されると解決されます。',
  },
]}
/>
