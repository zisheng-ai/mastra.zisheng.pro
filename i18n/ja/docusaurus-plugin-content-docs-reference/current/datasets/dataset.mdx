---
title: "リファレンス：Dataset クラス | Datasets"
description: "`Dataset` クラスのドキュメントです。このクラスは1つのデータセットを操作する公開 API を提供します。"
packages:
  - "@mastra/core"
---

# Dataset クラス

**追加バージョン：** `@mastra/core@1.4.0`

`Dataset` クラスは、1つのデータセットに対する項目の CRUD、バージョン管理、Experiment 管理のメソッドを提供します。`mastra.datasets.get()` または `mastra.datasets.create()` で取得します。

## 使用例 {/* #usage-examples */}

### 項目を追加して Experiment を実行する {/* #add-items-and-run-an-experiment */}

```typescript title="src/mastra/datasets/usage.ts"
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.create({
  name: 'QA pairs',
})

// Add items
await dataset.addItems({
  items: [
    { input: { question: 'What is AI?' }, groundTruth: { answer: 'Artificial Intelligence' } },
    { input: { question: 'What is ML?' }, groundTruth: { answer: 'Machine Learning' } },
  ],
})

// Run an experiment
const summary = await dataset.startExperiment({
  targetType: 'agent',
  targetId: 'my-agent',
  scorers: ['accuracy'],
})

console.log(`${summary.succeededCount}/${summary.totalItems} succeeded`)
```

### バージョンと履歴を管理する {/* #manage-versions-and-history */}

```typescript title="src/mastra/datasets/versioning.ts"
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// List versions
const { versions } = await dataset.listVersions()

// List items at a specific version
const items = await dataset.listItems({ version: 2 })

// Get item change history
const history = await dataset.getItemHistory({ itemId: 'item-id' })
```

## アクセス {/* #access */}

`Dataset` は直接インスタンス化しません。`DatasetsManager` から取得します：

```typescript
const dataset = await mastra.datasets.get({ id: 'dataset-id' })
// or
const dataset = await mastra.datasets.create({ name: 'My dataset' })
```

## プロパティ {/* #properties */}

完全なデータセットレコード（名前、説明、スキーマ、バージョン、タイムスタンプ）を取得するには、[`dataset.getDetails()`](/reference/datasets/getDetails) を呼び出します。

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '一意な識別子： dataset. Read-only.',
  },
]}
/>

## 関連項目 {/* #related */}

- [DatasetsManager クラス](/reference/datasets/datasets-manager)
- [dataset.startExperiment()](/reference/datasets/startExperiment)
- [dataset.addItems()](/reference/datasets/addItems)
- [dataset.listVersions()](/reference/datasets/listVersions)
