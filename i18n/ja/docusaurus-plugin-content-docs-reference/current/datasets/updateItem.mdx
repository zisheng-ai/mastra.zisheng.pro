---
title: "リファレンス：dataset.updateItem() | Datasets"
description: "`dataset.updateItem()` メソッドのドキュメントです。このメソッドはデータセット内の既存項目を更新します。"
packages:
  - "@mastra/core"
---

# dataset.updateItem()

**追加バージョン：** `@mastra/core@1.4.0`

データセット内の既存項目を更新します。指定したフィールドだけが更新されます。項目を更新すると新しいバージョンが作成されます。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const updated = await dataset.updateItem({
  itemId: 'item-id',
  input: { question: 'What is TypeScript?' },
  groundTruth: { answer: 'A typed superset of JavaScript' },
  metadata: { reviewed: true },
})
```

## パラメータ {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: '更新する項目の ID。',
  },
  {
    name: 'input',
    type: 'unknown',
    isOptional: true,
    description: '更新後の入力データ。',
  },
  {
    name: 'groundTruth',
    type: 'unknown',
    isOptional: true,
    description: '更新後の正解データ。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '更新後のメタデータ。',
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem>',
    description: '更新後のデータセット項目。項目の構造は dataset.addItem() を参照してください。',
  },
]}
/>
