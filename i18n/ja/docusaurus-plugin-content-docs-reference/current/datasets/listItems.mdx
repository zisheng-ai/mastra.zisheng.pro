---
title: "リファレンス：dataset.listItems() | Datasets"
description: "`dataset.listItems()` メソッドのドキュメントです。このメソッドは任意のページネーションとフィルタリングを使用してデータセットの項目を一覧表示します。"
packages:
  - "@mastra/core"
---

# dataset.listItems() {/* #datasetlistitems */}

**追加バージョン：** `@mastra/core@1.4.0`

データセット内の項目を一覧表示します。`version` だけを指定した場合、そのバージョンの全項目を含む単独の `DatasetItem[]` スナップショットを返します。それ以外（引数なし、または `version` の有無を問わず `search` / `page` / `perPage` を指定）の場合は、ページネーションされた `{ items, pagination }` 形式を返します。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Paginated list (default: page 0, 20 per page)
const result = await dataset.listItems()

// List with search
const filtered = await dataset.listItems({ search: 'TypeScript', page: 0, perPage: 10 })

// Paginated list scoped to a specific version
const versionedPage = await dataset.listItems({ version: 2, page: 0, perPage: 20 })

// Version-only snapshot returns a bare DatasetItem[] (deprecated form —
// prefer passing page/perPage to receive the paginated shape).
const versionedItems = await dataset.listItems({ version: 2 })
```

## パラメータ {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description:
      '項目を一覧表示するデータセットバージョン。単独で指定すると、そのバージョンの全項目を単独の `DatasetItem[]` スナップショットとして返します。`version` とともに `search` / `page` / `perPage` を指定すると、ページネーション形式に切り替わります。',
  },
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'ページネーションのページ番号。デフォルトは `0` です。',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: '1ページあたりの項目数。デフォルトは `20` です。',
  },
  {
    name: 'search',
    type: 'string',
    isOptional: true,
    description: '項目を絞り込む検索文字列。',
  },
]}
/>

## 戻り値 {/* #returns */}

`version` だけを指定した場合（`search`、`page`、`perPage` は未指定）：

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description:
      '指定したデータセットバージョンの全項目を含む単独の配列。この形式は後方互換性のために維持されていますが、非推奨です。常に以下のページネーション形式を受け取るには、`page` / `perPage`（または `search`）を指定してください。',
  },
]}
/>

<br />

それ以外の場合（引数なし、または `version` の有無を問わず `search` / `page` / `perPage` を指定）：

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'ページネーションされたレスポンス。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'items',
            type: 'DatasetItem[]',
            description: '現在のページの項目配列。',
          },
          {
            name: 'pagination',
            type: 'object',
            description: 'ページネーションのメタデータ。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'total',
                    type: 'number',
                    description: '項目の総数。',
                  },
                  {
                    name: 'page',
                    type: 'number',
                    description: '現在のページ番号。',
                  },
                  {
                    name: 'perPage',
                    type: 'number | false',
                    description: '1ページあたりの項目数。ページネーションしない場合は `false`。',
                  },
                  {
                    name: 'hasMore',
                    type: 'boolean',
                    description: '次のページがあるかどうか。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>
