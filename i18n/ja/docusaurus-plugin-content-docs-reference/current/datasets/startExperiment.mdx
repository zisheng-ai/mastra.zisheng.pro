---
title: "リファレンス：dataset.startExperiment() | Datasets"
description: "`dataset.startExperiment()` メソッドのドキュメントです。このメソッドはデータセットで Experiment を実行し、完了を待ちます。"
packages:
  - "@mastra/core"
---

# dataset.startExperiment()

**追加バージョン：** `@mastra/core@1.4.0`

データセットで Experiment を実行し、完了を待ちます。任意のスコアリングを使用して、すべての項目を対象（Agent、Workflow、Scorer）に対して実行します。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Run against a registered agent with a flat scorer list
const summary = await dataset.startExperiment({
  targetType: 'agent',
  targetId: 'my-agent',
  scorers: ['accuracy', 'relevancy'],
  maxConcurrency: 10,
})

// Or pass the same categorised shape accepted by runEvals
const summary2 = await dataset.startExperiment({
  targetType: 'agent',
  targetId: 'my-agent',
  scorers: {
    agent: [accuracyScorer],
    trajectory: [toolOrderScorer],
  },
})

// For workflow targets, score individual steps with their own scorers
const summary3 = await dataset.startExperiment({
  targetType: 'workflow',
  targetId: 'my-workflow',
  scorers: {
    workflow: [overallScorer],
    steps: {
      'fetch-data': [fetchScorer],
      transform: [transformScorer],
    },
    trajectory: [executionPathScorer],
  },
})

console.log(`${summary.succeededCount}/${summary.totalItems} succeeded`)
console.log(`Status: ${summary.status}`)
console.log(`${summary2.succeededCount}/${summary2.totalItems} succeeded`)
console.log(`Status: ${summary2.status}`)
```

## パラメータ {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'targetType',
    type: "'agent' | 'workflow' | 'scorer'",
    isOptional: true,
    description: '項目を実行する登録済み対象の種類。`targetId` と併用します。',
  },
  {
    name: 'targetId',
    type: 'string',
    isOptional: true,
    description: '登録済み対象の ID。`targetType` と併用します。',
  },
  {
    name: 'scorers',
    type: '(MastraScorer | string)[] | AgentScorerConfig | WorkflowScorerConfig',
    isOptional: true,
    description:
      '各結果を評価する Scorer。`MastraScorer` インスタンスまたは登録済み Scorer ID の単純な配列のほか、`runEvals` と同じ分類済み設定形式（`AgentScorerConfig` / `WorkflowScorerConfig`）を指定できます。Trajectory Scorer（`type: "trajectory"`）は、指定形式にかかわらず、事前抽出された `Trajectory` を出力として自動的に受け取ります。Workflow の場合は `scorers: { steps: { stepId: [...] } }` で Step ごとの Scorer を渡し、各 Step の出力に対して実行できます。その結果には元の `stepId` が含まれ、`targetScope: "span"` が維持されます（`runEvals` と同じ動作）。',
  },
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: '表示名 experiment.',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: '説明 experiment.',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: 'Experiment の任意のメタデータ。',
  },
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description: '特定のデータセットバージョンに固定します。デフォルトは最新バージョンです。',
  },
  {
    name: 'maxConcurrency',
    type: 'number',
    isOptional: true,
    description: '項目を同時実行する最大数。デフォルトは `5` です。',
  },
  {
    name: 'signal',
    type: 'AbortSignal',
    isOptional: true,
    description: 'Experiment をキャンセルするための AbortSignal。',
  },
  {
    name: 'itemTimeout',
    type: 'number',
    isOptional: true,
    description: '項目ごとの実行タイムアウト（ミリ秒）。',
  },
  {
    name: 'maxRetries',
    type: 'number',
    isOptional: true,
    description: '失敗時の項目ごとの最大再試行回数。デフォルトは `0`（再試行なし）です。Abort エラーは再試行しません。',
  },
  {
    name: 'unmockedToolPolicy',
    type: "'allow' | 'deny'",
    isOptional: true,
    defaultValue: "'allow'",
    description:
      '宣言されていない Agent Tool の呼び出しを制御します。`allow` は実際に実行します。`deny` は実行前に `TOOL_MOCK_NOT_DECLARED` で項目を失敗させます。項目単位の値は、この Experiment のデフォルト値を上書きします。',
  },
  {
    name: 'persistence',
    type: 'ExperimentPersistencePolicy',
    isOptional: true,
    description:
      'この実行で Experiment レコードとスコアレコードを書き込むかを制御します。対象と Scorer は引き続き実行され、結果は返される概要で確認できます。',
    properties: [
      {
        type: 'ExperimentPersistencePolicy',
        parameters: [
          {
            name: 'experiments',
            type: "'default' | 'none'",
            isOptional: true,
            defaultValue: "'default'",
            description:
              '`none` に設定すると、Experiment の作成、項目結果、進行状況、最終ステータスの書き込みを省略します。',
          },
          {
            name: 'scores',
            type: "'default' | 'none'",
            isOptional: true,
            defaultValue: "'default'",
            description: '`none` に設定すると、Scorer を実行したままスコアの書き込みを省略します。',
          },
        ],
      },
    ],
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<ExperimentSummary>',
    description: '完了した Experiment の概要。',
    properties: [
      {
        type: 'ExperimentSummary',
        parameters: [
          {
            name: 'experimentId',
            type: 'string',
            description: '一意な ID： experiment.',
          },
          {
            name: 'status',
            type: "'pending' | 'running' | 'completed' | 'failed'",
            description: 'Experiment の最終ステータス。',
          },
          {
            name: 'totalItems',
            type: 'number',
            description: 'データセット内の項目総数。',
          },
          {
            name: 'succeededCount',
            type: 'number',
            description: '成功した項目数。',
          },
          {
            name: 'failedCount',
            type: 'number',
            description: '失敗した項目数。',
          },
          {
            name: 'skippedCount',
            type: 'number',
            description: 'スキップされた項目数（Abort による場合など）。',
          },
          {
            name: 'completedWithErrors',
            type: 'boolean',
            description: '実行は完了したものの一部の項目が失敗した場合は `true`。',
          },
          {
            name: 'startedAt',
            type: 'Date',
            description: 'Experiment が開始された日時。',
          },
          {
            name: 'completedAt',
            type: 'Date',
            description: 'Experiment が完了した日時。',
          },
          {
            name: 'results',
            type: 'ItemWithScores[]',
            description: '全項目の結果とスコア。',
            properties: [
              {
                type: 'ItemWithScores',
                parameters: [
                  {
                    name: 'itemId',
                    type: 'string',
                    description: 'データセット項目の ID。',
                  },
                  {
                    name: 'itemVersion',
                    type: 'number',
                    description: '実行時の項目のデータセットバージョン。',
                  },
                  {
                    name: 'input',
                    type: 'unknown',
                    description: '対象に渡された入力データ。',
                  },
                  {
                    name: 'output',
                    type: 'unknown | null',
                    description: '対象からの出力。失敗した場合は `null`。',
                  },
                  {
                    name: 'groundTruth',
                    type: 'unknown | null',
                    description: 'データセット項目の期待される出力。',
                  },
                  {
                    name: 'error',
                    type: '{ message: string; stack?: string; code?: string } | null',
                    description: '実行に失敗した場合の構造化エラー。',
                  },
                  {
                    name: 'startedAt',
                    type: 'Date',
                    description: '項目の実行が開始された日時。',
                  },
                  {
                    name: 'completedAt',
                    type: 'Date',
                    description: '項目の実行が完了した日時。',
                  },
                  {
                    name: 'retryCount',
                    type: 'number',
                    description: '再試行回数。',
                  },
                  {
                    name: 'scores',
                    type: 'ScorerResult[]',
                    description: 'この項目に対するすべての Scorer の結果。',
                    properties: [
                      {
                        type: 'ScorerResult',
                        parameters: [
                          {
                            name: 'scorerId',
                            type: 'string',
                            description: 'Scorer の ID。',
                          },
                          {
                            name: 'scorerName',
                            type: 'string',
                            description: '表示名 scorer.',
                          },
                          {
                            name: 'score',
                            type: 'number | null',
                            description: '算出されたスコア。Scorer が失敗した場合は `null`。',
                          },
                          {
                            name: 'reason',
                            type: 'string | null',
                            description: 'スコアの理由または説明。',
                          },
                          {
                            name: 'error',
                            type: 'string | null',
                            description: 'Scorer が失敗した場合のエラーメッセージ。',
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## 関連項目 {/* #related */}

- [dataset.startExperimentAsync()](/reference/datasets/startExperimentAsync)
- [dataset.listExperiments()](/reference/datasets/listExperiments)
- [DatasetsManager.compareExperiments()](/reference/datasets/compareExperiments)
