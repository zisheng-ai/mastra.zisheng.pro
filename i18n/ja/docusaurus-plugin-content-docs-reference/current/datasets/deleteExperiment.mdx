---
title: "リファレンス：dataset.deleteExperiment() | Datasets"
description: "ID を指定して Experiment を削除する `dataset.deleteExperiment()` メソッドのドキュメントです。"
packages:
  - "@mastra/core"
---

# dataset.deleteExperiment()

**追加バージョン：** `@mastra/core@1.4.0`

ID を指定して、関連するすべての結果を含む Experiment（実行）を削除します。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteExperiment({ experimentId: 'exp-id' })
```

## パラメータ {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: '削除する Experiment の ID。',
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: 'Experiment とその結果が削除されると解決されます。',
  },
]}
/>
