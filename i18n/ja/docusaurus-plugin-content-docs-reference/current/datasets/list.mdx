---
title: "リファレンス：DatasetsManager.list() | Datasets"
description: "`DatasetsManager.list()` メソッドのドキュメントです。このメソッドはすべてのデータセットをページネーション付きで一覧表示します。"
packages:
  - "@mastra/core"
---

# DatasetsManager.list()

**追加バージョン：** `@mastra/core@1.4.0`

ページネーションと任意のフィルターを使用してデータセットを一覧表示します。フィルターはストレージ層で適用されるため、呼び出し側で結果を再フィルタリングする必要はありません。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const { datasets, pagination } = await mastra.datasets.list({ page: 0, perPage: 10 })

for (const ds of datasets) {
  console.log(`${ds.id}: ${ds.name} (v${ds.version})`)
}

// Filter by target type and name (case-insensitive substring)
const filtered = await mastra.datasets.list({
  page: 0,
  perPage: 20,
  filters: { targetType: 'agent', name: 'qa' },
})

// Filter by any-overlap match on target IDs
const forAgents = await mastra.datasets.list({
  filters: { targetIds: ['agent-a', 'agent-b'] },
})
```

## パラメータ {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'ページ番号。デフォルトは `0` です。',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: '1ページあたりのデータセット数。デフォルトは `20` です。',
  },
  {
    name: 'filters',
    type: 'object',
    isOptional: true,
    description: 'ストレージ層で適用する任意のフィルター。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'organizationId',
            type: 'string',
            isOptional: true,
            description: 'テナンシーフィルター。この組織のデータセットに限定します。',
          },
          {
            name: 'projectId',
            type: 'string',
            isOptional: true,
            description: 'テナンシーフィルター。このプロジェクトのデータセットに限定します。',
          },
          {
            name: 'candidateKey',
            type: 'string',
            isOptional: true,
            description: 'この候補キーに属するデータセットに限定します。',
          },
          {
            name: 'candidateId',
            type: 'string',
            isOptional: true,
            description: 'この候補 ID に属するデータセットに限定します。',
          },
          {
            name: 'targetType',
            type: "'agent' | 'workflow' | 'scorer' | 'processor'",
            isOptional: true,
            description: 'このコンポーネント種別を対象とするデータセットに限定します。',
          },
          {
            name: 'targetIds',
            type: 'string[]',
            isOptional: true,
            description:
              '`targetIds` がこの一覧と1つでも重複するデータセットに限定します。空の配列はフィルターなしとして扱います。',
          },
          {
            name: 'name',
            type: 'string',
            isOptional: true,
            description:
              'データセットの `name` を大文字小文字を区別せず部分一致で検索します。空文字列はフィルターなしとして扱います。',
          },
        ],
      },
    ],
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'ページネーションされたデータセット一覧。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'datasets',
            type: 'DatasetRecord[]',
            description: 'データセットレコードの配列。レコードの構造は dataset.getDetails() を参照してください。',
          },
          {
            name: 'pagination',
            type: 'PaginationInfo',
            description: '`total`、`page`、`perPage`、`hasMore` を含むページネーションメタデータ。',
          },
        ],
      },
    ],
  },
]}
/>
