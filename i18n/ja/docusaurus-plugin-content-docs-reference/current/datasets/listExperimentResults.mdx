---
title: "リファレンス：dataset.listExperimentResults() | Datasets"
description: "`dataset.listExperimentResults()` メソッドのドキュメントです。このメソッドは特定の Experiment の結果を一覧表示します。"
packages:
  - "@mastra/core"
---

# dataset.listExperimentResults()

**追加バージョン：** `@mastra/core@1.4.0`

任意のフィルターとページネーションを使用して、特定の Experiment の項目別結果を一覧表示します。フィルターはストレージ層で適用されます。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const { results, pagination } = await dataset.listExperimentResults({
  experimentId: 'exp-id',
  page: 0,
  perPage: 50,
})

// Restrict to results that still need review
const { results: pending } = await dataset.listExperimentResults({
  experimentId: 'exp-id',
  status: 'needs-review',
})

// Restrict to results tied to a specific trace
const { results: byTrace } = await dataset.listExperimentResults({
  experimentId: 'exp-id',
  traceId: 'trace-abc',
})

for (const result of results) {
  console.log(`Item ${result.itemId}: ${result.error ? 'FAILED' : 'OK'}`)
}
```

## パラメータ {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: '結果を一覧表示する Experiment の ID。',
  },
  {
    name: 'traceId',
    type: 'string',
    isOptional: true,
    description: 'この Trace ID に関連付けられた結果に限定します。',
  },
  {
    name: 'status',
    type: "'needs-review' | 'reviewed' | 'complete'",
    isOptional: true,
    description: '結果ごとのレビューステータスがこの値のものに限定します。',
  },
  {
    name: 'filters',
    type: 'ExperimentTenancyFilters',
    isOptional: true,
    description: 'マルチテナントのスコープフィルター（`organizationId`、`projectId`）。ストレージ層へ転送されます。',
  },
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'ページ番号。デフォルトは `0` です。',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: '1ページあたりの結果数。デフォルトは `20` です。',
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'ページネーションされた Experiment 結果。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'results',
            type: 'ExperimentResult[]',
            description: '項目単位の結果の配列。',
            properties: [
              {
                type: 'ExperimentResult',
                parameters: [
                  {
                    name: 'id',
                    type: 'string',
                    description: '一意な結果 ID。',
                  },
                  {
                    name: 'experimentId',
                    type: 'string',
                    description: '親 Experiment の ID。',
                  },
                  {
                    name: 'itemId',
                    type: 'string',
                    description: 'データセット項目の ID。',
                  },
                  {
                    name: 'itemDatasetVersion',
                    type: 'number | null',
                    description: '実行時の項目のデータセットバージョン。',
                  },
                  {
                    name: 'input',
                    type: 'unknown',
                    description: '対象に渡された入力データ。',
                  },
                  {
                    name: 'output',
                    type: 'unknown | null',
                    description: '対象からの出力。',
                  },
                  {
                    name: 'groundTruth',
                    type: 'unknown | null',
                    description: '期待される出力。',
                  },
                  {
                    name: 'error',
                    type: '{ message: string; stack?: string; code?: string } | null',
                    description: '実行に失敗した場合の構造化エラー。',
                  },
                  {
                    name: 'startedAt',
                    type: 'Date',
                    description: '実行が開始された日時。',
                  },
                  {
                    name: 'completedAt',
                    type: 'Date',
                    description: '実行が完了した日時。',
                  },
                  {
                    name: 'retryCount',
                    type: 'number',
                    description: '再試行回数。',
                  },
                  {
                    name: 'traceId',
                    type: 'string | null',
                    description: 'Observability に使う Trace ID。',
                  },
                  {
                    name: 'createdAt',
                    type: 'Date',
                    description: '結果レコードが作成された日時。',
                  },
                ],
              },
            ],
          },
          {
            name: 'pagination',
            type: 'PaginationInfo',
            description: '`total`、`page`、`perPage`、`hasMore` を含むページネーションメタデータ。',
          },
        ],
      },
    ],
  },
]}
/>
