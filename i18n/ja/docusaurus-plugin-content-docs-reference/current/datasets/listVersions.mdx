---
title: "リファレンス：dataset.listVersions() | Datasets"
description: "`dataset.listVersions()` メソッドのドキュメントです。このメソッドはデータセットの全バージョンを一覧表示します。"
packages:
  - "@mastra/core"
---

# dataset.listVersions()

**追加バージョン：** `@mastra/core@1.4.0`

ページネーションを使用してデータセットの全バージョンを一覧表示します。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const { versions, pagination } = await dataset.listVersions({ page: 0, perPage: 10 })

for (const version of versions) {
  console.log(`Version ${version.version} created at ${version.createdAt}`)
}
```

## パラメータ {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: 'ページ番号。デフォルトは `0` です。',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: '1ページあたりのバージョン数。デフォルトは `20` です。',
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: 'ページネーションされたバージョン一覧。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'versions',
            type: 'DatasetVersion[]',
            description: 'バージョンレコードの配列。',
            properties: [
              {
                type: 'DatasetVersion',
                parameters: [
                  {
                    name: 'id',
                    type: 'string',
                    description: 'バージョンレコードの一意な識別子。',
                  },
                  {
                    name: 'datasetId',
                    type: 'string',
                    description: '親データセットの ID。',
                  },
                  {
                    name: 'version',
                    type: 'number',
                    description: 'バージョン番号。',
                  },
                  {
                    name: 'createdAt',
                    type: 'Date',
                    description: 'このバージョンが作成された日時。',
                  },
                ],
              },
            ],
          },
          {
            name: 'pagination',
            type: 'object',
            description: 'ページネーションのメタデータ。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'total',
                    type: 'number',
                    description: 'バージョンの総数。',
                  },
                  {
                    name: 'page',
                    type: 'number',
                    description: '現在のページ番号。',
                  },
                  {
                    name: 'perPage',
                    type: 'number | false',
                    description: '1ページあたりのバージョン数。ページネーションしない場合は `false`。',
                  },
                  {
                    name: 'hasMore',
                    type: 'boolean',
                    description: '次のページがあるかどうか。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>
