---
title: "リファレンス：dataset.update() | Datasets"
description: "`dataset.update()` メソッドのドキュメントです。このメソッドはデータセットのメタデータとスキーマを更新します。"
packages:
  - "@mastra/core"
---

# dataset.update()

**追加バージョン：** `@mastra/core@1.4.0`

データセットのメタデータ、名前、説明、スキーマの一部またはすべてを更新します。スキーマの定義には Standard JSON Schema（Zod、Valibot、ArkType など）を使用します。

## 使用例

```typescript
import { Mastra } from '@mastra/core'
import { z } from 'zod'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Update with plain metadata
const updated = await dataset.update({
  name: 'Updated QA pairs',
  description: 'Revised evaluation set',
})

// Update with Zod schema (library that supports Standard JSON Schema)
const updated2 = await dataset.update({
  inputSchema: z.object({
    question: z.string(),
    context: z.string().optional(),
  }),
})
```

## パラメータ

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: '新しい表示名。',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: '新しい説明。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '更新後のメタデータ。',
  },
  {
    name: 'inputSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: '項目の入力に使う Standard JSON Schema。',
  },
  {
    name: 'groundTruthSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: '項目の正解データに使う Standard JSON Schema。',
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetRecord>',
    description: '更新後のデータセットレコード。完全な構造は dataset.getDetails() を参照してください。',
  },
]}
/>
