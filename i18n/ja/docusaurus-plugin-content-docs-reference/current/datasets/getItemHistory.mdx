---
title: "リファレンス：dataset.getItemHistory() | Datasets"
description: "`dataset.getItemHistory()` メソッドのドキュメントです。このメソッドはデータセット項目の完全な SCD-2 変更履歴を取得します。"
packages:
  - "@mastra/core"
---

# dataset.getItemHistory()

**追加バージョン：** `@mastra/core@1.4.0`

特定の項目について、すべてのデータセットバージョンにわたる完全な SCD-2（Slowly Changing Dimension Type 2）履歴を取得します。

## 使用例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const history = await dataset.getItemHistory({ itemId: 'item-id' })

for (const row of history) {
  console.log(`Version ${row.datasetVersion}: ${row.isDeleted ? 'deleted' : 'active'}`)
}
```

## パラメータ

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: '履歴を取得する項目の ID。',
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItemRow[]>',
    description: 'バージョン順に並んだ、バージョン付き項目行の配列。',
    properties: [
      {
        type: 'DatasetItemRow',
        parameters: [
          {
            name: 'id',
            type: 'string',
            description: '一意な行識別子。',
          },
          {
            name: 'datasetId',
            type: 'string',
            description: '親データセットの ID。',
          },
          {
            name: 'datasetVersion',
            type: 'number',
            description: 'この行のデータセットバージョン。',
          },
          {
            name: 'validTo',
            type: 'number | null',
            description: 'この行が置き換えられたバージョン。現在も有効な場合は `null`。',
          },
          {
            name: 'isDeleted',
            type: 'boolean',
            description: 'この行が削除を表すかどうか。',
          },
          {
            name: 'input',
            type: 'unknown',
            description: 'このバージョンの入力データ。',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: 'このバージョンの正解データ。',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: 'このバージョンのメタデータ。',
          },
          {
            name: 'createdAt',
            type: 'Date',
            description: 'この行が作成された日時。',
          },
          {
            name: 'updatedAt',
            type: 'Date',
            description: 'この行が最後に更新された日時。',
          },
        ],
      },
    ],
  },
]}
/>
