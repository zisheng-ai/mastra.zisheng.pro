---
title: "リファレンス：dataset.getExperiment() | Datasets"
description: "ID を指定して特定の Experiment を取得する `dataset.getExperiment()` メソッドのドキュメントです。"
packages:
  - "@mastra/core"
---

# dataset.getExperiment()

**追加バージョン：** `@mastra/core@1.4.0`

ID を指定して特定の Experiment（実行）を取得します。

## 使用例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const experiment = await dataset.getExperiment({ experimentId: 'exp-id' })

console.log(`Status: ${experiment.status}`)
console.log(`${experiment.succeededCount}/${experiment.totalItems} succeeded`)
```

## パラメータ {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: '取得する Experiment の ID。',
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Experiment>',
    description: 'Experiment レコード。完全な構造は dataset.listExperiments() を参照してください。',
  },
]}
/>
