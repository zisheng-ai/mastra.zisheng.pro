---
title: "リファレンス：dataset.addItems() | Datasets"
description: "`dataset.addItems()` メソッドのドキュメントです。このメソッドはデータセットに複数の項目を一括追加します。"
packages:
  - "@mastra/core"
---

# dataset.addItems()

**追加バージョン：** `@mastra/core@1.4.0`

1回の一括操作でデータセットに複数の項目を追加します。

## 使用例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const items = await dataset.addItems({
  items: [
    { input: { question: 'What is AI?' }, groundTruth: { answer: 'Artificial Intelligence' } },
    { input: { question: 'What is ML?' }, groundTruth: { answer: 'Machine Learning' } },
    { input: { question: 'What is DL?' }, metadata: { category: 'deep-learning' } },
  ],
})

console.log(`Added ${items.length} items`)
```

## パラメータ

<PropertiesTable
  content={[
  {
    name: 'items',
    type: 'Array<object>',
    description: '追加する項目の配列。各項目の構造は dataset.addItem() の入力と同じです。',
    properties: [
      {
        type: 'DatasetItem',
        parameters: [
          {
            name: 'input',
            type: 'unknown',
            description: '項目の入力データ。',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: '期待される出力または正解データ。',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: '任意のメタデータ。',
          },
        ],
      },
    ],
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description: '作成されたデータセット項目の配列。項目の構造は dataset.addItem() を参照してください。',
  },
]}
/>
