---
title: "リファレンス：dataset.deleteItems() | Datasets"
description: "データセットから複数の項目を一括削除する `dataset.deleteItems()` メソッドのドキュメントです。"
packages:
  - "@mastra/core"
---

# dataset.deleteItems()

**追加バージョン：** `@mastra/core@1.4.0`

1回の一括操作で、データセットから複数の項目を削除します。

## 使用例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItems({
  itemIds: ['item-1', 'item-2', 'item-3'],
})
```

## パラメータ

<PropertiesTable
  content={[
  {
    name: 'itemIds',
    type: 'string[]',
    description: '削除する項目 ID の配列。',
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: 'すべての項目が削除されると解決されます。',
  },
]}
/>
