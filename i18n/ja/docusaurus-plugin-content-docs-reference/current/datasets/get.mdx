---
title: "リファレンス：DatasetsManager.get() | Datasets"
description: "ID を指定して既存のデータセットを取得する `DatasetsManager.get()` メソッドのドキュメントです。"
packages:
  - "@mastra/core"
---

# DatasetsManager.get()

**追加バージョン：** `@mastra/core@1.4.0`

ID を指定して既存のデータセットを取得します。データセットが存在しない場合は `MastraError` をスローします。

## 使用例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Now use the dataset
const details = await dataset.getDetails()
console.log(details.name)
```

## パラメータ

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'データセットの一意な識別子。',
  },
]}
/>

## 戻り値

データセットが見つからない場合は `MastraError` をスローします。

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<Dataset>',
    description: 'Dataset インスタンス。',
  },
]}
/>
