---
title: "リファレンス: Tavily tools | Tools and MCP"
description: "Mastra Agent 向けの Web 検索、抽出、クロール、マップ Tool を提供する @mastra/tavily の API リファレンス。"
packages:
  - "@mastra/tavily"
---

# Tavily tools

`@mastra/tavily` パッケージは、[Tavily](https://app.tavily.com) API を Mastra 互換の Tool としてラップします。検索、抽出、クロール、マップ用のファクトリー関数を公開しています。各関数は、完全な Zod 入出力スキーマを備えた [`createTool()`](./create-tool) で作成された Tool を返します。

## インストール

```sh npm2yarn
npm install @mastra/tavily @tavily/core zod
```

## クイックスタート

共通設定を使用する4つの Tool をすべて取得するには、`createTavilyTools()` を使用します。

```typescript title="src/mastra/tools/index.ts"
import { createTavilyTools } from '@mastra/tavily'

const tools = createTavilyTools()
// Or pass an explicit API key:
// const tools = createTavilyTools({ apiKey: 'tvly-...' })
```

各 Tool は個別に作成することもできます。

```typescript title="src/mastra/tools/index.ts"
import { createTavilySearchTool, createTavilyExtractTool } from '@mastra/tavily'

const searchTool = createTavilySearchTool()
const extractTool = createTavilyExtractTool({ apiKey: 'tvly-...' })
```

デフォルトでは、すべての Tool が環境から `TAVILY_API_KEY` を読み取ります。明示的に `{ apiKey }` を渡すと上書きできます。

## 設定

すべてのファクトリー関数は、`@tavily/core` の `TavilyClientOptions` を受け取ります。

<PropertiesTable
  content={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Tavily API キー。指定しない場合は環境変数 `TAVILY_API_KEY` を使用します。',
    isOptional: true,
  },
  {
    name: 'clientName',
    type: 'string',
    description: '各リクエストの `X-Client-Name` ヘッダーで送信される属性文字列。',
    isOptional: true,
    defaultValue: "'mastra'",
  },
  {
    name: 'apiBaseURL',
    type: 'string',
    description: 'Tavily API のベース URL。',
    isOptional: true,
  },
  {
    name: 'proxies',
    type: 'object',
    description: '基盤となる HTTP クライアントに渡すプロキシ設定。',
    isOptional: true,
  },
  {
    name: 'projectId',
    type: 'string',
    description: 'リクエストのスコープ設定に使用する Tavily プロジェクト ID。',
    isOptional: true,
  },
]}
/>

## `createTavilyTools()`

共通設定を使用する4つの Tool をすべて含むオブジェクトを返します。

```typescript
import { createTavilyTools } from '@mastra/tavily'

const tools = createTavilyTools({ apiKey: 'tvly-...' })
// tools.tavilySearch, tools.tavilyExtract, tools.tavilyCrawl, tools.tavilyMap
```

**戻り値:** `{ tavilySearch, tavilyExtract, tavilyCrawl, tavilyMap }`

## `createTavilySearchTool()`

Tavily を使用して Web を検索する Tool を作成します。コンテンツの抜粋、任意の AI 生成回答、画像を含む関連性の高い結果を返します。

**Tool ID:** `tavily-search`

```typescript
import { createTavilySearchTool } from '@mastra/tavily'

const searchTool = createTavilySearchTool()
```

### 入力

<PropertiesTable
  content={[
  {
    name: 'query',
    type: 'string',
    description: '検索クエリ。',
  },
  {
    name: 'searchDepth',
    type: "'basic' | 'advanced' | 'fast' | 'ultra-fast'",
    description: "検索の深さ。標準的な結果には 'basic'、より詳細な結果には 'advanced'、低レイテンシーには 'fast' または 'ultra-fast' を使用します。",
    isOptional: true,
  },
  {
    name: 'maxResults',
    type: 'number',
    description: '返す結果の最大数（1～20）。',
    isOptional: true,
  },
  {
    name: 'includeAnswer',
    type: "boolean | 'basic' | 'advanced'",
    description: 'AI が生成した回答の概要を含めます。',
    isOptional: true,
  },
  {
    name: 'includeImages',
    type: 'boolean',
    description: 'クエリに関連する画像をレスポンスに含めます。',
    isOptional: true,
  },
  {
    name: 'includeImageDescriptions',
    type: 'boolean',
    description: '返される画像の説明を含めます。',
    isOptional: true,
  },
  {
    name: 'includeRawContent',
    type: "false | 'markdown' | 'text'",
    description: "各結果のクリーンアップ済み HTML コンテンツを含めます。無効にするには false、形式を指定するには 'markdown' または 'text' を渡します。",
    isOptional: true,
  },
  {
    name: 'includeDomains',
    type: 'string[]',
    description: '結果を指定したドメインに限定します。',
    isOptional: true,
  },
  {
    name: 'excludeDomains',
    type: 'string[]',
    description: '指定したドメインの結果を除外します。',
    isOptional: true,
  },
  {
    name: 'timeRange',
    type: "'day' | 'week' | 'month' | 'year'",
    description: '結果を新しさで絞り込みます。',
    isOptional: true,
  },
]}
/>

### 出力

<PropertiesTable
  content={[
  {
    name: 'query',
    type: 'string',
    description: '元の検索クエリ。',
  },
  {
    name: 'answer',
    type: 'string',
    description: 'AI が生成した回答の概要。',
    isOptional: true,
  },
  {
    name: 'images',
    type: '{ url: string; description?: string }[]',
    description: '関連画像。',
    isOptional: true,
  },
  {
    name: 'results',
    type: 'SearchResult[]',
    description: '検索結果の配列。',
    properties: [
      {
        type: 'SearchResult',
        parameters: [
          { name: 'title', type: 'string', description: '結果のタイトル。' },
          { name: 'url', type: 'string', description: '結果の URL。' },
          { name: 'content', type: 'string', description: 'コンテンツの抜粋。' },
          { name: 'score', type: 'number', description: '関連度スコア。' },
          { name: 'rawContent', type: 'string', description: 'ページ全体のコンテンツ（要求した場合）。', isOptional: true },
        ],
      },
    ],
  },
  {
    name: 'responseTime',
    type: 'number',
    description: 'サーバーのレスポンス時間（秒）。',
  },
]}
/>

## `createTavilyExtractTool()`

1つ以上の URL からコンテンツを抽出する Tool を作成します。1回のリクエストにつき最大20件の URL から、生のページコンテンツを Markdown またはテキスト形式で返します。

**Tool ID:** `tavily-extract`

```typescript
import { createTavilyExtractTool } from '@mastra/tavily'

const extractTool = createTavilyExtractTool()
```

### 入力

<PropertiesTable
  content={[
  {
    name: 'urls',
    type: 'string[]',
    description: 'コンテンツを抽出する URL（1～20件）。',
  },
  {
    name: 'extractDepth',
    type: "'basic' | 'advanced'",
    description: "抽出の深さ。テーブルや埋め込みコンテンツを取得するには 'advanced' を使用します。",
    isOptional: true,
  },
  {
    name: 'query',
    type: 'string',
    description: '抽出したコンテンツチャンクを関連度で再順位付けするためのユーザーの意図。',
    isOptional: true,
  },
  {
    name: 'includeImages',
    type: 'boolean',
    description: 'ページから抽出した画像を含めます。',
    isOptional: true,
  },
  {
    name: 'format',
    type: "'markdown' | 'text'",
    description: '抽出したコンテンツの出力形式。',
    isOptional: true,
    defaultValue: "'markdown'",
  },
]}
/>

### 出力

<PropertiesTable
  content={[
  {
    name: 'results',
    type: 'ExtractResult[]',
    description: '抽出に成功したページ。',
    properties: [
      {
        type: 'ExtractResult',
        parameters: [
          { name: 'url', type: 'string', description: 'ページの URL。' },
          { name: 'rawContent', type: 'string', description: '抽出されたページコンテンツ。' },
          { name: 'images', type: 'string[]', description: '抽出された画像の URL。', isOptional: true },
        ],
      },
    ],
  },
  {
    name: 'failedResults',
    type: 'FailedResult[]',
    description: '抽出に失敗した URL。',
    properties: [
      {
        type: 'FailedResult',
        parameters: [
          { name: 'url', type: 'string', description: '失敗した URL。' },
          { name: 'error', type: 'string', description: 'エラーメッセージ。' },
        ],
      },
    ],
  },
  {
    name: 'responseTime',
    type: 'number',
    description: 'サーバーのレスポンス時間（秒）。',
  },
]}
/>

## `createTavilyCrawlTool()`

指定した URL を起点に Web サイトをクロールする Tool を作成します。設定可能な深さ、幅、ドメイン制約に従って、検出したページからコンテンツを抽出します。

**Tool ID:** `tavily-crawl`

```typescript
import { createTavilyCrawlTool } from '@mastra/tavily'

const crawlTool = createTavilyCrawlTool()
```

### 入力

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: 'クロールを開始するルート URL。',
  },
  {
    name: 'maxDepth',
    type: 'number',
    description: 'ベース URL からのクロールの最大深度。',
    isOptional: true,
  },
  {
    name: 'maxBreadth',
    type: 'number',
    description: 'ページごとにたどるリンクの最大数。',
    isOptional: true,
  },
  {
    name: 'limit',
    type: 'number',
    description: 'クローラーが停止するまでに処理するページの総数。',
    isOptional: true,
  },
  {
    name: 'instructions',
    type: 'string',
    description: 'クローラーに対する自然言語の指示。',
    isOptional: true,
  },
  {
    name: 'selectPaths',
    type: 'string[]',
    description: '特定の URL パスを選択する正規表現パターン。',
    isOptional: true,
  },
  {
    name: 'selectDomains',
    type: 'string[]',
    description: '特定のドメインに限定する正規表現パターン。',
    isOptional: true,
  },
  {
    name: 'excludePaths',
    type: 'string[]',
    description: '特定の URL パスを除外する正規表現パターン。',
    isOptional: true,
  },
  {
    name: 'excludeDomains',
    type: 'string[]',
    description: '特定のドメインを除外する正規表現パターン。',
    isOptional: true,
  },
  {
    name: 'allowExternal',
    type: 'boolean',
    description: '外部ドメインへのリンクをたどるかどうか。',
    isOptional: true,
  },
  {
    name: 'extractDepth',
    type: "'basic' | 'advanced'",
    description: "抽出の深さ。テーブルや埋め込みコンテンツを取得するには 'advanced' を使用します。",
    isOptional: true,
  },
  {
    name: 'includeImages',
    type: 'boolean',
    description: 'クロールしたページの画像を含めます。',
    isOptional: true,
  },
  {
    name: 'format',
    type: "'markdown' | 'text'",
    description: '抽出したコンテンツの出力形式。',
    isOptional: true,
    defaultValue: "'markdown'",
  },
]}
/>

### 出力

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: 'クロールしたルート URL。',
  },
  {
    name: 'results',
    type: 'CrawlResult[]',
    description: '検出したページから抽出されたコンテンツ。',
    properties: [
      {
        type: 'CrawlResult',
        parameters: [
          { name: 'url', type: 'string', description: 'ページの URL。' },
          { name: 'rawContent', type: 'string', description: '抽出されたページコンテンツ。' },
          { name: 'images', type: 'string[]', description: 'ページで見つかった画像の URL。', isOptional: true },
        ],
      },
    ],
  },
  {
    name: 'responseTime',
    type: 'number',
    description: 'サーバーのレスポンス時間（秒）。',
  },
]}
/>

## `createTavilyMapTool()`

指定した URL を起点に Web サイトの構造をマッピングする Tool を作成します。ページコンテンツは抽出せず、URL を検出して一覧を返します。対象を絞った抽出の前にサイト構造を把握するために使用します。

**Tool ID:** `tavily-map`

```typescript
import { createTavilyMapTool } from '@mastra/tavily'

const mapTool = createTavilyMapTool()
```

### 入力

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: 'マッピングを開始するルート URL。',
  },
  {
    name: 'maxDepth',
    type: 'number',
    description: 'ベース URL からのマッピングの最大深度。',
    isOptional: true,
  },
  {
    name: 'maxBreadth',
    type: 'number',
    description: 'ページごとにたどるリンクの最大数。',
    isOptional: true,
  },
  {
    name: 'limit',
    type: 'number',
    description: 'マッパーが停止するまでに処理するリンクの総数。',
    isOptional: true,
  },
  {
    name: 'instructions',
    type: 'string',
    description: 'マッパーに対する自然言語の指示。',
    isOptional: true,
  },
  {
    name: 'selectPaths',
    type: 'string[]',
    description: '特定の URL パスを選択する正規表現パターン。',
    isOptional: true,
  },
  {
    name: 'selectDomains',
    type: 'string[]',
    description: '特定のドメインに限定する正規表現パターン。',
    isOptional: true,
  },
  {
    name: 'excludePaths',
    type: 'string[]',
    description: '特定の URL パスを除外する正規表現パターン。',
    isOptional: true,
  },
  {
    name: 'excludeDomains',
    type: 'string[]',
    description: '特定のドメインを除外する正規表現パターン。',
    isOptional: true,
  },
  {
    name: 'allowExternal',
    type: 'boolean',
    description: '外部ドメインのリンクを含めるかどうか。',
    isOptional: true,
  },
]}
/>

### 出力

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: 'マッピングしたルート URL。',
  },
  {
    name: 'results',
    type: 'string[]',
    description: '検出された URL。',
  },
  {
    name: 'responseTime',
    type: 'number',
    description: 'サーバーのレスポンス時間（秒）。',
  },
]}
/>

## Agent の例

次の例は、検索と抽出を組み合わせた調査 Agent を示します。

```typescript title="src/mastra/agents/index.ts"
import { Agent } from '@mastra/core/agent'
import { createTavilySearchTool, createTavilyExtractTool } from '@mastra/tavily'

const agent = new Agent({
  id: 'web-search-agent',
  name: 'Web Search Agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  instructions:
    'You are a web search assistant. Use search tool to find relevant pages, then use extract tool to get full content from the best results.',
  tools: {
    search: createTavilySearchTool(),
    extract: createTavilyExtractTool(),
  },
})
```

## 環境変数

| 変数 | 説明 |
| - | - |
| `TAVILY_API_KEY` | Tavily API キー。ファクトリー関数に `apiKey` を渡さなかった場合のデフォルトとして使用されます。 |

## 関連項目

- [`createTool()`](./create-tool)
- [Tavily API ドキュメント](https://docs.tavily.com)
