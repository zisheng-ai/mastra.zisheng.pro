---
title: "リファレンス: SemanticRecall | Processors"
description: "ベクトル埋め込みを使用して会話履歴をセマンティック検索できる Mastra の SemanticRecall processor のドキュメント。"
packages:
  - "@mastra/core"
  - "@mastra/pg"
---

# SemanticRecall

`SemanticRecall` は、ベクトル埋め込みを使用して会話履歴をセマンティック検索できる **hybrid processor** です。入力時にはセマンティック検索を実行し、関連する過去のメッセージを取得します。出力時には新しいメッセージの埋め込みを作成し、以後のセマンティック検索で取得できるようにします。

## 使用例

```typescript
import { SemanticRecall } from '@mastra/core/processors'
import { openai } from '@ai-sdk/openai'

const processor = new SemanticRecall({
  storage: memoryStorage,
  vector: vectorStore,
  embedder: openai.embedding('text-embedding-3-small'),
  topK: 5,
  messageRange: 2,
  scope: 'resource',
})
```

## コンストラクターパラメーター

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'SemanticRecallOptions',
    description: 'semantic recall processor の設定オプション',
    isOptional: false,
    properties: [
      {
        type: 'SemanticRecallOptions',
        parameters: [
          {
            name: 'storage',
            type: 'MemoryStorage',
            description: 'メッセージを取得するためのストレージインスタンス',
            isOptional: false,
          },
          {
            name: 'vector',
            type: 'MastraVector',
            description: 'セマンティック検索に使用するベクトルストア',
            isOptional: false,
          },
          {
            name: 'embedder',
            type: 'MastraEmbeddingModel<string>',
            description: 'クエリの埋め込みを生成する embedder',
            isOptional: false,
          },
          {
            name: 'topK',
            type: 'number',
            description: '取得する類似度の高いメッセージの数',
            isOptional: true,
            default: '4',
          },
          {
            name: 'messageRange',
            type: 'number | { before: number; after: number }',
            description:
              '各一致の前後に含めるコンテキストメッセージの数。前後で同じ単一の数値、または個別の値を持つオブジェクトを指定できます',
            isOptional: true,
            default: '1',
          },
          {
            name: 'scope',
            type: "'thread' | 'resource'",
            description:
              "セマンティック検索の範囲。'thread' は現在の thread 内のみ、'resource' はその resource のすべての thread を検索します",
            isOptional: true,
            default: "'resource'",
          },
          {
            name: 'threshold',
            type: 'number',
            description: '類似度スコアの最小しきい値（0〜1）。この値を下回るメッセージは除外されます',
            isOptional: true,
          },
          {
            name: 'indexName',
            type: 'string',
            description: 'ベクトルストアのインデックス名。未指定の場合は embedder モデルに基づいて自動生成されます',
            isOptional: true,
          },
          {
            name: 'logger',
            type: 'IMastraLogger',
            description: '構造化ログに使用する任意の logger インスタンス',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

## 戻り値

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "'semantic-recall' に設定された processor の識別子",
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: "'SemanticRecall' に設定された processor の表示名",
    isOptional: false,
  },
  {
    name: 'processInput',
    type: '(args: { messages: MastraDBMessage[]; messageList: MessageList; abort: (reason?: string) => never; tracingContext?: TracingContext; requestContext?: RequestContext }) => Promise<MessageList | MastraDBMessage[]>',
    description: '過去のメッセージをセマンティック検索し、関連するコンテキストをメッセージリストに追加します',
    isOptional: false,
  },
  {
    name: 'processOutputResult',
    type: '(args: { messages: MastraDBMessage[]; messageList?: MessageList; abort: (reason?: string) => never; tracingContext?: TracingContext; requestContext?: RequestContext }) => Promise<MessageList | MastraDBMessage[]>',
    description: '新しいメッセージの埋め込みを作成し、以後のセマンティック検索で利用できるようにします',
    isOptional: false,
  },
]}
/>

## 詳細な使用例

```typescript title="src/mastra/agents/semantic-memory-agent.ts"
import { Agent } from '@mastra/core/agent'
import { SemanticRecall, MessageHistory } from '@mastra/core/processors'
import { PostgresStorage } from '@mastra/pg'
import { PgVector } from '@mastra/pg'
import { openai } from '@ai-sdk/openai'

const storage = new PostgresStorage({
  id: 'pg-storage',
  connectionString: process.env.DATABASE_URL,
})

const vector = new PgVector({
  id: 'pg-vector',
  connectionString: process.env.DATABASE_URL,
})

const semanticRecall = new SemanticRecall({
  storage,
  vector,
  embedder: openai.embedding('text-embedding-3-small'),
  topK: 5,
  messageRange: { before: 2, after: 1 },
  scope: 'resource',
  threshold: 0.7,
})

export const agent = new Agent({
  id: 'semantic-memory-agent',
  name: 'semantic-memory-agent',
  instructions: 'You are a helpful assistant with semantic memory recall',
  model: '__GATEWAY_OPENAI_MODEL__',
  inputProcessors: [semanticRecall, new MessageHistory({ storage, lastMessages: 50 })],
  outputProcessors: [semanticRecall, new MessageHistory({ storage })],
})
```

## 動作

### 入力処理

1. 最後のユーザーメッセージからユーザークエリを抽出する
1. クエリの埋め込みを生成する
1. ベクトル検索を実行して、意味的に類似するメッセージを見つける
1. 一致したメッセージを、その前後のコンテキストとともに取得する（`messageRange` に基づく）
1. `scope: 'resource'` の場合、別 thread のメッセージをタイムスタンプ付きの system メッセージとして整形する
1. 取得したメッセージに `source: 'memory'` タグを付けて追加する

### 出力処理

1. 新しい user および assistant メッセージからテキストコンテンツを抽出する
1. 各メッセージの埋め込みを生成する
1. 埋め込みをメタデータ（メッセージ ID、thread ID、resource ID、role、content、timestamp）とともにベクトルストアへ保存する
1. 埋め込みに LRU キャッシュを使用して、重複する API 呼び出しを避ける

### thread をまたぐ取得

`scope` を `'resource'` に設定すると、processor は別の thread からメッセージを取得できます。これらのメッセージは、会話がいつ、どこで行われたかを示すコンテキストを提供するため、タイムスタンプと会話ラベル付きの system メッセージとして整形されます。

## 関連情報

- [Guardrails](/docs/agents/guardrails)
