---
title: "リファレンス: UnicodeNormalizer | Processors"
description: "Unicode テキストを正規化して一貫した形式に整え、問題を起こす可能性のある文字を除去する Mastra の UnicodeNormalizer のドキュメント。"
packages:
  - "@mastra/core"
---

# UnicodeNormalizer

`UnicodeNormalizer` は、メッセージを言語モデルへ送信する前に Unicode テキストを正規化し、一貫した形式に整えて問題を起こす可能性のある文字を除去する **input processor** です。Unicode 表現を処理して制御文字を除去し、空白の形式も統一します。

## 使用例 {/* #usage-example */}

```typescript
import { UnicodeNormalizer } from '@mastra/core/processors'

const processor = new UnicodeNormalizer({
  stripControlChars: true,
  collapseWhitespace: true,
})
```

## コンストラクターパラメーター {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'Options',
    description: 'Unicode テキストを正規化するための設定オプション',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'stripControlChars',
            type: 'boolean',
            description: '制御文字を除去するかどうか。有効にすると、`\t`、`\n`、`\r` 以外の制御文字を除去します',
            isOptional: true,
            default: 'false',
          },
          {
            name: 'preserveEmojis',
            type: 'boolean',
            description: '絵文字を保持するかどうか。無効にすると、制御文字を含む絵文字が除去される場合があります',
            isOptional: true,
            default: 'true',
          },
          {
            name: 'collapseWhitespace',
            type: 'boolean',
            description:
              '連続する空白をまとめるかどうか。有効にすると、複数のスペース、タブ、改行をそれぞれ1つにまとめます',
            isOptional: true,
            default: 'true',
          },
          {
            name: 'trim',
            type: 'boolean',
            description: '先頭と末尾の空白を除去するかどうか',
            isOptional: true,
            default: 'true',
          },
        ],
      },
    ],
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "'unicode-normalizer' に設定された processor の識別子",
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: '任意の processor 表示名',
    isOptional: true,
  },
  {
    name: 'processInput',
    type: '(args: { messages: MastraDBMessage[]; abort: (reason?: string) => never }) => MastraDBMessage[]',
    description: '入力メッセージを処理して Unicode テキストを正規化します',
    isOptional: false,
  },
]}
/>

## 詳細な使用例 {/* #extended-usage-example */}

```typescript title="src/mastra/agents/normalized-agent.ts"
import { Agent } from '@mastra/core/agent'
import { UnicodeNormalizer } from '@mastra/core/processors'

export const agent = new Agent({
  id: 'normalized-agent',
  name: 'normalized-agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
  inputProcessors: [
    new UnicodeNormalizer({
      stripControlChars: true,
      preserveEmojis: true,
      collapseWhitespace: true,
      trim: true,
    }),
  ],
})
```

## 関連情報 {/* #related */}

- [Guardrails](/docs/agents/guardrails)
