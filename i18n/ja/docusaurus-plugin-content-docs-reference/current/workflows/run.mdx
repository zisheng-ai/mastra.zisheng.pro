---
title: "リファレンス: Run クラス | Workflows"
description: Workflow の実行インスタンスを表す Mastra の Run クラスのドキュメント。
packages:
  - "@mastra/core"
---

# Run クラス

`Run` クラスは Workflow の実行インスタンスを表し、Workflow の実行を開始、再開、ストリーミング、監視するためのメソッドを提供します。

## 使用例 {/* #usage-example */}

```typescript
const run = await workflow.createRun()

const result = await run.start({
  inputData: { value: 'initial data' },
})

if (result.status === 'suspended') {
  const resumedResult = await run.resume({
    resumeData: { value: 'resume data' },
  })
}
```

## Run メソッド {/* #run-methods */}

<PropertiesTable
  content={[
  {
    name: 'start',
    type: '(options?: StartOptions) => Promise<WorkflowResult>',
    description: '入力データを使用して Workflow の実行を開始します',
    required: true,
  },
  {
    name: 'resume',
    type: '(options?: ResumeOptions) => Promise<WorkflowResult>',
    description: '中断された Workflow を特定のステップから再開します',
    required: true,
  },
  {
    name: 'stream',
    type: '(options?: StreamOptions) => MastraWorkflowStream',
    description: '拡張ストリーミング機能を使用し、Workflow の実行をイベントストリームとして監視します',
    required: true,
  },
  {
    name: 'resumeStream',
    type: '(options?: ResumeStreamOptions) => MastraWorkflowStream',
    description: 'ストリーミングを使用して、中断された Workflow を再開します',
    required: true,
  },
  {
    name: 'cancel',
    type: '() => Promise<{ message: string }>',
    description: 'Workflow の実行をキャンセルし、実行中のステップを停止して、後続のステップが実行されないようにします',
    required: true,
  },
  {
    name: 'restart',
    type: '(options?: RestartOptions) => Promise<WorkflowResult>',
    description: '最後にアクティブだったステップから Workflow の実行を再起動します',
    required: true,
  },
  {
    name: 'timeTravel',
    type: '(options?: TimeTravelOptions) => Promise<WorkflowResult>',
    description:
      '保存されたスナップショットデータまたは指定したカスタムコンテキストを使用し、任意のステップから Workflow を再実行します。',
    required: true,
  },
  {
    name: 'timeTravelStream',
    type: '(options?: TimeTravelOptions) => MastraWorkflowStream',
    description: 'ストリーミングを使用して Workflow の実行をタイムトラベルします',
    required: true,
  },
]}
/>

## Run のステータス {/* #run-status */}

Workflow Run の `status` は、現在の実行状態を示します。指定できる値は次のとおりです。

<PropertiesTable
  content={[
  {
    name: 'success',
    type: 'string',
    description: 'すべてのステップが正常に実行を完了し、有効な結果が出力されました',
  },
  {
    name: 'failed',
    type: 'string',
    description: 'Workflow の実行中にエラーが発生し、エラーの詳細を確認できます',
  },
  {
    name: 'suspended',
    type: 'string',
    description: 'Workflow の実行が再開を待って一時停止しており、中断されたステップの情報を確認できます',
  },
  {
    name: 'canceled',
    type: 'string',
    description:
      'Workflow の実行が cancel() メソッドによってキャンセルされ、実行中のステップが停止し、後続のステップが実行されない状態です',
  },
]}
/>

## 関連項目 {/* #related */}

- [Run.start()](./run-methods/start)
- [Run.resume()](./run-methods/resume)
- [Run.cancel()](./run-methods/cancel)
- [Run.restart()](./run-methods/restart)
- [Run.timeTravel()](./run-methods/timeTravel)
- [Run.stream()](/docs/workflows/overview#inspecting-workflow-stream-payloads)
- [Run.timeTravelStream()](../streaming/workflows/timeTravelStream)
