---
title: "リファレンス: Run.timeTravel() | Workflows"
description: 特定のステップから Workflow を再実行する `Run.timeTravel()` メソッドのドキュメント。
packages:
  - "@mastra/core"
---

# Run.timeTravel() {/* #runtimetravel */}

`.timeTravel()` メソッドは、保存されたスナップショットデータまたは指定したカスタムコンテキストを使用し、任意のステップから Workflow を再実行します。失敗した Workflow のデバッグや、異なる入力による個々のステップのテストに使用できます。Workflow 全体を再実行せずにエラーから復旧することもできます。

## 使用例 {/* #usage-example */}

```typescript
const run = await workflow.createRun()

const result = await run.timeTravel({
  step: 'step2',
  inputData: { value: 10 },
})
```

## パラメーター {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'step',
    type: 'Step<string, any, TInputSchema, any, any, any, TEngineType> | [...Step<string, any, any, any, any, any, TEngineType>[], Step<string, any, TInputSchema, any, any, any, TEngineType>] | string | string[]',
    description:
      "実行を開始する対象のステップ。Step インスタンス、Step の配列（ネストされた Workflow の場合）、ステップ ID の文字列、またはステップ ID の文字列配列を指定できます。ネストされた Workflow のステップには、ドット記法または配列（例: 'nestedWorkflow.step3' または ['nestedWorkflow', 'step3']）を使用します",
    isOptional: false,
  },
  {
    name: 'inputData',
    type: 'z.infer<TInputSchema>',
    description:
      '対象ステップの入力データ。ステップの入力スキーマに一致する必要があります。指定しない場合は、Workflow のスナップショットにあるデータを使用します',
    isOptional: true,
  },
  {
    name: 'resumeData',
    type: 'any',
    description: 'Workflow が以前に中断されていた場合に渡す再開データ',
    isOptional: true,
  },
  {
    name: 'initialState',
    type: 'z.infer<TState>',
    description: 'Workflow Run に設定する初期状態。実行前に Workflow レベルの状態を設定するために使用します',
    isOptional: true,
  },
  {
    name: 'context',
    type: 'TimeTravelContext<any, any, any, any>',
    description:
      '対象ステップより前のステップ結果を含む実行コンテキスト。各キーはステップ ID で、その値は status、payload、output、startedAt、endedAt、suspendPayload、resumePayload を含む StepResult オブジェクトです',
    isOptional: true,
  },
  {
    name: 'nestedStepsContext',
    type: 'Record<string, TimeTravelContext<any, any, any, any>>',
    description:
      'ネストされた Workflow ステップのコンテキスト。ネストされた Workflow ID をキーとし、それぞれにその Workflow のステップ結果が含まれます',
    isOptional: true,
  },
  {
    name: 'requestContext',
    type: 'RequestContext',
    description: 'タイムトラベル実行中に使用する Request Context データ',
    isOptional: true,
  },
  {
    name: 'outputWriter',
    type: '(chunk: TOutput) => Promise<void>',
    description: '生成された出力チャンクを処理する任意の非同期関数',
    isOptional: true,
  },
  {
    name: 'tracingContext',
    type: 'TracingContext',
    isOptional: true,
    description:
      '子 span の作成と metadata の追加に使用する Tracing context。Mastra の tracing system を使用すると自動的に挿入されます。',
    properties: [
      {
        parameters: [
          {
            name: 'currentSpan',
            type: 'Span',
            isOptional: true,
            description:
              '子 span の作成と metadata の追加に使用する現在の span。実行中にカスタムの子 span を作成したり、span attribute を更新したりするために使用します。',
          },
        ],
      },
    ],
  },
  {
    name: 'tracingOptions',
    type: 'TracingOptions',
    isOptional: true,
    description: 'Tracing 設定のオプション。',
    properties: [
      {
        parameters: [
          {
            name: 'metadata',
            type: 'Record<string, any>',
            isOptional: true,
            description:
              'ルート trace span に追加する metadata。ユーザー ID、セッション ID、feature flag などのカスタム属性を追加する場合に役立ちます。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'requestContextKeys',
            type: 'string[]',
            isOptional: true,
            description:
              "この trace の metadata として抽出する追加の RequestContext キー。ネストされた値にはドット記法（例: 'user.id'）を使用できます。",
          },
        ],
      },
      {
        parameters: [
          {
            name: 'traceId',
            type: 'string',
            isOptional: true,
            description:
              'この実行に使用する trace ID（1〜32 文字の 16 進数）。指定すると、この trace は指定した trace の一部になります。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'parentSpanId',
            type: 'string',
            isOptional: true,
            description:
              'この実行に使用する親 span ID（1〜16 文字の 16 進数）。指定すると、ルート span はこの span の子として作成されます。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'tags',
            type: 'string[]',
            isOptional: true,
            description: 'この trace に適用する tag。trace の分類とフィルタリングに使用する文字列ラベルです。',
          },
        ],
      },
    ],
  },
  {
    name: 'outputOptions',
    type: 'OutputOptions',
    isOptional: true,
    description: '出力設定のオプション。',
    properties: [
      {
        parameters: [
          {
            name: 'includeState',
            type: 'boolean',
            isOptional: true,
            description: '結果に Workflow Run の状態を含めるかどうか。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'includeResumeLabels',
            type: 'boolean',
            isOptional: true,
            description: '結果に resume label を含めるかどうか。',
          },
        ],
      },
    ],
  },
]}
/>

## 戻り値 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<WorkflowResult<TState, TInput, TOutput, TSteps>>',
    description: 'ステップの出力とステータスを含む Workflow の実行結果で解決される Promise',
  },
  {
    name: 'traceId',
    type: 'string',
    isOptional: true,
    description:
      'Tracing が有効な場合に、この実行に関連付けられる trace ID。ログの関連付けや実行フローのデバッグに使用します。',
  },
  {
    name: 'spanId',
    type: 'string',
    isOptional: true,
    description:
      'Tracing が有効な場合に、この実行に関連付けられるルート span ID。span 単位の検索と関連付けに使用します。',
  },
]}
/>

## その他の使用例 {/* #extended-usage-examples */}

### カスタムコンテキストを使用したタイムトラベル {/* #time-travel-with-custom-context */}

```typescript
const result = await run.timeTravel({
  step: 'step2',
  context: {
    step1: {
      status: 'success',
      payload: { value: 0 },
      output: { step1Result: 2 },
      startedAt: Date.now(),
      endedAt: Date.now(),
    },
  },
})
```

### ネストされた Workflow のステップへのタイムトラベル {/* #time-travel-to-nested-workflow-step */}

```typescript
// Using dot notation
const result = await run.timeTravel({
  step: 'nestedWorkflow.step3',
  inputData: { value: 10 },
})

// Using array of step IDs
const result = await run.timeTravel({
  step: ['nestedWorkflow', 'step3'],
  inputData: { value: 10 },
})
```

### 初期状態を使用したタイムトラベル {/* #time-travel-with-initial-state */}

```typescript
const result = await run.timeTravel({
  step: 'step2',
  inputData: { value: 10 },
  initialState: {
    counter: 5,
    metadata: { source: 'time-travel' },
  },
})
```

### ネストされた Workflow のコンテキストを使用したタイムトラベル {/* #time-travel-with-nested-workflows-context */}

```typescript
const result = await run.timeTravel({
  step: 'nestedWorkflow.step3',
  context: {
    step1: {
      status: 'success',
      payload: { value: 0 },
      output: { step1Result: 2 },
      startedAt: Date.now(),
      endedAt: Date.now(),
    },
    nestedWorkflow: {
      status: 'running',
      payload: { step1Result: 2 },
      startedAt: Date.now(),
    },
  },
  nestedStepsContext: {
    nestedWorkflow: {
      step2: {
        status: 'success',
        payload: { step1Result: 2 },
        output: { step2Result: 3 },
        startedAt: Date.now(),
        endedAt: Date.now(),
      },
    },
  },
})
```

## 注意事項 {/* #notes */}

- タイムトラベルは永続化された Workflow スナップショットを使用するため、Storage を設定する必要があります
- Workflow を再実行すると、Storage から既存のスナップショットが読み込まれます（利用可能な場合）
- 対象ステップより前のステップ結果は、スナップショットまたは指定したコンテキストから再構築されます
- 指定したステップから、指定または再構築された入力データを使用して実行が開始されます
- Workflow はその時点から完了まで続行されます
- まだ実行されていない Workflow でも、開始するステップのカスタムコンテキストまたは入力データを指定すれば、タイムトラベルを使用できます。

## 関連項目 {/* #related */}

- [タイムトラベル](/docs/workflows/time-travel)
- [Workflow の概要](/docs/workflows/overview#running-workflows)
- [Workflow.createRun()](../workflow-methods/create-run)
- [スナップショット](/docs/workflows/snapshots)
- [中断と再開](/docs/workflows/suspend-and-resume)
