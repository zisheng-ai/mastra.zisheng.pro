---
title: "リファレンス: Workflow クラス | Workflows"
description: 条件分岐とデータ検証を含む複雑な処理シーケンスのステートマシンを作成できる、Mastra の `Workflow` クラスのドキュメント。
packages:
  - "@mastra/core"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Workflow クラス

`Workflow` クラスを使用すると、条件分岐とデータ検証を含む複雑な処理シーケンスのステートマシンを作成できます。

## 使用例 {/* #usage-example */}

```typescript title="src/mastra/workflows/test-workflow.ts"
import { createWorkflow } from '@mastra/core/workflows'
import { z } from 'zod'

export const workflow = createWorkflow({
  id: 'test-workflow',
  inputSchema: z.object({
    value: z.string(),
  }),
  outputSchema: z.object({
    value: z.string(),
  }),
})
```

## スキーマを定義する {/* #define-schemas */}

Workflow の `inputSchema` と `outputSchema` は、[Standard JSON Schema](https://standardschema.dev/json-schema) をサポートする任意のライブラリで定義できます。[Zod](https://zod.dev/)、[Valibot](https://valibot.dev/)、[ArkType](https://arktype.io/) などのライブラリが該当します。

<Tabs>
  <TabItem value="zod" label="Zod">
    ```typescript {9,12,15,16} title="src/mastra/workflows/test-workflow.ts" prettier:false
    import { createWorkflow, createStep } from "@mastra/core/workflows";
    import { z } from "zod";

    const step1 = createStep({...});

    export const testWorkflow = createWorkflow({
      id: "test-workflow",
      inputSchema: z.object({
        message: z.string()
      }),
      outputSchema: z.object({
        output: z.string()
      })
    })
      .then(step1)
      .commit();
    ```
  </TabItem>

  <TabItem value="valibot" label="Valibot">
    ```typescript {10,13,16,17} title="src/mastra/workflows/test-workflow.ts" prettier:false
    import { createWorkflow, createStep } from "@mastra/core/workflows";
    import * as v from "valibot";
    import { toStandardJsonSchema } from "@valibot/to-json-schema";

    const step1 = createStep({...});

    export const testWorkflow = createWorkflow({
      id: "test-workflow",
      inputSchema: toStandardJsonSchema(v.object({
        message: v.string()
      })),
      outputSchema: toStandardJsonSchema(v.object({
        output: v.string()
      }))
    })
      .then(step1)
      .commit();
    ```
  </TabItem>

  <TabItem value="arktype" label="ArkType">
    ```typescript {9,12,15,16} title="src/mastra/workflows/test-workflow.ts" prettier:false
    import { createWorkflow, createStep } from "@mastra/core/workflows";
    import { type } from "arktype";

    const step1 = createStep({...});

    export const testWorkflow = createWorkflow({
      id: "test-workflow",
      inputSchema: type({
        message: "string"
      }),
      outputSchema: type({
        output: "string"
      })
    })
      .then(step1)
      .commit();
    ```
  </TabItem>
</Tabs>

## コンストラクターのパラメーター {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: 'Workflow の一意な識別子',
  },
  {
    name: 'inputSchema',
    type: 'StandardJSONSchemaV1',
    description: 'Workflow の入力構造を定義する Standard JSON Schema',
  },
  {
    name: 'outputSchema',
    type: 'StandardJSONSchemaV1',
    description: 'Workflow の出力構造を定義する Standard JSON Schema',
  },
  {
    name: 'stateSchema',
    type: 'StandardJSONSchemaV1',
    description:
      "Workflow state の任意の Standard JSON Schema。Mastra の state system を使用すると自動的に挿入されます。指定しない場合、型は 'any' になります。",
    isOptional: true,
  },
  {
    name: 'requestContextSchema',
    type: 'StandardJSONSchemaV1',
    description:
      'Request Context の値を検証する Standard JSON Schema。指定すると run.start() の開始時に context が検証され、検証に失敗した場合はエラーがスローされます。',
    isOptional: true,
  },
  {
    name: 'schedule',
    type: 'WorkflowScheduleConfig | WorkflowScheduleConfig[]',
    description:
      'Workflow の任意の cron スケジュール。単一の設定、または複数の頻度で起動するための設定配列を受け取ります。設定すると、Workflow は evented execution engine に自動的に昇格します。使用方法については、スケジュールされた Workflow のガイドを参照してください。',
    isOptional: true,
    properties: [
      {
        type: 'WorkflowScheduleConfig',
        parameters: [
          {
            name: 'id',
            type: 'string',
            description:
              'スケジュールの安定した識別子。スケジュールの配列を渡す場合は必須です。単一のスケジュールオブジェクトを渡す場合、デフォルトは Workflow ID です。',
            isOptional: true,
          },
          {
            name: 'cron',
            type: 'string',
            description: '5、6、または 7 パートの cron 式。Workflow の構築時に検証されます。',
          },
          {
            name: 'timezone',
            type: 'string',
            description:
              'IANA タイムゾーン（例: "America/New_York"）。デフォルトはホストのローカルタイムゾーンです。起動時刻がサーバーのロケールに左右されないよう、本番環境では明示的に設定してください。',
            isOptional: true,
          },
          {
            name: 'inputData',
            type: 'TInput',
            description: '起動のたびに Workflow の入力として渡されるペイロード。',
            isOptional: true,
          },
          {
            name: 'initialState',
            type: 'TState',
            description: 'Run の初期 state。',
            isOptional: true,
          },
          {
            name: 'requestContext',
            type: 'Record<string, unknown>',
            description: 'Run に付加される Request Context。',
            isOptional: true,
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            description: 'スケジュール行とともに永続化される任意の metadata。',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'options',
    type: 'WorkflowOptions',
    description: 'Workflow の任意のオプション',
    isOptional: true,
    properties: [
      {
        type: 'WorkflowOptions',
        parameters: [
          {
            name: 'tracingPolicy',
            type: 'TracingPolicy',
            description: 'Workflow の任意の tracing policy',
            isOptional: true,
          },
          {
            name: 'validateInputs',
            type: 'boolean',
            description:
              'Workflow の入力を検証するかどうかを指定する任意のフラグ。Workflow またはステップの input／resume data にある zodSchema のデフォルト値も適用します。start／resume 時に input／resume data の検証が失敗すると、Workflow は開始／再開せず、代わりにエラーをスローします。ステップの実行時に入力データの検証が失敗すると、そのステップが失敗し、Workflow も失敗してエラーが返されます。',
            isOptional: true,
            defaultValue: 'true',
          },
          {
            name: 'shouldPersistSnapshot',
            type: '(params: { stepResults: Record<string, StepResult<any, any, any, any>>; workflowStatus: WorkflowRunStatus }) => boolean',
            description: 'Workflow のスナップショットを永続化するかどうかを決定する任意のフラグ',
            isOptional: true,
            defaultValue: '() => true',
          },
          {
            name: 'pruneSnapshot',
            type: '(params: { snapshot: WorkflowRunState; workflowStatus: WorkflowRunStatus }) => WorkflowRunState',
            description:
              'Workflow のスナップショットを永続化する直前に変換する任意のフック。JSON にシリアライズ可能なデータを返し、Workflow の再開に必要なすべての情報（中断されたステップの suspendPayloads、suspendedPaths、executionPath など）を保持する必要があります。Agent Run ではスナップショットを最小限に保つため内部的に使用されます。ユーザーの Workflow では、デフォルトで完全なスナップショットが永続化されます。',
            isOptional: true,
          },
          {
            name: 'onFinish',
            type: '(result: WorkflowFinishCallbackResult) => void | Promise<void>',
            description:
              'Workflow がいずれかのステータス（success、failed、suspended、tripwire）で完了したときに呼び出されるコールバック。ステータス、出力、エラー、ステップ結果を含む Workflow の結果を受け取ります。このコールバック内でスローされたエラーは捕捉されてログに記録され、伝播しません。',
            isOptional: true,
            properties: [
              {
                type: 'WorkflowFinishCallbackResult',
                parameters: [
                  {
                    name: 'status',
                    type: 'WorkflowRunStatus',
                    description: "Workflow のステータス: 'success'、'failed'、'suspended'、または 'tripwire'",
                  },
                  {
                    name: 'result',
                    type: 'any',
                    description: "Workflow の出力（ステータスが 'success' の場合）",
                    isOptional: true,
                  },
                  {
                    name: 'error',
                    type: 'SerializedError',
                    description: "エラーの詳細（ステータスが 'failed' の場合）",
                    isOptional: true,
                  },
                  {
                    name: 'steps',
                    type: 'Record<string, StepResult>',
                    description: '各ステップのステータスと出力を含む個別のステップ結果',
                  },
                  {
                    name: 'tripwire',
                    type: 'StepTripwireInfo',
                    description: "Tripwire の情報（ステータスが 'tripwire' の場合）",
                    isOptional: true,
                  },
                  {
                    name: 'runId',
                    type: 'string',
                    description: 'この Workflow Run の一意な識別子',
                  },
                  {
                    name: 'workflowId',
                    type: 'string',
                    description: 'Workflow の識別子',
                  },
                  {
                    name: 'resourceId',
                    type: 'string',
                    description: '任意のリソース識別子（Run の作成時に指定した場合）',
                    isOptional: true,
                  },
                  {
                    name: 'getInitData',
                    type: '() => any',
                    description: 'Workflow に渡された初期入力データを返す関数',
                  },
                  {
                    name: 'mastra',
                    type: 'Mastra',
                    description: 'Mastra インスタンス（Workflow が Mastra に登録されている場合）',
                    isOptional: true,
                  },
                  {
                    name: 'requestContext',
                    type: 'RequestContext',
                    description: 'リクエストスコープの context データ',
                  },
                  {
                    name: 'logger',
                    type: 'IMastraLogger',
                    description: 'Workflow の logger インスタンス',
                  },
                  {
                    name: 'state',
                    type: 'Record<string, any>',
                    description: 'Workflow の現在の state オブジェクト',
                  },
                ],
              },
            ],
          },
          {
            name: 'onError',
            type: '(errorInfo: WorkflowErrorCallbackInfo) => void | Promise<void>',
            description:
              'Workflow が失敗した場合（failed または tripwire ステータス）にのみ呼び出されるコールバック。エラーの詳細とステップ結果を受け取ります。このコールバック内でスローされたエラーは捕捉されてログに記録され、伝播しません。',
            isOptional: true,
            properties: [
              {
                type: 'WorkflowErrorCallbackInfo',
                parameters: [
                  {
                    name: 'status',
                    type: "'failed' | 'tripwire'",
                    description: "Workflow のステータス（'failed' または 'tripwire'）",
                  },
                  {
                    name: 'error',
                    type: 'SerializedError',
                    description: 'エラーの詳細',
                    isOptional: true,
                  },
                  {
                    name: 'steps',
                    type: 'Record<string, StepResult>',
                    description: '各ステップのステータスと出力を含む個別のステップ結果',
                  },
                  {
                    name: 'tripwire',
                    type: 'StepTripwireInfo',
                    description: "Tripwire の情報（ステータスが 'tripwire' の場合）",
                    isOptional: true,
                  },
                  {
                    name: 'runId',
                    type: 'string',
                    description: 'この Workflow Run の一意な識別子',
                  },
                  {
                    name: 'workflowId',
                    type: 'string',
                    description: 'Workflow の識別子',
                  },
                  {
                    name: 'resourceId',
                    type: 'string',
                    description: '任意のリソース識別子（Run の作成時に指定した場合）',
                    isOptional: true,
                  },
                  {
                    name: 'getInitData',
                    type: '() => any',
                    description: 'Workflow に渡された初期入力データを返す関数',
                  },
                  {
                    name: 'mastra',
                    type: 'Mastra',
                    description: 'Mastra インスタンス（Workflow が Mastra に登録されている場合）',
                    isOptional: true,
                  },
                  {
                    name: 'requestContext',
                    type: 'RequestContext',
                    description: 'リクエストスコープの context データ',
                  },
                  {
                    name: 'logger',
                    type: 'IMastraLogger',
                    description: 'Workflow の logger インスタンス',
                  },
                  {
                    name: 'state',
                    type: 'Record<string, any>',
                    description: 'Workflow の現在の state オブジェクト',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## 初期 state を指定して実行する {/* #running-with-initial-state */}

Workflow Run を開始するときに `initialState` を渡すと、Workflow state の開始値を設定できます。

```typescript
const run = await workflow.createRun()

const result = await run.start({
  inputData: { value: 'hello' },
  initialState: {
    counter: 0,
    items: [],
  },
})
```

`initialState` オブジェクトは、Workflow の `stateSchema` で定義された構造と一致する必要があります。詳しくは、[Workflow state](/docs/workflows/workflow-state)を参照してください。

## Workflow のステータス {/* #workflow-status */}

Workflow の `status` は、現在の実行状態を示します。指定できる値は次のとおりです。

<PropertiesTable
  content={[
  {
    name: 'success',
    type: 'string',
    description: 'すべてのステップが正常に実行を完了し、有効な結果が出力されました',
  },
  {
    name: 'failed',
    type: 'string',
    description: 'Workflow の実行中にエラーが発生し、エラーの詳細を確認できます',
  },
  {
    name: 'suspended',
    type: 'string',
    description: 'Workflow の実行が再開を待って一時停止しており、中断されたステップの情報を確認できます',
  },
  {
    name: 'tripwire',
    type: 'string',
    description:
      'Workflow が processor の tripwire によって終了されました。これは、Workflow 内の Agent ステップが tripwire をトリガーした場合（たとえば、guardrail によってコンテンツがブロックされた場合）に発生します。結果から tripwire の情報を確認できます。',
  },
]}
/>

### tripwire ステータスを処理する {/* #handling-tripwire-status */}

Workflow に tripwire をトリガーする Agent ステップが含まれている場合、Workflow は `status: 'tripwire'` と tripwire の詳細を返します。

```typescript
const run = await workflow.createRun()
const result = await run.start({ inputData: { message: 'Hello' } })

if (result.status === 'tripwire') {
  console.log('Workflow terminated by tripwire:', result.tripwire?.reason)
  console.log('Processor ID:', result.tripwire?.processorId)
  console.log('Retry requested:', result.tripwire?.retry)
}
```

これは予期しないエラーを示す `status: 'failed'` とは異なります。tripwire ステータスは、processor が意図的に実行を停止したこと（たとえば、コンテンツモデレーションのため）を示します。

## 関連項目 {/* #related */}

- [Step クラス](./step)
- [Workflow state](/docs/workflows/workflow-state)
- [制御フロー](/docs/workflows/control-flow)
