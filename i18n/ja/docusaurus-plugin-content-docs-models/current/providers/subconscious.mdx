---
title: "Subconscious | モデル"
description: "Mastra で Subconscious モデルを使用します。利用可能なモデルは 2 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/subconscious.svg" alt="Subconscious ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Subconscious

Mastra のモデルルーターを通じて 2 個の Subconscious モデルを利用できます。認証には `SUBCONSCIOUS_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[Subconscious のドキュメント](https://docs.subconscious.dev)を参照してください。

```bash title=".env"
SUBCONSCIOUS_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "subconscious/subconscious/glm-5.2"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[Subconscious のドキュメント](https://docs.subconscious.dev)を確認してください。

:::

## モデル

<ProviderModelsTable
  models={[
  {
    model: 'subconscious/subconscious/glm-5.2',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1000000,
    maxOutput: 131072,
    inputCost: 1.4,
    outputCost: 4.4,
  },
  {
    model: 'subconscious/subconscious/tim-qwen3.6-27b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 8192,
    maxOutput: 5000,
    inputCost: 0.3,
    outputCost: 3,
  },
]}
/>

## 高度な設定

### カスタムヘッダー

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.subconscious.dev/v1",
    id: "subconscious/subconscious/glm-5.2",
    apiKey: process.env.SUBCONSCIOUS_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "subconscious/subconscious/tim-qwen3.6-27b"
      : "subconscious/subconscious/glm-5.2";
  }
});
```

## Provider を直接インストールする

この Provider はスタンドアロン package として直接インストールし、Mastra のモデルルーター文字列の代わりに使用することもできます。詳しくは、[package のドキュメント](https://www.npmjs.com/package/@ai-sdk/anthropic)を参照してください。

```bash npm2yarn
npm install @ai-sdk/anthropic
```
