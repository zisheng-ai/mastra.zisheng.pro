---
title: "Tinfoil | モデル"
description: "Mastra で Tinfoil モデルを使用します。利用可能なモデルは 7 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/tinfoil.svg" alt="Tinfoil ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Tinfoil {/* #tinfoil */}

Mastra のモデルルーターを通じて 7 個の Tinfoil モデルを利用できます。認証には `TINFOIL_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[Tinfoil のドキュメント](https://docs.tinfoil.sh)を参照してください。

```bash title=".env"
TINFOIL_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'tinfoil/gemma4-31b',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[Tinfoil のドキュメント](https://docs.tinfoil.sh)を確認してください。

:::

## モデル {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'tinfoil/gemma4-31b',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 32768,
    inputCost: 0.4,
    outputCost: 1,
  },
  {
    model: 'tinfoil/glm-5-2',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 384000,
    maxOutput: 131072,
    inputCost: 1.5,
    outputCost: 5.25,
  },
  {
    model: 'tinfoil/gpt-oss-120b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131000,
    maxOutput: 32768,
    inputCost: 0.15,
    outputCost: 0.6,
  },
  {
    model: 'tinfoil/gpt-oss-safeguard-120b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131000,
    maxOutput: 32768,
    inputCost: 0.15,
    outputCost: 0.6,
  },
  {
    model: 'tinfoil/kimi-k2-6',
    imageInput: true,
    audioInput: false,
    videoInput: true,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 262144,
    inputCost: 1.5,
    outputCost: 5.25,
  },
  {
    model: 'tinfoil/llama3-3-70b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 128000,
    maxOutput: 4096,
    inputCost: 1.75,
    outputCost: 2.75,
  },
  {
    model: 'tinfoil/nomic-embed-text',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: false,
    reasoning: false,
    contextWindow: 8192,
    maxOutput: 768,
    inputCost: 0.05,
    outputCost: null,
  },
]}
/>

## 高度な設定 {/* #advanced-configuration */}

### カスタムヘッダー {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://inference.tinfoil.sh/v1',
    id: 'tinfoil/gemma4-31b',
    apiKey: process.env.TINFOIL_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動的なモデル選択 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'tinfoil/nomic-embed-text' : 'tinfoil/gemma4-31b'
  },
})
```
