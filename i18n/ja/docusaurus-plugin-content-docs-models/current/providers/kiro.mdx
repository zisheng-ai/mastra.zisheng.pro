---
title: "Kiro | モデル"
description: "Mastra で Kiro モデルを使用します。利用可能なモデルは 12 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/kiro.svg" alt="Kiro ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Kiro {/* #kiro */}

Mastra のモデルルーターを通じて 12 個の Kiro モデルを利用できます。認証には `KIRO_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[Kiro のドキュメント](https://kiro.dev)を参照してください。

```bash title=".env"
KIRO_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'kiro/auto',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[Kiro のドキュメント](https://kiro.dev)を確認してください。

:::

## モデル {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'kiro/auto',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 1000000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/claude-haiku-4.5',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 200000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/claude-opus-4.5',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 200000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/claude-opus-4.6',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 1000000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/claude-opus-4.7',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 1000000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/claude-sonnet-4',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 200000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/claude-sonnet-4.5',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 200000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/claude-sonnet-4.6',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 1000000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/deepseek-3.2',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 164000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/minimax-m2.1',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 196000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/minimax-m2.5',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 196000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kiro/qwen3-coder-next',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 256000,
    maxOutput: 64000,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 高度な設定 {/* #advanced-configuration */}

### カスタムヘッダー {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://q.us-east-1.amazonaws.com',
    id: 'kiro/auto',
    apiKey: process.env.KIRO_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動的なモデル選択 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'kiro/qwen3-coder-next' : 'kiro/auto'
  },
})
```

## Provider を直接インストールする {/* #direct-provider-installation */}

この Provider はスタンドアロン package として直接インストールし、Mastra のモデルルーター文字列の代わりに使用することもできます。詳しくは、[package のドキュメント](https://www.npmjs.com/package/kiro-acp-ai-provider)を参照してください。

```bash npm2yarn
npm install kiro-acp-ai-provider
```
