---
title: "Upstage | モデル"
description: "Mastra で Upstage モデルを使用します。利用可能なモデルは 4 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/upstage.svg" alt="Upstage ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Upstage

Mastra のモデルルーターを通じて 4 個の Upstage モデルを利用できます。認証には `UPSTAGE_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[Upstage のドキュメント](https://developers.upstage.ai)を参照してください。

```bash title=".env"
UPSTAGE_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "upstage/solar-mini"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[Upstage のドキュメント](https://developers.upstage.ai)を確認してください。

:::

## モデル

<ProviderModelsTable
  models={[
  {
    model: 'upstage/solar-mini',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 32768,
    maxOutput: 4096,
    inputCost: 0.15,
    outputCost: 0.15,
  },
  {
    model: 'upstage/solar-pro2',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 65536,
    maxOutput: 8192,
    inputCost: 0.25,
    outputCost: 0.25,
  },
  {
    model: 'upstage/solar-pro3',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 8192,
    inputCost: 0.25,
    outputCost: 0.25,
  },
  {
    model: 'upstage/solar-pro4',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 524288,
    maxOutput: 131072,
    inputCost: 0.3,
    outputCost: 1.2,
  },
]}
/>

## 高度な設定

### カスタムヘッダー

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.upstage.ai/v1/solar",
    id: "upstage/solar-mini",
    apiKey: process.env.UPSTAGE_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "upstage/solar-pro4"
      : "upstage/solar-mini";
  }
});
```
