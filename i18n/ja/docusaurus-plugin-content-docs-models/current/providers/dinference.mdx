---
title: "DInference | モデル"
description: "Mastra で DInference モデルを使用します。利用可能なモデルは 6 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/dinference.svg" alt="DInference ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />DInference

Mastra のモデルルーターを通じて 6 個の DInference モデルを利用できます。認証には `DINFERENCE_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[DInference のドキュメント](https://dinference.com)を参照してください。

```bash title=".env"
DINFERENCE_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "dinference/glm-4.7"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[DInference のドキュメント](https://dinference.com)を確認してください。

:::

## モデル

<ProviderModelsTable
  models={[
  {
    model: 'dinference/glm-4.7',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 200000,
    maxOutput: 128000,
    inputCost: 0.45,
    outputCost: 1.65,
  },
  {
    model: 'dinference/glm-5',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 200000,
    maxOutput: 128000,
    inputCost: 0.75,
    outputCost: 2.4,
  },
  {
    model: 'dinference/glm-5.1',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 200000,
    maxOutput: 128000,
    inputCost: 1.25,
    outputCost: 3.89,
  },
  {
    model: 'dinference/glm-5.2',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1000000,
    maxOutput: 128000,
    inputCost: 1.25,
    outputCost: 3.89,
  },
  {
    model: 'dinference/gpt-oss-120b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 131072,
    maxOutput: 32768,
    inputCost: 0.0675,
    outputCost: 0.27,
  },
  {
    model: 'dinference/minimax-m2.5',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 200000,
    maxOutput: 32000,
    inputCost: 0.22,
    outputCost: 0.88,
  },
]}
/>

## 高度な設定

### カスタムヘッダー

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.dinference.com/v1",
    id: "dinference/glm-4.7",
    apiKey: process.env.DINFERENCE_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "dinference/minimax-m2.5"
      : "dinference/glm-4.7";
  }
});
```
