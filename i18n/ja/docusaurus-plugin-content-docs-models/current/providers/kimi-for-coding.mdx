---
title: "Kimi For Coding | モデル"
description: "Mastra で Kimi For Coding モデルを使用します。利用可能なモデルは 4 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/kimi-for-coding.svg" alt="Kimi For Coding ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Kimi For Coding

Mastra のモデルルーターを通じて 4 個の Kimi For Coding モデルを利用できます。認証には `KIMI_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[Kimi For Coding のドキュメント](https://www.kimi.com/code/docs/en/third-party-tools/other-coding-agents.html)を参照してください。

```bash title=".env"
KIMI_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "kimi-for-coding/k3"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[Kimi For Coding のドキュメント](https://www.kimi.com/code/docs/en/third-party-tools/other-coding-agents.html)を確認してください。

:::

## モデル {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'kimi-for-coding/k3',
    imageInput: true,
    audioInput: false,
    videoInput: true,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1048576,
    maxOutput: 131072,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kimi-for-coding/k3-256k',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 131072,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kimi-for-coding/kimi-for-coding',
    imageInput: true,
    audioInput: false,
    videoInput: true,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 32768,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'kimi-for-coding/kimi-for-coding-highspeed',
    imageInput: true,
    audioInput: false,
    videoInput: true,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 32768,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 高度な設定 {/* #advanced-configuration */}

### カスタムヘッダー {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.kimi.com/coding/v1",
    id: "kimi-for-coding/k3",
    apiKey: process.env.KIMI_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "kimi-for-coding/kimi-for-coding-highspeed"
      : "kimi-for-coding/k3";
  }
});
```

## Provider を直接インストールする {/* #direct-provider-installation */}

この Provider はスタンドアロン package として直接インストールし、Mastra のモデルルーター文字列の代わりに使用することもできます。詳しくは、[package のドキュメント](https://www.npmjs.com/package/@ai-sdk/anthropic)を参照してください。

```bash npm2yarn
npm install @ai-sdk/anthropic
```
