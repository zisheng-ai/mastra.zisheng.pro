---
title: "LongCat | モデル"
description: "Mastra で LongCat モデルを使用します。利用可能なモデルは 1 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/longcat.svg" alt="LongCat ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />LongCat

Mastra のモデルルーターを通じて 1 個の LongCat モデルを利用できます。認証には `LONGCAT_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[LongCat のドキュメント](https://longcat.chat/platform/docs/)を参照してください。

```bash title=".env"
LONGCAT_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "longcat/LongCat-2.0"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[LongCat のドキュメント](https://longcat.chat/platform/docs/)を確認してください。

:::

## モデル

<ProviderModelsTable
  models={[
  {
    "model": "longcat/LongCat-2.0",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1000000,
    "maxOutput": 131072,
    "inputCost": 0.75,
    "outputCost": 2.95
  }
]}
/>

## 高度な設定

### カスタムヘッダー

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.longcat.chat/openai",
    id: "longcat/LongCat-2.0",
    apiKey: process.env.LONGCAT_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "longcat/LongCat-2.0"
      : "longcat/LongCat-2.0";
  }
});
```


