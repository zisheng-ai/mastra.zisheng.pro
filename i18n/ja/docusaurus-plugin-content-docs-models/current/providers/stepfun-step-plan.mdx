---
title: "StepFun Step Plan (China) | モデル"
description: "Mastra で StepFun Step Plan (China) モデルを使用します。利用可能なモデルは 4 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/stepfun-step-plan.svg" alt="StepFun Step Plan (China) ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />StepFun Step Plan (China) {/* #stepfun-step-plan-china */}

Mastra のモデルルーターを通じて 4 個の StepFun Step Plan (China) モデルを利用できます。認証には `STEPFUN_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[StepFun Step Plan (China) のドキュメント](https://platform.stepfun.com/docs/zh/step-plan/integrations/reasoning-api)を参照してください。

```bash title=".env"
STEPFUN_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'stepfun-step-plan/step-3.5-flash',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[StepFun Step Plan (China) のドキュメント](https://platform.stepfun.com/docs/zh/step-plan/integrations/reasoning-api)を確認してください。

:::

## モデル {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'stepfun-step-plan/step-3.5-flash',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 256000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'stepfun-step-plan/step-3.5-flash-2603',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 256000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'stepfun-step-plan/step-3.7-flash',
    imageInput: true,
    audioInput: false,
    videoInput: true,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 256000,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'stepfun-step-plan/step-router-v1',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 256000,
    maxOutput: 256000,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 高度な設定 {/* #advanced-configuration */}

### カスタムヘッダー {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://api.stepfun.com/step_plan/v1',
    id: 'stepfun-step-plan/step-3.5-flash',
    apiKey: process.env.STEPFUN_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動的なモデル選択 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'stepfun-step-plan/step-router-v1' : 'stepfun-step-plan/step-3.5-flash'
  },
})
```
