---
title: "Sarvam AI | モデル"
description: "Mastra で Sarvam AI モデルを使用します。利用可能なモデルは 2 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/sarvam.svg" alt="Sarvam AI ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Sarvam AI

Mastra のモデルルーターを通じて 2 個の Sarvam AI モデルを利用できます。認証には `SARVAM_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[Sarvam AI のドキュメント](https://docs.sarvam.ai)を参照してください。

```bash title=".env"
SARVAM_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "sarvam/sarvam-105b"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[Sarvam AI のドキュメント](https://docs.sarvam.ai)を確認してください。

:::

## モデル

<ProviderModelsTable
  models={[
  {
    model: 'sarvam/sarvam-105b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 131072,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'sarvam/sarvam-30b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 65536,
    maxOutput: 65536,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 高度な設定

### カスタムヘッダー

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.sarvam.ai/v1",
    id: "sarvam/sarvam-105b",
    apiKey: process.env.SARVAM_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "sarvam/sarvam-30b"
      : "sarvam/sarvam-105b";
  }
});
```
