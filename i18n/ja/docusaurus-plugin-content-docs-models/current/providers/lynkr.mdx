---
title: "Lynkr | モデル"
description: "Mastra で Lynkr モデルを使用します。利用可能なモデルは 1 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/lynkr.svg" alt="Lynkr ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Lynkr

Mastra のモデルルーターを通じて 1 個の Lynkr モデルを利用できます。認証には `LYNKR_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[Lynkr のドキュメント](https://github.com/Fast-Editor/Lynkr)を参照してください。

```bash title=".env"
LYNKR_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "lynkr/lynkr-auto"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[Lynkr のドキュメント](https://github.com/Fast-Editor/Lynkr)を確認してください。

:::

## モデル

<ProviderModelsTable
  models={[
  {
    model: 'lynkr/lynkr-auto',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 128000,
    maxOutput: 8192,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 高度な設定

### カスタムヘッダー

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "http://127.0.0.1:8081/v1",
    id: "lynkr/lynkr-auto",
    apiKey: process.env.LYNKR_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "lynkr/lynkr-auto"
      : "lynkr/lynkr-auto";
  }
});
```
