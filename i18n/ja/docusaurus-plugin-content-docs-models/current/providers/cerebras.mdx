---
title: "Cerebras | モデル"
description: "Mastra で Cerebras モデルを使用します。利用可能なモデルは 3 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/cerebras.svg" alt="Cerebras ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Cerebras

Mastra のモデルルーターを通じて 3 個の Cerebras モデルを利用できます。認証には `CEREBRAS_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[Cerebras のドキュメント](https://inference-docs.cerebras.ai/models/overview)を参照してください。

```bash title=".env"
CEREBRAS_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "cerebras/gemma-4-31b"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

## モデル {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'cerebras/gemma-4-31b',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 40960,
    inputCost: 0.99,
    outputCost: 1.49,
  },
  {
    model: 'cerebras/gpt-oss-120b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 40960,
    inputCost: 0.35,
    outputCost: 0.75,
  },
  {
    model: 'cerebras/zai-glm-4.7',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 40960,
    inputCost: 2.25,
    outputCost: 2.75,
  },
]}
/>

## 高度な設定 {/* #advanced-configuration */}

### カスタムヘッダー {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    id: "cerebras/gemma-4-31b",
    apiKey: process.env.CEREBRAS_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "cerebras/zai-glm-4.7"
      : "cerebras/gemma-4-31b";
  }
});
```

## Provider を直接インストールする {/* #direct-provider-installation */}

この Provider はスタンドアロン package として直接インストールし、Mastra のモデルルーター文字列の代わりに使用することもできます。詳しくは、[package のドキュメント](https://www.npmjs.com/package/@ai-sdk/cerebras)を参照してください。

```bash npm2yarn
npm install @ai-sdk/cerebras
```

Provider 固有の詳細については、[AI SDK Cerebras Provider のドキュメント](https://ai-sdk.dev/providers/ai-sdk-providers/cerebras)を参照してください。
