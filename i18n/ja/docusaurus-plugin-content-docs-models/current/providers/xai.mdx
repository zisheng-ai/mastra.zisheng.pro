---
title: "xAI | モデル"
description: "Mastra で xAI モデルを使用します。利用可能なモデルは 10 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/xai.svg" alt="xAI ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />xAI

Mastra のモデルルーターを通じて 10 個の xAI モデルを利用できます。認証には `XAI_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[xAI のドキュメント](https://docs.x.ai/docs/models)を参照してください。

```bash title=".env"
XAI_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "xai/grok-4.20-0309-non-reasoning"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

## モデル {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'xai/grok-4.20-0309-non-reasoning',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 1000000,
    maxOutput: 30000,
    inputCost: 1.25,
    outputCost: 2.5,
  },
  {
    model: 'xai/grok-4.20-0309-reasoning',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1000000,
    maxOutput: 30000,
    inputCost: 1.25,
    outputCost: 2.5,
  },
  {
    model: 'xai/grok-4.20-multi-agent-0309',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: false,
    reasoning: true,
    contextWindow: 1000000,
    maxOutput: 30000,
    inputCost: 1.25,
    outputCost: 2.5,
  },
  {
    model: 'xai/grok-4.3',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1000000,
    maxOutput: 30000,
    inputCost: 1.25,
    outputCost: 2.5,
  },
  {
    model: 'xai/grok-4.5',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 500000,
    maxOutput: 500000,
    inputCost: 2,
    outputCost: 6,
  },
  {
    model: 'xai/grok-build-0.1',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 256000,
    maxOutput: 256000,
    inputCost: 1,
    outputCost: 2,
  },
  {
    model: 'xai/grok-imagine-image',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: false,
    reasoning: false,
    contextWindow: 8000,
    maxOutput: null,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'xai/grok-imagine-image-quality',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: false,
    reasoning: false,
    contextWindow: 8000,
    maxOutput: null,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'xai/grok-imagine-video',
    imageInput: true,
    audioInput: false,
    videoInput: true,
    toolUsage: false,
    reasoning: false,
    contextWindow: 1024,
    maxOutput: null,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'xai/grok-imagine-video-1.5',
    imageInput: true,
    audioInput: true,
    videoInput: false,
    toolUsage: false,
    reasoning: false,
    contextWindow: 1024,
    maxOutput: null,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 高度な設定 {/* #advanced-configuration */}

### カスタムヘッダー {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    id: "xai/grok-4.20-0309-non-reasoning",
    apiKey: process.env.XAI_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "xai/grok-imagine-video-1.5"
      : "xai/grok-4.20-0309-non-reasoning";
  }
});
```

## Provider オプション {/* #provider-options */}

xAI は `providerOptions` パラメーターを通じて、次の Provider 固有オプションをサポートします。

```typescript
const response = await agent.generate("Hello!", {
  providerOptions: {
    xai: {
      // See available options in the table below
    }
  }
});
```

### 利用可能なオプション {/* #available-options */}

<PropertiesTable
  content={[
  {
    name: 'reasoningEffort',
    type: '"none" | "low" | "medium" | "high" | undefined',
    description: '',
    isOptional: true,
  },
  {
    name: 'logprobs',
    type: 'boolean | undefined',
    description: '',
    isOptional: true,
  },
  {
    name: 'topLogprobs',
    type: 'number | undefined',
    description: '',
    isOptional: true,
  },
  {
    name: 'parallel_function_calling',
    type: 'boolean | undefined',
    description: '',
    isOptional: true,
  },
  {
    name: 'searchParameters',
    type: '{ mode: "off" | "auto" | "on"; returnCitations?: boolean | undefined; fromDate?: string | undefined; toDate?: string | undefined; maxSearchResults?: number | undefined; sources?: ({ ...; } | ... 2 more ... | { ...; })[] | undefined; } | undefined',
    description: '',
    isOptional: true,
  },
]}
/>

## Provider を直接インストールする {/* #direct-provider-installation */}

この Provider はスタンドアロン package として直接インストールし、Mastra のモデルルーター文字列の代わりに使用することもできます。詳しくは、[package のドキュメント](https://www.npmjs.com/package/@ai-sdk/xai)を参照してください。

```bash npm2yarn
npm install @ai-sdk/xai
```

Provider 固有の詳細については、[AI SDK xAI Provider のドキュメント](https://ai-sdk.dev/providers/ai-sdk-providers/xai)を参照してください。
