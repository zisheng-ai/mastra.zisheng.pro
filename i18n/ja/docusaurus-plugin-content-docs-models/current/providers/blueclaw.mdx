---
title: "Blue Claw | モデル"
description: "Mastra で Blue Claw モデルを使用します。利用可能なモデルは 2 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/blueclaw.svg" alt="Blue Claw ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Blue Claw {/* #blue-claw */}

Mastra のモデルルーターを通じて 2 個の Blue Claw モデルを利用できます。認証には `BLUECLAW_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[Blue Claw のドキュメント](https://blueclaw.network)を参照してください。

```bash title=".env"
BLUECLAW_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'blueclaw/Qwen/Qwen3.6-35B-A3B-FP8',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[Blue Claw のドキュメント](https://blueclaw.network)を確認してください。

:::

## モデル {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'blueclaw/Qwen/Qwen3.6-35B-A3B-FP8',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 65536,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'blueclaw/Qwen3.6-27B',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 196608,
    maxOutput: 65536,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 高度な設定 {/* #advanced-configuration */}

### カスタムヘッダー {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://openai.blueclaw.network/v1',
    id: 'blueclaw/Qwen/Qwen3.6-35B-A3B-FP8',
    apiKey: process.env.BLUECLAW_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動的なモデル選択 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'blueclaw/Qwen3.6-27B' : 'blueclaw/Qwen/Qwen3.6-35B-A3B-FP8'
  },
})
```
