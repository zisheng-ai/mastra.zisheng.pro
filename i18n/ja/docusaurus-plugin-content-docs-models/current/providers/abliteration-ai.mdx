---
title: "abliteration.ai | モデル"
description: "Mastra で abliteration.ai モデルを使用します。利用可能なモデルは 2 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/abliteration-ai.svg" alt="abliteration.ai ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />abliteration.ai

Mastra のモデルルーターを通じて 2 個の abliteration.ai モデルを利用できます。認証には `ABLIT_KEY` 環境変数が自動的に使用されます。

詳しくは、[abliteration.ai のドキュメント](https://docs.abliteration.ai/models)を参照してください。

```bash title=".env"
ABLIT_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "abliteration-ai/abliterated-model"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[abliteration.ai のドキュメント](https://docs.abliteration.ai/models)を確認してください。

:::

## モデル

<ProviderModelsTable
  models={[
  {
    "model": "abliteration-ai/abliterated-model",
    "imageInput": true,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 150000,
    "maxOutput": 8192,
    "inputCost": 3,
    "outputCost": 3
  },
  {
    "model": "abliteration-ai/abliterated-model-large",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 1000000,
    "maxOutput": 999990,
    "inputCost": 5,
    "outputCost": 5
  }
]}
/>

## 高度な設定

### カスタムヘッダー

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.abliteration.ai/v1",
    id: "abliteration-ai/abliterated-model",
    apiKey: process.env.ABLIT_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "abliteration-ai/abliterated-model-large"
      : "abliteration-ai/abliterated-model";
  }
});
```


