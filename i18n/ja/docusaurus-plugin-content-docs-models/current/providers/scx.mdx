---
title: "SCX.ai | モデル"
description: "Mastra で SCX.ai モデルを使用します。利用可能なモデルは 2 個です。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/scx.svg" alt="SCX.ai ロゴ" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />SCX.ai

Mastra のモデルルーターを通じて 2 個の SCX.ai モデルを利用できます。認証には `SCX_API_KEY` 環境変数が自動的に使用されます。

詳しくは、[SCX.ai のドキュメント](https://platform.scx.ai/docs)を参照してください。

```bash title=".env"
SCX_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "scx/MiniMax-M2.7"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra は OpenAI 互換の `/chat/completions` endpoint を使用します。一部の Provider 固有機能は利用できない場合があります。詳しくは、[SCX.ai のドキュメント](https://platform.scx.ai/docs)を確認してください。

:::

## モデル

<ProviderModelsTable
  models={[
  {
    model: 'scx/gpt-oss-120b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 131072,
    maxOutput: 131072,
    inputCost: 0.17,
    outputCost: 0.55,
  },
  {
    model: 'scx/MiniMax-M2.7',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 192000,
    maxOutput: 64000,
    inputCost: 0.48,
    outputCost: 1.79,
  },
]}
/>

## 高度な設定

### カスタムヘッダー

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://api.scx.ai/v1",
    id: "scx/MiniMax-M2.7",
    apiKey: process.env.SCX_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動的なモデル選択

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "scx/gpt-oss-120b"
      : "scx/MiniMax-M2.7";
  }
});
```
