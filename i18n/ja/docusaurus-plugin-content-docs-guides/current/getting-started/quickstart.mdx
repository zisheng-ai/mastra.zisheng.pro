---
title: "クイックスタート"
description: Agent Harness プロジェクトを数分でスキャフォールドし、Mastra を使い始めます。
---

import QuickstartPrompt from "@site/i18n/ja/docusaurus-plugin-content-docs/current/getting-started/_partial-quickstart-prompt.mdx";
import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Mastra クイックスタート {/* #mastra-quickstart */}

<Inject>
  **AI Agent 向け：** `npm create mastra@latest <project-name> -- --llm <provider>` を使用して、Mastra プロジェクトを非対話形式で作成します。`<provider>` は `openai`、`anthropic`、`google`、`xai` のいずれかに置き換えてください。Agent やモデル Provider を含まない最小構成のプロジェクトを作成するには、`npm create mastra@latest <project-name> -- --empty` を使用します。Mastra プロジェクトをゼロから構築する場合は、[手動インストールガイド](/guides/getting-started/manual-install)を参照してください。
</Inject>

`create-mastra` CLI は、Mastra プロジェクトを始めるための最も手軽な方法です。Workspace Tool、Memory、タスク追跡、Web アクセス、Schedule、Storage、Observability を備えた Agent Harness を作成します。

より細かく設定する場合は、[手動インストールガイド](/guides/getting-started/manual-install)を参照してください。既存のプロジェクトに Mastra を追加するには、[`mastra init`](/reference/cli/mastra#mastra-init) を使用します。

<QuickstartPrompt />

:::tip[📹 動画を見る]

[Mastra AI Agent コース](https://www.youtube.com/watch?v=lCmf_qrGfGA)では、Mastra で Agent を構築する方法を順を追って学べます。

:::

## 始める前に {/* #before-you-begin */}

OpenAI、Anthropic、Google Gemini、または xAI の API キーが必要です。セットアップ時の入力は省略でき、後から `.env` に追加できます。

## プロジェクトを作成する {/* #create-the-project */}

対話形式のセットアップを実行します。

<Tabs groupId="npm2yarn">
  <TabItem value="npm" label="npm">
    ```bash
    npm create mastra@latest
    ```
  </TabItem>

  <TabItem value="pnpm" label="pnpm">
    ```bash
    pnpm create mastra@latest
    ```
  </TabItem>

  <TabItem value="yarn" label="Yarn">
    ```bash
    yarn create mastra
    ```
  </TabItem>

  <TabItem value="bun" label="Bun">
    ```bash
    bunx create-mastra
    ```
  </TabItem>
</Tabs>

このコマンドでは、プロジェクト名、モデル Provider、任意の API キーの入力を求められます。その後、依存関係とコーディングアシスタント向けの Mastra Skill がインストールされます。既存のリポジトリ内でなければ、最初の Git コミットも作成されます。

生成された `src/mastra` ディレクトリには、Mastra のエントリーポイント、Agent、Workspace Tool、および関連するコードが含まれています。Mastra の標準的な構成については、[プロジェクト構造のリファレンス](/reference/project-structure)を参照してください。

:::tip

テンプレートから始めるには `--template <template>` を、Provider を含まない最小構成のプロジェクトを作成するには `--empty` を使用します。自動セットアップを省略するには、`--no-skills` または `--no-git` を使用します。すべてのフラグについては、[`create-mastra` リファレンス](/reference/cli/create-mastra)を参照してください。

:::

## Agent をテストする {/* #test-your-agent */}

ターミナルの指示に従ってプロジェクトディレクトリに移動し、開発サーバーを起動します。

```bash npm2yarn
npm run dev
```

[localhost:4111 の Studio](http://localhost:4111) を開き、Agent を選択してメッセージを送信します。セットアップ時に Provider の API キーの入力を省略した場合は、`.env.example` に記載されている API キーを `.env` ファイルに追加してください。

[Studio](/docs/studio/overview) を使用すれば、別途 UI を作成することなく Agent を構築してテストできます。

## 次のステップ {/* #next-steps */}

- Mastra を [Next.js](/guides/getting-started/next-js)、[React](/guides/getting-started/vite-react)、または [Astro](/guides/getting-started/astro) と統合する
- [AI を活用して Mastra を構築する方法](/reference/build-with-ai)を学ぶ
- [ガイド](/guides)に沿って Agent をゼロから構築する
- [Mastra YouTube チャンネル](https://www.youtube.com/@mastra-ai)を見る
