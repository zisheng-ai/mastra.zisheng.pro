---
title: "린커 | Model"
description: "Mastra와 함께 Lynkr Model을 사용하세요. 1개의 Model을 사용할 수 있습니다."
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/lynkr.svg" alt="Lynkr logo" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />린커

Mastra의 Model 라우터를 통해 1개의 Lynkr Model에 접속합니다. 인증은 다음을 사용하여 자동으로 처리됩니다.`LYNKR_API_KEY` environment variable.

자세한 내용은 다음에서 확인하세요.[Lynkr documentation](https://github.com/Fast-Editor/Lynkr).

```bash title=".env"
LYNKR_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "lynkr/lynkr-auto"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::정보

Mastra는 OpenAI 호환을 사용합니다.`/chat/completions` endpoint. Some provider-specific features may not be available. Check the [Lynkr documentation](https://github.com/Fast-Editor/Lynkr) for details.

:::

## Model {/* #models */}

<ProviderModelsTable
  models={[
{
  "model": "lynkr/lynkr-auto",
  "imageInput": false,
  "audioInput": false,
  "videoInput": false,
  "toolUsage": true,
  "reasoning": false,
  "contextWindow": 128000,
  "maxOutput": 8192,
  "inputCost": null,
  "outputCost": null
}
]}
/>

## 고급 구성 {/* #advanced-configuration */}

### 맞춤 헤더 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "http://127.0.0.1:8081/v1",
    id: "lynkr/lynkr-auto",
    apiKey: process.env.LYNKR_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 동적 Model 선택 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "lynkr/lynkr-auto"
      : "lynkr/lynkr-auto";
  }
});
```
