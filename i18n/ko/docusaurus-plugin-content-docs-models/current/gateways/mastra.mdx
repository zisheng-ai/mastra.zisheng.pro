---
title: "마스트라 | 게이트웨이 | 마스트라"
description: "게이트웨이를 사용하여 Model 공급자 간에 요청을 라우팅합니다."
---

{/* This file is MANUALLY MAINTAINED - DO NOT AUTO-GENERATE */}

# <img src="https://mastra.ai/brand/logo.svg" alt="Mastra logo" className="inline w-8 h-8 mr-2 align-middle invert dark:invert-0" />마스트라

게이트웨이는 내장된 OpenAI 호환 API 프록시입니다.[Observational Memory](https://gateway.mastra.ai/docs/features#observational-memory). Point any HTTP client, SDK, or framework at the gateway and every conversation is automatically remembered without any memory management code.

자세한 내용은 다음에서 확인하세요.[Gateway documentation](https://gateway.mastra.ai/docs).

## API 키 받기 {/* #get-an-api-key */}

이동[gateway.mastra.ai](https://gateway.mastra.ai) and sign up for a Mastra account. During the onboarding you'll get your personal API key to authenticate requests.

## 용법 {/* #usage */}

API 키를 환경 변수로 정의합니다.

```bash title=".env"
MASTRA_GATEWAY_API_KEY=your-gateway-key
```

게이트웨이 Model ID를 설정합니다.

```typescript title="src/mastra/agents/my-agent.ts"
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "mastra/openai/gpt-5-mini"
});
```

게이트웨이를 통해 실행되어야 하는 내장 Model에 동일한 접두사를 사용합니다.

```typescript title="src/mastra/rag/embed.ts"
import { ModelRouterEmbeddingModel } from "@mastra/core/llm";

const embedder = new ModelRouterEmbeddingModel("mastra/__GATEWAY_OPENAI_EMBEDDING_MODEL__");
```

통과하다`memory.thread` and `memory.resource` when you generate/stream responses to enable Observational Memory:

```typescript title="src/mastra/run.ts"
import { weatherAgent } from "./agents/weather-agent";

const memory = {
  thread: "assistant-thread-1",
  resource: "user-42",
};

const result = await weatherAgent.stream("My name is Alex and I prefer concise answers.", {
  memory,
});

for await (const chunk of result.textStream) {
  process.stdout.write(chunk);
}
```

## 구성 {/* #configuration */}

```bash
# Use gateway API key
MASTRA_GATEWAY_API_KEY=your-gateway-key
```

## 자세히 알아보기 {/* #learn-more */}

* [특징](https://gateway.mastra.ai/docs/features)
* [Model](https://gateway.mastra.ai/docs/models)
* [제한](https://gateway.mastra.ai/docs/limits)
* [API 참조](https://gateway.mastra.ai/docs/api/overview)
* [예](https://gateway.mastra.ai/docs/examples/)
