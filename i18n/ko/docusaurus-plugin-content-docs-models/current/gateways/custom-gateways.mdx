---
title: "사용자 정의 게이트웨이 | Model | 마스트라"
description: "개인 또는 전문 LLM 배포를 위한 사용자 정의 Model 게이트웨이 생성"
---

# 커스텀 Model 게이트웨이

사용자 정의 Model 게이트웨이를 사용하면 개인 또는 전문 LLM 공급자 통합을 구현할 수 있습니다.`MastraModelGatewayInterface`인터페이스 또는`MastraModelGateway`기본 클래스.

## 개요 {/* #overview */}

게이트웨이는 언어 Model에 액세스하기 위한 공급자별 논리를 처리합니다.

* 공급자 구성 및 Model 검색
* 인증 및 API Key 관리
* API 엔드포인트의 URL 구성
* 언어 Model 인스턴스 생성

다음을 지원하기 위해 사용자 정의 게이트웨이를 만듭니다.

* 개인 또는 기업 LLM 배포
* 사용자 정의 인증 체계
* 전문화된 라우팅 로직
* 고유 ID를 사용한 게이트웨이 버전 관리

## 사용자 정의 게이트웨이 생성 {/* #creating-a-custom-gateway */}

일반 객체 게이트웨이에는 `MastraModelGatewayInterface`를 구현하고, 기본 클래스의 기본값을 사용하려면 `MastraModelGateway`를 확장하세요.
```typescript
import { type MastraModelGatewayInterface, type ProviderConfig } from '@mastra/core/llm';
import { createOpenAICompatible } from '@ai-sdk/openai-compatible-v5';

const myPrivateGateway: MastraModelGatewayInterface = {
  id: 'private',
  name: 'My Private Gateway',
  async fetchProviders(): Promise<Record<string, ProviderConfig>> {
    return {
      'my-provider': {
        name: 'My Provider',
        models: ['__GATEWAY_OPENAI_MODEL__'],
        apiKeyEnvVar: 'MY_API_KEY',
        gateway: 'private',
        url: 'https://api.myprovider.com/v1',
      },
    };
  },
  buildUrl() {
    return 'https://api.myprovider.com/v1';
  },
  async getApiKey() {
    return process.env.MY_API_KEY ?? '';
  },
  async resolveLanguageModel({ modelId, providerId, apiKey }) {
    return createOpenAICompatible({
      name: providerId,
      apiKey,
      baseURL: 'https://api.myprovider.com/v1',
    }).chatModel(modelId);
  },
};
```

다음 예에서는`MastraModelGateway` class:

```typescript
import { MastraModelGateway, type ProviderConfig } from '@mastra/core/llm';
import { createOpenAICompatible } from '@ai-sdk/openai-compatible-v5';
import type { LanguageModelV2 } from '@ai-sdk/provider-v5';

class MyPrivateGateway extends MastraModelGateway {
  // Required: Unique identifier for the gateway
  // This ID is used as the prefix for all providers from this gateway
  readonly id = 'private';

  // Required: Human-readable name
  readonly name = 'My Private Gateway';

  /**
   * Fetch provider configurations from your gateway
   * Returns a record of provider configurations
   */
  async fetchProviders(): Promise<Record<string, ProviderConfig>> {
    return {
      'my-provider': {
        name: 'My Provider',
        models: ['__GATEWAY_OPENAI_MODEL__', '__GATEWAY_ANTHROPIC_MODEL_SONNET__'],
        apiKeyEnvVar: 'MY_API_KEY',
        gateway: this.id,
        url: 'https://api.myprovider.com/v1',
      },
    };
  }

  /**
   * Build the API URL for a model
   * @param modelId - Full model ID (e.g., "private/my-provider/model-1")
   * @param envVars - Environment variables (optional)
   */
  buildUrl(modelId: string, envVars?: Record<string, string>): string {
    return 'https://api.myprovider.com/v1';
  }

  /**
   * Get the API key for authentication
   * @param modelId - Full model ID
   */
  async getApiKey(modelId: string): Promise<string> {
    const apiKey = process.env.MY_API_KEY;
    if (!apiKey) {
      throw new Error(`Missing MY_API_KEY environment variable`);
    }
    return apiKey;
  }

  /**
   * Create a language model instance
   * @param args - Model ID, provider ID, and API key
   */
  async resolveLanguageModel({
    modelId,
    providerId,
    apiKey,
  }: {
    modelId: string;
    providerId: string;
    apiKey: string;
  }): Promise<LanguageModelV2> {
    const baseURL = this.buildUrl(`${providerId}/${modelId}`);

    return createOpenAICompatible({
      name: providerId,
      apiKey,
      baseURL,
      supportsStructuredOutputs: true,
    }).chatModel(modelId);
  }
}
```

### 게이트웨이 소유 인증 {/* #gateway-owned-authentication */}

게이트웨이가 자격 증명 조회를 담당하는 경우 `resolveAuth`를 추가하세요. Mastra는 `getApiKey()`로 대체하기 전에 이 훅을 사용합니다.
```typescript
const myPrivateGateway: MastraModelGatewayInterface = {
  // ...gateway fields and methods
  async resolveAuth() {
    const apiKey = process.env.MY_API_KEY;
    return apiKey ? { apiKey, source: 'gateway' } : undefined;
  },
};
```

## 사용자 정의 게이트웨이 등록 {/* #registering-custom-gateways */}

### 초기화 중 {/* #during-initialization */}

Mastra 인스턴스를 생성할 때 게이트웨이를 레코드로 전달합니다.

```typescript
import { Mastra } from '@mastra/core';

const mastra = new Mastra({
  gateways: {
    myGateway: new MyPrivateGateway(),
    anotherGateway: new AnotherGateway(),
  },
});
```

### 초기화 후 {/* #after-initialization */}

다음을 사용하여 동적으로 게이트웨이 추가`addGateway`:

```typescript
const mastra = new Mastra();

// Add with explicit key
mastra.addGateway(new MyPrivateGateway(), 'myGateway');

// Add using gateway's ID
mastra.addGateway(new MyPrivateGateway());
// Stored with key 'my-private-gateway' (the gateway's id)
```

## 사용자 정의 게이트웨이 사용 {/* #using-custom-gateways */}

게이트웨이 ID를 접두사로 사용하는 사용자 지정 게이트웨이의 참조 Model:

```typescript
import { Agent } from '@mastra/core/agent';

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'private/my-provider/__GATEWAY_OPENAI_MODEL__', // Uses MyPrivateGateway
});

mastra.addAgent(agent, 'myAgent');
```

Agent를 생성하거나 Model을 사용할 때 Mastra의 Model 라우터는 Model ID를 기반으로 적절한 게이트웨이를 자동으로 선택합니다. 게이트웨이 ID는 접두사 역할을 합니다. 일치하는 사용자 지정 게이트웨이가 없으면 기본 제공 게이트웨이로 대체됩니다.

### TypeScript 자동 완성 {/* #typescript-autocomplete */}

#### 개발 중 자동 유형 생성 {/* #automatic-type-generation-in-development */}

개발 모드(`MASTRA_DEV=true`)에서는 Mastra가 사용자 정의 게이트웨이의 TypeScript 타입을 자동으로 생성합니다.
1. **환경 변수 설정**:
   ```bash
   export MASTRA_DEV=true
   ```

2. **게이트웨이 등록**:
   ```typescript
   const mastra = new Mastra({
     gateways: {
       myGateway: new MyPrivateGateway(),
     },
   });
   ```

3. **유형이 자동으로 생성됩니다.**:
   * 게이트웨이를 추가하면 Mastra는 GatewayRegistry와 동기화됩니다.
   * 레지스트리는 사용자 정의 게이트웨이에서 공급자를 가져옵니다.
   * TypeScript 유형은 다음에서 재생성됩니다.`~/.cache/mastra/`
   * IDE는 몇 초 안에 새로운 유형을 선택합니다.

4. **이제 자동완성이 작동합니다**:
   ```typescript
   const agent = new Agent({
     model: 'my-gateway-id/my-provider/model-1', // Full autocomplete!
   });
   ```

#### 작동 원리 {/* #how-it-works */}

GatewayRegistry는 다음과 같은 시간별 동기화를 실행합니다.

* 등록된 모든 게이트웨이에서 `fetchProviders()`를 호출합니다.
* TypeScript 타입 정의를 생성합니다.
* 전역 캐시와 프로젝트의 `dist/` 디렉터리에 모두 기록합니다.
* TypeScript 서버가 변경 사항을 자동으로 감지합니다.
:::tip
게이트웨이를 처음 추가하는 경우 유형이 생성되는 데 몇 초 정도 걸릴 수 있습니다. 후속 업데이트는 매 시간마다 백그라운드에서 발생합니다.
:::

#### 수동 유형 생성 대안 {/* #manual-type-generation-alternatives */}

개발 모드에서 실행 중이 아니거나 즉각적인 유형 업데이트가 필요한 경우:

**옵션 1: 유형 어설션 사용(가장 간단함)**

```typescript
const agent = new Agent({
  id: 'my-agent',
  name: 'my-agent',
  instructions: 'You are a helpful assistant',
  model: 'private/my-provider/model-1' as any, // Bypass type checking
});
```

**옵션 2: 사용자 정의 유형 공용체 만들기(유형 안전)**

```typescript
import type { ModelRouterModelId } from '@mastra/core/llm';

// Define your custom model IDs
type CustomModelId =
  | 'private/my-provider/model-1'
  | 'private/my-provider/model-2'
  | 'private/my-provider/model-3';

// Combine with built-in models
type AllModelIds = ModelRouterModelId | CustomModelId;

const agent = new Agent({
  id: 'my-agent',
  name: 'my-agent',
  instructions: 'You are a helpful assistant',
  model: 'private/my-provider/model-1' satisfies AllModelIds,
});
```

**옵션 3: ModelRouterModelId를 전역으로 확장(고급)**

```typescript
// In a types.d.ts file in your project
// The import is required: it makes this file a module, so the block below
// merges into the existing types instead of replacing the module.
import '@mastra/core/llm';

declare module '@mastra/core/llm' {
  interface ProviderModelsMap {
    'my-provider': readonly ['model-1', 'model-2', 'model-3'];
  }
}
```

이는 사용자 정의 Model을 포함하도록 내장 유형을 확장하여 완전한 자동 완성 지원을 제공합니다.

## 게이트웨이 관리 {/* #gateway-management */}

### getGateway(키) {/* #getgatewaykey */}

등록 키로 게이트웨이를 검색합니다.

```typescript
const gateway = mastra.getGateway('myGateway');
console.log(gateway.name); // 'My Private Gateway'
```

### getGatewayById(ID) {/* #getgatewaybyidid */}

고유 ID로 게이트웨이를 검색합니다.

```typescript
const gateway = mastra.getGatewayById('my-private-gateway');
console.log(gateway.name); // 'My Private Gateway'
```

이는 다음과 같은 경우에 유용합니다.

* 게이트웨이에는 등록 키와 다른 명시적 ID가 있습니다.
* 여러 인스턴스에서 ID로 게이트웨이를 찾아야 합니다.
* 게이트웨이 버전 관리 지원(예:`'gateway-v1'`, `'gateway-v2'`)

### 목록게이트웨이() {/* #listgateways */}

등록된 모든 게이트웨이를 가져옵니다.

```typescript
const gateways = mastra.listGateways();
console.log(Object.keys(gateways)); // ['myGateway', 'anotherGateway']
```

## 게이트웨이 속성 {/* #gateway-properties */}

### 필수의 {/* #required */}

| 속성 | 유형 | 설명 |
|------------|------|-------------|
|`id` | `string` | 게이트웨이의 고유 식별자로, Model 문자열에서 게이트웨이 접두사로 사용됩니다. |
| `name` | `string` | 사람이 읽을 수 있는 게이트웨이 이름 |
### 행동 양식 {/* #methods */}

| 메서드 | 설명 |
|---------|-------------|
|`fetchProviders()` | Provider 구성을 가져옵니다. |
| `buildUrl(modelId, envVars?)` | Model의 API URL을 생성합니다. |
| `getApiKey(modelId)` | 인증에 사용할 API 키를 가져옵니다. |
| `resolveLanguageModel(args)` | 언어 Model 인스턴스를 생성합니다. |
| `getId()` | 게이트웨이 ID를 가져옵니다(`id` 또는 `name` 반환). |
## 공급자 구성 {/* #provider-configuration */}

`fetchProviders()` 메서드는 `ProviderConfig` 객체의 레코드를 반환합니다.
```typescript
interface ProviderConfig {
  name: string;                    // Display name
  models: string[];                // Available model IDs
  apiKeyEnvVar: string | string[]; // Environment variable(s) for API key
  gateway: string;                 // Gateway identifier
  url?: string;                    // Optional API base URL
  apiKeyHeader?: string;           // Optional custom auth header
  docUrl?: string;                 // Optional documentation URL
}
```

## 게이트웨이 ID와 키 {/* #gateway-ids-vs-keys */}

차이점 이해:

* **키**: Mastra에 게이트웨이를 추가할 때 사용하는 등록 키(레코드 키)
* **ID**: 게이트웨이의 고유 식별자(`id` 속성을 통해 지정하며, 설정하지 않으면 `name` 사용)
```typescript
class VersionedGateway extends MastraModelGateway {
  readonly id = 'my-gateway-v2';     // Unique ID for versioning and prefixing
  readonly name = 'My Gateway';       // Display name
}

const mastra = new Mastra({
  gateways: {
    currentGateway: new VersionedGateway(), // Key: 'currentGateway'
  },
});

// Retrieve by key
const byKey = mastra.getGateway('currentGateway');

// Retrieve by ID
const byId = mastra.getGatewayById('my-gateway-v2');

// Both return the same gateway
console.log(byKey === byId); // true
```

## Model ID 형식 {/* #model-id-format */}

사용자 정의 게이트웨이를 통해 액세스되는 Model은 다음 형식을 따릅니다.

```
[gatewayId]/[provider]/[model]
```

예:

* `private/my-provider/model-1`

## 고급 예 {/* #advanced-example */}

캐싱이 포함된 토큰 기반 게이트웨이:

```typescript
class TokenGateway extends MastraModelGateway {
  readonly id = 'token-gateway-v1';
  readonly name = 'Token Gateway';

  private tokenCache: Map<string, { token: string; expiresAt: number }> = new Map();

  async fetchProviders(): Promise<Record<string, ProviderConfig>> {
    const response = await fetch('https://api.gateway.com/providers');
    const data = await response.json();

    return {
      provider: {
        name: data.name,
        models: data.models,
        apiKeyEnvVar: 'GATEWAY_TOKEN',
        gateway: this.id,
      },
    };
  }

  async buildUrl(modelId: string, envVars?: Record<string, string>): Promise<string> {
    const token = await this.getApiKey(modelId);
    const siteId = envVars?.SITE_ID || process.env.SITE_ID;

    const response = await fetch(`https://api.gateway.com/sites/${siteId}/token`, {
      headers: { Authorization: `Bearer ${token}` },
    });

    const { url } = await response.json();
    return url;
  }

  async getApiKey(modelId: string): Promise<string> {
    const cached = this.tokenCache.get(modelId);

    if (cached && cached.expiresAt > Date.now()) {
      return cached.token;
    }

    const token = process.env.GATEWAY_TOKEN;
    if (!token) {
      throw new Error('Missing GATEWAY_TOKEN');
    }

    // Cache token for 1 hour
    this.tokenCache.set(modelId, {
      token,
      expiresAt: Date.now() + 3600000,
    });

    return token;
  }

  async resolveLanguageModel({ modelId, providerId, apiKey }: {
    modelId: string;
    providerId: string;
    apiKey: string;
  }): Promise<LanguageModelV2> {
    const baseURL = await this.buildUrl(`${providerId}/${modelId}`);

    return createOpenAICompatible({
      name: providerId,
      apiKey,
      baseURL,
      supportsStructuredOutputs: true,
    }).chatModel(modelId);
  }
}
```

## 오류 처리 {/* #error-handling */}

일반적인 오류 시나리오에 대한 설명 오류를 제공합니다.

```typescript
class RobustGateway extends MastraModelGateway {
  // ... properties

  async getApiKey(modelId: string): Promise<string> {
    const apiKey = process.env.MY_API_KEY;

    if (!apiKey) {
      throw new Error(
        `Missing MY_API_KEY environment variable for model: ${modelId}. ` +
        `Please set MY_API_KEY in your environment.`
      );
    }

    return apiKey;
  }

  async buildUrl(modelId: string, envVars?: Record<string, string>): Promise<string> {
    const baseUrl = envVars?.BASE_URL || process.env.BASE_URL;

    if (!baseUrl) {
      throw new Error(
        `No base URL configured for model: ${modelId}. ` +
        `Set BASE_URL environment variable or pass it in envVars.`
      );
    }

    return baseUrl;
  }
}
```

## 사용자 정의 게이트웨이 테스트 {/* #testing-custom-gateways */}

테스트 구조 예시:

```typescript
import { describe, it, expect, beforeEach } from 'vitest';
import { Mastra } from '@mastra/core';

describe('MyPrivateGateway', () => {
  beforeEach(() => {
    process.env.MY_API_KEY = 'test-key';
  });

  it('should fetch providers', async () => {
    const gateway = new MyPrivateGateway();
    const providers = await gateway.fetchProviders();

    expect(providers['my-provider']).toBeDefined();
    expect(providers['my-provider'].models).toContain('model-1');
  });

  it('should integrate with Mastra', () => {
    const mastra = new Mastra({
      gateways: {
        private: new MyPrivateGateway(),
      },
    });

    const gateway = mastra.getGateway('private');
    expect(gateway.name).toBe('My Private Gateway');
  });

  it('should resolve models by ID', () => {
    const mastra = new Mastra({
      gateways: {
        key: new MyPrivateGateway(),
      },
    });

    const gateway = mastra.getGatewayById('my-private-gateway');
    expect(gateway).toBeDefined();
  });
});
```

## 모범 사례 {/* #best-practices */}

1. **버전 관리에는 설명적인 ID 사용**: 게이트웨이의 버전을 관리해야 한다면 `id` 값을 명시적으로 설정하세요.   ```typescript
   readonly id = 'my-gateway-v1';
   ```

2. **적절한 오류 처리 구현**: 조치 가능한 메시지와 함께 설명 오류 발생

3. **비용이 많이 드는 작업을 캐시하세요.**: 적절한 경우 토큰, URL 또는 공급자 구성을 캐시합니다.

4. **환경 변수 검증**: `getApiKey`와 `buildUrl`에서 필수 환경 변수를 확인하세요.
5. **게이트웨이 문서화**: 게이트웨이의 목적과 구성을 설명하는 JSDoc 주석을 추가합니다.

6. **명명 규칙을 따르세요**: 공급자와 Model에 명확하고 일관된 이름을 사용합니다.

7. **비동기 작업 처리**: 네트워크 요청과 I/O 작업에는 `async/await`를 사용하세요.
8. **철저한 테스트**: 모든 게이트웨이 메소드에 대한 단위 테스트 작성

## Studio에서 커스텀 게이트웨이만 표시 {/* #showing-only-custom-gateways-in-studio */}

기본적으로 **Studio**에는 등록한 사용자 정의 게이트웨이와 함께 모든 외부 Model Provider(예: OpenAI, Anthropic, Gemini)가 표시됩니다. 외부 Provider를 숨기려면 `AUTO_BLOCK_EXTERNAL_PROVIDERS` 환경 변수를 설정하세요.
```bash
AUTO_BLOCK_EXTERNAL_PROVIDERS=true```

이 변수를 `true` 또는 `1`로 설정하면 Mastra는 사용자가 직접 등록한 게이트웨이의 Provider만 반환합니다. 정적 Provider 레지스트리와 기본 제공 게이트웨이(`models.dev`, `netlify`, `mastra`)는 Model 선택기에서 숨겨집니다. 모든 Model 트래픽을 하나의 비공개 게이트웨이로 라우팅하고 다른 Provider를 표시하고 싶지 않을 때 유용합니다.
이 변수가 설정되고 사용자 지정 게이트웨이가 등록되지 않은 경우 Model 선택기가 비어 있습니다. 해당 Model을 노출하려면 하나 이상의 사용자 정의 게이트웨이를 등록하세요.

## 내장 게이트웨이 {/* #built-in-gateways */}

Mastra에는 참조 구현으로 내장 게이트웨이가 포함되어 있습니다.

* **Netlify게이트웨이**: Netlify AI Gateway와 토큰 교환 연동
* **ModelDevGateway**: models.dev의 OpenAI 호환 공급자 레지스트리

게이트웨이 사용 예시는 [Netlify](/models/gateways/netlify), [OpenRouter](/models/gateways/openrouter), [Vercel](/models/gateways/vercel)을 참조하세요.