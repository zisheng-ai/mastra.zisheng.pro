---
title: "수파베이스 | 인증"
description: "Supabase Auth를 사용하여 Mastra 애플리케이션을 인증하는 `@mastra/auth-supabase` 패키지에 대한 문서입니다."
packages:
  - "@mastra/auth-supabase"
  - "@mastra/client-js"
  - "@mastra/core"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# 수파베이스

그만큼`@mastra/auth-supabase`패키지는 Supabase Auth를 사용하여 Mastra에 대한 인증을 제공합니다. Supabase의 인증 시스템을 사용하여 들어오는 요청을 확인하고 다음을 사용하여 Mastra 서버와 통합합니다.`auth`옵션.

## 전제조건 {/* #prerequisites */}

이 예제에서는 Supabase Auth를 사용합니다. Supabase 자격 증명을 `.env` 파일에 추가하고 Supabase 프로젝트가 올바르게 구성되어 있는지 확인하세요.
```env title=".env"
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
```

:::note

적절한 데이터 액세스 제어를 보장하려면 Superbase 행 수준 보안(RLS) 설정을 검토하세요.

:::

## 설치 {/* #installation */}

`MastraAuthSupabase` 클래스를 사용하기 전에 `@mastra/auth-supabase` 패키지를 설치해야 합니다.
```bash npm2yarn
npm install @mastra/auth-supabase@latest
```

## 사용예 {/* #usage-example */}

```typescript {2,6-9} title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthSupabase } from '@mastra/auth-supabase'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthSupabase({
      url: process.env.SUPABASE_URL,
      anonKey: process.env.SUPABASE_ANON_KEY,
    }),
  },
})
```

:::info

기본 `authorizeUser` 메서드는 `public` 스키마의 `users` 테이블에 있는 `isAdmin` 열을 확인합니다. 사용자 권한 부여를 맞춤 설정하려면 Provider를 생성할 때 사용자 지정 `authorizeUser` 함수를 제공하세요.
사용 가능한 모든 구성 옵션은 [MastraAuthSupabase](/reference/auth/supabase)를 참조하세요.
:::

## 클라이언트 측 설정 {/* #client-side-setup */}

Supabase 인증을 사용하는 경우 클라이언트 측 Supabase에서 액세스 토큰을 검색하여 Mastra 요청에 전달해야 합니다.

### 액세스 토큰 검색 {/* #retrieving-the-access-token */}

Supabase 클라이언트를 사용하여 사용자를 인증하고 액세스 토큰을 검색합니다.

```typescript title="lib/auth.ts"
import { createClient } from '@supabase/supabase-js'

const supabase = createClient('<supabase-url>', '<supabase-key>')

const authTokenResponse = await supabase.auth.signInWithPassword({
  email: "<user's email>",
  password: "<user's password>",
})

const accessToken = authTokenResponse.data?.session?.access_token
```

:::note

OAuth, 매직 링크 등 다른 인증 방식은 [Supabase 문서](https://supabase.com/docs/guides/auth)를 참조하세요.
:::

## 구성`MastraClient` {/* #configuring-mastraclient */}

`auth`가 활성화되면 `MastraClient`로 보내는 모든 요청의 `Authorization` 헤더에 유효한 Supabase 액세스 토큰을 포함해야 합니다.
```typescript {6} title="lib/mastra/mastra-client.ts"
import { MastraClient } from '@mastra/client-js'

export const mastraClient = new MastraClient({
  baseUrl: 'https://<mastra-api-url>',
  headers: {
    Authorization: `Bearer ${accessToken}`,
  },
})
```

:::info

Authorization 헤더의 액세스 토큰 앞에는 `Bearer`를 붙여야 합니다.
더 많은 구성 옵션은 [Mastra Client SDK](/docs/server/mastra-client)를 참조하세요.
:::

### 인증된 요청 만들기 {/* #making-authenticated-requests */}

Supabase 액세스 토큰으로 `MastraClient`를 구성하면 인증된 요청을 보낼 수 있습니다.
<Tabs>
  <TabItem value="react" label="React">
    ```tsx title="src/components/test-agent.tsx"
    import { mastraClient } from '../../lib/mastra-client'

    export const TestAgent = () => {
      async function handleClick() {
        const agent = mastraClient.getAgent('weatherAgent')

        const response = await agent.generate("What's the weather like in New York")

        console.log(response)
      }

      return <button onClick={handleClick}>Test Agent</button>
    }
    ```
  </TabItem>

  <TabItem value="curl" label="cURL">
    ```bash
    curl -X POST http://localhost:4111/api/agents/weatherAgent/generate \
      -H "Content-Type: application/json" \
      -H "Authorization: Bearer <your-supabase-access-token>" \
      -d '{
        "messages": "Weather in London"
      }'
    ```
  </TabItem>
</Tabs>
