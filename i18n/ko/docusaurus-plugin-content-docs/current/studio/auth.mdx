---
title: "인증 | 사진관"
description: "로그인 화면, 역할 기반 액세스 제어, 권한 기반 UI 렌더링을 통해 Studio 배포를 보호하세요."
packages:
  - "@mastra/core"
---

# 스튜디오 인증

구성할 때[authentication](/docs/server/auth/)Mastra 서버에서 Studio는 자동으로 로그인 화면을 표시하고 액세스 제어를 시행합니다. 하나의 구성으로 Studio UI와 API 경로를 모두 보호합니다.

인증이 없으면 Studio 및 모든 API 경로에 공개적으로 액세스할 수 있습니다.

## Studio 인증을 사용하는 경우 {/* #when-to-use-studio-auth */}

* 여러 팀 구성원이 공유 Studio 배포를 통해 Agent, Workflow 및 Tool와 상호 작용해야 합니다.
* 권한은 Agent를 실행하거나 Workflow를 편집하거나 데이터세트를 삭제할 수 있는 사람을 제한해야 합니다.
* 로그인 화면(SSO, 이메일/비밀번호 또는 둘 다)을 통해 Studio 배포에 대한 액세스를 제어해야 합니다.

## 빠른 시작 {/* #quickstart */}

Mastra 서버 구성에 인증 공급자를 추가합니다. 이 예제에서는[Simple Auth](/docs/server/auth/simple-auth) for a minimal setup:

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { SimpleAuth } from '@mastra/core/server'

export const mastra = new Mastra({
  server: {
    auth: new SimpleAuth({
      users: {
        'my-api-key': {
          id: 'user-1',
          name: 'Alice',
          role: 'admin',
        },
      },
    }),
  },
})
```

일단 구성되면 Studio는 로그인 화면을 표시하고 모든 API 요청에 대한 인증을 요구합니다. 방문[Auth docs](/docs/server/auth) for a full list of supported providers.

## 작동 원리 {/* #how-it-works */}

환경[`server.auth`](/reference/configuration#serverauth) does two things at once:

* **스튜디오 UI**: 로그인 화면을 표시합니다. 공급자에 따라 사용자는 SSO, 이메일/비밀번호 또는 둘 다를 통해 로그인합니다.
* **API 경로**: 모든 내장 경로에 대해 인증이 필요합니다(`/api/agents/*`, `/api/workflows/*`, etc.) and custom routes. This applies whether requests come from Studio or direct API calls.

Studio는 다음을 호출하여 사용 가능한 기능을 감지합니다.`GET /api/auth/capabilities` endpoint. The response tells Studio which login methods to render and, if the user is already authenticated, includes their user info and permissions.

## URL을 통해 토큰 전달 {/* #pass-a-token-through-the-url */}

다른 애플리케이션이 Studio에 삽입되거나 링크되면 다음을 통해 인증 토큰을 전달할 수 있습니다.`auth_header` URL parameter. This is useful when an external host already holds a token and wants to open an authenticated Studio session without showing the login screen.

그만큼`auth_header` value always populates the `Authorization` request header. The parameter sets that one header only, so include any scheme prefix the server expects in the value, such as `Bearer`.

쿼리 문자열에 토큰을 사용하여 Studio를 엽니다.

```
https://your-studio-host/?auth_header=Bearer%20your-token
```

Studio는 다음과 같이 토큰을 처리합니다.

* 그것은 읽습니다`auth_header` once on load and sends the value as the `Authorization` header on every API request in that session.
* 제거합니다`auth_header` from the address bar while preserving other query parameters and the hash.
* 토큰을 Memory에만 유지하고 로컬 저장소에 쓰지 않으므로 토큰은 일시적으로 유지되며 페이지를 다시 로드해도 지속되지 않습니다.

토큰은 URL 매개변수에 포함되므로 호스트 애플리케이션은 해당 URL이 생성되고 전송되는 방식을 담당합니다. URL 매개변수는 브라우저 기록, 리퍼러 헤더, 서버 액세스 로그를 통해 노출될 수 있습니다.

## 역할 기반 액세스 제어 {/* #role-based-access-control */}

RBAC를 사용하면 각 사용자가 Studio 내에서 보고 수행할 수 있는 내용을 제어할 수 있습니다. 인증과는 별개입니다.`server.auth` handles who the user is, while `server.rbac` handles what they can do.

### 기본 역할 {/* #default-roles */}

Mastra에는 네 가지 기본 역할이 포함되어 있습니다. 다음에서 가져오세요.`@mastra/core/auth/ee`:

| 역할 | 권한 |
| - | - |
|`owner` | Full access (`*`) |
| `admin` | Read, write, and execute |
| `member` | Read and execute |
| `viewer` | Read-only |

### RBAC 활성화 {/* #enable-rbac */}

사용`StaticRBACProvider` with the default roles or define your own:

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { SimpleAuth } from '@mastra/core/server'
import { StaticRBACProvider, DEFAULT_ROLES } from '@mastra/core/auth/ee'

export const mastra = new Mastra({
  server: {
    auth: new SimpleAuth({
      users: {
        'admin-key': { id: 'user-1', name: 'Alice', role: 'admin' },
        'viewer-key': { id: 'user-2', name: 'Bob', role: 'viewer' },
      },
    }),
    rbac: new StaticRBACProvider({
      roles: DEFAULT_ROLES,
      getUserRoles: user => [user.role],
    }),
  },
})
```

RBAC가 활성화되면 Studio는 사용자에게 권한이 없는 작업을 숨깁니다. 시청자에게는 삭제 버튼이 표시되지 않습니다. 구성원은 Agent 구성을 수정할 수 없습니다.

### 권한 형식 {/* #permission-format */}

권한은 패턴을 따릅니다.`{resource}:{action}`, with optional resource-level scoping:

| 패턴 | 의미 |
| - | - |
|`*` | Full access to everything |
| `*:read` | Read all resources |
| `agents:*` | All actions on agents |
| `agents:execute` | Execute agents only |
| `agents:read:my-id` | Read a specific agent by ID |

리소스에는 다음이 포함됩니다.`agents`, `workflows`, `tools`, `datasets`, `memory`, `scores`, `observability`, and others. Actions are `read`, `write`, `execute`, and `delete`.

### 외부 공급자 역할 매핑 {/* #map-external-provider-roles */}

ID 공급자가 이미 역할(예: 사무원 조직 또는 WorkOS 그룹)을 정의한 경우 이를 다음을 사용하여 Mastra 권한에 매핑합니다.`roleMapping`:

```typescript title="src/mastra/index.ts"
import { StaticRBACProvider } from '@mastra/core/auth/ee'

const rbac = new StaticRBACProvider({
  roleMapping: {
    'org:admin': ['*'],
    'org:member': ['*:read', '*:execute'],
    'org:viewer': ['*:read'],
  },
  getUserRoles: user => user.providerRoles,
})
```

## 로그인 방법 {/* #login-methods */}

Studio는 인증 공급자를 기반으로 로그인 화면을 조정합니다.

| 제공자 유형 | 로그인 UI |
| - | - |
| SSO 전용 | SSO 버튼(예: "WorkOS로 로그인") |
| 자격 증명만 | 이메일 및 비밀번호 양식 |
| 둘 다 | SSO 버튼 및 이메일/비밀번호 양식 |

공급자별로 가입을 활성화하거나 비활성화할 수 있습니다. 비활성화되면 Studio는 가입 링크를 숨기고 로그인 양식을 강제로 표시합니다.

## EE 라이선스 {/* #ee-licensing */}

Studio 인증 기능(SSO 로그인, RBAC, 권한 기반 UI)은 Mastra Enterprise Edition의 일부입니다. 단순 인증을 사용하거나 로컬로 실행할 때 라이선스는 선택 사항입니다. 타사 공급자를 사용한 프로덕션 배포에는 유효한 EE 라이선스가 필요합니다.[Mastra sales](https://mastra.ai/contact).

## 관련된 {/* #related */}

* [인증 개요](/docs/server/auth): 지원되는 인증 Provider의 전체 목록입니다.
* [스튜디오 배포](/docs/studio/deployment): Studio를 프로덕션에 배포합니다.
* [커스텀 API 경로](/docs/server/custom-api-routes#authentication): 개별 엔드포인트의 인증을 제어합니다.
