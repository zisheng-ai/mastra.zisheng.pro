import { CopyPrompt } from "@site/src/components/copy-prompt";

<CopyPrompt identifier="quickstart">
  새로운 마스트라 프로젝트를 생성합니다. Mastra는 최신 TypeScript 스택의 AI 애플리케이션 및 Agent를 위한 프레임워크입니다. 명령을 실행하기 전에 다음 질문을 한 번에 하나씩 물어보고 이미 제공되지 않은 경우 각 답변을 기다립니다.

  1. 프로젝트 이름? (기본값: "my-mastra-app")
  2. 공급자? (필수; 옵션: "openai", "anthropic", "google", "xai")

  공급자가 지원되지 않는 경우 다시 질문하고 지원되는 값을 나열하세요.

  달리다:`npm create mastra@latest <project-name> -- --llm <provider>`

  이 명령은 기본 Mastra 프로젝트를 생성하고, 감지된 코딩 도우미에 대한 Mastra 기술을 설치하고, 적절한 경우 Git을 초기화합니다.

  생성 후 프로젝트 디렉터리를 입력하고 개발 서버를 시작합니다.`npx bgproc start -n <project-name> -w -- npm run dev`

  `http://localhost:4111`에서 Mastra Studio를 엽니다. Studio는 Agent, Workflow, Tool을 구축하고 테스트하고 관리하기 위한 인터페이스입니다.
  또한 Mastra Model 라우터는 수천 개의 Model에 대한 액세스를 제공합니다.[https://mastra.ai/models](https://mastra.ai/models)
</CopyPrompt>
