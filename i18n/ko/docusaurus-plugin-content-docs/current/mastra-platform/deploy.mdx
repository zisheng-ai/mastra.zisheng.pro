---
title: "배포 | 마스트라 플랫폼"
description: "단일 명령을 사용하여 Mastra 애플리케이션을 Mastra 플랫폼에 배포하세요. mastra 배포를 통해 모든 환경에 구축, 검증, 프로비저닝 및 릴리스할 수 있습니다."
---

import Steps from "@site/src/components/Steps";
import StepItem from "@site/src/components/StepItem";

# Mastra 플랫폼에 배포

[`mastra deploy`](/reference/cli/mastra#mastra-deploy)Mastra 애플리케이션을 릴리스하기 위한 단일 명령입니다.[마스트라 플랫폼](/docs/mastra-platform/overview).

하나의 명령은 프로젝트를 빌드하고 포함하기 전에 유효성을 검사하며, 처음 실행 시 플랫폼 프로젝트와 환경을 생성하고, 배포하고, 빌드 로그를 스트리밍하고, 배포가 트래픽을 제공하면 공개 URL을 인쇄합니다.

```bash
mastra deploy
```

:::참고
이 페이지에서는 통합 배포 흐름을 다룹니다. 이전 분할 명령은[`mastra server deploy`](/docs/mastra-platform/server) and [`mastra studio deploy`](/docs/mastra-platform/studio), still work but `mastra deploy`권장 경로입니다.
:::

## 시작하기 전에 {/* #before-you-begin */}

당신은[Mastra application](/guides/getting-started/quickstart) and a [Mastra platform](https://projects.mastra.ai) account. If you're not authenticated, the CLI prompts you to log in on first use.

현지인`.env` file is optional. Environment variables stored on the platform are used as-is at deploy time, and managed resources like [hosted databases](/docs/mastra-platform/database) inject their own variables. Pass `--env-file` only when you want to layer local values on top.

## 첫 번째 배포 {/* #your-first-deploy */}

<Steps>
  <StepItem>
    프로젝트 디렉터리에서 다음을 실행합니다.

    ```bash
    mastra deploy
    ```

    처음 실행 시 CLI는 플랫폼 프로젝트(이름을 따서 명명)를 생성하라는 메시지를 표시합니다.`package.json`) and the `production` environment. Accept the prompts, or pass `--yes` to accept defaults without confirmation.
  </StepItem>

  <StepItem>
    CLI는 배송 전 실행 전 검사를 실행합니다. 로컬 파일 경로(플랫폼의 임시 파일 시스템에서 유지되지 않음)로 대체되는 스토리지는 일반적으로 배포를 차단합니다.

    ```
    file:./mastra.db will be used at runtime because TURSO_DATABASE_URL is not set
    ```

    오류가 발생하는 대신 CLI는 인라인으로 문제를 해결하도록 제안합니다.

    ```
    Preflight needs TURSO_DATABASE_URL for the production environment. Create a managed turso database now and attach it? (Y/n)
    ```

    Prompt를 수락하면 프로비저닝하는 데 몇 초가 걸립니다. 데이터베이스의 연결 변수는 배포에 자동으로 삽입되며, 데이터베이스에 복사할 내용은 없습니다.`.env` file. Decline it, or run in a non-interactive shell (CI, `--yes`), and the CLI falls back to the previous behavior: it prints the exact command to run yourself.

    ```bash
    mastra env db create production --kind turso
    ```

    환경 슬러그(`production` above) matches the environment the CLI would have deployed to — this is important because `mastra env db create` requires an environment argument in non-interactive shells when the project has more than one environment.

    :::참고
    프리플라이트가 대신 하드 코딩된 로컬 경로를 보고하는 경우(`Build contains a host-local storage URL`), it can't offer the inline fix — guard the path with an environment variable first so the file is only used during local development:

    ```ts title="src/mastra/index.ts"
    new LibSQLStore({
      id: 'mastra-storage',
      // Uses the hosted database when deployed, a local file during development
      url: process.env.TURSO_DATABASE_URL ?? 'file:./mastra.db',
      authToken: process.env.TURSO_AUTH_TOKEN,
    })
    ```

    :::
  </StepItem>

  <StepItem>
    달리다`mastra deploy` again. Preflight passes, the build uploads, and the CLI streams build logs until the deploy is live. Expect the full build and deploy to take between 30 seconds and a few minutes. The success message prints only when the new version is serving traffic.
  </StepItem>

  <StepItem>
    CLI에서 인쇄된 URL에서 배포를 확인하세요. 추가`/api/agents` to confirm it returns a JSON list of your agents.

    :::경고
    설정[authentication](/docs/server/auth) before exposing your endpoints publicly.
    :::
  </StepItem>
</Steps>

첫 번째 배포에서는 다음을 작성합니다.`.mastra-project.json` file linking your directory to the platform project. Commit it so later deploys, CI runs, and [`mastra env`](/docs/mastra-platform/environments) commands target the same project without extra flags.

## 다른 환경에 배포 {/* #deploy-to-another-environment */}

`mastra deploy`목표로`production` environment by default. Pass `--env` to target a different one. If the environment doesn't exist yet, the CLI offers to create it:

```bash
mastra deploy --env staging
```

각 환경에는 별도의 URL과 환경 변수가 있습니다. 그것은 또한 자신의 것을 가질 수 있습니다[hosted database](/docs/mastra-platform/database).

보다[Environments](/docs/mastra-platform/environments) for the full model.

## 지역을 선택하세요 {/* #choose-a-region */}

통과하다`--region` when a deploy creates a new environment to control where it runs. Use the `us` or `eu` shorthand:

```bash
mastra deploy --env production --region eu
```

지역은 환경이 생성될 때 고정됩니다. 환경에 연결된 데이터베이스는 해당 환경의 지역 근처에 자동으로 배치되고 관측 가능성 데이터는 환경의 상주 영역과 일치하는 수집 지역으로 라우팅됩니다. 보다[Regions](/docs/mastra-platform/regions) for the full list of supported regions, database placement, and observability co-location.

## 비행 전 확인 {/* #preflight-checks */}

Preflight는 무엇이든 포함하기 전에 빌드된 출력의 유효성을 검사하고 사용자 코드의 문제에만 플래그를 지정합니다.

* **로컬 저장소 경로**: 하드 블록. 파일 지원 스토리지(예:`file:./mastra.db`) is lost on every deploy. Preflight passes when the path is guarded by an environment variable that's set locally, stored on the platform, or provided by a managed database:

  ```ts title="src/mastra/storage.ts"
  import { LibSQLStore } from '@mastra/libsql'

  export const storage = new LibSQLStore({
    id: 'mastra-storage',
    // Uses the hosted database when deployed, a local file during development
    url: process.env.TURSO_DATABASE_URL ?? 'file:./mastra.db',
    authToken: process.env.TURSO_AUTH_TOKEN,
  })
  ```

* **환경 변수가 누락되었습니다.**: 코드에서 읽히지만 소스가 제공하지 않는 변수에 대한 경고입니다. 라이브러리 코드에서만 참조되는 변수는 제외됩니다.

실행 전 블록에 대해 권장되는 대응은 일반적으로 호스팅된 데이터베이스를 연결하거나 플랫폼에 변수를 저장하여 원인을 수정하는 것입니다.`--skip-preflight` exists as an escape hatch but skips the checks that prevent broken deploys.

배포하지 않고 검사를 실행합니다.

```bash
mastra lint --preflight
```

`mastra lint`로컬 env 파일만 볼 수 있습니다. 플랫폼에 저장되거나 관리형 데이터베이스에 의해 주입된 변수는 표시되지 않으므로 배포는 Lint가 여전히 오류를 보고하는 프리플라이트를 전달할 수 있습니다.

## 환경변수 {/* #environment-variables */}

배포는 세 가지 소스에서 환경 변수를 확인합니다.

* **관리되는 변수**: 호스팅된 데이터베이스와 같은 플랫폼 리소스에 의해 주입됩니다(예:`TURSO_DATABASE_URL`). The platform defines these, and you can't edit them.
* **저장된 변수**: 대시보드를 통해 프로젝트나 환경에 저장됩니다. 로컬 파일이 필요하지 않고 모든 배포에서 있는 그대로 사용됩니다.
* **로컬 환경 파일**: 명시적인 배포 계층`--env-file` or the ambient `.env` and `.env.local` files on top.

```bash
mastra deploy --env staging --env-file .env.staging
```

재배포 없이 실행 중인 서비스에서 변수를 변경하려면 대시보드에서 업데이트하고 다음을 실행하세요.[`mastra env restart`](/reference/cli/mastra#mastra-env-restart).

## 프로젝트 해결 {/* #project-resolution */}

모든 배포는 다음 순서로 대상 프로젝트를 해결합니다.

1. 그만큼`MASTRA_PROJECT_ID` environment variable
2. 그만큼`--project <name|slug|id>` flag
3. 그만큼`.mastra-project.json` file in the current directory

CI에서 설정`MASTRA_PROJECT_ID` and `MASTRA_API_TOKEN` and pass `--yes`:

```bash
mastra deploy --env production --yes
```

## 관련된 {/* #related */}

* [환경](/docs/mastra-platform/environments)
* [지역](/docs/mastra-platform/regions)
* [호스팅된 데이터베이스](/docs/mastra-platform/database)
* [`mastra deploy`CLI 참조](/reference/cli/mastra#mastra-deploy)
* [구성](/docs/mastra-platform/configuration)
