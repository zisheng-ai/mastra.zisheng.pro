---
title: "파일 시스템 | 작업공간"
description: "Agent에 영구 파일 저장소를 제공하도록 파일 시스템 공급자를 구성합니다."
packages:
  - "@mastra/core"
---

# 파일 시스템

**추가된 항목:** `@mastra/core@1.1.0`

파일 시스템 공급자는 Agent에 파일 읽기, 쓰기, 관리 기능을 제공합니다. 작업공간에서 파일 시스템을 구성하면 Agent는 파일 작업을 위한 Tool를 받습니다.

파일 시스템 공급자는 작업공간에 대한 모든 파일 작업을 처리합니다.

* **읽다**- 파일 내용 읽기
* **쓰다**- 파일 생성 및 업데이트
* **목록**- 선택적 글로브 패턴 필터링을 사용하여 디렉토리 찾아보기
* **삭제**- 파일 및 디렉토리 제거
* **통계**- 파일 메타데이터 가져오기
* **복사/이동**- 위치 간 파일 복사 또는 이동
* **그렙**- 정규식 패턴을 사용하여 파일 내용 검색

## 지원되는 Provider {/* #supported-providers */}

사용 가능한 Provider:

* [`LocalFilesystem`](/reference/workspace/local-filesystem): 디스크의 디렉토리에 파일을 저장합니다.
* [`S3Filesystem`](/reference/workspace/s3-filesystem): Amazon S3 또는 S3 호환 스토리지(R2, MinIO, Tigris)에 파일을 저장합니다.
* [`GCSFilesystem`](/reference/workspace/gcs-filesystem): Google Cloud Storage에 파일을 저장합니다.
* [`PlatformFilesystem`](/reference/workspace/platform-filesystem): Mastra Platform 작업 공간 버킷에 파일을 저장합니다.
* [`GoogleDriveFilesystem`](/reference/workspace/google-drive-filesystem): Google Drive 폴더에 파일을 저장합니다.
* [`AzureBlobFilesystem`](/reference/workspace/azure-blob-filesystem): Azure Blob Storage에 파일을 저장합니다.
* [`FilesSDKFilesystem`](/reference/workspace/files-sdk-filesystem): 어떤 위치에든 파일을 저장합니다.[FilesSDK](https://files-sdk.dev) adapter (S3, R2, GCS, Azure Blob, Vercel Blob, local filesystem, and more): useful when you want one provider that can target multiple backends
* [`AgentFSFilesystem`](/reference/workspace/agentfs-filesystem): AgentFS를 통해 Turso/SQLite 데이터베이스에 파일을 저장합니다.
* [`MesaFilesystem`](/reference/workspace/mesa-filesystem): 버전이 지정된 Mesa 저장소에 파일을 저장합니다.
* [`ArchilFilesystem`](/reference/workspace/archil-filesystem): Archil 탄력적 서버리스 디스크에 파일을 저장합니다.

:::팁

`LocalFilesystem`외부 서비스가 필요하지 않으므로 시작하는 가장 간단한 방법입니다. 클라우드 스토리지의 경우 다음을 사용하세요.`S3Filesystem`, `GCSFilesystem`, or `AzureBlobFilesystem`. For versioned storage, use `MesaFilesystem`. For database-backed storage without external services, use `AgentFSFilesystem`.

:::

## 기본 사용법 {/* #basic-usage */}

파일 시스템으로 작업공간을 생성하고 이를 Agent에 할당합니다. 그러면 Agent는 작업의 일부로 파일을 읽고, 쓰고, 관리할 수 있습니다.

```typescript title="src/mastra/agents/file-agent.ts"
import { Agent } from '@mastra/core/agent'
import { Workspace, LocalFilesystem } from '@mastra/core/workspace'

const workspace = new Workspace({
  filesystem: new LocalFilesystem({
    basePath: './workspace',
  }),
})

const agent = new Agent({
  id: 'file-agent',
  model: '__GATEWAY_OPENAI_MODEL__',
  instructions: 'You are a helpful file management assistant.',
  workspace,
})

// The agent now has filesystem tools available
const response = await agent.generate('List all files in the workspace')
```

## 방지 {/* #containment */}

기본적으로`LocalFilesystem` runs in **contained mode**, all file operations are restricted to stay within `basePath`. This prevents path traversal attacks and symlink escapes.

포함 모드에서:

* **상대 경로**(예:`src/index.ts`) resolve against `basePath`
* **절대 경로**(예:`/home/user/.config/file.txt`) are treated as real filesystem paths: if they fall outside `basePath` and any `allowedPaths`, a `PermissionError` is thrown
* **물결표 경로**(예:`~/Documents`) expand to the home directory and follow the same containment rules

Agent가 외부의 특정 경로에 액세스해야 하는 경우`basePath`, use `allowedPaths` to grant access without disabling containment entirely. Relative paths are resolved against `basePath`, and absolute paths are used as-is:

```typescript
const workspace = new Workspace({
  filesystem: new LocalFilesystem({
    basePath: './workspace',
    // highlight-next-line
    allowedPaths: ['~/.claude/skills', '../shared-data'],
  }),
})
```

허용된 경로는 다음을 사용하여 런타임 시 업데이트될 수 있습니다.`setAllowedPaths()` method:

```typescript
// Add a path dynamically
workspace.filesystem.setAllowedPaths(prev => [...prev, '/home/user/documents'])
```

이는 최소 권한 액세스에 권장되는 접근 방식입니다. Agent는 사용자가 허용하는 특정 디렉터리에만 접근할 수 있습니다.

Agent가 전체 파일 시스템에 대한 무제한 액세스가 필요한 경우 포함을 비활성화합니다.

```typescript
const workspace = new Workspace({
  filesystem: new LocalFilesystem({
    basePath: './workspace',
    // highlight-next-line
    contained: false,
  }),
})
```

언제`contained` is `false`, absolute paths are treated as real filesystem paths with no restriction.

## 동적 파일 시스템 {/* #dynamic-filesystem */}

그만큼`filesystem` option accepts a resolver function instead of a static instance. The resolver receives `requestContext` and returns a filesystem per request, allowing a single workspace to serve different filesystems based on the caller's identity, role, or tenant.

```typescript title="src/mastra/workspaces.ts"
import { Agent } from '@mastra/core/agent'
import { Workspace, LocalFilesystem } from '@mastra/core/workspace'

const workspace = new Workspace({
  // highlight-start
  filesystem: ({ requestContext }) => {
    const role = requestContext.get('agent-role') || 'guest'
    return new LocalFilesystem({
      basePath: `/workspaces/${role}`,
      readOnly: role !== 'admin',
    })
  },
  // highlight-end
})

const agent = new Agent({
  id: 'multi-role-agent',
  model: '__GATEWAY_OPENAI_MODEL__',
  workspace,
})
```

각 요청은 작업 공간 Tool 및 작업 공간 지침에 대한 자체 파일 시스템을 확인합니다.

```typescript
import { RequestContext } from '@mastra/core/request-context'

// Admin request — reads and writes from /workspaces/admin/
const adminCtx = new RequestContext([['agent-role', 'admin']])
await agent.generate('Write report.txt with Q4 results', { requestContext: adminCtx })

// Viewer request — reads from /workspaces/viewer/, writes are blocked
const viewerCtx = new RequestContext([['agent-role', 'viewer']])
await agent.generate('Read info.txt', { requestContext: viewerCtx })
```

작업 공간 지침은 동일하게 사용됩니다.`requestContext`, so the agent sees the filesystem context for the resolved provider.

예를 들어 데이터베이스에서 구성을 조회하기 위해 해석기는 비동기식일 수도 있습니다.

```typescript
const workspace = new Workspace({
  filesystem: async ({ requestContext }) => {
    const tenantConfig = await db.getTenant(requestContext.get('tenant-id'))
    return new LocalFilesystem({ basePath: tenantConfig.storagePath })
  },
})
```

:::메모

`filesystem`그리고`mounts` are mutually exclusive. You can't use a resolver function together with `mounts` in the same workspace.

:::

## 읽기 전용 모드 {/* #read-only-mode */}

Agent가 파일을 수정하지 못하도록 하려면 읽기 전용 모드를 활성화하세요.

```typescript
const workspace = new Workspace({
  filesystem: new LocalFilesystem({
    basePath: './workspace',
    // highlight-next-line
    readOnly: true,
  }),
})
```

정적 파일 시스템을 사용하면 Tool 작성(`write_file`, `edit_file`, `delete`, `mkdir`) are excluded from the agent's toolset entirely. The agent can still read and list files.

사용할 때[dynamic filesystem](#dynamic-filesystem), write tools are always included because `readOnly` isn't known until the resolver runs. Instead, write operations are blocked at runtime, the tool returns an error if the resolved filesystem is read-only.

## 마운트 및`CompositeFilesystem` {/* #mounts-and-compositefilesystem */}

당신이 사용할 때`mounts` option on a workspace, Mastra creates a `CompositeFilesystem` that routes file operations to the correct provider based on path prefix.

```typescript
import { Workspace } from '@mastra/core/workspace'
import { S3Filesystem } from '@mastra/s3'
import { GCSFilesystem } from '@mastra/gcs'
import { E2BSandbox } from '@mastra/e2b'

const workspace = new Workspace({
  mounts: {
    '/data': new S3Filesystem({
      bucket: 'my-bucket',
      region: 'us-east-1',
      accessKeyId: process.env.AWS_ACCESS_KEY_ID,
      secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY,
    }),
    '/skills': new GCSFilesystem({
      bucket: 'agent-skills',
    }),
  },
  sandbox: new E2BSandbox({ id: 'dev-sandbox' }),
})
```

이 구성을 사용하면 다음과 같습니다.

* `read_file('/data/input.csv')`S3 버킷에서 읽습니다.
* `write_file('/skills/guide.md', content)`GCS 버킷에 씁니다.
* `list_directory('/')`다음에 대한 가상 항목을 반환합니다.`/data` and `/skills`
* 샌드박스의 명령은 다음 위치의 파일에 액세스할 수 있습니다.`/data` and `/skills` via FUSE mounts

### 경로 라우팅 {/* #path-routing */}

경로가 탑재와 일치하지 않으면 작업이 실패하므로 모든 파일 경로는 탑재 접두사로 시작해야 합니다. 루트 디렉터리 나열(`/`) returns virtual directory entries for each mount point.

마운트 경로는 중첩될 수 없습니다. 예를 들어 두 경로 모두에 마운트할 수는 없습니다.`/data` and `/data/sub`.

### `filesystem`대`mounts` {/* #filesystem-vs-mounts */}

`filesystem`그리고`mounts` are mutually exclusive options on a workspace:

* 사&#xC6A9;**`filesystem`** when you have a single storage provider and don't need to mount it into a sandbox. The agent gets file tools that operate directly against the provider.
* 사&#xC6A9;**`mounts`** when you need cloud storage accessible inside a sandbox, or when you want to combine multiple providers. The workspace creates a CompositeFilesystem for file tools and FUSE-mounts the storage into the sandbox.

로컬 개발의 경우 일반적으로 필요하지 않습니다.`mounts`, a `LocalFilesystem` and `LocalSandbox` pointed at the same directory gives you both file tools and command execution on the same files. See [configuration patterns](/docs/workspace/overview#configuration-patterns) for more detail.

## Agent Tool {/* #agent-tools */}

작업 영역에서 파일 시스템을 구성하면 Agent는 파일 읽기, 쓰기, 나열 및 삭제를 위한 Tool를 받습니다. 보다[workspace class reference](/reference/workspace/workspace-class#filesystem-tools) for details.

## 관련된 {/* #related */}

* [로컬파일시스템 참조](/reference/workspace/local-filesystem)
* [S3파일 시스템 참조](/reference/workspace/s3-filesystem)
* [GCS파일 시스템 참조](/reference/workspace/gcs-filesystem)
* [GoogleDrive파일 시스템 참조](/reference/workspace/google-drive-filesystem)
* [AzureBlob파일 시스템 참조](/reference/workspace/azure-blob-filesystem)
* [FilesSDK파일 시스템 참조](/reference/workspace/files-sdk-filesystem)
* [AgentFS파일 시스템 참조](/reference/workspace/agentfs-filesystem)
* [메사파일시스템 참조](/reference/workspace/mesa-filesystem)
* [작업공간 개요](/docs/workspace/overview)
* [모래 상자](/docs/workspace/sandbox)
