---
title: "반응 + VITE | 프레임워크"
description: "Mastra를 React 및 Vite와 통합하는 단계별 가이드입니다."
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# React + Vite 프로젝트에 Mastra 통합

이 가이드에서는 Mastra를 사용하여 Tool 호출 AI Agent를 구축한 다음 Mastra의 독립 실행형 서버에서 직접 Agent를 호출하여 이를 React에 연결합니다.

당신은 사용할 것입니다[AI SDK UI](https://ai-sdk.dev/docs/ai-sdk-ui/overview) and [AI 요소](https://ai-sdk.dev/elements)아름답고 대화형 채팅 경험을 만들어 보세요.

## 시작하기 전에 {/* #before-you-begin */}

* 지원되는 API 키가 필요합니다.[model provider](/models). If you don't have a preference, use [OpenAI](/models/providers/openai).
* Node.js 설치`v22.13.0` or later

## 새로운 React + Vite 앱 만들기(선택 사항) {/* #create-a-new-react--vite-app-optional */}

Tailwind를 사용하는 React + Vite 앱이 이미 있다면 다음 단계로 건너뛰세요.

### 프로젝트 비계 {/* #project-scaffold */}

다음 명령을 실행하여[create a new React + Vite app](https://vite.dev/guide/#scaffolding-your-first-vite-project):

```bash npm2yarn
npm create vite@latest mastra-react -- --template react-ts
```

그러면 다음과 같은 프로젝트가 생성됩니다.`mastra-react`, but you can replace it with any name you want.

프로젝트 디렉터리로 이동합니다.

```bash
cd mastra-react
```

### 순풍 {/* #tailwind */}

다음으로 Tailwind를 설치합니다.

```bash npm2yarn
npm install tailwindcss @tailwindcss/vite
```

Vite 플러그인 구성:

```typescript title="vite.config.ts" {3-4,7-12}
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

모든 것을 교체하십시오.`src/index.css` with the following:

```css title="src/index.css"
@import 'tailwindcss';
```

이것을 추가하세요`compilerOptions` to `tsconfig.json`:

```jsonc title="tsconfig.json"
{
  // ...
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
    },
  },
}
```

편집하다`tsconfig.app.json` to resolve paths:

```jsonc title="tsconfig.app.json"
{
  "compilerOptions": {
    // ...
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
    },
    // ...
  },
}
```

## 마스트라 초기화 {/* #initialize-mastra */}

달리다[`mastra init`](/reference/cli/mastra#mastra-init). When prompted, choose a provider (e.g. OpenAI) and enter your key:

```bash npm2yarn
npx mastra@latest init
```

이것은`src/mastra` folder with an example weather agent and the following files:

* `index.ts`- Memory를 포함한 마스트라 구성
* `tools/weather-tool.ts`- 특정 위치의 날씨를 가져오는 Tool
* `agents/weather-agent.ts`- Tool를 사용하는 Prompt가 있는 기상 요원

당신은 전화할 것이다`weather-agent.ts` from your chat UI in the next steps.

## AI SDK UI 및 AI 요소 설치 {/* #install-ai-sdk-ui--ai-elements */}

Mastra 어댑터와 함께 AI SDK UI를 설치합니다.

```bash npm2yarn
npm install @mastra/ai-sdk@latest @ai-sdk/react ai
```

다음으로 AI 요소를 초기화합니다. 메시지가 표시되면 기본 옵션을 선택합니다.

```bash npm2yarn
npx ai-elements@latest
```

그러면 전체 AI Elements UI 구성 요소 라이브러리가`@/components/ai-elements` folder.

## 채팅 경로 만들기 {/* #create-a-chat-route */}

열기 ⁠`src/mastra/index.ts` and add a [⁠chatRoute()](/reference/ai-sdk/chat-route) to your config. This creates an API route your React frontend can call for AI SDK-compatible chat responses, which you’ll use with ⁠useChat() next.

```ts title="src/mastra/index.ts"
import { Mastra } from '@mastra/core/mastra'
// Existing imports...
import { chatRoute } from '@mastra/ai-sdk'

export const mastra = new Mastra({
  // Existing config...
  server: {
    apiRoutes: [
      chatRoute({
        path: '/chat/:agentId',
      }),
    ],
  },
})
```

## 채팅 UI 추가 {/* #add-the-chat-ui */}

교체`src/App.tsx` file to create a chat interface:

```tsx title="src/App.tsx"
import * as React from 'react'
import { DefaultChatTransport, type ToolUIPart } from 'ai'
import { useChat } from '@ai-sdk/react'

import {
  PromptInput,
  PromptInputBody,
  PromptInputTextarea,
} from '@/components/ai-elements/prompt-input'

import {
  Conversation,
  ConversationContent,
  ConversationScrollButton,
} from '@/components/ai-elements/conversation'

import { Message, MessageContent, MessageResponse } from '@/components/ai-elements/message'

import { Tool, ToolHeader, ToolContent, ToolInput, ToolOutput } from '@/components/ai-elements/tool'

export default function App() {
  const [input, setInput] = React.useState<string>('')

  const { messages, sendMessage, status } = useChat({
    transport: new DefaultChatTransport({
      api: 'http://localhost:4111/chat/weather-agent',
    }),
  })

  const handleSubmit = async () => {
    if (!input.trim()) return

    sendMessage({ text: input })
    setInput('')
  }

  return (
    <div className="relative mx-auto size-full h-screen max-w-4xl p-6">
      <div className="flex h-full flex-col">
        <Conversation className="h-full">
          <ConversationContent>
            {messages.map(message => (
              <div key={message.id}>
                {message.parts?.map((part, i) => {
                  if (part.type === 'text') {
                    return (
                      <Message key={`${message.id}-${i}`} from={message.role}>
                        <MessageContent>
                          <MessageResponse>{part.text}</MessageResponse>
                        </MessageContent>
                      </Message>
                    )
                  }

                  if (part.type?.startsWith('tool-')) {
                    return (
                      <Tool key={`${message.id}-${i}`}>
                        <ToolHeader
                          type={(part as ToolUIPart).type}
                          state={(part as ToolUIPart).state || 'output-available'}
                          className="cursor-pointer"
                        />
                        <ToolContent>
                          <ToolInput input={(part as ToolUIPart).input || {}} />
                          <ToolOutput
                            output={(part as ToolUIPart).output}
                            errorText={(part as ToolUIPart).errorText}
                          />
                        </ToolContent>
                      </Tool>
                    )
                  }

                  return null
                })}
              </div>
            ))}
            <ConversationScrollButton />
          </ConversationContent>
        </Conversation>
        <PromptInput onSubmit={handleSubmit} className="mt-20">
          <PromptInputBody>
            <PromptInputTextarea
              onChange={e => setInput(e.target.value)}
              className="md:leading-10"
              value={input}
              placeholder="Ask about the weather..."
              disabled={status !== 'ready'}
            />
          </PromptInputBody>
        </PromptInput>
      </div>
    </div>
  )
}
```

이 구성 요소는 연결됩니다.[`useChat()`](https://ai-sdk.dev/docs/reference/ai-sdk-ui/use-chat) to the `chat/weather-agent` endpoint, sending prompts there and streaming the response back in chunks.

다음을 사용하여 응답 텍스트를 렌더링합니다.[`<MessageResponse>`](https://ai-sdk.dev/elements/components/message#messageresponse-) component, and shows any tool invocations with the [`<Tool>`](https://ai-sdk.dev/elements/components/tool) component.

## Agent 테스트 {/* #test-your-agent */}

채팅 인터페이스로 Agent를 테스트하려면 Mastra 서버와 Vite 개발 서버를 모두 실행하세요.

1. Mastra 개발 서버를 시작합니다.

   ```bash npm2yarn
   npx mastra dev
   ```
2. 별도의 터미널 창에서 Vite 개발 서버를 시작합니다.

   ```bash npm2yarn
   npm run dev
   ```
3. 다음에서 신청서를 엽니다.[http://localhost:5173](http://localhost:5173)
4. 날씨에 대해 물어보세요. API 키가 올바르게 설정되면 응답을 받게 됩니다.

## 다음 단계 {/* #next-steps */}

React를 사용하여 Mastra Agent를 구축한 것을 축하합니다! 🎉

여기에서 자신만의 Tool와 논리를 사용하여 프로젝트를 확장할 수 있습니다.

* 자세히 알아보기[agents](/docs/agents/overview)
* Agent에 고유한 권한을 부여하세요.[tools](/docs/agents/using-tools)
* 인간과 같은 추가[memory](/docs/memory/overview) to your agent

준비가 되면 Mastra가 AI SDK UI 및 React와 통합되는 방법과 Agent를 어디에나 배포하는 방법에 대해 자세히 알아보세요.

* Mastra를 다음과 통합하세요.[AI SDK UI](/guides/build-your-ui/ai-sdk-ui)
* Agent 배포[anywhere](/docs/deployment/overview)
