---
title: "참조: mastra.schedules | 일정"
description: "Agent 및 Workflow 크론 일정을 생성하고 관리하기 위한 서비스인 mastra.schedules에 대한 API 참조입니다."
packages:
  - "@mastra/core"
---

# `mastra.schedules`

**추가된 항목:** `@mastra/core@1.50.0`

`mastra.schedules`지속적인 크론 일정을 위한 CRUD 서비스입니다. 이를 사용하여 Agent 또는 Workflow에 대한 일정을 생성, 나열, 업데이트, 일시 중지, 재개, 수동 실행 및 삭제합니다.

사용 패턴 및 개념은 다음을 참조하세요.[Schedules](/docs/long-running-agents/schedules).

## 사용예 {/* #usage-example */}

Agent 일정을 만듭니다.

```typescript title="src/mastra/schedules.ts"
const schedule = await mastra.schedules.create({
  agentId: 'pinger',
  cron: '0 * * * *',
  prompt: 'Give me a status update.',
})
```

Workflow 일정을 만듭니다.

```typescript title="src/mastra/schedules.ts"
const schedule = await mastra.schedules.create({
  workflowId: 'daily-report',
  cron: '0 9 * * *',
  inputData: { reportType: 'summary' },
})
```

일정에는 일정 도메인을 구현하는 스토리지 어댑터가 필요합니다. 지원되는 어댑터는 다음과 같습니다.`@mastra/libsql`, `@mastra/pg`, `@mastra/mysql`, `@mastra/mongodb`, `@mastra/convex`, 그리고`@mastra/spanner`.

## 행동 양식 {/* #methods */}

### 일정 만들기 {/* #create-schedules */}

#### `create(input)` {/* #createinput */}

Agent 또는 Workflow 일정을 만듭니다. 통과하다`agentId` to create an agent schedule, or `workflowId` to create a workflow schedule.

```typescript
const schedule = await mastra.schedules.create({
  agentId: 'pinger',
  cron: '0 * * * *',
  prompt: 'Give me a status update.',
})
```

##### Agent 일정 입력 {/* #agent-schedule-input */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description:
    'Optional stable schedule ID. The value is normalized to `agent_<slug>`. When omitted, Mastra generates an `agent_<uuid>` ID.',
  isOptional: true,
},
{
  name: 'agentId',
  type: 'string',
  description: 'ID of the agent to run on each schedule fire.',
},
{
  name: 'cron',
  type: 'string',
  description: 'Cron expression for the schedule. Accepts 5-, 6-, or 7-part cron expressions and Croner nicknames.',
},
{
  name: 'prompt',
  type: 'string',
  description: 'Prompt sent to the agent on each schedule fire.',
},
{
  name: 'name',
  type: 'string',
  description: 'Free-form label for distinguishing multiple schedules on the same agent or thread.',
  isOptional: true,
},
{
  name: 'timezone',
  type: 'string',
  description: 'IANA timezone used to resolve cron fire times, for example `America/New_York`.',
  isOptional: true,
},
{
  name: 'threadId',
  type: 'string',
  description:
    'Thread that receives the scheduled signal. When omitted, each fire runs threadless with `agent.generate()`.',
  isOptional: true,
},
{
  name: 'resourceId',
  type: 'string',
  description: 'Resource ID for threaded schedules. Required when `threadId` is set.',
  isOptional: true,
},
{
  name: 'signalType',
  type: 'AgentSignalType',
  description: 'Signal type for threaded schedule fires. Defaults to `notification`.',
  isOptional: true,
},
{
  name: 'tagName',
  type: 'string',
  description: 'XML tag name used to render the scheduled signal. Defaults to `schedule`.',
  isOptional: true,
},
{
  name: 'attributes',
  type: 'AgentSignalAttributes',
  description: 'Attributes rendered on the scheduled signal XML tag.',
  isOptional: true,
},
{
  name: 'providerOptions',
  type: 'Record<string, unknown>',
  description: 'JSON-safe provider options merged into the schedule signal payload on every fire.',
  isOptional: true,
},
{
  name: 'ifActive',
  type: 'ScheduleIfActive',
  description: 'Behavior when the target thread is actively streaming. Requires `threadId`.',
  isOptional: true,
},
{
  name: 'ifIdle',
  type: 'ScheduleIfIdle',
  description: 'Behavior when the target thread is idle. Requires `threadId`.',
  isOptional: true,
},
{
  name: 'metadata',
  type: 'Record<string, unknown>',
  description: 'Arbitrary metadata stored with the schedule row.',
  isOptional: true,
},
{
  name: 'status',
  type: "'active' | 'paused'",
  description: 'Initial lifecycle status. Defaults to `active`.',
  isOptional: true,
  defaultValue: "'active'",
},
]}
/>

##### Workflow우 일정 입력 {/* #workflow-schedule-input */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description:
    'Optional stable schedule ID. The value is normalized to `schedule_<slug>`. When omitted, Mastra generates a `schedule_<uuid>` ID.',
  isOptional: true,
},
{
  name: 'workflowId',
  type: 'string',
  description: 'ID of the workflow to start on each schedule fire.',
},
{
  name: 'cron',
  type: 'string',
  description: 'Cron expression for the schedule. Accepts 5-, 6-, or 7-part cron expressions and Croner nicknames.',
},
{
  name: 'timezone',
  type: 'string',
  description: 'IANA timezone used to resolve cron fire times.',
  isOptional: true,
},
{
  name: 'inputData',
  type: 'unknown',
  description: 'Input data passed to the workflow run.',
  isOptional: true,
},
{
  name: 'initialState',
  type: 'unknown',
  description: 'Initial workflow state for the scheduled run.',
  isOptional: true,
},
{
  name: 'requestContext',
  type: 'Record<string, unknown>',
  description: 'Request context passed to the workflow run.',
  isOptional: true,
},
{
  name: 'metadata',
  type: 'Record<string, unknown>',
  description: 'Arbitrary metadata stored with the schedule row.',
  isOptional: true,
},
{
  name: 'status',
  type: "'active' | 'paused'",
  description: 'Initial lifecycle status. Defaults to `active`.',
  isOptional: true,
  defaultValue: "'active'",
},
]}
/>

### 일정 읽기 {/* #read-schedules */}

#### `get(id)` {/* #getid */}

ID별로 일정을 가져옵니다. 베어 Agent 일정 ID도 정규화된 일정 ID로 확인됩니다.`agent_<slug>` form.

```typescript
const schedule = await mastra.schedules.get('pinger')
```

#### `list(filter?)` {/* #listfilter */}

일정을 나열합니다. 필터가 없으면 Agent 및 Workflow 일정이 반환됩니다.

```typescript
const schedules = await mastra.schedules.list({
  agentId: 'pinger',
  status: 'active',
})
```

<PropertiesTable
  content={[
{
  name: 'filter',
  type: 'ListSchedulesFilter',
  description: 'Optional filters for the list operation.',
  isOptional: true,
  properties: [
    {
      type: 'ListSchedulesFilter',
      parameters: [
        {
          name: 'agentId',
          type: 'string',
          description: 'Return only agent schedules for this agent.',
          isOptional: true,
        },
        {
          name: 'workflowId',
          type: 'string',
          description: 'Return only workflow schedules for this workflow.',
          isOptional: true,
        },
        {
          name: 'threadId',
          type: 'string',
          description: 'Return only agent schedules for this thread.',
          isOptional: true,
        },
        {
          name: 'resourceId',
          type: 'string',
          description: 'Return only agent schedules for this resource.',
          isOptional: true,
        },
        {
          name: 'name',
          type: 'string',
          description: 'Return only agent schedules with this label.',
          isOptional: true,
        },
        {
          name: 'status',
          type: "'active' | 'paused'",
          description: 'Return only schedules with this status.',
          isOptional: true,
        },
      ],
    },
  ],
},
]}
/>

### 업데이트 일정 {/* #update-schedules */}

#### `update(id, patch)` {/* #updateid-patch */}

일정을 업데이트합니다. 작고 보기 어리석은 동물`cron` or `timezone` recomputes the next fire time. Updating `status` from `paused` to `active` also recomputes the next fire time.

```typescript
const updated = await mastra.schedules.update('pinger', {
  cron: '*/30 * * * *',
  prompt: 'Give me a status update every 30 minutes.',
})
```

Agent 일정 패치가 업데이트될 수 있음`cron`, `timezone`, `prompt`, `name`, `signalType`, `tagName`, `attributes`, `providerOptions`, `ifActive`, `ifIdle`, `metadata`, and `status`. `threadId`그리고`resourceId`패치가 불가능합니다. 스레드 대상을 변경해야 할 경우 새 일정을 만듭니다.

Workflow 일정 패치가 업데이트될 수 있습니다.`cron`, `timezone`, `inputData`, `initialState`, `requestContext`, `metadata`, and `status`. Agent-only patch fields such as `prompt`, `signalType`, and `ifIdle` throw on workflow schedules.

### 수명주기 {/* #lifecycle */}

#### `pause(id)` {/* #pauseid */}

일정을 일시 중지합니다. 일시 중지는 지속적이고 멱등적입니다.

```typescript
const paused = await mastra.schedules.pause('pinger')
```

#### `resume(id)` {/* #resumeid */}

일시 중지된 일정을 재개하고 현재 시간에서 다음 실행 시간을 다시 계산합니다.

```typescript
const active = await mastra.schedules.resume('pinger')
```

#### `run(id)` {/* #runid */}

크론 주기를 변경하지 않고 즉시 일정을 한 번 실행합니다.

```typescript
const run = await mastra.schedules.run('pinger')
```

Agent 일정의 경우`claimId` uses `manual_<scheduleId>_<timestamp>`. For workflow schedules, `claimId` uses `sched_<scheduleId>_<timestamp>` and is reused as the workflow run ID.

#### `delete(id)` {/* #deleteid */}

일정을 삭제합니다. 누락된 일정을 삭제하는 것은 아무런 작업이 아닙니다.

```typescript
await mastra.schedules.delete('pinger')
```

## 행동 예약 {/* #schedule-behavior */}

* Agent 일정 ID는`agent_` prefix. Workflow schedule IDs created through `mastra.schedules.create()` use the `schedule_` prefix.
* 스레드 Agent 일정에는 다음이 필요합니다.`resourceId` when `threadId` is set.
* `signalType`, `ifActive`, `ifIdle`, and `resourceId` require `threadId`.
* Workflow 일정은 다음과 같은 Agent 전용 패치 필드를 허용하지 않습니다.`prompt`, `signalType`, or `ifIdle`.
* `run()`수동 실행을 즉시 게시하고 저장된 크론 주기를 변경하지 않습니다.
