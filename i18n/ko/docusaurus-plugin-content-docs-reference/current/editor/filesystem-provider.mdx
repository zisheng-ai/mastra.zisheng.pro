---
title: "참조: FilesystemProvider | 편집자"
description: "MastraEditor에 파일 시스템 구현을 등록하는 인터페이스인 FilesystemProvider에 대한 참조입니다."
packages:
  - "@mastra/core"
---

# 파일 시스템 공급자

`FilesystemProvider`파일 시스템을 등록하기 위해 패키지가 구현하는 인터페이스입니다.[`MastraEditor`](/reference/editor/mastra-editor). 편집자가 전화함`createFilesystem(config)`작업 공간 수분 공급 시간에`provider`저장된 이드에서[`StorageWorkspaceRef`](/reference/editor/storage-workspace-ref)파일 시스템 구성을 조회 키로 사용합니다.

내장`local` filesystem provider is auto-registered. External providers (for example, S3 or GCS) are supplied via `MastraEditorConfig.filesystems`.

## 사용예 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'
import { s3FilesystemProvider } from '@mastra/s3'

new MastraEditor({
  filesystems: {
    [s3FilesystemProvider.id]: s3FilesystemProvider,
  },
})
```

## 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description:
    'Unique provider identifier (for example, `"local"`, `"s3"`). Must match `StorageFilesystemConfig.provider` on every stored workspace that uses this provider.',
},
{
  name: 'name',
  type: 'string',
  description: 'Human-readable name for UI display.',
},
{
  name: 'description',
  type: 'string',
  description: 'Short description shown in the workspace filesystem picker.',
  isOptional: true,
},
{
  name: 'configSchema',
  type: 'Record<string, unknown>',
  description: 'JSON Schema describing provider-specific configuration. Used by the UI to render config forms.',
  isOptional: true,
},
{
  name: 'createFilesystem',
  type: '(config: TConfig) => WorkspaceFilesystem | Promise<WorkspaceFilesystem>',
  description:
    'Create a runtime `WorkspaceFilesystem` instance from the stored config. Called at workspace hydration time.',
},
]}
/>

## 공급자 구현 {/* #implementing-a-provider */}

```typescript title="src/providers/my-filesystem.ts"
import type { FilesystemProvider, WorkspaceFilesystem } from '@mastra/core/editor'

export const myFilesystemProvider: FilesystemProvider<{ bucket: string; region: string }> = {
  id: 'my-fs',
  name: 'My Filesystem',
  description: 'Object-store-backed filesystem.',
  configSchema: {
    type: 'object',
    required: ['bucket', 'region'],
    properties: {
      bucket: { type: 'string' },
      region: { type: 'string' },
    },
  },
  async createFilesystem(config): Promise<WorkspaceFilesystem> {
    return createMyFilesystem({ bucket: config.bucket, region: config.region })
  },
}
```

등록되면 관리자는 인라인 작업 공간 구성 또는 저장된 공급자를 참조할 수 있습니다.[`StorageWorkspaceRef`](/reference/editor/storage-workspace-ref).

## 관련된 {/* #related */}

* [작업공간](https://agent-builder.mastra.ai/workspace): 개념 및 작업 사례.
* [스토리지작업공간참조](/reference/editor/storage-workspace-ref): 저장된 구성은 다음에서 사용됩니다.`createFilesystem`.
* [샌드박스 제공자](/reference/editor/sandbox-provider): 명령 실행을 위한 형제 공급자입니다.
* [MastraEditor 클래스](/reference/editor/mastra-editor): 공급자 레지스트리.
