---
title: "참조: StorageWorkspaceRef | 편집자"
description: "작업공간에 저장된 Agent를 가리키는 구별된 공용체인 StorageWorkspaceRef에 대한 참조입니다."
packages:
  - "@mastra/core"
---

# 스토리지작업공간참조

`StorageWorkspaceRef`저장된 Agent에 작업공간을 연결하는 데 사용되는 식별된 공용체입니다. ID로 Mastra 런타임에 등록된 작업공간을 가리키거나 작업공간 스냅샷을 인라인에 포함합니다.

[`BuilderAgentDefaults.workspace`](https://agent-builder.mastra.ai/reference/builder-agent-defaults)와 저장된 Agent 레코드에서 사용하는 유형입니다.
## 사용예 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'

// Variant 1 — reference a runtime workspace by id
new MastraEditor({
  builder: {
    enabled: true,
    configuration: {
      agent: {
        workspace: { type: 'id', workspaceId: 'builder-workspace' },
      },
    },
  },
})

// Variant 2 — inline a workspace snapshot
new MastraEditor({
  builder: {
    enabled: true,
    configuration: {
      agent: {
        workspace: {
          type: 'inline',
          config: {
            name: 'inline-builder-workspace',
            filesystem: {
              provider: 'local',
              config: { basePath: './agent-files' },
            },
          },
        },
      },
    },
  },
})
```

## 변형 {/* #variants */}

<PropertiesTable
  content={[
{
  name: "{ type: 'id'; workspaceId: string }",
  type: 'IdRef',
  description:
    '`new Mastra({ workspace })` 또는 `mastra.addWorkspace()`를 통해 Mastra 런타임에 등록된 Workspace를 참조합니다. id는 관리자 구성, 저장된 Agent 레코드 및 하이드레이션을 연결하는 조인 키입니다.',
  properties: [    {
      type: "{ type: 'id'; workspaceId: string }",
      parameters: [        {
          name: 'type',
          type: "'id'",
          description: '판별자입니다. 리터럴 `"id"`여야 합니다.',
        },        {
          name: 'workspaceId',
          type: 'string',
          description:
            'Mastra 런타임에 등록된 Workspace의 ID입니다. Builder가 Workspace의 스냅샷을 만들고 영구 저장한 후 나중에 하이드레이션하는 데 사용합니다.',
        },
      ],
    },
  ],
},{
  name: "{ type: 'inline'; config: StorageWorkspaceSnapshotType }",
  type: 'InlineRef',
  description:
    'Workspace 스냅샷을 직접 포함합니다. Builder는 `inline-<sha256(config)[:12]>` 형식의 결정적 ID를 도출하고 스냅샷을 영구 저장하므로, 동일한 인라인 구성은 여러 Agent에서 중복 제거됩니다.',
  properties: [    {
      type: "{ type: 'inline'; config: StorageWorkspaceSnapshotType }",
      parameters: [        {
          name: 'type',
          type: "'inline'",
          description: '판별자입니다. 리터럴 `"inline"`이어야 합니다.',
        },        {
          name: 'config',
          type: 'StorageWorkspaceSnapshotType',
          description:
            '직렬화된 Workspace 구성입니다(파일 시스템, Sandbox, 마운트, 검색, Skill, Tool). 필드 목록은 아래의 `StorageWorkspaceSnapshotType` 섹션을 참조하세요.',
        },
      ],
    },
  ],
},
]}
/>
## `StorageWorkspaceSnapshotType` {/* #storageworkspacesnapshottype */}

아래에 삽입된 `{ type: 'inline', config }` 형태입니다. `@mastra/core/storage`에 정의되어 있습니다.
<PropertiesTable
  content={[
{
  name: 'name',
  type: 'string',
  description: 'Workspace의 표시 이름입니다.',
},{
  name: 'description',
  type: 'string',
  description: 'Workspace 목록에 표시되는 용도 설명입니다.',
  isOptional: true,
},{
  name: 'filesystem',
  type: 'StorageFilesystemConfig',
  description:
    '기본 파일 시스템 구성입니다. `provider`는 `MastraEditor.filesystems`에 등록된 ID와 일치해야 합니다(기본 제공: `local`).',
  isOptional: true,
},{
  name: 'sandbox',
  type: 'StorageSandboxConfig',
  description:
    'Sandbox 구성입니다. `provider`는 `MastraEditor.sandboxes`에 등록된 ID와 일치해야 합니다(기본 제공: `local`).',
  isOptional: true,
},{
  name: 'mounts',
  type: 'Record<string, StorageFilesystemConfig>',
  description: 'Workspace에 마운트되는 추가 파일 시스템입니다. 마운트 경로를 키로 사용합니다.',
  isOptional: true,
},{
  name: 'search',
  type: 'StorageSearchConfig',
  description: '검색 구성입니다(벡터 Provider, 임베더, BM25 설정).',
  isOptional: true,
},{
  name: 'skills',
  type: 'string[]',
  description: '이 Workspace에 할당된 Skill 엔터티 ID입니다.',
  isOptional: true,
},{
  name: 'tools',
  type: 'StorageWorkspaceToolsConfig',
  description: 'Workspace Tool 구성입니다(허용 목록, 기본값).',
  isOptional: true,
},{
  name: 'autoSync',
  type: 'boolean',
  description: '파일 시스템과 Sandbox 간의 자동 동기화입니다.',
  isOptional: true,
  defaultValue: 'false',
},{
  name: 'operationTimeout',
  type: 'number',
  description: '개별 작업의 제한 시간(밀리초)입니다.',
  isOptional: true,
},
]}
/>
## 수분 공급 {/* #hydration */}

`StorageWorkspaceRef`는 `list()`가 아닌 `mastra.editor.agent.getById()`에서 지연 해석됩니다. Editor는 `MastraEditor.filesystems` / `MastraEditor.sandboxes`에서 파일 시스템 및 Sandbox Provider를 조회하여 런타임 `Workspace`를 구체화합니다. 누락된 Provider는 경고로 표시되며 Workspace는 생략됩니다.
## 관련된 {/* #related */}

* [작업공간](https://agent-builder.mastra.ai/workspace): 개념 및 작업 사례.
* [BuilderAgentDefaults](https://agent-builder.mastra.ai/reference/builder-agent-defaults): 이 유형이 Builder 기본값으로 고정되어 있는 곳입니다.
* [MastraEditor 클래스](/reference/editor/mastra-editor): 파일 시스템 및 샌드박스 공급자를 등록합니다.
* [스토리지브라우저참조](/reference/editor/storage-browser-ref): 브라우저 구성을 위한 형제 참조 유형입니다.
