---
title: "스튜디오 | 파일 기반 Agent"
description: "studio.ts를 사용하여 파일 규칙에 따라 Mastra Studio를 구성합니다."
packages:
  - "@mastra/core"
  - "@mastra/auth-workos"
---

# 사진관

마스트라 구성[Studio](/docs/studio/overview)에서`studio.ts`바로 아래 파일`src/mastra/`. 파일은 기본적으로`StudioConfig`Studio 인증 및 승인을 위한 개체: Studio에 액세스할 수 있는 사람과 인증된 사용자가 수행할 수 있는 작업.

파일 기반 규칙에 대해서는 이 페이지를 사용하십시오. 인증 공급자, 역할 기반 액세스 제어(RBAC) 및 세분화된 권한 부여(FGA)의 경우 다음을 참조하세요.[Studio auth](/docs/studio/auth).

## 빠른 시작 {/* #quickstart */}

로컬 개발에는 `studio.ts`가 필요하지 않습니다. 개발 서버를 시작하고 Studio를 여세요.
```bash npm2yarn
npm run dev
```

로컬 개발 환경에서는 인증 없이 Studio가 열립니다. 공유 또는 프로덕션 배포를 보호할 준비가 되면 `studio.ts`를 추가하세요.
## 인증 추가 {/* #add-authentication */}

사용자가 Studio에 액세스하기 전에 로그인을 요구하려면 인증 공급자를 사용하세요. 이 예제에서는[WorkOS](/docs/server/auth/workos):

```typescript title="src/mastra/studio.ts"
import { MastraAuthWorkos } from '@mastra/auth-workos'
import type { StudioConfig } from '@mastra/core/server'

export default {
  auth: new MastraAuthWorkos(),
} satisfies StudioConfig
```

인증 Provider는 사용자의 신원을 처리합니다. 로그인 후 사용자가 수행할 수 있는 작업을 확인하려면 동일한 구성에 RBAC 또는 FGA를 추가하세요. 전체 예제는 [Studio 인증](/docs/studio/auth)을 확인하세요.
## 코드 우선순위 {/* #precedence-with-code */}

코드로 등록한 Studio 구성이 `studio.ts`보다 우선합니다. 프로젝트 전체에서 하나의 Studio 구성으로 충분하면 `studio.ts`를 사용하세요. 인증이 `src/mastra/index.ts`의 런타임 연결에 따라 달라지면 코드 등록을 사용하세요.