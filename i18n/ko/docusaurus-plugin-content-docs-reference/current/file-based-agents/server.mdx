---
title: "서버 | 파일 기반 Agent"
description: "server.ts를 사용하여 파일 규칙에 따라 Mastra HTTP 서버를 구성합니다."
packages:
  - "@mastra/core"
---

# 섬기는 사람

Mastra는 HTTP를 구성합니다.[server](/docs/server/mastra-server)에서`server.ts`바로 아래 파일`src/mastra/`. 서버는 Agent, Workflow 및 기타 등록된 기본 요소를 REST 엔드포인트로 노출하고 파일은 기본적으로 동일한 항목을 내보냅니다.`ServerConfig`당신이 전달할 모양[`new Mastra()`](/reference/core/mastra-class).

파일 기반 규칙에 대해서는 이 페이지를 사용하십시오. 서버 기능, 미들웨어, 사용자 정의 경로, 생성된 API 문서 및 배포 동작에 대해서는 다음을 참조하세요.[Server overview](/docs/server/mastra-server).

## 빠른 시작 {/* #quickstart */}

`src/mastra/server.ts`를 만들고 `ServerConfig` 객체를 기본 내보내기하세요.
```typescript title="src/mastra/server.ts"
import type { ServerConfig } from '@mastra/core/server'

export default {
  port: 4111,
  host: '0.0.0.0',
} satisfies ServerConfig
```

## 일반적인 구성 {/* #common-configurations */}

`server.ts`코드 등록과 동일한 서버 옵션을 구성할 수 있습니다.

| 요구 사항 | 사용 항목 |
| - | - |
| 배포 플랫폼 주소에 바인딩 | `host` 및 `port` |
| 브라우저 접근 제한 | `cors` |
| 웹훅 또는 상태 확인 추가 | `apiRoutes` |
| 생성된 API 문서 활성화 | `build.openAPIDocs` 및 `build.swaggerUI` |
예제는 [Mastra 서버](/docs/server/mastra-server), [미들웨어](/docs/server/middleware), [사용자 지정 API 라우트](/docs/server/custom-api-routes)를 참조하세요.
## 코드 우선순위 {/* #precedence-with-code */}

코드로 등록한 서버 구성이 `server.ts`보다 우선합니다. 파일 기반 서버 구성이 적용될 때 채널 웹훅처럼 시작 과정에서 추가된 라우트는 유지됩니다.