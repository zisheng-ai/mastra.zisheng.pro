---
title: "로거 | 파일 기반 Agent"
description: "logger.ts를 사용하여 파일 규칙에 따라 Mastra 로거를 설정하십시오."
packages:
  - "@mastra/core"
  - "@mastra/loggers"
---

# 나무꾼

Mastra는 프로젝트의[logger](/docs/observability/logging)에서`logger.ts`바로 아래 파일`src/mastra/`. 파일은 기본적으로 내장된 로거를 대체하는 로거를 내보냅니다.`ConsoleLogger`Agent, Workflow 및 기타 구성 요소 전반에 걸쳐 사용됩니다.

파일 기반 규칙에 대해서는 이 페이지를 사용하십시오. 로그 수준, 전송 및 공급자 세부정보는 다음을 참조하세요.[logging](/docs/observability/logging).

## 빠른 시작 {/* #quickstart */}

만들다`src/mastra/logger.ts` and default-export a logger, such as [`PinoLogger`](/reference/logging/pino-logger):

```typescript title="src/mastra/logger.ts"
import { PinoLogger } from '@mastra/loggers'

export default new PinoLogger({
  name: 'Mastra',
  level: 'info',
})
```

Mastra는 저장, Observability 및 파일 기반 Agent 전에 로거를 등록하므로 해당 기본 요소는 연결된 대로 이 로거를 통해 기록합니다.

## 코드 우선순위 {/* #precedence-with-code */}

코드 등록 로깅이 승리합니다`logger.ts`. If you pass `logger` to `new Mastra({ logger })` in `src/mastra/index.ts` (or set `logger: false` to disable logging), `logger.ts` is ignored with a warning. Use `logger.ts` when one project-wide logger is enough. Use code registration when setup depends on runtime wiring.
