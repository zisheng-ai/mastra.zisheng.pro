---
title: "참조: CloudflareDeployer | 배포자"
description: "Cloudflare Workers에 Mastra 애플리케이션을 배포하는 CloudflareDeployer 클래스에 대한 문서입니다."
packages:
  - "@mastra/deployer-cloudflare"
---

# Cloudflare배포자

그만큼`CloudflareDeployer`Mastra 서버를 번들로 묶어 생성합니다.`wrangler.jsonc`Cloudflare를 준수하는 파일[랭글러 구성](https://developers.cloudflare.com/workers/wrangler/configuration/). Cloudflare는 이를 Cloudflare 작업자로 배포합니다.

## 설치 {/* #installation */}

사용하려면`CloudflareDeployer`, install the `@mastra/deployer-cloudflare` package:

```bash npm2yarn
npm install @mastra/deployer-cloudflare@latest
```

## 사용예 {/* #usage-example */}

수입`CloudflareDeployer` and set it as the deployer in your Mastra configuration:

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { CloudflareDeployer } from '@mastra/deployer-cloudflare'

export const mastra = new Mastra({
  deployer: new CloudflareDeployer({
    name: 'your-project-name',
    routes: [
      {
        pattern: 'example.com/*',
        zone_name: 'example.com',
        custom_domain: true,
      },
    ],
    vars: {
      NODE_ENV: 'production',
    },
    d1_databases: [
      {
        binding: 'DB',
        database_name: 'my-database',
        database_id: 'd1-database-id',
        preview_database_id: 'your-preview-database-id',
      },
    ],
    kv_namespaces: [
      {
        binding: 'CACHE',
        id: 'kv-namespace-id',
      },
    ],
  }),
})
```

## 생성자 옵션 {/* #constructor-options */}

그만큼`CloudflareDeployer` constructor accepts the same configuration options as `wrangler.jsonc`. See the [Wrangler configuration documentation](https://developers.cloudflare.com/workers/wrangler/configuration/) for all available options.

## 기미 {/* #secrets */}

귀하의 환경 변수`.env` file aren't written to `wrangler.jsonc`. This prevents secrets from being committed to source control.

당신의`.env` secrets available to your Cloudflare Worker, upload them as [Cloudflare Secrets](https://developers.cloudflare.com/workers/configuration/secrets/):

```bash
npx wrangler secret bulk .env
```

사용`vars` in the `CloudflareDeployer` constructor only for non-sensitive configuration values like `NODE_ENV`.

## 빌드 출력 {/* #build-output */}

달리고 난 후`mastra build`, the deployer generates a `wrangler.jsonc` file conforming to Cloudflare's [wrangler configuration](https://developers.cloudflare.com/workers/wrangler/configuration/). It points to files inside `.mastra/output` so you need to run `mastra build` before deploying with Wrangler.

## Cloudflare 바인딩 {/* #cloudflare-bindings */}

Cloudflare 배포자를 사용하면 다음에서 런타임 바인딩을 가져올 수 있습니다.`cloudflare:workers` in your Mastra config file. Mastra automatically preserves protocol-based runtime imports like `cloudflare:workers` during `mastra build` without trying to install them as npm dependencies.

```typescript title="src/mastra/index.ts"
import { env } from 'cloudflare:workers'
import { Mastra } from '@mastra/core'
import { registerApiRoute } from '@mastra/core/server'
import { CloudflareDeployer } from '@mastra/deployer-cloudflare'

export const mastra = new Mastra({
  deployer: new CloudflareDeployer({
    name: 'my-worker',
    kv_namespaces: [{ binding: 'CACHE', id: 'your-kv-namespace-id' }],
  }),
  server: {
    apiRoutes: [
      registerApiRoute('/bindings', {
        method: 'GET',
        requiresAuth: false,
        handler: async c => {
          await env.CACHE.put('status', 'ok')
          return c.json({ status: await env.CACHE.get('status') })
        },
      }),
    ],
  },
})
```

:::참고\[바인딩을 내부에 인라인으로 유지하세요.`new Mastra({...})`]
Mastra's Babel plugin defers `new Mastra(...)` so that `env` is available when it runs. Code outside that call evaluates at module load before `env` is populated.

```typescript
// ✅ Works — binding is inside new Mastra(), so it's deferred
export const mastra = new Mastra({
  storage: new D1Store({ binding: env.DB }),
})

// ❌ Breaks — env.DB is undefined at module load
const storage = new D1Store({ binding: env.DB })
export const mastra = new Mastra({ storage })
```

:::
