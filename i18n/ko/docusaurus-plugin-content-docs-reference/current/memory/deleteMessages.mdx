---
title: "참조: Memory.deleteMessages() | Memory"
description: "ID별로 여러 메시지를 삭제하는 Mastra의 'Memory.deleteMessages()' 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Memory.삭제메시지()

그만큼`.deleteMessages()`메소드는 ID별로 여러 메시지를 삭제합니다.

## 사용예 {/* #usage-example */}

```typescript
await memory?.deleteMessages(['671ae63f-3a91-4082-a907-fe7de78e10ec'])
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'messageIds',
  type: 'string[]',
  description: '삭제할 메시지 ID 배열',
  isOptional: false,
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'void',
  type: 'Promise<void>',
  description: '모든 메시지가 삭제되면 이행되는 Promise',
},
]}
/>
## 확장된 사용 예 {/* #extended-usage-example */}

```typescript title="src/test-memory.ts"
import { mastra } from './mastra'
import { MastraDBMessage } from '@mastra/core'

const agent = mastra.getAgent('agent')
const memory = await agent.getMemory()

const { messages } = await memory!.recall({ threadId: 'thread-123' })

const messageIds = messages.map((message: MastraDBMessage) => message.id)
await memory?.deleteMessages([...messageIds])
```

## 관련된 {/* #related */}

* [Memory 클래스 참조](/reference/memory/memory-class)
* [상기하다](/reference/memory/recall)
* [Memory 시작하기](/docs/memory/overview)
