---
title: "참조: MastraModelOutput | 스트리밍"
description: "MastraModelOutput에 대한 전체 참조 - Model 출력에 대한 스트리밍 및 약속 기반 액세스를 통해 Agent.stream()이 반환한 스트림 개체입니다."
packages:
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# MastraModel출력

그만큼`MastraModelOutput`클래스는 다음에 의해 반환됩니다.[.개울()](./stream)Model 출력에 대한 스트리밍 및 약속 기반 액세스를 모두 제공합니다. 구조화된 출력 생성, Tool 호출, 추론 및 자세한 사용량 추적을 지원합니다.

```typescript
// MastraModelOutput is returned by agent.stream()
const stream = await agent.stream('Hello world')
```

설정 및 기본 사용법은 다음을 참조하세요.[.stream()](./stream) method documentation.

## 스트리밍 속성 {/* #streaming-properties */}

이러한 속성은 생성된 Model 출력에 대한 실시간 액세스를 제공합니다.

<PropertiesTable
  content={[
{
  name: 'fullStream',
  type: 'ReadableStream<ChunkType<OUTPUT>>',
  description:
    "텍스트, Tool 호출, 추론, 메타데이터 및 제어 청크를 포함한 모든 청크 유형의 전체 스트림입니다. Model 응답의 모든 측면에 세분화된 접근을 제공합니다.",
  properties: [    {
      type: 'ReadableStream',
      parameters: [
        {
          name: 'ChunkType',
          type: 'ChunkType<OUTPUT>',
          description: '스트리밍 중 방출될 수 있는 모든 청크 유형',
        },
      ],
    },
  ],
},{
  name: 'textStream',
  type: 'ReadableStream<string>',
  description:
    '증분 텍스트 콘텐츠만 포함하는 스트림입니다. 모든 메타데이터, Tool 호출 및 제어 청크를 필터링하여 생성 중인 텍스트만 제공합니다.',
},{
  name: 'objectStream',
  type: 'ReadableStream<Partial<OUTPUT>>',
  description:
    "출력 스키마를 사용할 때 점진적으로 갱신되는 구조화 객체의 스트림입니다. 객체가 구성되는 동안 부분 객체를 방출하여 구조화된 데이터 생성을 실시간으로 시각화할 수 있습니다.",
  properties: [    {
      type: 'ReadableStream',
      parameters: [
        {
          name: 'PartialSchemaOutput',
          type: 'Partial<OUTPUT>',
          description: '정의된 스키마와 일치하는 부분적으로 완성된 객체',
        },
      ],
    },
  ],
},{
  name: 'elementStream',
  type: 'ReadableStream<OUTPUT extends (infer T)[] ? T : never>',
  description:
    "출력 스키마가 배열 유형을 정의할 때 개별 배열 요소를 제공하는 스트림입니다. 전체 배열이 완성될 때까지 기다리지 않고 각 요소가 완성되는 즉시 방출합니다.",
},
]}
/>
## 약속 기반 속성 {/* #promise-based-properties */}

이러한 속성은 스트림이 완료된 후 최종 값으로 확인됩니다.

<PropertiesTable
  content={[
{
  name: 'text',
  type: 'Promise<string>',
  description: 'Model이 반환한 전체 연결 텍스트 응답입니다. 텍스트 생성이 완료되면 이행됩니다.',
},{
  name: 'object',
  type: 'Promise<OUTPUT>',
  description:
    '출력 스키마를 사용할 때의 전체 구조화 객체 응답입니다. 이행 전에 스키마에 따라 검증됩니다. 검증에 실패하면 거부됩니다.',
  properties: [    {
      type: 'Promise',
      parameters: [
        {
          name: 'InferSchemaOutput',
          type: 'OUTPUT',
          description: '정확한 스키마 정의와 일치하는 완전한 타입의 객체',
        },
      ],
    },
  ],
},{
  name: 'reasoning',
  type: 'Promise<string>',
  description:
    "추론을 지원하는 Model(예: OpenAI의 o1 시리즈)의 전체 추론 텍스트입니다. 추론 기능이 없는 Model에서는 빈 문자열을 반환합니다.",
},{
  name: 'reasoningText',
  type: 'Promise<string | undefined>',
  description:
    "추론 콘텐츠에 접근하는 대체 방법입니다. 추론을 지원하지 않는 Model에서는 undefined일 수 있으며, 'reasoning'은 빈 문자열을 반환합니다.",
},{
  name: 'toolCalls',
  type: 'Promise<ToolCallChunk[]>',
  description:
    '실행 중 이루어진 모든 Tool 호출 청크의 배열입니다. 각 청크에는 Tool 메타데이터와 실행 세부 정보가 포함됩니다.',
  properties: [    {
      type: 'ToolCallChunk',
      parameters: [
        {
          name: 'type',
          type: "'tool-call'",
          description: '청크 유형 식별자',
        },        {
          name: 'runId',
          type: 'string',
          description: '실행 식별자',
        },        {
          name: 'from',
          type: 'ChunkFrom',
          description: '청크의 출처(AGENT, WORKFLOW 등)',
        },        {
          name: 'payload',
          type: 'ToolCallPayload',
          description: 'toolCallId, toolName, args 및 실행 세부 정보를 포함한 Tool 호출 데이터',
        },
      ],
    },
  ],
},{
  name: 'toolResults',
  type: 'Promise<ToolResultChunk[]>',
  description:
    'Tool 호출에 대응하는 모든 Tool 결과 청크의 배열입니다. 실행 결과와 오류 정보가 포함됩니다.',
  properties: [    {
      type: 'ToolResultChunk',
      parameters: [
        {
          name: 'type',
          type: "'tool-result'",
          description: '청크 유형 식별자',
        },        {
          name: 'runId',
          type: 'string',
          description: '실행 식별자',
        },        {
          name: 'from',
          type: 'ChunkFrom',
          description: '청크의 출처(AGENT, WORKFLOW 등)',
        },        {
          name: 'payload',
          type: 'ToolResultPayload',
          description: 'toolCallId, toolName, result 및 오류 상태를 포함한 Tool 결과 데이터',
        },
      ],
    },
  ],
},{
  name: 'usage',
  type: 'Promise<LanguageModelUsage>',
  description:
    '입력 토큰, 출력 토큰, 총 토큰 및 추론 토큰(추론 Model의 경우)을 포함한 토큰 사용량 통계입니다.',
  properties: [    {
      type: 'Record',
      parameters: [
        {
          name: 'inputTokens',
          type: 'number',
          description: '입력 Prompt에서 소비된 토큰',
        },        {
          name: 'outputTokens',
          type: 'number',
          description: '응답에서 생성된 토큰',
        },        {
          name: 'totalTokens',
          type: 'number',
          description: '입력 토큰과 출력 토큰의 합계',
        },        {
          name: 'reasoningTokens',
          type: 'number',
          isOptional: true,
          description: '숨겨진 추론 토큰(추론 Model의 경우)',
        },        {
          name: 'cachedInputTokens',
          type: 'number',
          isOptional: true,
          description: '캐시에 적중한 입력 토큰 수',
        },
      ],
    },
  ],
},{
  name: 'finishReason',
  type: 'Promise<string | undefined>',
  description:
    "생성이 중지된 이유(예: 'stop', 'length', 'tool_calls', 'content_filter')입니다. 스트림이 완료되지 않았으면 undefined입니다.",
  properties: [    {
      type: 'enum',
      parameters: [
        {
          name: 'stop',
          type: "'stop'",
          description: 'Model이 자연스럽게 완료됨',
        },        {
          name: 'length',
          type: "'length'",
          description: '최대 토큰 한도에 도달함',
        },        {
          name: 'tool_calls',
          type: "'tool_calls'",
          description: 'Model이 Tool을 호출함',
        },        {
          name: 'content_filter',
          type: "'content_filter'",
          description: '콘텐츠가 필터링됨',
        },
      ],
    },
  ],
},{
  name: 'response',
  type: 'Promise<Response>',
  description: 'Model Provider가 반환한 응답 메타데이터와 메시지입니다.',
  properties: [    {
      type: 'Response',
      parameters: [
        {
          name: 'id',
          type: 'string',
          isOptional: true,
          description: 'Model Provider가 반환한 응답 ID',
        },        {
          name: 'timestamp',
          type: 'Date',
          isOptional: true,
          description: '응답 타임스탬프',
        },        {
          name: 'modelId',
          type: 'string',
          isOptional: true,
          description: '이 응답에 사용된 Model 식별자',
        },        {
          name: 'headers',
          type: 'Record<string, string>',
          isOptional: true,
          description: 'Model Provider가 반환한 응답 헤더',
        },        {
          name: 'messages',
          type: 'ResponseMessage[]',
          isOptional: true,
          description: 'Model 형식의 응답 메시지',
        },        {
          name: 'uiMessages',
          type: 'UIMessage[]',
          isOptional: true,
          description: 'UI 형식의 응답 메시지로, 출력 프로세서가 추가한 모든 메타데이터를 포함함',
        },
      ],
    },
  ],
},
]}
/>
## 오류 속성 {/* #error-properties */}

<PropertiesTable
  content={[
{
  name: 'error',
  type: 'string | Error | { message: string; stack: string; } | undefined',
  description:
    '스트림에서 오류가 발생한 경우의 오류 정보입니다. 오류가 발생하지 않았으면 undefined입니다. 문자열 메시지, Error 객체 또는 스택 추적을 포함한 직렬화된 오류일 수 있습니다.',
},
]}
/>
## 행동 양식 {/* #methods */}

<PropertiesTable
  content={[
{
  name: 'getFullOutput',
  type: '() => Promise<FullOutput>',
  description:
    '텍스트, 구조화 객체, Tool 호출, 사용량 통계, 추론 및 메타데이터 등 모든 결과를 포함하는 종합 출력 객체를 반환합니다. 모든 스트림 결과에 접근할 수 있는 편리한 단일 메서드입니다.',
  properties: [    {
      type: 'FullOutput',
      parameters: [
        {
          name: 'text',
          type: 'string',
          description: '전체 텍스트 응답',
        },        {
          name: 'object',
          type: 'OUTPUT',
          isOptional: true,
          description: '스키마가 제공된 경우 구조화된 출력',
        },        {
          name: 'toolCalls',
          type: 'ToolCallChunk[]',
          description: '이루어진 모든 Tool 호출 청크',
        },        {
          name: 'toolResults',
          type: 'ToolResultChunk[]',
          description: '모든 Tool 결과 청크',
        },        {
          name: 'usage',
          type: 'Record<string, number>',
          description: '토큰 사용량 통계',
        },        {
          name: 'reasoning',
          type: 'string',
          isOptional: true,
          description: '사용 가능한 경우 추론 텍스트',
        },        {
          name: 'finishReason',
          type: 'string',
          isOptional: true,
          description: '생성이 완료된 이유',
        },        {
          name: 'response',
          type: 'Response',
          description: 'Model Provider가 반환한 응답 메타데이터와 메시지',
        },
      ],
    },
  ],
},{
  name: 'consumeStream',
  type: '(options?: ConsumeStreamOptions) => Promise<void>',
  description:
    '청크를 처리하지 않고 전체 스트림을 수동으로 소비합니다. 최종 Promise 기반 결과만 필요하며 스트림 소비를 트리거하려는 경우 유용합니다.',
  properties: [    {
      type: 'ConsumeStreamOptions',
      parameters: [
        {
          name: 'onError',
          type: '(error: Error) => void',
          isOptional: true,
          description: '스트림 오류를 처리하는 콜백',
        },
      ],
    },
  ],
},
]}
/>
## 사용 예 {/* #usage-examples */}

### 기본 텍스트 스트리밍 {/* #basic-text-streaming */}

```typescript
const stream = await agent.stream('Write a haiku')

// Stream text as it's generated
for await (const text of stream.textStream) {
  process.stdout.write(text)
}

// Or get the complete text
const fullText = await stream.text
console.log(fullText)
```

### 구조화된 출력 스트리밍 {/* #structured-output-streaming */}

```typescript
const stream = await agent.stream('Generate user data', {
  structuredOutput: {
    schema: z.object({
      name: z.string(),
      age: z.number(),
      email: z.string(),
    }),
  },
})

// Stream partial objects
for await (const partial of stream.objectStream) {
  console.log('Progress:', partial) // { name: "John" }, { name: "John", age: 30 }, ...
}

// Get final validated object
const user = await stream.object
console.log('Final:', user) // { name: "John", age: 30, email: "john@example.com" }
```

````

### Tool Calls and Results

```typescript
const stream = await agent.stream("What's the weather in NYC?", {
  tools: { weather: weatherTool }
});
// Monitor tool calls
const toolCalls = await stream.toolCalls;
const toolResults = await stream.toolResults;
console.log("Tools called:", toolCalls);
console.log("Results:", toolResults);````

### 완전한 출력 액세스 {/* #complete-output-access */}

```typescript
const stream = await agent.stream('Analyze this data')
const output = await stream.getFullOutput()
console.log({
  text: output.text,
  usage: output.usage,
  reasoning: output.reasoning,
  finishReason: output.finishReason,
})```

### 전체 스트림 처리 {/* #full-stream-processing */}

```typescript
const stream = await agent.stream('Complex task')
for await (const chunk of stream.fullStream) {
  switch (chunk.type) {
    case 'text-delta':
      process.stdout.write(chunk.payload.text)
      break
    case 'tool-call':
      console.log(`Calling ${chunk.payload.toolName}...`)
      break
    case 'reasoning-delta':
      console.log(`Reasoning: ${chunk.payload.text}`)
      break
    case 'finish':
      console.log(`Done! Reason: ${chunk.payload.stepResult.reason}`)      // Access response messages with any metadata added by output processors
      const uiMessages = chunk.payload.response?.uiMessages
      if (uiMessages) {
        console.log('Response messages:', uiMessages)
      }
      break
  }
}```

### 오류 처리 {/* #error-handling */}

```typescript
const stream = await agent.stream('Analyze this data')
try {
  // Option 1: Handle errors in consumeStream
  await stream.consumeStream({
    onError: error => {
      console.error('Stream error:', error)
    },
  })
  const result = await stream.text
} catch (error) {
  console.error('Failed to get result:', error)
}
// Option 2: Check error property
const result = await stream.getFullOutput()
if (stream.error) {
  console.error('Stream had errors:', stream.error)
}```

## 관련 유형 {/* #related-types */}

* [.개울()](./stream): MastraModelOutput을 반환하는 메서드
* [청크 유형](../ChunkType): 전체 스트림에서 가능한 모든 청크 유형
