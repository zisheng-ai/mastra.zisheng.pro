---
title: "참조: Run.resumeStream() | 스트리밍"
description: "일시 중단된 Workflow 실행을 실시간으로 재개하고 스트리밍할 수 있는 Workflow의 'Run.resumeStream()' 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 실행.이력스트림()

그만큼`.resumeStream()`메서드는 새 데이터로 일시 중단된 Workflow 실행을 재개하므로 특정 단계부터 실행을 계속하고 이벤트 스트림을 관찰할 수 있습니다.

## 사용예 {/* #usage-example */}

```typescript
const run = await workflow.createRun()

const stream = run.stream({
  inputData: {
    value: 'initial data',
  },
})

const result = await stream.result

if (result!.status === 'suspended') {
  const resumedStream = await run.resumeStream({
    resumeData: {
      value: 'resume data',
    },
  })
}
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'resumeData',
  type: 'z.infer<TInput>',
  description: "Workflow의 입력 스키마와 일치하는 입력 데이터",
  isOptional: true,
},{
  name: 'requestContext',
  type: 'RequestContext',
  description: 'Workflow 실행 중 사용할 요청 컨텍스트 데이터',
  isOptional: true,
},{
  name: 'step',
  type: 'Step<string, any, any, any, any, TEngineType>',
  description: '실행을 재개할 단계',
  isOptional: true,
},{
  name: 'forEachIndex',
  type: 'number',
  description:
    '일시 중단된 `.foreach()` 단계의 특정 반복을 대상으로 지정합니다. 재개할 반복의 0부터 시작하는 인덱스를 전달하면 다른 반복은 일시 중단된 상태로 유지됩니다. 생략하면 동일한 `resumeData`를 사용하여 해당 단계에서 일시 중단된 모든 반복을 재개합니다.',
  isOptional: true,
},{
  name: 'tracingOptions',
  type: 'TracingOptions',
  isOptional: true,
  description: 'Tracing 구성 옵션입니다.',
  properties: [    {
      parameters: [
        {
          name: 'metadata',
          type: 'Record<string, any>',
          isOptional: true,
          description:
            '루트 Trace 스팬에 추가할 메타데이터입니다. 사용자 ID, 세션 ID 또는 기능 플래그와 같은 사용자 정의 속성을 추가할 때 유용합니다.',
        },
      ],
    },    {
      parameters: [
        {
          name: 'requestContextKeys',
          type: 'string[]',
          isOptional: true,
          description:
            "이 Trace의 메타데이터로 추출할 추가 RequestContext 키입니다. 중첩 값에 점 표기법(예: 'user.id')을 지원합니다.",
        },
      ],
    },    {
      parameters: [
        {
          name: 'traceId',
          type: 'string',
          isOptional: true,
          description:
            '이 실행에 사용할 Trace ID(1~32자의 16진수)입니다. 제공하면 이 Trace가 지정된 Trace의 일부가 됩니다.',
        },
      ],
    },    {
      parameters: [
        {
          name: 'parentSpanId',
          type: 'string',
          isOptional: true,
          description:
            '이 실행에 사용할 상위 스팬 ID(1~16자의 16진수)입니다. 제공하면 루트 스팬이 이 스팬의 하위로 생성됩니다.',
        },
      ],
    },    {
      parameters: [
        {
          name: 'tags',
          type: 'string[]',
          isOptional: true,
          description: '이 Trace에 적용할 태그입니다. Trace를 분류하고 필터링하기 위한 문자열 레이블입니다.',
        },
      ],
    },
  ],
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'stream',
  type: 'MastraWorkflowStream<ChunkType>',
  description: '추가적인 Workflow 전용 속성으로 ReadableStream<ChunkType>을 확장한 사용자 정의 스트림입니다.',
},{
  name: 'stream.status',
  type: 'Promise<RunStatus>',
  description: '현재 Workflow 실행 상태로 이행되는 Promise입니다.',
},{
  name: 'stream.result',
  type: 'Promise<WorkflowResult<TState, TOutput, TSteps>>',
  description: '최종 Workflow 결과로 이행되는 Promise입니다.',
},{
  name: 'stream.usage',
  type: 'Promise<{ inputTokens: number; outputTokens: number; totalTokens: number, reasoningTokens?: number, cacheInputTokens?: number }>',
  description: '토큰 사용량 통계로 이행되는 Promise입니다.',
},
]}
/>
## 이벤트 스트리밍 {/* #stream-events */}

스트림은 Workflow 실행 중 이벤트 유형을 방출합니다. 각 이벤트에는 `type` 필드와 관련 데이터가 담긴 `payload`가 있습니다.
* **`workflow-start`**: Workflow 실행이 시작됩니다.
* **`workflow-step-start`**: 단계 실행이 시작됩니다.
* **`workflow-step-output`**: 단계의 사용자 정의 출력입니다.
* **`workflow-step-result`**: 단계가 결과와 함께 완료됩니다.
* **`workflow-finish`**: 사용량 통계와 함께 Workflow 실행이 완료됩니다. 성공적으로 실행된 경우 `payload.finalWorkflowResult`에 Workflow의 최종 결과가 담깁니다.
## 관련된 {/* #related */}

* [Workflow 개요](/docs/workflows/overview#running-workflows)
* [Workflow.createRun()](/reference/workflows/workflow-methods/create-run)
* [실행.스트림()](./stream)
