---
title: "참조: 구성 | Observability"
description: "구성 유형 및 레지스트리 기능 추적"
packages:
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# 구성

## `ObservabilityRegistryConfig` {/* #observabilityregistryconfig */}

```typescript
interface ObservabilityRegistryConfig {
  default?: { enabled?: boolean }
  configs?: Record<string, Omit<ObservabilityInstanceConfig, 'name'> | ObservabilityInstance>
  configSelector?: ConfigSelector
  sensitiveDataFilter?: boolean | SensitiveDataFilterOptions
}
```

<PropertiesTable
  content={[
{
  name: 'default',
  type: '{ enabled?: boolean }',
  description: 'Enable default configuration with MastraStorageExporter and MastraPlatformExporter',
  required: false,
},
{
  name: 'configs',
  type: "Record<string, Omit<ObservabilityInstanceConfig, 'name'> | ObservabilityInstance>",
  description: 'Named observability instance configurations or pre-instantiated instances',
  required: false,
},
{
  name: 'configSelector',
  type: 'ConfigSelector',
  description: 'Runtime configuration selector function',
  required: false,
},
{
  name: 'sensitiveDataFilter',
  type: 'boolean | SensitiveDataFilterOptions',
  description:
    'Controls whether a [SensitiveDataFilter](/reference/observability/tracing/processors/sensitive-data-filter) is auto-applied to every configured instance. Defaults to `true`. Pass `false` to opt out, or pass a `SensitiveDataFilterOptions` object to customize fields, redaction token, or redaction style. If a config already includes a `SensitiveDataFilter` in `spanOutputProcessors`, it is not duplicated. Pre-instantiated instances are not modified.',
  required: false,
},
]}
/>

## `ObservabilityInstanceConfig` {/* #observabilityinstanceconfig */}

```typescript
interface ObservabilityInstanceConfig {
  name: string
  serviceName: string
  sampling?: SamplingStrategy
  exporters?: ObservabilityExporter[]
  spanOutputProcessors?: SpanOutputProcessor[]
  includeInternalSpans?: boolean
  excludeSpanTypes?: SpanType[]
  spanFilter?: (span: AnyExportedSpan) => boolean
  requestContextKeys?: string[]
  serializationOptions?: SerializationOptions
  logging?: { enabled?: boolean; level?: LogLevel }
}
```

<PropertiesTable
  content={[
{
  name: 'name',
  type: 'string',
  description: 'Configuration identifier',
  required: true,
},
{
  name: 'serviceName',
  type: 'string',
  description: 'Service name in traces',
  required: true,
},
{
  name: 'sampling',
  type: 'SamplingStrategy',
  description: 'Sampling configuration (defaults to ALWAYS)',
  required: false,
},
{
  name: 'exporters',
  type: 'ObservabilityExporter[]',
  description: 'Trace data exporters',
  required: false,
},
{
  name: 'spanOutputProcessors',
  type: 'SpanOutputProcessor[]',
  description: 'Span output processors',
  required: false,
},
{
  name: 'includeInternalSpans',
  type: 'boolean',
  description: 'Include spans internal to Mastra operations',
  required: false,
},
{
  name: 'excludeSpanTypes',
  type: 'SpanType[]',
  description:
    'Span types to exclude from export. Useful for reducing noise and costs in per-span billing platforms. See the [Span filtering reference](/reference/observability/tracing/span-filtering) for details.',
  required: false,
},
{
  name: 'spanFilter',
  type: '(span: AnyExportedSpan) => boolean',
  description:
    'Filter function to control which spans are exported. Return true to keep, false to drop. Runs after excludeSpanTypes and spanOutputProcessors. See the [Span filtering reference](/reference/observability/tracing/span-filtering) for details.',
  required: false,
},
{
  name: 'requestContextKeys',
  type: 'string[]',
  description: 'RequestContext keys to extract as metadata (supports dot notation)',
  required: false,
},
{
  name: 'serializationOptions',
  type: 'SerializationOptions',
  description: 'Options for controlling serialization of span data (input/output/attributes)',
  required: false,
  properties: [
    {
      type: 'SerializationOptions',
      parameters: [
        {
          name: 'maxStringLength',
          type: 'number',
          description: 'Maximum length for string values (default: 1024)',
          isOptional: true,
        },
        {
          name: 'maxDepth',
          type: 'number',
          description: 'Maximum depth for nested objects (default: 6)',
          isOptional: true,
        },
        {
          name: 'maxArrayLength',
          type: 'number',
          description: 'Maximum number of items in arrays (default: 50)',
          isOptional: true,
        },
        {
          name: 'maxObjectKeys',
          type: 'number',
          description: 'Maximum number of keys in objects (default: 50)',
          isOptional: true,
        },
      ],
    },
  ],
},
{
  name: 'logging',
  type: '{ enabled?: boolean; level?: LogLevel }',
  description:
    'Controls log forwarding to observability storage. See [Logging to observability storage](/docs/observability/logging#logging-to-observability-storage) for details.',
  required: false,
  properties: [
    {
      type: '{ enabled?: boolean; level?: LogLevel }',
      parameters: [
        {
          name: 'enabled',
          type: 'boolean',
          description: 'Set to false to disable log forwarding to observability storage (default: true)',
          isOptional: true,
        },
        {
          name: 'level',
          type: 'LogLevel',
          description:
            "Minimum log level to forward. One of 'debug', 'info', 'warn', 'error', 'fatal' (default: 'debug')",
          isOptional: true,
        },
      ],
    },
  ],
},
]}
/>

## `SamplingStrategy` {/* #samplingstrategy */}

```typescript
type SamplingStrategy =
  | { type: 'always' }
  | { type: 'never' }
  | { type: 'ratio'; probability: number }
  | { type: 'custom'; sampler: (options?: TracingOptions) => boolean }
```

## `ConfigSelector` {/* #configselector */}

```typescript
type ConfigSelector = (
  options: ConfigSelectorOptions,
  availableConfigs: ReadonlyMap<string, ObservabilityInstance>,
) => string | undefined
```

## `ConfigSelectorOptions` {/* #configselectoroptions */}

```typescript
interface ConfigSelectorOptions {
  requestContext?: RequestContext
}
```

## 레지스트리 방법 {/* #registry-methods */}

Observability 클래스는 Observability 인스턴스를 관리하기 위한 메서드를 제공합니다.

### `registerInstance` {/* #registerinstance */}

```typescript prettier:false
registerInstance(
  name: string,
  instance: ObservabilityInstance,
  isDefault?: boolean
): void;
```

관측 가능성 인스턴스를 레지스트리에 등록합니다.

### `getInstance` {/* #getinstance */}

```typescript prettier:false
getInstance(name: string): ObservabilityInstance | undefined;
```

이름으로 Observability 인스턴스를 검색합니다.

### `getDefaultInstance` {/* #getdefaultinstance */}

```typescript prettier:false
getDefaultInstance(): ObservabilityInstance | undefined;
```

기본 관측 가능성 인스턴스를 반환합니다.

### `getSelectedInstance` {/* #getselectedinstance */}

```typescript prettier:false
getSelectedInstance(
  options: ConfigSelectorOptions
): ObservabilityInstance | undefined;
```

구성 선택기 또는 기본값으로 선택한 관측 가능성 인스턴스를 반환합니다.

### `listInstances` {/* #listinstances */}

```typescript prettier:false
listInstances(): ReadonlyMap<string, ObservabilityInstance>;
```

등록된 모든 관측 가능성 인스턴스를 반환합니다.

### `hasInstance` {/* #hasinstance */}

```typescript prettier:false
hasInstance(name: string): boolean;
```

관측 가능성 인스턴스가 존재하는지 확인합니다.

### `setConfigSelector` {/* #setconfigselector */}

```typescript prettier:false
setConfigSelector(selector: ConfigSelector): void;
```

구성 선택기 기능을 설정합니다.

### `unregisterInstance` {/* #unregisterinstance */}

```typescript prettier:false
unregisterInstance(name: string): boolean;
```

레지스트리에서 Observability 인스턴스를 제거합니다.

### 분명한 {/* #clear */}

```typescript prettier:false
clear(): void;
```

종료하지 않고 모든 인스턴스를 지웁니다.

### 일시 휴업 {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>;
```

모든 Observability 인스턴스를 종료하고 레지스트리를 지웁니다.

## 또한보십시오 {/* #see-also */}

### 선적 서류 비치 {/* #documentation */}

* [추적 개요](/docs/observability/tracing/overview): 개념 및 사용 가이드
* [샘플링 전략](/docs/observability/tracing/overview#sampling-strategies): 샘플링 구성 세부정보
* [다중 구성 설정](/docs/observability/overview#multi-config-setup): 여러 구성 사용

### 참조 {/* #reference */}

* [추적 클래스](/reference/observability/tracing/instances): 핵심 추적 클래스
* [인터페이스](/reference/observability/tracing/interfaces): 유형 정의
* [스팬 참조](/reference/observability/tracing/spans): 스팬 수명주기

### 수출업체 {/* #exporters */}

* [MastraStorage수출업체](/reference/observability/tracing/exporters/mastra-storage-exporter): Mastra Storage에 대한 관측 가능성 이벤트를 지속합니다.
* [MastraPlatform수출자](/reference/observability/tracing/exporters/mastra-platform-exporter): 관측 가능성 이벤트를 Mastra 플랫폼으로 보냅니다.
* [브레인트러스트](/reference/observability/tracing/exporters/braintrust): Braintrust 통합
* [랭퓨즈](/reference/observability/tracing/exporters/langfuse): Langfuse 통합
* [랭스미스](/reference/observability/tracing/exporters/langsmith): LangSmith 통합
* [스팬 필터링](/reference/observability/tracing/span-filtering): 범위를 선택적으로 제외
