---
title: "참조: 범위 필터링 | Observability"
description: "excludeSpanTypes,spanFilter 및 범위 필터링 동작에 대한 참조"
packages:
  - "@mastra/core"
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# 스팬 필터링

범위 필터링은 관측 가능성 구성에서 내보내는 범위를 제어합니다. 이를 사용하여 소음을 줄이거나 스팬당 비용을 낮춥니다. 또한 특정 내보내기 또는 환경에 중요한 범위만 유지할 수도 있습니다.

추적 구성에 대한 간략한 개요는 다음을 참조하세요.[Tracing](/docs/observability/tracing/overview).

## 사용예 {/* #usage-example */}

다음 예에서는 결합 방법을 보여줍니다.`excludeSpanTypes` and `spanFilter` in one observability config:

```ts title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { SpanType } from '@mastra/core/observability'
import { Observability, MastraStorageExporter } from '@mastra/observability'
import { LangfuseExporter } from '@mastra/langfuse'

export const mastra = new Mastra({
  observability: new Observability({
    configs: {
      default: {
        serviceName: 'my-app',
        exporters: [new MastraStorageExporter(), new LangfuseExporter()],
        excludeSpanTypes: [SpanType.MODEL_CHUNK, SpanType.MODEL_STEP],
        spanFilter: span => {
          if (span.type === SpanType.TOOL_CALL && span.attributes?.success) {
            return false
          }

          return true
        },
      },
    },
  }),
})
```

## 구성 옵션 {/* #configuration-options */}

<PropertiesTable
  content={[
{
  name: 'excludeSpanTypes',
  type: 'SpanType[]',
  description:
    'Drops spans by type before processors and spanFilter run. Use this for broad, low-overhead filtering.',
  isOptional: true,
},
{
  name: 'spanFilter',
  type: '(span: AnyExportedSpan) => boolean',
  description:
    'Receives the exported span and returns true to keep it or false to drop it. Use this when filtering depends on attributes, metadata, timing, or entity identifiers.',
  isOptional: true,
},
]}
/>

### `excludeSpanTypes` {/* #excludespantypes */}

사용`excludeSpanTypes` when you want to suppress whole categories of spans with minimal configuration.

현재 지원되는 값 세트는 다음을 참조하세요.[`SpanType` enum](https://github.com/mastra-ai/mastra/blob/main/packages/core/src/observability/types/tracing.ts).

```ts title="src/mastra/index.ts"
import { SpanType } from '@mastra/core/observability'

excludeSpanTypes: [SpanType.MODEL_CHUNK, SpanType.MODEL_STEP, SpanType.WORKFLOW_SLEEP]
```

### `spanFilter` {/* #spanfilter */}

필터링이 내보낸 스팬 콘텐츠에 따라 달라지는 경우 다음을 사용하세요.`spanFilter`.

```ts title="src/mastra/index.ts"
import { SpanType } from '@mastra/core/observability'

spanFilter: span => {
  if (span.type === SpanType.MODEL_CHUNK) return false
  if (span.type === SpanType.TOOL_CALL && span.attributes?.success) return false

  return true
}
```

추가 예:

```ts title="src/mastra/index.ts"
// Only export spans from production
spanFilter: span => span.metadata?.environment === 'production'

// Exclude spans from a specific entity
spanFilter: span => span.entityId !== 'noisy-agent'
```

## 처리순서 {/* #processing-order */}

범위 필터링은 내보내기 시 다음 순서로 발생합니다.

1. 다음을 제외하면 내부 스팬은 삭제됩니다.`includeInternalSpans` is `true`.
2. `excludeSpanTypes`일치하는 범위 유형을 제거합니다.
3. `spanOutputProcessors`나머지 범위를 변환합니다.
4. `spanFilter`최종 내보낸 범위를 유지할지 여부를 결정합니다.

만약에`spanFilter` throws an error, Mastra keeps the span and logs the error to avoid silent data loss.

## 관련된 {/* #related */}

* [추적 개요](/docs/observability/tracing/overview)설정과 컨셉을 위한
* [구성 API](/reference/observability/tracing/configuration)~을 위한`ObservabilityInstanceConfig`
* [민감한 데이터 필터](/docs/observability/integrations/processors/sensitive-data-filter)범위 데이터 수정용
