---
title: "참조: DeepEvalExporter | Observability"
description: "추적을 위한 자신감 있는 AI 내보내기"
packages:
  - "@mastra/deepeval"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# DeepEvalExporter

추적 데이터를 다음으로 보냅니다.[Confident AI](https://www.confident-ai.com/)평가 및 Observability을 위해. 수출자`@mastra/deepeval`, DeepEval SDK를 기반으로 구축되었습니다.

설정 및 사용법은 [Confident AI exporter](/docs/observability/integrations/exporters/confident-ai)를 참조하세요.
## 건설자 {/* #constructor */}

```typescript prettier:false
new DeepEvalExporter(config?: DeepEvalExporterConfig)
```

## `DeepEvalExporterConfig` {/* #deepevalexporterconfig */}

```typescript
interface DeepEvalExporterConfig extends BaseExporterConfig {
  apiKey?: string
  environment?: string
  name?: string
  tags?: string[]
  metadata?: Record<string, any>
  threadId?: string
  userId?: string
  testCaseId?: string
  turnId?: string
  metricCollection?: string
  traceMetricCollection?: string
  llmMetricCollection?: string
  agentMetricCollection?: string
  toolMetricCollectionMap?: Record<string, string>
  prompt?: Prompt
  debug?: boolean
  traceCaptureSink?: (trace: Trace) => void
}
```

`BaseExporterConfig`를 확장하며, 포함되는 항목은 다음과 같습니다.
* `logger?: IMastraLogger`- 로거 인스턴스
* `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'`- 로그 수준(기본값: INFO)

<PropertiesTable
  content={[
{
  name: 'apiKey',
  type: 'string',
  description:
    'Confident AI API 키입니다. 지정하지 않으면 CONFIDENT_API_KEY 환경 변수를 사용합니다. 키를 찾을 수 없으면 exporter가 자체적으로 비활성화됩니다.',
  isOptional: true,
},{
  name: 'environment',
  type: 'string',
  description: "Trace 환경입니다. 지정하지 않으면 CONFIDENT_TRACE_ENVIRONMENT를 사용하고, 그마저 없으면 'development'를 사용합니다.",
  isOptional: true,
},{
  name: 'name',
  type: 'string',
  description: '모든 Trace에 적용할 이름입니다. 기본값은 Mastra serviceName입니다.',
  isOptional: true,
},{
  name: 'tags',
  type: 'string[]',
  description: '모든 Trace에 적용할 태그입니다.',
  isOptional: true,
},{
  name: 'metadata',
  type: 'Record<string, any>',
  description: '모든 Trace에 적용할 메타데이터입니다.',
  isOptional: true,
},{
  name: 'threadId',
  type: 'string',
  description: '모든 Trace에 적용할 스레드 ID입니다. 루트 span 메타데이터의 threadId가 요청별로 이 값을 재정의합니다.',
  isOptional: true,
},{
  name: 'userId',
  type: 'string',
  description: '모든 Trace에 적용할 사용자 ID입니다. 루트 span 메타데이터의 userId가 요청별로 이 값을 재정의합니다.',
  isOptional: true,
},{
  name: 'testCaseId',
  type: 'string',
  description:
    '모든 Trace에 적용할 테스트 사례 ID입니다. 루트 span 메타데이터의 testCaseId가 요청별로 이 값을 재정의합니다.',
  isOptional: true,
},{
  name: 'turnId',
  type: 'string',
  description: '모든 Trace에 적용할 턴 ID입니다. 루트 span 메타데이터의 turnId가 요청별로 이 값을 재정의합니다.',
  isOptional: true,
},{
  name: 'metricCollection',
  type: 'string',
  description: 'Trace 수준 메트릭 컬렉션입니다.',
  isOptional: true,
},{
  name: 'traceMetricCollection',
  type: 'string',
  description: 'Trace 수준 메트릭 컬렉션입니다. metricCollection보다 우선합니다.',
  isOptional: true,
},{
  name: 'llmMetricCollection',
  type: 'string',
  description: 'LLM span에 적용할 메트릭 컬렉션입니다.',
  isOptional: true,
},{
  name: 'agentMetricCollection',
  type: 'string',
  description: 'Agent span에 적용할 메트릭 컬렉션입니다.',
  isOptional: true,
},{
  name: 'toolMetricCollectionMap',
  type: 'Record<string, string>',
  description: 'Tool 이름별로 적용할 메트릭 컬렉션입니다.',
  isOptional: true,
},{
  name: 'prompt',
  type: 'Prompt',
  description: 'LLM span에 연결할 Confident AI 관리형 Prompt 링크입니다.',
  isOptional: true,
},{
  name: 'debug',
  type: 'boolean',
  description: 'exporter 구성 및 내보내기 오류를 콘솔에 기록합니다.',
  isOptional: true,
},{
  name: 'traceCaptureSink',
  type: '(trace: Trace) => void',
  description:
    '완료된 Trace를 Confident AI에 게시하지 않고 로컬에서 수신합니다. 오프라인 평가에 사용하세요.',
  isOptional: true,
},{
  name: 'logLevel',
  type: "'debug' | 'info' | 'warn' | 'error'",
  description: "로거 레벨(기본값: 'info')",
  isOptional: true,
},
]}
/>
## 행동 양식 {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

추적 이벤트를 Confident AI로 내보냅니다. Mastra 범위 스트림에서 추적 트리를 구축하고 루트 범위가 끝나면 추적을 게시합니다.

### `flush` {/* #flush */}

```typescript prettier:false
async flush(): Promise<void>
```

내보내기를 종료하지 않고 진행 중인 추적 게시물이 정착될 때까지 기다립니다.

### `shutdown` {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

열려 있는 모든 추적을 마무리하고 보류 중인 게시물을 플러시합니다.

## 용법 {/* #usage */}

### Zero-Config(환경 변수 사용) {/* #zero-config-using-environment-variables */}

```typescript
import { DeepEvalExporter } from '@mastra/deepeval'

// Reads from CONFIDENT_API_KEY, CONFIDENT_TRACE_ENVIRONMENT
const exporter = new DeepEvalExporter()
```

### 명시적 구성 {/* #explicit-configuration */}

```typescript
import { DeepEvalExporter } from '@mastra/deepeval'

const exporter = new DeepEvalExporter({
  apiKey: process.env.CONFIDENT_API_KEY,
  environment: 'production',
})
```

## 스팬 유형 매핑 {/* #span-type-mapping */}

| 마스트라 스팬 유형 | 자신 있는 AI 스팬 유형 |
| - | - |
|`AGENT_RUN`, `WORKFLOW_RUN` | `AGENT` |
| `MODEL_GENERATION` | `LLM` |
| `TOOL_CALL`, `MCP_TOOL_CALL`, `PROVIDER_TOOL_CALL`, `CLIENT_TOOL_CALL` | `TOOL` |
| `RAG_EMBEDDING`, `RAG_VECTOR_OPERATION` | `RETRIEVER`|
| 내보낸 기타 모든 범위 유형 |`CUSTOM` |

이벤트 span(예: `MODEL_CHUNK`)은 삭제됩니다.