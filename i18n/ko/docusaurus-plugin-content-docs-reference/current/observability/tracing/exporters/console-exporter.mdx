---
title: "참조: ConsoleExporter | Observability"
description: "ConsoleExporter에 대한 API 참조"
packages:
  - "@mastra/core"
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# 콘솔 내보내기

디버깅 및 개발을 위해 추적 이벤트를 콘솔에 출력합니다.

## 건설자 {/* #constructor */}

```typescript prettier:false
new ConsoleExporter(config?: BaseExporterConfig)
```

<PropertiesTable
  content={[
{
  name: 'config',
  type: 'BaseExporterConfig',
  description: '구성 옵션',
  isOptional: true,
  properties: [    {
      type: 'BaseExporterConfig',
      parameters: [
        {
          name: 'logger',
          type: 'IMastraLogger',
          description: '로거 인스턴스(지정하지 않으면 INFO 레벨의 ConsoleLogger 사용)',
          isOptional: true,
        },        {
          name: 'logLevel',
          type: "LogLevel | 'debug' | 'info' | 'warn' | 'error'",
          description: 'exporter의 로그 레벨(기본값: INFO)',
          isOptional: true,
        },
      ],
    },
  ],
},
]}
/>
## 속성 {/* #properties */}

```typescript prettier:false
readonly name = 'tracing-console-exporter';
```

## 행동 양식 {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

추적 이벤트를 콘솔로 내보냅니다.

<PropertiesTable
  props={[
{
  name: 'event',
  type: 'TracingEvent',
  description: '내보낼 tracing 이벤트',
  required: true,
},
]}
/>
### 일시 휴업 {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

종료 메시지를 기록합니다.

## 출력 형식 {/* #output-format */}

내보내기는 이벤트 유형에 따라 다양한 형식을 출력합니다.

### SPAN\_STARTED {/* #span_started */}

```
🚀 SPAN_STARTED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Trace ID: [trace id]
   Input: [formatted input]
   Attributes: [formatted attributes]
────────────────────────────────────────
```

### SPAN\_ENDED {/* #span_ended */}

```
✅ SPAN_ENDED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Duration: [duration]ms
   Trace ID: [trace id]
   Input: [formatted input]
   Output: [formatted output]
   Error: [formatted error if present]
   Attributes: [formatted attributes]
────────────────────────────────────────
```

### SPAN\_UPDATED {/* #span_updated */}

```
📝 SPAN_UPDATED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Trace ID: [trace id]
   Input: [formatted input]
   Output: [formatted output]
   Error: [formatted error if present]
   Updated Attributes: [formatted attributes]
────────────────────────────────────────
```

## 용법 {/* #usage */}

```typescript
import { ConsoleExporter } from '@mastra/observability'
import { ConsoleLogger, LogLevel } from '@mastra/core/logger'

// Use default logger (INFO level)
const exporter = new ConsoleExporter()

// Use custom log level
const exporter = new ConsoleExporter({
  logLevel: 'debug',
})

// Use custom logger instance
const customLogger = new ConsoleLogger({ level: LogLevel.DEBUG })
const exporterWithLogger = new ConsoleExporter({
  logger: customLogger,
})
```

## 구현 세부정보 {/* #implementation-details */}

* 공백 2칸 들여쓰기를 사용하여 속성 형식을 JSON으로 지정합니다.
* 범위 기간을 밀리초 단위로 계산하고 표시합니다.
* 직렬화 오류를 정상적으로 처리합니다.
* 구현되지 않은 이벤트 유형을 경고로 기록합니다.
* 이벤트 사이에 80자 구분선을 사용합니다.

## 또한보십시오 {/* #see-also */}

### 선적 서류 비치 {/* #documentation */}

* [추적 개요](/docs/observability/tracing/overview): 전체 가이드
* [수출업체](/docs/observability/integrations/overview#exporters): 수출업체 개념

### 기타 수출업체 {/* #other-exporters */}

* [MastraStorage수출업체](/reference/observability/tracing/exporters/mastra-storage-exporter): 저장 지속성
* [MastraPlatform수출자](/reference/observability/tracing/exporters/mastra-platform-exporter): 마스트라 플랫폼
* [랭퓨즈](/reference/observability/tracing/exporters/langfuse): Langfuse 통합
* [브레인트러스트](/reference/observability/tracing/exporters/braintrust): Braintrust 통합

### 참조 {/* #reference */}

* [구성](/reference/observability/tracing/configuration): 구성 옵션
* [인터페이스](/reference/observability/tracing/interfaces): 유형 정의
