---
title: "참조: OtelExporter | Observability"
description: "추적 및 로그를 위한 OpenTelemetry 내보내기 Tool"
packages:
  - "@mastra/otel-exporter"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# 오텔엑스포터

모든 OpenTelemetry 호환 관찰 플랫폼에 추적 및 로그를 보냅니다. 추적은 표준화된 GenAI 의미 체계 규칙을 사용합니다. 로그는 원래 심각도와 메시지 본문을 전달하며 OTEL 로그 레코드의 기본 추적 컨텍스트와 추적을 통해 추적과 상관됩니다.`mastra.traceId` / `mastra.spanId`속성.

## 건설자 {/* #constructor */}

```typescript prettier:false
new OtelExporter(config: OtelExporterConfig)
```

## `OtelExporterConfig` {/* #otelexporterconfig */}

```typescript
interface OtelExporterConfig {
  provider?: ProviderConfig
  signals?: {
    traces?: boolean
    logs?: boolean
  }
  timeout?: number
  batchSize?: number
  logLevel?: 'debug' | 'info' | 'warn' | 'error'
}
```

<PropertiesTable
  props={[
{
  name: 'provider',
  type: 'ProviderConfig',
  description: 'Provider별 구성(아래 참조)',
  required: true,
},{
  name: 'signals',
  type: '{ traces?: boolean; logs?: boolean }',
  description:
    '신호별 토글입니다. 둘 다 기본값은 true입니다. 해당 신호를 비활성화하려면 값을 false로 설정하세요. 로그를 내보내려면 선택한 프로토콜에 맞는 `@opentelemetry/exporter-logs-otlp-*` 패키지도 설치되어 있어야 합니다.',
  required: false,
},{
  name: 'timeout',
  type: 'number',
  description: '내보내기 제한 시간(밀리초, 기본값: 30000)',
  required: false,
},{
  name: 'batchSize',
  type: 'number',
  description: '배치당 span/로그 수(기본값: 512)',
  required: false,
},{
  name: 'logLevel',
  type: "'debug' | 'info' | 'warn' | 'error'",
  description: "로거 레벨(기본값: 'warn')",
  required: false,
},{
  name: 'resourceAttributes',
  type: 'DetectedResourceAttributes',
  description: '선택적 OpenTelemetry 리소스 속성(여기의 값이 모든 기본값을 재정의함)',
  required: false,
},
]}
/>
## 공급자 구성 {/* #provider-configurations */}

### `Dash0Config` {/* #dash0config */}

```typescript
interface Dash0Config {
  apiKey?: string
  endpoint?: string
  dataset?: string
}
```

<PropertiesTable
  props={[
{
  name: 'apiKey',
  type: 'string',
  description: 'Dash0 API 키입니다. 지정하지 않으면 DASH0_API_KEY 환경 변수를 사용합니다.',
  required: false,
},{
  name: 'endpoint',
  type: 'string',
  description: 'Dash0 수집 엔드포인트입니다. 지정하지 않으면 DASH0_ENDPOINT 환경 변수를 사용합니다.',
  required: false,
},{
  name: 'dataset',
  type: 'string',
  description: '데이터세트 이름입니다. 지정하지 않으면 DASH0_DATASET 환경 변수를 사용합니다.',
  required: false,
},
]}
/>
### `SignozConfig` {/* #signozconfig */}

```typescript
interface SignozConfig {
  apiKey?: string
  region?: 'us' | 'eu' | 'in'
  endpoint?: string
}
```

<PropertiesTable
  props={[
{
  name: 'apiKey',
  type: 'string',
  description: 'SigNoz 수집 키입니다. 지정하지 않으면 SIGNOZ_API_KEY 환경 변수를 사용합니다.',
  required: false,
},{
  name: 'region',
  type: "'us' | 'eu' | 'in'",
  description: "SigNoz Cloud 리전입니다. 지정하지 않으면 SIGNOZ_REGION 환경 변수를 사용합니다. (기본값: 'us')",
  required: false,
},{
  name: 'endpoint',
  type: 'string',
  description: '사용자 정의 엔드포인트입니다. 지정하지 않으면 SIGNOZ_ENDPOINT 환경 변수를 사용합니다.',
  required: false,
},
]}
/>
### `NewRelicConfig` {/* #newrelicconfig */}

```typescript
interface NewRelicConfig {
  apiKey?: string
  endpoint?: string
}
```

<PropertiesTable
  props={[
{
  name: 'apiKey',
  type: 'string',
  description: 'New Relic 라이선스 키입니다. 지정하지 않으면 NEW_RELIC_LICENSE_KEY 환경 변수를 사용합니다.',
  required: false,
},{
  name: 'endpoint',
  type: 'string',
  description:
    '사용자 정의 엔드포인트입니다. 지정하지 않으면 NEW_RELIC_ENDPOINT 환경 변수를 사용합니다. (기본값: https://otlp.nr-data.net:443/v1/traces)',
  required: false,
},
]}
/>
### `TraceloopConfig` {/* #traceloopconfig */}

```typescript
interface TraceloopConfig {
  apiKey?: string
  destinationId?: string
  endpoint?: string
}
```

<PropertiesTable
  props={[
{
  name: 'apiKey',
  type: 'string',
  description: 'Traceloop API 키입니다. 지정하지 않으면 TRACELOOP_API_KEY 환경 변수를 사용합니다.',
  required: false,
},{
  name: 'destinationId',
  type: 'string',
  description: '대상 식별자입니다. 지정하지 않으면 TRACELOOP_DESTINATION_ID 환경 변수를 사용합니다.',
  required: false,
},{
  name: 'endpoint',
  type: 'string',
  description:
    '사용자 정의 엔드포인트입니다. 지정하지 않으면 TRACELOOP_ENDPOINT 환경 변수를 사용합니다. (기본값: https://api.traceloop.com/v1/traces)',
  required: false,
},
]}
/>
### `LaminarConfig` {/* #laminarconfig */}

```typescript
interface LaminarConfig {
  apiKey?: string
  endpoint?: string
}
```

<PropertiesTable
  props={[
{
  name: 'apiKey',
  type: 'string',
  description: 'Laminar 프로젝트 API 키입니다. 지정하지 않으면 LMNR_PROJECT_API_KEY 환경 변수를 사용합니다.',
  required: false,
},{
  name: 'endpoint',
  type: 'string',
  description: '사용자 정의 엔드포인트입니다. 지정하지 않으면 LAMINAR_ENDPOINT 환경 변수를 사용합니다. (기본값: https://api.lmnr.ai/v1/traces)',
  required: false,
},
]}
/>
### `CustomConfig` {/* #customconfig */}

```typescript
interface CustomConfig {
  endpoint: string
  protocol?: 'http/json' | 'http/protobuf' | 'grpc' | 'zipkin'
  headers?: Record<string, string>
}
```

<PropertiesTable
  props={[
{
  name: 'endpoint',
  type: 'string',
  description: 'OTEL 수집기 엔드포인트 URL',
  required: true,
},{
  name: 'protocol',
  type: "'http/json' | 'http/protobuf' | 'grpc' | 'zipkin'",
  description: "내보내기 프로토콜(기본값: 'http/json')",
  required: false,
},{
  name: 'headers',
  type: 'Record<string, string>',
  description: '인증을 위한 사용자 정의 헤더',
  required: false,
},
]}
/>
## 행동 양식 {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

구성된 OTEL 백엔드로 추적 이벤트를 내보냅니다.

### 플러시 {/* #flush */}

```typescript prettier:false
async flush(): Promise<void>
```

강제로 내보내기를 종료하지 않고 버퍼링된 모든 범위를 OTEL 백엔드로 플러시합니다. 런타임이 종료되기 전에 범위를 내보내야 하는 서버리스 환경에 유용합니다.

### 일시 휴업 {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

보류 중인 추적을 플러시하고 내보내기를 종료합니다.

## 사용 예 {/* #usage-examples */}

### Zero-Config(환경 변수 사용) {/* #zero-config-using-environment-variables */}

```typescript
import { OtelExporter } from '@mastra/otel-exporter'

// Set SIGNOZ_API_KEY, SIGNOZ_REGION environment variables
const exporter = new OtelExporter({ provider: { signoz: {} } })

// Or for other providers:
// Set DASH0_API_KEY, DASH0_ENDPOINT for Dash0
// Set NEW_RELIC_LICENSE_KEY for New Relic
// Set TRACELOOP_API_KEY for Traceloop
// Set LMNR_PROJECT_API_KEY for Laminar
```

### 명시적 구성 {/* #explicit-configuration */}

```typescript
import { OtelExporter } from '@mastra/otel-exporter'

const exporter = new OtelExporter({
  provider: {
    signoz: {
      apiKey: process.env.SIGNOZ_API_KEY,
      region: 'us',
    },
  },
})
```

### 커스텀 엔드포인트 포함 {/* #with-custom-endpoint */}

```typescript
const exporter = new OtelExporter({
  provider: {
    custom: {
      endpoint: 'https://my-collector.example.com/v1/traces',
      protocol: 'http/protobuf',
      headers: {
        'x-api-key': process.env.API_KEY,
      },
    },
  },
  timeout: 60000,
  logLevel: 'debug',
})
```

## 프로토콜 요구 사항 {/* #protocol-requirements */}

공급자마다 다른 OTEL 내보내기 패키지가 필요합니다. 추적 및 로그 내보내기는 독립적입니다. 추적만 필요한 경우 추적 패키지만 설치하고, 로그 내보내기도 원하는 경우 선택한 프로토콜에 대한 추적 및 로그 패키지를 모두 설치합니다. Zipkin은 OTLP 로그를 지원하지 않습니다.

| 프로토콜 | 추적 패키지 | 로그 패키지 |
| - | - | - |
| gRPC |`@opentelemetry/exporter-trace-otlp-grpc` (+ `@grpc/grpc-js`) | `@opentelemetry/exporter-logs-otlp-grpc` (+ `@grpc/grpc-js`) |
| HTTP/Protobuf | `@opentelemetry/exporter-trace-otlp-proto` | `@opentelemetry/exporter-logs-otlp-proto` |
| HTTP/JSON | `@opentelemetry/exporter-trace-otlp-http` | `@opentelemetry/exporter-logs-otlp-http` |
| Zipkin | `@opentelemetry/exporter-zipkin` | not supported |

## 태그 지원 {/* #tags-support */}

OtelExporter는 분류 및 필터링을 위한 추적 태그 지정을 지원합니다. 태그는 루트 스팬에만 적용되며`mastra.tags` attribute.

### 용법 {/* #usage */}

```typescript
const result = await agent.generate('Hello', {
  tracingOptions: {
    tags: ['production', 'experiment-v2', 'user-request'],
  },
})
```

### 태그가 저장되는 방법 {/* #how-tags-are-stored */}

백엔드 호환성을 극대화하기 위해 태그는 `mastra.tags` span 속성에 JSON 문자열 배열로 저장됩니다.
```json
{
  "mastra.tags": "[\"production\",\"experiment-v2\",\"user-request\"]"
}
```

:::참고
OpenTelemetry 사양은 기본 배열 속성을 지원하지만 많은 백엔드(Jaeger, Zipkin, Tempo)에서는 배열 지원이 제한되어 있습니다. JSON 문자열은 모든 관찰 플랫폼에서 일관된 동작을 보장합니다.
:::

## 관련된 {/* #related */}

* [OtelExporter 가이드](/docs/observability/integrations/exporters/otel): 공급자 구성이 포함된 설정 가이드
* [오텔브릿지](/docs/observability/integrations/bridges/otel): 양방향 OTEL 컨텍스트 통합용
* [추적 개요](/docs/observability/tracing/overview): 일반적인 추적 개념
