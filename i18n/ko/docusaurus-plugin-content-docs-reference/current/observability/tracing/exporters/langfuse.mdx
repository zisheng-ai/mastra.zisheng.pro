---
title: "참조: LangfuseExporter | Observability"
description: "Tracing을 위한 Langfuse 내보내기"
packages:
  - "@mastra/langfuse"
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# Langfuse수출업체

Observability을 위해 추적 데이터를 Langfuse로 보냅니다.

## 건설자 {/* #constructor */}

```typescript prettier:false
new LangfuseExporter(config: LangfuseExporterConfig)
```

## `LangfuseExporterConfig` {/* #langfuseexporterconfig */}

```typescript
interface LangfuseExporterConfig extends BaseExporterConfig {
  publicKey?: string
  secretKey?: string
  baseUrl?: string
  realtime?: boolean
  flushAt?: number
  flushInterval?: number
  environment?: string
  release?: string
}
```

`BaseExporterConfig`를 확장하며, 포함되는 항목은 다음과 같습니다.
* `logger?: IMastraLogger`- 로거 인스턴스
* `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'`- 로그 수준(기본값: INFO)

<PropertiesTable
  props={[
{
  name: 'publicKey',
  type: 'string',
  description: 'Langfuse API 키입니다. 지정하지 않으면 LANGFUSE_PUBLIC_KEY 환경 변수를 사용합니다.',
  required: false,
},{
  name: 'secretKey',
  type: 'string',
  description: 'Langfuse 비밀 키입니다. 지정하지 않으면 LANGFUSE_SECRET_KEY 환경 변수를 사용합니다.',
  required: false,
},{
  name: 'baseUrl',
  type: 'string',
  description: 'Langfuse 호스트 URL입니다. 지정하지 않으면 LANGFUSE_BASE_URL 환경 변수를 사용합니다. 기본값: https://cloud.langfuse.com',
  required: false,
},{
  name: 'realtime',
  type: 'boolean',
  description: '실시간 모드를 활성화합니다. span을 배치 처리하지 않고 즉시 내보냅니다.',
  required: false,
},{
  name: 'flushAt',
  type: 'number',
  description: 'OTEL 내보내기 배치당 최대 span 수입니다. 내부 LangfuseSpanProcessor로 전달됩니다.',
  required: false,
},{
  name: 'flushInterval',
  type: 'number',
  description:
    '대기 중인 span을 내보내기 전까지의 최대 시간(초)입니다. 내부 LangfuseSpanProcessor로 전달됩니다.',
  required: false,
},{
  name: 'environment',
  type: 'string',
  description: 'Trace의 환경 태그입니다(예: production, staging). 필터링할 수 있도록 Langfuse UI에 표시됩니다.',
  required: false,
},{
  name: 'release',
  type: 'string',
  description: 'Trace의 릴리스 태그입니다(예: git 커밋 해시). 버전 추적에 사용됩니다.',
  required: false,
},{
  name: 'logLevel',
  type: "'debug' | 'info' | 'warn' | 'error'",
  description: "로거 레벨(기본값: 'warn')",
  required: false,
},
]}
/>
## 속성 {/* #properties */}

### `client` {/* #client */}

```typescript prettier:false
get client(): LangfuseClient | undefined
```

고급 Langfuse 기능을 위한 `LangfuseClient` 인스턴스입니다. exporter가 비활성화되어 있으면(자격 증명 누락) `undefined`를 반환합니다. Prompt 관리, 평가, 데이터세트, 직접 API 액세스에 사용하세요.
## 행동 양식 {/* #methods */}

### 플러시 {/* #flush */}

```typescript prettier:false
async flush(): Promise<void>
```

Force는 종료하지 않고 버퍼링된 모든 범위와 점수를 Langfuse로 플러시합니다. 런타임이 종료되기 전에 데이터를 내보내야 하는 서버리스 환경에 유용합니다.

### 일시 휴업 {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

보류 중인 데이터를 플러시하고 스팬 프로세서와 클라이언트를 모두 종료합니다.

## 용법 {/* #usage */}

### Zero-Config(환경 변수 사용) {/* #zero-config-using-environment-variables */}

```typescript
import { LangfuseExporter } from '@mastra/langfuse'

// Reads from LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_BASE_URL
const exporter = new LangfuseExporter()
```

### 명시적 구성 {/* #explicit-configuration */}

```typescript
import { LangfuseExporter } from '@mastra/langfuse'

const exporter = new LangfuseExporter({
  publicKey: process.env.LANGFUSE_PUBLIC_KEY,
  secretKey: process.env.LANGFUSE_SECRET_KEY,
  baseUrl: 'https://cloud.langfuse.com',
  realtime: true,
})
```

## 스팬 매핑 {/* #span-mapping */}

Mastra span은 Otel 형식으로 변환된 후 `LangfuseSpanProcessor`를 통해 Langfuse로 전송됩니다. Langfuse는 `gen_ai.*` 속성에 따라 span을 자동으로 매핑합니다.
* 다음을 포함하는 스팬`model` attribute → Langfuse generations
* 기타 모든 스팬 → Langfuse 스팬
* 상위-하위 관계는 Otel 추적/스팬 ID를 통해 보존됩니다.

## Prompt 연결 {/* #prompt-linking */}

`withLangfusePrompt` 헬퍼를 사용하여 LLM 생성을 [Langfuse Prompt 관리](https://langfuse.com/docs/prompt-management)에 연결합니다.
```typescript
import { buildTracingOptions } from '@mastra/observability'
import { LangfuseExporter, withLangfusePrompt } from '@mastra/langfuse'

const exporter = new LangfuseExporter()

// Fetch prompt via the Langfuse client
const prompt = await exporter.client.prompt.get('customer-support', { type: 'text' })

const agent = new Agent({
  id: 'support-agent',
  name: 'support-agent',
  instructions: prompt.compile(),
  model: '__GATEWAY_OPENAI_MODEL__',
  defaultGenerateOptions: {
    tracingOptions: buildTracingOptions(
      withLangfusePrompt({ name: prompt.name, version: prompt.version }),
    ),
  },
})
```

### 도우미 기능 {/* #helper-functions */}

#### `withLangfusePrompt(prompt)` {/* #withlangfusepromptprompt */}

추적 옵션에 Langfuse Prompt 메타데이터를 추가합니다.

```typescript
// Link by name and version (required for Langfuse v5)
withLangfusePrompt({ name: 'my-prompt', version: 1 })
```

Prompt 메타데이터는 범위 속성으로 전달되고 Langfuse는 생성을 해당 Prompt에 연결합니다.
