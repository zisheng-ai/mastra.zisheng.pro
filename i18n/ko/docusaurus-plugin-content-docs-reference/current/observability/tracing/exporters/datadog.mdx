---
title: "참조: DatadogExporter | Observability"
description: "추적을 위한 Datadog LLM Observability 내보내기 Tool"
packages:
  - "@mastra/core"
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# Datadog수출업체

모니터링 및 분석을 위해 추적 데이터를 Datadog의 LLM Observability 제품으로 보냅니다.

:::정보
당신도 사용하는 경우`dd-trace` APM auto-instrumentation and need it correctly parented under Mastra spans, use the [DatadogBridge](/reference/observability/tracing/bridges/datadog)대신에. 내보내기는 실행이 완료된 후 LLM 관찰 데이터를 전송하므로 참여하지 않습니다.`dd-trace`의 라이브 범위입니다.
:::

## 건설자 {/* #constructor */}

```typescript prettier:false
new DatadogExporter(config: DatadogExporterConfig)
```

## `DatadogExporterConfig` {/* #datadogexporterconfig */}

```typescript
interface DatadogExporterConfig extends BaseExporterConfig {
  apiKey?: string
  mlApp?: string
  site?: string
  service?: string
  env?: string
  agentless?: boolean
  integrationsEnabled?: boolean
}
```

확장`BaseExporterConfig`, which includes:

* `logger?: IMastraLogger`- 로거 인스턴스
* `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'`- 로그 수준(기본값: INFO)

<PropertiesTable
  props={[
{
  name: 'apiKey',
  type: 'string',
  description: 'Datadog API key. Required for agentless mode. Falls back to DD_API_KEY env var.',
  required: false,
},
{
  name: 'mlApp',
  type: 'string',
  description: 'ML application name for grouping traces. Required. Falls back to DD_LLMOBS_ML_APP env var.',
  required: false,
},
{
  name: 'site',
  type: 'string',
  description:
    "Datadog site (e.g., 'datadoghq.com', 'datadoghq.eu', 'us3.datadoghq.com'). Falls back to DD_SITE env var. Default: 'datadoghq.com'.",
  required: false,
},
{
  name: 'service',
  type: 'string',
  description: 'Service name for the application. Defaults to mlApp value.',
  required: false,
},
{
  name: 'env',
  type: 'string',
  description: "Environment name (e.g., 'production', 'staging'). Falls back to DD_ENV env var.",
  required: false,
},
{
  name: 'agentless',
  type: 'boolean',
  description: 'Use direct HTTPS intake (true) or local Datadog Agent (false). Default: true.',
  required: false,
},
{
  name: 'integrationsEnabled',
  type: 'boolean',
  description: 'Enable dd-trace automatic integrations. Default: false.',
  required: false,
},
]}
/>

## 행동 양식 {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

추적 이벤트를 Datadog LLM Observability로 내보냅니다.

### 플러시 {/* #flush */}

```typescript prettier:false
async flush(): Promise<void>
```

강제로 내보내기를 종료하지 않고 버퍼링된 모든 범위를 Datadog으로 플러시합니다. 런타임이 종료되기 전에 범위를 내보내야 하는 서버리스 환경에 유용합니다.

### 일시 휴업 {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

보류 중인 데이터를 플러시하고 내보내기를 종료합니다. 보류 중인 정리 타이머를 취소하고 LLM 관찰 기능을 비활성화합니다.

## 용법 {/* #usage */}

### Zero-Config(환경 변수 사용) {/* #zero-config-using-environment-variables */}

```typescript
import { DatadogExporter } from '@mastra/datadog'

// Reads from DD_LLMOBS_ML_APP, DD_API_KEY, DD_SITE, DD_ENV
const exporter = new DatadogExporter()
```

### 명시적 구성 {/* #explicit-configuration */}

```typescript
import { DatadogExporter } from '@mastra/datadog'

const exporter = new DatadogExporter({
  mlApp: 'my-llm-app',
  apiKey: process.env.DD_API_KEY,
  site: 'datadoghq.com',
  env: 'production',
})
```

### 로컬 Datadog Agent 사용 {/* #with-local-datadog-agent */}

```typescript
const exporter = new DatadogExporter({
  mlApp: 'my-llm-app',
  agentless: false, // Use local Datadog Agent
  env: 'production',
})
```

## 스팬 매핑 {/* #span-mapping */}

Mastra 범위 유형은 Datadog LLM Observability 범위 종류에 매핑됩니다.

| 마스트라 스팬타입 | 데이터독 종류 |
| - | - |
|`AGENT_RUN` | `agent` |
| `MODEL_GENERATION` | `workflow` |
| `MODEL_STEP` | `llm` |
| `TOOL_CALL` | `tool` |
| `MCP_TOOL_CALL` | `tool` |
| `PROVIDER_TOOL_CALL` | `tool` |
| `WORKFLOW_RUN` | `workflow`|
| 기타 모든 유형 |`task` |

매핑되지 않은 모든 범위 유형(포함`MODEL_CHUNK`, `WORKFLOW_STEP`, `GENERIC`, and future span types) automatically default to `task`.

## 환경변수 {/* #environment-variables */}

내보내기는 다음 환경 변수에서 구성을 읽습니다.

| 변수 | 설명 |
| - | - |
|`DD_API_KEY` | Datadog API key |
| `DD_LLMOBS_ML_APP` | ML application name |
| `DD_SITE` | Datadog site |
| `DD_ENV` | Environment name |
| `DD_LLMOBS_AGENTLESS_ENABLED` | Set to 'false' or '0' to use local agent |
