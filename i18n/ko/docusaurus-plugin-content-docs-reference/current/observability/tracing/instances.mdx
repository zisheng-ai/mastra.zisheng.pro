---
title: "참조: 추적 | Observability"
description: "핵심 추적 클래스 및 메서드"
packages:
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# Observability 인스턴스

## `DefaultObservabilityInstance` {/* #defaultobservabilityinstance */}

ObservabilityInstance 인터페이스의 기본 구현입니다.

### 건설자 {/* #constructor */}

```typescript prettier:false
new DefaultObservabilityInstance(config: ObservabilityInstanceConfig)
```

지정된 구성을 사용하여 새 DefaultObservabilityInstance를 만듭니다.

### 속성 {/* #properties */}

BaseObservabilityInstance의 모든 속성과 메서드를 상속합니다.

## `BaseObservabilityInstance` {/* #baseobservabilityinstance */}

사용자 정의 ObservabilityInstance 구현을 위한 기본 클래스입니다.

### 행동 양식 {/* #methods */}

#### `getConfig` {/* #getconfig */}

```typescript prettier:false
getConfig(): Readonly<Required<ObservabilityInstanceConfig>>
```

현재 관측 가능성 구성을 반환합니다.

#### `getExporters` {/* #getexporters */}

```typescript prettier:false
getExporters(): readonly ObservabilityExporter[]
```

구성된 모든 내보내기를 반환합니다.

#### `getSpanOutputProcessors` {/* #getspanoutputprocessors */}

```typescript prettier:false
getSpanOutputProcessors(): readonly SpanOutputProcessor[]
```

구성된 모든 범위 출력 프로세서를 반환합니다.

#### `getLogger` {/* #getlogger */}

```typescript prettier:false
getLogger(): IMastraLogger
```

내보내기 및 기타 구성 요소에 대한 로거 인스턴스를 반환합니다.

#### `startSpan` {/* #startspan */}

```typescript prettier:false
startSpan<TType extends SpanType>(
  options: StartSpanOptions<TType>
): Span<TType>
```

특정 SpanType의 새 범위를 시작합니다. 상위 항목이 제공되지 않은 경우 추적의 루트 범위를 생성합니다.

<PropertiesTable
  props={[
{
  name: 'type',
  type: 'SpanType',
  description: 'Type of span to create',
  required: true,
},
{
  name: 'name',
  type: 'string',
  description: 'Name of the span',
  required: true,
},
{
  name: 'parent',
  type: 'AnySpan',
  description: 'Parent span (if not root)',
  required: false,
},
{
  name: 'attributes',
  type: 'SpanTypeMap[TType]',
  description: 'Type-specific attributes',
  required: false,
},
{
  name: 'metadata',
  type: 'Record<string, any>',
  description: 'User-defined metadata',
  required: false,
},
{
  name: 'input',
  type: 'any',
  description: 'Initial input data',
  required: false,
},
{
  name: 'customSamplerOptions',
  type: 'CustomSamplerOptions',
  description: 'Options for custom sampler',
  required: false,
},
]}
/>

#### 일시 휴업 {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

모든 내보내기 및 프로세서를 종료하고 리소스를 정리합니다.

## 맞춤 구현 {/* #custom-implementation */}

사용자 정의 ObservabilityInstance 구현을 생성하려면 BaseObservabilityInstance를 확장하십시오.

```typescript
class CustomObservabilityInstance extends BaseObservabilityInstance {
  constructor(config: ObservabilityInstanceConfig) {
    super(config)
    // Custom initialization
  }

  // Override methods as needed
  startSpan<TType extends SpanType>(options: StartSpanOptions<TType>): Span<TType> {
    // Custom span creation logic
    return super.startSpan(options)
  }
}
```

## 또한보십시오 {/* #see-also */}

### 선적 서류 비치 {/* #documentation */}

* [추적 개요](/docs/observability/tracing/overview): 개념 및 사용 가이드
* [구성 참조](/reference/observability/tracing/configuration): 구성 옵션
* [인터페이스 참조](/reference/observability/tracing/interfaces): 유형 정의
* [스팬 참조](/reference/observability/tracing/spans): 스팬 수명주기 및 방법

### 수출업체 {/* #exporters */}

* [MastraStorage수출업체](/reference/observability/tracing/exporters/mastra-storage-exporter): 저장 지속성
* [MastraPlatform수출자](/reference/observability/tracing/exporters/mastra-platform-exporter): 마스트라 플랫폼 통합
* [콘솔 내보내기](/reference/observability/tracing/exporters/console-exporter): 디버그 출력
