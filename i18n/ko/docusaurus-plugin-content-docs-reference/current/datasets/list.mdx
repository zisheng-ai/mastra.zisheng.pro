---
title: "참조: DatasetsManager.list() | 데이터 세트"
description: "페이지 매김이 포함된 모든 데이터세트를 나열하는 `DatasetsManager.list()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# DatasetsManager.list()

**추가된 항목:** `@mastra/core@1.4.0`

페이지 매김 및 선택적 필터를 사용하여 데이터세트를 나열합니다. 필터는 저장소 계층에 적용되므로 호출자는 결과를 사후 필터링할 필요가 없습니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const { datasets, pagination } = await mastra.datasets.list({ page: 0, perPage: 10 })

for (const ds of datasets) {
  console.log(`${ds.id}: ${ds.name} (v${ds.version})`)
}

// Filter by target type and name (case-insensitive substring)
const filtered = await mastra.datasets.list({
  page: 0,
  perPage: 20,
  filters: { targetType: 'agent', name: 'qa' },
})

// Filter by any-overlap match on target IDs
const forAgents = await mastra.datasets.list({
  filters: { targetIds: ['agent-a', 'agent-b'] },
})
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'page',
  type: 'number',
  isOptional: true,
  description: '페이지 번호입니다. 기본값은 `0`입니다.',
},{
  name: 'perPage',
  type: 'number',
  isOptional: true,
  description: '페이지당 데이터 세트 수입니다. 기본값은 `20`입니다.',
},{
  name: 'filters',
  type: 'object',
  isOptional: true,
  description: '스토리지 계층에 적용되는 선택적 필터입니다.',
  properties: [    {
      type: 'object',
      parameters: [
        {
          name: 'organizationId',
          type: 'string',
          isOptional: true,
          description: '테넌시 필터: 이 조직의 데이터 세트로 제한합니다.',
        },        {
          name: 'projectId',
          type: 'string',
          isOptional: true,
          description: '테넌시 필터: 이 프로젝트의 데이터 세트로 제한합니다.',
        },        {
          name: 'candidateKey',
          type: 'string',
          isOptional: true,
          description: '이 후보 키에 속한 데이터 세트로 제한합니다.',
        },        {
          name: 'candidateId',
          type: 'string',
          isOptional: true,
          description: '이 후보 ID에 속한 데이터 세트로 제한합니다.',
        },        {
          name: 'targetType',
          type: "'agent' | 'workflow' | 'scorer' | 'processor'",
          isOptional: true,
          description: '이 컴포넌트 유형을 대상으로 하는 데이터 세트로 제한합니다.',
        },        {
          name: 'targetIds',
          type: 'string[]',
          isOptional: true,
          description:
            '`targetIds`가 이 목록과 하나 이상 겹치는 데이터 세트로 제한합니다. 빈 배열은 필터가 없는 것으로 처리됩니다.',
        },        {
          name: 'name',
          type: 'string',
          isOptional: true,
          description: '데이터 세트 `name`에 대해 대소문자를 구분하지 않는 부분 문자열 일치를 수행합니다. 빈 문자열은 필터가 없는 것으로 처리됩니다.',
        },
      ],
    },
  ],
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<object>',
  description: '페이지가 매겨진 데이터 세트 목록입니다.',
  properties: [    {
      type: 'object',
      parameters: [
        {
          name: 'datasets',
          type: 'DatasetRecord[]',
          description: '데이터 세트 레코드의 배열입니다. 레코드 구조는 dataset.getDetails()를 참조하세요.',
        },        {
          name: 'pagination',
          type: 'PaginationInfo',
          description: '`total`, `page`, `perPage`, `hasMore`가 포함된 페이지네이션 메타데이터입니다.',
        },
      ],
    },
  ],
},
]}
/>