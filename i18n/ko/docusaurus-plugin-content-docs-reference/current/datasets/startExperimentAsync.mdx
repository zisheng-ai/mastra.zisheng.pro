---
title: "참조:dataset.startExperimentAsync() | 데이터 세트"
description: "비동기식으로 실험을 시작하는 `dataset.startExperimentAsync()` 메서드에 대한 문서입니다."
packages:
  - '@mastra/core'
---

# 데이터 세트.startExperimentAsync()

**추가된 항목:** `@mastra/core@1.4.0`

실험을 비동기식으로 시작합니다(Fire-and-forget). 실험 ID와 함께 즉시 반환됩니다.`'pending'` status. The experiment runs in the background.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Start experiment without waiting
const { experimentId, status } = await dataset.startExperimentAsync({
  targetType: 'agent',
  targetId: 'my-agent',
  scorers: ['accuracy'],
})

console.log(`Experiment ${experimentId} started with status: ${status}`)

// Check progress later
const experiment = await dataset.getExperiment({ experimentId })
console.log(`Current status: ${experiment.status}`)
```

## 매개변수 {/* #parameters */}

같은 것을 취한다`StartExperimentConfig` as [`dataset.startExperiment()`](/reference/datasets/startExperiment).

언제`persistence.experiments` is set to `'none'`, `startExperimentAsync()` doesn't persist an experiment record, progress updates, or item results. Score persistence remains controlled separately by `persistence.scores`. 실험 이벤트 관찰자가 없으면 실행은 실행 후 잊어버리게 되며 실험 API는 완료 또는 실패 여부를 보고할 수 없습니다.

동기 사용[`startExperiment()`](/reference/datasets/startExperiment) when the caller needs the returned summary. An experiment event observer can receive lifecycle events and the terminal summary.

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<object>',
  description: 'Immediate response with experiment ID.',
  properties: [
    {
      type: 'object',
      parameters: [
        {
          name: 'experimentId',
          type: 'string',
          description: 'Unique ID of the created experiment.',
        },
        {
          name: 'status',
          type: "'pending'",
          description: "Always `'pending'` since the experiment hasn't started executing yet.",
        },
      ],
    },
  ],
},
]}
/>

## 관련된 {/* #related */}

* [데이터세트.시작실험()](/reference/datasets/startExperiment)
* [데이터 세트.getExperiment()](/reference/datasets/getExperiment)
