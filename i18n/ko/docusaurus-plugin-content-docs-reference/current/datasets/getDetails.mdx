---
title: "참조: 데이터세트.getDetails() | 데이터 세트"
description: "저장소에서 전체 데이터 세트 레코드를 검색하는 `dataset.getDetails()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 데이터세트.getDetails()

**추가된 항목:** `@mastra/core@1.4.0`

메타데이터, 스키마, 버전 정보를 포함하여 스토리지에서 전체 데이터 세트 레코드를 검색합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Get dataset details
const details = await dataset.getDetails()
console.log(details.name, details.version)
```

## 매개변수 {/* #parameters */}

이 메서드는 매개변수를 사용하지 않습니다.

## 보고 {/* #returns */}

다음을 반환합니다.`Promise<DatasetRecord>`. Throws `MastraError` if the dataset isn't found.

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'Unique identifier of the dataset.',
},
{
  name: 'name',
  type: 'string',
  description: 'Display name of the dataset.',
},
{
  name: 'description',
  type: 'string',
  isOptional: true,
  description: 'Description of the dataset.',
},
{
  name: 'metadata',
  type: 'Record<string, unknown>',
  isOptional: true,
  description: 'Arbitrary metadata associated with the dataset.',
},
{
  name: 'inputSchema',
  type: 'Record<string, unknown>',
  isOptional: true,
  description: 'JSON Schema for item inputs.',
},
{
  name: 'groundTruthSchema',
  type: 'Record<string, unknown>',
  isOptional: true,
  description: 'JSON Schema for item ground truths.',
},
{
  name: 'version',
  type: 'number',
  description: 'Current version number of the dataset.',
},
{
  name: 'createdAt',
  type: 'Date',
  description: 'When the dataset was created.',
},
{
  name: 'updatedAt',
  type: 'Date',
  description: 'When the dataset was last updated.',
},
]}
/>
