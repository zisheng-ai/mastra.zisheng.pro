---
title: "참조: 데이터세트 클래스 | 데이터 세트"
description: "단일 데이터세트와 상호작용하기 위한 공개 API를 제공하는 Mastra의 `Dataset` 클래스에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 데이터 세트 클래스

**추가된 항목:** `@mastra/core@1.4.0`

그만큼`Dataset` class provides methods for item CRUD, versioning, and experiment management on a single dataset. Obtained via `mastra.datasets.get()`또는`mastra.datasets.create()`.

## 사용 예 {/* #usage-examples */}

### 항목 추가 및 실험 실행 {/* #add-items-and-run-an-experiment */}

```typescript title="src/mastra/datasets/usage.ts"
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.create({
  name: 'QA pairs',
})

// Add items
await dataset.addItems({
  items: [
    { input: { question: 'What is AI?' }, groundTruth: { answer: 'Artificial Intelligence' } },
    { input: { question: 'What is ML?' }, groundTruth: { answer: 'Machine Learning' } },
  ],
})

// Run an experiment
const summary = await dataset.startExperiment({
  targetType: 'agent',
  targetId: 'my-agent',
  scorers: ['accuracy'],
})

console.log(`${summary.succeededCount}/${summary.totalItems} succeeded`)
```

### 버전 및 기록 관리 {/* #manage-versions-and-history */}

```typescript title="src/mastra/datasets/versioning.ts"
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// List versions
const { versions } = await dataset.listVersions()

// List items at a specific version
const items = await dataset.listItems({ version: 2 })

// Get item change history
const history = await dataset.getItemHistory({ itemId: 'item-id' })
```

## 입장 {/* #access */}

`Dataset`직접 인스턴스화되지 않습니다. 다음을 통해 구입하세요.`DatasetsManager`:

```typescript
const dataset = await mastra.datasets.get({ id: 'dataset-id' })
// or
const dataset = await mastra.datasets.create({ name: 'My dataset' })
```

## 속성 {/* #properties */}

전체 데이터 세트 레코드(이름, 설명, 스키마, 버전, 타임스탬프)를 보려면 다음을 호출하세요.[`dataset.getDetails()`](/reference/datasets/getDetails).

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'The unique identifier of the dataset. Read-only.',
},
]}
/>

## 관련된 {/* #related */}

* [DatasetsManager 클래스](/reference/datasets/datasets-manager)
* [데이터세트.시작실험()](/reference/datasets/startExperiment)
* [데이터세트.추가항목()](/reference/datasets/addItems)
* [데이터 세트.listVersions()](/reference/datasets/listVersions)
