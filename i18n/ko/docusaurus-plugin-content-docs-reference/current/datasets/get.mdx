---
title: "참조: DatasetsManager.get() | 데이터 세트"
description: "ID로 기존 데이터세트를 검색하는 `DatasetsManager.get()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# DatasetsManager.get()

**추가된 항목:** `@mastra/core@1.4.0`

ID로 기존 데이터 세트를 검색합니다. 던진다`MastraError` if the dataset doesn't exist.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Now use the dataset
const details = await dataset.getDetails()
console.log(details.name)
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'Unique identifier of the dataset.',
},
]}
/>

## 보고 {/* #returns */}

던지기`MastraError` if the dataset isn't found.

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<Dataset>',
  description: 'A Dataset instance.',
},
]}
/>
