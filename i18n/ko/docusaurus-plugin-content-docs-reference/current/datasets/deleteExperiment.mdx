---
title: "참조:dataset.deleteExperiment() | 데이터 세트"
description: "ID별로 실험을 삭제하는 `dataset.deleteExperiment()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 데이터 세트.삭제실험()

**추가된 항목:** `@mastra/core@1.4.0`

모든 관련 결과를 포함하여 ID별로 실험(실행)을 삭제합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteExperiment({ experimentId: 'exp-id' })
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'experimentId',
  type: 'string',
  description: '삭제할 실험의 ID입니다.',
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<void>',
  description: '실험과 그 결과가 삭제되면 이행됩니다.',
},
]}
/>