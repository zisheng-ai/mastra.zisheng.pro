---
title: "참조:dataset.deleteItem() | 데이터 세트"
description: "데이터세트에서 단일 항목을 삭제하는 `dataset.deleteItem()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 데이터세트.삭제항목()

**추가된 항목:** `@mastra/core@1.4.0`

ID별로 데이터 세트에서 단일 항목을 삭제합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItem({ itemId: 'item-id' })
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'itemId',
  type: 'string',
  description: '삭제할 항목의 ID입니다.',
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<void>',
  description: '항목이 삭제되면 이행됩니다.',
},
]}
/>