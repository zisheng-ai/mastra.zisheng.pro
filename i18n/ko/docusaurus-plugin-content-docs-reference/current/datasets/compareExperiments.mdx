---
title: "참조: DatasetsManager.compareExperiments() | 데이터 세트"
description: "두 개 이상의 실험을 비교하는 `DatasetsManager.compareExperiments()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# DatasetsManager.compareExperiments()

**추가된 항목:** `@mastra/core@1.4.0`

두 개 이상의 실험을 비교하여 항목별 및 득점자별 비교를 생성합니다. 실험 ID가 2개 이상 필요합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const comparison = await mastra.datasets.compareExperiments({
  experimentIds: ['exp-baseline', 'exp-new'],
  baselineId: 'exp-baseline',
})

console.log(`Baseline: ${comparison.baselineId}`)

for (const item of comparison.items) {
  console.log(`Item ${item.itemId}:`)
  console.log(`  Input: ${JSON.stringify(item.input)}`)

  for (const [expId, result] of Object.entries(item.results)) {
    if (result) {
      console.log(
        `  ${expId}: output=${JSON.stringify(result.output)}, scores=${JSON.stringify(result.scores)}`,
      )
    }
  }
}
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'experimentIds',
  type: 'string[]',
  description: '비교할 실험 ID의 배열입니다. 최소 2개를 포함해야 합니다.',
},{
  name: 'baselineId',
  type: 'string',
  isOptional: true,
  description: '기준 실험의 ID입니다. 기본값은 `experimentIds`의 첫 번째 ID입니다.',
},
]}
/>
## 보고 {/* #returns */}

제공된 실험 ID가 2개 미만이면 `MastraError`가 발생합니다.
<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<object>',
  description: '비교 결과입니다.',
  properties: [    {
      type: 'object',
      parameters: [
        {
          name: 'baselineId',
          type: 'string',
          description: '비교에 사용된 기준 실험의 ID입니다.',
        },        {
          name: 'items',
          type: 'Array<object>',
          description: '항목별 비교 데이터입니다.',
          properties: [            {
              type: 'object',
              parameters: [
                {
                  name: 'itemId',
                  type: 'string',
                  description: '데이터 세트 항목의 ID입니다.',
                },                {
                  name: 'input',
                  type: 'unknown | null',
                  description: '항목의 입력 데이터입니다.',
                },                {
                  name: 'groundTruth',
                  type: 'unknown | null',
                  description: '항목의 정답 데이터입니다.',
                },                {
                  name: 'results',
                  type: 'Record<string, { output: unknown; scores: Record<string, number | null> } | null>',
                  description:
                    '실험 ID를 키로 사용하는 결과입니다. 각 항목에는 해당 실험의 출력과 채점 결과가 포함됩니다.',
                },
              ],
            },
          ],
        },
      ],
    },
  ],
},
]}
/>
## 관련된 {/* #related */}

* [데이터세트.시작실험()](/reference/datasets/startExperiment)
* [데이터세트.목록실험()](/reference/datasets/listExperiments)
