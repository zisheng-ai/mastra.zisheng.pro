---
title: "참조:dataset.deleteItems() | 데이터 세트"
description: "데이터세트에서 여러 항목을 일괄 삭제하는 `dataset.deleteItems()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 데이터세트.삭제항목()

**추가된 항목:** `@mastra/core@1.4.0`

단일 대량 작업으로 데이터세트에서 여러 항목을 삭제합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItems({
  itemIds: ['item-1', 'item-2', 'item-3'],
})
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'itemIds',
  type: 'string[]',
  description: 'Array of item IDs to delete.',
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<void>',
  description: 'Resolves when all items are deleted.',
},
]}
/>
