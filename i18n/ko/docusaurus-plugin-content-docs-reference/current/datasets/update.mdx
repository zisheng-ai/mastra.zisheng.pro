---
title: "참조: 데이터세트.업데이트() | 데이터 세트"
description: "데이터 세트 메타데이터 및 스키마를 업데이트하는 `dataset.update()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 데이터세트.업데이트()

**추가된 항목:** `@mastra/core@1.4.0`

데이터 세트 메타데이터, 이름, 설명 및/또는 스키마를 업데이트합니다. 스키마 정의에는 표준 JSON 스키마(Zod, Valibot, ArkType 등)를 사용합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'
import { z } from 'zod'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Update with plain metadata
const updated = await dataset.update({
  name: 'Updated QA pairs',
  description: 'Revised evaluation set',
})

// Update with Zod schema (library that supports Standard JSON Schema)
const updated2 = await dataset.update({
  inputSchema: z.object({
    question: z.string(),
    context: z.string().optional(),
  }),
})
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'name',
  type: 'string',
  isOptional: true,
  description: 'New display name.',
},
{
  name: 'description',
  type: 'string',
  isOptional: true,
  description: 'New description.',
},
{
  name: 'metadata',
  type: 'Record<string, unknown>',
  isOptional: true,
  description: 'Updated metadata.',
},
{
  name: 'inputSchema',
  type: 'StandardJSONSchemaV1',
  isOptional: true,
  description: 'Standard JSON Schema for item inputs.',
},
{
  name: 'groundTruthSchema',
  type: 'StandardJSONSchemaV1',
  isOptional: true,
  description: 'Standard JSON Schema for item ground truths.',
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<DatasetRecord>',
  description: 'The updated dataset record. See dataset.getDetails() for the full shape.',
},
]}
/>
