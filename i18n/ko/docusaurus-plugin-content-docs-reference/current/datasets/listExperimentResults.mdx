---
title: "참조:dataset.listExperimentResults() | 데이터 세트"
description: "특정 실험의 결과를 나열하는 `dataset.listExperimentResults()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 데이터 세트.listExperimentResults()

**추가된 항목:** `@mastra/core@1.4.0`

선택적 필터 및 페이지 매김을 사용하여 특정 실험에 대한 개별 항목 결과를 나열합니다. 필터는 저장 계층에 적용됩니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const { results, pagination } = await dataset.listExperimentResults({
  experimentId: 'exp-id',
  page: 0,
  perPage: 50,
})

// Restrict to results that still need review
const { results: pending } = await dataset.listExperimentResults({
  experimentId: 'exp-id',
  status: 'needs-review',
})

// Restrict to results tied to a specific trace
const { results: byTrace } = await dataset.listExperimentResults({
  experimentId: 'exp-id',
  traceId: 'trace-abc',
})

for (const result of results) {
  console.log(`Item ${result.itemId}: ${result.error ? 'FAILED' : 'OK'}`)
}
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'experimentId',
  type: 'string',
  description: 'ID of the experiment to list results for.',
},
{
  name: 'traceId',
  type: 'string',
  isOptional: true,
  description: 'Restrict results to those linked to this trace ID.',
},
{
  name: 'status',
  type: "'needs-review' | 'reviewed' | 'complete'",
  isOptional: true,
  description: 'Restrict results to this per-result review status.',
},
{
  name: 'filters',
  type: 'ExperimentTenancyFilters',
  isOptional: true,
  description: 'Multi-tenant scoping filters (`organizationId`, `projectId`). Forwarded to the storage layer.',
},
{
  name: 'page',
  type: 'number',
  isOptional: true,
  description: 'Page number. Defaults to `0`.',
},
{
  name: 'perPage',
  type: 'number',
  isOptional: true,
  description: 'Number of results per page. Defaults to `20`.',
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<object>',
  description: 'Paginated experiment results.',
  properties: [
    {
      type: 'object',
      parameters: [
        {
          name: 'results',
          type: 'ExperimentResult[]',
          description: 'Array of item-level results.',
          properties: [
            {
              type: 'ExperimentResult',
              parameters: [
                {
                  name: 'id',
                  type: 'string',
                  description: 'Unique result ID.',
                },
                {
                  name: 'experimentId',
                  type: 'string',
                  description: 'ID of the parent experiment.',
                },
                {
                  name: 'itemId',
                  type: 'string',
                  description: 'ID of the dataset item.',
                },
                {
                  name: 'itemDatasetVersion',
                  type: 'number | null',
                  description: 'Dataset version of the item when executed.',
                },
                {
                  name: 'input',
                  type: 'unknown',
                  description: 'Input data passed to the target.',
                },
                {
                  name: 'output',
                  type: 'unknown | null',
                  description: 'Output from the target.',
                },
                {
                  name: 'groundTruth',
                  type: 'unknown | null',
                  description: 'Expected output.',
                },
                {
                  name: 'error',
                  type: '{ message: string; stack?: string; code?: string } | null',
                  description: 'Structured error if execution failed.',
                },
                {
                  name: 'startedAt',
                  type: 'Date',
                  description: 'When execution started.',
                },
                {
                  name: 'completedAt',
                  type: 'Date',
                  description: 'When execution completed.',
                },
                {
                  name: 'retryCount',
                  type: 'number',
                  description: 'Number of retry attempts.',
                },
                {
                  name: 'traceId',
                  type: 'string | null',
                  description: 'Trace ID for observability.',
                },
                {
                  name: 'createdAt',
                  type: 'Date',
                  description: 'When the result record was created.',
                },
              ],
            },
          ],
        },
        {
          name: 'pagination',
          type: 'PaginationInfo',
          description: 'Pagination metadata with `total`, `page`, `perPage`, and `hasMore`.',
        },
      ],
    },
  ],
},
]}
/>
