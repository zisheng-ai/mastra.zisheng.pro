---
title: "참조: 데이터세트.getItemHistory() | 데이터 세트"
description: "데이터세트 항목의 전체 SCD-2 변경 내역을 검색하는 `dataset.getItemHistory()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 데이터세트.getItemHistory()

**추가된 항목:** `@mastra/core@1.4.0`

모든 데이터 세트 버전에서 특정 항목의 전체 SCD-2(Slowly Changing Dimension Type 2) 기록을 검색합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const history = await dataset.getItemHistory({ itemId: 'item-id' })

for (const row of history) {
  console.log(`Version ${row.datasetVersion}: ${row.isDeleted ? 'deleted' : 'active'}`)
}
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'itemId',
  type: 'string',
  description: '기록을 조회할 항목의 ID입니다.',
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<DatasetItemRow[]>',
  description: '버전순으로 정렬된 버전별 항목 행의 배열입니다.',
  properties: [    {
      type: 'DatasetItemRow',
      parameters: [
        {
          name: 'id',
          type: 'string',
          description: '행의 고유 식별자입니다.',
        },        {
          name: 'datasetId',
          type: 'string',
          description: '상위 데이터 세트의 ID입니다.',
        },        {
          name: 'datasetVersion',
          type: 'number',
          description: '이 행의 데이터 세트 버전입니다.',
        },        {
          name: 'validTo',
          type: 'number | null',
          description: '이 행이 대체된 버전이며, 아직 현재 행이면 `null`입니다.',
        },        {
          name: 'isDeleted',
          type: 'boolean',
          description: '이 행이 삭제를 나타내는지 여부입니다.',
        },        {
          name: 'input',
          type: 'unknown',
          description: '이 버전의 입력 데이터입니다.',
        },        {
          name: 'groundTruth',
          type: 'unknown',
          isOptional: true,
          description: '이 버전의 정답입니다.',
        },        {
          name: 'metadata',
          type: 'Record<string, unknown>',
          isOptional: true,
          description: '이 버전의 메타데이터입니다.',
        },        {
          name: 'createdAt',
          type: 'Date',
          description: '이 행이 생성된 시점입니다.',
        },        {
          name: 'updatedAt',
          type: 'Date',
          description: '이 행이 마지막으로 업데이트된 시점입니다.',
        },
      ],
    },
  ],
},
]}
/>