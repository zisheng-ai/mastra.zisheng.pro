---
title: "참조: 데이터세트.addItems() | 데이터 세트"
description: "데이터세트에 여러 항목을 대량으로 추가하는 `dataset.addItems()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 데이터세트.추가항목()

**추가된 항목:** `@mastra/core@1.4.0`

단일 대량 작업으로 데이터세트에 여러 항목을 추가합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const items = await dataset.addItems({
  items: [
    { input: { question: 'What is AI?' }, groundTruth: { answer: 'Artificial Intelligence' } },
    { input: { question: 'What is ML?' }, groundTruth: { answer: 'Machine Learning' } },
    { input: { question: 'What is DL?' }, metadata: { category: 'deep-learning' } },
  ],
})

console.log(`Added ${items.length} items`)
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'items',
  type: 'Array<object>',
  description: '추가할 항목의 배열입니다. 각 항목은 dataset.addItem() 입력과 동일한 구조를 가집니다.',
  properties: [    {
      type: 'DatasetItem',
      parameters: [
        {
          name: 'input',
          type: 'unknown',
          description: '항목의 입력 데이터입니다.',
        },        {
          name: 'groundTruth',
          type: 'unknown',
          isOptional: true,
          description: '예상 출력 또는 정답 데이터입니다.',
        },        {
          name: 'metadata',
          type: 'Record<string, unknown>',
          isOptional: true,
          description: '임의의 메타데이터입니다.',
        },
      ],
    },
  ],
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<DatasetItem[]>',
  description: '생성된 데이터 세트 항목의 배열입니다. 항목 구조는 dataset.addItem()을 참조하세요.',
},
]}
/>