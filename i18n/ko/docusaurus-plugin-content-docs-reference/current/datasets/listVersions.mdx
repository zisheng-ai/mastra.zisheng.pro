---
title: "참조: 데이터세트.listVersions() | 데이터 세트"
description: "데이터세트의 모든 버전을 나열하는 `dataset.listVersions()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 데이터 세트.listVersions()

**추가된 항목:** `@mastra/core@1.4.0`

페이지 매김을 사용하여 데이터 세트의 모든 버전을 나열합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const { versions, pagination } = await dataset.listVersions({ page: 0, perPage: 10 })

for (const version of versions) {
  console.log(`Version ${version.version} created at ${version.createdAt}`)
}
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'page',
  type: 'number',
  isOptional: true,
  description: '페이지 번호입니다. 기본값은 `0`입니다.',
},{
  name: 'perPage',
  type: 'number',
  isOptional: true,
  description: '페이지당 버전 수입니다. 기본값은 `20`입니다.',
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<object>',
  description: '페이지가 매겨진 버전 목록입니다.',
  properties: [    {
      type: 'object',
      parameters: [
        {
          name: 'versions',
          type: 'DatasetVersion[]',
          description: '버전 레코드의 배열입니다.',
          properties: [            {
              type: 'DatasetVersion',
              parameters: [
                {
                  name: 'id',
                  type: 'string',
                  description: '버전 레코드의 고유 식별자입니다.',
                },                {
                  name: 'datasetId',
                  type: 'string',
                  description: '상위 데이터 세트의 ID입니다.',
                },                {
                  name: 'version',
                  type: 'number',
                  description: '버전 번호입니다.',
                },                {
                  name: 'createdAt',
                  type: 'Date',
                  description: '이 버전이 생성된 시점입니다.',
                },
              ],
            },
          ],
        },        {
          name: 'pagination',
          type: 'object',
          description: '페이지네이션 메타데이터입니다.',
          properties: [            {
              type: 'object',
              parameters: [
                {
                  name: 'total',
                  type: 'number',
                  description: '전체 버전 수입니다.',
                },                {
                  name: 'page',
                  type: 'number',
                  description: '현재 페이지 번호입니다.',
                },                {
                  name: 'perPage',
                  type: 'number | false',
                  description: '페이지당 버전 수이며, 페이지네이션을 사용하지 않으면 `false`입니다.',
                },                {
                  name: 'hasMore',
                  type: 'boolean',
                  description: '추가 페이지가 있는지 여부입니다.',
                },
              ],
            },
          ],
        },
      ],
    },
  ],
},
]}
/>