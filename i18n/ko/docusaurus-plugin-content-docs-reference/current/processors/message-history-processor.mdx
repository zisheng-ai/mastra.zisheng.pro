---
title: "참조: MessageHistory | 프로세서"
description: "대화 기록의 검색 및 지속성을 처리하는 Mastra의 MessageHistory 프로세서에 대한 문서입니다."
packages:
  - "@mastra/core"
  - "@mastra/pg"
---

# 메시지 기록

그만큼`MessageHistory`는**하이브리드 프로세서**메시지 기록의 검색과 지속성을 모두 처리합니다. 입력 시 스토리지에서 기록 메시지를 가져와서 대화 앞에 추가합니다. 출력 시 새 메시지를 스토리지에 유지합니다.

## 사용예 {/* #usage-example */}

```typescript
import { MessageHistory } from '@mastra/core/processors'

const processor = new MessageHistory({
  storage: memoryStorage,
  lastMessages: 50,
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'options',
  type: 'MessageHistoryOptions',
  description: 'Configuration options for the message history processor',
  isOptional: false,
  properties: [
    {
      type: 'MessageHistoryOptions',
      parameters: [
        {
          name: 'storage',
          type: 'MemoryStorage',
          description: 'Storage instance for retrieving and persisting messages',
          isOptional: false,
        },
        {
          name: 'lastMessages',
          type: 'number',
          description: 'Maximum number of historical messages to retrieve. If not specified, retrieves all messages',
          isOptional: true,
        },
      ],
    },
  ],
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: "Processor identifier set to 'message-history'",
  isOptional: false,
},
{
  name: 'name',
  type: 'string',
  description: "Processor display name set to 'MessageHistory'",
  isOptional: false,
},
{
  name: 'processInput',
  type: '(args: { messages: MastraDBMessage[]; messageList: MessageList; abort: (reason?: string) => never; tracingContext?: TracingContext; requestContext?: RequestContext }) => Promise<MessageList | MastraDBMessage[]>',
  description: 'Fetches historical messages from storage and adds them to the message list',
  isOptional: false,
},
{
  name: 'processOutputResult',
  type: '(args: { messages: MastraDBMessage[]; messageList: MessageList; abort: (reason?: string) => never; tracingContext?: TracingContext; requestContext?: RequestContext }) => Promise<MessageList>',
  description: 'Persists new messages (user input and assistant response) to storage, excluding system messages',
  isOptional: false,
},
]}
/>

## 확장된 사용 예 {/* #extended-usage-example */}

```typescript title="src/mastra/agents/memory-agent.ts"
import { Agent } from '@mastra/core/agent'
import { MessageHistory } from '@mastra/core/processors'
import { PostgresStorage } from '@mastra/pg'

const storage = new PostgresStorage({
  connectionString: process.env.DATABASE_URL,
})

export const agent = new Agent({
  id: 'memory-agent',
  name: 'memory-agent',
  instructions: 'You are a helpful assistant with conversation memory',
  model: '__GATEWAY_OPENAI_MODEL__',
  inputProcessors: [
    new MessageHistory({
      storage,
      lastMessages: 100,
    }),
  ],
  outputProcessors: [
    new MessageHistory({
      storage,
    }),
  ],
})
```

## 행동 {/* #behavior */}

### 입력 처리 {/* #input-processing */}

1. 검색`threadId` from the request context
2. 저장소에서 기록 메시지를 가져옵니다(생성 날짜순, 내림차순)
3. 시스템 메시지를 필터링합니다(데이터베이스에 저장하면 안 됨).
4. ID별 중복을 방지하여 기록 메시지를 수신 메시지와 병합합니다.
5. 다음과 같은 기록 메시지를 추가합니다.`source: 'memory'` tag

### 출력 처리 {/* #output-processing */}

1. 검색`threadId` from the request context
2. 다음과 같은 경우 지속성을 건너뜁니다.`readOnly` is set in memory config
3. 메시지에서 불완전한 Tool 호출을 필터링합니다.
4. 새로운 사용자 입력 및 보조자 응답 메시지를 스토리지에 유지합니다.
5. 스레드의 업데이트`updatedAt` timestamp

## 관련된 {/* #related */}

* [난간](/docs/agents/guardrails)
