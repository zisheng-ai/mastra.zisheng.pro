---
title: "참조: PrefillErrorHandler | 프로세서"
description: "계속 메시지를 추가하고 재시도하여 보조 응답 사전 채우기 오류를 처리하는 Mastra의 PrefillErrorHandler 프로세서에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# PrefillErrorHandler

그만큼`PrefillErrorHandler`은**오류 프로세서**보조자 응답 미리 채우기 오류를 처리합니다. 이 오류는 대화가 보조 메시지로 끝나고 Model이 보조 응답을 미리 채우는 것으로 해석하기 때문에 요청을 거부할 때 발생합니다.

오류가 감지되면 프로세서는 숨겨진 메시지를 보냅니다.`system-reminder` signal with `continue` as its contents and signals a retry. The reminder is persisted as signal metadata, which keeps it available for retry reconstruction and raw history while standard UI-facing message conversions hide it.

이 프로세서를 다음에 추가하십시오.`errorProcessors` when you want Mastra to recover from assistant prefill rejections (for example Anthropic's "assistant message prefill" and Qwen/llama.cpp's "assistant response prefill is incompatible with `enable_thinking`" errors).

## 작동 원리 {/* #how-it-works */}

1. 알려진 보조 사전 채우기 거부 메시지와 함께 LLM API 호출이 실패합니다.
2. `PrefillErrorHandler`이것이 첫 번째 재시도인지 확인합니다.
3. 숨겨진 메시지를 보냅니다.`system-reminder` signal with `continue` as its contents
4. 그것은 반환`{ retry: true }` to signal the LLM call should be retried with the modified messages

이제 프로세서는 대화가 현재 보조 메시지로 끝나는지 다시 확인하는 대신 API 거부 자체에 반응합니다. 이는 후행 메시지 형태가 이미 업스트림으로 변환된 경우에도 공급자가 사전 채우기 의미 체계에 대한 요청을 거부하는 경우에 탄력성을 갖게 합니다.

## 사용예 {/* #usage-example */}

추가하다`PrefillErrorHandler` to `errorProcessors` for any agent that should retry assistant-prefill failures:

```typescript title="src/mastra/agents/my-agent.ts"
import { Agent } from '@mastra/core/agent'
import { PrefillErrorHandler } from '@mastra/core/processors'

export const agent = new Agent({
  id: 'my-agent',
  name: 'my-agent',
  instructions: 'You are a helpful assistant.',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  errorProcessors: [new PrefillErrorHandler()],
})
```

사용자 정의 복구 동작을 원하는 경우 자체 오류 프로세서를 제공하십시오.`processAPIError` method:

```typescript title="src/mastra/agents/custom-error-handling.ts"
import { Agent } from '@mastra/core/agent'
import type { Processor } from '@mastra/core/processors'

const customErrorHandler: Processor = {
  id: 'custom-prefill-error-handler',
  processAPIError({ error, messageList, retryCount }) {
    // Your custom logic here
  },
}

export const agent = new Agent({
  id: 'my-agent',
  name: 'my-agent',
  instructions: 'You are a helpful assistant.',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  errorProcessors: [customErrorHandler],
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

그만큼`PrefillErrorHandler` takes no constructor parameters.

## 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: "'prefill-error-handler'",
  description: 'Processor identifier.',
  isOptional: false,
},
{
  name: 'name',
  type: "'Prefill Error Handler'",
  description: 'Processor display name.',
  isOptional: false,
},
{
  name: 'processAPIError',
  type: '(args: ProcessAPIErrorArgs) => ProcessAPIErrorResult | void',
  description:
    'Handles known assistant-prefill errors by sending a hidden system reminder signal and signaling retry. Only triggers on the first retry attempt.',
  isOptional: false,
},
]}
/>

## 관련된 {/* #related */}

* [프로세서 인터페이스](/reference/processors/processor-interface)
* [난간](/docs/agents/guardrails)
