---
title: "참조: BatchPartsProcessor | 프로세서"
description: "배출 빈도를 줄이기 위해 여러 스트림 부분을 함께 일괄 처리하는 Mastra의 BatchPartsProcessor에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# BatchParts프로세서

그만큼`BatchPartsProcessor`은**출력 프로세서**스트리밍 중 방출 빈도를 줄이기 위해 여러 스트림 부분을 함께 일괄 처리합니다. 이 프로세서는 네트워크 오버헤드를 줄이고, 작은 텍스트 청크를 통합하여 사용자 경험을 개선하고, 부분이 클라이언트에 내보내지는 시기를 제어하여 스트리밍 성능을 최적화하는 데 유용합니다.

## 사용예 {/* #usage-example */}

```typescript
import { BatchPartsProcessor } from '@mastra/core/processors'

const processor = new BatchPartsProcessor({
  batchSize: 5,
  maxWaitTime: 100,
  emitOnNonText: true,
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'options',
  type: 'Options',
  description: 'Configuration options for batching stream parts',
  isOptional: true,
  properties: [
    {
      type: 'Options',
      parameters: [
        {
          name: 'batchSize',
          type: 'number',
          description: 'Number of parts to batch together before emitting',
          isOptional: true,
          default: '5',
        },
        {
          name: 'maxWaitTime',
          type: 'number',
          description:
            "Maximum time to wait before emitting a batch (in milliseconds). If set, will emit the current batch even if it hasn't reached batchSize",
          isOptional: true,
          default: 'undefined (no timeout)',
        },
        {
          name: 'emitOnNonText',
          type: 'boolean',
          description: 'Whether to emit immediately when a non-text part is encountered',
          isOptional: true,
          default: 'true',
        },
      ],
    },
  ],
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: "Processor identifier set to 'batch-parts'",
  isOptional: false,
},
{
  name: 'name',
  type: 'string',
  description: 'Optional processor display name',
  isOptional: true,
},
{
  name: 'processOutputStream',
  type: '(args: { part: ChunkType; streamParts: ChunkType[]; state: Record<string, any>; abort: (reason?: string) => never }) => Promise<ChunkType | null>',
  description: 'Processes streaming output parts to batch them together',
  isOptional: false,
},
{
  name: 'flush',
  type: '(state?: BatchPartsState) => ChunkType | null',
  description: 'Force flush any remaining batched parts when the stream ends',
  isOptional: false,
},
]}
/>

## 확장된 사용 예 {/* #extended-usage-example */}

```typescript title="src/mastra/agents/batched-agent.ts"
import { Agent } from '@mastra/core/agent'
import { BatchPartsProcessor } from '@mastra/core/processors'

export const agent = new Agent({
  id: 'batched-agent',
  name: 'batched-agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
  outputProcessors: [
    new BatchPartsProcessor({
      batchSize: 5,
      maxWaitTime: 100,
      emitOnNonText: true,
    }),
  ],
})
```

## 관련된 {/* #related */}

* [난간](/docs/agents/guardrails)
