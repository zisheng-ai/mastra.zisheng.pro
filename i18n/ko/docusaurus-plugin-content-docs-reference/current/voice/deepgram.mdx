---
title: "참고자료: Deepgram | 목소리"
description: "다양한 음성 Model 및 언어로 텍스트 음성 변환 및 음성 텍스트 변환 기능을 제공하는 Deepgram 음성 구현에 대한 문서입니다."
packages:
  - "@mastra/voice-deepgram"
---

# 딥그램

Mastra의 Deepgram 음성 구현은 Deepgram의 API를 사용하여 텍스트 음성 변환(TTS) 및 음성 텍스트 변환(STT) 기능을 제공합니다. 음성 합성 및 전사에 대한 구성 가능한 옵션을 통해 여러 음성 Model 및 언어를 지원합니다.

## 사용예 {/* #usage-example */}

```typescript
import { DeepgramVoice } from '@mastra/voice-deepgram'

// Initialize with default configuration (uses DEEPGRAM_API_KEY environment variable)
const voice = new DeepgramVoice()

// Initialize with custom configuration
const voice = new DeepgramVoice({
  speechModel: {
    name: 'aura',
    apiKey: 'your-api-key',
  },
  listeningModel: {
    name: 'nova-2',
    apiKey: 'your-api-key',
  },
  speaker: 'asteria-en',
})

// Text-to-Speech
const audioStream = await voice.speak('Hello, world!')

// Speech-to-Text
const transcript = await voice.listen(audioStream)
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'speechModel',
  type: 'DeepgramVoiceConfig',
  description: '텍스트 음성 변환 기능을 위한 구성입니다.',
  isOptional: true,
  defaultValue: "{ name: 'aura' }",
  properties: [    {
      type: 'DeepgramVoiceConfig',
      parameters: [
        {
          name: 'name',
          type: 'DeepgramModel',
          description: '사용할 Deepgram Model',
          isOptional: true,
        },        {
          name: 'apiKey',
          type: 'string',
          description: 'Deepgram API 키입니다. 없으면 DEEPGRAM_API_KEY 환경 변수를 사용합니다.',
          isOptional: true,
        },        {
          name: 'properties',
          type: 'Record<string, any>',
          description: 'Deepgram API에 전달할 추가 속성',
          isOptional: true,
        },        {
          name: 'language',
          type: 'string',
          description: 'Model의 언어 코드',
          isOptional: true,
        },
      ],
    },
  ],
},{
  name: 'listeningModel',
  type: 'DeepgramVoiceConfig',
  description: '음성 텍스트 변환 기능을 위한 구성입니다.',
  isOptional: true,
  defaultValue: "{ name: 'nova' }",
  properties: [    {
      type: 'DeepgramVoiceConfig',
      parameters: [
        {
          name: 'name',
          type: 'DeepgramModel',
          description: '사용할 Deepgram Model',
          isOptional: true,
        },        {
          name: 'apiKey',
          type: 'string',
          description: 'Deepgram API 키입니다. 없으면 DEEPGRAM_API_KEY 환경 변수를 사용합니다.',
          isOptional: true,
        },        {
          name: 'properties',
          type: 'Record<string, any>',
          description: 'Deepgram API에 전달할 추가 속성',
          isOptional: true,
        },        {
          name: 'language',
          type: 'string',
          description: 'Model의 언어 코드',
          isOptional: true,
        },
      ],
    },
  ],
},{
  name: 'speaker',
  type: 'DeepgramVoiceId',
  description: '텍스트 음성 변환에 사용할 기본 음성',
  isOptional: true,
  defaultValue: "'asteria-en'",
},
]}
/>
## 행동 양식 {/* #methods */}

### `speak()` {/* #speak */}

구성된 음성 Model 및 음성을 사용하여 텍스트를 음성으로 변환합니다.

<PropertiesTable
  content={[
{
  name: 'input',
  type: 'string | NodeJS.ReadableStream',
  description: '음성으로 변환할 텍스트입니다. 스트림을 제공하면 먼저 텍스트로 변환합니다.',
  isOptional: false,
},{
  name: 'options',
  type: 'object',
  description: '음성 합성을 위한 추가 옵션',
  isOptional: true,
  properties: [    {
      type: 'object',
      parameters: [
        {
          name: 'speaker',
          type: 'string',
          description: '이 요청에 사용할 화자를 기본값 대신 지정합니다.',
          isOptional: true,
        },
      ],
    },
  ],
},
]}
/>
보고:`Promise<NodeJS.ReadableStream>`

### `listen()` {/* #listen */}

구성된 청취 Model을 사용하여 음성을 텍스트로 변환합니다.

<PropertiesTable
  content={[
{
  name: 'audioStream',
  type: 'NodeJS.ReadableStream',
  description: '전사할 오디오 스트림',
  isOptional: false,
},{
  name: 'options',
  type: 'object',
  description: 'Deepgram API에 전달할 추가 옵션',
  isOptional: true,
},
]}
/>
보고:`Promise<string>`

### `getSpeakers()` {/* #getspeakers */}

사용 가능한 음성 옵션 목록을 반환합니다.

<PropertiesTable
  content={[
{
  name: 'voiceId',
  type: 'string',
  description: '음성의 고유 식별자',
  isOptional: false,
},
]}
/>