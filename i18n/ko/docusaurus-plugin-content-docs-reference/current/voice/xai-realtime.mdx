---
title: "참고: xAI 실시간 음성 | 목소리"
description: "xAI Grok 음성 Agent API를 통해 실시간 음성 대화를 제공하는 XAIRealtimeVoice 클래스에 대한 문서입니다."
packages:
  - "@mastra/node-audio"
  - "@mastra/voice-xai-realtime"
---

# xAI 실시간 음성

그만큼`XAIRealtimeVoice`클래스는 xAI Grok Voice Agent API를 사용하여 실시간 음성 상호 작용 기능을 제공합니다. Mastra를 구현합니다.`MastraVoice`실시간 계약을 맺고 양방향 오디오 스트리밍, 텍스트 전환, 서버 VAD, xAI 음성, 기능 Tool 및 xAI 서버 측 Tool를 지원합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Agent } from '@mastra/core/agent'
import { getMicrophoneStream, playAudio } from '@mastra/node-audio'
import { XAIRealtimeVoice } from '@mastra/voice-xai-realtime'

const voice = new XAIRealtimeVoice({
  apiKey: process.env.XAI_API_KEY,
  model: 'grok-voice-think-fast-1.0',
  speaker: 'eve',
  instructions: 'You are a concise voice assistant.',
  turnDetection: { type: 'server_vad' },
})

const agent = new Agent({
  id: 'voice-agent',
  name: 'Voice Agent',
  instructions: 'You are a helpful voice assistant.',
  model: 'xai/grok-4.3',
  voice,
})

await agent.voice.connect()

agent.voice.on('speaker', audioStream => {
  playAudio(audioStream)
})

agent.voice.on('writing', ({ text, role }) => {
  console.log(`${role}: ${text}`)
})

await agent.voice.speak('How can I help you today?')

const microphoneStream = getMicrophoneStream()
await agent.voice.send(microphoneStream)

agent.voice.close()
```

## 구성 {/* #configuration */}

### 생성자 옵션 {/* #constructor-options */}

<PropertiesTable
  content={[
{
  name: 'apiKey',
  type: 'string',
  description: 'xAI API key. Falls back to the XAI_API_KEY environment variable.',
  isOptional: true,
},
{
  name: 'ephemeralToken',
  type: 'string',
  description: 'Short-lived xAI token sent with the WebSocket protocol instead of an authorization header.',
  isOptional: true,
},
{
  name: 'model',
  type: 'XAIRealtimeModel',
  description: 'The Grok voice model to use.',
  isOptional: true,
  defaultValue: "'grok-voice-think-fast-1.0'",
},
{
  name: 'speaker',
  type: 'XAIVoice',
  description:
    'Voice ID to use for speech output. Built-in values are eve, ara, rex, sal, and leo. Custom xAI voice IDs are also supported.',
  isOptional: true,
  defaultValue: "'eve'",
},
{
  name: 'instructions',
  type: 'string',
  description: 'System instructions sent in session.update.',
  isOptional: true,
},
{
  name: 'turnDetection',
  type: 'XAITurnDetection',
  description: 'Voice activity detection configuration.',
  isOptional: true,
  defaultValue: "{ type: 'server_vad' }",
},
{
  name: 'audio',
  type: 'XAIAudioConfig',
  description: 'Input and output audio format configuration.',
  isOptional: true,
  defaultValue: '24 kHz audio/pcm input and output',
},
{
  name: 'serverTools',
  type: 'XAIServerTool[]',
  description:
    'xAI server-side tools to send in session.update. Supports file_search, web_search, x_search, and mcp. These are merged with session.tools.',
  isOptional: true,
},
{
  name: 'session',
  type: 'Partial<XAISessionConfig>',
  description: 'Additional xAI session fields to merge into the initial session.update event.',
  isOptional: true,
},
{
  name: 'url',
  type: 'string',
  description: 'Override the xAI realtime WebSocket URL.',
  isOptional: true,
  defaultValue: "'wss://api.x.ai/v1/realtime'",
},
{
  name: 'debug',
  type: 'boolean',
  description:
    'Enable debug logging for received xAI events. Debug logs can include transcripts and tool-call arguments.',
  isOptional: true,
  defaultValue: 'false',
},
]}
/>

### VoiceConfig 패턴 {/* #voiceconfig-pattern */}

Mastra의 공유 음성 구성 형태를 사용할 수도 있습니다.

```typescript
const voice = new XAIRealtimeVoice({
  speaker: 'ara',
  realtimeConfig: {
    model: 'grok-voice-think-fast-1.0',
    apiKey: process.env.XAI_API_KEY,
    options: {
      instructions: 'Answer briefly.',
      turnDetection: { type: 'server_vad', threshold: 0.85 },
    },
  },
})
```

## 입증 {/* #authentication */}

사용`apiKey` or `XAI_API_KEY` for server-side applications. This provider is built for Node.js server-side runtimes. If you already mint xAI ephemeral tokens on your server, you can pass one as `ephemeralToken`; the provider uses the `xai-client-secret.<token>` WebSocket protocol instead of an authorization header. If both `apiKey` and `ephemeralToken`구성되면 공급자는 임시 토큰을 사용합니다.

## 행동 양식 {/* #methods */}

### `connect()` {/* #connect */}

WebSocket 연결을 설정하고 초기 정보를 보냅니다.`session.update`.

<PropertiesTable
  content={[
{
  name: 'requestContext',
  type: 'RequestContext',
  description: 'Optional Mastra request context passed to function tool executions.',
  isOptional: true,
},
]}
/>

보고:`Promise<void>`

### `close()` {/* #close */}

WebSocket 연결을 닫고 활성 발언자 스트림을 종료하며 대기 중인 이벤트, 보류 중인 함수 호출 상태 및 요청 컨텍스트를 지웁니다.`disconnect()` is an alias for `close()`.

보고:`void`

### `addInstructions()` {/* #addinstructions */}

세션 지침을 설정합니다. WebSocket이 열려 있으면 공급자는`session.update`. Passing `undefined` stores an empty string and clears the active instructions on the current session or the next connection.

<PropertiesTable
  content={[
{
  name: 'instructions',
  type: 'string',
  description: 'System instructions to send to xAI.',
  isOptional: true,
},
]}
/>

보고:`void`

### `addTools()` {/* #addtools */}

Mastra 기능 Tool를 등록하고 연결되면 다음으로 세션 Tool를 새로 고칩니다.`session.update`.

<PropertiesTable
  content={[
{
  name: 'tools',
  type: 'ToolsInput',
  description: 'Mastra tools to expose as xAI function tools.',
  isOptional: true,
},
]}
/>

보고:`void`

### `updateConfig()` {/* #updateconfig */}

보낸다`session.update` event with additional xAI session fields.

<PropertiesTable
  content={[
{
  name: 'sessionConfig',
  type: 'Partial<XAISessionConfig>',
  description: 'Session fields to update.',
  isOptional: false,
},
]}
/>

보고:`void`

### `speak()` {/* #speak */}

다음을 사용하여 텍스트 차례를 보냅니다.`conversation.item.create` and then requests a response.

<PropertiesTable
  content={[
{
  name: 'input',
  type: 'string | NodeJS.ReadableStream',
  description: 'Text or a readable text stream to send as user input.',
  isOptional: false,
},
{
  name: 'options.speaker',
  type: 'XAIVoice',
  description: 'Voice override. This updates the active xAI session voice and is used for subsequent turns.',
  isOptional: true,
},
{
  name: 'options.response',
  type: 'Record<string, unknown>',
  description: 'Additional xAI response.create fields.',
  isOptional: true,
},
]}
/>

보고:`Promise<void>`

### `send()` {/* #send */}

실시간 오디오 청크를 스트리밍합니다.`input_audio_buffer.append`.

`send()`열린 연결이 필요합니다. 이후 라이브 마이크 오디오에 사용`connect()` resolves. Readable stream chunks must be binary audio chunks (`Buffer`, `ArrayBuffer`, or a typed array).

<PropertiesTable
  content={[
{
  name: 'audioData',
  type: 'NodeJS.ReadableStream | Int16Array',
  description: 'PCM audio stream or Int16Array audio data.',
  isOptional: false,
},
{
  name: 'eventId',
  type: 'string',
  description: 'Optional xAI event ID.',
  isOptional: true,
},
]}
/>

보고:`Promise<void>`

### `listen()` {/* #listen */}

다음을 사용하여 유한한 오디오 스트림을 보냅니다.`input_audio_buffer.append`. By default it commits the input buffer and requests a response.

<PropertiesTable
  content={[
{
  name: 'audioData',
  type: 'NodeJS.ReadableStream',
  description: 'Audio stream to send.',
  isOptional: false,
},
{
  name: 'options.commit',
  type: 'boolean',
  description: 'Whether to send input_audio_buffer.commit after the audio item.',
  isOptional: true,
  defaultValue: 'true',
},
{
  name: 'options.createResponse',
  type: 'boolean',
  description: 'Whether to send response.create after the audio item.',
  isOptional: true,
  defaultValue: 'true',
},
]}
/>

보고:`Promise<void>`

### `answer()` {/* #answer */}

보낸다`response.create` to ask xAI to continue the conversation.

보고:`Promise<void>`

### `commitAudioBuffer()`그리고`clearAudioBuffer()` {/* #commitaudiobuffer-and-clearaudiobuffer */}

수동 회전 제어를 위해 일치하는 xAI 실시간 클라이언트 이벤트를 보냅니다.

보고:`Promise<void>`

### `cancelResponse()` {/* #cancelresponse */}

보낸다`response.cancel` to interrupt an in-flight response.

<PropertiesTable
  content={[
{
  name: 'responseId',
  type: 'string',
  description: 'Optional xAI response ID to cancel.',
  isOptional: true,
},
{
  name: 'eventId',
  type: 'string',
  description: 'Optional xAI event ID.',
  isOptional: true,
},
]}
/>

보고:`Promise<void>`

## 이벤트 {/* #events */}

`XAIRealtimeVoice`xAI 실시간 서버 이벤트를 Mastra 음성 이벤트에 매핑합니다.

* `speaker`: 보조 오디오에 대해 읽을 수 있는 스트림을 내보냅니다.
* `speaking`: 보조 오디오 델타를 방출합니다.
* `speaking.done`: 보조 오디오 응답이 완료되면 내보냅니다.
* `writing`: 보조 텍스트 델타 및 사용자 입력 기록을 내보냅니다.
* `error`: xAI 및 공급자 실행 오류를 발생시킵니다. 또한 Tool 실행 오류와 잘못된 함수 호출 인수를 내보냅니다. Tool 오류에는 다음이 포함됩니다.`details.call_id` and `details.name`.
* `close`: WebSocket이 닫힐 때 방출됩니다.
* `tool-call-start`: Mastra 기능 Tool가 실행되기 전에 방출됩니다.
* `tool-call-result`: Mastra 기능 Tool가 반환된 후 방출됩니다.

원시 xAI 이벤트 이름도 생성되므로 다음과 같은 이벤트를 구독할 수 있습니다.`response.output_audio.delta`, `response.text.delta`, `response.function_call_arguments.done`, and `response.done`.

## Tool {/* #tools */}

### 마스트라 기능 Tool {/* #mastra-function-tools */}

다음으로 추가된 Tool`addTools()` are converted into xAI function tools and included in `session.update`.

```typescript
import { createTool } from '@mastra/core/tools'
import { z } from 'zod'

const weatherTool = createTool({
  id: 'getWeather',
  description: 'Get current weather for a location.',
  inputSchema: z.object({
    location: z.string(),
  }),
  execute: async ({ location }) => {
    return { location, temperature: 22 }
  },
})

voice.addTools({ getWeather: weatherTool })
```

xAI가 방출될 때`response.function_call_arguments.done`, the provider executes the matching Mastra tool and sends a `function_call_output` item. If xAI emits multiple function calls for one response, the provider waits for every tool result and the response's `response.done` event before sending one continuation `response.create`.

### xAI 서버 측 Tool {/* #xai-server-side-tools */}

xAI 서버 측 Tool는 세션 구성을 통해 전달되고 xAI에 의해 실행됩니다. 전달된 Tool`session.tools` and `serverTools` are merged:

```typescript
const voice = new XAIRealtimeVoice({
  apiKey: process.env.XAI_API_KEY,
  serverTools: [
    { type: 'web_search' },
    { type: 'x_search', allowed_x_handles: ['xai'] },
    { type: 'file_search', vector_store_ids: ['collection_123'], max_num_results: 10 },
    {
      type: 'mcp',
      server_url: 'https://mcp.example.com/mcp',
      server_label: 'business-tools',
      allowed_tools: ['lookup_order'],
    },
  ],
})
```

## 오디오 형식 {/* #audio-formats */}

기본 입력 및 출력 형식은 24kHz PCM16입니다. 지원되는 PCM 샘플 속도 또는 전화 통신 코덱을 구성할 수도 있습니다.

```typescript
const voice = new XAIRealtimeVoice({
  audio: {
    input: { format: { type: 'audio/pcm', rate: 16000 } },
    output: { format: { type: 'audio/pcm', rate: 16000 } },
  },
})
```

지원되는 형식 유형은 다음과 같습니다.`audio/pcm`, `audio/pcmu`, and `audio/pcma`. PCM supports the documented sample rates from 8 kHz through 48 kHz. `audio/pcmu` and `audio/pcma` are G.711 telephony codecs and use 8 kHz.
