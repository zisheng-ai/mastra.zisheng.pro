---
title: "참조: AWS Nova Sonic 음성 | 목소리"
description: "AWS Bedrock Nova 2 Sonic을 통해 실시간 음성 대 음성 기능을 제공하는 NovaSonicVoice 클래스에 대한 설명서입니다."
packages:
  - "@mastra/node-audio"
  - "@mastra/voice-aws-nova-sonic"
---

# AWS 노바 소닉 목소리

그만큼`NovaSonicVoice`클래스는 실시간 음성 대 음성 기능을 제공합니다.[AWS 베드락 노바 2 소닉](https://docs.aws.amazon.com/nova/latest/userguide/speech.html). Model에 대한 양방향 스트림을 열고 보조 오디오, 기록된 텍스트, Tool 호출, 회전 경계 및 중단에 대한 이벤트를 내보냅니다.

## 사용예 {/* #usage-example */}

```typescript title="src/mastra/voice.ts"
import { NovaSonicVoice } from '@mastra/voice-aws-nova-sonic'
import { playAudio, getMicrophoneStream } from '@mastra/node-audio'

// Initialize using the default AWS credential provider chain
const voice = new NovaSonicVoice({
  region: 'us-east-1',
  speaker: 'matthew',
})

// Or pass explicit credentials
const voiceWithCredentials = new NovaSonicVoice({
  region: 'us-east-1',
  speaker: 'tiffany',
  credentials: {
    accessKeyId: process.env.AWS_ACCESS_KEY_ID!,
    secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY!,
  },
})

// Establish the bidirectional stream
await voice.connect()

// Listen for assistant audio (Int16Array PCM)
voice.on('speaking', ({ audioData }) => {
  if (audioData) playAudio(audioData)
})

// Listen for transcribed text from the user and assistant
voice.on('writing', ({ text, role, generationStage }) => {
  console.log(`${role} (${generationStage ?? 'FINAL'}): ${text}`)
})

// Stream microphone audio in real time
const microphoneStream = getMicrophoneStream()
await voice.send(microphoneStream)

// Disconnect when done
voice.close()
```

## 입증 {/* #authentication */}

`NovaSonicVoice`다음이 없을 때 AWS SDK 자격 증명 확인 체인을 사용합니다.`credentials` option is passed. Mastra calls `defaultProvider()` from `@aws-sdk/credential-provider-node`, which checks (in order) environment variables, shared credentials files, IAM role for EC2, ECS, EKS, and other standard sources.

정적 자격 증명을 사용하려면 생성자에 전달하세요.

```typescript
new NovaSonicVoice({
  region: 'us-east-1',
  credentials: {
    accessKeyId: process.env.AWS_ACCESS_KEY_ID!,
    secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY!,
    sessionToken: process.env.AWS_SESSION_TOKEN,
  },
})
```

음성 제공자는 자격 증명 값을 기록하지 않습니다.

## 구성 {/* #configuration */}

### 생성자 옵션 {/* #constructor-options */}

<PropertiesTable
  content={[
{
  name: 'region',
  type: "'us-east-1' | 'us-west-2' | 'ap-northeast-1'",
  description: 'AWS region that hosts the Nova Sonic model.',
  isOptional: true,
  defaultValue: "'us-east-1'",
},
{
  name: 'model',
  type: 'string',
  description: 'Bedrock model ID for the bidirectional stream.',
  isOptional: true,
  defaultValue: "'amazon.nova-2-sonic-v1:0'",
},
{
  name: 'credentials',
  type: 'AwsCredentialIdentity',
  description: 'Static AWS credentials. When omitted the default AWS credential provider chain is used.',
  isOptional: true,
},
{
  name: 'speaker',
  type: 'string | NovaSonicVoiceConfigDetails',
  description:
    "Default voice for the assistant. Pass a voice ID string such as 'matthew' or an object that includes a language code and gender.",
  isOptional: true,
  defaultValue: "'matthew'",
},
{
  name: 'languageCode',
  type: 'NovaSonicLanguageCode',
  description: 'Language code used for the session. Polyglot voices support all listed languages.',
  isOptional: true,
},
{
  name: 'instructions',
  type: 'string',
  description: 'System prompt sent at session start. Equivalent to calling addInstructions() before connect().',
  isOptional: true,
},
{
  name: 'tools',
  type: 'NovaSonicToolConfig[]',
  description:
    "Tools exposed to the model. When the voice instance is attached to an Agent, the Agent's tools are added automatically.",
  isOptional: true,
},
{
  name: 'sessionConfig',
  type: 'NovaSonicSessionConfig',
  description: 'Inference, turn-detection, and tool-choice configuration. See Session configuration below.',
  isOptional: true,
},
{
  name: 'debug',
  type: 'boolean',
  description: 'Enable verbose logging for stream events. Sensitive fields are masked.',
  isOptional: true,
  defaultValue: 'false',
},
]}
/>

### 세션 구성 {/* #session-configuration */}

`sessionConfig`추론 매개변수와 순서대로 동작을 제어합니다. 모든 필드는 선택 사항입니다.

<PropertiesTable
  content={[
{
  name: 'inferenceConfiguration',
  type: 'object',
  description: 'Sampling and decoding parameters.',
  isOptional: true,
  properties: [
    {
      type: 'object',
      parameters: [
        { name: 'maxTokens', type: 'number', description: 'Maximum tokens generated per turn.', isOptional: true },
        { name: 'temperature', type: 'number', description: 'Sampling temperature.', isOptional: true },
        { name: 'topP', type: 'number', description: 'Nucleus sampling probability.', isOptional: true },
        { name: 'topK', type: 'number', description: 'Top-k sampling.', isOptional: true },
        { name: 'stopSequences', type: 'string[]', description: 'Sequences that end generation.', isOptional: true },
      ],
    },
  ],
},
{
  name: 'turnDetectionConfiguration',
  type: 'object',
  description: 'Endpointing sensitivity for turn detection.',
  isOptional: true,
  properties: [
    {
      type: 'object',
      parameters: [
        {
          name: 'endpointingSensitivity',
          type: "'HIGH' | 'MEDIUM' | 'LOW'",
          description:
            'Pause duration before the model considers a turn complete. HIGH ends turns fastest (about 1.5s pause), MEDIUM is balanced (about 1.75s), LOW waits longest (about 2s).',
          isOptional: true,
        },
      ],
    },
  ],
},
{
  name: 'toolChoice',
  type: "'auto' | 'any' | { tool: { name: string } }",
  description: 'How the model decides whether to call a tool.',
  isOptional: true,
},
{
  name: 'enableKnowledgeGrounding',
  type: 'boolean',
  description: 'Enable retrieval-augmented grounding against a Bedrock knowledge base.',
  isOptional: true,
},
{
  name: 'knowledgeBaseConfig',
  type: '{ knowledgeBaseId?: string; dataSourceId?: string }',
  description: 'Knowledge base used when knowledge grounding is enabled.',
  isOptional: true,
},
]}
/>

## 행동 양식 {/* #methods */}

### `connect()` {/* #connect */}

AWS Bedrock에 대한 양방향 스트림을 열고 초기 세션, Prompt 및 시스템 이벤트를 보냅니다. 이 전에 전화해`speak`, `listen`, or `send`.

<PropertiesTable
  content={[
{
  name: 'options',
  type: '{ requestContext?: RequestContext }',
  description: 'Optional request context propagated to tool calls made during the session.',
  isOptional: true,
},
]}
/>

보고:`Promise<void>`

### `speak()` {/* #speak */}

텍스트 Prompt에 대한 음성을 합성하여 내보냅니다.`speaking` events as audio is produced.

<PropertiesTable
  content={[
{
  name: 'input',
  type: 'string | NodeJS.ReadableStream',
  description: 'Text or text stream to synthesize.',
  isOptional: false,
},
{
  name: 'options',
  type: 'NovaSonicVoiceOptions',
  description: 'Per-call overrides such as the speaker or language code.',
  isOptional: true,
},
]}
/>

보고:`Promise<void>`

### `send()` {/* #send */}

마이크 오디오(또는 모든 PCM 소스)를 Model로 스트리밍합니다. 실시간으로 지속적인 대화를 하려면 이 기능을 사용하세요.

<PropertiesTable
  content={[
{
  name: 'audioData',
  type: 'NodeJS.ReadableStream | Int16Array',
  description: '16-bit PCM audio to forward to the model.',
  isOptional: false,
},
]}
/>

보고:`Promise<void>`

### `listen()` {/* #listen */}

위임하는 편의 래퍼`send()`. Use it when you want a single transcription pass over a finite audio stream.

<PropertiesTable
  content={[
{
  name: 'audioData',
  type: 'NodeJS.ReadableStream',
  description: 'Audio stream to transcribe.',
  isOptional: false,
},
]}
/>

보고:`Promise<void>`

### `endAudioInput()` {/* #endaudioinput */}

Model이 응답을 마무리할 수 있도록 현재 오디오 턴의 끝을 신호로 보냅니다. 사용자가 말하기를 멈추고 공급자가 서버 측 회전 감지에 대해 구성되지 않은 경우 이 호출을 호출하세요.

보고:`Promise<void>`

### `addInstructions()` {/* #addinstructions */}

활성 세션에 대한 시스템 Prompt를 업데이트합니다.

<PropertiesTable
  content={[
{
  name: 'instructions',
  type: 'string',
  description: 'System prompt to apply to the session.',
  isOptional: true,
},
]}
/>

보고:`void`

### `addTools()` {/* #addtools */}

음성 인스턴스에 Tool를 등록합니다. 언제`NovaSonicVoice` is attached to an Agent, the Agent's tools are added automatically.

<PropertiesTable
  content={[
{
  name: 'tools',
  type: 'ToolsInput',
  description: 'Tools exposed to the model.',
  isOptional: true,
},
]}
/>

보고:`void`

### `getSpeakers()` {/* #getspeakers */}

Nova 2 Sonic에서 지원하는 음성 목록을 반환합니다.

보고:`Promise<Array<{ voiceId: string; name: string; language: string; locale: string; gender: 'masculine' | 'feminine'; polyglot: boolean }>>`

### `getListener()` {/* #getlistener */}

음성 인스턴스가 현재 공개 스트림을 보유하고 있는지 여부를 반환합니다.

보고:`Promise<{ enabled: boolean }>`

### `close()` {/* #close */}

양방향 스트림을 닫고 기본 Bedrock 클라이언트를 삭제합니다. 대화가 끝나면 이것을 호출하세요.

보고:`void`

### `on()` / `off()` {/* #on--off */}

이벤트 리스너를 등록하고 제거합니다. 보다[Voice events](./voice.events) for the shared event API.

## 이벤트 {/* #events */}

`NovaSonicVoice`다음 이벤트를 내보냅니다.

<PropertiesTable
  content={[
{
  name: 'speaking',
  type: 'event',
  description: 'Assistant audio chunk. Callback receives { audioData: Int16Array, sampleRate?: number }.',
},
{
  name: 'writing',
  type: 'event',
  description:
    "Transcribed text from the user or assistant. Callback receives { text: string, role: 'assistant' | 'user', generationStage?: 'SPECULATIVE' | 'FINAL' }.",
},
{
  name: 'toolCall',
  type: 'event',
  description:
    'Model requested a tool call. Callback receives { name: string, args: Record<string, any>, id: string }.',
},
{
  name: 'interrupt',
  type: 'event',
  description:
    "User or model interrupted the current turn. Callback receives { type: 'user' | 'model', timestamp: number }.",
},
{
  name: 'turnComplete',
  type: 'event',
  description: 'Model finished its turn. Callback receives { timestamp: number }.',
},
{
  name: 'session',
  type: 'event',
  description:
    "Session state transition. Callback receives { state: 'connecting' | 'connected' | 'disconnected' | 'disconnecting' | 'error' }.",
},
{
  name: 'usage',
  type: 'event',
  description:
    'Token usage for the turn. Callback receives { inputTokens: number, outputTokens: number, totalTokens: number }.',
},
{
  name: 'error',
  type: 'event',
  description: 'Stream or provider error. Callback receives { message: string, code?: string, details?: unknown }.',
},
]}
/>

`generationStage`임시 성적표를 구별합니다(`'SPECULATIVE'`) from finalized ones (`'FINAL'`). Use `'FINAL'` text for persistent storage and `'SPECULATIVE'` text for live captions.

## 사용 가능한 음성 {/* #available-voices */}

Nova 2 Sonic은 10개 지역에서 음성을 제공합니다. Tiffany와 Matthew는 다중 언어를 구사하며 지원되는 모든 언어를 구사할 수 있습니다.

| 음성 ID | 이름 | 언어 | 로케일 | 성별 | 다중언어 |
| - | - | - | - | - | - |
|`tiffany` | Tiffany | English | en-US | feminine | yes |
| `matthew` | Matthew | English | en-US | masculine | yes |
| `amy` | Amy | English | en-GB | feminine | no |
| `olivia` | Olivia | English | en-AU | feminine | no |
| `kiara` | Kiara | English | en-IN | feminine | no |
| `arjun` | Arjun | English | en-IN | masculine | no |
| `ambre` | Ambre | French | fr-FR | feminine | no |
| `florian` | Florian | French | fr-FR | masculine | no |
| `beatrice` | Beatrice | Italian | it-IT | feminine | no |
| `lorenzo` | Lorenzo | Italian | it-IT | masculine | no |
| `tina` | Tina | German | de-DE | feminine | no |
| `lennart` | Lennart | German | de-DE | masculine | no |
| `lupe` | Lupe | Spanish | es-US | feminine | no |
| `carlos` | Carlos | Spanish | es-US | masculine | no |
| `carolina` | Carolina | Portuguese | pt-BR | feminine | no |
| `leo` | Leo | Portuguese | pt-BR | masculine | no |
| `kiara` | Kiara | Hindi | hi-IN | feminine | no |
| `arjun` | Arjun | Hindi | hi-IN | masculine | no |

## 메모 {/* #notes */}

* 오디오는 16비트 PCM으로 스트리밍됩니다. 어시스턴트 오디오는 다음과 같이 방출됩니다.`Int16Array` on the `speaking` event.
* 음성 인스턴스는 호출해야 합니다.`connect()` before any other streaming method.
* `close()`근본을 파괴한다`BedrockRuntimeClient` to release the HTTP/2 session.
* Nova 2 Sonic은 다음에서 사용할 수 있습니다.`us-east-1`, `us-west-2`, and `ap-northeast-1`. Other regions throw a configuration error during construction.
