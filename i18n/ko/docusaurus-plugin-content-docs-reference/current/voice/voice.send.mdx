---
title: "참조: voice.send() | 목소리"
description: "지속적인 처리를 위해 오디오 데이터를 스트리밍하는 실시간 음성 공급자에서 사용할 수 있는 send() 메서드에 대한 문서입니다."
packages:
  - "@mastra/node-audio"
  - "@mastra/node-speaker"
  - "@mastra/voice-openai-realtime"
---

# 음성.전송()

그만큼`send()`방법은 지속적인 처리를 위해 오디오 데이터를 실시간으로 음성 공급자에게 스트리밍합니다. 이 방법은 실시간 음성 대 음성 대화에 필수적이며 마이크 입력을 AI 서비스에 직접 보낼 수 있습니다.

## 사용예 {/* #usage-example */}

```typescript
import { OpenAIRealtimeVoice } from '@mastra/voice-openai-realtime'
import Speaker from '@mastra/node-speaker'
import { getMicrophoneStream } from '@mastra/node-audio'

const speaker = new Speaker({
  sampleRate: 24100, // Audio sample rate in Hz - standard for high-quality audio on MacBook Pro
  channels: 1, // Mono audio output (as opposed to stereo which would be 2)
  bitDepth: 16, // Bit depth for audio quality - CD quality standard (16-bit resolution)
})

// Initialize a real-time voice provider
const voice = new OpenAIRealtimeVoice({
  realtimeConfig: {
    model: 'gpt-5.1-realtime',
    apiKey: process.env.OPENAI_API_KEY,
  },
})

// Connect to the real-time service
await voice.connect()

// Set up event listeners for responses
voice.on('writing', ({ text, role }) => {
  console.log(`${role}: ${text}`)
})

voice.on('speaker', stream => {
  stream.pipe(speaker)
})

// Get microphone stream (implementation depends on your environment)
const microphoneStream = getMicrophoneStream()

// Send audio data to the voice provider
await voice.send(microphoneStream)

// You can also send audio data as Int16Array
const audioBuffer = getAudioBuffer() // Assume this returns Int16Array
await voice.send(audioBuffer)
```

## 매개변수 {/* #parameters */}

<br />

<PropertiesTable
  content={[
{
  name: 'audioData',
  type: 'NodeJS.ReadableStream | Int16Array',
  description:
    'Audio data to send to the voice provider. Can be a readable stream (like a microphone stream) or an Int16Array of audio samples.',
  isOptional: false,
},
]}
/>

## 반환 값 {/* #return-value */}

다음을 반환합니다.`Promise<void>` that resolves when the audio data has been accepted by the voice provider.

## 메모 {/* #notes */}

* 이 방법은 음성-음성 기능을 지원하는 실시간 음성 공급자에 의해서만 구현됩니다.
* 이 기능을 지원하지 않는 음성 Provider에서 호출하면 경고를 기록하고 즉시 해결됩니다.
* 당신은 전화해야합니다`connect()` before using `send()` to establish the WebSocket connection
* 오디오 형식 요구 사항은 특정 음성 Provider에 따라 다릅니다.
* 지속적인 대화를 위해 일반적으로 전화를 겁니다.`send()` to transmit user audio, then `answer()` to trigger the AI response
* 공급자는 일반적으로 오디오를 처리할 때 전사된 텍스트와 함께 '쓰기' 이벤트를 내보냅니다.
* AI가 응답하면 공급자는 오디오 응답과 함께 '말하기' 이벤트를 내보냅니다.
