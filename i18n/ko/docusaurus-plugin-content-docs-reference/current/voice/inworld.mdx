---
title: "참고: 인월드 | 목소리"
description: "Inworld AI를 사용하여 스트리밍 텍스트 음성 변환 및 일괄 음성 텍스트 변환 기능을 제공하는 InworldVoice 클래스에 대한 문서입니다."
packages:
  - "@mastra/voice-inworld"
---

# 인월드

Mastra의 Inworld 음성 구현은 Inworld AI의 API를 사용하여 스트리밍 TTS(텍스트 음성 변환) 및 일괄 음성 텍스트 변환(STT) 기능을 제공합니다. 여러 TTS 및 STT Model, 구성 가능한 오디오 인코딩 및 프로그레시브 오디오 스트리밍을 지원합니다.

실시간 전이중 음성 대 음성의 경우 동일한 패키지를 내보냅니다.[`InworldRealtimeVoice`](/reference/voice/inworld-realtime).

## 사용예 {/* #usage-example */}

```typescript
import { InworldVoice } from '@mastra/voice-inworld'

// Initialize with default configuration (uses INWORLD_API_KEY environment variable)
const voice = new InworldVoice()

// Initialize with custom configuration
const voice = new InworldVoice({
  speechModel: {
    name: 'inworld-tts-2',
    apiKey: 'your-api-key',
  },
  listeningModel: {
    name: 'groq/whisper-large-v3',
    apiKey: 'your-api-key',
  },
  speaker: 'Dennis',
})

// Text-to-Speech (streaming)
const audioStream = await voice.speak('Hello, world!')

// Speech-to-Text
const transcript = await voice.listen(audioStream)
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'speechModel',
  type: 'InworldVoiceConfig',
  description: 'Configuration for text-to-speech functionality.',
  isOptional: true,
  defaultValue: "{ name: 'inworld-tts-2' }",
  properties: [
    {
      type: 'InworldVoiceConfig',
      parameters: [
        {
          name: 'name',
          type: "'inworld-tts-2' | 'inworld-tts-1.5-max' | 'inworld-tts-1.5-mini'",
          description: 'The Inworld TTS model to use.',
          isOptional: true,
          defaultValue: "'inworld-tts-2'",
        },
        {
          name: 'apiKey',
          type: 'string',
          description: 'Inworld API key. Falls back to INWORLD_API_KEY environment variable.',
          isOptional: true,
        },
      ],
    },
  ],
},
{
  name: 'listeningModel',
  type: 'InworldListeningConfig',
  description: 'Configuration for speech-to-text functionality.',
  isOptional: true,
  defaultValue: "{ name: 'groq/whisper-large-v3' }",
  properties: [
    {
      type: 'InworldListeningConfig',
      parameters: [
        {
          name: 'name',
          type: "'groq/whisper-large-v3'",
          description: 'The Inworld STT model to use.',
          isOptional: true,
          defaultValue: "'groq/whisper-large-v3'",
        },
        {
          name: 'apiKey',
          type: 'string',
          description: 'Inworld API key. Falls back to INWORLD_API_KEY environment variable.',
          isOptional: true,
        },
      ],
    },
  ],
},
{
  name: 'speaker',
  type: 'string',
  description: 'Default voice ID to use for text-to-speech.',
  isOptional: true,
  defaultValue: "'Dennis'",
},
{
  name: 'audioEncoding',
  type: "'LINEAR16' | 'MP3' | 'OGG_OPUS' | 'ALAW' | 'MULAW' | 'FLAC' | 'PCM' | 'WAV'",
  description: 'Default audio encoding for TTS output.',
  isOptional: true,
  defaultValue: "'MP3'",
},
{
  name: 'sampleRateHertz',
  type: 'number',
  description: 'Default sample rate for TTS output.',
  isOptional: true,
  defaultValue: '48000',
},
{
  name: 'language',
  type: 'string',
  description: 'Default BCP-47 language code for STT.',
  isOptional: true,
  defaultValue: "'en-US'",
},
]}
/>

## 행동 양식 {/* #methods */}

### `speak(input, options?)` {/* #speakinput-options */}

Inworld의 스트리밍 TTS 엔드포인트를 사용하여 텍스트를 음성으로 변환합니다. 오디오 청크가 도착하면 점진적으로 내보내는 읽기 가능한 스트림을 반환합니다.

```typescript
const audioStream = await voice.speak('Hello, world!', {
  speaker: 'Olivia',
  audioEncoding: 'WAV',
  sampleRateHertz: 24000,
  speakingRate: 1.2,
  temperature: 0.8,
})
```

<PropertiesTable
  content={[
{
  name: 'input',
  type: 'string | NodeJS.ReadableStream',
  description: 'Text to convert to speech. If a stream is provided, it will be converted to text first.',
  isOptional: false,
},
{
  name: 'options',
  type: 'InworldSpeakOptions',
  description: 'Additional options for speech synthesis.',
  isOptional: true,
  properties: [
    {
      type: 'InworldSpeakOptions',
      parameters: [
        {
          name: 'speaker',
          type: 'string',
          description: 'Override the default speaker for this request.',
          isOptional: true,
        },
        {
          name: 'audioEncoding',
          type: 'AudioEncoding',
          description: 'Override the default audio encoding.',
          isOptional: true,
        },
        {
          name: 'sampleRateHertz',
          type: 'number',
          description: 'Override the default sample rate.',
          isOptional: true,
        },
        {
          name: 'speakingRate',
          type: 'number',
          description: 'Adjust the speaking rate.',
          isOptional: true,
        },
        {
          name: 'temperature',
          type: 'number',
          description:
            'Controls voice variability. Honored on `inworld-tts-1.5-*` models; ignored by `inworld-tts-2`.',
          isOptional: true,
        },
        {
          name: 'deliveryMode',
          type: "'STABLE' | 'BALANCED' | 'CREATIVE'",
          description: 'Steering control for delivery style. Only honored by `inworld-tts-2`.',
          isOptional: true,
        },
        {
          name: 'language',
          type: 'string',
          description: 'BCP-47 language code for this request. Auto-detected when omitted.',
          isOptional: true,
        },
      ],
    },
  ],
},
]}
/>

**보고:** `Promise<NodeJS.ReadableStream>`

### `listen(input, options?)` {/* #listeninput-options */}

Inworld의 일괄 STT 엔드포인트를 사용하여 음성을 텍스트로 변환합니다.

```typescript
const transcript = await voice.listen(audioStream, {
  audioEncoding: 'MP3',
  sampleRateHertz: 44100,
  language: 'ja-JP',
})
```

<PropertiesTable
  content={[
{
  name: 'input',
  type: 'NodeJS.ReadableStream',
  description: 'Audio stream to transcribe.',
  isOptional: false,
},
{
  name: 'options',
  type: 'InworldListenOptions',
  description: 'Additional options for transcription.',
  isOptional: true,
  properties: [
    {
      type: 'InworldListenOptions',
      parameters: [
        {
          name: 'audioEncoding',
          type: "'LINEAR16' | 'MP3' | 'OGG_OPUS' | 'FLAC' | 'AUTO_DETECT'",
          description: 'Audio encoding of the input stream.',
          isOptional: true,
          defaultValue: "'AUTO_DETECT'",
        },
        {
          name: 'sampleRateHertz',
          type: 'number',
          description: 'Sample rate of the input audio.',
          isOptional: true,
          defaultValue: '16000',
        },
        {
          name: 'language',
          type: 'string',
          description: 'BCP-47 language code for transcription.',
          isOptional: true,
          defaultValue: "'en-US'",
        },
        {
          name: 'numberOfChannels',
          type: 'number',
          description: 'Number of audio channels in the input.',
          isOptional: true,
          defaultValue: '1',
        },
      ],
    },
  ],
},
]}
/>

**보고:** `Promise<string>`

### `getSpeakers()` {/* #getspeakers */}

Inworld API에서 사용 가능한 음성 목록을 반환합니다.

```typescript
const speakers = await voice.getSpeakers()
// [{ voiceId: 'Dennis', name: 'Dennis', language: 'en', description: '...', tags: ['friendly'], source: 'SYSTEM' }, ...]
```

**보고:** `Promise<Array<{ voiceId: string; name: string; language: string; description: string; tags: string[]; source: string }>>`

## 메모 {/* #notes */}

* TTS 엔드포인트는 프로그레시브 NDJSON 스트리밍을 사용하므로 전체 응답이 수신되기 전에 오디오 재생을 시작할 수 있습니다.
* API 키는 다음을 통해 제공될 수 있습니다.`speechModel` or `listeningModel` config, or the `INWORLD_API_KEY` environment variable. TTS and STT keys are resolved independently: passing distinct `speechModel.apiKey` and `listeningModel.apiKey` values lets each service use its own credential. If only one is provided, it's reused for both services as a fallback before the env var.
* `inworld-tts-2`기본 플래그십 Model입니다. 사용`deliveryMode` (`STABLE` | `BALANCED` | `CREATIVE`) to steer delivery style on this model. The `temperature` option is ignored on `inworld-tts-2`.
* 그만큼`inworld-tts-1.5-mini` model offers lower latency at the cost of reduced voice quality compared to `inworld-tts-1.5-max`.
