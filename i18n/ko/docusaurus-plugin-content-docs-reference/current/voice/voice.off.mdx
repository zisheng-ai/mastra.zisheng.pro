---
title: "참조: voice.off() | 목소리"
description: "음성 이벤트에 대한 이벤트 리스너를 제거하는 음성 제공자에서 사용할 수 있는 off() 메서드에 대한 문서입니다."
packages:
  - "@mastra/voice-openai-realtime"
---

# 음성.오프()

그만큼`off()`메소드는 이전에 등록된 이벤트 리스너를 제거합니다.`on()`방법. 이는 실시간 음성 기능을 갖춘 장기 실행 애플리케이션에서 리소스를 정리하고 Memory 누수를 방지하는 데 특히 유용합니다.

## 사용예 {/* #usage-example */}

```typescript
import { OpenAIRealtimeVoice } from '@mastra/voice-openai-realtime'
import chalk from 'chalk'

// Initialize a real-time voice provider
const voice = new OpenAIRealtimeVoice({
  realtimeConfig: {
    model: 'gpt-5.1-realtime',
    apiKey: process.env.OPENAI_API_KEY,
  },
})

// Connect to the real-time service
await voice.connect()

// Define the callback function
const writingCallback = ({ text, role }) => {
  if (role === 'user') {
    process.stdout.write(chalk.green(text))
  } else {
    process.stdout.write(chalk.blue(text))
  }
}

// Register event listener
voice.on('writing', writingCallback)

// Later, when you want to remove the listener
voice.off('writing', writingCallback)
```

## 매개변수 {/* #parameters */}

<br />

<PropertiesTable
  content={[
{
  name: 'event',
  type: 'string',
  description: "Name of the event to stop listening for (e.g., 'speaking', 'writing', 'error')",
  isOptional: false,
},
{
  name: 'callback',
  type: 'function',
  description: 'The same callback function that was passed to on()',
  isOptional: false,
},
]}
/>

## 반환 값 {/* #return-value */}

이 메서드는 값을 반환하지 않습니다.

## 메모 {/* #notes */}

* 콜백이 다음으로 전달되었습니다.`off()` must be the same function reference that was passed to `on()`
* 콜백을 찾을 수 없으면 메서드는 아무런 효과가 없습니다.
* 이 방법은 주로 이벤트 기반 통신을 지원하는 실시간 음성 공급자와 함께 사용됩니다.
* 이벤트를 지원하지 않는 음성 Provider에서 호출되면 경고를 기록하고 아무 작업도 수행하지 않습니다.
* 장기 실행 애플리케이션에서 Memory 누수를 방지하려면 이벤트 리스너를 제거하는 것이 중요합니다.
