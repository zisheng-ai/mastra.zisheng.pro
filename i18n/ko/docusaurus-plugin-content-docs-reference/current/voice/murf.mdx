---
title: "참조: 머프 | 목소리"
description: "텍스트 음성 변환 기능을 제공하는 Murf 음성 구현에 대한 문서입니다."
packages:
  - "@mastra/voice-murf"
---

# 머프

Mastra의 Murf 음성 구현은 Murf의 AI 음성 서비스를 사용하여 텍스트 음성 변환(TTS) 기능을 제공합니다. 다양한 언어에 걸쳐 다양한 음성을 지원합니다.

## 사용예 {/* #usage-example */}

```typescript
import { MurfVoice } from '@mastra/voice-murf'

// Initialize with default configuration (uses MURF_API_KEY environment variable)
const voice = new MurfVoice()

// Initialize with custom configuration
const voice = new MurfVoice({
  speechModel: {
    name: 'GEN2',
    apiKey: 'your-api-key',
    properties: {
      format: 'MP3',
      rate: 1.0,
      pitch: 1.0,
      sampleRate: 48000,
      channelType: 'STEREO',
    },
  },
  speaker: 'en-US-cooper',
})

// Text-to-Speech with default settings
const audioStream = await voice.speak('Hello, world!')

// Text-to-Speech with custom properties
const audioStream = await voice.speak('Hello, world!', {
  speaker: 'en-UK-hazel',
  properties: {
    format: 'WAV',
    rate: 1.2,
    style: 'casual',
  },
})

// Get available voices
const voices = await voice.getSpeakers()
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'speechModel',
  type: 'MurfConfig',
  description: '텍스트-음성 기능의 구성',
  isOptional: true,
  defaultValue: "{ name: 'GEN2' }",
  properties: [    {
      type: 'MurfConfig',
      parameters: [
        {
          name: 'name',
          type: "'GEN1' | 'GEN2'",
          description: '사용할 Murf Model 세대',
          isOptional: false,
          defaultValue: "'GEN2'",
        },        {
          name: 'apiKey',
          type: 'string',
          description: 'Murf API 키입니다. 지정하지 않으면 MURF_API_KEY 환경 변수를 사용합니다',
          isOptional: true,
        },        {
          name: 'properties',
          type: 'object',
          description: '모든 음성 합성 요청에 적용할 기본 속성',
          isOptional: true,
          properties: [            {
              type: 'object',
              parameters: [
                {
                  name: 'style',
                  type: 'string',
                  description: '음성의 말하기 스타일',
                  isOptional: true,
                },                {
                  name: 'rate',
                  type: 'number',
                  description: '말하기 속도 배율',
                  isOptional: true,
                },                {
                  name: 'pitch',
                  type: 'number',
                  description: '음성 피치 조정값',
                  isOptional: true,
                },                {
                  name: 'sampleRate',
                  type: '8000 | 24000 | 44100 | 48000',
                  description: 'Hz 단위의 오디오 샘플링 속도',
                  isOptional: true,
                },                {
                  name: 'format',
                  type: "'MP3' | 'WAV' | 'FLAC' | 'ALAW' | 'ULAW'",
                  description: '출력 오디오 형식',
                  isOptional: true,
                },                {
                  name: 'channelType',
                  type: "'STEREO' | 'MONO'",
                  description: '오디오 채널 구성',
                  isOptional: true,
                },                {
                  name: 'pronunciationDictionary',
                  type: 'Record<string, string>',
                  description: '사용자 정의 발음 매핑',
                  isOptional: true,
                },                {
                  name: 'encodeAsBase64',
                  type: 'boolean',
                  description: '오디오를 Base64로 인코딩할지 여부',
                  isOptional: true,
                },                {
                  name: 'variation',
                  type: 'number',
                  description: '음성 변형 매개변수',
                  isOptional: true,
                },                {
                  name: 'audioDuration',
                  type: 'number',
                  description: '초 단위의 목표 오디오 길이',
                  isOptional: true,
                },                {
                  name: 'multiNativeLocale',
                  type: 'string',
                  description: '다국어 지원에 사용할 로캘',
                  isOptional: true,
                },
              ],
            },
          ],
        },
      ],
    },
  ],
},{
  name: 'speaker',
  type: 'string',
  description: '텍스트-음성 변환에 사용할 기본 음성 ID',
  isOptional: true,
  defaultValue: "'en-UK-hazel'",
},
]}
/>
## 행동 양식 {/* #methods */}

### `speak()` {/* #speak */}

Murf의 API를 사용하여 텍스트를 음성으로 변환합니다.

<PropertiesTable
  content={[
{
  name: 'input',
  type: 'string | NodeJS.ReadableStream',
  description: '음성으로 변환할 텍스트입니다. 스트림을 제공하면 먼저 텍스트로 변환됩니다.',
  isOptional: false,
},{
  name: 'options',
  type: 'object',
  description: '음성 합성 옵션',
  isOptional: true,
  properties: [    {
      type: 'object',
      parameters: [
        {
          name: 'speaker',
          type: 'string',
          description: '이 요청의 기본 화자를 재정의합니다',
          isOptional: true,
        },        {
          name: 'properties',
          type: 'object',
          description: '이 요청의 기본 음성 속성을 재정의합니다',
          isOptional: true,
        },
      ],
    },
  ],
},
]}
/>
보고:`Promise<NodeJS.ReadableStream>`

### `getSpeakers()` {/* #getspeakers */}

각 노드에 다음이 포함된 사용 가능한 음성 옵션의 배열을 반환합니다.

<PropertiesTable
  content={[
{
  name: 'voiceId',
  type: 'string',
  description: '음성의 고유 식별자',
  isOptional: false,
},{
  name: 'name',
  type: 'string',
  description: '음성의 표시 이름',
  isOptional: false,
},{
  name: 'language',
  type: 'string',
  description: '음성의 언어 코드',
  isOptional: false,
},{
  name: 'gender',
  type: 'string',
  description: '음성의 성별',
  isOptional: false,
},
]}
/>
### `listen()` {/* #listen */}

이 방법은 Murf에서 지원되지 않으며 오류가 발생합니다. Murf는 음성-텍스트 기능을 제공하지 않습니다.

## 중요 사항 {/* #important-notes */}

1. Murf API 키가 필요합니다. `MURF_API_KEY` 환경 변수를 통해 설정하거나 생성자에 전달하세요.
2. 이 서비스는 GEN2를 기본 Model 버전으로 사용합니다.
3. 음성 속성은 생성자 수준에서 설정하고 요청별로 재정의할 수 있습니다.
4. 이 서비스는 형식, 샘플링 속도, 채널 유형과 같은 속성을 통해 광범위한 오디오 사용자 정의를 지원합니다.
5. 음성-텍스트 기능은 지원되지 않습니다.