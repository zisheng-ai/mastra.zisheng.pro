---
title: "참조: Perplexity Tool | Tool 및 MCP"
description: "Mastra Agent를 위한 Perplexity 검색 Tool를 제공하는 @mastra/perplexity에 대한 API 참조입니다."
packages:
  - "@mastra/perplexity"
---

# 난해함 Tool

그만큼`@mastra/perplexity`패키지는[혼란 검색 API](https://docs.perplexity.ai/docs/search/quickstart)Mastra 호환 Tool로 사용됩니다. 다음으로 생성된 Tool를 반환하는 팩토리 함수를 노출합니다.[`createTool()`](./create-tool)그리고 전체 Zod 입력/출력 스키마.

Perplexity로 구동되는 채팅 완료 또는 Agent Workflow의 경우 Mastra의 내장 기능을 사용하세요.[Perplexity model provider](/models/providers/perplexity) or [Perplexity Agent provider](/models/providers/perplexity-agent). Those are separate from this Search tool.

## 설치 {/* #installation */}

Zod와 함께 패키지를 설치합니다.

```sh npm2yarn
npm install @mastra/perplexity zod
```

## 사용예 {/* #usage-example */}

다음 예에서는 기본 구성을 사용하여 검색 Tool를 만듭니다. 기본적으로 Tool는 다음을 읽습니다.`PERPLEXITY_API_KEY` (with `PPLX_API_KEY` as a fallback) from the environment. Pass `{ apiKey }` explicitly to override.

```typescript title="src/mastra/tools/index.ts"
import { createPerplexitySearchTool } from '@mastra/perplexity'

const searchTool = createPerplexitySearchTool()
```

API 키를 명시적으로 전달하려면 다음 안내를 따르세요.

```typescript title="src/mastra/tools/index.ts"
import { createPerplexitySearchTool } from '@mastra/perplexity'

const searchTool = createPerplexitySearchTool({ apiKey: 'pplx-...' })
```

## 구성 {/* #configuration */}

모든 공장 ​​기능은`PerplexityClientOptions` object:

<PropertiesTable
  content={[
{
  name: 'apiKey',
  type: 'string',
  description:
    'Perplexity API key. Falls back to the `PERPLEXITY_API_KEY` then `PPLX_API_KEY` environment variables.',
  isOptional: true,
},
{
  name: 'baseUrl',
  type: 'string',
  description: 'Override the API base URL.',
  isOptional: true,
  defaultValue: "'https://api.perplexity.ai'",
},
{
  name: 'fetch',
  type: 'typeof fetch',
  description: 'Custom `fetch` implementation. Useful for tests, retries, or instrumentation.',
  isOptional: true,
},
]}
/>

## 행동 양식 {/* #methods */}

### 공장 기능 {/* #factory-functions */}

#### `createPerplexityTools(config?)` {/* #createperplexitytoolsconfig */}

제공된 구성을 공유하는 모든 Perplexity Tool가 포함된 개체를 반환합니다.

```typescript
import { createPerplexityTools } from '@mastra/perplexity'

const tools = createPerplexityTools({ apiKey: 'pplx-...' })
// tools.perplexitySearch
```

보고:`{ perplexitySearch }`

#### `createPerplexitySearchTool(config?)` {/* #createperplexitysearchtoolconfig */}

Perplexity Search API를 사용하여 웹을 검색하는 Tool를 만듭니다. 제목, URL, 스니펫 및 선택적 게시 날짜와 함께 순위가 매겨진 결과를 반환합니다.

Tool는 ID로 등록됩니다`perplexity-search`.

```typescript
import { createPerplexitySearchTool } from '@mastra/perplexity'

const searchTool = createPerplexitySearchTool()
```

##### 입력 {/* #input */}

<PropertiesTable
  content={[
{
  name: 'query',
  type: 'string',
  description: 'The search query.',
},
{
  name: 'maxResults',
  type: 'number',
  description: 'Maximum number of results to return (1-20).',
  isOptional: true,
},
{
  name: 'searchDomainFilter',
  type: 'string[]',
  description:
    'Restrict (or exclude) results by domain. Prefix a domain with `-` to exclude it (for example, `-pinterest.com`). Do not mix allow- and deny-list entries in the same call.',
  isOptional: true,
},
{
  name: 'searchRecencyFilter',
  type: "'hour' | 'day' | 'week' | 'month' | 'year'",
  description: 'Only return results from within the given recency window.',
  isOptional: true,
},
{
  name: 'searchAfterDateFilter',
  type: 'string',
  description: 'Only return results published on or after this date. Format: m/d/yyyy.',
  isOptional: true,
},
{
  name: 'searchBeforeDateFilter',
  type: 'string',
  description: 'Only return results published on or before this date. Format: m/d/yyyy.',
  isOptional: true,
},
]}
/>

##### 산출 {/* #output */}

<PropertiesTable
  content={[
{
  name: 'query',
  type: 'string',
  description: 'The original search query.',
},
{
  name: 'results',
  type: 'SearchResult[]',
  description: 'Array of search results.',
  properties: [
    {
      type: 'SearchResult',
      parameters: [
        { name: 'title', type: 'string', description: 'Result title.' },
        { name: 'url', type: 'string', description: 'Result URL.' },
        { name: 'snippet', type: 'string', description: 'Content snippet.' },
        { name: 'date', type: 'string', description: 'Publication date when available.', isOptional: true },
      ],
    },
  ],
},
]}
/>

## Agent 예시 {/* #agent-example */}

다음 예에서는 응답하기 전에 새로운 웹 결과를 가져올 수 있도록 Agent에 검색 Tool를 등록합니다.

```typescript title="src/mastra/agents/index.ts"
import { Agent } from '@mastra/core/agent'
import { createPerplexitySearchTool } from '@mastra/perplexity'

const agent = new Agent({
  id: 'research-agent',
  name: 'Research Agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  instructions:
    'You are a research assistant. Use the perplexity-search tool to find up-to-date information from the web before answering.',
  tools: {
    search: createPerplexitySearchTool(),
  },
})
```

## 환경변수 {/* #environment-variables */}

| 변수 | 설명 |
| - | - |
|`PERPLEXITY_API_KEY` | Your Perplexity API key. Used as the default when `apiKey` isn't passed to a factory function. |
| `PPLX_API_KEY` | Fallback used when `PERPLEXITY_API_KEY` is unset. |

## 관련된 {/* #related */}

* [`createTool()`](./create-tool)
* [Perplexity 검색 빠른 시작](https://docs.perplexity.ai/docs/search/quickstart)
* [Perplexity Agent 빠른 시작](https://docs.perplexity.ai/docs/agent/quickstart)
* [Perplexity Model 제공자](/models/providers/perplexity)
* [Perplexity Agent 제공자](/models/providers/perplexity-agent)
