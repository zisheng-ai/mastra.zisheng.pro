---
title: "참조: createCodeMode() | Tool 및 MCP"
description: "샌드박스에서 다중 Tool 계산을 실행하는 데 사용되는 createCodeMode()에 대한 API 참조입니다."
packages:
  - "@mastra/core"
---

# createCodeMode()

**추가된 항목:** `@mastra/core@1.38.0`

`createCodeMode()` 함수는 Agent가 여러 Tool을 사용하는 계산을 하나의 TypeScript 함수로 실행할 수 있도록 Tool과 생성된 지침을 반환합니다. 생성된 코드는 Workspace Sandbox에서 실행되며, 각 `external_*` 호출은 유효성 검사, 요청 컨텍스트 및 Tracing을 거쳐 호스트에서 실제 Tool을 실행합니다.
개념적 개요는 다음을 참조하세요.[Code mode](/docs/agents/code-mode).

## 사용예 {/* #usage-example */}

코드 모드 Tool를 만들고, 생성된 지침을 Agent에 추가하고, 반환된 Tool를 동일한 ID로 등록합니다.

```typescript title="src/mastra/agents/shop-agent.ts"
import { Agent } from '@mastra/core/agent'
import { createCodeMode, createTool } from '@mastra/core/tools'
import { LocalSandbox } from '@mastra/core/workspace'
import { z } from 'zod'

const getTopProducts = createTool({
  id: 'getTopProducts',
  description: 'Get top selling products',
  inputSchema: z.object({ limit: z.number() }),
  outputSchema: z.object({
    products: z.array(z.object({ id: z.string(), name: z.string(), totalSales: z.number() })),
  }),
  execute: async ({ limit }) => fetchTopProducts(limit),
})

const getProductRatings = createTool({
  id: 'getProductRatings',
  description: 'Get ratings for a product',
  inputSchema: z.object({ productId: z.string() }),
  outputSchema: z.object({ ratings: z.array(z.object({ score: z.number() })) }),
  execute: async ({ productId }) => fetchRatings(productId),
})

const { tool, instructions } = createCodeMode({
  tools: { getTopProducts, getProductRatings },
  sandbox: new LocalSandbox(),
})

export const shopAgent = new Agent({
  id: 'shop-assistant',
  name: 'shop-assistant',
  instructions: ['You are a helpful shopping assistant.', instructions],
  model: '__GATEWAY_OPENAI_MODEL__',
  tools: { execute_typescript: tool },
})
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'config',
  type: 'CodeModeConfig',
  description: '코드 모드 Tool 및 생성되는 지침의 구성입니다.',
  properties: [    {
      type: 'CodeModeConfig',
      parameters: [
        {
          name: 'tools',
          type: 'ToolsInput',
          description:
            '생성된 코드에 `external_<id>` 함수로 노출되는 Tool입니다. 이 Tool만 호출할 수 있습니다.',
        },        {
          name: 'sandbox',
          type: 'WorkspaceSandbox',
          description:
            '생성된 코드를 실행하는 데 사용되는 Sandbox입니다. Agent가 Sandbox를 제공하는 Workspace에서 실행되거나 전송 계층이 자체 실행 경계(예: `IsolatedVmCodeModeTransport`)를 제공하지 않는 한 필수입니다. 호스트에서 명시적으로 실행하려면 `new LocalSandbox()`를 전달하세요.',
          isOptional: true,
        },        {
          name: 'timeout',
          type: 'number',
          description: '실행 제한 시간(밀리초)입니다.',
          isOptional: true,
          defaultValue: '30000',
        },        {
          name: 'id',
          type: 'string',
          description: '생성되는 Tool ID입니다.',
          isOptional: true,
          defaultValue: 'execute_typescript',
        },
      ],
    },
  ],
},{
  name: 'transport',
  type: 'CodeModeTransport',
  description:
    'Sandbox에서 생성된 코드를 실행하는 데 사용할 선택적 전송 구현입니다. 기본 전송은 Workspace Sandbox 프로세스 API를 통해 stdio JSON-RPC를 사용합니다. `requiresSandbox: false`를 선언하는 전송(예: [IsolatedVmCodeModeTransport](/reference/tools/isolated-vm-transport))은 Sandbox 없이 실행됩니다.',
  isOptional: true,
},
]}
/>
## 보고 {/* #returns */}

다음을 반환합니다.`CodeModeResult` object.

<PropertiesTable
  content={[
{
  name: 'tool',
  type: 'Tool<any, any>',
  description: '생성된 코드 모드 Tool입니다. 기본 ID는 `execute_typescript`입니다.',
},{
  name: 'instructions',
  type: 'string',
  description: '구성된 Tool에 대한 형식 지정된 `external_*` 선언이 포함된 생성된 Model 지침입니다.',
},
]}
/>
## 코드모드Tool결과 {/* #codemodetoolresult */}

생성된 Tool는`CodeModeToolResult`.

<PropertiesTable
  content={[
{
  name: 'success',
  type: 'boolean',
  description: '생성된 코드가 예외를 발생시키지 않고 완료될 때까지 실행되었는지 여부입니다.',
},{
  name: 'result',
  type: 'unknown',
  description: '생성된 코드가 반환한 값입니다.',
  isOptional: true,
},{
  name: 'logs',
  type: 'string[]',
  description:
    '`console.log`, `console.info`, `console.warn`, `console.error`에서 캡처한 콘솔 출력을 순서대로 나열합니다.',
  isOptional: true,
},{
  name: 'error',
  type: '{ message: string; name?: string; line?: number }',
  description: '생성된 코드가 예외를 발생시키거나 실행에 실패한 경우의 오류 세부 정보입니다.',
  isOptional: true,
  properties: [    {
      type: '{ message: string; name?: string; line?: number }',
      parameters: [        {
          name: 'message',
          type: 'string',
          description: '오류 메시지입니다.',
        },        {
          name: 'name',
          type: 'string',
          description: '사용 가능한 경우 오류 이름입니다.',
          isOptional: true,
        },        {
          name: 'line',
          type: 'number',
          description: '사용 가능한 경우 실패와 관련된 줄 번호입니다.',
          isOptional: true,
        },
      ],
    },
  ],
},
]}
/>
## 검사 지침 {/* #inspecting-instructions */}

Tool 집합에 대해 생성되는 정확한 Prompt 내용을 확인하려면 `createCodeModeInstructions()`를 사용하세요.
```typescript title="src/mastra/tools/code-mode-instructions.ts"
import { createCodeModeInstructions } from '@mastra/core/tools'

console.log(
  createCodeModeInstructions({
    tools: { getTopProducts, getProductRatings },
  }),
)
```

지침에는 사용 계약과 구성된 각 Tool에 대해 형식이 지정된 `declare function external_<id>(...)` 줄이 포함됩니다. Tool ID는 유효한 TypeScript 함수 이름으로 정리되며, 정리된 이름이 충돌하면 오류가 발생합니다.
## createCodeModeTool() {/* #createcodemodetool */}

Tool만 필요하고 지침을 별도로 관리하려면 `createCodeModeTool()`을 사용하세요. 대부분의 Agent는 Tool과 일치하는 지침을 함께 유지할 수 있도록 `createCodeMode()`을 사용해야 합니다.
```typescript title="src/mastra/tools/code-mode-tool.ts"
import { createCodeModeTool } from '@mastra/core/tools'
import { LocalSandbox } from '@mastra/core/workspace'

export const codeModeTool = createCodeModeTool({
  tools: { getTopProducts, getProductRatings },
  sandbox: new LocalSandbox(),
})
```

## 관련된 {/* #related */}

* [코드 모드](/docs/agents/code-mode)
* [생성Tool()](/reference/tools/create-tool)
* [작업공간 개요](/docs/workspace/overview)
