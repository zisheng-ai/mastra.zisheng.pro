---
title: "참고: Tavily Tool | Tool 및 MCP"
description: "Mastra Agent를 위한 웹 검색, 추출, 크롤링 및 매핑 Tool를 제공하는 @mastra/tavily에 대한 API 참조입니다."
packages:
  - "@mastra/tavily"
---

# 타빌리 Tool

그만큼`@mastra/tavily`패키지는[타빌리](https://app.tavily.com)Mastra 호환 Tool로서의 API. 검색, 추출, 크롤링 및 매핑을 위한 팩토리 기능을 노출합니다. 각 함수는 다음으로 생성된 Tool를 반환합니다.[`createTool()`](./create-tool)여기에는 전체 Zod 입력/출력 스키마가 포함됩니다.

## 설치 {/* #installation */}

```sh npm2yarn
npm install @mastra/tavily @tavily/core zod
```

## 빠른 시작 {/* #quick-start */}

공유 구성이 적용된 네 가지 Tool을 모두 가져오려면 `createTavilyTools()`를 사용하세요.
```typescript title="src/mastra/tools/index.ts"
import { createTavilyTools } from '@mastra/tavily'

const tools = createTavilyTools()
// Or pass an explicit API key:
// const tools = createTavilyTools({ apiKey: 'tvly-...' })
```

각 Tool를 개별적으로 만들 수도 있습니다.

```typescript title="src/mastra/tools/index.ts"
import { createTavilySearchTool, createTavilyExtractTool } from '@mastra/tavily'

const searchTool = createTavilySearchTool()
const extractTool = createTavilyExtractTool({ apiKey: 'tvly-...' })
```

기본적으로 모든 Tool은 환경에서 `TAVILY_API_KEY`를 읽습니다. 재정의하려면 `{ apiKey }`를 명시적으로 전달할 수 있습니다.
## 구성 {/* #configuration */}

모든 팩토리 함수는 `@tavily/core`의 `TavilyClientOptions`를 허용합니다.
<PropertiesTable
  content={[
{
  name: 'apiKey',
  type: 'string',
  description: 'Tavily API 키입니다. 지정하지 않으면 `TAVILY_API_KEY` 환경 변수를 사용합니다.',
  isOptional: true,
},{
  name: 'clientName',
  type: 'string',
  description: '각 요청의 `X-Client-Name` 헤더로 전송되는 출처 표시 문자열입니다.',
  isOptional: true,
  defaultValue: "'mastra'",
},{
  name: 'apiBaseURL',
  type: 'string',
  description: 'Tavily API의 기본 URL입니다.',
  isOptional: true,
},{
  name: 'proxies',
  type: 'object',
  description: '내부 HTTP 클라이언트에 전달되는 프록시 구성입니다.',
  isOptional: true,
},{
  name: 'projectId',
  type: 'string',
  description: '요청 범위를 지정하는 Tavily 프로젝트 ID입니다.',
  isOptional: true,
},
]}
/>
## `createTavilyTools()` {/* #createtavilytools */}

공유 구성이 있는 네 가지 Tool가 모두 포함된 개체를 반환합니다.

```typescript
import { createTavilyTools } from '@mastra/tavily'

const tools = createTavilyTools({ apiKey: 'tvly-...' })
// tools.tavilySearch, tools.tavilyExtract, tools.tavilyCrawl, tools.tavilyMap
```

**보고:** `{ tavilySearch, tavilyExtract, tavilyCrawl, tavilyMap }`

## `createTavilySearchTool()` {/* #createtavilysearchtool */}

Tavily를 사용하여 웹을 검색하는 Tool를 만듭니다. 콘텐츠 조각, 선택적 AI 생성 답변 및 이미지와 함께 관련 결과를 반환합니다.

**Tool ID:** `tavily-search`

```typescript
import { createTavilySearchTool } from '@mastra/tavily'

const searchTool = createTavilySearchTool()
```

### 입력 {/* #input */}

<PropertiesTable
  content={[
{
  name: 'query',
  type: 'string',
  description: '검색 쿼리입니다.',
},{
  name: 'searchDepth',
  type: "'basic' | 'advanced' | 'fast' | 'ultra-fast'",
  description:
    "검색 깊이입니다. 표준 결과에는 'basic', 더 철저한 결과에는 'advanced', 짧은 지연 시간에는 'fast'/'ultra-fast'를 사용하세요.",
  isOptional: true,
},{
  name: 'maxResults',
  type: 'number',
  description: '반환할 최대 결과 수입니다(1~20).',
  isOptional: true,
},{
  name: 'includeAnswer',
  type: "boolean | 'basic' | 'advanced'",
  description: 'AI가 생성한 답변 요약을 포함합니다.',
  isOptional: true,
},{
  name: 'includeImages',
  type: 'boolean',
  description: '응답에 쿼리 관련 이미지를 포함합니다.',
  isOptional: true,
},{
  name: 'includeImageDescriptions',
  type: 'boolean',
  description: '반환된 이미지의 설명을 포함합니다.',
  isOptional: true,
},{
  name: 'includeRawContent',
  type: "false | 'markdown' | 'text'",
  description: "각 결과에서 정리된 HTML 콘텐츠를 포함합니다. 비활성화하려면 false를 전달하고, 형식을 지정하려면 'markdown'/'text'를 전달하세요.",
  isOptional: true,
},{
  name: 'includeDomains',
  type: 'string[]',
  description: '결과를 지정된 도메인으로 제한합니다.',
  isOptional: true,
},{
  name: 'excludeDomains',
  type: 'string[]',
  description: '지정된 도메인의 결과를 제외합니다.',
  isOptional: true,
},{
  name: 'timeRange',
  type: "'day' | 'week' | 'month' | 'year'",
  description: '최신성에 따라 결과를 필터링합니다.',
  isOptional: true,
},
]}
/>
### 산출 {/* #output */}

<PropertiesTable
  content={[
{
  name: 'query',
  type: 'string',
  description: '원래 검색 쿼리입니다.',
},{
  name: 'answer',
  type: 'string',
  description: 'AI가 생성한 답변 요약입니다.',
  isOptional: true,
},{
  name: 'images',
  type: '{ url: string; description?: string }[]',
  description: '관련 이미지입니다.',
  isOptional: true,
},{
  name: 'results',
  type: 'SearchResult[]',
  description: '검색 결과 배열입니다.',
  properties: [    {
      type: 'SearchResult',
      parameters: [
        { name: 'title', type: 'string', description: 'Result title.' },
        { name: 'url', type: 'string', description: 'Result URL.' },
        { name: 'content', type: 'string', description: 'Content snippet.' },
        { name: 'score', type: 'number', description: 'Relevance score.' },
        { name: 'rawContent', type: 'string', description: 'Full-page content (when requested).', isOptional: true },
      ],
    },
  ],
},
{
  name: 'responseTime',
  type: 'number',
  description: '서버 응답 시간(초)입니다.',
},
]}
/>
## `createTavilyExtractTool()` {/* #createtavilyextracttool */}

하나 이상의 URL에서 콘텐츠를 추출하는 Tool를 만듭니다. 요청당 최대 20개의 URL을 사용하여 원시 페이지 콘텐츠를 마크다운 또는 텍스트 형식으로 반환합니다.

**Tool ID:** `tavily-extract`

```typescript
import { createTavilyExtractTool } from '@mastra/tavily'

const extractTool = createTavilyExtractTool()
```

### 입력 {/* #input-1 */}

<PropertiesTable
  content={[
{
  name: 'urls',
  type: 'string[]',
  description: '콘텐츠를 추출할 URL입니다(1~20개).',
},{
  name: 'extractDepth',
  type: "'basic' | 'advanced'",
  description: "추출 깊이입니다. 표와 삽입된 콘텐츠를 가져오려면 'advanced'를 사용하세요.",
  isOptional: true,
},{
  name: 'query',
  type: 'string',
  description: '추출된 콘텐츠 청크를 관련성에 따라 재정렬하는 데 사용할 사용자 의도입니다.',
  isOptional: true,
},{
  name: 'includeImages',
  type: 'boolean',
  description: '페이지에서 추출한 이미지를 포함합니다.',
  isOptional: true,
},{
  name: 'format',
  type: "'markdown' | 'text'",
  description: '추출된 콘텐츠의 출력 형식입니다.',
  isOptional: true,
  defaultValue: "'markdown'",
},
]}
/>
### 산출 {/* #output-1 */}

<PropertiesTable
  content={[
{
  name: 'results',
  type: 'ExtractResult[]',
  description: '성공적으로 추출된 페이지입니다.',
  properties: [    {
      type: 'ExtractResult',
      parameters: [
        { name: 'url', type: 'string', description: 'Page URL.' },
        { name: 'rawContent', type: 'string', description: 'Extracted page content.' },
        { name: 'images', type: 'string[]', description: 'Extracted image URLs.', isOptional: true },
      ],
    },
  ],
},
{
  name: 'failedResults',
  type: 'FailedResult[]',
  description: '추출에 실패한 URL입니다.',
  properties: [    {
      type: 'FailedResult',
      parameters: [
        { name: 'url', type: 'string', description: 'Failed URL.' },
        { name: 'error', type: 'string', description: 'Error message.' },
      ],
    },
  ],
},
{
  name: 'responseTime',
  type: 'number',
  description: '서버 응답 시간(초)입니다.',
},
]}
/>
## `createTavilyCrawlTool()` {/* #createtavilycrawltool */}

URL에서 시작하여 웹사이트를 크롤링하는 Tool를 만듭니다. 구성 가능한 깊이, 너비 및 도메인 제약 조건을 사용하여 검색된 페이지에서 콘텐츠를 추출합니다.

**Tool ID:** `tavily-crawl`

```typescript
import { createTavilyCrawlTool } from '@mastra/tavily'

const crawlTool = createTavilyCrawlTool()
```

### 입력 {/* #input-2 */}

<PropertiesTable
  content={[
{
  name: 'url',
  type: 'string',
  description: '크롤링을 시작할 루트 URL입니다.',
},{
  name: 'maxDepth',
  type: 'number',
  description: '기본 URL부터 적용되는 최대 크롤링 깊이입니다.',
  isOptional: true,
},{
  name: 'maxBreadth',
  type: 'number',
  description: '페이지마다 따라갈 최대 링크 수입니다.',
  isOptional: true,
},{
  name: 'limit',
  type: 'number',
  description: '크롤러가 중지하기 전까지 처리할 전체 페이지 수입니다.',
  isOptional: true,
},{
  name: 'instructions',
  type: 'string',
  description: '크롤러에 제공할 자연어 지침입니다.',
  isOptional: true,
},{
  name: 'selectPaths',
  type: 'string[]',
  description: '특정 URL 경로를 선택하는 정규식 패턴입니다.',
  isOptional: true,
},{
  name: 'selectDomains',
  type: 'string[]',
  description: '특정 도메인으로 제한하는 정규식 패턴입니다.',
  isOptional: true,
},{
  name: 'excludePaths',
  type: 'string[]',
  description: '특정 URL 경로를 제외하는 정규식 패턴입니다.',
  isOptional: true,
},{
  name: 'excludeDomains',
  type: 'string[]',
  description: '특정 도메인을 제외하는 정규식 패턴입니다.',
  isOptional: true,
},{
  name: 'allowExternal',
  type: 'boolean',
  description: '외부 도메인 링크를 따라갈지 여부입니다.',
  isOptional: true,
},{
  name: 'extractDepth',
  type: "'basic' | 'advanced'",
  description: "추출 깊이입니다. 표와 삽입된 콘텐츠를 가져오려면 'advanced'를 사용하세요.",
  isOptional: true,
},{
  name: 'includeImages',
  type: 'boolean',
  description: '크롤링한 페이지의 이미지를 포함합니다.',
  isOptional: true,
},{
  name: 'format',
  type: "'markdown' | 'text'",
  description: '추출된 콘텐츠의 출력 형식입니다.',
  isOptional: true,
  defaultValue: "'markdown'",
},
]}
/>
### 산출 {/* #output-2 */}

<PropertiesTable
  content={[
{
  name: 'baseUrl',
  type: 'string',
  description: '크롤링한 루트 URL입니다.',
},{
  name: 'results',
  type: 'CrawlResult[]',
  description: '발견된 페이지에서 추출한 콘텐츠입니다.',
  properties: [    {
      type: 'CrawlResult',
      parameters: [
        { name: 'url', type: 'string', description: 'Page URL.' },
        { name: 'rawContent', type: 'string', description: 'Extracted page content.' },
        { name: 'images', type: 'string[]', description: 'Image URLs found on the page.', isOptional: true },
      ],
    },
  ],
},
{
  name: 'responseTime',
  type: 'number',
  description: '서버 응답 시간(초)입니다.',
},
]}
/>
## `createTavilyMapTool()` {/* #createtavilymaptool */}

URL에서 시작하여 웹사이트 구조를 매핑하는 Tool를 만듭니다. 페이지 콘텐츠를 추출하지 않고 URL 목록을 검색하고 반환합니다. 이를 사용하여 대상 추출 전에 사이트 구조를 이해합니다.

**Tool ID:** `tavily-map`

```typescript
import { createTavilyMapTool } from '@mastra/tavily'

const mapTool = createTavilyMapTool()
```

### 입력 {/* #input-3 */}

<PropertiesTable
  content={[
{
  name: 'url',
  type: 'string',
  description: '매핑을 시작할 루트 URL입니다.',
},{
  name: 'maxDepth',
  type: 'number',
  description: '기본 URL부터 적용되는 최대 매핑 깊이입니다.',
  isOptional: true,
},{
  name: 'maxBreadth',
  type: 'number',
  description: '페이지마다 따라갈 최대 링크 수입니다.',
  isOptional: true,
},{
  name: 'limit',
  type: 'number',
  description: '매퍼가 중지하기 전까지 처리할 전체 링크 수입니다.',
  isOptional: true,
},{
  name: 'instructions',
  type: 'string',
  description: '매퍼에 제공할 자연어 지침입니다.',
  isOptional: true,
},{
  name: 'selectPaths',
  type: 'string[]',
  description: '특정 URL 경로를 선택하는 정규식 패턴입니다.',
  isOptional: true,
},{
  name: 'selectDomains',
  type: 'string[]',
  description: '특정 도메인으로 제한하는 정규식 패턴입니다.',
  isOptional: true,
},{
  name: 'excludePaths',
  type: 'string[]',
  description: '특정 URL 경로를 제외하는 정규식 패턴입니다.',
  isOptional: true,
},{
  name: 'excludeDomains',
  type: 'string[]',
  description: '특정 도메인을 제외하는 정규식 패턴입니다.',
  isOptional: true,
},{
  name: 'allowExternal',
  type: 'boolean',
  description: '외부 도메인 링크를 포함할지 여부입니다.',
  isOptional: true,
},
]}
/>
### 산출 {/* #output-3 */}

<PropertiesTable
  content={[
{
  name: 'baseUrl',
  type: 'string',
  description: '매핑한 루트 URL입니다.',
},{
  name: 'results',
  type: 'string[]',
  description: '발견된 URL입니다.',
},{
  name: 'responseTime',
  type: 'number',
  description: '서버 응답 시간(초)입니다.',
},
]}
/>
## Agent 예시 {/* #agent-example */}

다음 예에서는 검색과 추출을 결합하는 연구 Agent를 보여줍니다.

```typescript title="src/mastra/agents/index.ts"
import { Agent } from '@mastra/core/agent'
import { createTavilySearchTool, createTavilyExtractTool } from '@mastra/tavily'

const agent = new Agent({
  id: 'web-search-agent',
  name: 'Web Search Agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  instructions:
    'You are a web search assistant. Use search tool to find relevant pages, then use extract tool to get full content from the best results.',
  tools: {
    search: createTavilySearchTool(),
    extract: createTavilyExtractTool(),
  },
})
```

## 환경변수 {/* #environment-variables */}

| 변수 | 설명 |
| - | - |
|`TAVILY_API_KEY` | Tavily API 키입니다. 팩토리 함수에 `apiKey`를 전달하지 않았을 때 기본값으로 사용됩니다. |
## 관련된 {/* #related */}

* [`createTool()`](./create-tool)
* [Tavily API 문서](https://docs.tavily.com)
