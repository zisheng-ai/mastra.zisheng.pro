---
title: "참조: Bright Data Tool | Tool 및 MCP"
description: "Bright Data의 SERP API 및 Web Unlocker가 지원하는 웹 검색 및 가져오기 Tool를 제공하는 @mastra/brightdata에 대한 API 참조입니다."
packages:
  - "@mastra/brightdata"
---

# 브라이트 데이터 Tool

그만큼`@mastra/brightdata`패키지는[브라이트 데이터 SDK](https://github.com/brightdata/bright-data-sdk-node)Mastra 호환 Tool로. 웹 검색 및 웹 가져오기를 위한 팩토리 기능을 노출합니다. 각 함수는 다음으로 생성된 Tool를 반환합니다.[`createTool()`](./create-tool)여기에는 전체 Zod 입력/출력 스키마가 포함됩니다.

검색 Tool는 Bright Data의 지원을 받습니다.[SERP API](https://brightdata.com/products/serp-api) and the fetch tool by [Web Unlocker](https://brightdata.com/products/web-unlocker). Both bypass bot detection and CAPTCHAs.

## 설치 {/* #installation */}

```sh npm2yarn
npm install @mastra/brightdata zod
```

## 빠른 시작 {/* #quick-start */}

사용`createBrightDataTools()` to get both tools with shared configuration:

```typescript title="src/mastra/tools/index.ts"
import { createBrightDataTools } from '@mastra/brightdata'

const { webSearch, webFetch } = createBrightDataTools()
// Or pass an explicit API key:
// const { webSearch, webFetch } = createBrightDataTools({ apiKey: 'brd-...' })
```

각 Tool를 개별적으로 만들 수도 있습니다.

```typescript title="src/mastra/tools/index.ts"
import { createBrightDataSearchTool, createBrightDataFetchTool } from '@mastra/brightdata'

const searchTool = createBrightDataSearchTool()
const fetchTool = createBrightDataFetchTool({ apiKey: 'brd-...' })
```

기본적으로 모든 Tool는 다음을 읽습니다.`BRIGHTDATA_API_TOKEN` from the environment. You can pass `{ apiKey }` explicitly to override.

## 구성 {/* #configuration */}

모든 공장 ​​기능이 허용됩니다.`BrightDataClientOptions` from `@brightdata/sdk`:

<PropertiesTable
  content={[
{
  name: 'apiKey',
  type: 'string',
  description: 'Bright Data API token. Falls back to the `BRIGHTDATA_API_TOKEN` environment variable.',
  isOptional: true,
},
]}
/>

에서 지원하는 추가 필드`bdclient` constructor (such as `timeout`, `webUnlockerZone`, `serpZone`, and `rateLimit`) can be passed through the same options object.

## `createBrightDataTools()` {/* #createbrightdatatools */}

공유 구성이 있는 두 Tool를 모두 포함하는 개체를 반환합니다.

```typescript
import { createBrightDataTools } from '@mastra/brightdata'

const tools = createBrightDataTools({ apiKey: 'brd-...' })
// tools.webSearch, tools.webFetch
```

**보고:** `{ webSearch, webFetch }`

## `createBrightDataSearchTool()` {/* #createbrightdatasearchtool */}

Bright Data의 SERP API를 통해 Google을 검색하는 Tool를 만듭니다. 구문 분석된 유기적 결과를 반환합니다.

**Tool ID:** `brightdata-search`

```typescript
import { createBrightDataSearchTool } from '@mastra/brightdata'

const searchTool = createBrightDataSearchTool()
```

### 입력 {/* #input */}

<PropertiesTable
  content={[
{
  name: 'query',
  type: 'string',
  description: 'The search query.',
},
{
  name: 'country',
  type: 'string',
  description: 'Two-letter country code for geo-targeted results (for example, `us` or `gb`).',
  isOptional: true,
},
{
  name: 'start',
  type: 'number',
  description: 'Result offset for pagination. For example, `10` returns the second page of 10 results.',
  isOptional: true,
},
]}
/>

### 산출 {/* #output */}

<PropertiesTable
  content={[
{
  name: 'query',
  type: 'string',
  description: 'The original search query.',
},
{
  name: 'results',
  type: 'SearchResult[]',
  description: 'Organic search results. Entries missing a link or title are filtered out.',
  properties: [
    {
      type: 'SearchResult',
      parameters: [
        { name: 'link', type: 'string', description: 'Result URL.' },
        { name: 'title', type: 'string', description: 'Result title.' },
        { name: 'description', type: 'string', description: 'Result snippet.' },
      ],
    },
  ],
},
{
  name: 'currentPage',
  type: 'number',
  description:
    'Page number returned by the SERP API. Defaults to `1` when the upstream response omits or returns a non-positive value.',
},
]}
/>

## `createBrightDataFetchTool()` {/* #createbrightdatafetchtool */}

Bright Data의 Web Unlocker를 통해 웹페이지를 가져오고 페이지 콘텐츠를 Markdown으로 반환하는 Tool를 만듭니다.

**Tool ID:** `brightdata-fetch`

```typescript
import { createBrightDataFetchTool } from '@mastra/brightdata'

const fetchTool = createBrightDataFetchTool()
```

### 입력 {/* #input-1 */}

<PropertiesTable
  content={[
{
  name: 'url',
  type: 'string',
  description: 'The URL to fetch. Must be a valid HTTP or HTTPS URL.',
},
]}
/>

### 산출 {/* #output-1 */}

<PropertiesTable
  content={[
{
  name: 'url',
  type: 'string',
  description: 'The input URL.',
},
{
  name: 'content',
  type: 'string',
  description: 'Page content as Markdown.',
},
]}
/>

## Agent 예시 {/* #agent-example */}

다음 예는 검색과 가져오기를 결합하는 연구 Agent를 보여줍니다.

```typescript title="src/mastra/agents/index.ts"
import { Agent } from '@mastra/core/agent'
import { createBrightDataTools } from '@mastra/brightdata'

const { webSearch, webFetch } = createBrightDataTools()

const agent = new Agent({
  id: 'research-agent',
  name: 'Research Agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  instructions:
    'You are a research assistant. Use the search tool to find relevant pages, then use the fetch tool to read full Markdown content from the best results.',
  tools: {
    webSearch,
    webFetch,
  },
})
```

## 환경변수 {/* #environment-variables */}

| 변수 | 설명 |
| - | - |
|`BRIGHTDATA_API_TOKEN` | Your Bright Data API token. Used as the default when `apiKey` isn't passed to a factory function. |

## 관련된 {/* #related */}

* [`createTool()`](./create-tool)
* [브라이트 데이터 SERP API](https://brightdata.com/products/serp-api)
* [브라이트 데이터 웹 언락커](https://brightdata.com/products/web-unlocker)
* [Node.js용 Bright Data SDK](https://github.com/brightdata/bright-data-sdk-node)
