---
title: "참조: MSSQL 스토리지 | 저장"
description: "Mastra의 MSSQL 스토리지 구현에 대한 문서입니다."
packages:
  - "@mastra/core"
  - "@mastra/mssql"
---

# MSSQL 스토리지

MSSQL 스토리지 구현은 Microsoft SQL Server 데이터베이스를 사용하여 프로덕션에 즉시 사용 가능한 스토리지 솔루션을 제공합니다.

## 설치 {/* #installation */}

```bash npm2yarn
npm install @mastra/mssql@latest
```

## 용법 {/* #usage */}

```typescript
import { MSSQLStore } from '@mastra/mssql'

const storage = new MSSQLStore({
  id: 'mssql-storage',
  connectionString: process.env.DATABASE_URL,
})
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'connectionString',
  type: 'string',
  description:
    'MSSQL connection string (e.g., Server=localhost,1433;Database=mydb;User Id=sa;Password=password;Encrypt=true;TrustServerCertificate=true)',
  isOptional: false,
},
{
  name: 'schemaName',
  type: 'string',
  description: 'The name of the schema you want the storage to use. Will use the default schema if not provided.',
  isOptional: true,
},
]}
/>

## 생성자 예 {/* #constructor-examples */}

인스턴스화할 수 있습니다.`MSSQLStore` in the following ways:

```ts
import { MSSQLStore } from '@mastra/mssql'

// Using a connection string only
const store1 = new MSSQLStore({
  id: 'mssql-storage-1',
  connectionString:
    'Server=localhost,1433;Database=mydb;User Id=sa;Password=password;Encrypt=true;TrustServerCertificate=true',
})

// Using a connection string with a custom schema name
const store2 = new MSSQLStore({
  id: 'mssql-storage-2',
  connectionString:
    'Server=localhost,1433;Database=mydb;User Id=sa;Password=password;Encrypt=true;TrustServerCertificate=true',
  schemaName: 'custom_schema', // optional
})

// Using individual connection parameters
const store4 = new MSSQLStore({
  id: 'mssql-storage-3',
  server: 'localhost',
  port: 1433,
  database: 'mydb',
  user: 'user',
  password: 'password',
})

// Individual parameters with schemaName
const store5 = new MSSQLStore({
  id: 'mssql-storage-4',
  server: 'localhost',
  port: 1433,
  database: 'mydb',
  user: 'user',
  password: 'password',
  schemaName: 'custom_schema', // optional
})
```

## 추가 참고사항 {/* #additional-notes */}

### 스키마 관리 {/* #schema-management */}

저장소 구현은 스키마 생성 및 업데이트를 자동으로 처리합니다. 다음 테이블이 생성됩니다.

* `mastra_workflow_snapshot`: Workflow 상태 및 실행 데이터를 저장합니다.
* `mastra_evals`: 평가 결과 및 메타데이터를 저장합니다.
* `mastra_threads`: 대화 스레드를 저장합니다.
* `mastra_messages`: 개별 메시지를 저장합니다.
* `mastra_traces`: 원격 측정 및 추적 데이터를 저장합니다.
* `mastra_scorers`: 점수 및 평가 데이터를 저장합니다.
* `mastra_resources`: 자원 작업 Memory 데이터를 저장합니다.

### 초기화 {/* #initialization */}

Mastra 클래스에 스토리지를 전달하면,`init()` is called automatically before any storage operation:

```typescript
import { Mastra } from '@mastra/core'
import { MSSQLStore } from '@mastra/mssql'

const storage = new MSSQLStore({
  connectionString: process.env.DATABASE_URL,
})

const mastra = new Mastra({
  storage, // init() is called automatically
})
```

Mastra 없이 직접 저장소를 사용하는 경우 다음을 호출해야 합니다.`init()` explicitly to create the tables:

```typescript
import { MSSQLStore } from '@mastra/mssql'

const storage = new MSSQLStore({
  id: 'mssql-storage',
  connectionString: process.env.DATABASE_URL,
})

// Required when using storage directly
await storage.init()

// Access domain-specific stores via getStore()
const memoryStore = await storage.getStore('memory')
const thread = await memoryStore?.getThreadById({ threadId: '...' })
```

:::경고
만약에`init()` isn't called, tables won't be created and storage operations will fail silently or throw errors.
:::

### 직접 데이터베이스 및 풀 액세스 {/* #direct-database-and-pool-access */}

`MSSQLStore`mssql 연결 풀을 공개 필드로 노출합니다.

```typescript
store.pool // mssql connection pool instance
```

직접 쿼리 및 사용자 정의 트랜잭션 관리를 지원합니다. 다음 필드를 사용하는 경우:

* 적절한 연결 및 거래 처리에 대한 책임은 귀하에게 있습니다.
* 매장 폐쇄 (`store.close()`) will destroy the associated connection pool.
* 직접 액세스는 MSSQLStore 메서드에서 제공하는 추가 논리 또는 유효성 검사를 우회합니다.

이 접근 방식은 낮은 수준의 액세스가 필요한 고급 시나리오를 위한 것입니다.
