---
title: "참조: Aurora DSQL 스토리지 | 저장"
description: "Mastra의 Amazon Aurora DSQL 스토리지 구현에 대한 설명서입니다."
packages:
  - "@mastra/core"
  - "@mastra/dsql"
  - "@mastra/memory"
---

# Aurora DSQL 스토리지

Aurora DSQL 스토리지 구현은 IAM 인증과 함께 Amazon Aurora DSQL을 사용하는 스토리지를 제공합니다.

Aurora DSQL은 PostgreSQL 확장 기능(`CREATE EXTENSION`), including `pgvector`. 벡터 저장의 경우 다음과 같은 별도의 벡터 저장소를 사용하십시오.`@mastra/s3vectors`.

## 설치 {/* #installation */}

```bash copy
npm install @mastra/dsql@beta
```

## 전제조건 {/* #prerequisites */}

* Amazon Aurora DSQL 클러스터
* DSQL 클러스터에 대한 액세스 권한이 있는 AWS 자격 증명(IAM 인증)

## 용법 {/* #usage */}

```typescript copy showLineNumbers
import { DSQLStore } from '@mastra/dsql'

const storage = new DSQLStore({
  id: 'my-dsql-store',
  host: 'abc123.dsql.us-east-1.on.aws',
  // region is auto-detected from host, or specify explicitly:
  // region: 'us-east-1',
  // user: 'admin', // default
  // database: 'postgres', // default
})

// Initialize the store (creates tables if needed)
await storage.init()
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'Unique identifier for this store instance',
  isOptional: false,
},
{
  name: 'host',
  type: 'string',
  description: 'DSQL cluster endpoint (e.g., abc123.dsql.us-east-1.on.aws)',
  isOptional: false,
},
{
  name: 'pool',
  type: 'pg.Pool',
  description:
    'Pre-configured pg.Pool instance. Use this for direct control over the connection pool. Cannot be used with host configuration.',
  isOptional: true,
},
{
  name: 'user',
  type: 'string',
  description: "Database user. Aurora DSQL's admin role is 'admin'.",
  isOptional: true,
  default: "'admin'",
},
{
  name: 'database',
  type: 'string',
  description: "Database name. Aurora DSQL exposes a single database named 'postgres' per cluster.",
  isOptional: true,
  default: "'postgres'",
},
{
  name: 'region',
  type: 'string',
  description: 'AWS region. Extracted from host if not provided.',
  isOptional: true,
},
{
  name: 'schemaName',
  type: 'string',
  description: 'PostgreSQL schema name where Mastra tables and indexes are created.',
  isOptional: true,
  default: "'public'",
},
{
  name: 'customCredentialsProvider',
  type: 'AwsCredentialIdentityProvider',
  description: 'Custom AWS credentials provider for IAM authentication.',
  isOptional: true,
},
{
  name: 'max',
  type: 'number',
  description: 'Maximum connections in the pool.',
  isOptional: true,
  default: '10',
},
{
  name: 'min',
  type: 'number',
  description: 'Minimum connections in the pool.',
  isOptional: true,
  default: '0',
},
{
  name: 'idleTimeoutMillis',
  type: 'number',
  description: 'Close idle connections after this many milliseconds.',
  isOptional: true,
  default: '600000',
},
{
  name: 'maxLifetimeSeconds',
  type: 'number',
  description:
    "Maximum connection lifetime in seconds. Must be less than 3600 due to Aurora DSQL's 60-minute connection limit.",
  isOptional: true,
  default: '3300',
},
{
  name: 'connectionTimeoutMillis',
  type: 'number',
  description: 'Connection acquisition timeout in milliseconds.',
  isOptional: true,
  default: '5000',
},
{
  name: 'allowExitOnIdle',
  type: 'boolean',
  description: 'Allow the process to exit when all connections are idle.',
  isOptional: true,
  default: 'true',
},
]}
/>

## 생성자 예 {/* #constructor-examples */}

인스턴스화할 수 있습니다.`DSQLStore` in the following ways:

```typescript copy showLineNumbers
import { DSQLStore } from '@mastra/dsql'

// Basic configuration (region auto-detected from host)
const store1 = new DSQLStore({
  id: 'my-dsql-store',
  host: 'abc123.dsql.us-east-1.on.aws',
})

// With explicit region and schema
const store2 = new DSQLStore({
  id: 'my-dsql-store',
  host: 'abc123.dsql.us-east-1.on.aws',
  region: 'us-east-1',
  schemaName: 'my_app',
})

// With custom credentials provider
import { fromNodeProviderChain } from '@aws-sdk/credential-providers'

const store3 = new DSQLStore({
  id: 'my-dsql-store',
  host: 'abc123.dsql.us-east-1.on.aws',
  customCredentialsProvider: fromNodeProviderChain(),
})

// With connection pool settings
const store4 = new DSQLStore({
  id: 'my-dsql-store',
  host: 'abc123.dsql.us-east-1.on.aws',
  max: 20,
  min: 2,
  idleTimeoutMillis: 300000,
  maxLifetimeSeconds: 3000,
  connectionTimeoutMillis: 10000,
})

// Using a pre-configured pg.Pool
import { Pool } from 'pg'
import { AuroraDSQLClient } from '@aws/aurora-dsql-node-postgres-connector'

const pool = new Pool({
  host: 'abc123.dsql.us-east-1.on.aws',
  Client: AuroraDSQLClient,
  region: 'us-east-1',
})

const store5 = new DSQLStore({
  id: 'my-dsql-store',
  pool,
})
```

## 추가 참고사항 {/* #additional-notes */}

### 스키마 관리 {/* #schema-management */}

저장소 구현은 스키마 생성 및 업데이트를 자동으로 처리합니다. 다음 테이블이 생성됩니다.

* `mastra_workflow_snapshot`: Workflow 상태 및 실행 데이터를 저장합니다.
* `mastra_threads`: 대화 스레드를 저장합니다.
* `mastra_messages`: 개별 메시지를 저장합니다.
* `mastra_ai_spans`: Observability을 위해 범위 데이터를 저장합니다.
* `mastra_scorers`: 점수 및 평가 데이터를 저장합니다.
* `mastra_resources`: 자원 작업 Memory 데이터를 저장합니다.
* `mastra_agents`: Agent 데이터를 저장합니다.

### 초기화 {/* #initialization */}

Mastra 클래스에 스토리지를 전달하면,`init()` is called automatically before any storage operation:

```typescript copy showLineNumbers
import { Mastra } from '@mastra/core'
import { DSQLStore } from '@mastra/dsql'

const storage = new DSQLStore({
  id: 'my-dsql-store',
  host: 'abc123.dsql.us-east-1.on.aws',
})

const mastra = new Mastra({
  storage, // init() is called automatically
})
```

Mastra 없이 직접 저장소를 사용하는 경우 다음을 호출해야 합니다.`init()` explicitly to create the tables:

```typescript copy showLineNumbers
import { DSQLStore } from '@mastra/dsql'

const storage = new DSQLStore({
  id: 'my-dsql-store',
  host: 'abc123.dsql.us-east-1.on.aws',
})

// Required when using storage directly
await storage.init()

// Access domain-specific stores via getStore()
const memoryStore = await storage.getStore('memory')
const thread = await memoryStore?.getThreadById({ threadId: '...' })
```

:::경고
만약에`init()` isn't called, tables won't be created and storage operations will fail silently or throw errors.
:::

### 직접 데이터베이스 및 풀 액세스 {/* #direct-database-and-pool-access */}

`DSQLStore`기본 데이터베이스 클라이언트와 pg.Pool 인스턴스를 모두 공개 필드로 노출합니다.

```typescript copy showLineNumbers
storage.db // Database client for executing queries
storage.pool // Underlying pg.Pool instance
```

직접 쿼리 및 사용자 정의 트랜잭션 관리를 지원합니다. 다음 필드를 사용하는 경우:

* 적절한 연결 및 거래 처리에 대한 책임은 귀하에게 있습니다.
* 매장 폐쇄 (`storage.close()`) will destroy the connection pool if it was created by the store.
* 직접 액세스는 DSQLStore 메서드에서 제공하는 추가 논리 또는 유효성 검사를 우회합니다.

이 접근 방식은 낮은 수준의 액세스가 필요한 고급 시나리오를 위한 것입니다.

### Aurora DSQL 세부 사항 {/* #aurora-dsql-specifics */}

#### IAM 전용 인증 {/* #iam-only-authentication */}

연결은 IAM으로 인증됩니다. 데이터베이스 비밀번호는 필요하지 않습니다.`@mastra/dsql` uses `@aws/aurora-dsql-node-postgres-connector` to generate short-lived auth tokens. You can provide a custom credentials provider via `customCredentialsProvider`.

#### 단일 데이터베이스, 스키마 기반 격리 {/* #single-database-schema-based-isolation */}

각 클러스터는 단일 데이터베이스를 노출합니다.`postgres`. Logical separation is done via schemas. The `schemaName` option controls where Mastra tables are created.

#### PostgreSQL 확장 없음 {/* #no-postgresql-extensions */}

`CREATE EXTENSION`지원되지 않습니다. 여기에는 다음이 포함됩니다`pgvector`, `PostGIS`, and others. For vector storage, use a separate store such as `@mastra/s3vectors` alongside `DSQLStore`.

#### JSON이 텍스트로 저장됨 {/* #json-stored-as-text */}

JSON/JSONB는 쿼리 유형으로 사용할 수 있지만 열 유형으로는 사용할 수 없습니다.`@mastra/dsql` stores structured fields (metadata, content, etc.) in `TEXT` columns and casts to JSON at query time.

#### 스키마 및 DDL 제약조건 {/* #schema-and-ddl-constraints */}

일부 PostgreSQL 기능은 사용할 수 없습니다.

* 외래 키 제약조건
* `TRUNCATE`
* 동기식`CREATE INDEX`

인덱스는 다음을 사용하여 비동기적으로 생성됩니다.`CREATE INDEX ASYNC`. The store's `init()` and index helper APIs respect these constraints.

#### 트랜잭션 및 낙관적 동시성 {/* #transactions-and-optimistic-concurrency */}

Aurora DSQL은 낙관적 동시성 제어(OCC)를 사용하며 경합이 있는 경우 재시도 가능한 OCC 오류를 반환할 수 있습니다. 거래 기간과 규모에 제한이 있습니다. 대규모 대량 작업은 애플리케이션 수준에서 더 작은 배치로 분할되어야 합니다.

#### 연결 수명 {/* #connection-lifetime */}

개별 연결은 약 60분으로 제한됩니다. 기본값`maxLifetimeSeconds: 3300` ensures connections are recycled before hitting this limit.

## 사용예 {/* #usage-example */}

### Agent에 Memory 추가 {/* #adding-memory-to-an-agent */}

Agent에 Aurora DSQL Memory를 추가하려면 다음을 사용하세요.`Memory` class and create a new `storage` key using `DSQLStore`. The `host` should point to your Aurora DSQL cluster endpoint.

```typescript title="src/mastra/agents/example-dsql-agent.ts" copy showLineNumbers
import { Memory } from '@mastra/memory'
import { Agent } from '@mastra/core/agent'
import { DSQLStore } from '@mastra/dsql'

export const dsqlAgent = new Agent({
  id: 'dsql-agent',
  name: 'DSQL Agent',
  instructions:
    'You are an AI agent with the ability to automatically recall memories from previous interactions.',
  model: '__GATEWAY_OPENAI_MODEL__',
  memory: new Memory({
    storage: new DSQLStore({
      id: 'dsql-agent-storage',
      host: process.env.DSQL_HOST!,
    }),
    options: {
      generateTitle: true, // Explicitly enable automatic title generation
    },
  }),
})
```

### Agent 사용 {/* #using-the-agent */}

사용`memoryOptions` to scope recall for this request. Set `lastMessages: 5` to limit recency-based recall, and use `semanticRecall` to fetch the `topK: 3` most relevant messages, including `messageRange: 2` neighboring messages for context around each match.

```typescript title="src/test-dsql-agent.ts" copy showLineNumbers
import 'dotenv/config'

import { mastra } from './mastra'

const threadId = '123'
const resourceId = 'user-456'

const agent = mastra.getAgent('dsql-agent')

const message = await agent.stream('My name is Mastra', {
  memory: {
    thread: threadId,
    resource: resourceId,
  },
})

await message.textStream.pipeTo(new WritableStream())

const stream = await agent.stream("What's my name?", {
  memory: {
    thread: threadId,
    resource: resourceId,
  },
  memoryOptions: {
    lastMessages: 5,
    semanticRecall: {
      topK: 3,
      messageRange: 2,
    },
  },
})

for await (const chunk of stream.textStream) {
  process.stdout.write(chunk)
}
```

## 인덱스 관리 {/* #index-management */}

Aurora DSQL 스토리지는 쿼리 성능을 최적화하는 인덱스 관리 기능을 제공합니다.

### 자동 성과 지표 {/* #automatic-performance-indexes */}

Aurora DSQL 스토리지는 일반적인 쿼리 패턴에 대해 초기화 중에 복합 인덱스를 자동으로 생성합니다.

* `mastra_threads_resourceid_createdat_idx`: (resourceId, 생성된 At)
* `mastra_messages_thread_id_createdat_idx`: (thread\_id, 생성된 위치)
* `mastra_ai_spans_traceid_startedat_idx`: (추적 ID, 시작 위치)
* `mastra_ai_spans_parentspanid_startedat_idx`: (parentSpanId, startAt)
* `mastra_ai_spans_name_idx`: (이름)
* `mastra_ai_spans_spantype_startedat_idx`: (범위 유형, 시작 위치)
* `mastra_scores_trace_id_span_id_created_at_idx`: (traceId,spanId,createdAt)

Aurora DSQL은 다음을 사용하여 이러한 인덱스를 비동기식으로 생성합니다.`CREATE INDEX ASYNC`. Because index creation is asynchronous, new indexes may not be immediately available after `init()`. The store will continue to function without them, but queries may be slower until index creation completes.

### 사용자 정의 색인 생성 {/* #creating-custom-indexes */}

특정 쿼리 패턴을 최적화하기 위해 추가 인덱스를 만듭니다.

```typescript copy showLineNumbers
await storage.createIndex({
  name: 'idx_threads_resource',
  table: 'mastra_threads',
  columns: ['resourceId'],
})

await storage.createIndex({
  name: 'idx_messages_composite',
  table: 'mastra_messages',
  columns: ['thread_id', 'createdAt'],
})
```

Aurora DSQL은 허용하지 않습니다.`ASC`/`DESC` in `CREATE INDEX ASYNC`. If you include them, they will be automatically stripped.

### 인덱스 옵션 {/* #index-options */}

<PropertiesTable
  content={[
{
  name: 'name',
  type: 'string',
  description: 'Unique name for the index',
  isOptional: false,
},
{
  name: 'table',
  type: 'string',
  description: "Table name (e.g., 'mastra_threads')",
  isOptional: false,
},
{
  name: 'columns',
  type: 'string[]',
  description: 'Array of column names. ASC/DESC modifiers are automatically stripped for Aurora DSQL compatibility.',
  isOptional: false,
},
{
  name: 'unique',
  type: 'boolean',
  description: 'Create a unique index.',
  isOptional: true,
  default: 'false',
},
{
  name: 'concurrent',
  type: 'boolean',
  description: 'Ignored in Aurora DSQL. Indexes are always created asynchronously.',
  isOptional: true,
},
{
  name: 'where',
  type: 'string',
  description: 'Partial index condition.',
  isOptional: true,
},
{
  name: 'method',
  type: 'string',
  description: 'Ignored in Aurora DSQL. Only btree indexes are supported.',
  isOptional: true,
},
{
  name: 'opclass',
  type: 'string',
  description: 'Ignored in Aurora DSQL.',
  isOptional: true,
},
{
  name: 'storage',
  type: 'Record<string, any>',
  description: 'Ignored in Aurora DSQL.',
  isOptional: true,
},
{
  name: 'tablespace',
  type: 'string',
  description: 'Ignored in Aurora DSQL. Tablespaces are not supported.',
  isOptional: true,
},
]}
/>

### 인덱스 관리 {/* #managing-indexes */}

기존 인덱스를 나열하고 모니터링합니다.

```typescript copy showLineNumbers
// List all indexes
const allIndexes = await storage.listIndexes()
console.log(allIndexes)
// [
//   {
//     name: 'mastra_threads_pkey',
//     table: 'mastra_threads',
//     columns: ['id'],
//     unique: true,
//     size: '16 KB',
//     definition: 'CREATE UNIQUE INDEX...'
//   },
//   ...
// ]

// List indexes for a specific table
const threadIndexes = await storage.listIndexes('mastra_threads')

// Get detailed statistics for an index
const stats = await storage.describeIndex('idx_threads_resource')
console.log(stats)
// {
//   name: 'idx_threads_resource',
//   table: 'mastra_threads',
//   columns: ['resourceId'],
//   unique: false,
//   size: '128 KB',
//   definition: 'CREATE INDEX idx_threads_resource...',
//   method: 'btree',
//   scans: 1542,
//   tuples_read: 45230,
//   tuples_fetched: 12050
// }

// Drop an index
await storage.dropIndex('idx_threads_status')
```

### 스키마별 인덱스 {/* #schema-specific-indexes */}

사용자 정의 스키마를 사용하면 스키마 접두사를 사용하여 인덱스가 생성됩니다.

```typescript copy showLineNumbers
const storage = new DSQLStore({
  id: 'my-dsql-store',
  host: 'abc123.dsql.us-east-1.on.aws',
  schemaName: 'custom_schema',
})

// Creates index as: custom_schema_idx_threads_status
await storage.createIndex({
  name: 'idx_threads_status',
  table: 'mastra_threads',
  columns: ['status'],
})
```

## 관련 자료 {/* #related-resources */}

* [Aurora DSQL 설명서](https://docs.aws.amazon.com/aurora-dsql/)
* [SQL 참조](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/working-with-aurora-dsql-sql.html)
* [지원되는 SQL 기능](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/working-with-postgresql-compatibility-supported-sql-features.html)
* [지원되지 않는 PostgreSQL 기능](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/working-with-postgresql-compatibility-unsupported-features.html)
* [지원되는 데이터 유형](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/working-with-postgresql-compatibility-supported-data-types.html)
* [비동기 인덱스](https://docs.aws.amazon.com/aurora-dsql/latest/userguide/working-with-create-index-async.html)
