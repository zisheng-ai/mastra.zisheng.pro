---
title: "참조: Google Cloud Spanner 스토리지 | 저장"
description: "Mastra의 Google Cloud Spanner 저장소 구현에 대한 문서입니다."
packages:
  - "@mastra/core"
  - "@mastra/spanner"
---

# Google Cloud Spanner 스토리지

Google Cloud Spanner 스토리지 구현은 Mastra를 위한 수평적 고용량의 강력하고 일관된 스토리지 백엔드를 제공합니다. Cloud Spanner의 GoogleSQL 언어를 대상으로 합니다.

## 설치 {/* #installation */}

```bash npm2yarn
npm install @mastra/spanner@latest
```

## 용법 {/* #usage */}

```typescript
import { SpannerStore } from '@mastra/spanner'

const storage = new SpannerStore({
  id: 'spanner-storage',
  projectId: process.env.SPANNER_PROJECT_ID!,
  instanceId: process.env.SPANNER_INSTANCE_ID!,
  databaseId: process.env.SPANNER_DATABASE_ID!,
})
```

인스턴스와 데이터베이스가 이미 존재해야 합니다. 어댑터는 처음 사용할 때 필요한 테이블을 생성하므로 Spanner 클라이언트에 제공된 사용자 인증 정보에는 스키마 변경을 실행하거나 실행하려면 권한이 필요합니다.`storage.init()` once during a deploy step with elevated credentials).

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'Unique identifier for this storage instance.',
  isOptional: false,
},
{
  name: 'projectId',
  type: 'string',
  description: 'Google Cloud project ID. Required unless `database` is provided.',
  isOptional: true,
},
{
  name: 'instanceId',
  type: 'string',
  description: 'Cloud Spanner instance ID. Required unless `database` is provided.',
  isOptional: true,
},
{
  name: 'databaseId',
  type: 'string',
  description: 'Cloud Spanner database ID. Required unless `database` is provided.',
  isOptional: true,
},
{
  name: 'database',
  type: '@google-cloud/spanner Database',
  description:
    'Pre-configured Spanner Database handle. Use this when you manage the Spanner client elsewhere (for example, to share auth or connection options across services).',
  isOptional: true,
},
{
  name: 'spannerOptions',
  type: 'object',
  description:
    'Options forwarded to the `@google-cloud/spanner` client constructor. Use this to set credentials, custom endpoints, or to point at the local emulator.',
  isOptional: true,
},
{
  name: 'disableInit',
  type: 'boolean',
  description:
    'When true, skip automatic table creation on first use. You must call `storage.init()` explicitly during a separate deploy step.',
  isOptional: true,
  defaultValue: 'false',
},
{
  name: 'skipDefaultIndexes',
  type: 'boolean',
  description: 'When true, skip creation of default indexes during initialization.',
  isOptional: true,
  defaultValue: 'false',
},
{
  name: 'indexes',
  type: 'CreateIndexOptions[]',
  description:
    'Custom secondary indexes to create. Each index must specify the table it belongs to. Indexes are routed to the appropriate domain based on the table name.',
  isOptional: true,
},
{
  name: 'initMode',
  type: "'sync' | 'validate'",
  description:
    "Controls schema-initialization behavior. `'sync'` creates missing tables, columns, and indexes during `init()` (the historical behavior). `'validate'` issues no DDL and instead verifies that every expected table, column, and default/custom index already exists, throwing a typed user error if anything is missing — useful when an external process (Terraform, Liquibase, a release pipeline, etc.) owns the schema and Mastra should only verify it.",
  isOptional: true,
  defaultValue: "'sync'",
},
]}
/>

## 생성자 예 {/* #constructor-examples */}

인스턴스화할 수 있습니다.`SpannerStore` in several ways:

```typescript
import { Spanner } from '@google-cloud/spanner'
import { SpannerStore } from '@mastra/spanner'

// Using projectId / instanceId / databaseId
const store1 = new SpannerStore({
  id: 'spanner-storage-1',
  projectId: 'my-gcp-project',
  instanceId: 'my-instance',
  databaseId: 'mastra',
})

// Reusing an existing Spanner Database handle
const spanner = new Spanner({ projectId: 'my-gcp-project' })
const database = spanner.instance('my-instance').database('mastra')

const store2 = new SpannerStore({
  id: 'spanner-storage-2',
  database,
})

// Using the local Spanner emulator (set the SPANNER_EMULATOR_HOST env var)
process.env.SPANNER_EMULATOR_HOST = 'localhost:9010'
const store3 = new SpannerStore({
  id: 'spanner-storage-emulator',
  projectId: 'test-project',
  instanceId: 'test-instance',
  databaseId: 'test-db',
  spannerOptions: { servicePath: 'localhost', port: 9010, sslCreds: undefined },
})
```

## 추가 참고사항 {/* #additional-notes */}

### 스키마 관리 {/* #schema-management */}

스토리지 어댑터는 모두 GoogleSQL 언어를 사용하여 다음 테이블을 생성합니다.

* `mastra_workflow_snapshot`: Workflow 상태 및 실행 데이터
* `mastra_threads`: 대화 스레드
* `mastra_messages`: 개별 메시지
* `mastra_resources`: 자원 작업 Memory
* `mastra_scorers`: 평가점수
* `mastra_background_tasks`: 백그라운드 Tool 실행 상태
* `mastra_agents`: Thin Agent 기록(ID, 상태, 활성 버전)
* `mastra_agent_versions`: 버전이 지정된 Agent 구성 스냅샷
* `mastra_mcp_clients` / `mastra_mcp_client_versions`: MCP client configurations and their version history
* `mastra_mcp_servers` / `mastra_mcp_server_versions`: MCP server configurations and their version history
* `mastra_skills` / `mastra_skill_versions`: skill records and versioned skill snapshots (instructions, references, scripts, assets, content tree)
* `mastra_skill_blobs`: SHA-256 해시로 키가 지정된 콘텐츠 주소 지정이 가능한 Blob 저장소, 스킬 버전 콘텐츠에 사용됨
* `mastra_prompt_blocks` / `mastra_prompt_block_versions`: prompt block records and versioned content snapshots (template content, rules, request-context schema)
* `mastra_scorer_definitions` / `mastra_scorer_definition_versions`: scorer definition records and versioned config snapshots (judge instructions, model, score range, preset config, default sampling)
* `mastra_schedules` / `mastra_schedule_triggers`: cron-driven workflow schedules and trigger history, consumed by Mastra's built-in `WorkflowScheduler`
* `mastra_workspaces` / `mastra_workspace_versions`: workspace records and versioned configuration snapshots (filesystem, sandbox, mounts, search, skills, tools)
* `mastra_datasets` / `mastra_dataset_items` / `mastra_dataset_versions`: evaluation datasets, SCD-2 versioned items, and version snapshots
* `mastra_experiments` / `mastra_experiment_results`: experiment runs and their per-item results
* `mastra_favorites`: 비정규화된 Agent 및 스킬에 대한 사용자별 즐겨찾기`favoriteCount` maintained on the parent record
* `mastra_channel_installations` / `mastra_channel_config`: multi-platform channel installations and per-platform configuration
* `mastra_ai_spans`: Observability을 위한 AI 추적 범위(스튜디오 추적 UI를 구동하는 데 사용되는 추적별 및 범위별 레코드)

테이블은 다음을 사용하여 생성됩니다.`STRING(MAX)` for text and JSON payloads, `INT64`, `FLOAT64`, `BOOL`, and `TIMESTAMP`.

다음 표에는 Spanner 관련 항목이 포함되어 있습니다.`STORED` generated columns that the adapter populates from JSON payloads so common filters can use a regular secondary index instead of a `JSON_VALUE` scan:

* `mastra_workflow_snapshot.snapshotStatus`: 추출물`$.status` from `snapshot`. Backs `listWorkflowRuns({ status })`.
* `mastra_schedules.target_workflow_id`: 추출물`$.workflowId` from `target`. Backs `listSchedules({ workflowId })`.

둘 다 다음을 통해 추가됩니다.`ALTER TABLE ... ADD COLUMN IF NOT EXISTS` during `init()` and skipped under `initMode: 'validate'` (where the schema is owned externally). When the column is absent, the adapter falls back to a `JSON_VALUE` filter at runtime.

어댑터는 스키마를 생성하거나 사용하지 않습니다. 격리를 위해 전용 데이터베이스를 사용합니다.

### 초기화 {/* #initialization */}

저장소를`Mastra` class, `init()` is called automatically before any storage operation:

```typescript
import { Mastra } from '@mastra/core'
import { SpannerStore } from '@mastra/spanner'

const storage = new SpannerStore({
  id: 'spanner-storage',
  projectId: process.env.SPANNER_PROJECT_ID!,
  instanceId: process.env.SPANNER_INSTANCE_ID!,
  databaseId: process.env.SPANNER_DATABASE_ID!,
})

const mastra = new Mastra({
  storage, // init() is called automatically
})
```

스토리지를 직접 사용하는 경우`init()` once before the first operation. Spanner doesn't allow concurrent schema changes, so `SpannerStore.init()` runs each domain's setup sequentially.

```typescript
const storage = new SpannerStore({
  id: 'spanner-storage',
  projectId: process.env.SPANNER_PROJECT_ID!,
  instanceId: process.env.SPANNER_INSTANCE_ID!,
  databaseId: process.env.SPANNER_DATABASE_ID!,
})

await storage.init()
const memory = await storage.getStore('memory')
const thread = await memory?.getThreadById({ threadId: '...' })
```

:::경고
만약에`init()` isn't called and `disableInit` is true, the required tables won't exist and storage operations will fail.
:::

### GoogleSQL 세부사항 {/* #googlesql-specifics */}

몇 가지 동작이 다른 관계형 어댑터와 다릅니다.

* Upsert 사용`INSERT OR UPDATE`. Spanner doesn't provide a `RETURNING` clause for upserts, so callers needing the post-write state must read it back.
* 아니요`TRUNCATE` exists. `dangerouslyClearAll()` issues `DELETE WHERE TRUE`.
* 식별자는 백틱으로 인용됩니다.
* DDL은 다음을 통해 적용됩니다.`database.updateSchema(...)`, which is asynchronous (long-running operation).
* `NULLS FIRST/LAST`지원되지 않습니다. NULL 처리를 사용한 주문은 다음을 통해 에뮬레이션됩니다.`IS NULL` ordering key.
* JSON 포함은 기본적으로 지원되지 않습니다.`listTraces` `metadata` and `scope` filters compile to per-key `JSON_VALUE(...) = @v` equality checks, and `tags` filters compile to `EXISTS` over `JSON_QUERY_ARRAY(...)`. This differs from Postgres' `@>` containment operator (which can match nested structure in a single index scan): most one-shot lookups still work but deeply nested structural matches aren't expressible.

### 직접 데이터베이스 액세스 {/* #direct-database-access */}

`SpannerStore`기본 Spanner 클라이언트 객체를 노출합니다.

```typescript
store.database // @google-cloud/spanner Database
store.instance // @google-cloud/spanner Instance (when created internally)
store.spanner // @google-cloud/spanner Spanner client (when created internally)
```

이는 맞춤형 트랜잭션이나 스키마 검사와 같은 고급 시나리오를 위한 것입니다. 데이터베이스를 직접 재사용하는 경우 어댑터의 유효성 검사 및 JSON 변환 논리를 우회합니다.

### 에뮬레이터를 사용한 로컬 개발 {/* #local-development-with-the-emulator */}

Docker를 사용하여 Cloud Spanner 에뮬레이터를 로컬에서 실행합니다.

```bash
docker run -p 9010:9010 -p 9020:9020 gcr.io/cloud-spanner-emulator/emulator
```

세트`SPANNER_EMULATOR_HOST=localhost:9010` and create the instance and database before running your app:

```bash
gcloud spanner instances create test-instance --config=emulator-config --nodes=1
gcloud spanner databases create test-db --instance=test-instance
```

그런 다음 Node.js 프로세스에 설정된 동일한 env var에 연결하세요. 그만큼`@google-cloud/spanner` client detects the emulator automatically.
