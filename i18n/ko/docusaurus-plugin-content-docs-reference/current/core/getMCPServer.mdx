---
title: "참조: Mastra.getMCPServer() | 핵심"
description: "ID 및 선택적 버전으로 특정 MCP 서버 인스턴스를 검색하는 Mastra의 `Mastra.getMCPServer()` 메소드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 마스트라.getMCPServer()

그만큼`.getMCPServer()`메소드는 레지스트리 키(서버를 등록할 때 사용되는 키)로 MCP 서버 인스턴스를 검색합니다.`mcpServers`구성). 서버의 내장으로 검색하는 경우`id`재산, 사용`.getMCPServerById()`대신에.

## 사용예 {/* #usage-example */}

```typescript
// Register an MCP server with a registry key
const myServer = new MCPServer({
  id: 'my-mcp-server',
  name: 'My Server',
  version: '1.0.0',
  tools: {/* ... */},
})

export const mastra = new Mastra({
  mcpServers: {
    customKey: myServer, // 'customKey' is the registry key
  },
})

// Retrieve by registry key
const server = mastra.getMCPServer('customKey')

// Alternatively, retrieve by intrinsic ID
const serverById = mastra.getMCPServerById('my-mcp-server')
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'registryKey',
  type: 'string',
  description:
    "mcpServers 구성 객체에 MCP 서버를 등록할 때 사용한 레지스트리 키입니다. 서버 자체의 id 속성이 아니라 키-값 쌍의 구성 키입니다.",
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'server',
  type: 'MCPServerBase | undefined',
  description: '지정된 레지스트리 키의 MCP 서버 인스턴스입니다. 찾지 못하면 undefined입니다.',
},
]}
/>
## 관련 방법 {/* #related-methods */}

* [Mastra.getMCPServerById()](/reference/core/getMCPServerById): 본질적으로 MCP 서버를 검색합니다.`id` property
* [Mastra.listMCPServers()](/reference/core/listMCPServers): 등록된 모든 MCP 서버 나열

## 또한보십시오 {/* #see-also */}

* [MCP 개요](/docs/mcp/overview)
* [MCP 서버 참조](/reference/tools/mcp-server)
