---
title: "참조: Mastra.getTool() | 핵심"
description: "Mastra 등록 키로 Tool을 검색하는 `Mastra.getTool()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 마스트라.getTool()

그만큼`.getTool()`메서드는 등록 키를 사용하여 Mastra 수준 레지스트리에서 Tool을 검색합니다.

## 사용예 {/* #usage-example */}

Tool을 `weather` 키로 등록한 다음, 해당 키를 사용하여 가져옵니다.
```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core/mastra'
import { weatherTool } from './tools/weather-tool'

export const mastra = new Mastra({
  tools: {
    weather: weatherTool,
  },
})

const tool = mastra.getTool('weather')
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'name',
  type: 'TToolName extends keyof TTools',
  description: 'Mastra `tools` 레지스트리에 있는 Tool의 등록 키입니다.',
},
]}
/>
## 관련된 {/* #related */}

* [Agent 간 Tool 공유](/docs/agents/using-tools#share-tools-across-agents)
* [마스트라 수업](/reference/core/mastra-class)
* [마스트라.getToolById()](/reference/core/getToolById)
* [Mastra.listTools()](/reference/core/listTools)
