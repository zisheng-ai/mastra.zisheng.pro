---
title: "참조: Mastra.setLogger() | 핵심"
description: "모든 구성 요소(Agent, Workflow 등)에 대한 로거를 설정하는 Mastra의 `Mastra.setLogger()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 마스트라.setLogger()

그만큼`.setLogger()`메서드는 Mastra 인스턴스의 모든 구성 요소(Agent, Workflow 등)에 대한 로거를 설정하는 데 사용됩니다. 이 메소드는 로거 속성이 있는 단일 객체 매개변수를 허용합니다.

## 사용예 {/* #usage-example */}

```typescript
mastra.setLogger({ logger: new PinoLogger({ name: 'testLogger' }) })
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'options',
  type: '{ logger: TLogger }',
  description: 'An object containing the logger instance to set for all components.',
  properties: [
    {
      type: '{ logger: TLogger }',
      parameters: [
        {
          name: 'logger',
          type: 'TLogger',
          description: 'The logger instance to set for all components (agents, workflows, etc.).',
        },
      ],
    },
  ],
},
]}
/>

## 보고 {/* #returns */}

이 메서드는 값을 반환하지 않습니다.

## 관련된 {/* #related */}

* [로깅 개요](/docs/observability/logging)
* [로거 참조](/reference/logging/pino-logger)
