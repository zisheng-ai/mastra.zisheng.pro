---
title: "참조: Mastra.listMCPServers() | 핵심"
description: "등록된 모든 MCP 서버 인스턴스를 검색하는 Mastra의 `Mastra.listMCPServers()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Mastra.listMCPServers()

그만큼`.listMCPServers()`메소드는 Mastra 인스턴스에 등록된 모든 MCP 서버 인스턴스를 검색하는 데 사용됩니다. 반환된 레코드는 레지스트리 키(`mcpServers`구성)을 키로 사용합니다.

## 사용예 {/* #usage-example */}

```typescript
// Register MCP servers
const server1 = new MCPServer({
  id: 'server-one',
  name: 'Server One',
  version: '1.0.0',
  tools: {/* ... */},
})

const server2 = new MCPServer({
  id: 'server-two',
  name: 'Server Two',
  version: '1.0.0',
  tools: {/* ... */},
})

export const mastra = new Mastra({
  mcpServers: {
    firstServer: server1, // Registry key: 'firstServer'
    secondServer: server2, // Registry key: 'secondServer'
  },
})

// List all registered servers
const servers = mastra.listMCPServers()
// Returns: { firstServer: MCPServerBase, secondServer: MCPServerBase }
```

## 매개변수 {/* #parameters */}

이 메서드는 매개변수를 허용하지 않습니다.

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'servers',
  type: 'Record<string, MCPServerBase> | undefined',
  description:
    '등록된 모든 MCP 서버 인스턴스의 레코드입니다. 키는 레지스트리 키(mcpServers 구성에서 가져옴)이고 값은 MCPServerBase 인스턴스입니다. 등록된 서버가 없으면 undefined입니다.',
},
]}
/>
## 관련 방법 {/* #related-methods */}

* [마스트라.getMCPServer()](/reference/core/getMCPServer): 레지스트리 키로 MCP 서버를 검색합니다.
* [Mastra.getMCPServerById()](/reference/core/getMCPServerById): 본질적으로 MCP 서버를 검색합니다.`id` property

## 또한보십시오 {/* #see-also */}

* [MCP 개요](/docs/mcp/overview)
* [MCP 서버 참조](/reference/tools/mcp-server)
