---
title: "참조: Mastra.getGateway() | 핵심"
description: "등록 키로 등록된 게이트웨이 검색"
packages:
  - "@mastra/core"
---

# 마스트라.getGateway()

등록 키(Mastra에 게이트웨이를 추가할 때 사용되는 키)로 게이트웨이를 검색합니다.

## 사용예 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({
  gateways: {
    myGateway: new MyCustomGateway(),
  },
})

const gateway = mastra.getGateway('myGateway')
console.log(gateway.name) // 'My Custom Gateway'
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'key',
  type: 'string',
  description: 'The registration key of the gateway',
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'gateway',
  type: 'MastraModelGateway',
  description: 'The gateway instance',
},
]}
/>

## 던지기 {/* #throws */}

지정된 키를 사용하는 게이트웨이가 없으면 오류가 발생합니다.

## 관련된 {/* #related */}

* [Mastra.getGatewayById()](/reference/core/getGatewayById): ID로 게이트웨이 가져오기
* [Mastra.listGateways()](/reference/core/listGateways): 모든 게이트웨이 나열
* [마스트라.추가게이트웨이()](/reference/core/addGateway): 게이트웨이 추가
* [마스트라Model게이트웨이](/reference/core/mastra-model-gateway): 게이트웨이 기본 클래스
* [사용자 정의 게이트웨이 가이드](/models/gateways/custom-gateways): 커스텀 게이트웨이 생성
