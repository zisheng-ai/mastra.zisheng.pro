---
title: "참조: Mastra.listWorkflows() | 핵심"
description: "구성된 모든 Workflow를 검색하는 Mastra의 `Mastra.listWorkflows()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Mastra.listWorkflows()

그만큼`.listWorkflows()`메서드는 Mastra 인스턴스에 구성된 모든 Workflow를 검색하는 데 사용됩니다. 이 메서드는 선택적 옵션 개체를 허용합니다.

## 사용예 {/* #usage-example */}

```typescript
mastra.listWorkflows()
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'options',
  type: '{ serialized?: boolean }',
  description:
    '선택적 구성 객체입니다. `serialized`가 true이면 전체 Workflow 인스턴스 대신 name 속성만 있는 단순화된 Workflow 객체를 반환합니다.',
  optional: true,
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'workflows',
  type: 'Record<string, Workflow>',
  description:
    '구성된 모든 Workflow의 레코드입니다. 키는 Workflow ID이고 값은 Workflow 인스턴스입니다(serialized가 true이면 단순화된 객체).',
},
]}
/>
## 관련된 {/* #related */}

* [Workflow 개요](/docs/workflows/overview)
