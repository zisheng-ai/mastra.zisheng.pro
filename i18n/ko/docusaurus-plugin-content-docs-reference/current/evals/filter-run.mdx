---
title: "참조: filterRun() | 평가"
description: "채점자 입력 데이터를 필터링하고 제한하기 위해 prepareRun 함수를 생성하는 유틸리티인 filterRun()에 대한 API 참조입니다."
packages:
  - '@mastra/core'
---

# 필터실행()

생성`prepareRun`선언적 옵션에서 함수를 실행합니다. 결과를 다음으로 전달합니다.`createScorer()`메시지를 필터링하고 컨텍스트 크기를 제한합니다. 또한 채점자 파이프라인이 실행되기 전에 불필요한 필드를 삭제합니다.

사용[`filterRun()`](#usage-example) for declarative filtering. Write a custom `prepareRun` function directly when you need imperative logic that `filterRun()`덮지 않습니다. 보다[맞춤 채점자: 입력 필터링](/docs/evals/custom-scorers#input-filtering)더 많은 것을 위해.

## 사용예 {/* #usage-example */}

다음 예에서는 가장 최근의 컨텍스트 메시지 20개로 제한되는 Tool 호출과 텍스트 메시지만 보는 채점기를 생성합니다.

```typescript title="src/mastra/scorers/tool-scorer.ts"
import { createScorer, filterRun } from '@mastra/core/evals'

const toolScorer = createScorer({
  id: 'tool-usage',
  description: 'Evaluates tool usage patterns',
  type: 'agent',
  prepareRun: filterRun({
    partTypes: ['tool-invocation', 'text'],
    maxRememberedMessages: 20,
  }),
}).generateScore(({ run }) => {
  // run.input.rememberedMessages contains only tool and text messages
  // run.output contains only tool and text messages
  return 1
})
```

### Tool 이름으로 필터링 {/* #filter-by-tool-name */}

특정 Tool와 관련된 메시지만 보관하세요.

```typescript
import { createScorer, filterRun } from '@mastra/core/evals'

const fileEditScorer = createScorer({
  id: 'file-edit-quality',
  description: 'Evaluates file editing patterns',
  type: 'agent',
  prepareRun: filterRun({
    toolNames: ['write_file', 'string_replace_lsp', 'view'],
  }),
}).generateScore(({ run }) => {
  // Only messages with these tool calls remain
  return 1
})
```

### 필드 삭제 {/* #drop-fields */}

채점자에게 필요하지 않은 필드를 제거하세요.

```typescript
import { createScorer, filterRun } from '@mastra/core/evals'

const simpleScorer = createScorer({
  id: 'response-length',
  description: 'Checks response length',
  type: 'agent',
  prepareRun: filterRun({
    dropRequestContext: true,
    dropExpectedTrajectory: true,
    dropGroundTruth: true,
    maxOutputMessages: 5,
  }),
}).generateScore(({ run }) => {
  return run.output.length > 0 ? 1 : 0
})
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'options',
  type: 'FilterRunOptions',
  isOptional: false,
  description: 'Configuration object that controls what data the scorer receives.',
  properties: [
    {
      type: 'object',
      parameters: [
        {
          name: 'partTypes',
          type: 'MastraPartType[]',
          isOptional: true,
          description:
            "Keep only messages whose parts match these types. Each entry is prefix-matched against the message part's type. Plain text messages (no tool invocations) are always kept unless explicitly excluded. System messages and tagged system messages are never filtered.",
        },
        {
          name: 'toolNames',
          type: 'string[]',
          isOptional: true,
          description:
            'Keep only tool-invocation messages for these specific tools. Each entry is prefix-matched against the tool name. Non-tool messages (text, data) are unaffected.',
        },
        {
          name: 'maxRememberedMessages',
          type: 'number',
          isOptional: true,
          description:
            'Maximum number of messages to keep in remembered messages (context). Takes from the end (most recent). Applied after type and tool filtering.',
        },
        {
          name: 'maxOutputMessages',
          type: 'number',
          isOptional: true,
          description:
            'Maximum number of messages to keep in the output. Takes from the end. Applied after type and tool filtering.',
        },
        {
          name: 'dropRequestContext',
          type: 'boolean',
          isOptional: true,
          description: 'Remove request context from the run entirely.',
        },
        {
          name: 'dropExpectedTrajectory',
          type: 'boolean',
          isOptional: true,
          description: 'Remove expected trajectory from the run.',
        },
        {
          name: 'dropGroundTruth',
          type: 'boolean',
          isOptional: true,
          description: 'Remove ground truth from the run.',
        },
      ],
    },
  ],
},
]}
/>

**보고:** `(run: ScorerRun) => ScorerRun`. A function suitable for the `prepareRun` option on [`createScorer()`](/reference/evals/create-scorer).

## 부품 유형 {/* #part-types */}

그만큼`partTypes` option accepts `MastraPartType` values. Each value is prefix-matched, so `'data-'` matches all data part types.

| 유형 | 설명 |
| - | - |
|`'text'` | Text content parts |
| `'tool-invocation'` | Tool call and result parts |
| `'reasoning'` | Chain-of-thought reasoning parts |
| `'step-start'` | Step marker parts |
| `'image'` | Image parts |
| `'file'` | File parts |
| `'source'` | Source reference parts |
| `'source-document'` | Source document parts |
| `'data-'` | All data parts (matches any `data-*` prefix) |
| `'data-om-'`| 관찰 기억 데이터 부분 |
|`'data-workspace-'`| 작업공간 데이터 부분 |
|`'data-sandbox-'`| 샌드박스 데이터 부분 |
|`'data-tool-'`| Tool 관련 데이터 부분 |

## 필터링 동작 {/* #filtering-behavior */}

* **부품 유형 필터링**둘 다에 적용됩니다`input.rememberedMessages` and `output` when they contain agent message arrays.
* **Tool 이름 필터링**Tool 호출이 포함된 메시지에만 영향을 미칩니다. 텍스트 전용 메시지가 전달됩니다.
* **시스템 메시지** (`systemMessages`, `taggedSystemMessages`) are never filtered, regardless of `partTypes` or `toolNames`.
* **메시지 한도** (`maxRememberedMessages`, `maxOutputMessages`) apply after type and tool filtering, taking the most recent messages.
* 둘 다일 때`partTypes` and `toolNames` are set, a message must satisfy both filters to be kept.
