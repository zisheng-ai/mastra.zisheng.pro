---
title: "참조: CachingPubSub | PubSub"
description: "재개 가능한 스트림에 대한 이벤트 캐싱 및 재생을 추가하는 게시/구독 래퍼인 CachingPubSub에 대한 API 참조입니다."
packages:
  - "@mastra/core"
---

# 캐싱PubSub

`CachingPubSub`무엇이든 래핑[`PubSub`](/reference/pubsub/base)구현하고 이벤트 캐싱 및 재생을 추가합니다. 주제별로 게시된 모든 이벤트를 캐시에 기록하므로 늦게 연결하거나 연결이 끊긴 후 다시 연결하는 구독자는 라이브 이벤트를 계속하기 전에 놓친 이벤트를 재생할 수 있습니다.

이를 사용하여 다음과 같이 기록을 유지하지 않는 전송 위에 재개 가능한 스트림을 구축합니다.[`EventEmitterPubSub`](/reference/pubsub/event-emitter). Transports that already persist events, such as [`RedisStreamsPubSub`](/reference/pubsub/redis-streams), don't need this wrapper.

`CachingPubSub`일괄 처리에 투명합니다.`subscribe()` forwards `options.batch` to the inner pub/sub, and `supportsNativeBatching`내면의 가치를 반영합니다. 일괄 처리를 지원하지 않는 내부를 포장하면 일괄 배송이 되지 않습니다.`options.batch`통과되었습니다.

## 사용예 {/* #usage-example */}

내부 pub/sub를 래핑하고 이벤트를 저장할 서버 캐시를 제공합니다.

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { CachingPubSub, EventEmitterPubSub } from '@mastra/core/events'
import { InMemoryServerCache } from '@mastra/core/cache'

const cache = new InMemoryServerCache()
const pubsub = new CachingPubSub(new EventEmitterPubSub(), cache)

export const mastra = new Mastra({
  pubsub,
})
```

그만큼`withCaching` helper returns the same instance and reads better when wrapping inline:

```typescript
import { withCaching, EventEmitterPubSub } from '@mastra/core/events'
import { InMemoryServerCache } from '@mastra/core/cache'

const pubsub = withCaching(new EventEmitterPubSub(), new InMemoryServerCache())
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'inner',
  type: 'PubSub',
  description:
    'The pub/sub implementation to wrap. All publishes and live subscriptions pass through to this instance.',
  isOptional: false,
},
{
  name: 'cache',
  type: 'MastraServerCache',
  description: 'Cache used to store events per topic for replay.',
  isOptional: false,
},
{
  name: 'options',
  type: 'CachingPubSubOptions',
  description: 'Optional configuration.',
  isOptional: true,
  properties: [
    {
      name: 'keyPrefix',
      type: 'string',
      description: 'Prefix for cache keys, used to namespace events.',
      isOptional: true,
      defaultValue: 'pubsub:',
    },
    {
      name: 'logger',
      type: 'IMastraLogger',
      description: 'Logger for errors. Falls back to `console.error` when omitted.',
      isOptional: true,
    },
  ],
},
]}
/>

## 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'supportsNativeBatching',
  type: 'boolean',
  description: 'Mirrors the inner pub/sub. Returns `true` only when the wrapped implementation supports batching.',
},
]}
/>

## 행동 양식 {/* #methods */}

`CachingPubSub`구현[`PubSub`](/reference/pubsub/base) contract. It overrides the replay methods to read cached events. The methods below describe the caching behavior.

### `publish(topic, event)` {/* #publishtopic-event */}

순차 인덱스로 이벤트를 캐시한 다음 내부 pub/sub에 게시합니다.

```typescript
await pubsub.publish('my-topic', {
  type: 'example',
  data: { value: 1 },
  runId: 'run-123',
})
```

### `subscribeWithReplay(topic, cb)` {/* #subscribewithreplaytopic-cb */}

주제에 대해 캐시된 모든 이벤트를 재생한 다음 라이브 이벤트를 구독합니다.

```typescript
await pubsub.subscribeWithReplay('my-topic', event => {
  console.log(event)
})
```

### `subscribeFromOffset(topic, offset, cb)` {/* #subscribefromoffsettopic-offset-cb */}

다음에서 시작하여 캐시된 이벤트를 재생합니다.`offset`, then subscribes to live events. Use this when the client knows its last position, to avoid replaying the whole history.

```typescript
await pubsub.subscribeFromOffset('my-topic', 42, event => {
  console.log(event)
})
```

### `getHistory(topic, offset?)` {/* #gethistorytopic-offset */}

다음에서 시작하여 주제에 대해 캐시된 이벤트를 반환합니다.`offset`.

```typescript
const events = await pubsub.getHistory('my-topic', 0)
```

보고:`Promise<Event[]>`

### `clearTopic(topic)` {/* #cleartopictopic */}

주제의 캐시된 이벤트 및 오프셋 카운터를 제거하고 호출을 내부 pub/sub로 전달하여 지속적인 전송(예:[`RedisStreamsPubSub`](/reference/pubsub/redis-streams)) can delete their retained state too. Mastra's run lifecycles call this automatically when a run finishes.

```typescript
await pubsub.clearTopic('my-topic')
```

## 기능 {/* #functions */}

### `withCaching(pubsub, cache, options?)` {/* #withcachingpubsub-cache-options */}

다음을 구성하는 편의 래퍼`CachingPubSub`. Accepts the same arguments as the constructor and returns the new instance.

```typescript
import { withCaching, EventEmitterPubSub } from '@mastra/core/events'
import { InMemoryServerCache } from '@mastra/core/cache'

const pubsub = withCaching(new EventEmitterPubSub(), new InMemoryServerCache(), {
  keyPrefix: 'events:',
})
```

보고:`CachingPubSub`
