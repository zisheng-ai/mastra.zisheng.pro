---
title: "참조: createInngestAgent() | Agent"
description: "Inngest 기반 내구성 실행 및 재개 가능한 스트림으로 Agent를 래핑하는 createInngestAgent()에 대한 API 참조입니다."
packages:
  - "@mastra/inngest"
---

# `createInngestAgent()`

`createInngestAgent()`기존을 래핑[`Agent`](/reference/agents/agent)~와 함께[섭취](https://www.inngest.com/docs)-강력한 내구성 실행. 좋다[`createDurableAgent()`](/reference/agents/durable-agent#createdurableagentoptions), 이벤트를 스트리밍합니다.[PubSub](/docs/server/pubsub)재개 가능한 스트림을 지원하지만 프로세스 내 대신 Ingest의 실행 엔진에서 Agent 루프를 실행합니다. 실행이 프로세스를 다시 시작한 후에도 유지되어야 하거나 분산 환경에서 실행되어야 하는 경우 이를 사용합니다.

프로세스 내 내구성 실행에는 [`createDurableAgent()`](/reference/agents/durable-agent#createdurableagentoptions)를 사용하세요. 기본 제공 Workflow 엔진에서 실행 후 결과를 기다리지 않는 방식으로 처리하려면 [`createEventedAgent()`](/reference/agents/durable-agent#createeventedagentoptions)를 사용하세요.
## 사용예 {/* #usage-example */}

Ingest 클라이언트를 설정하고, Agent를 래핑하고, 이를 Mastra에 등록하고, Ingest 서비스 엔드포인트를 노출합니다.

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { Agent } from '@mastra/core/agent'
import { createInngestAgent, serve as inngestServe } from '@mastra/inngest'
import { Inngest } from 'inngest'

const inngest = new Inngest({ id: 'my-app' })

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
})

const durableAgent = createInngestAgent({ agent, inngest })

export const mastra = new Mastra({
  agents: { myAgent: durableAgent },
  server: {
    apiRoutes: [
      {
        path: '/inngest/api',
        method: 'ALL',
        createHandler: async ({ mastra }) => inngestServe({ mastra, inngest }),
      },
    ],
  },
})
```

응답을 스트리밍하고 결과를 읽습니다.

```typescript
const { output, runId, cleanup } = await durableAgent.stream('Hello!')

const text = await output.text

cleanup()
```

## `createInngestAgent(options)` {/* #createinngestagentoptions */}

`Agent`를 Inngest 기반 내구성 실행 및 재개 가능한 스트림으로 래핑합니다.
```typescript
import { createInngestAgent } from '@mastra/inngest'

const durableAgent = createInngestAgent({ agent, inngest })
```

보고:[`InngestAgent`](#inngestagent-interface)

### 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'agent',
  type: 'Agent',
  isOptional: false,
  description:
    'Inngest 내구성 실행으로 래핑할 Agent입니다. `InngestAgent`에서 구현하지 않은 메서드(예: `listTools()` 및 `getMemory()`)는 Proxy를 통해 이 Agent에 위임됩니다.',
},{
  name: 'inngest',
  type: 'Inngest',
  isOptional: false,
  description:
    'Inngest 클라이언트 인스턴스입니다. Workflow 이벤트를 전송하고 SDK v4에서는 실시간 스트림 이벤트를 게시하는 데 사용됩니다.',
},{
  name: 'id',
  type: 'string',
  isOptional: true,
  description: 'ID 재정의입니다.',
  defaultValue: 'agent.id',
},{
  name: 'name',
  type: 'string',
  isOptional: true,
  description: '이름 재정의입니다.',
  defaultValue: 'agent.name',
},{
  name: 'pubsub',
  type: 'PubSub',
  isOptional: true,
  description:
    '스트리밍 이벤트를 위한 PubSub 인스턴스입니다. 기본 `InngestPubSub`은 프로세스 간에 작동하는 Inngest Realtime을 사용합니다.',
  defaultValue: 'InngestPubSub',
},{
  name: 'cache',
  type: 'MastraServerCache',
  isOptional: true,
  description:
    '저장된 스트림 이벤트를 위한 캐시로, 재개 가능한 스트림을 지원합니다. 제공하면 PubSub이 자동으로 `CachingPubSub`으로 래핑됩니다. 생략하면 Agent가 Mastra 인스턴스에서 캐시를 상속합니다.',
},{
  name: 'mastra',
  type: 'Mastra',
  isOptional: true,
  description: 'Observability를 위한 Mastra 인스턴스입니다. Agent가 Mastra에 등록되면 자동으로 설정됩니다.',
},
]}
/>
## `InngestAgent`인터페이스 {/* #inngestagent-interface */}

`createInngestAgent()`가 반환한 객체입니다. 아래의 내구성 실행 메서드를 제공합니다. 명시적으로 정의되지 않은 모든 속성이나 메서드(예: `listTools()` 및 `getMemory()`)는 Proxy를 통해 기본 Agent로 전달됩니다.
### 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'Agent ID입니다.',
},{
  name: 'name',
  type: 'string',
  description: 'Agent 이름입니다.',
},{
  name: 'agent',
  type: 'Agent',
  description: '기본 Mastra Agent입니다.',
},{
  name: 'inngest',
  type: 'Inngest',
  description: 'Inngest 클라이언트입니다.',
},{
  name: 'cache',
  type: 'MastraServerCache | undefined',
  description: '재개 가능한 스트림이 활성화된 경우 해석된 캐시 인스턴스입니다.',
},{
  name: 'pubsub',
  type: 'PubSub',
  description: '스트리밍 이벤트에 사용되는 PubSub 인스턴스입니다.',
},
]}
/>
## 행동 양식 {/* #methods */}

### 실행 {/* #execution */}

#### `stream(messages, options?)` {/* #streammessages-options */}

Ingest의 내구성 있는 실행 엔진을 사용하여 응답을 스트리밍합니다. Workflow는 PubSub 구독이 설정된 후 Ingest 이벤트를 통해 트리거됩니다.

```typescript
const { output, runId, cleanup } = await durableAgent.stream('Hello!', {
  onChunk: chunk => console.log(chunk),
  onFinish: result => console.log('done', result),
})

const text = await output.text
cleanup()
```

보고:[`Promise<InngestAgentStreamResult>`](#inngestagentstreamresult)

#### `resume(runId, resumeData, options?)` {/* #resumerunid-resumedata-options */}

예를 들어 Tool 승인 후 일시 중지된 Ingest 실행을 재개합니다. 스토리지에서 Workflow 스냅샷을 로드하고 일시 중단된 단계를 찾은 다음 재개 이벤트를 Ingest로 보냅니다.

```typescript
const { output, cleanup } = await durableAgent.resume(
  runId,
  {
    approved: true,
  },
  { threadId: 'thread-1', resourceId: 'user-1' },
)

await output.text
cleanup()
```

세 번째 인수에는 수명 주기 콜백 외에도 `threadId`와 `resourceId`가 포함됩니다.
<PropertiesTable
  content={[
{
  name: 'threadId',
  type: 'string',
  isOptional: true,
  description: '재개된 실행과 연결할 스레드 ID입니다.',
},{
  name: 'resourceId',
  type: 'string',
  isOptional: true,
  description: '재개된 실행과 연결할 리소스 ID입니다.',
},{
  name: 'onChunk',
  type: '(chunk: ChunkType) => void | Promise<void>',
  isOptional: true,
  description: '스트리밍되는 각 청크마다 호출됩니다.',
},{
  name: 'onStepFinish',
  type: '(result: AgentStepFinishEventData) => void | Promise<void>',
  isOptional: true,
  description: 'Agent 루프의 단계가 완료될 때 호출됩니다.',
},{
  name: 'onFinish',
  type: '(result: AgentFinishEventData) => void | Promise<void>',
  isOptional: true,
  description: '실행이 완료될 때 호출됩니다.',
},{
  name: 'onError',
  type: '(error: Error) => void | Promise<void>',
  isOptional: true,
  description: '실행 중 오류가 발생할 때 호출됩니다.',
},{
  name: 'onSuspended',
  type: '(data: AgentSuspendedEventData) => void | Promise<void>',
  isOptional: true,
  description: '실행이 일시 중지될 때 호출됩니다.',
},
]}
/>
보고:[`Promise<InngestAgentStreamResult>`](#inngestagentstreamresult)

#### `generate(messages, options?)` {/* #generatemessages-options */}

Inngest의 내구성 실행 엔진에서 응답을 실행하고 단일 `FullOutput`으로 해석합니다. 실행이 일시 중지되면 `generate()`는 `finishReason: 'suspended'`와 함께 해석됩니다. `runId` 옵션은 선택 사항입니다. 생략하면 `generate()`가 실행 ID를 생성하고 `result.runId`에 반환합니다. [`resumeGenerate()`](#resumegeneraterunid-resumedata-options)을 사용하여 실행을 계속하세요. 호출자에게 일시 중지 콜백이 필요한 경우 `onSuspended`와 함께 [`stream()`](#streammessages-options)을 사용하세요.
```typescript
const result = await durableAgent.generate('Delete the old records', {
  requireToolApproval: true,
})

result.runId // Generated automatically
result.finishReason // 'suspended' when approval is required
```

보고:`Promise<FullOutput<TOutput>>`

#### `resumeGenerate(runId, resumeData, options?)` {/* #resumegeneraterunid-resumedata-options */}

일시 중지된 `generate()` 실행을 재개하고 단일 `FullOutput`으로 해석합니다.
```typescript
if (!result.runId) {
  throw new Error('Run ID is missing')
}

const resumedResult = await durableAgent.resumeGenerate(result.runId, { approved: true })
```

보고:`Promise<FullOutput<TOutput>>`

#### `observe(runId, options?)` {/* #observerunid-options */}

기존 실행에 다시 연결하고 라이브 이벤트를 전달하기 전에 캐시된 이벤트를 재생합니다. 네트워크 연결이 끊어진 후 사용하세요. 알려진 위치부터 재생하려면 `offset`을 전달하세요.
```typescript
const { output, cleanup } = await durableAgent.observe(runId, {
  offset: 0,
  onChunk: chunk => console.log(chunk),
})

await output.text
```

`observe()`의 결과에는 `threadId` 또는 `resourceId`가 포함되지 않습니다.
보고:`Promise<Omit<InngestAgentStreamResult, 'threadId' | 'resourceId'>>`

:::warning

`observe()`가 반환한 `cleanup()`은 실행의 레지스트리 항목과 캐시된 이벤트를 제거합니다. 실행 사용을 마쳤을 때만 호출하세요. 실행이 일시 중지되었고 나중에 재개하려는 경우 `cleanup()`을 호출하지 마세요.
:::

#### `prepare(messages, options?)` {/* #preparemessages-options */}

트리거하지 않고 지속 가능한 실행을 위한 실행을 준비합니다. Ingest Workflow 이벤트를 수동으로 트리거하는 데 사용할 수 있는 직렬화된 Workflow 입력을 반환합니다.

```typescript
const { runId, messageId, workflowInput, threadId, resourceId } = await durableAgent.prepare(
  'Summarize the document',
  {
    memory: { threadId: 'thread-1', resourceId: 'user-1' },
  },
)
```

보고:

```typescript
interface PrepareResult {
  runId: string
  messageId: string
  workflowInput: any
  threadId?: string
  resourceId?: string
}
```

### 내성 {/* #introspection */}

#### `isInngestAgent(obj)` {/* #isinngestagentobj */}

객체가 객체인지 확인하는 유형 가드`InngestAgent`.

```typescript
import { isInngestAgent } from '@mastra/inngest'

if (isInngestAgent(agent)) {
  // agent is InngestAgent
}
```

보고:`boolean`

## 스트림 옵션 {/* #stream-options */}

`stream()`은 `InngestAgentStreamOptions` 객체를 받습니다. [`DurableAgent.stream()`](/reference/agents/durable-agent#stream-options)과 동일한 Agent 실행 옵션과 수명 주기 콜백을 지원합니다.
<PropertiesTable
  content={[
{
  name: 'runId',
  type: 'string',
  isOptional: true,
  description: '이 실행의 고유 식별자입니다. 나중에 `resume()` 또는 `observe()`와 함께 사용하세요.',
},{
  name: 'instructions',
  type: "AgentExecutionOptions['instructions']",
  isOptional: true,
  description: "이 실행에서 Agent의 기본 지침을 재정의합니다.",
},{
  name: 'context',
  type: 'ModelMessage[]',
  isOptional: true,
  description: 'Agent에 제공할 추가 컨텍스트 메시지입니다.',
},{
  name: 'memory',
  type: 'object',
  isOptional: true,
  description: '대화 지속성 및 검색을 위한 Memory 구성입니다.',
},{
  name: 'requestContext',
  type: 'RequestContext',
  isOptional: true,
  description: '이 실행의 동적 구성과 상태를 전달하는 요청 컨텍스트입니다.',
},{
  name: 'maxSteps',
  type: 'number',
  isOptional: true,
  description: '실행할 최대 단계 수입니다.',
},{
  name: 'toolsets',
  type: 'object',
  isOptional: true,
  description: '이 실행에서 사용할 수 있는 추가 Tool 세트입니다.',
},{
  name: 'clientTools',
  type: 'object',
  isOptional: true,
  description: '실행 중에 사용할 수 있는 클라이언트 측 Tool입니다.',
},{
  name: 'toolChoice',
  type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
  isOptional: true,
  description: 'Tool 선택 전략입니다.',
},{
  name: 'modelSettings',
  type: 'object',
  isOptional: true,
  description: 'temperature와 같은 Model별 설정입니다.',
},{
  name: 'requireToolApproval',
  type: 'boolean',
  isOptional: true,
  description: '모든 Tool 호출에 승인을 요구하며, 재개될 때까지 실행을 일시 중단합니다.',
},{
  name: 'autoResumeSuspendedTools',
  type: 'boolean',
  isOptional: true,
  description: '외부 `resume()` 호출을 기다리지 않고 일시 중단된 Tool을 자동으로 재개합니다.',
},{
  name: 'toolCallConcurrency',
  type: 'number',
  isOptional: true,
  description: '동시에 실행할 최대 Tool 호출 수입니다.',
},{
  name: 'includeRawChunks',
  type: 'boolean',
  isOptional: true,
  description: '스트림 출력에 원시 Provider 청크를 포함합니다.',
},{
  name: 'maxProcessorRetries',
  type: 'number',
  isOptional: true,
  description: '생성당 최대 프로세서 재시도 횟수입니다.',
},{
  name: 'untilIdle',
  type: 'boolean | { maxIdleMs?: number }',
  isOptional: true,
  description:
    '설정하면 Agent가 유휴 상태가 될 때까지 백그라운드 작업이 이어지는 동안 스트림을 열린 상태로 유지합니다. 기본 5분 유휴 시간 제한을 사용하려면 `true`를 전달하고, 사용자 지정하려면 `{ maxIdleMs }`를 전달하세요.',
},{
  name: 'onChunk',
  type: '(chunk: ChunkType) => void | Promise<void>',
  isOptional: true,
  description: '스트리밍되는 각 청크에 대해 호출됩니다.',
},{
  name: 'onStepFinish',
  type: '(result: AgentStepFinishEventData) => void | Promise<void>',
  isOptional: true,
  description: 'Agent 루프의 단계가 완료될 때 호출됩니다.',
},{
  name: 'onFinish',
  type: '(result: AgentFinishEventData) => void | Promise<void>',
  isOptional: true,
  description: '실행이 완료될 때 호출됩니다.',
},{
  name: 'onError',
  type: '(error: Error) => void | Promise<void>',
  isOptional: true,
  description: '실행 중 오류가 발생할 때 호출됩니다.',
},{
  name: 'onSuspended',
  type: '(data: AgentSuspendedEventData) => void | Promise<void>',
  isOptional: true,
  description: 'Tool 승인 등의 이유로 실행이 일시 중단될 때 호출됩니다.',
},
]}
/>
`observe()`는 수명 주기 콜백(`onChunk`, `onStepFinish`, `onFinish`, `onError`, `onSuspended`)과 재생 시작 위치를 제어하는 `offset`을 허용합니다.
## `InngestAgentStreamResult` {/* #inngestagentstreamresult */}

반환된 객체에는 `stream()`과 `resume()`이 포함됩니다. `observe()` 메서드는 동일한 형태를 반환하지만 `threadId`와 `resourceId`는 생략합니다.
```typescript
interface InngestAgentStreamResult<OUTPUT = undefined> {
  output: MastraModelOutput<OUTPUT>
  readonly fullStream: ReadableStream<any>
  runId: string
  threadId?: string
  resourceId?: string
  cleanup: () => void
}
```

<PropertiesTable
  content={[
{
  name: 'output',
  type: 'MastraModelOutput',
  description: '스트리밍 출력입니다. 전체 텍스트를 가져오려면 `output.text`를 기다리고, 스트림을 소비하려면 `output.fullStream`을 사용하세요.',
},{
  name: 'fullStream',
  type: 'ReadableStream',
  description: '`output.fullStream`에 위임하는 전체 이벤트 스트림입니다.',
},{
  name: 'runId',
  type: 'string',
  description: '고유한 실행 ID입니다. 다시 연결하려면 이를 `resume()` 또는 `observe()`에 전달하세요.',
},{
  name: 'threadId',
  type: 'string',
  isOptional: true,
  description: 'Memory를 사용할 때의 스레드 ID입니다.',
},{
  name: 'resourceId',
  type: 'string',
  isOptional: true,
  description: 'Memory를 사용할 때의 리소스 ID입니다.',
},{
  name: 'cleanup',
  type: '() => void',
  description: 'PubSub 구독을 해제하고 실행의 레지스트리 항목을 정리합니다. 실행 사용을 마치면 호출하세요.',
},
]}
/>
## Ingest 기능 제공 {/* #serving-inngest-functions */}

`@mastra/inngest` 패키지는 HTTP 프레임워크에 Inngest Workflow 함수를 등록하기 위한 `serve()`와 `createServe()`를 제공합니다.
### `serve(options)` {/* #serveoptions */}

Hono(기본 프레임워크)를 사용하여 Mastra Workflow를 제공합니다. Mastra에서 모든 Ingest 지원 Workflow를 수집하고 이를 Ingest 기능으로 등록합니다.

```typescript
import { serve } from '@mastra/inngest'

app.use('/inngest/api', async c => {
  return serve({ mastra, inngest })(c)
})
```

### `createServe(adapter)` {/* #createserveadapter */}

모든 Inngest 서버 어댑터(`inngest/express`, `inngest/fastify`, `inngest/next` 등)를 받아 해당 프레임워크용 serve 함수를 반환하는 팩토리입니다.
```typescript title="Express"
import { createServe } from '@mastra/inngest'
import { serve } from 'inngest/express'

const serveExpress = createServe(serve)
app.use('/inngest/api', serveExpress({ mastra, inngest }))
```

```typescript title="Next.js"
import { createServe } from '@mastra/inngest'
import { serve } from 'inngest/next'

const serveNext = createServe(serve)
export const { GET, POST, PUT } = serveNext({ mastra, inngest })
```

### 서브 옵션 {/* #serve-options */}

<PropertiesTable
  content={[
{
  name: 'mastra',
  type: 'Mastra',
  isOptional: false,
  description: '등록된 Agent와 Workflow를 포함하는 Mastra 인스턴스입니다.',
},{
  name: 'inngest',
  type: 'Inngest',
  isOptional: false,
  description: 'Inngest 클라이언트 인스턴스입니다.',
},{
  name: 'functions',
  type: 'InngestFunction.Like[]',
  isOptional: true,
  description: 'Mastra Workflow와 함께 제공할 추가 Inngest 함수입니다.',
},{
  name: 'registerOptions',
  type: 'RegisterOptions',
  isOptional: true,
  description: 'Inngest 등록 핸들러에 전달되는 옵션입니다.',
},
]}
/>
## 관련된 {/* #related */}

* [내구성 Agent 참조](/reference/agents/durable-agent)
* [Agent 클래스](/reference/agents/agent)
* [Ingest 배포 가이드](/guides/deployment/inngest)
* [PubSub](/docs/server/pubsub)
