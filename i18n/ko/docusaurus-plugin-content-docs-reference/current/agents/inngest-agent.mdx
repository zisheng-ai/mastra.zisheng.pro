---
title: "참조: createInngestAgent() | Agent"
description: "Inngest 기반 내구성 실행 및 재개 가능한 스트림으로 Agent를 래핑하는 createInngestAgent()에 대한 API 참조입니다."
packages:
  - "@mastra/inngest"
---

# `createInngestAgent()`

`createInngestAgent()`기존을 래핑[`Agent`](/reference/agents/agent)~와 함께[섭취](https://www.inngest.com/docs)-강력한 내구성 실행. 좋다[`createDurableAgent()`](/reference/agents/durable-agent#createdurableagentoptions), 이벤트를 스트리밍합니다.[PubSub](/docs/server/pubsub)재개 가능한 스트림을 지원하지만 프로세스 내 대신 Ingest의 실행 엔진에서 Agent 루프를 실행합니다. 실행이 프로세스를 다시 시작한 후에도 유지되어야 하거나 분산 환경에서 실행되어야 하는 경우 이를 사용합니다.

프로세스 내 내구성 실행의 경우 다음을 사용합니다.[`createDurableAgent()`](/reference/agents/durable-agent#createdurableagentoptions). For fire-and-forget execution on the built-in workflow engine, use [`createEventedAgent()`](/reference/agents/durable-agent#createeventedagentoptions).

## 사용예 {/* #usage-example */}

Ingest 클라이언트를 설정하고, Agent를 래핑하고, 이를 Mastra에 등록하고, Ingest 서비스 엔드포인트를 노출합니다.

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { Agent } from '@mastra/core/agent'
import { createInngestAgent, serve as inngestServe } from '@mastra/inngest'
import { Inngest } from 'inngest'

const inngest = new Inngest({ id: 'my-app' })

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
})

const durableAgent = createInngestAgent({ agent, inngest })

export const mastra = new Mastra({
  agents: { myAgent: durableAgent },
  server: {
    apiRoutes: [
      {
        path: '/inngest/api',
        method: 'ALL',
        createHandler: async ({ mastra }) => inngestServe({ mastra, inngest }),
      },
    ],
  },
})
```

응답을 스트리밍하고 결과를 읽습니다.

```typescript
const { output, runId, cleanup } = await durableAgent.stream('Hello!')

const text = await output.text

cleanup()
```

## `createInngestAgent(options)` {/* #createinngestagentoptions */}

래핑`Agent` with Inngest-powered durable execution and resumable streams.

```typescript
import { createInngestAgent } from '@mastra/inngest'

const durableAgent = createInngestAgent({ agent, inngest })
```

보고:[`InngestAgent`](#inngestagent-interface)

### 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'agent',
  type: 'Agent',
  isOptional: false,
  description:
    'The Agent to wrap with Inngest durable execution. Methods not implemented by `InngestAgent` (e.g., `listTools()` and `getMemory()`) delegate to this agent via a Proxy.',
},
{
  name: 'inngest',
  type: 'Inngest',
  isOptional: false,
  description:
    'The Inngest client instance. Used to send workflow events and, in SDK v4, publish realtime stream events.',
},
{
  name: 'id',
  type: 'string',
  isOptional: true,
  description: 'ID override.',
  defaultValue: 'agent.id',
},
{
  name: 'name',
  type: 'string',
  isOptional: true,
  description: 'Name override.',
  defaultValue: 'agent.name',
},
{
  name: 'pubsub',
  type: 'PubSub',
  isOptional: true,
  description:
    'PubSub instance for streaming events. The default `InngestPubSub` uses Inngest Realtime, which works across processes.',
  defaultValue: 'InngestPubSub',
},
{
  name: 'cache',
  type: 'MastraServerCache',
  isOptional: true,
  description:
    'Cache for stored stream events, which enables resumable streams. When provided, the PubSub is automatically wrapped with `CachingPubSub`. If omitted, the agent inherits the cache from the Mastra instance.',
},
{
  name: 'mastra',
  type: 'Mastra',
  isOptional: true,
  description: 'Mastra instance for observability. Set automatically when the agent is registered with Mastra.',
},
]}
/>

## `InngestAgent`인터페이스 {/* #inngestagent-interface */}

반환된 객체`createInngestAgent()`. It provides the durable execution methods below. Any property or method not explicitly defined (e.g., `listTools()` and `getMemory()`) is forwarded to the underlying agent via a Proxy.

### 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'The agent ID.',
},
{
  name: 'name',
  type: 'string',
  description: 'The agent name.',
},
{
  name: 'agent',
  type: 'Agent',
  description: 'The underlying Mastra Agent.',
},
{
  name: 'inngest',
  type: 'Inngest',
  description: 'The Inngest client.',
},
{
  name: 'cache',
  type: 'MastraServerCache | undefined',
  description: 'The resolved cache instance, if resumable streams are enabled.',
},
{
  name: 'pubsub',
  type: 'PubSub',
  description: 'The PubSub instance used for streaming events.',
},
]}
/>

## 행동 양식 {/* #methods */}

### 실행 {/* #execution */}

#### `stream(messages, options?)` {/* #streammessages-options */}

Ingest의 내구성 있는 실행 엔진을 사용하여 응답을 스트리밍합니다. Workflow는 PubSub 구독이 설정된 후 Ingest 이벤트를 통해 트리거됩니다.

```typescript
const { output, runId, cleanup } = await durableAgent.stream('Hello!', {
  onChunk: chunk => console.log(chunk),
  onFinish: result => console.log('done', result),
})

const text = await output.text
cleanup()
```

보고:[`Promise<InngestAgentStreamResult>`](#inngestagentstreamresult)

#### `resume(runId, resumeData, options?)` {/* #resumerunid-resumedata-options */}

예를 들어 Tool 승인 후 일시 중지된 Ingest 실행을 재개합니다. 스토리지에서 Workflow 스냅샷을 로드하고 일시 중단된 단계를 찾은 다음 재개 이벤트를 Ingest로 보냅니다.

```typescript
const { output, cleanup } = await durableAgent.resume(
  runId,
  {
    approved: true,
  },
  { threadId: 'thread-1', resourceId: 'user-1' },
)

await output.text
cleanup()
```

세 번째 인수는 다음과 같습니다.`threadId` and `resourceId` in addition to the lifecycle callbacks:

<PropertiesTable
  content={[
{
  name: 'threadId',
  type: 'string',
  isOptional: true,
  description: 'Thread ID to associate with the resumed run.',
},
{
  name: 'resourceId',
  type: 'string',
  isOptional: true,
  description: 'Resource ID to associate with the resumed run.',
},
{
  name: 'onChunk',
  type: '(chunk: ChunkType) => void | Promise<void>',
  isOptional: true,
  description: 'Called for each streamed chunk.',
},
{
  name: 'onStepFinish',
  type: '(result: AgentStepFinishEventData) => void | Promise<void>',
  isOptional: true,
  description: 'Called when a step in the agentic loop finishes.',
},
{
  name: 'onFinish',
  type: '(result: AgentFinishEventData) => void | Promise<void>',
  isOptional: true,
  description: 'Called when the run finishes.',
},
{
  name: 'onError',
  type: '(error: Error) => void | Promise<void>',
  isOptional: true,
  description: 'Called when the run errors.',
},
{
  name: 'onSuspended',
  type: '(data: AgentSuspendedEventData) => void | Promise<void>',
  isOptional: true,
  description: 'Called when the run suspends.',
},
]}
/>

보고:[`Promise<InngestAgentStreamResult>`](#inngestagentstreamresult)

#### `generate(messages, options?)` {/* #generatemessages-options */}

Ingest의 내구성 있는 실행 엔진에서 응답을 실행하고 단일로 해결합니다.`FullOutput`. If the run suspends, `generate()` resolves with `finishReason: 'suspended'`. The `runId` option is optional. If you omit it, `generate()` creates a run ID and returns it in `result.runId`. Continue the run with [`resumeGenerate()`](#resumegeneraterunid-resumedata-options). Use [`stream()`](#streammessages-options) with `onSuspended` when the caller needs a suspension callback.

```typescript
const result = await durableAgent.generate('Delete the old records', {
  requireToolApproval: true,
})

result.runId // Generated automatically
result.finishReason // 'suspended' when approval is required
```

보고:`Promise<FullOutput<TOutput>>`

#### `resumeGenerate(runId, resumeData, options?)` {/* #resumegeneraterunid-resumedata-options */}

일시 중지된 작업을 재개합니다.`generate()` run and resolves with a single `FullOutput`.

```typescript
if (!result.runId) {
  throw new Error('Run ID is missing')
}

const resumedResult = await durableAgent.resumeGenerate(result.runId, { approved: true })
```

보고:`Promise<FullOutput<TOutput>>`

#### `observe(runId, options?)` {/* #observerunid-options */}

기존 실행에 다시 연결하여 라이브 이벤트를 전달하기 전에 캐시된 이벤트를 재생합니다. 네트워크 연결이 끊어진 후 사용하세요. 통과하다`offset` to start replay from a known position.

```typescript
const { output, cleanup } = await durableAgent.observe(runId, {
  offset: 0,
  onChunk: chunk => console.log(chunk),
})

await output.text
```

결과`observe()` doesn't include `threadId` or `resourceId`.

보고:`Promise<Omit<InngestAgentStreamResult, 'threadId' | 'resourceId'>>`

:::경고

그만큼`cleanup()` returned by `observe()` destroys the run's registry entries and cached events. Only call it when you are done with the run. If the run is suspended and you intend to resume later, don't call `cleanup()`.

:::

#### `prepare(messages, options?)` {/* #preparemessages-options */}

트리거하지 않고 지속 가능한 실행을 위한 실행을 준비합니다. Ingest Workflow 이벤트를 수동으로 트리거하는 데 사용할 수 있는 직렬화된 Workflow 입력을 반환합니다.

```typescript
const { runId, messageId, workflowInput, threadId, resourceId } = await durableAgent.prepare(
  'Summarize the document',
  {
    memory: { threadId: 'thread-1', resourceId: 'user-1' },
  },
)
```

보고:

```typescript
interface PrepareResult {
  runId: string
  messageId: string
  workflowInput: any
  threadId?: string
  resourceId?: string
}
```

### 내성 {/* #introspection */}

#### `isInngestAgent(obj)` {/* #isinngestagentobj */}

객체가 객체인지 확인하는 유형 가드`InngestAgent`.

```typescript
import { isInngestAgent } from '@mastra/inngest'

if (isInngestAgent(agent)) {
  // agent is InngestAgent
}
```

보고:`boolean`

## 스트림 옵션 {/* #stream-options */}

`stream()`받아들인다`InngestAgentStreamOptions` object. It supports the same agent execution options as [`DurableAgent.stream()`](/reference/agents/durable-agent#stream-options), plus lifecycle callbacks.

<PropertiesTable
  content={[
{
  name: 'runId',
  type: 'string',
  isOptional: true,
  description: 'Unique identifier for this run. Use it later with `resume()` or `observe()`.',
},
{
  name: 'instructions',
  type: "AgentExecutionOptions['instructions']",
  isOptional: true,
  description: "Overrides the agent's default instructions for this run.",
},
{
  name: 'context',
  type: 'ModelMessage[]',
  isOptional: true,
  description: 'Additional context messages to provide to the agent.',
},
{
  name: 'memory',
  type: 'object',
  isOptional: true,
  description: 'Memory configuration for conversation persistence and retrieval.',
},
{
  name: 'requestContext',
  type: 'RequestContext',
  isOptional: true,
  description: 'Request context carrying dynamic configuration and state for this run.',
},
{
  name: 'maxSteps',
  type: 'number',
  isOptional: true,
  description: 'Maximum number of steps to run.',
},
{
  name: 'toolsets',
  type: 'object',
  isOptional: true,
  description: 'Additional tool sets available for this run.',
},
{
  name: 'clientTools',
  type: 'object',
  isOptional: true,
  description: 'Client-side tools available during execution.',
},
{
  name: 'toolChoice',
  type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
  isOptional: true,
  description: 'Tool selection strategy.',
},
{
  name: 'modelSettings',
  type: 'object',
  isOptional: true,
  description: 'Model-specific settings such as temperature.',
},
{
  name: 'requireToolApproval',
  type: 'boolean',
  isOptional: true,
  description: 'Require approval for all tool calls, which suspends the run until resumed.',
},
{
  name: 'autoResumeSuspendedTools',
  type: 'boolean',
  isOptional: true,
  description: 'Automatically resume tools that suspended, instead of waiting for an external `resume()` call.',
},
{
  name: 'toolCallConcurrency',
  type: 'number',
  isOptional: true,
  description: 'Maximum number of tool calls to execute concurrently.',
},
{
  name: 'includeRawChunks',
  type: 'boolean',
  isOptional: true,
  description: 'Include raw provider chunks in the stream output.',
},
{
  name: 'maxProcessorRetries',
  type: 'number',
  isOptional: true,
  description: 'Maximum number of processor retries per generation.',
},
{
  name: 'untilIdle',
  type: 'boolean | { maxIdleMs?: number }',
  isOptional: true,
  description:
    'When set, keeps the stream open across background-task continuations until the agent is idle. Pass `true` for the default 5-minute idle timeout, or `{ maxIdleMs }` to customise.',
},
{
  name: 'onChunk',
  type: '(chunk: ChunkType) => void | Promise<void>',
  isOptional: true,
  description: 'Called for each streamed chunk.',
},
{
  name: 'onStepFinish',
  type: '(result: AgentStepFinishEventData) => void | Promise<void>',
  isOptional: true,
  description: 'Called when a step in the agentic loop finishes.',
},
{
  name: 'onFinish',
  type: '(result: AgentFinishEventData) => void | Promise<void>',
  isOptional: true,
  description: 'Called when the run finishes.',
},
{
  name: 'onError',
  type: '(error: Error) => void | Promise<void>',
  isOptional: true,
  description: 'Called when the run errors.',
},
{
  name: 'onSuspended',
  type: '(data: AgentSuspendedEventData) => void | Promise<void>',
  isOptional: true,
  description: 'Called when the run suspends, for example for tool approval.',
},
]}
/>

`observe()`수명 주기 콜백을 허용합니다(`onChunk`, `onStepFinish`, `onFinish`, `onError`, `onSuspended`) plus an `offset` to control where replay starts.

## `InngestAgentStreamResult` {/* #inngestagentstreamresult */}

반환된 객체`stream()` and `resume()`. The `observe()` method returns the same shape but omits `threadId` and `resourceId`.

```typescript
interface InngestAgentStreamResult<OUTPUT = undefined> {
  output: MastraModelOutput<OUTPUT>
  readonly fullStream: ReadableStream<any>
  runId: string
  threadId?: string
  resourceId?: string
  cleanup: () => void
}
```

<PropertiesTable
  content={[
{
  name: 'output',
  type: 'MastraModelOutput',
  description: 'The streaming output. Await `output.text` for the full text, or consume `output.fullStream`.',
},
{
  name: 'fullStream',
  type: 'ReadableStream',
  description: 'The full event stream, delegating to `output.fullStream`.',
},
{
  name: 'runId',
  type: 'string',
  description: 'The unique run ID. Pass it to `resume()` or `observe()` to reconnect.',
},
{
  name: 'threadId',
  type: 'string',
  isOptional: true,
  description: 'Thread ID when using memory.',
},
{
  name: 'resourceId',
  type: 'string',
  isOptional: true,
  description: 'Resource ID when using memory.',
},
{
  name: 'cleanup',
  type: '() => void',
  description: 'Unsubscribes from PubSub and clears registry entries for the run. Call it when done with the run.',
},
]}
/>

## Ingest 기능 제공 {/* #serving-inngest-functions */}

그만큼`@mastra/inngest` package provides `serve()` and `createServe()` to register Inngest workflow functions with your HTTP framework.

### `serve(options)` {/* #serveoptions */}

Hono(기본 프레임워크)를 사용하여 Mastra Workflow를 제공합니다. Mastra에서 모든 Ingest 지원 Workflow우를 수집하고 이를 Ingest 기능으로 등록합니다.

```typescript
import { serve } from '@mastra/inngest'

app.use('/inngest/api', async c => {
  return serve({ mastra, inngest })(c)
})
```

### `createServe(adapter)` {/* #createserveadapter */}

모든 Ingest 서버 어댑터를 수용하는 공장(`inngest/express`, `inngest/fastify`, `inngest/next`, etc.) and returns a serve function for that framework.

```typescript title="Express"
import { createServe } from '@mastra/inngest'
import { serve } from 'inngest/express'

const serveExpress = createServe(serve)
app.use('/inngest/api', serveExpress({ mastra, inngest }))
```

```typescript title="Next.js"
import { createServe } from '@mastra/inngest'
import { serve } from 'inngest/next'

const serveNext = createServe(serve)
export const { GET, POST, PUT } = serveNext({ mastra, inngest })
```

### 서브 옵션 {/* #serve-options */}

<PropertiesTable
  content={[
{
  name: 'mastra',
  type: 'Mastra',
  isOptional: false,
  description: 'The Mastra instance containing registered agents and workflows.',
},
{
  name: 'inngest',
  type: 'Inngest',
  isOptional: false,
  description: 'The Inngest client instance.',
},
{
  name: 'functions',
  type: 'InngestFunction.Like[]',
  isOptional: true,
  description: 'Additional Inngest functions to serve alongside Mastra workflows.',
},
{
  name: 'registerOptions',
  type: 'RegisterOptions',
  isOptional: true,
  description: 'Options passed to the Inngest registration handler.',
},
]}
/>

## 관련된 {/* #related */}

* [내구성 Agent 참조](/reference/agents/durable-agent)
* [Agent 클래스](/reference/agents/agent)
* [Ingest 배포 가이드](/guides/deployment/inngest)
* [PubSub](/docs/server/pubsub)
