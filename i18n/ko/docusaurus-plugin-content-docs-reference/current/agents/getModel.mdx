---
title: "참조: Agent.getModel() | Agent"
description: "Agent를 구동하는 언어 Model을 검색하는 Mastra Agent의 `Agent.getModel()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Agent.getModel()

그만큼`.getModel()`메서드는 Agent에 대해 구성된 언어 Model을 검색하여 함수인 경우 이를 확인합니다. 이 방법은 Agent의 기능을 강화하는 기본 Model에 액세스하는 데 사용됩니다.

## 사용예 {/* #usage-example */}

```typescript
await agent.getModel()
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'options',
  type: '{ requestContext?: RequestContext }',
  isOptional: true,
  defaultValue: '{}',
  description: 'Optional configuration object containing request context.',
  properties: [
    {
      type: '{ requestContext?: RequestContext }',
      parameters: [
        {
          name: 'requestContext',
          type: 'RequestContext',
          isOptional: true,
          defaultValue: 'undefined',
          description: 'Request Context for dependency injection and contextual information.',
        },
      ],
    },
  ],
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'model',
  type: 'MastraLanguageModel | Promise<MastraLanguageModel>',
  description:
    'The language model configured for the agent, either as a direct instance or a promise that resolves to the model.',
},
]}
/>

## 확장된 사용 예 {/* #extended-usage-example */}

```typescript
await agent.getModel({
  requestContext: new RequestContext(),
})
```

## 관련된 {/* #related */}

* [Agent 개요](/docs/agents/overview)
* [요청 컨텍스트](/docs/server/request-context)
