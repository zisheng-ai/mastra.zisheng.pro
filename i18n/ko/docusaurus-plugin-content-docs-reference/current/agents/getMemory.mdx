---
title: "참조: Agent.getMemory() | Agent"
description: "Agent와 연결된 Memory 시스템을 검색하는 Mastra Agent의 `Agent.getMemory()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Agent.getMemory()

그만큼`.getMemory()`메소드는 Agent와 연관된 Memory 시스템을 검색합니다. 이 방법은 대화 전반에 걸쳐 정보를 저장하고 검색하기 위해 Agent의 Memory 기능에 액세스하는 데 사용됩니다.

## 사용예 {/* #usage-example */}

```typescript
await agent.getMemory()
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'options',
  type: '{ requestContext?: RequestContext }',
  isOptional: true,
  defaultValue: '{}',
  description: '요청 컨텍스트가 포함된 선택적 구성 객체입니다.',
  properties: [    {
      type: '{ requestContext?: RequestContext }',
      parameters: [        {
          name: 'requestContext',
          type: 'RequestContext',
          isOptional: true,
          defaultValue: 'new RequestContext()',
          description: '종속성 주입 및 컨텍스트 정보를 위한 Request Context입니다.',
        },
      ],
    },
  ],
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'memory',
  type: 'Promise<MastraMemory | undefined>',
  description:
    'Agent에 구성된 Memory 시스템으로 해석되는 프로미스입니다. Memory 시스템이 구성되지 않은 경우 undefined로 해석됩니다.',
},
]}
/>
## 확장된 사용 예 {/* #extended-usage-example */}

```typescript
await agent.getMemory({
  requestContext: new RequestContext(),
})
```

## 관련된 {/* #related */}

* [Memory](/docs/memory/overview)
* [요청 컨텍스트](/docs/server/request-context)
