---
title: "참조: Agent.getMetadata() | Agent"
description: "Agent에 대해 구성된 메타데이터를 검색하는 Mastra Agent의 `Agent.getMetadata()` 메서드에 대한 문서입니다."
packages:
  - '@mastra/core'
---

# Agent.getMetadata()

그만큼`.getMetadata()`메서드는 Agent에 대해 구성된 메타데이터를 검색하여 함수인 경우 이를 확인합니다. Agent ID 또는 이름에 해당 데이터를 인코딩하지 않고 메타데이터를 사용하여 클라이언트의 Agent를 분류합니다.

## 사용예 {/* #usage-example */}

```typescript
await agent.getMetadata()
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'options',
  type: '{ requestContext?: RequestContext }',
  isOptional: true,
  defaultValue: '{}',
  description: 'Optional configuration object containing request context.',
  properties: [
    {
      type: '{ requestContext?: RequestContext }',
      parameters: [
        {
          name: 'requestContext',
          type: 'RequestContext',
          isOptional: true,
          defaultValue: 'undefined',
          description: 'Request Context passed to dynamic metadata functions. Ignored when metadata is static.',
        },
      ],
    },
  ],
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'metadata',
  type: 'Record<string, unknown> | Promise<Record<string, unknown>>',
  description:
    'The metadata configured for the agent, or `undefined` if no metadata was configured. Returns either directly or as a promise that resolves to the metadata when defined as a function.',
},
]}
/>

## 확장된 사용 예 {/* #extended-usage-example */}

정적 메타데이터:

```typescript title="src/mastra/agents/support-agent.ts"
import { Agent } from '@mastra/core/agent'

export const supportAgent = new Agent({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'You help customers with support requests.',
  model: '__GATEWAY_OPENAI_MODEL__',
  metadata: { type: 'support' },
})

const metadata = supportAgent.getMetadata()
```

요청 컨텍스트에서 확인된 동적 메타데이터:

```typescript title="src/mastra/agents/support-agent.ts"
import { Agent } from '@mastra/core/agent'

export const supportAgent = new Agent({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'You help customers with support requests.',
  model: '__GATEWAY_OPENAI_MODEL__',
  metadata: ({ requestContext }) => ({
    type: 'support',
    tenant: requestContext.get('tenant'),
  }),
})

const metadata = await supportAgent.getMetadata({ requestContext })
```

## 관련된 {/* #related */}

* [Agent 개요](/docs/agents/overview)
* [요청 컨텍스트](/docs/server/request-context)
