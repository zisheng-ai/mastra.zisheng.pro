---
title: "참조: createSkill() | 기술"
description: "파일 시스템 없이 인라인 Agent 기술을 생성하기 위한 팩토리 함수인 createSkill()에 대한 API 참조입니다."
packages:
  - "@mastra/core"
---

# `createSkill()`

Agent에게 직접 전달할 수 있는 인라인 스킬을 생성합니다.`skills`파일 시스템이나 작업 공간이 없는 구성입니다.

반환된 객체는 다음을 구현합니다.`Skill` interface and works anywhere a filesystem-discovered skill works.

## 사용예 {/* #usage-example */}

```typescript title="src/mastra/skills.ts"
import { createSkill } from '@mastra/core/skills'

const reviewSkill = createSkill({
  name: 'code-review',
  description: 'Use when reviewing code changes.',
  instructions: `
    When reviewing code:
    1. Check for correctness
    2. Check for style consistency
    3. Look for potential bugs
  `,
  references: {
    'checklist.md': '# Review Checklist\n- No unused imports\n- Error handling present',
  },
})
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'name',
  type: 'string',
  description:
    'Unique skill name. Used as the identifier when agents activate the skill. Must be a valid slug (lowercase, hyphens allowed).',
},
{
  name: 'description',
  type: 'string',
  description:
    'Short description shown to the agent in the system message. Helps the agent decide when to use the skill.',
},
{
  name: 'instructions',
  type: 'string',
  description:
    'The full skill instructions in markdown. Loaded into the conversation when the agent activates the skill.',
},
{
  name: 'references',
  type: 'Record<string, string>',
  isOptional: true,
  description:
    'Supporting documents the agent can read with the `skill_read` tool. Keys are filenames, values are file contents.',
},
{
  name: 'license',
  type: 'string',
  isOptional: true,
  description: 'SPDX license identifier for the skill.',
},
{
  name: 'compatibility',
  type: 'unknown',
  isOptional: true,
  description:
    'Compatibility requirements. The Agent Skills spec leaves this flexible — can be a string array, object, or any structure your tooling expects.',
},
{
  name: 'user-invocable',
  type: 'boolean',
  isOptional: true,
  description: 'Whether end users can invoke this skill directly. Defaults to `undefined` (agent decides).',
},
{
  name: 'metadata',
  type: 'Record<string, unknown>',
  isOptional: true,
  description: 'Arbitrary metadata attached to the skill.',
},
]}
/>

## 반환 값 {/* #return-value */}

반환`InlineSkill` object that implements the `Skill` interface:

```typescript
interface InlineSkill extends Skill {
  __inline: true
  __referenceContents: Record<string, string>
}
```

스킬에는 다음과 같은 합성 경로가 할당됩니다.`inline/<name>` (e.g., `inline/code-review`).

## 확인 {/* #validation */}

`createSkill()`파일 시스템 기술과 동일한 규칙을 사용하여 입력의 유효성을 검사합니다.

* `name`필수이며 유효한 슬러그여야 합니다.
* `description`필수
* `instructions`필수이며 비어 있지 않습니다.

유효성 검사에 실패하면`Error` is thrown with details about the invalid fields.

```typescript
// Throws: Invalid skill "": name is required
createSkill({ name: '', description: 'test', instructions: 'test' })
```

## 관련된 {/* #related */}

* [Agent 기술](/docs/agents/skills)
* [작업 공간 기술](/docs/workspace/skills)
* [`.getSkill()`참조](/reference/agents/getSkill)
* [`.listSkills()`참조](/reference/agents/listSkills)
