---
title: "참조: .listSkills() | Agent"
description: "구성된 모든 기술에 대한 메타데이터를 반환하는 Agent .listSkills() 메서드에 대한 API 참조입니다."
packages:
  - "@mastra/core"
---

# `.listSkills()`

Agent 수준 기술과 작업 영역 기술을 모두 포함하여 Agent이 사용할 수 있는 모든 기술에 대한 메타데이터를 반환합니다.

## 사용예 {/* #usage-example */}

```typescript title="src/routes/skills.ts"
import { agent } from '../mastra/agents'

const skills = await agent.listSkills()

for (const skill of skills) {
  console.log(`${skill.name}: ${skill.description}`)
}
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'options',
  type: 'object',
  isOptional: true,
  description: 'Options for skill resolution.',
  properties: [
    {
      type: 'object',
      parameters: [
        {
          name: 'requestContext',
          type: 'RequestContext',
          isOptional: true,
          description: 'Request context passed to dynamic skill resolvers.',
        },
      ],
    },
  ],
},
]}
/>

## 반환 값 {/* #return-value */}

보고`Promise<SkillMetadata[]>`.

각 항목에는 검색된 기술에 대한 메타데이터가 포함되어 있습니다.

```typescript
interface SkillMetadata {
  name: string
  description: string
  path: string
  source: { type: string; projectPath: string }
  references: string[]
  scripts: string[]
  assets: string[]
  license?: string
  compatibility?: string[]
  'user-invocable'?: boolean
  metadata?: Record<string, unknown>
}
```

## 병합 동작 {/* #merging-behavior */}

Agent 수준 기술과 작업 영역 기술이 모두 구성된 경우`.listSkills()` returns the merged set. Agent-level skills take precedence on name conflicts. If both define a skill called `code-review`, only the agent-level version is returned.

## 관련된 {/* #related */}

* [Agent 기술](/docs/agents/skills)
* [`.getSkill()`참조](/reference/agents/getSkill)
* [`createSkill()`참조](/reference/agents/createSkill)
