---
title: "참조: MastraAuthClerk 클래스 | 인증"
description: "Clerk 인증을 사용하여 Mastra 애플리케이션을 인증하는 MastraAuthClerk 클래스에 대한 API 참조입니다."
packages:
  - "@mastra/auth-clerk"
  - "@mastra/core"
---

# MastraAuthClerk 클래스

그만큼`MastraAuthClerk`클래스는 Clerk를 사용하여 Mastra 애플리케이션에 대한 인증을 제공합니다. 사무원이 발행한 JWT 토큰으로 들어오는 요청을 확인하고 다음을 사용하여 Mastra 서버와 통합합니다.`auth`옵션.

## 사용예 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthClerk } from '@mastra/auth-clerk'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthClerk({
      jwksUri: process.env.CLERK_JWKS_URI,
      publishableKey: process.env.CLERK_PUBLISHABLE_KEY,
      secretKey: process.env.CLERK_SECRET_KEY,
    }),
  },
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'publishableKey',
  type: 'string',
  description: 'Clerk 게시 가능 키입니다. Clerk Dashboard의 API Keys에서 찾을 수 있습니다.',
  isOptional: true,
  defaultValue: 'process.env.CLERK_PUBLISHABLE_KEY',
},{
  name: 'secretKey',
  type: 'string',
  description: 'Clerk 비밀 키입니다. 서버 측 인증 및 토큰 검증에 사용됩니다.',
  isOptional: true,
  defaultValue: 'process.env.CLERK_SECRET_KEY',
},{
  name: 'jwksUri',
  type: 'string',
  description: 'Clerk 애플리케이션의 JWKS URI입니다. JWT 서명을 검증하는 데 사용됩니다.',
  isOptional: true,
  defaultValue: 'process.env.CLERK_JWKS_URI',
},{
  name: 'name',
  type: 'string',
  description: '인증 Provider 인스턴스의 사용자 정의 이름입니다.',
  isOptional: true,
},{
  name: 'authorizeUser',
  type: '(user: User, request: HonoRequest) => Promise<boolean> | boolean',
  description:
    '사용자에게 액세스를 허용할지 결정하는 사용자 정의 권한 부여 함수입니다. 토큰 검증 후 호출됩니다. 기본적으로 모든 인증된 사용자를 허용합니다.',
  isOptional: true,
},
]}
/>
## 관련된 {/* #related */}

[MastraAuthClerk 클래스](/docs/server/auth/clerk)
