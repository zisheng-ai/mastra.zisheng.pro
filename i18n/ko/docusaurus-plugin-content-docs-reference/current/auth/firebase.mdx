---
title: "참조: MastraAuthFirebase 클래스 | 인증"
description: "Firebase 인증을 사용하여 Mastra 애플리케이션을 인증하는 MastraAuthFirebase 클래스에 대한 API 참조입니다."
packages:
  - "@mastra/auth-firebase"
  - "@mastra/core"
---

# MastraAuthFirebase 클래스

그만큼`MastraAuthFirebase`클래스는 Firebase 인증을 사용하여 Mastra에 대한 인증을 제공합니다. Firebase ID 토큰을 사용하여 들어오는 요청을 확인하고 다음을 사용하여 Mastra 서버와 통합합니다.`auth`옵션.

## 사용 예 {/* #usage-examples */}

### 환경 변수를 사용한 기본 사용법 {/* #basic-usage-with-environment-variables */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthFirebase } from '@mastra/auth-firebase'

// Automatically uses FIREBASE_SERVICE_ACCOUNT and FIRESTORE_DATABASE_ID env vars
export const mastra = new Mastra({
  server: {
    auth: new MastraAuthFirebase(),
  },
})
```

### 맞춤 구성 {/* #custom-configuration */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthFirebase } from '@mastra/auth-firebase'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthFirebase({
      serviceAccount: '/path/to/service-account-key.json',
      databaseId: 'your-database-id',
    }),
  },
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'serviceAccount',
  type: 'string',
  description:
    'Path to the Firebase service account JSON file. This file contains the credentials needed to verify Firebase ID tokens on the server side.',
  isOptional: true,
  defaultValue: 'process.env.FIREBASE_SERVICE_ACCOUNT',
},
{
  name: 'databaseId',
  type: 'string',
  description: "The Firestore database ID to use. Typically '(default)' for the default database.",
  isOptional: true,
  defaultValue: 'process.env.FIRESTORE_DATABASE_ID || process.env.FIREBASE_DATABASE_ID',
},
{
  name: 'name',
  type: 'string',
  description: 'Custom name for the auth provider instance.',
  isOptional: true,
  defaultValue: '"firebase"',
},
{
  name: 'authorizeUser',
  type: '(user: FirebaseUser) => Promise<boolean> | boolean',
  description:
    "Custom authorization function to determine if a user should be granted access. Called after token verification. By default, checks for the presence of a document in the 'user_access' collection keyed by the user's UID.",
  isOptional: true,
},
]}
/>

## 환경변수 {/* #environment-variables */}

생성자 옵션이 제공되지 않으면 다음 환경 변수가 자동으로 사용됩니다.

<PropertiesTable
  content={[
{
  name: 'FIREBASE_SERVICE_ACCOUNT',
  type: 'string',
  description: 'Path to Firebase service account JSON file. Used if serviceAccount option is not provided.',
  isOptional: true,
},
{
  name: 'FIRESTORE_DATABASE_ID',
  type: 'string',
  description: 'Firestore database ID. Primary environment variable for database configuration.',
  isOptional: true,
},
{
  name: 'FIREBASE_DATABASE_ID',
  type: 'string',
  description:
    'Alternative environment variable for Firestore database ID. Used if FIRESTORE_DATABASE_ID is not set.',
  isOptional: true,
},
]}
/>

## 기본 인증 동작 {/* #default-authorization-behavior */}

기본적으로`MastraAuthFirebase` uses Firestore to manage user access:

1. Firebase ID 토큰을 성공적으로 확인한 후`authorizeUser` method is called
2. 에 문서가 있는지 확인합니다.`user_access` collection with the user's UID as the document ID
3. 문서가 존재하면 사용자에게 권한이 부여됩니다. 그렇지 않으면 액세스가 거부됩니다.
4. 사용되는 Firestore 데이터베이스는 다음에 의해 결정됩니다.`databaseId` parameter or environment variables

## Firebase 사용자 유형 {/* #firebase-user-type */}

그만큼`FirebaseUser` type used in the `authorizeUser` function corresponds to Firebase's `DecodedIdToken` interface, which includes:

* `uid`: 사용자의 고유 식별자
* `email`: 사용자의 이메일 주소(사용 가능한 경우)
* `email_verified`: 이메일 인증 여부
* `name`: 사용자의 표시 이름(사용 가능한 경우)
* `picture`: 사용자 프로필 사진의 URL(사용 가능한 경우)
* `auth_time`: 사용자가 인증한 경우
* 기타 표준 JWT 주장

## 관련된 {/* #related */}

[MastraAuthFirebase 클래스](/docs/server/auth/firebase)
