---
title: "참조: MastraAuthSupabase 클래스 | 인증"
description: "Supabase Auth를 사용하여 Mastra 애플리케이션을 인증하는 MastraAuthSupabase 클래스에 대한 API 참조입니다."
packages:
  - "@mastra/auth-supabase"
  - "@mastra/core"
---

# MastraAuthSupabase 클래스

그만큼`MastraAuthSupabase`클래스는 Supabase Auth를 사용하여 Mastra에 대한 인증을 제공합니다. Supabase의 인증 시스템을 사용하여 들어오는 요청을 확인하고 다음을 사용하여 Mastra 서버와 통합합니다.`auth`옵션.

## 사용예 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthSupabase } from '@mastra/auth-supabase'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthSupabase({
      url: process.env.SUPABASE_URL,
      anonKey: process.env.SUPABASE_ANON_KEY,
    }),
  },
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'url',
  type: 'string',
  description: 'Supabase 프로젝트의 URL입니다. Supabase 프로젝트 설정에서 확인할 수 있습니다.',
  isOptional: true,
  defaultValue: 'process.env.SUPABASE_URL',
},{
  name: 'anonKey',
  type: 'string',
  description: 'Supabase 프로젝트의 익명/공개 키입니다. 클라이언트 측 인증에 사용됩니다.',
  isOptional: true,
  defaultValue: 'process.env.SUPABASE_ANON_KEY',
},{
  name: 'name',
  type: 'string',
  description: '인증 Provider 인스턴스의 사용자 지정 이름입니다.',
  isOptional: true,
},{
  name: 'authorizeUser',
  type: '(user: User, request: HoneRequest) => Promise<boolean> | boolean',
  description:
    "사용자에게 액세스를 허용할지 결정하는 사용자 지정 권한 부여 함수입니다. 토큰 검증 후 호출됩니다. 기본적으로 'users' 테이블의 'isAdmin' 열을 확인합니다.",
  isOptional: true,
},
]}
/>
## 관련된 {/* #related */}

[MastraAuthSupabase](/docs/server/auth/supabase)
