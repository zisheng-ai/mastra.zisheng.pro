---
title: "참고: Hono 어댑터 | 섬기는 사람"
description: "@mastra/hono 서버 어댑터에 대한 API 참조입니다."
packages:
  - "@mastra/hono"
---

import Steps from "@site/src/components/Steps";
import StepItem from "@site/src/components/StepItem";
import PropertiesTable from "@site/src/components/PropertiesTable";

# 호노 어댑터

그만큼`@mastra/hono`패키지는 Mastra를 실행하기 위한 서버 어댑터를 제공합니다.[호노](https://hono.dev). 일반적인 어댑터 개념(생성자 옵션, 초기화 흐름 등)은 다음을 참조하세요.[서버 어댑터](/docs/server/server-adapters).

## 설치 {/* #installation */}

Hono 어댑터 및 Hono 프레임워크를 설치합니다.

```bash npm2yarn
npm install @mastra/hono@latest hono
```

## 사용예 {/* #usage-example */}

```typescript title="server.ts"
import { Hono } from 'hono'
import { HonoBindings, HonoVariables, MastraServer } from '@mastra/hono'
import { mastra } from './mastra'

const app = new Hono<{ Bindings: HonoBindings; Variables: HonoVariables }>()
const server = new MastraServer({ app, mastra })

await server.init()

export default app
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'app',
  type: 'Hono',
  description: 'Hono app instance',
  isOptional: false,
},
{
  name: 'mastra',
  type: 'Mastra',
  description: 'Mastra instance',
  isOptional: false,
},
{
  name: 'prefix',
  type: 'string',
  description: 'Route path prefix (e.g., `/api/v2`)',
  isOptional: true,
  defaultValue: "''",
},
{
  name: 'openapiPath',
  type: 'string',
  description: 'Path to serve OpenAPI spec (e.g., `/openapi.json`)',
  isOptional: true,
  defaultValue: "''",
},
{
  name: 'bodyLimitOptions',
  type: '{ maxSize: number, onError: (err) => unknown }',
  description: 'Request body size limits',
  isOptional: true,
},
{
  name: 'streamOptions',
  type: '{ redact?: boolean }',
  description: 'Stream redaction config. When true, redacts sensitive data from streams.',
  isOptional: true,
  defaultValue: '{ redact: true }',
},
{
  name: 'customRouteAuthConfig',
  type: 'Map<string, boolean>',
  description:
    'Per-route auth overrides. Keys are `METHOD:PATH` (e.g., `GET:/api/health`). Value `false` makes route public, `true` requires auth.',
  isOptional: true,
},
{
  name: 'tools',
  type: 'Record<string, Tool>',
  description: 'Available tools for the server',
  isOptional: true,
},
{
  name: 'taskStore',
  type: 'InMemoryTaskStore',
  description: 'Task store for A2A (Agent-to-Agent) operations',
  isOptional: true,
},
{
  name: 'mcpOptions',
  type: 'MCPOptions',
  typeDescription: '{ serverless?: boolean, sessionIdGenerator?: () => string }',
  description:
    'MCP transport options. Set `serverless: true` for stateless environments like Cloudflare Workers or Vercel Edge.',
  isOptional: true,
},
]}
/>

## 커스텀 경로 추가 {/* #adding-custom-routes */}

Hono 앱에 직접 경로를 추가하세요.

```typescript title="server.ts"
import { Hono } from 'hono'
import { HonoBindings, HonoVariables, MastraServer } from '@mastra/hono'

const app = new Hono<{ Bindings: HonoBindings; Variables: HonoVariables }>()
const server = new MastraServer({ app, mastra })

// Before init - runs before Mastra middleware
app.get('/early-health', c => c.json({ status: 'ok' }))

await server.init()

// After init - has access to Mastra context
app.get('/custom', c => {
  const mastraInstance = c.get('mastra')
  return c.json({ agents: Object.keys(mastraInstance.listAgents()) })
})
```

:::팁

이전에 추가된 경로`init()` run without Mastra context. Add routes after `init()` to access the Mastra instance and request context.

:::

다음과 같은 Mastra 관리 인증 및 경로 메타데이터를 원하는 경우`requiresAuth`, prefer [`registerApiRoute()`](/reference/server/register-api-route). For raw Hono routes mounted directly on `app`, use `createAuthMiddleware()`:

```typescript title="server.ts"
import { Hono } from 'hono'
import { createAuthMiddleware, HonoBindings, HonoVariables, MastraServer } from '@mastra/hono'
import { mastra } from './mastra'

const app = new Hono<{ Bindings: HonoBindings; Variables: HonoVariables }>()
const server = new MastraServer({ app, mastra })

await server.init()

app.get('/custom/protected', createAuthMiddleware({ mastra }), c => {
  const user = c.get('requestContext').get('user')
  return c.json({ user })
})

app.get('/custom/public', createAuthMiddleware({ mastra, requiresAuth: false }), c => {
  return c.json({ ok: true })
})
```

## 컨텍스트에 액세스 {/* #accessing-context */}

Hono 미들웨어 및 경로 핸들러에서 다음을 통해 Mastra 컨텍스트에 액세스합니다.`c.get()`:

```typescript
app.get('/custom', async c => {
  const mastra = c.get('mastra')
  const requestContext = c.get('requestContext')
  const abortSignal = c.get('abortSignal')

  const agent = mastra.getAgent('myAgent')
  return c.json({ agent: agent.name })
})
```

사용 가능한 컨텍스트 키:

| 열쇠 | 설명 |
| - | - |
|`mastra` | Mastra instance |
| `requestContext` | Request context map |
| `abortSignal` | Request cancellation signal |
| `tools` | Available tools |
| `taskStore` | Task store for A2A operations |
| `customRouteAuthConfig` | Per-route auth overrides |
| `user` | Authenticated user (if auth configured) |

## 미들웨어 추가 {/* #adding-middleware */}

이전 또는 이후에 Hono 미들웨어 추가`init()`:

```typescript title="server.ts"
import { Hono } from 'hono'
import { HonoBindings, HonoVariables, MastraServer } from '@mastra/hono'

const app = new Hono<{ Bindings: HonoBindings; Variables: HonoVariables }>()

// Middleware before init
app.use('*', async (c, next) => {
  console.log(`${c.req.method} ${c.req.url}`)
  await next()
})

const server = new MastraServer({ app, mastra })
await server.init()

// Middleware after init has access to Mastra context
app.use('*', async (c, next) => {
  const mastra = c.get('mastra')
  await next()
})
```

## 수동 초기화 {/* #manual-initialization */}

사용자 정의 미들웨어 주문의 경우 대신 각 메소드를 개별적으로 호출하십시오.`init()`. See [manual initialization](/docs/server/server-adapters#manual-initialization) for details.

## 예 {/* #examples */}

* [호노 어댑터](https://github.com/mastra-ai/mastra/tree/main/examples/server-hono-adapter): 기본 Hono 서버 설정

## 관련된 {/* #related */}

* [서버 어댑터](/docs/server/server-adapters): 공유 어댑터 개념
* [Mastra서버 참조](/reference/server/mastra-server): 전체 API 참조
* [createRoute() 참조](/reference/server/create-route): 유형이 안전한 사용자 지정 경로 만들기
