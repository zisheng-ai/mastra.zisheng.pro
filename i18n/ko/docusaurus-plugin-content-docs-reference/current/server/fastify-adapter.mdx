---
title: "참조: 어댑터 고정 | 섬기는 사람"
description: "@mastra/fastify 서버 어댑터에 대한 API 참조입니다."
packages:
  - "@mastra/fastify"
---

import Steps from "@site/src/components/Steps";
import StepItem from "@site/src/components/StepItem";
import PropertiesTable from "@site/src/components/PropertiesTable";

# 어댑터 고정

그만큼`@mastra/fastify`패키지는 Mastra를 실행하기 위한 서버 어댑터를 제공합니다.[고정하다](https://fastify.dev). 일반적인 어댑터 개념(생성자 옵션, 초기화 흐름 등)은 다음을 참조하세요.[서버 어댑터](/docs/server/server-adapters).

## 설치 {/* #installation */}

Fastify 어댑터 및 Fastify 프레임워크를 설치합니다.

```bash npm2yarn
npm install @mastra/fastify@latest fastify
```

## 사용예 {/* #usage-example */}

```typescript title="server.ts"
import Fastify from 'fastify'
import { MastraServer } from '@mastra/fastify'
import { mastra } from './mastra'

const app = Fastify({ logger: true })
const server = new MastraServer({ app, mastra })

await server.init()

app.listen({ port: 3000 }, (err, address) => {
  if (err) {
    console.error(err)
    process.exit(1)
  }
  console.log(`Server running on ${address}`)
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'app',
  type: 'FastifyInstance',
  description: 'Fastify 앱 인스턴스',
  isOptional: false,
},{
  name: 'mastra',
  type: 'Mastra',
  description: 'Mastra 인스턴스',
  isOptional: false,
},{
  name: 'prefix',
  type: 'string',
  description: '경로 경로 접두사(예: `/api/v2`)',
  isOptional: true,
  defaultValue: "''",
},{
  name: 'openapiPath',
  type: 'string',
  description: 'OpenAPI 명세를 제공할 경로(예: `/openapi.json`)',
  isOptional: true,
  defaultValue: "''",
},{
  name: 'bodyLimitOptions',
  type: 'BodyLimitOptions',
  typeDescription: '{ maxSize: number, onError: (error: unknown) => unknown }',
  description: '요청 본문 크기 제한',
  isOptional: true,
},{
  name: 'streamOptions',
  type: 'StreamOptions',
  typeDescription: '{ redact?: boolean }',
  description:
    '스트림 마스킹 구성입니다. true(기본값)이면 클라이언트로 보내기 전에 스트림 청크에서 민감한 데이터(시스템 Prompt, Tool 정의, API 키)를 마스킹합니다.',
  isOptional: true,
  defaultValue: '{ redact: true }',
},{
  name: 'customRouteAuthConfig',
  type: 'Map<string, boolean>',
  description:
    '경로별 인증 재정의입니다. 키는 `METHOD:PATH` 형식입니다(예: `GET:/api/health`). 값이 `false`이면 경로가 공개되고, `true`이면 인증이 필요합니다.',
  isOptional: true,
},{
  name: 'tools',
  type: 'ToolsInput',
  typeDescription: 'Record<string, ToolAction | VercelTool>',
  description: '서버에서 사용할 수 있는 Tool',
  isOptional: true,
},{
  name: 'taskStore',
  type: 'InMemoryTaskStore',
  description: 'A2A(Agent-to-Agent) 작업을 위한 작업 저장소',
  isOptional: true,
},{
  name: 'mcpOptions',
  type: 'MCPOptions',
  typeDescription: '{ serverless?: boolean, sessionIdGenerator?: () => string }',
  description:
    'MCP 전송 옵션입니다. Cloudflare Workers 또는 Vercel Edge 같은 상태 비저장 환경에서는 `serverless: true`로 설정하세요.',
  isOptional: true,
},
]}
/>
## 원시 경로 보호 {/* #protecting-raw-routes */}

`requiresAuth` 같은 Mastra 관리 인증 및 경로 메타데이터가 필요하다면 [`registerApiRoute()`](/reference/server/register-api-route)를 사용하는 것이 좋습니다. 앱에 직접 마운트된 원시 Fastify 경로에는 `createAuthMiddleware()`를 사용하세요.
```typescript title="server.ts"
import Fastify from 'fastify'
import { createAuthMiddleware, MastraServer } from '@mastra/fastify'
import { mastra } from './mastra'

const app = Fastify()
const server = new MastraServer({ app, mastra })

await server.init()

app.get('/custom/protected', { preHandler: createAuthMiddleware({ mastra }) }, async request => {
  const user = request.requestContext.get('user')
  return { user }
})

app.get(
  '/custom/public',
  { preHandler: createAuthMiddleware({ mastra, requiresAuth: false }) },
  async () => {
    return { ok: true }
  },
)
```

## 수동 초기화 {/* #manual-initialization */}

사용자 정의 미들웨어 순서가 필요하다면 `init()` 대신 각 메서드를 개별적으로 호출하세요. 자세한 내용은 [수동 초기화](/docs/server/server-adapters#manual-initialization)를 참조하세요.
## 예 {/* #examples */}

* [고정 어댑터](https://github.com/mastra-ai/mastra/tree/main/examples/server-fastify-adapter): 기본 Fastify 서버 설정

## 관련된 {/* #related */}

* [서버 어댑터](/docs/server/server-adapters): 공유 어댑터 개념
* [Mastra서버 참조](/reference/server/mastra-server): 전체 API 참조
* [createRoute() 참조](/reference/server/create-route): 유형이 안전한 사용자 지정 경로 만들기
