---
title: "참조: 삽입 | 조각"
description: "AI SDK를 사용하여 Mastra에 기능을 삽입하는 방법에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 포함시키다

Mastra는 AI SDK를 사용합니다.`embed`그리고`embedMany`텍스트 입력에 대한 벡터 임베딩을 생성하는 기능을 통해 유사성 검색 및 RAG Workflow를 활성화합니다.

## 단일 임베딩 {/* #single-embedding */}

그만큼`embed` function generates a vector embedding for a single text input:

```typescript
import { embed } from 'ai'
import { ModelRouterEmbeddingModel } from '@mastra/core/llm'

const result = await embed({
  model: new ModelRouterEmbeddingModel('openai/text-embedding-3-small'),
  value: 'Your text to embed',
  maxRetries: 2, // optional, defaults to 2
})
```

### 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'model',
  type: 'EmbeddingModel',
  description: "The embedding model to use (e.g. openai.embedding('text-embedding-3-small'))",
},
{
  name: 'value',
  type: 'string | Record<string, any>',
  description: 'The text content or object to embed',
},
{
  name: 'maxRetries',
  type: 'number',
  description: 'Maximum number of retries per embedding call. Set to 0 to disable retries.',
  isOptional: true,
  defaultValue: '2',
},
{
  name: 'abortSignal',
  type: 'AbortSignal',
  description: 'Optional abort signal to cancel the request',
  isOptional: true,
},
{
  name: 'headers',
  type: 'Record<string, string>',
  description: 'Additional HTTP headers for the request (only for HTTP-based providers)',
  isOptional: true,
},
]}
/>

### 반환 값 {/* #return-value */}

<PropertiesTable
  content={[
{
  name: 'embedding',
  type: 'number[]',
  description: 'The embedding vector for the input',
},
]}
/>

## 다중 임베딩 {/* #multiple-embeddings */}

여러 텍스트를 한 번에 삽입하려면 다음을 사용하세요.`embedMany` function:

```typescript
import { embedMany } from 'ai'

const result = await embedMany({
  model: new ModelRouterEmbeddingModel('openai/text-embedding-3-small'),
  values: ['First text', 'Second text', 'Third text'],
  maxRetries: 2, // optional, defaults to 2
})
```

### 매개변수 {/* #parameters-1 */}

<PropertiesTable
  content={[
{
  name: 'model',
  type: 'EmbeddingModel',
  description: "The embedding model to use (e.g. openai.embedding('text-embedding-3-small'))",
},
{
  name: 'values',
  type: 'string[] | Record<string, any>[]',
  description: 'Array of text content or objects to embed',
},
{
  name: 'maxRetries',
  type: 'number',
  description: 'Maximum number of retries per embedding call. Set to 0 to disable retries.',
  isOptional: true,
  defaultValue: '2',
},
{
  name: 'abortSignal',
  type: 'AbortSignal',
  description: 'Optional abort signal to cancel the request',
  isOptional: true,
},
{
  name: 'headers',
  type: 'Record<string, string>',
  description: 'Additional HTTP headers for the request (only for HTTP-based providers)',
  isOptional: true,
},
]}
/>

### 반환 값 {/* #return-value-1 */}

<PropertiesTable
  content={[
{
  name: 'embeddings',
  type: 'number[][]',
  description: 'Array of embedding vectors corresponding to the input values',
},
]}
/>

## 사용 예 {/* #example-usage */}

```typescript
import { embed, embedMany } from 'ai'

// Single embedding
const singleResult = await embed({
  model: new ModelRouterEmbeddingModel('openai/text-embedding-3-small'),
  value: 'What is the meaning of life?',
})

// Multiple embeddings
const multipleResult = await embedMany({
  model: new ModelRouterEmbeddingModel('openai/text-embedding-3-small'),
  values: [
    'First question about life',
    'Second question about universe',
    'Third question about everything',
  ],
})
```

Vercel AI SDK의 임베딩에 대한 자세한 내용은 다음을 참조하세요.

* [AI SDK 임베딩 개요](https://sdk.vercel.ai/docs/ai-sdk-core/embeddings)
* [포함시키다()](https://sdk.vercel.ai/docs/reference/ai-sdk-core/embed)
* [포함많은()](https://sdk.vercel.ai/docs/reference/ai-sdk-core/embed-many)
