---
title: "참고: GraphRAG | 조각"
description: "증강 생성 검색에 대한 그래프 기반 접근 방식을 구현하는 Mastra의 GraphRAG 클래스에 대한 문서입니다."
packages:
  - "@mastra/rag"
---

# 그래프RAG

그만큼`GraphRAG`클래스는 증강 생성을 검색하기 위한 그래프 기반 접근 방식을 구현합니다. 노드가 문서를 나타내고 가장자리가 의미론적 관계를 나타내는 문서 청크에서 지식 그래프를 생성하여 그래프 순회를 통해 직접적인 유사성 일치와 관련 콘텐츠 검색을 모두 가능하게 합니다.

## 기본 사용법 {/* #basic-usage */}

```typescript
import { GraphRAG } from '@mastra/rag'

const graphRag = new GraphRAG({
  dimension: 1536,
  threshold: 0.7,
})

// Create the graph from chunks and embeddings
graphRag.createGraph(documentChunks, embeddings)

// Query the graph with embedding
const results = await graphRag.query({
  query: queryEmbedding,
  topK: 10,
  randomWalkSteps: 100,
  restartProb: 0.15,
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'dimension',
  type: 'number',
  description: '임베딩 벡터의 차원입니다.',
  isOptional: true,
  defaultValue: '1536',
},{
  name: 'threshold',
  type: 'number',
  description: '노드 사이에 에지를 생성하기 위한 유사도 임곗값입니다(0-1).',
  isOptional: true,
  defaultValue: '0.7',
},
]}
/>
## 행동 양식 {/* #methods */}

### `createGraph` {/* #creategraph */}

문서 청크와 해당 임베딩으로부터 지식 그래프를 생성합니다.

```typescript prettier:false
createGraph(chunks: GraphChunk[], embeddings: GraphEmbedding[]): void
```

#### 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'chunks',
  type: 'GraphChunk[]',
  description: '텍스트와 메타데이터가 포함된 문서 청크 배열입니다.',
  isOptional: false,
},{
  name: 'embeddings',
  type: 'GraphEmbedding[]',
  description: '청크에 대응하는 임베딩 배열입니다.',
  isOptional: false,
},
]}
/>
### 질문 {/* #query */}

벡터 유사성과 그래프 순회를 결합한 그래프 기반 검색을 수행합니다.

```typescript prettier:false
query({
  query,
  topK = 10,
  randomWalkSteps = 100,
  restartProb = 0.15
}: {
  query: number[];
  topK?: number;
  randomWalkSteps?: number;
  restartProb?: number;
}): RankedNode[]
```

#### 매개변수 {/* #parameters-1 */}

<PropertiesTable
  content={[
{
  name: 'query',
  type: 'number[]',
  description: '쿼리 임베딩 벡터입니다.',
  isOptional: false,
},{
  name: 'topK',
  type: 'number',
  description: '반환할 결과 수입니다.',
  isOptional: true,
  defaultValue: '10',
},{
  name: 'randomWalkSteps',
  type: 'number',
  description: '랜덤 워크의 단계 수입니다.',
  isOptional: true,
  defaultValue: '100',
},{
  name: 'restartProb',
  type: 'number',
  description: '쿼리 노드에서 워크를 다시 시작할 확률',
  isOptional: true,
  defaultValue: '0.15',
},
]}
/>
#### 보고 {/* #returns */}

각 노드에 다음 항목이 포함된 `RankedNode` 객체의 배열을 반환합니다.
<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: '노드의 고유 식별자',
},{
  name: 'content',
  type: 'string',
  description: '문서 청크의 텍스트 콘텐츠',
},{
  name: 'metadata',
  type: 'Record<string, any>',
  description: '청크와 연결된 추가 메타데이터',
},{
  name: 'score',
  type: 'number',
  description: '그래프 순회에서 산출된 종합 관련성 점수',
},
]}
/>
## 고급 예 {/* #advanced-example */}

```typescript
const graphRag = new GraphRAG({
  dimension: 1536,
  threshold: 0.8, // Stricter similarity threshold
})

// Create graph from chunks and embeddings
graphRag.createGraph(documentChunks, embeddings)

// Query with custom parameters
const results = await graphRag.query({
  query: queryEmbedding,
  topK: 5,
  randomWalkSteps: 200,
  restartProb: 0.2,
})
```

## 관련된 {/* #related */}

* [createGraphRAGTool](../tools/graph-rag-tool)
