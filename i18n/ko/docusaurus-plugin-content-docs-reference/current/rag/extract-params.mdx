---
title: "참조: ExtractParams | 조각"
description: "Mastra의 메타데이터 추출 구성에 대한 문서입니다."
packages:
  - "@mastra/rag"
---

# ExtractParams

ExtractParams는 LLM 분석을 사용하여 문서 청크에서 메타데이터 추출을 구성합니다.

## 예 {/* #example */}

```typescript
import { MDocument } from '@mastra/rag'

const doc = MDocument.fromText(text)
const chunks = await doc.chunk({
  extract: {
    title: true, // Extract titles using default settings
    summary: true, // Generate summaries using default settings
    keywords: true, // Extract keywords using default settings
  },
})

// Example output:
// chunks[0].metadata = {
//   documentTitle: "AI Systems Overview",
//   sectionSummary: "Overview of artificial intelligence concepts and applications",
//   excerptKeywords: "KEYWORDS: AI, machine learning, algorithms"
// }
```

## 매개변수 {/* #parameters */}

`extract` 매개변수는 다음 필드를 허용합니다.
<PropertiesTable
  content={[
{
  name: 'title',
  type: 'boolean | TitleExtractorsArgs',
  isOptional: true,
  description: '제목 추출을 활성화합니다. 기본 설정을 사용하려면 true로 설정하고, 그렇지 않으면 사용자 지정 구성을 제공하세요.',
},{
  name: 'summary',
  type: 'boolean | SummaryExtractArgs',
  isOptional: true,
  description: '요약 추출을 활성화합니다. 기본 설정을 사용하려면 true로 설정하고, 그렇지 않으면 사용자 지정 구성을 제공하세요.',
},{
  name: 'questions',
  type: 'boolean | QuestionAnswerExtractArgs',
  isOptional: true,
  description: '질문 생성을 활성화합니다. 기본 설정을 사용하려면 true로 설정하고, 그렇지 않으면 사용자 지정 구성을 제공하세요.',
},{
  name: 'keywords',
  type: 'boolean | KeywordExtractArgs',
  isOptional: true,
  description: '키워드 추출을 활성화합니다. 기본 설정을 사용하려면 true로 설정하고, 그렇지 않으면 사용자 지정 구성을 제공하세요.',
},{
  name: 'schema',
  type: 'SchemaExtractArgs',
  isOptional: true,
  description: 'Zod 스키마를 사용한 구조화된 메타데이터 추출을 활성화합니다.',
},
]}
/>
## 추출기 인수 {/* #extractor-arguments */}

### `TitleExtractorsArgs` {/* #titleextractorsargs */}

<PropertiesTable
  content={[
{
  name: 'llm',
  type: 'MastraLanguageModel',
  isOptional: true,
  description: '제목 추출에 사용할 AI SDK 언어 Model입니다.',
},{
  name: 'nodes',
  type: 'number',
  isOptional: true,
  description: '추출할 제목 노드 수입니다.',
},{
  name: 'nodeTemplate',
  type: 'string',
  isOptional: true,
  description: '제목 노드 추출용 사용자 지정 Prompt 템플릿입니다. {context} 플레이스홀더를 포함해야 합니다.',
},{
  name: 'combineTemplate',
  type: 'string',
  isOptional: true,
  description: '제목 결합용 사용자 지정 Prompt 템플릿입니다. {context} 플레이스홀더를 포함해야 합니다.',
},
]}
/>
### `SummaryExtractArgs` {/* #summaryextractargs */}

<PropertiesTable
  content={[
{
  name: 'llm',
  type: 'MastraLanguageModel',
  isOptional: true,
  description: '요약 추출에 사용할 AI SDK 언어 Model입니다.',
},{
  name: 'summaries',
  type: "('self' | 'prev' | 'next')[]",
  isOptional: true,
  description:
    "생성할 요약 유형 목록입니다. 'self'(현재 청크), 'prev'(이전 청크), 'next'(다음 청크)만 포함할 수 있습니다.",
},{
  name: 'promptTemplate',
  type: 'string',
  isOptional: true,
  description: '요약 생성용 사용자 지정 Prompt 템플릿입니다. {context} 플레이스홀더를 포함해야 합니다.',
},
]}
/>
### `QuestionAnswerExtractArgs` {/* #questionanswerextractargs */}

<PropertiesTable
  content={[
{
  name: 'llm',
  type: 'MastraLanguageModel',
  isOptional: true,
  description: '질문 생성에 사용할 AI SDK 언어 Model입니다.',
},{
  name: 'questions',
  type: 'number',
  isOptional: true,
  description: '생성할 질문 수입니다.',
},{
  name: 'promptTemplate',
  type: 'string',
  isOptional: true,
  description:
    '질문 생성용 사용자 지정 Prompt 템플릿입니다. {context} 및 {numQuestions} 플레이스홀더를 모두 포함해야 합니다.',
},{
  name: 'embeddingOnly',
  type: 'boolean',
  isOptional: true,
  description: 'true이면 실제 질문 없이 임베딩만 생성합니다.',
},
]}
/>
### `KeywordExtractArgs` {/* #keywordextractargs */}

<PropertiesTable
  content={[
{
  name: 'llm',
  type: 'MastraLanguageModel',
  isOptional: true,
  description: '키워드 추출에 사용할 AI SDK 언어 Model입니다.',
},{
  name: 'keywords',
  type: 'number',
  isOptional: true,
  description: '추출할 키워드 수입니다.',
},{
  name: 'promptTemplate',
  type: 'string',
  isOptional: true,
  description:
    '키워드 추출용 사용자 지정 Prompt 템플릿입니다. {context} 및 {maxKeywords} 플레이스홀더를 모두 포함해야 합니다.',
},
]}
/>
### `SchemaExtractArgs` {/* #schemaextractargs */}

<PropertiesTable
  content={[
{
  name: 'schema',
  type: 'ZodType',
  isOptional: false,
  description: '추출할 데이터의 구조를 정의하는 Zod 스키마입니다.',
},{
  name: 'llm',
  type: 'MastraLanguageModel',
  isOptional: true,
  description: '추출에 사용할 AI SDK 언어 Model입니다.',
},{
  name: 'instructions',
  type: 'string',
  isOptional: true,
  description: '추출할 항목을 LLM에 지정하는 지침입니다.',
},{
  name: 'metadataKey',
  type: 'string',
  isOptional: true,
  description: '추출 결과를 중첩할 키입니다. 생략하면 결과가 메타데이터 객체에 펼쳐집니다.',
},
]}
/>
## 고급 예 {/* #advanced-example */}

```typescript
import { MDocument } from '@mastra/rag'

const doc = MDocument.fromText(text)
const chunks = await doc.chunk({
  extract: {
    // Title extraction with custom settings
    title: {
      nodes: 2, // Extract 2 title nodes
      nodeTemplate: 'Generate a title for this: {context}',
      combineTemplate: 'Combine these titles: {context}',
    },

    // Summary extraction with custom settings
    summary: {
      summaries: ['self'], // Generate summaries for current chunk
      promptTemplate: 'Summarize this: {context}',
    },

    // Question generation with custom settings
    questions: {
      questions: 3, // Generate 3 questions
      promptTemplate: 'Generate {numQuestions} questions about: {context}',
      embeddingOnly: false,
    },

    // Keyword extraction with custom settings
    keywords: {
      keywords: 5, // Extract 5 keywords
      promptTemplate: 'Extract {maxKeywords} key terms from: {context}',
    },

    // Schema extraction with Zod
    schema: {
      schema: z.object({
        productName: z.string(),
        category: z.enum(['electronics', 'clothing']),
      }),
      instructions: 'Extract product information.',
      metadataKey: 'product',
    },
  },
})

// Example output:
// chunks[0].metadata = {
//   documentTitle: "AI in Modern Computing",
//   sectionSummary: "Overview of AI concepts and their applications in computing",
//   questionsThisExcerptCanAnswer: "1. What is machine learning?\n2. How do neural networks work?",
//   excerptKeywords: "1. Machine learning\n2. Neural networks\n3. Training data",
//   product: {
//     productName: "Neural Net 2000",
//     category: "electronics"
//   }
// }
```

## 제목 추출을 위한 문서 그룹화 {/* #document-grouping-for-title-extraction */}

`TitleExtractor`를 사용할 때 각 청크의 `metadata` 필드에 공유 `docId`를 지정하여 여러 청크를 그룹화한 후 제목을 추출할 수 있습니다. 동일한 `docId`를 가진 모든 청크에는 동일한 추출 제목이 적용됩니다. `docId`를 설정하지 않으면 각 청크가 제목 추출을 위한 별도의 문서로 처리됩니다.
**예:**

```ts
import { MDocument } from '@mastra/rag'

const doc = new MDocument({
  docs: [
    { text: 'chunk 1', metadata: { docId: 'docA' } },
    { text: 'chunk 2', metadata: { docId: 'docA' } },
    { text: 'chunk 3', metadata: { docId: 'docB' } },
  ],
  type: 'text',
})

await doc.extractMetadata({ title: true })
// The first two chunks will share a title, while the third chunk will be assigned a separate title.
```
