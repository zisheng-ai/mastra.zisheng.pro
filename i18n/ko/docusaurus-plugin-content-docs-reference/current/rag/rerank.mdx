---
title: "참조: rerank() | 조각"
description: "벡터 검색 결과에 대한 고급 재순위 기능을 제공하는 Mastra의 재순위 기능에 대한 문서입니다."
packages:
  - "@mastra/rag"
---

# 순위 재지정()

그만큼`rerank()`기능은 의미적 관련성, 벡터 유사성 및 위치 기반 점수를 결합하여 벡터 검색 결과에 대한 고급 재순위 기능을 제공합니다.

```typescript
function rerank(
  results: QueryResult[],
  query: string,
  modelConfig: ModelConfig,
  options?: RerankerFunctionOptions,
): Promise<RerankResult[]>
```

## 사용예 {/* #usage-example */}

```typescript
import { rerank } from '@mastra/rag'

const model = '__GATEWAY_OPENAI_MODEL__'

const rerankedResults = await rerank(vectorSearchResults, 'How do I deploy to production?', model, {
  weights: {
    semantic: 0.5,
    vector: 0.3,
    position: 0.2,
  },
  topK: 3,
})
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'results',
  type: 'QueryResult[]',
  description: '재순위화할 벡터 검색 결과',
  isOptional: false,
},{
  name: 'query',
  type: 'string',
  description: '관련성을 평가하는 데 사용되는 검색 쿼리 텍스트',
  isOptional: false,
},{
  name: 'model',
  type: 'MastraLanguageModel',
  description: '재순위화에 사용할 언어 Model',
  isOptional: false,
},{
  name: 'options',
  type: 'RerankerFunctionOptions',
  description: '재순위화 Model의 옵션',
  isOptional: true,
  properties: [    {
      type: 'RerankerFunctionOptions',
      parameters: [
        {
          name: 'weights',
          type: 'WeightConfig',
          description: '여러 점수 산정 구성 요소의 가중치(합계가 1이어야 함)',
          isOptional: true,
          properties: [            {
              type: 'number',
              parameters: [
                {
                  name: 'semantic',
                  description: '의미적 관련성의 가중치',
                  isOptional: true,
                  type: 'number (기본값: 0.4)',
                },
              ],
            },            {
              type: 'number',
              parameters: [
                {
                  name: 'vector',
                  description: '벡터 유사도의 가중치',
                  isOptional: true,
                  type: 'number (기본값: 0.4)',
                },
              ],
            },            {
              type: 'number',
              parameters: [
                {
                  name: 'position',
                  description: '위치 기반 점수의 가중치',
                  isOptional: true,
                  type: 'number (기본값: 0.2)',
                },
              ],
            },
          ],
        },        {
          name: 'queryEmbedding',
          type: 'number[]',
          description: '쿼리의 임베딩',
          isOptional: true,
        },        {
          name: 'topK',
          type: 'number',
          description: '반환할 상위 결과 수',
          isOptional: true,
          defaultValue: '3',
        },
      ],
    },
  ],
},
]}
/>
rerank 함수는 Vercel AI SDK의 모든 LanguageModel을 허용합니다. `rerank-v3.5`와 같은 Cohere Model을 사용하면 Cohere의 재순위화 기능을 자동으로 사용합니다.
:::note

순위를 다시 매기는 동안 의미 체계 점수가 제대로 작동하려면 각 결과에 텍스트 콘텐츠가 포함되어야 합니다.`metadata.text` field.

:::

## 보고 {/* #returns */}

이 함수는 배열을 반환합니다.`RerankResult` objects:

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'QueryResult',
  description: '원본 쿼리 결과',
},{
  name: 'score',
  type: 'number',
  description: '종합 재순위화 점수(0~1)',
},{
  name: 'details',
  type: 'ScoringDetails',
  description: '상세 점수 정보',
},
]}
/>
### `ScoringDetails` {/* #scoringdetails */}

<PropertiesTable
  content={[
{
  name: 'semantic',
  type: 'number',
  description: '의미적 관련성 점수(0~1)',
},{
  name: 'vector',
  type: 'number',
  description: '벡터 유사도 점수(0~1)',
},{
  name: 'position',
  type: 'number',
  description: '위치 기반 점수(0~1)',
},{
  name: 'queryAnalysis',
  type: 'object',
  description: '쿼리 분석 세부 정보',
  isOptional: true,
  properties: [    {
      type: 'number',
      parameters: [
        {
          name: 'magnitude',
          description: '쿼리의 크기',
        },
      ],
    },    {
      type: 'number[]',
      parameters: [
        {
          name: 'dominantFeatures',
          description: '쿼리의 주요 특성',
        },
      ],
    },
  ],
},
]}
/>
## 관련된 {/* #related */}

* [createVectorQueryTool](../tools/vector-query-tool)
