---
title: "참조: Workflow.branch() | Workflow"
description: "단계 사이에 조건부 분기를 생성하는 Workflow의 'Workflow.branch()' 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Workflow우.branch()

그만큼`.branch()`메서드는 Workflow 단계 사이에 조건부 분기를 생성하여 이전 단계의 결과에 따라 다양한 경로를 선택할 수 있도록 합니다.

## 사용예 {/* #usage-example */}

```typescript
workflow.branch([
  [async ({ context }) => true, step1],
  [async ({ context }) => false, step2],
])
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'steps',
  type: '[() => boolean, Step]',
  description:
    'An array of tuples, each containing a condition function and a step to execute if the condition is true',
  isOptional: false,
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'workflow',
  type: 'NewWorkflow',
  description: 'The workflow instance for method chaining',
},
]}
/>

## 관련된 {/* #related */}

* [조건부 분기 논리](/docs/workflows/control-flow#conditional-logic-with-branch)
* [제어 흐름](/docs/workflows/control-flow)
