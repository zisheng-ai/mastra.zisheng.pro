---
title: "참조: Workflow.dowhile() | Workflow"
description: "조건이 충족되는 동안 단계를 실행하는 루프를 생성하는 Workflow의 'Workflow.dowhile()' 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Workflow.dowhile()

그만큼`.dowhile()`메소드는 조건이 충족되는 동안 단계를 실행합니다. 조건을 평가하기 전에 항상 단계를 한 번 이상 실행합니다. 조건이 처음 평가될 때,`iterationCount`~이다`1`.

## 사용예 {/* #usage-example */}

```typescript
workflow.dowhile(step1, async ({ inputData }) => true)
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'step',
  type: 'Step',
  description: 'The step instance to execute in the loop',
  isOptional: false,
},
{
  name: 'condition',
  type: '(params : ExecuteParams & { iterationCount: number }) => Promise<boolean>',
  description:
    'A function that returns a boolean indicating whether to continue the loop. The function receives the execution parameters and the iteration count.',
  isOptional: false,
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'workflow',
  type: 'Workflow',
  description: 'The workflow instance for method chaining',
},
]}
/>

## 관련된 {/* #related */}

* [제어 흐름](/docs/workflows/control-flow)

* [ExecuteParams](../step#constructor-parameters)
