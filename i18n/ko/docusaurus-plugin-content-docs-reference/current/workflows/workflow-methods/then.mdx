---
title: "참조: Workflow.then() | Workflow"
description: "단계 간 순차적 종속성을 생성하는 Workflow의 'Workflow.then()' 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Workflow우.then()

그만큼`.then()`메서드는 Workflow 단계 간에 순차적 종속성을 생성하여 단계가 특정 순서로 실행되도록 합니다.

## 사용예 {/* #usage-example */}

```typescript
workflow.then(step1).then(step2)
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'step',
  type: 'Step',
  description: 'The step instance that should execute after the previous step completes',
  isOptional: false,
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'workflow',
  type: 'NewWorkflow',
  description: 'The workflow instance for method chaining',
},
]}
/>

## 관련된 {/* #related */}

* [제어 흐름](/docs/workflows/control-flow)
