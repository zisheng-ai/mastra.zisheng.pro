---
title: "참조: Workflow.createRun() | Workflow"
description: "새 Workflow 실행 인스턴스를 생성하는 Workflow의 'Workflow.createRun()' 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Workflow우.createRun()

그만큼`.createRun()`메서드는 새로운 Workflow 실행 인스턴스를 생성하여 특정 입력 데이터로 Workflow를 실행할 수 있도록 합니다. 이는 다음을 반환하는 현재 API입니다.`Run`사례.

## 사용예 {/* #usage-example */}

```typescript
await workflow.createRun()
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'runId',
  type: 'string',
  description: 'Optional custom identifier for the workflow run',
  isOptional: true,
},
{
  name: 'resourceId',
  type: 'string',
  description:
    'Optional identifier to associate the workflow run with a specific resource (e.g., user ID, tenant ID). This value is persisted with the workflow run and can be used for filtering and querying runs.',
  isOptional: true,
},
{
  name: 'disableScorers',
  type: 'boolean',
  description: 'Optional flag to disable scorers for this workflow run',
  isOptional: true,
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'run',
  type: 'Run',
  description: 'A new workflow run instance that can be used to execute the workflow',
},
]}
/>

## 확장된 사용 예 {/* #extended-usage-example */}

```typescript
const workflow = mastra.getWorkflow('workflow')

const run = await workflow.createRun()

const result = await run.start({
  inputData: {
    value: 10,
  },
})
```

## 사용`resourceId` {/* #using-resourceid */}

그만큼`resourceId` parameter associates a workflow run with a specific resource, such as a user or tenant. This is useful for multi-tenant applications or when you need to track which user initiated a workflow.

```typescript
const workflow = mastra.getWorkflow('workflow')

// Create a run associated with a specific user
const run = await workflow.createRun({
  resourceId: 'user-123',
})

const result = await run.start({
  inputData: {
    value: 10,
  },
})

// Later, retrieve the run and access the resourceId
const storedRun = await workflow.getWorkflowRunById(run.runId)
console.log(storedRun.resourceId) // "user-123"
```

## 관련된 {/* #related */}

* [클래스 실행](../run)
* [Workflow우 개요](/docs/workflows/overview)
