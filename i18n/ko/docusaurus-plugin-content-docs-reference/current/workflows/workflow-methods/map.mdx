---
title: "참조: Workflow.map() | Workflow"
description: "이전 단계의 출력 데이터를 후속 단계의 입력으로 매핑하는 Workflow의 'Workflow.map()' 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Workflow.맵()

그만큼`.map()`메서드는 이전 단계의 출력 데이터를 후속 단계의 입력으로 매핑하므로 단계 간에 데이터를 변환할 수 있습니다.

## 사용예 {/* #usage-example */}

```typescript
workflow.map(async ({ inputData }) => `${inputData.value} - map`)
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'mappingFunction',
  type: '(params: { inputData: any }) => any',
  description: '입력 데이터를 변환하고 매핑된 결과를 반환하는 함수',
  isOptional: false,
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'workflow',
  type: 'Workflow',
  description: '메서드 체이닝을 위한 Workflow 인스턴스',
},
]}
/>
## 사용`inputData` {/* #using-inputdata */}

이전 단계의 전체 출력에 접근하려면 `inputData`를 사용하세요.
```typescript {3} filename="src/mastra/workflows/test-workflow.ts" prettier:false
  .then(step1)
  .map(({ inputData }) => {
    console.log(inputData);
  })
```

## 사용`getStepResult()` {/* #using-getstepresult */}

특정 단계의 인스턴스를 참조하여 전체 출력에 접근하려면 `getStepResult()`를 사용하세요.
```typescript {3} filename="src/mastra/workflows/test-workflow.ts" prettier:false
  .then(step1)
  .map(async ({ getStepResult }) => {
    console.log(getStepResult(step1));
  })
```

## 사용`getInitData()` {/* #using-getinitdata */}

Workflow에 제공된 초기 입력 데이터에 접근하려면 `getInitData<typeof workflow>()`를 사용하세요.
```typescript prettier:false {3} filename="src/mastra/workflows/test-workflow.ts"
  .then(step1)
  .map(async ({ getInitData }) => {
    console.log(getInitData<any>());
  })
```

## 사용`mapVariable()` {/* #using-mapvariable */}

객체 형태의 `.map()`은 필드를 매핑하는 선언적 대체 구문을 제공합니다. 함수를 작성하는 대신, 각 키는 새로운 필드 이름이고 각 값은 `mapVariable()`을 사용해 이전 단계나 Workflow 입력에서 데이터를 추출하는 객체를 정의합니다.
Workflow 모듈에서 `mapVariable()`을 가져오세요.
```typescript filename="src/mastra/workflows/test-workflow.ts"
import { mapVariable } from '@mastra/core/workflows'
```

### 단계 출력에서 ​​필드 추출 {/* #extracting-fields-from-step-outputs */}

단계 출력에서 특정 필드를 추출하여 새로운 필드 이름에 매핑하려면 `step`과 함께 `mapVariable()`을 사용하세요. `path` 매개변수는 추출할 필드를 지정합니다.
이 예제에서는 `step1` 출력의 `value` 필드를 추출하여 `details`라는 새 필드에 매핑합니다.
```typescript prettier:false {3-6} filename="src/mastra/workflows/test-workflow.ts"
  .then(step1)
  .map({
    details: mapVariable({
      step: step1,
      path: "value"
    })
  })
```

### Workflow 입력에서 필드 추출 {/* #extracting-fields-from-workflow-input */}

Workflow의 초기 입력 데이터에서 특정 필드를 추출하려면 `initData`와 함께 `mapVariable()`을 사용하세요. 원래 Workflow 입력을 이후 단계에 전달해야 할 때 유용합니다.
이 예제에서는 Workflow 입력의 `value` 필드를 추출하여 `details`라는 필드에 매핑합니다.
```typescript prettier:false {6-9} filename="src/mastra/workflows/test-workflow.ts"
export const testWorkflow = createWorkflow({...});

testWorkflow
  .then(step1)
  .map({
    details: mapVariable({
      initData: testWorkflow,
      path: "value"
    })
  })
```

## 관련된 {/* #related */}

* [입력 데이터 매핑](/docs/workflows/control-flow#input-data-mapping)
