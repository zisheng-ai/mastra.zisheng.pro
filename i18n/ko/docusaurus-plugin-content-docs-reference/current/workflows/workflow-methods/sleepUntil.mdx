---
title: "참조: Workflow.sleepUntil() | Workflow"
description: "지정된 날짜까지 실행을 일시 중지하는 Workflow의 `Workflow.sleepUntil()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Workflow.sleepUntil()

그만큼`.sleepUntil()`메소드는 지정된 날짜까지 실행을 일시 중지합니다.

## 사용예 {/* #usage-example */}

```typescript
workflow.sleepUntil(new Date(Date.now() + 5000))
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'dateOrCallback',
  type: 'Date | ((params: ExecuteFunctionParams) => Promise<Date>)',
  description:
    'Date 객체 또는 Date를 반환하는 콜백 함수입니다. 콜백은 실행 컨텍스트를 받아 입력 데이터를 기반으로 목표 시간을 동적으로 계산할 수 있습니다.',
  isOptional: false,
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'workflow',
  type: 'Workflow',
  description: '메서드 체이닝을 위한 Workflow 인스턴스',
},
]}
/>
## 확장된 사용 예 {/* #extended-usage-example */}

```typescript prettier:false
import { createWorkflow, createStep } from "@mastra/core/workflows";

const step1 = createStep({...});
const step2 = createStep({...});

export const testWorkflow = createWorkflow({...})
  .then(step1)
  .sleepUntil(async ({ inputData }) => {
    const { delayInMs } = inputData;
    return new Date(Date.now() + delayInMs);
  })
  .then(step2)
  .commit();
```
