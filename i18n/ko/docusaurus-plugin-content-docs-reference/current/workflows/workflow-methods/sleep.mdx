---
title: "참조: Workflow.sleep() | Workflow"
description: "지정된 밀리초 동안 실행을 일시 중지하는 Workflow의 'Workflow.sleep()' 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# Workflow우.수면()

그만큼`.sleep()`메서드는 지정된 밀리초 동안 실행을 일시 중지합니다. 런타임 정의 지연에 대해 정적 숫자 또는 콜백 함수를 허용합니다.

## 사용예 {/* #usage-example */}

```typescript
workflow.sleep(5000)
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'milliseconds',
  type: 'number | ((context: { inputData: any }) => number | Promise<number>)',
  description: 'The number of milliseconds to pause execution, or a callback that returns the delay',
  isOptional: false,
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'workflow',
  type: 'Workflow',
  description: 'The workflow instance for method chaining',
},
]}
/>

## 확장된 사용 예 {/* #extended-usage-example */}

```typescript prettier:false
import { createWorkflow, createStep } from "@mastra/core/workflows";

const step1 = createStep({...});
const step2 = createStep({...});

export const testWorkflow = createWorkflow({...})
  .then(step1)
  .sleep(async ({ inputData }) => {
    const { delayInMs } = inputData;
    return delayInMs;
  })
  .then(step2)
  .commit();
```
