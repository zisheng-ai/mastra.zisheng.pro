---
title: "참조: Run.restart() | Workflow"
description: "서버 연결이 끊어진 활성 Workflow 실행을 다시 시작하는 Workflow의 'Run.restart()' 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 실행.재시작()

그만큼`.restart()`메서드는 서버 연결이 끊어진 활성 Workflow 실행을 다시 시작하므로 연결이 끊어진 순간(마지막 활성 단계)부터 실행을 계속할 수 있습니다.

## 사용예 {/* #usage-example */}

```typescript
const run = await workflow.createRun()

const result = await run.start({ inputData: { value: 'initial data' } })

const restartedResult = await run.restart()
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'requestContext',
  type: 'RequestContext',
  description: '재개할 때 사용할 요청 컨텍스트 데이터',
  isOptional: true,
},{
  name: 'tracingContext',
  type: 'TracingContext',
  isOptional: true,
  description:
    "하위 스팬을 생성하고 메타데이터를 추가하기 위한 추적 컨텍스트입니다. Mastra의 추적 시스템을 사용하면 자동으로 주입됩니다.",
  properties: [    {
      parameters: [
        {
          name: 'currentSpan',
          type: 'Span',
          isOptional: true,
          description:
            '하위 스팬을 생성하고 메타데이터를 추가하기 위한 현재 스팬입니다. 실행 중 사용자 지정 하위 스팬을 생성하거나 스팬 속성을 업데이트할 때 사용하세요.',
        },
      ],
    },
  ],
},{
  name: 'tracingOptions',
  type: 'TracingOptions',
  isOptional: true,
  description: '추적 구성 옵션입니다.',
  properties: [    {
      parameters: [
        {
          name: 'metadata',
          type: 'Record<string, any>',
          isOptional: true,
          description:
            '루트 Trace 스팬에 추가할 메타데이터입니다. 사용자 ID, 세션 ID, 기능 플래그와 같은 사용자 지정 속성을 추가할 때 유용합니다.',
        },
      ],
    },    {
      parameters: [
        {
          name: 'requestContextKeys',
          type: 'string[]',
          isOptional: true,
          description:
            "이 Trace의 메타데이터로 추출할 추가 RequestContext 키입니다. 중첩된 값에 점 표기법을 지원합니다(예: 'user.id').",
        },
      ],
    },    {
      parameters: [
        {
          name: 'traceId',
          type: 'string',
          isOptional: true,
          description:
            '이 실행에 사용할 Trace ID입니다(1~32자의 16진수). 제공하면 이 Trace가 지정된 Trace의 일부가 됩니다.',
        },
      ],
    },    {
      parameters: [
        {
          name: 'parentSpanId',
          type: 'string',
          isOptional: true,
          description:
            '이 실행에 사용할 부모 span ID입니다(1~16자의 16진수). 제공하면 루트 span이 이 span의 자식으로 생성됩니다.',
        },
      ],
    },    {
      parameters: [
        {
          name: 'tags',
          type: 'string[]',
          isOptional: true,
          description: '이 Trace에 적용할 태그입니다. Trace를 분류하고 필터링하는 문자열 레이블입니다.',
        },
      ],
    },
  ],
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<WorkflowResult<TState, TOutput, TSteps>>',
  description: '단계 출력과 상태가 포함된 Workflow 실행 결과로 이행되는 Promise입니다',
},{
  name: 'traceId',
  type: 'string',
  isOptional: true,
  description:
    'Tracing이 활성화된 경우 이 실행과 연결된 Trace ID입니다. 로그를 연결하고 실행 흐름을 디버깅하는 데 사용합니다.',
},{
  name: 'spanId',
  type: 'string',
  isOptional: true,
  description:
    'Tracing이 활성화된 경우 이 실행과 연결된 루트 span ID입니다. span 수준 조회와 연결에 사용합니다.',
},
]}
/>
## 관련된 {/* #related */}

* [Workflow 개요](/docs/workflows/overview#running-workflows)
* [Workflow 다시 시작](/docs/workflows/overview#restarting-active-workflow-runs)
* [Workflow.createRun()](../workflow-methods/create-run)
