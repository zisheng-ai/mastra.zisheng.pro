---
title: "참조: Run.startAsync() | Workflow"
description: "완료를 기다리지 않고(fire-and-forget) Workflow 실행을 시작하는 Workflow의 'Run.startAsync()' 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 실행.startAsync()

그만큼`.startAsync()`메서드는 완료를 기다리지 않고 Workflow 실행을 시작합니다. 와 함께 즉시 반환됩니다.`runId`, Workflow가 백그라운드에서 실행될 수 있도록 합니다. 이는 장기 실행 Workflow나 예약된 작업에 유용합니다. 또한 Workflow 완료 시 차단을 방지합니다.

## 사용예 {/* #usage-example */}

```typescript
const run = await workflow.createRun()

// Fire-and-forget - returns immediately
const { runId } = await run.startAsync({
  inputData: {
    value: 'initial data',
  },
})

// Optionally poll for completion later
const result = await workflow.getWorkflowRunExecutionResult(runId)
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'inputData',
  type: 'z.infer<TInput>',
  description: "Input data that matches the workflow's input schema",
  isOptional: true,
},
{
  name: 'requestContext',
  type: 'RequestContext',
  description: 'Request Context data to use during workflow execution',
  isOptional: true,
},
{
  name: 'initialState',
  type: 'z.infer<TState>',
  description: 'Initial state to use for the workflow execution',
  isOptional: true,
},
{
  name: 'tracingOptions',
  type: 'TracingOptions',
  isOptional: true,
  description: 'Options for Tracing configuration.',
  properties: [
    {
      parameters: [
        {
          name: 'metadata',
          type: 'Record<string, any>',
          isOptional: true,
          description:
            'Metadata to add to the root trace span. Useful for adding custom attributes like user IDs, session IDs, or feature flags.',
        },
      ],
    },
    {
      parameters: [
        {
          name: 'traceId',
          type: 'string',
          isOptional: true,
          description:
            'Trace ID to use for this execution (1-32 hexadecimal characters). If provided, this trace will be part of the specified trace.',
        },
      ],
    },
  ],
},
{
  name: 'outputOptions',
  type: 'OutputOptions',
  isOptional: true,
  description: 'Options for output configuration.',
  properties: [
    {
      parameters: [
        {
          name: 'includeState',
          type: 'boolean',
          isOptional: true,
          description: 'Whether to include the workflow run state in the result.',
        },
      ],
    },
  ],
},
]}
/>

## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'runId',
  type: 'string',
  description: 'The unique identifier for this workflow run. Use this to check status or retrieve results later.',
},
]}
/>

## 언제 사용하나요?`startAsync()` {/* #when-to-use-startasync */}

사용`startAsync()` instead of `start()` when:

* **장기 실행 Workflow**: Workflow를 완료하는 데 몇 분 또는 몇 시간이 걸릴 수 있습니다.
* **예약된/크론 트리거**: 스케줄러를 차단하지 않고 Workflow를 트리거하고 싶습니다.
* **폴링 실패 방지**: Ingest Workflow를 사용하면`start()` polls for completion which can fail and cause retries. `startAsync()` avoids this issue
* **백그라운드 처리**: 작업을 대기열에 추가하고 결과를 비동기적으로 처리하고 싶습니다.

## Workflow 상태 확인 중 {/* #checking-workflow-status */}

전화 후`startAsync()`, you can check the workflow status using:

```typescript
// Get the execution result (including step outputs)
const result = await workflow.getWorkflowRunExecutionResult(runId)

if (result?.status === 'success') {
  console.log('Workflow completed:', result.steps)
} else if (result?.status === 'failed') {
  console.log('Workflow failed:', result.error)
} else if (result?.status === 'running') {
  console.log('Workflow still running...')
}
```

## 관련된 {/* #related */}

* [실행.시작()](./start): Workflow를 시작하고 완료될 때까지 기다립니다.
* [Workflow우 개요](/docs/workflows/overview)
* [Workflow우.createRun()](../workflow-methods/create-run)
