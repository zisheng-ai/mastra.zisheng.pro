---
title: "참조: Run.start() | Workflow"
description: "입력 데이터로 Workflow 실행을 시작하는 Workflow의 `Run.start()` 메서드에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 실행.시작()

그만큼`.start()`메서드는 입력 데이터로 Workflow 실행을 시작하므로 처음부터 Workflow를 실행할 수 있습니다.

## 사용예 {/* #usage-example */}

```typescript
const run = await workflow.createRun()

const result = await run.start({
  inputData: {
    value: 'initial data',
  },
})
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'inputData',
  type: 'z.infer<TInput>',
  description: "Workflow의 입력 스키마와 일치하는 입력 데이터입니다",
  isOptional: true,
},{
  name: 'requestContext',
  type: 'RequestContext',
  description: 'Workflow 실행 중 사용할 Request Context 데이터입니다',
  isOptional: true,
},{
  name: 'outputWriter',
  type: '(chunk: TOutput) => Promise<void>',
  description: '출력 청크가 생성될 때 처리하는 선택적 비동기 함수입니다',
  isOptional: true,
},{
  name: 'tracingContext',
  type: 'TracingContext',
  isOptional: true,
  description:
    "자식 span을 생성하고 메타데이터를 추가하기 위한 Tracing 컨텍스트입니다. Mastra의 Tracing 시스템을 사용할 때 자동으로 주입됩니다.",
  properties: [    {
      parameters: [
        {
          name: 'currentSpan',
          type: 'Span',
          isOptional: true,
          description:
            '자식 span을 생성하고 메타데이터를 추가하기 위한 현재 span입니다. 실행 중 사용자 정의 자식 span을 생성하거나 span 속성을 업데이트하는 데 사용합니다.',
        },
      ],
    },
  ],
},{
  name: 'tracingOptions',
  type: 'TracingOptions',
  isOptional: true,
  description: 'Tracing 구성을 위한 옵션입니다.',
  properties: [    {
      parameters: [
        {
          name: 'metadata',
          type: 'Record<string, any>',
          isOptional: true,
          description:
            '루트 Trace span에 추가할 메타데이터입니다. 사용자 ID, 세션 ID 또는 기능 플래그와 같은 사용자 정의 속성을 추가할 때 유용합니다.',
        },
      ],
    },    {
      parameters: [
        {
          name: 'requestContextKeys',
          type: 'string[]',
          isOptional: true,
          description:
            "이 Trace의 메타데이터로 추출할 추가 RequestContext 키입니다. 중첩 값에는 점 표기법을 지원합니다(예: 'user.id').",
        },
      ],
    },    {
      parameters: [
        {
          name: 'traceId',
          type: 'string',
          isOptional: true,
          description:
            '이 실행에 사용할 Trace ID입니다(1~32자의 16진수). 제공하면 이 Trace가 지정된 Trace의 일부가 됩니다.',
        },
      ],
    },    {
      parameters: [
        {
          name: 'parentSpanId',
          type: 'string',
          isOptional: true,
          description:
            '이 실행에 사용할 부모 span ID입니다(1~16자의 16진수). 제공하면 루트 span이 이 span의 자식으로 생성됩니다.',
        },
      ],
    },    {
      parameters: [
        {
          name: 'tags',
          type: 'string[]',
          isOptional: true,
          description: '이 Trace에 적용할 태그입니다. Trace를 분류하고 필터링하는 문자열 레이블입니다.',
        },
      ],
    },
  ],
},{
  name: 'outputOptions',
  type: 'OutputOptions',
  isOptional: true,
  description: '출력 구성을 위한 옵션입니다.',
  properties: [    {
      parameters: [
        {
          name: 'includeState',
          type: 'boolean',
          isOptional: true,
          description: '결과에 Workflow 실행 상태를 포함할지 여부입니다.',
        },
      ],
    },
  ],
},
]}
/>
## 보고 {/* #returns */}

<PropertiesTable
  content={[
{
  name: 'result',
  type: 'Promise<WorkflowResult<TState, TOutput, TSteps>>',
  description: '단계 출력과 상태가 포함된 Workflow 실행 결과로 이행되는 Promise입니다',
},{
  name: 'traceId',
  type: 'string',
  isOptional: true,
  description:
    'Tracing이 활성화된 경우 이 실행과 연결된 Trace ID입니다. 로그를 연결하고 실행 흐름을 디버깅하는 데 사용합니다.',
},{
  name: 'spanId',
  type: 'string',
  isOptional: true,
  description:
    'Tracing이 활성화된 경우 이 실행과 연결된 루트 span ID입니다. span 수준 조회와 연결에 사용합니다.',
},
]}
/>
## 확장된 사용 예 {/* #extended-usage-example */}

```typescript
import { RequestContext } from '@mastra/core/request-context'

const run = await workflow.createRun()

const requestContext = new RequestContext()
requestContext.set('variable', false)

const result = await run.start({
  inputData: {
    value: 'initial data',
  },
  requestContext,
})
```

## 관련된 {/* #related */}

* [Workflow 개요](/docs/workflows/overview#running-workflows)
* [Workflow.createRun()](../workflow-methods/create-run)
