---
title: "참조: 클래스 실행 | Workflow"
description: "Workflow 실행 인스턴스를 나타내는 Mastra의 Run 클래스에 대한 문서입니다."
packages:
  - "@mastra/core"
---

# 수업을 진행하세요

그만큼`Run`클래스는 Workflow 실행 인스턴스를 나타내며 Workflow 실행을 시작, 재개, 스트리밍 및 모니터링하는 메서드를 제공합니다.

## 사용예 {/* #usage-example */}

```typescript
const run = await workflow.createRun()

const result = await run.start({
  inputData: { value: 'initial data' },
})

if (result.status === 'suspended') {
  const resumedResult = await run.resume({
    resumeData: { value: 'resume data' },
  })
}
```

## 메소드 실행 {/* #run-methods */}

<PropertiesTable
  content={[
{
  name: 'start',
  type: '(options?: StartOptions) => Promise<WorkflowResult>',
  description: '입력 데이터로 Workflow 실행을 시작합니다',
  required: true,
},{
  name: 'resume',
  type: '(options?: ResumeOptions) => Promise<WorkflowResult>',
  description: '일시 중지된 Workflow를 특정 단계부터 재개합니다',
  required: true,
},{
  name: 'stream',
  type: '(options?: StreamOptions) => MastraWorkflowStream',
  description: '향상된 스트리밍 기능을 사용하여 Workflow 실행을 이벤트 스트림으로 모니터링합니다',
  required: true,
},{
  name: 'resumeStream',
  type: '(options?: ResumeStreamOptions) => MastraWorkflowStream',
  description: '스트리밍을 지원하며 일시 중지된 Workflow를 재개합니다',
  required: true,
},{
  name: 'cancel',
  type: '() => Promise<{ message: string }>',
  description:
    'Workflow 실행을 취소하여 실행 중인 모든 단계를 중지하고 이후 단계가 실행되지 않도록 합니다',
  required: true,
},{
  name: 'restart',
  type: '(options?: RestartOptions) => Promise<WorkflowResult>',
  description: '마지막 활성 단계부터 Workflow 실행을 다시 시작합니다',
  required: true,
},{
  name: 'timeTravel',
  type: '(options?: TimeTravelOptions) => Promise<WorkflowResult>',
  description:
    '저장된 스냅샷 데이터나 사용자가 제공한 사용자 정의 컨텍스트를 사용하여 특정 단계부터 Workflow를 다시 실행합니다.',
  required: true,
},{
  name: 'timeTravelStream',
  type: '(options?: TimeTravelOptions) => MastraWorkflowStream',
  description: '스트리밍을 지원하며 Workflow 실행을 시간 이동합니다',
  required: true,
},
]}
/>
## 실행 상태 {/* #run-status */}

Workflow 실행의 `status`는 현재 실행 상태를 나타냅니다. 가능한 값은 다음과 같습니다.
<PropertiesTable
  content={[
{
  name: 'success',
  type: 'string',
  description: '모든 단계가 유효한 결과 출력과 함께 성공적으로 실행을 완료했습니다',
},{
  name: 'failed',
  type: 'string',
  description: 'Workflow 실행 중 오류가 발생했으며 오류 세부 정보를 확인할 수 있습니다',
},{
  name: 'suspended',
  type: 'string',
  description: 'Workflow 실행이 재개를 기다리며 일시 중지되었고, 일시 중지된 단계 정보를 확인할 수 있습니다',
},{
  name: 'canceled',
  type: 'string',
  description:
    'cancel() 메서드를 통해 Workflow 실행이 취소되어 실행 중인 모든 단계가 중지되고 이후 단계가 실행되지 않습니다',
},
]}
/>
## 관련된 {/* #related */}

* [실행.시작()](./run-methods/start)
* [실행.이력서()](./run-methods/resume)
* [실행.취소()](./run-methods/cancel)
* [실행.재시작()](./run-methods/restart)
* [실행.시간여행()](./run-methods/timeTravel)
* [실행.스트림()](/docs/workflows/overview#inspecting-workflow-stream-payloads)
* [Run.timeTravelStream()](../streaming/workflows/timeTravelStream)
