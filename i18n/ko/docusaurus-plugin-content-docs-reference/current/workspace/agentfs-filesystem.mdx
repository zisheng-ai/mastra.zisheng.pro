---
title: "참조: AgentFSFilesystem | 작업공간"
description: "Turso/SQLite 지원 스토리지용 AgentFSFilesystem 공급자에 대한 문서입니다."
packages:
  - "@mastra/agentfs"
---

# AgentFS파일 시스템

다음을 통해 Turso/SQLite 데이터베이스에 파일을 저장합니다.[AgentFS](https://github.com/nichochar/agentfs)SDK. 파일은 로컬 SQLite 데이터베이스의 세션 전반에 걸쳐 유지되므로 외부 클라우드 서비스 없이 Agent에 내구성 있는 스토리지를 제공합니다. 인터페이스에 대한 자세한 내용은 다음을 참조하세요.[Workspace파일 시스템 인터페이스](/reference/workspace/filesystem).

## 설치 {/* #installation */}

```bash npm2yarn
npm install @mastra/agentfs
```

## 용법 {/* #usage */}

Workspace에 `AgentFSFilesystem`을 추가하고 Agent에 할당하세요. `AgentFSFilesystem` 클래스를 인스턴스화할 때 `agentId`, `path` 또는 `agent` 중 하나 이상을 제공해야 합니다.
```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { AgentFSFilesystem } from '@mastra/agentfs'

const workspace = new Workspace({
  filesystem: new AgentFSFilesystem({
    agentId: 'my-agent',
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_OPENAI_MODEL__',
  workspace,
})
```

### 명시적 데이터베이스 경로 사용 {/* #using-an-explicit-database-path */}

기본적으로 데이터베이스는 `.agentfs` 디렉터리에 `agentId`를 파일 이름으로 사용하여 저장됩니다. 사용자 지정 데이터베이스 경로를 지정할 수도 있습니다.
```typescript
import { AgentFSFilesystem } from '@mastra/agentfs'

const filesystem = new AgentFSFilesystem({
  path: '/data/my-agent.db',
})
```

### 미리 열린 AgentFS 인스턴스 사용 {/* #using-a-pre-opened-agentfs-instance */}

AgentFS 수명주기를 직접 관리해야 하는 경우:

```typescript
import { AgentFS } from 'agentfs-sdk'
import { AgentFSFilesystem } from '@mastra/agentfs'

const agent = await AgentFS.open({ id: 'my-agent' })

const filesystem = new AgentFSFilesystem({
  agent, // caller manages open/close
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

`agentId`, `path` 또는 `agent` 중 하나 이상을 제공해야 합니다.
<PropertiesTable
  content={[
{
  name: 'agentId',
  type: 'string',
  description: 'Agent ID — `.agentfs/<agentId>.db`에 데이터베이스를 생성합니다',
  isOptional: true,
},{
  name: 'path',
  type: 'string',
  description: '명시적인 데이터베이스 파일 경로(agentId의 대안)',
  isOptional: true,
},{
  name: 'agent',
  type: 'AgentFS',
  description: '미리 열린 AgentFS 인스턴스입니다. 제공하면 호출자가 수명 주기(열기/닫기)를 관리합니다.',
  isOptional: true,
},{
  name: 'id',
  type: 'string',
  description: '이 파일 시스템 인스턴스의 고유 식별자',
  isOptional: true,
  defaultValue: '자동 생성',
},{
  name: 'displayName',
  type: 'string',
  description: 'UI에 표시되는 사용자 친화적인 이름',
  isOptional: true,
  defaultValue: "'AgentFS'",
},{
  name: 'icon',
  type: 'FilesystemIcon',
  description: 'UI의 아이콘 식별자',
  isOptional: true,
  defaultValue: "'database'",
},{
  name: 'description',
  type: 'string',
  description: 'UI에 표시되는 이 파일 시스템의 간단한 설명',
  isOptional: true,
},{
  name: 'readOnly',
  type: 'boolean',
  description: 'true이면 모든 쓰기 작업이 차단됩니다',
  isOptional: true,
  defaultValue: 'false',
},
]}
/>
## 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: '파일 시스템 인스턴스 식별자',
},{
  name: 'name',
  type: 'string',
  description: "Provider 이름('AgentFSFilesystem')",
},{
  name: 'provider',
  type: 'string',
  description: "Provider 식별자('agentfs')",
},{
  name: 'readOnly',
  type: 'boolean | undefined',
  description: '파일 시스템이 읽기 전용 모드인지 여부',
},
]}
/>