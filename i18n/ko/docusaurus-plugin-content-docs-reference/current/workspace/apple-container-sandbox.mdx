---
title: "참조: AppleContainerSandbox | 작업공간"
description: "작업공간 명령 실행을 위한 로컬 Apple 컨테이너 CLI 샌드박스인 AppleContainerSandbox에 대한 API 참조입니다."
packages:
  - "@mastra/apple-container"
---

# Apple컨테이너샌드박스

Apple을 통해 로컬 OCI Linux 컨테이너 내에서 명령을 실행합니다.[`container`](https://github.com/apple/container)CLI. 공급자는 수명이 긴 컨테이너를 시작하고`container exec`작업 공간 명령의 경우. 인터페이스에 대한 자세한 내용은 다음을 참조하세요.[WorkspaceSandbox 인터페이스](/reference/workspace/sandbox).

## 설치 {/* #installation */}

```bash npm2yarn
npm install @mastra/apple-container
```

Apple Silicon Mac에서 macOS 26 이상을 실행하고 Apple의 `container` CLI가 설치되어 있어야 합니다. Provider를 사용하기 전에 컨테이너 시스템을 시작하세요.
```bash
container system start
```

## 용법 {/* #usage */}

Workspace에 `AppleContainerSandbox`를 추가하고 Agent에 할당하세요.
```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { AppleContainerSandbox } from '@mastra/apple-container'

const workspace = new Workspace({
  sandbox: new AppleContainerSandbox({
    image: 'node:22-slim',
    volumes: {
      '/Users/me/project': '/workspace',
    },
    workingDir: '/workspace',
  }),
})

const agent = new Agent({
  id: 'dev-agent',
  name: 'Dev Agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})

const response = await agent.generate('Run `node --version`.')
console.log(response.text)
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: '이 Sandbox 인스턴스의 고유 식별자입니다.',
  isOptional: true,
  defaultValue: '자동 생성',
},{
  name: 'name',
  type: 'string',
  description:
    '`container run --name`에 전달되는 Apple 컨테이너 이름입니다. `[a-zA-Z0-9_.-]` 이외의 문자는 `-`로 대체되며, 결과가 영숫자로 시작하지 않으면 접두사가 추가됩니다.',
  isOptional: true,
  defaultValue: 'Sandbox `id`',
},{
  name: 'image',
  type: 'string',
  description: '컨테이너에 사용할 OCI 이미지입니다.',
  isOptional: true,
  defaultValue: "'node:22-slim'",
},{
  name: 'command',
  type: 'string[]',
  description: '컨테이너 초기화 명령입니다. exec 기반 명령 실행을 위해 컨테이너가 계속 실행되도록 해야 합니다.',
  isOptional: true,
  defaultValue: "['sleep', 'infinity']",
},{
  name: 'env',
  type: 'Record<string, string>',
  description: '컨테이너 및 명령 실행에 설정할 환경 변수입니다.',
  isOptional: true,
},{
  name: 'volumes',
  type: 'Record<string, string>',
  description: '호스트와 컨테이너 간 바인드 마운트입니다. 키는 호스트 경로이고 값은 컨테이너 경로입니다.',
  isOptional: true,
},{
  name: 'mounts',
  type: 'string[]',
  description: '원시 `container run --mount` 사양입니다.',
  isOptional: true,
},{
  name: 'network',
  type: 'string',
  description: 'Apple 컨테이너 네트워크 연결 사양입니다.',
  isOptional: true,
},{
  name: 'publishedPorts',
  type: 'string[]',
  description: '`--publish`로 전달되는 포트 게시 사양입니다.',
  isOptional: true,
},{
  name: 'publishedSockets',
  type: 'string[]',
  description: '`--publish-socket`으로 전달되는 소켓 게시 사양입니다.',
  isOptional: true,
},{
  name: 'cpus',
  type: 'number | string',
  description: '할당할 CPU 수입니다.',
  isOptional: true,
},{
  name: 'memory',
  type: 'string',
  description: "메모리 할당량입니다(예: '1G').",
  isOptional: true,
},{
  name: 'platform',
  type: 'string',
  description: "OCI 플랫폼입니다(예: 'linux/arm64').",
  isOptional: true,
},{
  name: 'arch',
  type: 'string',
  description: '다중 아키텍처 이미지를 선택할 때 사용할 이미지 아키텍처입니다.',
  isOptional: true,
},{
  name: 'os',
  type: 'string',
  description: '다중 플랫폼 이미지를 선택할 때 사용할 이미지 운영 체제입니다.',
  isOptional: true,
},{
  name: 'rosetta',
  type: 'boolean',
  description: '컨테이너에서 Rosetta를 활성화합니다.',
  isOptional: true,
  defaultValue: 'false',
},{
  name: 'readonlyRootfs',
  type: 'boolean',
  description: '컨테이너 루트 파일 시스템을 읽기 전용으로 마운트합니다.',
  isOptional: true,
  defaultValue: 'false',
},{
  name: 'ssh',
  type: 'boolean',
  description: '호스트 SSH Agent 소켓을 전달합니다.',
  isOptional: true,
  defaultValue: 'false',
},{
  name: 'init',
  type: 'boolean',
  description: "컨테이너에서 Apple의 init 프로세스를 활성화합니다.",
  isOptional: true,
  defaultValue: 'true',
},{
  name: 'virtualization',
  type: 'boolean',
  description: '컨테이너에 가상화 기능을 노출합니다.',
  isOptional: true,
  defaultValue: 'false',
},{
  name: 'capAdd',
  type: 'string[]',
  description: '추가할 Linux 기능입니다.',
  isOptional: true,
},{
  name: 'capDrop',
  type: 'string[]',
  description: '제거할 Linux 기능입니다.',
  isOptional: true,
},{
  name: 'tmpfs',
  type: 'string[]',
  description: '`--tmpfs`로 전달되는 tmpfs 대상 경로입니다(예: `/tmp`).',
  isOptional: true,
},{
  name: 'dns',
  type: 'string[]',
  description: 'DNS 네임서버 IP입니다.',
  isOptional: true,
},{
  name: 'dnsSearch',
  type: 'string[]',
  description: 'DNS 검색 도메인입니다.',
  isOptional: true,
},{
  name: 'noDns',
  type: 'boolean',
  description: '컨테이너에 DNS를 구성하지 않습니다.',
  isOptional: true,
  defaultValue: 'false',
},{
  name: 'labels',
  type: 'Record<string, string>',
  description: '추가 컨테이너 레이블입니다. Mastra 레이블(mastra.sandbox, mastra.sandbox.id)은 항상 포함됩니다.',
  isOptional: true,
},{
  name: 'workingDir',
  type: 'string',
  description: '컨테이너 내부의 작업 디렉터리입니다.',
  isOptional: true,
  defaultValue: "'/workspace'",
},{
  name: 'timeout',
  type: 'number',
  description: '기본 명령 제한 시간(밀리초)입니다.',
  isOptional: true,
  defaultValue: '300000 (5분)',
},{
  name: 'deleteOnDestroy',
  type: 'boolean',
  description:
    'Sandbox가 삭제될 때 Apple 컨테이너를 삭제합니다. false이면 삭제하는 대신 컨테이너를 중지합니다.',
  isOptional: true,
  defaultValue: 'true',
},{
  name: 'containerBinary',
  type: 'string',
  description: 'Apple 컨테이너 CLI의 경로 또는 이름입니다.',
  isOptional: true,
  defaultValue: "'container'",
},{
  name: 'instructions',
  type: 'string | function',
  description:
    'getInstructions()가 반환하는 기본 지침을 재정의하는 사용자 지정 지침입니다. 지침을 표시하지 않으려면 빈 문자열을 전달하세요.',
  isOptional: true,
},
]}
/>
## 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'Sandbox 인스턴스 식별자입니다.',
},{
  name: 'name',
  type: 'string',
  description: "Provider 이름('AppleContainerSandbox').",
},{
  name: 'provider',
  type: 'string',
  description: "Provider 식별자('apple-container').",
},{
  name: 'status',
  type: 'ProviderStatus',
  description: "'pending' | 'starting' | 'running' | 'stopping' | 'stopped' | 'destroying' | 'destroyed' | 'error'",
},{
  name: 'containerId',
  type: 'string',
  description: '알려진 경우 Apple 컨테이너 ID이며, 그렇지 않으면 구성된 컨테이너 이름입니다.',
},
]}
/>
## 환경변수 {/* #environment-variables */}

`env`를 사용하여 컨테이너 수준에서 환경 변수를 설정합니다. 명령별 환경 변수는 `executeCommand` 옵션을 통해서도 전달할 수 있습니다.
```typescript
const sandbox = new AppleContainerSandbox({
  image: 'node:22-slim',
  env: {
    NODE_ENV: 'development',
  },
})

await sandbox.executeCommand('node', ['-e', 'console.log(process.env.TASK_ID)'], {
  env: { TASK_ID: '42' },
})
```

## 바인드 마운트 {/* #bind-mounts */}

다음을 사용하여 호스트 디렉터리를 컨테이너에 마운트합니다.`volumes` option:

```typescript
const sandbox = new AppleContainerSandbox({
  image: 'node:22-slim',
  volumes: {
    '/Users/me/project': '/workspace/project',
    '/Users/me/.npm': '/root/.npm',
  },
})
```

바인드 마운트는 컨테이너 생성 시 적용됩니다. 샌드박스가 시작되기 전에 호스트 경로가 존재해야 합니다.

## 리소스 및 플랫폼 옵션 {/* #resource-and-platform-options */}

Apple 컨테이너 CLI 옵션은 생성자를 통해 전달될 수 있습니다.

```typescript
const sandbox = new AppleContainerSandbox({
  image: 'node:22-slim',
  volumes: {
    '/Users/me/project': '/workspace',
  },
  cpus: 2,
  memory: '2G',
  platform: 'linux/arm64',
  readonlyRootfs: true,
  tmpfs: ['/tmp'],
})
```

이러한 옵션은 새 컨테이너가 생성될 때만 적용됩니다. Sandbox가 동일한 이름을 가진 기존 컨테이너에 다시 연결되면 변경된 런타임 옵션을 적용하기 위해 Sandbox를 삭제한 후 다시 생성하세요.
Apple의 `--tmpfs`는 `/tmp`와 같은 컨테이너 경로만 허용하며, `/tmp:rw,size=256m`과 같은 Docker 스타일 옵션 사양은 허용하지 않습니다.
`readonlyRootfs`를 활성화할 때는 `workingDir`이 이미지 또는 바인드 마운트에서 제공하는 경로를 가리키는지 확인하세요. 쓰기 가능한 tmpfs도 지원됩니다.
## 보안 Model {/* #security-model */}

`AppleContainerSandbox`는 호스트의 Apple `container` 서비스를 통해 로컬 컨테이너를 실행합니다. 생성자 옵션은 신뢰할 수 있는 서버 측 구성으로 취급하세요.
* `volumes`, `mounts`, `publishedSockets`는 호스트 경로를 컨테이너화된 코드에 노출할 수 있습니다.
* `publishedPorts`는 호스트 또는 네트워크에서 컨테이너 내 서비스를 노출할 수 있습니다. 로컬 접근만 허용하려면 `127.0.0.1`에 바인딩하세요.
* `ssh`는 호스트 SSH Agent 소켓을 전달합니다.
* `capAdd`와 `virtualization`은 컨테이너화된 코드가 수행할 수 있는 작업의 범위를 확장할 수 있습니다.
* `containerBinary`는 신뢰할 수 있는 코드를 위한 생성자 전용 이스케이프 해치이며, 직렬화 가능한 편집기 Provider 스키마에는 포함되지 않습니다.
워크로드에 필요한 가장 좁은 마운트와 기능을 사용하세요. 기존 컨테이너는 샌드박스 ID에 대한 Mastra 소유권 레이블이 있는 경우에만 다시 연결됩니다. 이 공급자가 만든 컨테이너에는 config-hash 레이블도 포함되어 있습니다. 해당 레이블이 있는 경우 이미지, 명령, 마운트, 포트, 기능 또는 작업 디렉터리와 같은 변경할 수 없는 런타임 옵션이 변경되면 다시 연결이 실패합니다.

## 제한사항 {/* #limitations */}

`AppleContainerSandbox`는 `executeCommand()`를 사용하여 포그라운드 Workspace 명령 실행을 구현합니다. 백그라운드 프로세스나 LSP 세션을 위한 `SandboxProcessManager`는 아직 제공하지 않습니다.
명령 시간 초과는 컨테이너 내부에서 적용되므로 시간 초과된 명령은 컨테이너 런타임에 의해 정리됩니다. 중단 신호는 호스트 CLI 대기 경로를 취소하므로 컨테이너 내 정리가 중요한 경우 명령 시간 초과 대신 사용해서는 안 됩니다.

## 재연결 {/* #reconnection */}

`AppleContainerSandbox`는 구성된 이름의 컨테이너를 검사하여 다시 연결합니다. `start()`가 호출되면 다음과 같이 동작합니다.
* 실행 중인 컨테이너는 재사용됩니다.
* 중지된 컨테이너가 다시 시작됩니다.
* 구성된 이미지에서 누락된 컨테이너가 생성됩니다.
* 이름이 구성되어 있지만 Mastra 소유권 라벨이 일치하지 않는 컨테이너는 관리되지 않고 실패합니다.
* 변경할 수 없는 런타임 옵션과 일치하지 않는 구성 해시 레이블이 있는 Mastra 소유 컨테이너는 재사용되지 않고 실패합니다.

```typescript
const sandbox = new AppleContainerSandbox({ id: 'persistent-sandbox' })
await sandbox.start()

const sandbox2 = new AppleContainerSandbox({ id: 'persistent-sandbox' })
await sandbox2.start()
```

## 편집자 Provider {/* #editor-provider */}

저장된 Sandbox 구성을 하이드레이션하려면 `MastraEditor`에 Provider를 등록하세요.
```typescript
import { MastraEditor } from '@mastra/editor'
import { appleContainerSandboxProvider } from '@mastra/apple-container'

const editor = new MastraEditor({
  sandboxes: {
    [appleContainerSandboxProvider.id]: appleContainerSandboxProvider,
  },
})
```

## 관련된 {/* #related */}

* [WorkspaceSandbox 인터페이스](/reference/workspace/sandbox)
* [DockerSandbox 참조](/reference/workspace/docker-sandbox)
* [로컬샌드박스 참조](/reference/workspace/local-sandbox)
* [작업공간 개요](/docs/workspace/overview)
