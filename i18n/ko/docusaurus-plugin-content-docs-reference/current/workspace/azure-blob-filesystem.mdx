---
title: "참조: AzureBlobFilesystem | 작업공간"
description: "Azure Blob Storage용 AzureBlobFilesystem 공급자에 대한 설명서입니다."
packages:
  - "@mastra/azure"
---

# AzureBlob파일 시스템

Azure Blob Storage 컨테이너에 파일을 저장합니다. 인터페이스에 대한 자세한 내용은 다음을 참조하세요.[WorkspaceFilesystem Interface](/reference/workspace/filesystem).

## 설치 {/* #installation */}

```bash npm2yarn
npm install @mastra/azure
```

## 용법 {/* #usage */}

Workspace에 `AzureBlobFilesystem`을 추가하고 Agent에 할당합니다:
```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const workspace = new Workspace({
  filesystem: new AzureBlobFilesystem({
    container: 'my-container',
    connectionString: process.env.AZURE_STORAGE_CONNECTION_STRING,
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  workspace,
})
```

### 계정 키 {/* #account-key */}

전체 연결 문자열을 전달하지 않으려면 계정 이름과 계정 키를 사용하세요.

```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  accountKey: process.env.AZURE_STORAGE_ACCOUNT_KEY,
})
```

### 공유 액세스 서명 토큰 {/* #shared-access-signature-token */}

SAS(공유 액세스 서명) 토큰을 `accountName` 또는 `endpoint`와 함께 사용합니다:
```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  sasToken: process.env.AZURE_STORAGE_SAS_TOKEN,
})
```

### 기본Azure 자격 증명 {/* #defaultazurecredential */}

환경에서 Azure ID 자격 증명을 제공하는 경우 `DefaultAzureCredential`을 사용합니다:
```typescript
import { AzureBlobFilesystem } from '@mastra/azure/blob'

const filesystem = new AzureBlobFilesystem({
  container: 'my-container',
  accountName: process.env.AZURE_STORAGE_ACCOUNT_NAME,
  useDefaultCredential: true,
})
```

`DefaultAzureCredential`을 사용할 때는 `@azure/identity`를 설치합니다:
```bash npm2yarn
npm install @azure/identity
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'container',
  type: 'string',
  description: 'Azure Blob Storage 컨테이너 이름',
},{
  name: 'connectionString',
  type: 'string',
  description: '전체 Azure Storage 연결 문자열입니다. 다른 인증 옵션보다 우선합니다.',
  isOptional: true,
},{
  name: 'accountName',
  type: 'string',
  description: 'Azure Storage 계정 이름입니다. connectionString 또는 endpoint를 사용하지 않는 경우 필수입니다.',
  isOptional: true,
},{
  name: 'accountKey',
  type: 'string',
  description: 'Azure Storage 계정 키입니다.',
  isOptional: true,
},{
  name: 'sasToken',
  type: 'string',
  description: '공유 액세스 서명 토큰입니다. accountName 또는 endpoint가 필요합니다.',
  isOptional: true,
},{
  name: 'useDefaultCredential',
  type: 'boolean',
  description: '@azure/identity의 DefaultAzureCredential을 사용합니다.',
  isOptional: true,
  defaultValue: 'false',
},{
  name: 'endpoint',
  type: 'string',
  description: '사용자 지정 Blob 서비스 엔드포인트 URL입니다. Azurite를 사용하는 로컬 개발에 사용됩니다.',
  isOptional: true,
},{
  name: 'prefix',
  type: 'string',
  description: '모든 키에 적용할 선택적 접두사입니다(하위 디렉터리처럼 작동).',
  isOptional: true,
},{
  name: 'id',
  type: 'string',
  description: '이 파일 시스템 인스턴스의 고유 식별자입니다.',
  isOptional: true,
  defaultValue: '자동 생성',
},{
  name: 'displayName',
  type: 'string',
  description: 'UI에 표시할 사용자 친화적인 이름입니다.',
  isOptional: true,
},{
  name: 'icon',
  type: 'FilesystemIcon',
  description: 'UI에 사용할 아이콘 식별자입니다.',
  isOptional: true,
},{
  name: 'description',
  type: 'string',
  description: 'UI에 표시할 이 파일 시스템의 짧은 설명입니다.',
  isOptional: true,
},{
  name: 'readOnly',
  type: 'boolean',
  description: 'true이면 모든 쓰기 작업이 차단됩니다.',
  isOptional: true,
  defaultValue: 'false',
},
]}
/>
## 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: '파일 시스템 인스턴스 식별자입니다.',
},{
  name: 'name',
  type: 'string',
  description: "Provider 이름입니다('AzureBlobFilesystem').",
},{
  name: 'provider',
  type: 'string',
  description: "Provider 식별자입니다('azure-blob').",
},{
  name: 'readOnly',
  type: 'boolean | undefined',
  description: '파일 시스템이 읽기 전용 모드인지 여부입니다.',
},
]}
/>
## 행동 양식 {/* #methods */}

AzureBlobFilesystem은 다음을 구현합니다.[WorkspaceFilesystem interface](/reference/workspace/filesystem), providing all standard filesystem methods:

* `readFile(path, options?)`- 파일 내용 읽기
* `writeFile(path, content, options?)`- 파일에 콘텐츠 쓰기
* `appendFile(path, content)`- 파일에 콘텐츠 추가
* `deleteFile(path, options?)`- 파일 삭제
* `copyFile(src, dest, options?)`- 파일 복사
* `moveFile(src, dest, options?)`- 파일 이동 또는 이름 바꾸기
* `mkdir(path, options?)`- 디렉토리 생성
* `rmdir(path, options?)`- 디렉토리 제거
* `readdir(path, options?)`- 디렉토리 내용 나열
* `exists(path)`- 경로가 존재하는지 확인
* `stat(path)`- 파일 또는 디렉터리 메타데이터 가져오기

### `init()` {/* #init */}

파일 시스템을 초기화합니다. 컨테이너 액세스 및 자격 증명을 확인합니다.

```typescript
await filesystem.init()
```

### `getInfo()` {/* #getinfo */}

이 파일 시스템 인스턴스에 대한 메타데이터를 반환합니다.

```typescript
const info = filesystem.getInfo()
// { id: '...', name: 'AzureBlobFilesystem', provider: 'azure-blob', status: 'ready' }
```

### `getContainer()` {/* #getcontainer */}

기본 Azure Blob Storage를 반환합니다.`ContainerClient`.

```typescript
const container = await filesystem.getContainer()
```

### `getMountConfig()` {/* #getmountconfig */}

Azure Blob 파일 시스템 탑재를 지원하는 샌드박스 공급자에 대한 탑재 구성을 반환합니다.

```typescript
const config = filesystem.getMountConfig()
// { type: 'azure-blob', container: 'my-container', ... }
```

:::note

Azure Blob Sandbox 마운트는 Sandbox Provider의 `azure-blob` 마운트 구성 지원 여부에 따라 달라집니다. Sandbox Provider가 Azure Blob 마운트를 지원하지 않으면 Workspace 파일을 직접 조작할 때 `filesystem`을 사용하세요.
:::

## 관련된 {/* #related */}

* [WorkspaceFilesystem 인터페이스](/reference/workspace/filesystem)
* [S3파일 시스템 참조](/reference/workspace/s3-filesystem)
* [GCS파일 시스템 참조](/reference/workspace/gcs-filesystem)
* [작업공간 개요](/docs/workspace/overview)
