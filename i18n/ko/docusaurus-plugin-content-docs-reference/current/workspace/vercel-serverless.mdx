---
title: "참조: VercelServerlessSandbox | 작업공간"
description: "명령을 Vercel Functions로 배포하고 실행하는 서버리스 샌드박스 공급자인 VercelServerlessSandbox에 대한 API 참조입니다."
packages:
  - "@mastra/vercel"
---

# Vercel서버리스샌드박스

다음과 같이 명령을 실행합니다.[Vercel](https://vercel.com)서버리스 기능. 자동 확장을 통해 전역적으로 분산된 제로 인프라 실행을 제공합니다. 인터페이스에 대한 자세한 내용은 다음을 참조하세요.[WorkspaceSandbox 인터페이스](/reference/workspace/sandbox).

:::경고

VercelServerlessSandbox는 상태 비저장입니다. 영구 파일 시스템, 대화형 셸 또는 장기 실행 백그라운드 프로세스를 제공하지 않습니다. 오직`/tmp` is writable, and it's ephemeral between invocations.

:::

:::메모

영구 파일 시스템을 갖춘 전체 Linux MicroVM의 경우,`sudo` access, exposed ports, and background processes, use [`VercelSandbox`](/reference/workspace/vercel-sandbox), which is backed by the [Vercel Sandbox](https://vercel.com/docs/vercel-sandbox) product.

:::

## 설치 {/* #installation */}

```bash npm2yarn
npm install @mastra/vercel
```

## 용법 {/* #usage */}

추가`VercelServerlessSandbox` to a workspace and assign it to an agent:

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { VercelServerlessSandbox } from '@mastra/vercel'

const workspace = new Workspace({
  sandbox: new VercelServerlessSandbox({
    token: process.env.VERCEL_TOKEN,
  }),
})

const agent = new Agent({
  id: 'dev-agent',
  name: 'dev-agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})
```

### 커스텀 리소스를 사용한 팀 범위 배포 {/* #team-scoped-deployment-with-custom-resources */}

```typescript
const workspace = new Workspace({
  sandbox: new VercelServerlessSandbox({
    token: process.env.VERCEL_TOKEN,
    teamId: 'team_abc123',
    regions: ['iad1', 'sfo1'],
    maxDuration: 120,
    memory: 3008,
    env: {
      NODE_ENV: 'production',
    },
  }),
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'token',
  type: 'string',
  description: 'Vercel API token. Falls back to `VERCEL_TOKEN` environment variable.',
  isOptional: true,
},
{
  name: 'teamId',
  type: 'string',
  description: 'Vercel team ID for team-scoped deployments.',
  isOptional: true,
},
{
  name: 'projectName',
  type: 'string',
  description: 'Existing Vercel project name. Auto-generated if omitted.',
  isOptional: true,
},
{
  name: 'regions',
  type: 'string[]',
  description: 'Deployment regions.',
  isOptional: true,
  defaultValue: "['iad1']",
},
{
  name: 'maxDuration',
  type: 'number',
  description: 'Function maximum duration in seconds.',
  isOptional: true,
  defaultValue: '60',
},
{
  name: 'memory',
  type: 'number',
  description: 'Function memory in MB.',
  isOptional: true,
  defaultValue: '1024',
},
{
  name: 'env',
  type: 'Record<string, string>',
  description:
    'Environment variables baked into the deployed function source. These are embedded at deploy time, not set dynamically.',
  isOptional: true,
},
{
  name: 'commandTimeout',
  type: 'number',
  description: 'Per-invocation command timeout in milliseconds.',
  isOptional: true,
  defaultValue: '55000',
},
{
  name: 'instructions',
  type: 'string | ((opts) => string)',
  description:
    'Custom instructions that override the default instructions returned by `getInstructions()`. Pass a string to fully replace, or a function to extend the defaults.',
  isOptional: true,
},
{
  name: 'onStart',
  type: 'SandboxLifecycleHook',
  description: 'Lifecycle hook called after the sandbox reaches `running` status.',
  isOptional: true,
},
{
  name: 'onStop',
  type: 'SandboxLifecycleHook',
  description: 'Lifecycle hook called before the sandbox stops.',
  isOptional: true,
},
{
  name: 'onDestroy',
  type: 'SandboxLifecycleHook',
  description: 'Lifecycle hook called before the sandbox is destroyed.',
  isOptional: true,
},
]}
/>

## 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'Unique identifier for this sandbox instance.',
},
{
  name: 'name',
  type: "'VercelServerlessSandbox'",
  description: 'Human-readable name.',
},
{
  name: 'provider',
  type: "'vercel-serverless'",
  description: 'Provider type identifier.',
},
{
  name: 'status',
  type: 'ProviderStatus',
  description:
    "Current lifecycle status: `'pending'`, `'starting'`, `'running'`, `'stopping'`, `'stopped'`, `'destroying'`, `'destroyed'`, or `'error'`.",
},
]}
/>

## 제한사항 {/* #limitations */}

VercelServerlessSandbox는 내부적으로 서버리스 기능을 사용합니다. 이는 다음을 의미합니다.

* **영구 파일 시스템 없음**: 작성된 파일`/tmp` are ephemeral and cleared between invocations.
* **대화형 쉘 없음**: 명령은 다음을 통해 실행됩니다.`/bin/sh -c` with no stdin streaming.
* **백그라운드 프로세스 없음**: 프로세스 관리자, PID 또는 장기 실행 작업이 없습니다.
* **장착 없음**: 클라우드 스토리지 마운트(S3, GCS)는 지원되지 않습니다.
* **시간 초과 제한**: 최대 실행 시간은 다음과 같이 제한됩니다.`maxDuration` (default 60s).

## 관련된 {/* #related */}

* [WorkspaceSandbox 인터페이스](/reference/workspace/sandbox)
* [작업실 수업](/reference/workspace/workspace-class)
* [E2B샌드박스](/reference/workspace/e2b-sandbox): 지속적인 파일 시스템 및 프로세스 관리 기능을 갖춘 모든 기능을 갖춘 클라우드 샌드박스
