---
title: "참조: ArchilFilesystem | 작업공간"
description: "Archil의 탄력적인 서버리스 파일 시스템을 위한 ArchilFilesystem 공급자에 대한 API 참조입니다."
packages:
  - "@mastra/archil"
---

# Archil파일 시스템

다음에 파일을 저장합니다.[Archil](https://docs.archil.com)탄력적인 서버리스 디스크. 빠른 읽기/쓰기를 위해 S3 호환 개체 API를 결합합니다.`exec()`POSIX 쉘 작업 및`grep()`병렬 서버 측 검색의 경우. 인터페이스에 대한 자세한 내용은 다음을 참조하세요.[Workspace파일 시스템 인터페이스](/reference/workspace/filesystem).

## 설치 {/* #installation */}

```bash npm2yarn
npm install @mastra/archil
```

## 용법 {/* #usage */}

Workspace에 `ArchilFilesystem`을 추가하고 Agent에 할당하세요.
```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { ArchilFilesystem } from '@mastra/archil'

const workspace = new Workspace({
  filesystem: new ArchilFilesystem({
    diskId: 'dsk-0123456789abcdef',
    apiKey: process.env.ARCHIL_API_KEY,
    region: 'aws-us-east-1',
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  workspace,
})
```

### 초기화 시 새 디스크 만들기 {/* #creating-a-new-disk-on-init */}

기존 디스크가 없으면 `createDiskOptions`를 제공하여 자동으로 생성하세요.
```typescript
import { ArchilFilesystem } from '@mastra/archil'

const filesystem = new ArchilFilesystem({
  apiKey: process.env.ARCHIL_API_KEY,
  region: 'aws-us-east-1',
  createDiskOptions: {
    name: 'my-agent-workspace',
  },
})

await filesystem.init()
// filesystem.diskId now contains the newly created disk ID
```

### 환경 변수 대체 {/* #environment-variable-fallbacks */}

`apiKey`와 `region`을 생략하면 Provider가 환경 변수에서 값을 읽습니다.
* `ARCHIL_API_KEY`- API 키
* `ARCHIL_REGION`- 지역(예:`aws-us-east-1`)
* `ARCHIL_S3_BASE_URL`- 사용자 정의 S3 엔드포인트(선택 사항)

```typescript
import { ArchilFilesystem } from '@mastra/archil'

// Uses ARCHIL_API_KEY and ARCHIL_REGION from environment
const filesystem = new ArchilFilesystem({
  diskId: 'dsk-0123456789abcdef',
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'diskId',
  type: 'string',
  description: '기존 Archil 디스크 ID(예: "dsk-0123456789abcdef")입니다. `createDiskOptions`와 함께 사용할 수 없습니다.',
  isOptional: true,
},{
  name: 'createDiskOptions',
  type: 'CreateDiskRequest',
  description: '초기화 시 새 디스크를 생성하기 위한 옵션입니다. `diskId`와 함께 사용할 수 없습니다.',
  isOptional: true,
},{
  name: 'apiKey',
  type: 'string',
  description: 'Archil API 키입니다. 지정하지 않으면 `ARCHIL_API_KEY` 환경 변수를 사용합니다.',
  isOptional: true,
},{
  name: 'region',
  type: 'string',
  description: 'Archil 리전입니다(예: "aws-us-east-1"). 지정하지 않으면 `ARCHIL_REGION` 환경 변수를 사용합니다.',
  isOptional: true,
},{
  name: 'baseUrl',
  type: 'string',
  description: '사용자 지정 Archil 제어 플레인 URL입니다(테스트 또는 자체 호스팅 배포용).',
  isOptional: true,
},{
  name: 's3BaseUrl',
  type: 'string',
  description: '사용자 지정 S3 호환 API URL입니다. 지정하지 않으면 `ARCHIL_S3_BASE_URL` 환경 변수를 사용합니다.',
  isOptional: true,
},{
  name: 'id',
  type: 'string',
  description: '이 파일 시스템 인스턴스의 고유 식별자입니다.',
  isOptional: true,
  defaultValue: '자동 생성',
},{
  name: 'displayName',
  type: 'string',
  description: 'UI에 표시할 사용자 친화적인 이름입니다.',
  isOptional: true,
},{
  name: 'icon',
  type: 'FilesystemIcon',
  description: 'UI에 사용할 아이콘 식별자입니다.',
  isOptional: true,
},{
  name: 'description',
  type: 'string',
  description: 'UI에 표시할 이 파일 시스템의 짧은 설명입니다.',
  isOptional: true,
},{
  name: 'readOnly',
  type: 'boolean',
  description: 'true이면 모든 쓰기 작업이 차단됩니다.',
  isOptional: true,
  defaultValue: 'false',
},
]}
/>
## 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: '파일 시스템 인스턴스 식별자입니다.',
},{
  name: 'name',
  type: 'string',
  description: "Provider 이름입니다('ArchilFilesystem').",
},{
  name: 'provider',
  type: 'string',
  description: "Provider 식별자입니다('archil').",
},{
  name: 'diskId',
  type: 'string | undefined',
  description: 'Archil 디스크 ID입니다(init 후 사용 가능).',
},{
  name: 'readOnly',
  type: 'boolean | undefined',
  description: '파일 시스템이 읽기 전용 모드인지 여부입니다.',
},
]}
/>
## 행동 양식 {/* #methods */}

ArchilFilesystem은 다음을 구현합니다.[WorkspaceFilesystem interface](/reference/workspace/filesystem), providing all standard filesystem methods:

* `readFile(path, options?)`- 파일 내용 읽기
* `writeFile(path, content, options?)`- 파일에 콘텐츠 쓰기
* `appendFile(path, content)`- 파일에 콘텐츠 추가
* `deleteFile(path, options?)`- 파일 삭제
* `copyFile(src, dest, options?)`- 파일 복사
* `moveFile(src, dest, options?)`- 파일 이동 또는 이름 바꾸기
* `mkdir(path, options?)`- 디렉토리 생성
* `rmdir(path, options?)`- 디렉토리 제거
* `readdir(path, options?)`- 디렉토리 내용 나열
* `exists(path)`- 경로가 존재하는지 확인
* `stat(path)`- 파일 또는 디렉터리 메타데이터 가져오기

### `init()` {/* #init */}

파일 시스템을 초기화합니다. 기존 디스크에 연결하거나 새 디스크를 만듭니다.

```typescript
await filesystem.init()
```

### `getInfo()` {/* #getinfo */}

이 파일 시스템 인스턴스에 대한 메타데이터를 반환합니다.

```typescript
const info = filesystem.getInfo()
// { id: '...', name: 'ArchilFilesystem', provider: 'archil', status: 'ready' }
```

### Archil 관련 방법 {/* #archil-specific-methods */}

#### `exec(command)` {/* #execcommand */}

디스크에서 쉘 명령을 실행하십시오. 종료 코드, stdout 및 stderr를 반환합니다.

```typescript
const result = await filesystem.exec('ls -la /data')
// { exitCode: 0, stdout: '...', stderr: '' }
```

#### `grep(options)` {/* #grepoptions */}

디스크의 파일 전체에 대해 병렬 서버측 검색을 실행합니다.

```typescript
const results = await filesystem.grep({
  directory: '/logs',
  pattern: 'ERROR',
  recursive: true,
  glob: '*.log',
})
// { matches: [...], stoppedReason: null }
```

#### `share(path, options?)` {/* #sharepath-options */}

파일 공유를 위해 서명된 URL을 생성합니다.

```typescript
const { url } = await filesystem.share('/reports/output.pdf', {
  expiresIn: 3600,
})
```

## 관련된 {/* #related */}

* [WorkspaceFilesystem 인터페이스](/reference/workspace/filesystem)
* [S3파일 시스템 참조](/reference/workspace/s3-filesystem)
* [GCS파일 시스템 참조](/reference/workspace/gcs-filesystem)
* [AzureBlob파일 시스템 참조](/reference/workspace/azure-blob-filesystem)
* [Archil 문서](https://docs.archil.com)
