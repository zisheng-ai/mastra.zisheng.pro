---
title: "참고: VercelSandbox | 작업공간"
description: "Vercel Sandbox 임시 Firecracker MicroVM 제품이 지원하는 샌드박스 Provider인 VercelSandbox에 대한 API 참조입니다."
packages:
  - "@mastra/vercel"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Vercel샌드박스

내부에서 명령을 실행합니다.[Vercel Sandbox](https://vercel.com/docs/vercel-sandbox)그건 임시적이야[폭죽](https://firecracker-microvm.github.io/)Amazon Linux 2023을 실행하는 MicroVM. 지속적인 세션 내 파일 시스템을 제공합니다.`sudo`액세스, 노출된 포트 및 백그라운드 프로세스. 인터페이스에 대한 자세한 내용은 다음을 참조하세요.[WorkspaceSandbox 인터페이스](/reference/workspace/sandbox).

:::메모

이는 다음과 구별됩니다.[`VercelServerlessSandbox`](/reference/workspace/vercel-serverless), which runs commands as stateless Vercel serverless **Functions**. `VercelSandbox` runs a full Linux MicroVM with a persistent filesystem and long-running processes.

:::

## 설치 {/* #installation */}

```bash npm2yarn
npm install @mastra/vercel
```

## 입증 {/* #authentication */}

그만큼`@vercel/sandbox` SDK uses a Vercel OIDC token automatically when no explicit credentials are provided. If you provide `token`, `teamId`, or `projectId`, provide all three values together.

<Tabs>
  <TabItem value="oidc" label="OIDC (recommended)">
    로컬 개발의 경우 프로젝트를 연결하고 개발 토큰을 가져옵니다.

    ```bash
    vercel link
    vercel env pull
    ```

    Vercel에서는 인증이 자동으로 처리되므로 구성이 필요하지 않습니다.
  </TabItem>

  <TabItem value="env-file" label="Access token (.env)">
    OIDC가 없는 환경의 경우 세 가지 값을 모두 함께 제공합니다.

    ```bash
    VERCEL_TOKEN=your-token
    VERCEL_TEAM_ID=your-team-id
    VERCEL_PROJECT_ID=your-project-id
    ```
  </TabItem>

  <TabItem value="constructor" label="Constructor">
    ```typescript
    new VercelSandbox({
      token: 'your-token',
      teamId: 'your-team-id',
      projectId: 'your-project-id',
    })
    ```
  </TabItem>
</Tabs>

## 용법 {/* #usage */}

추가`VercelSandbox` to a workspace and assign it to an agent:

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { VercelSandbox } from '@mastra/vercel'

const workspace = new Workspace({
  sandbox: new VercelSandbox({
    runtime: 'node24',
    timeout: 600_000,
  }),
})

const agent = new Agent({
  id: 'code-agent',
  name: 'Code Agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})

const response = await agent.generate('Print "Hello, world!" and show the Node.js version.')

console.log(response.text)
```

### 리소스 및 노출된 포트 {/* #resources-and-exposed-ports */}

vCPU(vCPU당 Memory 2048MB)를 할당하고 포트를 노출하여 샌드박스 내부에서 실행되는 네트워크 서비스에 연결합니다.

```typescript
const sandbox = new VercelSandbox({
  runtime: 'node24',
  resources: { vcpus: 4 },
  ports: [3000],
})

const workspace = new Workspace({ sandbox })
await sandbox.start()

// The public HTTPS domain for an exposed port is available via getInfo()
const { metadata } = sandbox.getInfo()
console.log(metadata?.domains) // { 3000: 'https://....vercel.run' }
```

### 스트리밍 출력 {/* #streaming-output */}

다음을 통해 실시간으로 명령 출력 스트리밍`onStdout` and `onStderr` callbacks:

```typescript
await sandbox.executeCommand('sh', ['-c', 'for i in 1 2 3; do echo "line $i"; sleep 1; done'], {
  onStdout: chunk => process.stdout.write(chunk),
  onStderr: chunk => process.stderr.write(chunk),
})
```

두 콜백 모두 선택 사항이며 독립적으로 사용할 수 있습니다.

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'Unique identifier for this sandbox instance.',
  isOptional: true,
  defaultValue: 'Auto-generated',
},
{
  name: 'sandboxName',
  type: 'string',
  description: 'Optional name passed to the Vercel API. Auto-generated if omitted.',
  isOptional: true,
},
{
  name: 'token',
  type: 'string',
  description: 'Vercel API token. Falls back to the VERCEL_TOKEN environment variable. Omit to use the OIDC token.',
  isOptional: true,
},
{
  name: 'teamId',
  type: 'string',
  description: 'Vercel team ID. Falls back to the VERCEL_TEAM_ID environment variable.',
  isOptional: true,
},
{
  name: 'projectId',
  type: 'string',
  description: 'Vercel project ID. Falls back to the VERCEL_PROJECT_ID environment variable.',
  isOptional: true,
},
{
  name: 'runtime',
  type: "'node24' | 'node22' | 'node26' | 'python3.13'",
  description: 'Sandbox runtime.',
  isOptional: true,
  defaultValue: "'node24'",
},
{
  name: 'timeout',
  type: 'number',
  description: 'Timeout in milliseconds before the sandbox auto-terminates.',
  isOptional: true,
  defaultValue: '300000 (5 minutes)',
},
{
  name: 'resources',
  type: '{ vcpus?: number }',
  description: 'Resource allocation. Each vCPU comes with 2048 MB of memory.',
  isOptional: true,
},
{
  name: 'ports',
  type: 'number[]',
  description:
    'Ports to expose from the sandbox (up to 15). Public HTTPS domains are available via getInfo().metadata.domains.',
  isOptional: true,
},
{
  name: 'env',
  type: 'Record<string, string>',
  description: 'Default environment variables inherited by all commands.',
  isOptional: true,
  defaultValue: '{}',
},
{
  name: 'metadata',
  type: 'Record<string, unknown>',
  description: 'Custom metadata surfaced via getInfo().',
  isOptional: true,
  defaultValue: '{}',
},
{
  name: 'instructions',
  type: 'string | ((opts) => string)',
  description:
    'Override the default instructions returned by getInstructions(). Pass a string to replace them, or a function to extend the defaults.',
  isOptional: true,
},
{
  name: 'onStart',
  type: 'SandboxLifecycleHook',
  description: 'Lifecycle hook called after the sandbox reaches `running` status.',
  isOptional: true,
},
{
  name: 'onStop',
  type: 'SandboxLifecycleHook',
  description: 'Lifecycle hook called before the sandbox stops.',
  isOptional: true,
},
{
  name: 'onDestroy',
  type: 'SandboxLifecycleHook',
  description: 'Lifecycle hook called before the sandbox is destroyed.',
  isOptional: true,
},
]}
/>

## 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'Sandbox instance identifier.',
},
{
  name: 'name',
  type: "'VercelSandbox'",
  description: 'Human-readable name.',
},
{
  name: 'provider',
  type: "'vercel-sandbox'",
  description: 'Provider type identifier.',
},
{
  name: 'status',
  type: 'ProviderStatus',
  description: "'pending' | 'starting' | 'running' | 'stopping' | 'stopped' | 'destroying' | 'destroyed' | 'error'",
},
{
  name: 'sandbox',
  type: 'Sandbox',
  description:
    'The underlying @vercel/sandbox Sandbox instance. Throws SandboxNotReadyError if the sandbox has not been started.',
},
{
  name: 'processes',
  type: 'VercelSandboxProcessManager',
  description:
    'Background process manager. See [SandboxProcessManager reference](/reference/workspace/process-manager).',
},
]}
/>

## 백그라운드 프로세스 {/* #background-processes */}

`VercelSandbox`백그라운드 프로세스를 생성하고 관리하기 위한 프로세스 관리자가 포함되어 있습니다. 생성된 각 프로세스는 MicroVM 내에서 분리된 명령으로 실행되며 출력은 명령 로그를 통해 스트리밍됩니다.

```typescript
const sandbox = new VercelSandbox({ runtime: 'node24', ports: [3000] })
await sandbox.start()

const handle = await sandbox.processes.spawn('node server.js', {
  env: { PORT: '3000' },
  onStdout: data => console.log(data),
})

console.log(handle.stdout)
await handle.kill()
```

참조[`SandboxProcessManager` reference](/reference/workspace/process-manager) for the full API.

:::메모

Vercel Sandbox SDK는 명령 실행을 위해 stdin 채널을 노출하지 않으므로`handle.sendStdin()` throws. Filesystem mounting (FUSE) is also not supported by this provider.

:::

## 제한 {/* #limits */}

* vCPU당 Memory가 2048MB인 최대 32개의 vCPU.
* 최대 15개의 노출된 포트.
* 파일 시스템은 임시적입니다. 세션 내에서만 지속되며 샌드박스가 중지되면 손실됩니다.
* 최대 실행 시간은 계획에 따라 다르며(Hobby에서는 45분, Pro 및 Enterprise에서는 최대 24시간) 기본값은 5분입니다.

참조[Vercel Sandbox documentation](https://vercel.com/docs/vercel-sandbox) for current limits and pricing.
