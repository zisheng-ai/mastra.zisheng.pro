---
title: "참조: toAISdkMessages() | AI SDK"
description: "Mastra 메시지를 AI SDK v5 또는 v6용 AI SDK UI 메시지로 변환하는 함수인 toAISdkMessages()에 대한 API 참조입니다."
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdk메시지()

메시지를 입력 형식에서 AI SDK UI 메시지 형식으로 변환합니다.`toAISdkMessages()`기존 AI SDK v5/기본 동작을 유지합니다. 앱이 AI SDK v6에 대해 입력된 경우 다음을 통과하세요.`version: 'v6'`.

`version: 'v6'`MessageList AI SDK v6 UI 출력 경로를 사용합니다. MessageList 입력 감지 및 수집은 변경되지 않습니다.

## 사용예 {/* #usage-example */}

```typescript title="app/chat/page.tsx"
import { toAISdkMessages } from '@mastra/ai-sdk/ui'
import { useChat } from 'ai/react'

// Stored messages from your database, memory, or API
const storedMessages = [
  { id: '1', role: 'user', content: 'Hello', parts: [{ type: 'text', text: 'Hello' }] },
  {
    id: '2',
    role: 'assistant',
    content: 'Hi there!',
    parts: [{ type: 'text', text: 'Hi there!' }],
  },
]

export default function Chat() {
  const { messages } = useChat({
    initialMessages: toAISdkMessages(storedMessages, { version: 'v6' }),
  })

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role}: {message.parts.map(part => (part.type === 'text' ? part.text : null))}
        </div>
      ))}
    </div>
  )
}
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'messages',
  type: 'MessageListInput',
  description:
    '변환할 메시지입니다. 문자열, 문자열 배열, 단일 메시지 객체 또는 지원되는 형식의 메시지 객체 배열일 수 있습니다.',
  isOptional: false,
},{
  name: 'options.version',
  type: "'v5' | 'v6'",
  description:
    "반환할 AI SDK 메시지 타입을 선택합니다. 기존 기본 동작을 사용하려면 생략하거나 `'v5'`를 전달하세요. 앱이 AI SDK v6 `useChat()` 메시지 타입을 기준으로 타입이 지정된 경우 `'v6'`을 전달하세요.",
  isOptional: true,
},
]}
/>
## 보고 {/* #returns */}

선택한 버전에 맞게 타입이 지정된 AI SDK `UIMessage` 객체 배열을 반환합니다.
<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: '고유한 메시지 식별자입니다.',
},{
  name: 'role',
  type: "'user' | 'assistant' | 'system'",
  description: '메시지 발신자의 역할입니다.',
},{
  name: 'parts',
  type: 'UIMessagePart[]',
  description: '텍스트, Tool 결과, 파일, 추론, 출처 및 단계 마커를 포함하는 UI 부분의 배열입니다.',
},{
  name: 'metadata',
  type: 'Record<string, unknown>',
  description: 'createdAt, threadId, resourceId 및 사용자 정의 필드를 포함하는 선택적 메타데이터입니다.',
  isOptional: true,
},
]}
/>
## 예 {/* #examples */}

### 기본 AI SDK v5 유형 사용 {/* #using-the-default-ai-sdk-v5-types */}

```typescript
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const messages = toAISdkMessages(['Hello', 'How can I help you today?'])
```

### AI SDK v6 메시지 유형 반환 {/* #returning-ai-sdk-v6-message-types */}

```typescript
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const messages = toAISdkMessages(['Hello', 'How can I help you today?'], {
  version: 'v6',
})
```

### Mastra 클라이언트로 메시지 로드 {/* #loading-messages-with-mastra-client */}

```typescript
import { MastraClient } from '@mastra/client-js'
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const client = new MastraClient()

const { messages } = await client.listThreadMessages('thread-id', { agentId: 'myAgent' })
const uiMessages = toAISdkMessages(messages)
```

## 관련된 {/* #related */}

* [`toAISdkStream()`](/reference/ai-sdk/to-ai-sdk-stream)
* [`toAISdkV5Messages()`](/reference/ai-sdk/to-ai-sdk-v5-messages)
* [`toAISdkV4Messages()`](/reference/ai-sdk/to-ai-sdk-v4-messages)
