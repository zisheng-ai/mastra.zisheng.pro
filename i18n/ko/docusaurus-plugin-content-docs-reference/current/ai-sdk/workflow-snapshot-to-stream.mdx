---
title: "참조: WorkflowSnapshotToStream() | AI SDK"
description: "지속형 Workflow 실행을 AI SDK 호환 스트림으로 변환하는 함수인 WorkflowSnapshotToStream()에 대한 API 참조입니다."
packages:
  - "@mastra/ai-sdk"
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# WorkflowSnapshotToStream()

변환합니다`WorkflowState`객체(에 의해 반환됨)`workflow.getWorkflowRunById()`)로`ReadableStream`AI SDK UIMessage 데이터 부분. 스트림에는 동일한 내용이 포함되어 있습니다.`WorkflowDataPart`그리고`WorkflowStepDataPart`살아있는 덩어리[`workflowRoute()`](/reference/ai-sdk/workflow-route)생산합니다.

라이브 Workflow 스트림을 렌더링하는 동일한 UI 구성 요소를 사용하여 완료되거나 일시 중단된 Workflow 실행을 표시하려는 경우 이를 사용합니다.

## 사용예 {/* #usage-example */}

과거 Workflow 실행을 스트림으로 재생하는 Next.js 앱 라우터 엔드포인트:

```typescript title="app/api/workflow-replay/route.ts"
import { workflowSnapshotToStream } from '@mastra/ai-sdk'
import { createUIMessageStreamResponse } from 'ai'
import { mastra } from '@/src/mastra'

export async function GET(req: Request) {
  const { searchParams } = new URL(req.url)
  const runId = searchParams.get('runId')!

  const workflowRun = await mastra.getWorkflow('myWorkflow').getWorkflowRunById(runId)

  if (!workflowRun) {
    return new Response('Not found', { status: 404 })
  }

  const stream = workflowSnapshotToStream(workflowRun)
  return createUIMessageStreamResponse({ stream })
}
```

## 매개변수 {/* #parameters */}

<PropertiesTable
  content={[
{
  name: 'workflowRun',
  type: 'WorkflowState',
  description:
    '`getWorkflowRunById()` 또는 Workflow 실행 API에서 반환되는 Workflow 실행 상태 객체입니다. `runId`, `workflowName`, `status`, `steps`를 포함합니다.',
  isOptional: false,
},
]}
/>
## 보고 {/* #returns */}

`ReadableStream`: 다음을 포함하는 AI SDK UIMessage 데이터 부분의 스트림입니다.

* `start` 마커
* Workflow 상태와 모든 단계 요약이 포함된 `WorkflowDataPart`
* 전체 출력이 포함된 각 단계의 `WorkflowStepDataPart`
* `finish` 마커
이 스트림을 AI SDK의 [`createUIMessageStreamResponse()`](https://ai-sdk.dev/docs/reference/ai-sdk-ui/create-ui-message-stream-response) 또는 `createUIMessageStream()`에 전달하세요.
## 관련된 {/* #related */}

* [`workflowRoute()`](/reference/ai-sdk/workflow-route): 라이브 Workflow 실행을 스트리밍합니다.
* [`handleWorkflowStream()`](/reference/ai-sdk/handle-workflow-stream): 라이브 Workflow 스트리밍을 위한 프레임워크 독립적 핸들러
* [`toAISdkStream()`](/reference/ai-sdk/to-ai-sdk-stream): 라이브 Mastra 스트림을 AI SDK 형식으로 변환
* [`toAISdkMessages()`](/reference/ai-sdk/to-ai-sdk-messages): 저장된 Agent 메시지를 AI SDK 형식으로 변환
