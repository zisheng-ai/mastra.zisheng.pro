---
title: "참조: AcpAgent 클래스 | ACP"
description: "ACP 호환 코딩 Agent를 Mastra 하위 Agent로 래핑하는 AcpAgent 클래스에 대한 API 참조입니다."
packages:
  - "@mastra/acp"
  - "@mastra/core"
---

# AcpAgent 클래스

그만큼`AcpAgent`클래스는 ACP(Agent 클라이언트 프로토콜) 호환 코딩 Agent를 Mastra 하위 Agent로 래핑합니다. 상위 Mastra Agent가 저장소 검사 및 코드 편집을 위임해야 할 때 사용하세요. 또한 다른 ACP 지원 작업을 하위 Agent에 위임할 수도 있습니다.

대신 상위 Agent가 ACP Agent를 Tool로 호출하도록 하려면 다음을 사용하세요.[`createACPTool()`](/reference/acp/create-acp-tool).

## 사용예 {/* #usage-example */}

상위 Agent에 ACP 호환 코딩 Agent를 등록합니다.`agents` map:

```typescript title="src/mastra/agents/code-supervisor.ts"
import { AcpAgent } from '@mastra/acp'
import { Agent } from '@mastra/core/agent'

const codeAgent = new AcpAgent({
  id: 'code-agent',
  name: 'Code Agent',
  description: 'An ACP-compatible coding agent that can inspect and edit files',
  command: 'acp-agent',
  args: ['--stdio'],
  cwd: process.cwd(),
})

export const codeSupervisor = new Agent({
  id: 'code-supervisor',
  name: 'Code Supervisor',
  instructions: 'Delegate code editing tasks to the code-agent subagent.',
  model: '__GATEWAY_OPENAI_MODEL__',
  agents: {
    codeAgent,
  },
})
```

Claude Code의 경우 ACP 지원은`@agentclientprotocol/claude-agent-acp` bridge package. Configure the ACP agent command to run the bridge, then select a Claude model after session creation:

```typescript title="src/mastra/agents/claude-code-agent.ts"
import { AcpAgent } from '@mastra/acp'

export const claudeCodeAgent = new AcpAgent({
  id: 'claude-code-agent',
  name: 'Claude Code Agent',
  description: 'Use Claude Code through ACP.',
  command: 'npx',
  args: ['@agentclientprotocol/claude-agent-acp'],
  cwd: process.cwd(),
  model: '__AI_SDK_ANTHROPIC_MODEL_SONNET__',
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'string',
  description: 'Unique identifier for the subagent.',
},
{
  name: 'name',
  type: 'string',
  description: 'Display name used during agent delegation. Defaults to `id`.',
  isOptional: true,
},
{
  name: 'description',
  type: 'string',
  description: 'Description shown to the model when it can delegate to this subagent.',
},
{
  name: 'command',
  type: 'string',
  description: 'ACP agent executable to spawn.',
},
{
  name: 'args',
  type: 'string[]',
  description: 'Arguments passed to the ACP agent executable.',
  isOptional: true,
  defaultValue: '[]',
},
{
  name: 'env',
  type: 'Record<string, string>',
  description: 'Environment variables to merge with the current process environment when spawning the ACP process.',
  isOptional: true,
},
{
  name: 'cwd',
  type: 'string',
  description:
    'Working directory for the ACP process and ACP session. Also used as the default local filesystem base path.',
  isOptional: true,
  defaultValue: 'process.cwd()',
},
{
  name: 'session',
  type: 'Partial<NewSessionRequest>',
  description: 'ACP session creation options. Defaults to `cwd` or `process.cwd()` and an empty MCP server list.',
  isOptional: true,
},
{
  name: 'initialize',
  type: 'Partial<InitializeRequest>',
  description:
    'ACP initialization options. Defaults to Mastra client information, the current ACP protocol version, and read/write filesystem capabilities.',
  isOptional: true,
},
{
  name: 'authMethodId',
  type: 'string',
  description: 'ACP authentication method ID to invoke after initialization and before session creation.',
  isOptional: true,
},
{
  name: 'persistSession',
  type: 'boolean',
  description:
    'Whether to keep the ACP process and session alive after each prompt. Set to `false` to stop the process after each prompt completes.',
  isOptional: true,
  defaultValue: 'true',
},
{
  name: 'onPermissionRequest',
  type: '(request: RequestPermissionRequest) => Promise<RequestPermissionResponse>',
  description:
    'Callback invoked when the ACP agent requests permission. Defaults to selecting the first permission option, or cancelling when no option is available.',
  isOptional: true,
},
{
  name: 'createClient',
  type: '(defaultClient: Client) => Client',
  description:
    'Customize the ACP client used to answer agent requests. Receives the default client so it can be wrapped or extended, for example with `extMethod` and `extNotification` handlers. See [Extension methods](/reference/acp/create-acp-tool#extension-methods).',
  isOptional: true,
},
{
  name: 'workspace',
  type: 'Workspace',
  description:
    'Workspace used for ACP file read and write requests. Defaults to a `Workspace` backed by `LocalFilesystem` at `cwd` or `process.cwd()`.',
  isOptional: true,
},
{
  name: 'model',
  type: 'ModelId',
  description: 'Model ID to select after ACP session creation using the ACP `session/set_model` method.',
  isOptional: true,
},
]}
/>

## 속성 {/* #properties */}

<PropertiesTable
  content={[
{
  name: 'id',
  type: 'TId',
  description: 'Readonly subagent identifier from the constructor options.',
},
{
  name: 'name',
  type: 'string',
  description: 'Readonly display name for this subagent.',
},
{
  name: 'description',
  type: 'string',
  description: 'Readonly description shown when the parent agent can delegate to this subagent.',
},
{
  name: 'connection',
  type: 'ACPConnection',
  description:
    'Readonly ACP connection used to start the agent process, create sessions, send prompts, stream updates, and manage models.',
},
]}
/>

## 행동 양식 {/* #methods */}

### 세대 {/* #generation */}

#### `generate(messages, options?)` {/* #generatemessages-options */}

ACP Agent에 Prompt를 보내고, ACP 응답의 텍스트 청크를 버퍼링하고, Mastra 하위 Agent 생성 결과를 반환합니다.

```typescript
const result = await codeAgent.generate('Inspect the repository and summarize the test setup')

console.log(result.text)
```

#### `stream(messages, options?)` {/* #streammessages-options */}

ACP Agent에 Prompt를 보내고 Mastra 하위 Agent 스트림 결과를 반환합니다. ACP`agent_message_chunk` updates are emitted as Mastra `text-delta` chunks.

```typescript
const result = await codeAgent.stream('Refactor the selected module and explain each change')

for await (const chunk of result.fullStream) {
  if (chunk.type === 'text-delta') {
    process.stdout.write(chunk.payload.text)
  }
}
```

`resumeGenerate()`그리고`resumeStream()` aren't supported and throw an error when called.

### Model 관리 {/* #model-management */}

#### `getAvailableModels()` {/* #getavailablemodels */}

필요한 경우 ACP 프로세스를 시작하고 ACP 세션에서 광고한 Model 목록을 반환합니다.

```typescript
const models = await codeAgent.getAvailableModels()
// [{ modelId: '__AI_SDK_ANTHROPIC_MODEL_SONNET__', name: 'Claude Sonnet' }, ...]
```

#### `setModel(modelId)` {/* #setmodelmodelid */}

활성 ACP 세션에 대한 Model을 선택합니다. ACP Agent가 사용 가능한 Model을 광고하는 경우 Model ID는 해당 Model 중 하나와 일치해야 합니다.

```typescript
await codeAgent.setModel('__AI_SDK_ANTHROPIC_MODEL_SONNET__')
```

## 세션 수명주기 {/* #session-lifecycle */}

`AcpAgent`구성된 것을 시작합니다`command` on first use and initializes the ACP client. It then creates an ACP session. By default, `persistSession` is `true`, so the process and session stay alive across `generate()`, `stream()`, `getAvailableModels()`, and `setModel()` calls.

세트`persistSession: false` when each prompt should run in a fresh ACP process:

```typescript title="src/mastra/agents/code-agent.ts"
import { AcpAgent } from '@mastra/acp'

export const codeAgent = new AcpAgent({
  id: 'code-agent',
  description: 'Run one isolated ACP coding task',
  command: 'acp-agent',
  args: ['--stdio'],
  cwd: process.cwd(),
  persistSession: false,
})
```

와 함께`persistSession: false`, `@mastra/acp` stops the ACP process after each prompt completes.

## 작업공간 통합 {/* #workspace-integration */}

ACP 파일 작업은 Mastra를 통해 이루어집니다.`Workspace` abstraction. If you don't pass `workspace`, `@mastra/acp` creates a `Workspace` backed by `LocalFilesystem` and uses `cwd` or `process.cwd()` as the filesystem base path.

사용자 정의 전달`Workspace` when the ACP agent should read and write through a specific filesystem implementation:

```typescript title="src/mastra/agents/code-agent.ts"
import { AcpAgent } from '@mastra/acp'
import { LocalFilesystem, Workspace } from '@mastra/core/workspace'

const workspace = new Workspace({
  filesystem: new LocalFilesystem({
    basePath: process.cwd(),
  }),
})

export const codeAgent = new AcpAgent({
  id: 'code-agent',
  description: 'Run coding tasks in a controlled workspace',
  command: 'acp-agent',
  args: ['--stdio'],
  workspace,
})
```

사용`cwd` and `workspace` together when the ACP process should start in one directory but file operations should use an explicitly configured workspace root.

## 권한 처리 {/* #permission-handling */}

ACP Agent는 계속하기 전에 클라이언트에게 권한 옵션을 선택하도록 요청할 수 있습니다. 기본적으로`AcpAgent` selects the first option returned by the ACP agent, or cancels when no option is available.

통과하다`onPermissionRequest` to inspect the request and return your own permission response:

```typescript title="src/mastra/agents/code-agent.ts"
import { AcpAgent } from '@mastra/acp'

export const codeAgent = new AcpAgent({
  id: 'code-agent',
  description: 'Use an ACP-compatible coding agent',
  command: 'acp-agent',
  args: ['--stdio'],
  async onPermissionRequest(request) {
    const allowOption = request.options.find(option => option.name === 'Allow')

    if (!allowOption) {
      return { outcome: { outcome: 'cancelled' } }
    }

    return {
      outcome: {
        outcome: 'selected',
        optionId: allowOption.optionId,
      },
    }
  },
})
```

이 콜백을 사용하여 로컬 정책을 시행하거나 권한 제목을 검사하세요. 또한 결정을 자체 승인 흐름으로 라우팅할 수도 있습니다.

## 관련된 {/* #related */}

* [Agent 클라이언트 프로토콜 문서](/docs/agents/acp)
* [createACPTool() 참조](/reference/acp/create-acp-tool)
* [Agent 참조](/reference/agents/agent)
* [하위 Agent](/docs/capabilities/subagents)
* [Agent 클라이언트 프로토콜 소개](https://agentclientprotocol.com/overview/introduction)
* [Agent 클라이언트 프로토콜 스키마](https://agentclientprotocol.com/protocol/schema)
