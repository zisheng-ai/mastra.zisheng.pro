---
title: "참조: AgentBrowser 클래스 | 브라우저"
description: "접근성 트리 스냅샷 및 요소 참조를 사용하는 결정적 브라우저 자동화 공급자인 AgentBrowser에 대한 API 참조입니다."
packages:
  - "@mastra/agent-browser"
---

# AgentBrowser 클래스

그만큼`AgentBrowser`클래스는 다음을 사용하여 결정적 브라우저 자동화를 제공합니다.[Agent 브라우저](https://github.com/vercel-labs/agent-browser)도서관. 접근성 트리 스냅샷과 요소 참조(예:`@e5`) 정확하고 재현 가능한 상호 작용을 위해.

사용`AgentBrowser` when you need reliable, deterministic browser automation. For AI-powered interactions using natural language, see [`StagehandBrowser`](/reference/browser/stagehand-browser).

## 사용예 {/* #usage-example */}

```typescript title="src/mastra/agents/index.ts"
import { Agent } from '@mastra/core/agent'
import { AgentBrowser } from '@mastra/agent-browser'

const browser = new AgentBrowser({
  headless: true,
  viewport: { width: 1280, height: 720 },
  scope: 'thread',
})

export const browserAgent = new Agent({
  id: 'browser-agent',
  name: 'Browser Agent',
  instructions: `You can browse the web. Use browser_snapshot to see the page structure,
then interact with elements using their refs (e.g., @e5).`,
  model: '__GATEWAY_OPENAI_MODEL__',
  browser,
})
```

## 생성자 매개변수 {/* #constructor-parameters */}

<PropertiesTable
  content={[
{
  name: 'headless',
  type: 'boolean',
  description: 'Whether to run the browser in headless mode (no visible UI).',
  isOptional: true,
  defaultValue: 'true',
},
{
  name: 'viewport',
  type: "{ width: number; height: number } | 'window'",
  description: "Browser viewport dimensions, or 'window' to match the real browser window instead of a fixed size.",
  isOptional: true,
  defaultValue: '{ width: 1280, height: 720 }',
},
{
  name: 'timeout',
  type: 'number',
  description: 'Default timeout in milliseconds for browser operations.',
  isOptional: true,
  defaultValue: '30000',
},
{
  name: 'cdpUrl',
  type: 'string | (() => string | Promise<string>)',
  description: 'CDP WebSocket URL for connecting to an existing browser. Useful for cloud browser providers.',
  isOptional: true,
},
{
  name: 'scope',
  type: "'shared' | 'thread'",
  description:
    "Browser instance scope. 'shared' shares one browser across all threads. 'thread' gives each thread its own browser.",
  isOptional: true,
  defaultValue: "'thread' (or 'shared' when cdpUrl is provided)",
},
{
  name: 'onLaunch',
  type: '(args: { browser: MastraBrowser }) => void | Promise<void>',
  description: 'Callback invoked after the browser is ready.',
  isOptional: true,
},
{
  name: 'onClose',
  type: '(args: { browser: MastraBrowser }) => void | Promise<void>',
  description: 'Callback invoked before the browser closes.',
  isOptional: true,
},
{
  name: 'screencast',
  type: 'ScreencastOptions',
  description: 'Configuration for streaming browser frames to Studio.',
  isOptional: true,
},
{
  name: 'recording',
  type: 'BrowserRecordingOptions',
  description:
    'Alpha option for adding browser recording tools. Provide outputDir to add browser_record and browser_record_caption to the toolset. You can also set maxDurationMs, maxWidth, and maxHeight as defaults for every recording.',
  isOptional: true,
},
{
  name: 'excludeTools',
  type: 'BrowserToolName[]',
  description:
    'Tool names to exclude from the browser toolset. Use this to disable specific tools for models that do not support certain capabilities, such as vision.',
  isOptional: true,
},
]}
/>

## Tool {/* #tools */}

`AgentBrowser`브라우저 자동화를 위한 16가지 결정론적 Tool를 제공합니다. 요소와 상호 작용하는 모든 Tool는 접근성 트리 스냅샷의 참조를 사용합니다.

언제`recording` is configured, `AgentBrowser` also adds the alpha `browser_record` and `browser_record_caption` tools. See [Browser recording (alpha)](/docs/browser/recording).

### 핵심 Tool {/* #core-tools */}

| Tool | 설명 |
| - | - |
|`browser_goto` | Navigate to a URL |
| `browser_snapshot` | Get accessibility tree snapshot with element refs |
| `browser_click` | Click an element by ref |
| `browser_type` | Type text into an element |
| `browser_press` | Press keyboard keys |
| `browser_select` | Select option from dropdown |
| `browser_scroll` | Scroll the page or element |
| `browser_screenshot`| 스크린샷을 PNG로 캡처합니다(기본적으로 뷰포트, 설정`fullPage: true`전체 페이지의 경우) |
|`browser_close`| 브라우저 닫기 |

### 확장 Tool {/* #extended-tools */}

| Tool | 설명 |
| - | - |
|`browser_hover` | Hover over an element |
| `browser_back` | Go back in browser history |
| `browser_dialog` | Handle browser dialogs (alert, confirm, prompt) |
| `browser_wait` | Wait for element state changes |
| `browser_tabs` | Manage browser tabs (list, new, switch, close) |
| `browser_drag` | Drag and drop elements |
| `browser_evaluate` | Execute JavaScript in the page (escape hatch) |

특정 Tool를 제외하려면 다음을 통과하십시오.`excludeTools` in the constructor:

```typescript
const browser = new AgentBrowser({
  excludeTools: ['browser_screenshot'],
})
```

## Tool 참조 {/* #tool-reference */}

### `browser_goto` {/* #browser_goto */}

URL로 이동합니다.

```text
// Tool input
{
  "url": "https://example.com",
  "waitUntil": "domcontentloaded",
  "timeout": 30000
}
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`url` | `string` | URL to open |
| `waitUntil` | `"load" \| "domcontentloaded" \| "networkidle"` | When to consider navigation complete (optional) |
| `timeout` | `number` | Navigation timeout in ms (optional) |

### `browser_snapshot` {/* #browser_snapshot */}

페이지의 접근성 트리 스냅샷을 가져옵니다. 다음과 같은 요소 참조를 반환합니다.`@e5` that you use with other tools.

```text
// Tool input
{
  "interactiveOnly": true,
  "maxDepth": 10
}
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`interactiveOnly` | `boolean` | Only include interactive elements (optional) |
| `maxDepth` | `number` | Maximum tree depth (optional) |

**예제 출력:**

```text
[document] Example Page
  [banner]
    [link @e1] Home
    [link @e2] About
  [main]
    [heading @e3] Welcome
    [textbox @e4] Search...
    [button @e5] Submit
```

### `browser_click` {/* #browser_click */}

스냅샷의 참조를 사용하여 요소를 클릭합니다.

```text
{
  "ref": "@e5",
  "button": "left",
  "clickCount": 1,
  "modifiers": ["Control", "Shift"]
}
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`ref` | `string` | Element ref from snapshot (required) |
| `button` | `"left" \| "right" \| "middle"` | Mouse button (optional) |
| `clickCount` | `number` | Number of activations, 2 for double (optional) |
| `modifiers` | `string[]` | Modifier keys (optional) |

### `browser_type` {/* #browser_type */}

입력 요소에 텍스트를 입력합니다.

```text
// Tool input
{
  "ref": "@e4",
  "text": "search query",
  "clear": true,
  "delay": 50
}
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`ref` | `string` | Element ref from snapshot (required) |
| `text` | `string` | Text to type (required) |
| `clear` | `boolean` | Clear existing content first (optional) |
| `delay` | `number` | Delay between keystrokes in ms (optional) |

### `browser_press` {/* #browser_press */}

키보드 키를 누르세요.

```text
// Tool input
{
  "key": "Enter",
  "modifiers": ["Control"]
}

// Key combinations
{ "key": "Control+a" }
{ "key": "Control+c" }
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`key` | `string` | Key name (e.g., "Enter", "Tab", "Escape", "Control+a") (required) |
| `modifiers` | `string[]` | Modifier keys (optional) |

### `browser_select` {/* #browser_select */}

드롭다운에서 옵션을 선택하세요. 다음 중 하나를 제공하세요.`value`, `label`, or `index`.

```text
// Tool input - by value
{
  "ref": "@e10",
  "value": "option-value"
}

// Tool input - by label
{
  "ref": "@e10",
  "label": "Option Text"
}

// Tool input - by index
{
  "ref": "@e10",
  "index": 0
}
```

### `browser_scroll` {/* #browser_scroll */}

페이지 또는 특정 요소를 스크롤합니다.

```text
// Tool input
{
  "direction": "down",
  "amount": 300,
  "ref": "@e15"
}
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`direction` | `"up" \| "down" \| "left" \| "right"` | Scroll direction (required) |
| `amount` | `number` | Pixels to scroll, default 300 (optional) |
| `ref` | `string` | Element to scroll, scrolls page if omitted (optional) |

### `browser_hover` {/* #browser_hover */}

요소 위로 마우스를 가져가면 호버 효과가 실행됩니다.

```text
// Tool input
{
  "ref": "@e7"
}
```

### `browser_back` {/* #browser_back */}

브라우저 기록으로 돌아갑니다.

```text
// Tool input (no parameters required)
{}
```

### `browser_dialog` {/* #browser_dialog */}

브라우저 대화 상자(경고, 확인, Prompt)를 처리합니다. 대화 상자를 트리거하는 요소를 클릭하고 처리합니다.

```text
// Tool input
{
  "triggerRef": "@e5",
  "action": "accept",
  "text": "response"
}
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`triggerRef` | `string` | Element that triggers the dialog (required) |
| `action` | `"accept" \| "dismiss"` | How to handle the dialog (required) |
| `text` | `string` | Text for prompt dialogs (optional) |

### `browser_wait` {/* #browser_wait */}

요소가 특정 상태에 도달할 때까지 기다립니다.

```text
// Tool input
{
  "ref": "@e20",
  "state": "visible",
  "timeout": 30000
}
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`ref` | `string` | Element ref to wait for (optional) |
| `state` | `"visible" \| "hidden" \| "attached" \| "detached"` | State to wait for (optional) |
| `timeout` | `number` | Max wait time in ms (optional) |

### `browser_tabs` {/* #browser_tabs */}

브라우저 탭을 관리합니다.

```text
// List all tabs
{ "action": "list" }

// Open new tab
{ "action": "new", "url": "https://example.com" }

// Switch to tab by index
{ "action": "switch", "index": 0 }

// Close tab by index
{ "action": "close", "index": 1 }
```

### `browser_drag` {/* #browser_drag */}

요소를 대상 위치로 드래그합니다.

```text
// Tool input
{
  "sourceRef": "@e10",
  "targetRef": "@e20"
}
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`sourceRef` | `string` | Element to drag (required) |
| `targetRef` | `string` | Drop target element (required) |

### `browser_evaluate` {/* #browser_evaluate */}

페이지 컨텍스트에서 JavaScript를 실행합니다. 다른 Tool로 사용 사례를 다룰 수 없을 때 탈출용 해치로 사용하세요.

```text
// Tool input
{
  "script": "document.title",
  "returnValue": true
}
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`script` | `string` | JavaScript to execute (required) |
| `returnValue` | `boolean` | Whether to return the result (optional) |

### `browser_screenshot` {/* #browser_screenshot */}

현재 페이지의 스크린샷을 PNG(기본적으로 뷰포트)로 캡처합니다.`fullPage: true` for full-page capture). Returns image content that vision-capable models can interpret directly. Use `browser_snapshot` when you only need text or structured data.

```text
// Viewport only (default)
{}

// Full scrollable page
{ "fullPage": true }
```

| 매개변수 | 유형 | 설명 |
| - | - | - |
|`fullPage` | `boolean` | Capture the full scrollable page instead of only the viewport (optional, default: false) |

### `browser_close` {/* #browser_close */}

브라우저를 닫고 리소스를 정리하세요.

```text
// Tool input (no parameters required)
{}
```

## 심판의 작동 방식 {/* #how-refs-work */}

그만큼`browser_snapshot` tool returns an accessibility tree with element refs like `@e1`, `@e2`, etc. These refs are stable identifiers you use with other tools:

1. 부르다`browser_snapshot` to see the page structure
2. 상호작용하려는 요소를 찾으세요.
3. 다음과 같은 상호작용 Tool와 함께 해당 참조를 사용하세요.`browser_type` or `browser_scroll`.

```text
// 1. Get snapshot
// Returns: [textbox @e4] Search... [link @e5] Home

// 2. Type in the search box
{ "tool": "browser_type", "input": { "ref": "@e4", "text": "mastra" } }

// 3. Navigate to home
{ "tool": "browser_goto", "input": { "url": "https://example.com" } }
```

## 관련된 {/* #related */}

* [마스트라브라우저](/reference/browser/mastra-browser): 기본 클래스 참조
* [Stagehand브라우저](/reference/browser/stagehand-browser): AI 기반 대안
* [브라우저 개요](/docs/browser/overview): 개념 가이드
* [Agent 브라우저 가이드](/docs/browser/agent-browser): 이용안내
