---
title: "API 참조: Mastra 플랫폼"
description: "Mastra 플랫폼 REST API에 대한 API 참조입니다."
packages:
  - "mastra"
---

# API 참조

## 회원 API {/* #members-api */}

아래 엔드포인트의 루트 URL은 다음과 같습니다.`/v1/org/members`

| 방법 | 엔드포인트 | 설명 |
| - | - | - |
| 받기 |`/` | List members |
| POST | `/invitations`| 초대장 보내기 |
| 받기 |`/invitations`| 초대장 나열 |
| 삭제 |`/invitations/:id`| 초대 취소 |
| 패치 |`/:id`| 회원 역할 업데이트 |
| 삭제 |`/:id`| 회원 삭제 |

## API 토큰 {/* #api-tokens */}

아래 엔드포인트의 루트 URL은 다음과 같습니다.`/v1/auth/tokens`

| 방법 | 엔드포인트 | 설명 |
| - | - | - |
| 포스트 |`/` | Create an API token |
| GET | `/` | List API tokens |
| DELETE | `/:id` | Delete an API token |

## 스튜디오 API {/* #studio-api */}

아래 엔드포인트의 루트 URL은 다음과 같습니다.`/v1/studio`

| 방법 | 엔드포인트 | 설명 |
| - | - | - |
| 받기 |`/projects` | List studio projects |
| POST | `/projects` | Create a project |
| GET | `/projects/:id` | Get project detail and deploys |
| DELETE | `/projects/:id` | Delete a project |
| POST | `/deploys` | Create a deploy |
| POST | `/deploys/:id/upload-complete` | Confirm artifact upload |
| GET | `/deploys/:id` | Get deploy status |
| GET | `/deploys/:id/logs` | Get deploy logs |
| GET | `/deploys/:id/logs/stream` | Stream deploy logs (SSE) |

## 서버 API {/* #server-api */}

아래 엔드포인트의 루트 URL은 다음과 같습니다.`/v1/server`

| 방법 | 엔드포인트 | 설명 |
| - | - | - |
| 받기 |`/projects` | List server projects |
| POST | `/projects` | Create a project |
| GET | `/projects/:id` | Get project detail |
| DELETE | `/projects/:id` | Delete a project |
| PUT | `/projects/:id/env` | Set environment variables |
| GET | `/projects/:id/env` | Get environment variables |
| POST | `/projects/:id/env/restart` | Restart after env change |
| GET | `/projects/:id/domains` | List domains |
| POST | `/projects/:id/domains` | Add a custom domain |
| GET | `/projects/:id/runtime-logs` | Get runtime logs |
| POST | `/projects/:id/redeploy`| 재배포 트리거 |
| 포스트 |`/projects/:id/always-on`| 상시 사용 설정 |
| 삭제 |`/projects/:id/always-on`| 상시 사용 중지 |
| 포스트 |`/deploys`| 배포 만들기 |
| 포스트 |`/deploys/:id/upload-complete`| 아티팩트 업로드 확인 |
| 받기 |`/deploys/:id`| 배포 상태 가져오기 |
| 받기 |`/deploys/:id/logs`| 배포 로그 가져오기 |
| 포스트 |`/deploys/:id/cancel`| 대기 중이거나 진행 중인 배포 취소 |
| 삭제 |`/deploys/:id`| 실행 중인 배포 중지 |

## 게이트웨이 API {/* #gateway-api */}

아래 엔드포인트의 루트 URL은 다음과 같습니다.`/v1/gateway`

| 방법 | 엔드포인트 | 설명 |
| - | - | - |
| 받기 |`/projects` | List gateway projects |
| GET | `/projects/:id` | Get project detail |
| PATCH | `/projects/:id` | Update project settings |
| DELETE | `/projects/:id` | Delete a project |
| POST | `/projects/provision` | Provision a new gateway project |
| GET | `/projects/:id/keys` | List API keys |
| POST | `/projects/:id/keys` | Create an API key |
| DELETE | `/projects/:id/keys/:keyId` | Revoke an API key |
| GET | `/projects/:id/keys/:keyId/bindings` | List key-level provider bindings |
| POST | `/projects/:id/keys/:keyId/bindings` | Add a key-level provider binding (BYOK) |
| DELETE | `/projects/:id/keys/:keyId/bindings/:provider` | Remove a key-level binding |
| GET | `/projects/:id/bindings` | List project-level provider bindings |
| POST | `/projects/:id/bindings` | Add a project-level provider binding (BYOK) |
| DELETE | `/projects/:id/bindings/:provider` | Remove a project-level binding |
| GET | `/projects/:id/usage/has-activity` | Check whether project has any requests |
| GET | `/projects/:id/usage/activity` | Get aggregated usage activity |
| GET | `/projects/:id/usage/memory-activity` | Get memory-token totals over a time range |
| GET | `/projects/:id/usage/logs` | Get request logs |
| GET | `/projects/:id/usage/logs/facets` | Get log filter facets |
| GET | `/projects/:id/usage/logs/:requestId` | Get specific request detail |
| GET | `/projects/:id/memory/threads`| 스레드 나열(대시보드) |
| 포스트 |`/projects/:id/memory/threads`| 스레드 만들기(대시보드) |
| 받기 |`/projects/:id/memory/threads/:threadId`| 스레드 가져오기(대시보드) |
| 패치 |`/projects/:id/memory/threads/:threadId`| 업데이트 스레드(대시보드) |
| 삭제 |`/projects/:id/memory/threads/:threadId`| 스레드 삭제(대시보드) |
| 포스트 |`/projects/:id/memory/threads/:threadId/clone`| 스레드 복제(대시보드) |
| 받기 |`/projects/:id/memory/threads/:threadId/messages`| 메시지 나열(대시보드) |
| 포스트 |`/projects/:id/memory/threads/:threadId/messages`| 메시지 저장(대시보드) |
| 삭제 |`/projects/:id/memory/threads/:threadId/messages`| 메시지 삭제(대시보드) |
| 받기 |`/projects/:id/memory/threads/:threadId/observations/history`| 관찰 이력(대시보드) |
| 받기 |`/models`| 사용 가능한 Model 목록 |

## 게이트웨이 프록시 엔드포인트 {/* #gateway-proxy-endpoints */}

방문[Gateway documentation](https://gateway.mastra.ai/docs) for more details.
