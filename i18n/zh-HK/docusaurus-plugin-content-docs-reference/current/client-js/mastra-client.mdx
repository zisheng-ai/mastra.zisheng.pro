---
title: "參考：Mastra client SDK | Client SDK"
description: 了解如何使用 client-js SDK 與 Mastra 互動。
packages:
  - "@mastra/client-js"
---

# Mastra client SDK

Mastra Client SDK 提供型別安全的介面，讓你的客戶端環境可與 [Mastra Server](/docs/deployment/mastra-server) 互動。

## 使用範例

```typescript title="lib/mastra/mastra-client.ts"
import { MastraClient } from '@mastra/client-js'

export const mastraClient = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})
```

## `RequestContext`

在客戶端 SDK 使用 `RequestContext` 時，請從 `@mastra/client-js` 匯入。

```typescript
import { MastraClient, RequestContext } from '@mastra/client-js'

const client = new MastraClient({
  baseUrl: 'http://localhost:4111/',
})

const requestContext = new RequestContext()
requestContext.set('userId', 'user-123')

const agent = client.getAgent('support-agent')

const response = await agent.generate('Summarize this ticket', {
  requestContext,
})
```

你亦可將 `requestContext` 以 `Record<string, any>` 傳入。

## 參數

<PropertiesTable
  content={[
  {
    name: 'baseUrl',
    type: 'string',
    description: 'Mastra API 的基礎 URL。所有請求均會相對於此 URL 傳送。',
    isOptional: false,
  },
  {
    name: 'retries',
    type: 'number',
    description: '請求失敗後，在擲回錯誤前重試的次數。',
    isOptional: true,
    defaultValue: '3',
  },
  {
    name: 'backoffMs',
    type: 'number',
    description:
      '重試失敗請求前的初始延遲（毫秒）。每次重試都會將此值加倍（指數退避）。',
    isOptional: true,
    defaultValue: '300',
  },
  {
    name: 'maxBackoffMs',
    type: 'number',
    description: '最長退避時間（毫秒），防止兩次重試之間等候過久。',
    isOptional: true,
    defaultValue: '5000',
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: '包含每次請求都要加入的自訂 HTTP 標頭之物件。',
    isOptional: true,
  },
  {
    name: 'credentials',
    type: '"omit" | "same-origin" | "include"',
    description:
      '請求的憑證模式。詳情請參閱 https://developer.mozilla.org/en-US/docs/Web/API/Request/credentials。',
    isOptional: true,
  },
]}
/>

## 方法

<PropertiesTable
  content={[
  {
    name: 'listAgents()',
    type: 'Promise<Record<string, GetAgentResponse>>',
    description: '傳回所有可用的 Agent 實例。',
  },
  {
    name: 'getAgent(agentId)',
    type: 'Agent',
    description: '按 ID 取得指定 Agent 實例。',
  },
  {
    name: 'listMemoryThreads(params)',
    type: 'Promise<StorageThreadType[]>',
    description:
      '取得指定資源及 Agent 的記憶執行緒。必須提供 `resourceId` 及 `agentId`。',
  },
  {
    name: 'createMemoryThread(params)',
    type: 'Promise<MemoryThread>',
    description: '使用指定參數建立新的記憶執行緒。',
  },
  {
    name: 'getMemoryThread({ threadId, agentId })',
    type: 'MemoryThread',
    description: '按 ID 取得指定記憶執行緒。',
  },
  {
    name: 'saveMessageToMemory(params)',
    type: 'Promise<{ messages: (MastraMessageV1 | MastraDBMessage)[] }>',
    description: '將一則或多則訊息儲存至記憶系統，並傳回已儲存的訊息。',
  },
  {
    name: 'getMemoryStatus()',
    type: 'Promise<MemoryStatus>',
    description: '傳回記憶系統目前的狀態。',
  },
  {
    name: 'listTools()',
    type: 'Record<string, Tool>',
    description: '傳回所有可用的 Tool。',
  },
  {
    name: 'getTool(toolId)',
    type: 'Tool',
    description: '按 ID 取得指定 Tool 實例。',
  },
  {
    name: 'listWorkflows()',
    type: 'Record<string, Workflow>',
    description: '傳回所有可用的 Workflow 實例。',
  },
  {
    name: 'getWorkflow(workflowId)',
    type: 'Workflow',
    description: '按 ID 取得指定 Workflow 實例。',
  },
  {
    name: 'getAgentBuilderActions()',
    type: 'Promise<Record<string, WorkflowInfo>>',
    description:
      '傳回所有可用的 Agent Builder 動作。請參閱 [Agent Builder API](https://agent-builder.mastra.ai/reference/client)。',
  },
  {
    name: 'getAgentBuilderAction(actionId)',
    type: 'AgentBuilder',
    description:
      '按 ID 取得 Agent Builder 動作。請參閱 [Agent Builder API](https://agent-builder.mastra.ai/reference/client)。',
  },
  {
    name: 'responses',
    type: 'Responses',
    description:
      '提供 OpenAI 形式的 Responses API 輔助函數，包括 `create()`、`retrieve()`、`stream()` 及 `delete()`。',
  },
  {
    name: 'conversations',
    type: 'Conversations',
    description: '提供對話輔助函數，包括 `create()`、`retrieve()`、`delete()` 及 `items.list()`。',
  },
  {
    name: 'getVector(vectorName)',
    type: 'MastraVector',
    description: '按名稱傳回向量儲存庫實例。',
  },
  {
    name: 'listLogs(params)',
    type: 'Promise<LogEntry[]>',
    description: '取得符合指定篩選條件的系統日誌。',
  },
  {
    name: 'getLog(params)',
    type: 'Promise<LogEntry>',
    description: '按 ID 或篩選條件取得指定日誌項目。',
  },
  {
    name: 'listLogTransports()',
    type: 'string[]',
    description: '傳回已設定的日誌傳輸類型清單。',
  },
  {
    name: 'getTrace(traceId)',
    type: 'Promise<TraceRecord>',
    description: '按 ID 取得指定 Trace，包括其所有 span 及詳情。',
  },
  {
    name: 'getTraces(params)',
    type: 'Promise<GetTracesResponse>',
    description:
      '取得可選擇篩選的 Trace 根 span 分頁清單。使用 `getTrace()` 取得包含所有 span 的完整 Trace。',
  },
]}
/>
