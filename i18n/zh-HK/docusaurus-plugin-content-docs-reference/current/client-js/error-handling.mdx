---
title: "參考：錯誤處理 | Client SDK"
description: 了解 Mastra client-js SDK 內置的重試機制及錯誤處理功能。
packages:
  - "@mastra/client-js"
---

# 錯誤處理 {/* #error-handling */}

Mastra Client SDK 內置重試機制及錯誤處理功能。

## 錯誤處理 {/* #error-handling-1 */}

所有 API 方法都可能擲回錯誤，你可以捕捉並處理：

```typescript
try {
  const agent = mastraClient.getAgent('agent-id')
  const response = await agent.generate('Hello')
} catch (error) {
  console.error('An error occurred:', error.message)
}
```
