---
title: "參考：mastra.schedules | 排程"
description: "mastra.schedules 的 API 參考；這項服務用於建立及管理 Agent 和 Workflow 的 cron 排程。"
packages:
  - "@mastra/core"
---

# `mastra.schedules`

**新增於：** `@mastra/core@1.50.0`

`mastra.schedules` 是用於持久保存 cron 排程的 CRUD 服務。你可以用它為 Agent 或 Workflow 建立、列出、更新、暫停、恢復、手動執行及刪除排程。

有關使用模式和概念，請參閱[排程](/docs/long-running-agents/schedules)。

## 使用範例

建立 Agent 排程：

```typescript title="src/mastra/schedules.ts"
const schedule = await mastra.schedules.create({
  agentId: 'pinger',
  cron: '0 * * * *',
  prompt: 'Give me a status update.',
})
```

建立 Workflow 排程：

```typescript title="src/mastra/schedules.ts"
const schedule = await mastra.schedules.create({
  workflowId: 'daily-report',
  cron: '0 9 * * *',
  inputData: { reportType: 'summary' },
})
```

排程需要實作 schedules domain 的儲存適配器。支援的適配器包括 `@mastra/libsql`、`@mastra/pg`、`@mastra/mysql`、`@mastra/mongodb`、`@mastra/convex` 及 `@mastra/spanner`。

## 方法

### 建立排程

#### `create(input)`

建立 Agent 或 Workflow 排程。傳入 `agentId` 以建立 Agent 排程，或傳入 `workflowId` 以建立 Workflow 排程。

```typescript
const schedule = await mastra.schedules.create({
  agentId: 'pinger',
  cron: '0 * * * *',
  prompt: 'Give me a status update.',
})
```

##### Agent 排程輸入

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description:
      '可選的固定排程 ID。值會標準化為 `agent_<slug>`。如省略，Mastra 會產生 `agent_<uuid>` ID。',
    isOptional: true,
  },
  {
    name: 'agentId',
    type: 'string',
    description: '每次觸發排程時要執行的 Agent ID。',
  },
  {
    name: 'cron',
    type: 'string',
    description: '排程的 cron 表達式。接受由 5、6 或 7 部分組成的 cron 表達式及 Croner 暱稱。',
  },
  {
    name: 'prompt',
    type: 'string',
    description: '每次觸發排程時傳送給 Agent 的提示詞。',
  },
  {
    name: 'name',
    type: 'string',
    description: '自由格式標籤，用於區分同一 Agent 或 thread 上的多個排程。',
    isOptional: true,
  },
  {
    name: 'timezone',
    type: 'string',
    description: '用於計算 cron 觸發時間的 IANA 時區，例如 `America/New_York`。',
    isOptional: true,
  },
  {
    name: 'threadId',
    type: 'string',
    description:
      '接收排程 signal 的 thread。如省略，每次觸發都不使用 thread，並透過 `agent.generate()` 執行。',
    isOptional: true,
  },
  {
    name: 'resourceId',
    type: 'string',
    description: '有 thread 排程的資源 ID。設定 `threadId` 時必須提供。',
    isOptional: true,
  },
  {
    name: 'signalType',
    type: 'AgentSignalType',
    description: '有 thread 排程觸發時的 signal 類型。預設為 `notification`。',
    isOptional: true,
  },
  {
    name: 'tagName',
    type: 'string',
    description: '用於呈現排程 signal 的 XML 標籤名稱。預設為 `schedule`。',
    isOptional: true,
  },
  {
    name: 'attributes',
    type: 'AgentSignalAttributes',
    description: '在排程 signal 的 XML 標籤上呈現的屬性。',
    isOptional: true,
  },
  {
    name: 'providerOptions',
    type: 'Record<string, unknown>',
    description: '每次觸發時合併至排程 signal payload、可安全用於 JSON 的 Provider 選項。',
    isOptional: true,
  },
  {
    name: 'ifActive',
    type: 'ScheduleIfActive',
    description: '目標 thread 正在串流時的行為。需要 `threadId`。',
    isOptional: true,
  },
  {
    name: 'ifIdle',
    type: 'ScheduleIfIdle',
    description: '目標 thread 閒置時的行為。需要 `threadId`。',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: '與排程記錄一同儲存的任意 metadata。',
    isOptional: true,
  },
  {
    name: 'status',
    type: "'active' | 'paused'",
    description: '初始生命週期狀態。預設為 `active`。',
    isOptional: true,
    defaultValue: "'active'",
  },
]}
/>

##### Workflow 排程輸入

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description:
      '可選的固定排程 ID。值會標準化為 `schedule_<slug>`。如省略，Mastra 會產生 `schedule_<uuid>` ID。',
    isOptional: true,
  },
  {
    name: 'workflowId',
    type: 'string',
    description: '每次觸發排程時要啟動的 Workflow ID。',
  },
  {
    name: 'cron',
    type: 'string',
    description: '排程的 cron 表達式。接受由 5、6 或 7 部分組成的 cron 表達式及 Croner 暱稱。',
  },
  {
    name: 'timezone',
    type: 'string',
    description: '用於計算 cron 觸發時間的 IANA 時區。',
    isOptional: true,
  },
  {
    name: 'inputData',
    type: 'unknown',
    description: '傳入 Workflow 執行的輸入資料。',
    isOptional: true,
  },
  {
    name: 'initialState',
    type: 'unknown',
    description: '排程執行時 Workflow 的初始狀態。',
    isOptional: true,
  },
  {
    name: 'requestContext',
    type: 'Record<string, unknown>',
    description: '傳入 Workflow 執行的請求 context。',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: '與排程記錄一同儲存的任意 metadata。',
    isOptional: true,
  },
  {
    name: 'status',
    type: "'active' | 'paused'",
    description: '初始生命週期狀態。預設為 `active`。',
    isOptional: true,
    defaultValue: "'active'",
  },
]}
/>

### 讀取排程

#### `get(id)`

按 ID 取得排程。未加前綴的 Agent 排程 ID 亦會解析為標準化的 `agent_<slug>` 格式。

```typescript
const schedule = await mastra.schedules.get('pinger')
```

#### `list(filter?)`

列出排程。如未指定篩選條件，會傳回 Agent 和 Workflow 排程。

```typescript
const schedules = await mastra.schedules.list({
  agentId: 'pinger',
  status: 'active',
})
```

<PropertiesTable
  content={[
  {
    name: 'filter',
    type: 'ListSchedulesFilter',
    description: '列表操作的可選篩選條件。',
    isOptional: true,
    properties: [
      {
        type: 'ListSchedulesFilter',
        parameters: [
          {
            name: 'agentId',
            type: 'string',
            description: '只傳回此 Agent 的 Agent 排程。',
            isOptional: true,
          },
          {
            name: 'workflowId',
            type: 'string',
            description: '只傳回此 Workflow 的 Workflow 排程。',
            isOptional: true,
          },
          {
            name: 'threadId',
            type: 'string',
            description: '只傳回此 thread 的 Agent 排程。',
            isOptional: true,
          },
          {
            name: 'resourceId',
            type: 'string',
            description: '只傳回此資源的 Agent 排程。',
            isOptional: true,
          },
          {
            name: 'name',
            type: 'string',
            description: '只傳回附有此標籤的 Agent 排程。',
            isOptional: true,
          },
          {
            name: 'status',
            type: "'active' | 'paused'",
            description: '只傳回具有此狀態的排程。',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

### 更新排程

#### `update(id, patch)`

更新排程。變更 `cron` 或 `timezone` 會重新計算下次觸發時間。將 `status` 由 `paused` 更新為 `active` 亦會重新計算下次觸發時間。

```typescript
const updated = await mastra.schedules.update('pinger', {
  cron: '*/30 * * * *',
  prompt: 'Give me a status update every 30 minutes.',
})
```

Agent 排程 patch 可以更新 `cron`、`timezone`、`prompt`、`name`、`signalType`、`tagName`、`attributes`、`providerOptions`、`ifActive`、`ifIdle`、`metadata` 和 `status`。`threadId` 和 `resourceId` 不可透過 patch 更新。如需變更目標 thread，請建立新排程。

Workflow 排程 patch 可以更新 `cron`、`timezone`、`inputData`、`initialState`、`requestContext`、`metadata` 和 `status`。在 Workflow 排程使用 `prompt`、`signalType` 和 `ifIdle` 等只適用於 Agent 的 patch 欄位會拋出錯誤。

### 生命週期

#### `pause(id)`

暫停排程。暫停操作會持久保存，而且具備冪等性。

```typescript
const paused = await mastra.schedules.pause('pinger')
```

#### `resume(id)`

恢復已暫停的排程，並由目前時間起重新計算下次觸發時間。

```typescript
const active = await mastra.schedules.resume('pinger')
```

#### `run(id)`

立即觸發排程一次，而不變更其 cron 執行週期。

```typescript
const run = await mastra.schedules.run('pinger')
```

對於 Agent 排程，`claimId` 使用 `manual_<scheduleId>_<timestamp>`。對於 Workflow 排程，`claimId` 使用 `sched_<scheduleId>_<timestamp>`，並會重用為 Workflow 執行 ID。

#### `delete(id)`

刪除排程。刪除不存在的排程時不會執行任何操作。

```typescript
await mastra.schedules.delete('pinger')
```

## 排程行為

- Agent 排程 ID 使用 `agent_` 前綴。透過 `mastra.schedules.create()` 建立的 Workflow 排程 ID 使用 `schedule_` 前綴。
- 設定 `threadId` 時，有 thread 的 Agent 排程必須提供 `resourceId`。
- `signalType`、`ifActive`、`ifIdle` 和 `resourceId` 需要 `threadId`。
- Workflow 排程不接受 `prompt`、`signalType` 或 `ifIdle` 等只適用於 Agent 的 patch 欄位。
- `run()` 會立即發佈一次手動觸發，且不會變更已儲存的 cron 執行週期。
