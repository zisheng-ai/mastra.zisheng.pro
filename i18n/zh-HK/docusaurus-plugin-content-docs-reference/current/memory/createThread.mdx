---
title: "參考：Memory.createThread() | Memory"
description: "Mastra `Memory.createThread()` 方法的文件；此方法在 memory 系統中建立新的對話 thread。"
packages:
  - "@mastra/memory"
---

# Memory.createThread()

`.createThread()` 方法在 memory 系統中建立新的對話 thread。每個 thread 代表一段獨立的對話或情境，並可包含多則訊息。

## 使用範例

```typescript
await memory?.createThread({ resourceId: 'user-123' })
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'resourceId',
    type: 'string',
    description: '此 thread 所屬資源的標識符（例如使用者 ID、項目 ID）',
    isOptional: false,
  },
  {
    name: 'threadId',
    type: 'string',
    description: 'thread 的自訂 ID（選填）。如未提供，系統會產生一個 ID。',
    isOptional: true,
  },
  {
    name: 'title',
    type: 'string',
    description: 'thread 的標題（選填）',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: '要與 thread 關聯的 metadata（選填）',
    isOptional: true,
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '已建立 thread 的唯一標識符',
  },
  {
    name: 'resourceId',
    type: 'string',
    description: '與 thread 關聯的資源 ID',
  },
  {
    name: 'title',
    type: 'string',
    description: 'thread 的標題（如有提供）',
  },
  {
    name: 'createdAt',
    type: 'Date',
    description: 'thread 的建立時間',
  },
  {
    name: 'updatedAt',
    type: 'Date',
    description: 'thread 上次更新的時間',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: '與 thread 關聯的其他 metadata',
  },
]}
/>

## 延伸使用範例

```typescript title="src/test-memory.ts"
import { mastra } from './mastra'

const agent = mastra.getAgent('agent')
const memory = await agent.getMemory()

const thread = await memory?.createThread({
  resourceId: 'user-123',
  title: 'Memory Test Thread',
  metadata: {
    source: 'test-script',
    purpose: 'memory-testing',
  },
})

const response = await agent.generate('message for agent', {
  memory: {
    thread: thread!.id,
    resource: thread!.resourceId,
  },
})

console.log(response.text)
```

### 相關內容

- [Memory class 參考](/reference/memory/memory-class)
- [Memory 入門](/docs/memory/overview)（涵蓋 thread 概念）
- [getThreadById](/reference/memory/getThreadById)
- [listThreads](/reference/memory/listThreads)
- [recall](/reference/memory/recall)
