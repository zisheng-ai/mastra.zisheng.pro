---
title: "參考：Memory.deleteMessages() | Memory"
description: "Mastra `Memory.deleteMessages()` 方法的文件；此方法按 ID 刪除多則訊息。"
packages:
  - "@mastra/core"
---

# Memory.deleteMessages()

`.deleteMessages()` 方法按 ID 刪除多則訊息。

## 使用範例 {/* #usage-example */}

```typescript
await memory?.deleteMessages(['671ae63f-3a91-4082-a907-fe7de78e10ec'])
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messageIds',
    type: 'string[]',
    description: '要刪除的訊息 ID 陣列',
    isOptional: false,
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'void',
    type: 'Promise<void>',
    description: '所有訊息刪除後便會解析的 Promise',
  },
]}
/>

## 延伸使用範例 {/* #extended-usage-example */}

```typescript title="src/test-memory.ts"
import { mastra } from './mastra'
import { MastraDBMessage } from '@mastra/core'

const agent = mastra.getAgent('agent')
const memory = await agent.getMemory()

const { messages } = await memory!.recall({ threadId: 'thread-123' })

const messageIds = messages.map((message: MastraDBMessage) => message.id)
await memory?.deleteMessages([...messageIds])
```

## 相關內容 {/* #related */}

- [Memory class 參考](/reference/memory/memory-class)
- [recall](/reference/memory/recall)
- [Memory 入門](/docs/memory/overview)
