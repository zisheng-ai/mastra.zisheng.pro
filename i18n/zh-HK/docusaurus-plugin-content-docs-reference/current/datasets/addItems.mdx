---
title: "參考：dataset.addItems() | 資料集"
description: "以下是 `dataset.addItems()` 方法的文檔；此方法會批量將多個項目加入資料集。"
packages:
  - "@mastra/core"
---

# dataset.addItems()

**新增於：** `@mastra/core@1.4.0`

透過單次批量操作將多個項目加入資料集。

## 使用範例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const items = await dataset.addItems({
  items: [
    { input: { question: 'What is AI?' }, groundTruth: { answer: 'Artificial Intelligence' } },
    { input: { question: 'What is ML?' }, groundTruth: { answer: 'Machine Learning' } },
    { input: { question: 'What is DL?' }, metadata: { category: 'deep-learning' } },
  ],
})

console.log(`Added ${items.length} items`)
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'items',
    type: 'Array<object>',
    description: '要加入的項目陣列。每個項目的結構與 dataset.addItem() 的輸入相同。',
    properties: [
      {
        type: 'DatasetItem',
        parameters: [
          {
            name: 'input',
            type: 'unknown',
            description: '項目的輸入資料。',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: '預期輸出或基準答案。',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: '任意元資料。',
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description: '已建立資料集項目的陣列。 請參閱 dataset.addItem() 了解項目結構。',
  },
]}
/>
