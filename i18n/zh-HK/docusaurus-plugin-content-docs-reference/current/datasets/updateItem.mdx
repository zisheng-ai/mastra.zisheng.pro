---
title: "參考：dataset.updateItem() | 資料集"
description: "以下是 `dataset.updateItem()` 方法的文檔；此方法會更新資料集中的現有項目。"
packages:
  - "@mastra/core"
---

# dataset.updateItem() {/* #datasetupdateitem */}

**新增於：** `@mastra/core@1.4.0`

更新資料集中的現有項目。 只會更新所提供的欄位。更新項目會建立新版本。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const updated = await dataset.updateItem({
  itemId: 'item-id',
  input: { question: 'What is TypeScript?' },
  groundTruth: { answer: 'A typed superset of JavaScript' },
  metadata: { reviewed: true },
})
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: '要更新的項目 ID。',
  },
  {
    name: 'input',
    type: 'unknown',
    isOptional: true,
    description: '已更新的輸入資料。',
  },
  {
    name: 'groundTruth',
    type: 'unknown',
    isOptional: true,
    description: '已更新的基準答案。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '已更新的元資料。',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem>',
    description: '已更新的資料集項目。 請參閱 dataset.addItem() 了解項目結構。',
  },
]}
/>
