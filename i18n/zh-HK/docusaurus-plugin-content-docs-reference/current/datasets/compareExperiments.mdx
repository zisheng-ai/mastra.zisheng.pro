---
title: "參考：DatasetsManager.compareExperiments() | 資料集"
description: "以下是 `DatasetsManager.compareExperiments()` 方法的文檔；此方法會比較兩個或以上的實驗。"
packages:
  - "@mastra/core"
---

# DatasetsManager.compareExperiments()

**新增於：** `@mastra/core@1.4.0`

比較兩個或以上的實驗，並產生每個項目及每個評分器的比較結果。必須提供至少兩個實驗 ID。

## 使用範例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const comparison = await mastra.datasets.compareExperiments({
  experimentIds: ['exp-baseline', 'exp-new'],
  baselineId: 'exp-baseline',
})

console.log(`Baseline: ${comparison.baselineId}`)

for (const item of comparison.items) {
  console.log(`Item ${item.itemId}:`)
  console.log(`  Input: ${JSON.stringify(item.input)}`)

  for (const [expId, result] of Object.entries(item.results)) {
    if (result) {
      console.log(
        `  ${expId}: output=${JSON.stringify(result.output)}, scores=${JSON.stringify(result.scores)}`,
      )
    }
  }
}
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'experimentIds',
    type: 'string[]',
    description: '要比較的實驗 ID 陣列，必須包含至少兩個 ID。',
  },
  {
    name: 'baselineId',
    type: 'string',
    isOptional: true,
    description: '基準實驗的 ID。預設為 `experimentIds` 中的第一個 ID。',
  },
]}
/>

## 傳回值

如果提供少於兩個實驗 ID，便會拋出 `MastraError`。

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: '比較結果。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'baselineId',
            type: 'string',
            description: '用於比較的基準實驗 ID。',
          },
          {
            name: 'items',
            type: 'Array<object>',
            description: '各項目的比較資料。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'itemId',
                    type: 'string',
                    description: '資料集項目的 ID。',
                  },
                  {
                    name: 'input',
                    type: 'unknown | null',
                    description: '項目的輸入資料。',
                  },
                  {
                    name: 'groundTruth',
                    type: 'unknown | null',
                    description: '項目的基準答案。',
                  },
                  {
                    name: 'results',
                    type: 'Record<string, { output: unknown; scores: Record<string, number | null> } | null>',
                    description:
                      '以實驗 ID 為鍵的結果。每個項目包含該實驗的輸出及評分器結果。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## 相關內容

- [dataset.startExperiment()](/reference/datasets/startExperiment)
- [dataset.listExperiments()](/reference/datasets/listExperiments)
