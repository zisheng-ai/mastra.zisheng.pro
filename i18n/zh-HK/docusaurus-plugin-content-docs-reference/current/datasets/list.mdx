---
title: "參考：DatasetsManager.list() | 資料集"
description: "以下是 `DatasetsManager.list()` 方法的文檔；此方法會以分頁方式列出所有資料集。"
packages:
  - "@mastra/core"
---

# DatasetsManager.list()

**新增於：** `@mastra/core@1.4.0`

以分頁方式列出資料集，並可套用篩選條件。 篩選條件會在儲存層套用，因此呼叫端毋須再篩選結果。

## 使用範例

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const { datasets, pagination } = await mastra.datasets.list({ page: 0, perPage: 10 })

for (const ds of datasets) {
  console.log(`${ds.id}: ${ds.name} (v${ds.version})`)
}

// Filter by target type and name (case-insensitive substring)
const filtered = await mastra.datasets.list({
  page: 0,
  perPage: 20,
  filters: { targetType: 'agent', name: 'qa' },
})

// Filter by any-overlap match on target IDs
const forAgents = await mastra.datasets.list({
  filters: { targetIds: ['agent-a', 'agent-b'] },
})
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: '頁碼。預設為 `0`。',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: '每頁的資料集數目。預設為 `20`。',
  },
  {
    name: 'filters',
    type: 'object',
    isOptional: true,
    description: '在儲存層套用的可選篩選條件。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'organizationId',
            type: 'string',
            isOptional: true,
            description: '租戶篩選：只包含此組織中的資料集。',
          },
          {
            name: 'projectId',
            type: 'string',
            isOptional: true,
            description: '租戶篩選：只包含此項目中的資料集。',
          },
          {
            name: 'candidateKey',
            type: 'string',
            isOptional: true,
            description: '只包含屬於此候選鍵的資料集。',
          },
          {
            name: 'candidateId',
            type: 'string',
            isOptional: true,
            description: '只包含屬於此候選 ID 的資料集。',
          },
          {
            name: 'targetType',
            type: "'agent' | 'workflow' | 'scorer' | 'processor'",
            isOptional: true,
            description: '只包含以此元件類型為目標的資料集。',
          },
          {
            name: 'targetIds',
            type: 'string[]',
            isOptional: true,
            description: '只包含 `targetIds` 與此清單有交集的資料集（任意重疊即相符）。空陣列視為不設篩選。',
          },
          {
            name: 'name',
            type: 'string',
            isOptional: true,
            description: '對資料集 `name` 執行不分大小寫的子字串配對。空字串視為不設篩選。',
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: '分頁資料集清單。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'datasets',
            type: 'DatasetRecord[]',
            description: '資料集記錄陣列。請參閱 dataset.getDetails() 了解記錄結構。',
          },
          {
            name: 'pagination',
            type: 'PaginationInfo',
            description: '包含 `total`、`page`、`perPage` 及 `hasMore` 的分頁元資料。',
          },
        ],
      },
    ],
  },
]}
/>
