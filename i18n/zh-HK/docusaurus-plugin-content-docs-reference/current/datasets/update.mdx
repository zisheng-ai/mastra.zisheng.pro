---
title: "參考：dataset.update() | 資料集"
description: "以下是 `dataset.update()` 方法的文檔；此方法會更新資料集元資料及結構描述。"
packages:
  - "@mastra/core"
---

# dataset.update() {/* #datasetupdate */}

**新增於：** `@mastra/core@1.4.0`

更新資料集的元資料、名稱、描述及／或結構描述。 請使用 Standard JSON Schema（例如 Zod、Valibot、ArkType）定義結構描述。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'
import { z } from 'zod'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Update with plain metadata
const updated = await dataset.update({
  name: 'Updated QA pairs',
  description: 'Revised evaluation set',
})

// Update with Zod schema (library that supports Standard JSON Schema)
const updated2 = await dataset.update({
  inputSchema: z.object({
    question: z.string(),
    context: z.string().optional(),
  }),
})
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    isOptional: true,
    description: '新的顯示名稱。',
  },
  {
    name: 'description',
    type: 'string',
    isOptional: true,
    description: '新的描述。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '已更新的元資料。',
  },
  {
    name: 'inputSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: '項目輸入的 Standard JSON Schema。',
  },
  {
    name: 'groundTruthSchema',
    type: 'StandardJSONSchemaV1',
    isOptional: true,
    description: '項目基準答案的 Standard JSON Schema。',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetRecord>',
    description: '已更新的資料集記錄。 請參閱 dataset.getDetails() 了解完整結構。',
  },
]}
/>
