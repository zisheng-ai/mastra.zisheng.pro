---
title: "參考：dataset.listVersions() | 資料集"
description: "以下是 `dataset.listVersions()` 方法的文檔；此方法會列出資料集的所有版本。"
packages:
  - "@mastra/core"
---

# dataset.listVersions()

**新增於：** `@mastra/core@1.4.0`

以分頁方式列出資料集的所有版本。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const { versions, pagination } = await dataset.listVersions({ page: 0, perPage: 10 })

for (const version of versions) {
  console.log(`Version ${version.version} created at ${version.createdAt}`)
}
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: '頁碼。預設為 `0`。',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: '每頁的版本數目。預設為 `20`。',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: '分頁版本清單。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'versions',
            type: 'DatasetVersion[]',
            description: '版本記錄陣列。',
            properties: [
              {
                type: 'DatasetVersion',
                parameters: [
                  {
                    name: 'id',
                    type: 'string',
                    description: '版本記錄的唯一識別碼。',
                  },
                  {
                    name: 'datasetId',
                    type: 'string',
                    description: '上層資料集的 ID。',
                  },
                  {
                    name: 'version',
                    type: 'number',
                    description: '版本號碼。',
                  },
                  {
                    name: 'createdAt',
                    type: 'Date',
                    description: '此版本的建立時間。',
                  },
                ],
              },
            ],
          },
          {
            name: 'pagination',
            type: 'object',
            description: '分頁元資料。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'total',
                    type: 'number',
                    description: '版本總數。',
                  },
                  {
                    name: 'page',
                    type: 'number',
                    description: '目前頁碼。',
                  },
                  {
                    name: 'perPage',
                    type: 'number | false',
                    description: '每頁版本數目；如不分頁則為 `false`。',
                  },
                  {
                    name: 'hasMore',
                    type: 'boolean',
                    description: '是否仍有更多頁面。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>
