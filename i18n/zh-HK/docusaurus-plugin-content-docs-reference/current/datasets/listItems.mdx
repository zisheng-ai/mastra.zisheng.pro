---
title: "參考：dataset.listItems() | 資料集"
description: "以下是 `dataset.listItems()` 方法的文檔；此方法會列出資料集中的項目，並支援可選的分頁及篩選。"
packages:
  - "@mastra/core"
---

# dataset.listItems()

**新增於：** `@mastra/core@1.4.0`

列出資料集中的項目。只提供 `version` 時，會傳回該版本所有項目的純 `DatasetItem[]` 快照。其他情況（沒有參數，或提供 `search`／`page`／`perPage`，不論有否提供 `version`）則會傳回分頁 `{ items, pagination }` 結構。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

// Paginated list (default: page 0, 20 per page)
const result = await dataset.listItems()

// List with search
const filtered = await dataset.listItems({ search: 'TypeScript', page: 0, perPage: 10 })

// Paginated list scoped to a specific version
const versionedPage = await dataset.listItems({ version: 2, page: 0, perPage: 20 })

// Version-only snapshot returns a bare DatasetItem[] (deprecated form —
// prefer passing page/perPage to receive the paginated shape).
const versionedItems = await dataset.listItems({ version: 2 })
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'version',
    type: 'number',
    isOptional: true,
    description:
      '要列出項目的資料集版本。單獨傳入時，會以純 `DatasetItem[]` 快照傳回該版本的所有項目。將 `search`／`page`／`perPage` 與 `version` 一併傳入，便會改用分頁結構。',
  },
  {
    name: 'page',
    type: 'number',
    isOptional: true,
    description: '分頁頁碼。預設為 `0`。',
  },
  {
    name: 'perPage',
    type: 'number',
    isOptional: true,
    description: '每頁的項目數目。預設為 `20`。',
  },
  {
    name: 'search',
    type: 'string',
    isOptional: true,
    description: '用於篩選項目的搜尋字串。',
  },
]}
/>

## 傳回值 {/* #returns */}

只提供 `version`（沒有 `search`、`page` 或 `perPage`）時：

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItem[]>',
    description:
      '指定資料集版本中所有項目的純陣列。此結構只為向後兼容而保留，現已棄用；傳入 `page`／`perPage`（或 `search`）即可一律接收下方的分頁結構。',
  },
]}
/>

<br />

其他所有情況（沒有參數，或提供 `search`／`page`／`perPage`，不論有否提供 `version`）時：

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<object>',
    description: '分頁回應。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'items',
            type: 'DatasetItem[]',
            description: '目前頁面的項目陣列。',
          },
          {
            name: 'pagination',
            type: 'object',
            description: '分頁元資料。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'total',
                    type: 'number',
                    description: '項目總數。',
                  },
                  {
                    name: 'page',
                    type: 'number',
                    description: '目前頁碼。',
                  },
                  {
                    name: 'perPage',
                    type: 'number | false',
                    description: '每頁項目數目；如不分頁則為 `false`。',
                  },
                  {
                    name: 'hasMore',
                    type: 'boolean',
                    description: '是否仍有更多頁面。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>
