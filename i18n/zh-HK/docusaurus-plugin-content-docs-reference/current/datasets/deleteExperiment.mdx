---
title: "參考：dataset.deleteExperiment() | 資料集"
description: "以下是 `dataset.deleteExperiment()` 方法的文檔；此方法會按 ID 刪除實驗。"
packages:
  - "@mastra/core"
---

# dataset.deleteExperiment() {/* #datasetdeleteexperiment */}

**新增於：** `@mastra/core@1.4.0`

按 ID 刪除實驗（執行），包括所有相關結果。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteExperiment({ experimentId: 'exp-id' })
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'experimentId',
    type: 'string',
    description: '要刪除的實驗 ID。',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: '實驗及其結果刪除後便會完成。',
  },
]}
/>
