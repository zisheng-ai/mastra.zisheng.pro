---
title: "參考：dataset.deleteItems() | 資料集"
description: "以下是 `dataset.deleteItems()` 方法的文檔；此方法會批量從資料集刪除多個項目。"
packages:
  - "@mastra/core"
---

# dataset.deleteItems()

**新增於：** `@mastra/core@1.4.0`

透過單次批量操作從資料集刪除多個項目。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

await dataset.deleteItems({
  itemIds: ['item-1', 'item-2', 'item-3'],
})
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemIds',
    type: 'string[]',
    description: '要刪除的項目 ID 陣列。',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<void>',
    description: '所有項目刪除後便會完成。',
  },
]}
/>
