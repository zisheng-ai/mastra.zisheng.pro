---
title: "參考：dataset.getItemHistory() | 資料集"
description: "以下是 `dataset.getItemHistory()` 方法的文檔；此方法會取得資料集項目的完整 SCD-2 變更歷史。"
packages:
  - "@mastra/core"
---

# dataset.getItemHistory() {/* #datasetgetitemhistory */}

**新增於：** `@mastra/core@1.4.0`

取得指定項目在所有資料集版本中的完整 SCD-2（緩慢變化維度類型 2）歷史記錄。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({/* storage config */})

const dataset = await mastra.datasets.get({ id: 'dataset-id' })

const history = await dataset.getItemHistory({ itemId: 'item-id' })

for (const row of history) {
  console.log(`Version ${row.datasetVersion}: ${row.isDeleted ? 'deleted' : 'active'}`)
}
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'itemId',
    type: 'string',
    description: '要取得其歷史記錄的項目 ID。',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<DatasetItemRow[]>',
    description: '按版本排序的項目版本資料列陣列。',
    properties: [
      {
        type: 'DatasetItemRow',
        parameters: [
          {
            name: 'id',
            type: 'string',
            description: '資料列的唯一識別碼。',
          },
          {
            name: 'datasetId',
            type: 'string',
            description: '上層資料集的 ID。',
          },
          {
            name: 'datasetVersion',
            type: 'number',
            description: '此資料列的資料集版本。',
          },
          {
            name: 'validTo',
            type: 'number | null',
            description: '此資料列被取代時的版本；如仍為目前版本則為 `null`。',
          },
          {
            name: 'isDeleted',
            type: 'boolean',
            description: '此資料列是否代表刪除操作。',
          },
          {
            name: 'input',
            type: 'unknown',
            description: '此版本的輸入資料。',
          },
          {
            name: 'groundTruth',
            type: 'unknown',
            isOptional: true,
            description: '此版本的基準答案。',
          },
          {
            name: 'metadata',
            type: 'Record<string, unknown>',
            isOptional: true,
            description: '此版本的元資料。',
          },
          {
            name: 'createdAt',
            type: 'Date',
            description: '此資料列的建立時間。',
          },
          {
            name: 'updatedAt',
            type: 'Date',
            description: '此資料列上次更新的時間。',
          },
        ],
      },
    ],
  },
]}
/>
