---
title: "參考：Agent.streamLegacy()（舊版）| 串流"
description: "Mastra Agent 舊版 `Agent.streamLegacy()` 方法的文件。此方法已棄用，並會在未來版本中移除。"
packages:
  - "@mastra/core"
---

# Agent.streamLegacy()（舊版）

:::warning

**已棄用**：此方法已棄用，並且只適用於 V1 模型。V2 模型請改用新的 [`.stream()`](./stream) 方法。升級詳情請參閱[遷移指南](/guides/migrations/vnext-to-standard-apis)。

:::

`.streamLegacy()` 是舊版 Agent 串流 API，用於即時串流 V1 模型 Agent 的回應。此方法接受訊息及選用的串流選項。

## 使用範例

```typescript
await agent.streamLegacy('message for agent')
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'string | string[] | CoreMessage[] | AiMessageType[] | UIMessageWithMetadata[]',
    description: '要傳送給 Agent 的訊息。可以是單一字串、字串陣列或結構化訊息物件。',
  },
  {
    name: 'options',
    type: 'AgentStreamOptions<OUTPUT, EXPERIMENTAL_OUTPUT>',
    isOptional: true,
    description: '串流處理程序的選用設定。',
    properties: [
      {
        type: 'AgentStreamOptions',
        parameters: [
          {
            name: 'abortSignal',
            type: 'AbortSignal',
            isOptional: true,
            description: '可讓你中止 Agent 執行的訊號物件。訊號中止後，所有進行中的操作都會終止。',
          },
          {
            name: 'context',
            type: 'CoreMessage[]',
            isOptional: true,
            description: '提供給 Agent 的額外內容脈絡訊息。',
          },
          {
            name: 'experimental_output',
            type: 'Zod schema | JsonSchema7',
            isOptional: true,
            description: '啟用結構化輸出，並同時產生文字及進行 Tool 呼叫。模型產生的回應會符合所提供的 schema。',
          },
          {
            name: 'instructions',
            type: 'string',
            isOptional: true,
            description:
              '自訂指示，覆蓋 Agent 針對這次產生作業使用的預設指示。適合用於動態修改 Agent 行為，而毋須建立新的 Agent 實例。',
          },
          {
            name: 'output',
            type: 'Zod schema | JsonSchema7',
            isOptional: true,
            description: '定義預期的輸出結構。可以是 JSON Schema 物件或 Zod schema。',
          },
          {
            name: 'memory',
            type: 'object',
            isOptional: true,
            description: '記憶體設定。這是管理記憶體的建議方式。',
            properties: [
              {
                parameters: [
                  {
                    name: 'thread',
                    type: 'string | { id: string; metadata?: Record<string, any>, title?: string }',
                    isOptional: false,
                    description: '對話 thread，可以是字串 ID，或包含 `id` 及選用 `metadata` 的物件。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'resource',
                    type: 'string',
                    isOptional: false,
                    description: '與 thread 關聯的使用者或資源標識符。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'options',
                    type: 'MemoryConfig',
                    isOptional: true,
                    description: '記憶體行為的設定，例如訊息記錄及語意回憶。',
                  },
                ],
              },
            ],
          },
          {
            name: 'maxSteps',
            type: 'number',
            isOptional: true,
            defaultValue: '5',
            description: '允許的最大執行步驟數。',
          },
          {
            name: 'maxRetries',
            type: 'number',
            isOptional: true,
            defaultValue: '2',
            description: '最大重試次數。設為 0 可停用重試。',
          },
          {
            name: 'memoryOptions',
            type: 'MemoryConfig',
            isOptional: true,
            description: '**已棄用。** 請改用 `memory.options`。記憶體管理的設定選項。',
            properties: [
              {
                parameters: [
                  {
                    name: 'lastMessages',
                    type: 'number | false',
                    isOptional: true,
                    description: '要加入內容脈絡的近期訊息數量；設為 false 可停用。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'semanticRecall',
                    type: "boolean | { topK: number; messageRange: number | { before: number; after: number }; scope?: 'thread' | 'resource' }",
                    isOptional: true,
                    description: '啟用語意回憶以尋找相關的過往訊息。可以是布林值或詳細設定。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'workingMemory',
                    type: 'WorkingMemory',
                    isOptional: true,
                    description: '工作記憶功能的設定。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'threads',
                    type: '{ generateTitle?: boolean | { model: DynamicArgument<MastraLanguageModel>; instructions?: DynamicArgument<string> } }',
                    isOptional: true,
                    description: 'Thread 專用設定，包括自動產生標題。',
                  },
                ],
              },
            ],
          },
          {
            name: 'onFinish',
            type: 'StreamTextOnFinishCallback<any> | StreamObjectOnFinishCallback<OUTPUT>',
            isOptional: true,
            description: '串流完成時呼叫的回呼函式，會接收最終結果。',
          },
          {
            name: 'onStepFinish',
            type: 'StreamTextOnStepFinishCallback<any> | never',
            isOptional: true,
            description: '每個執行步驟完成後呼叫的回呼函式，會以 JSON 字串接收步驟詳情。不適用於結構化輸出。',
          },
          {
            name: 'resourceId',
            type: 'string',
            isOptional: true,
            description:
              '**已棄用。** 請改用 `memory.resource`。與 Agent 互動的使用者或資源標識符。如有提供 threadId，亦必須提供此值。',
          },
          {
            name: 'telemetry',
            type: 'TelemetrySettings',
            isOptional: true,
            description: '串流期間收集遙測資料的設定。',
            properties: [
              {
                parameters: [
                  {
                    name: 'isEnabled',
                    type: 'boolean',
                    isOptional: true,
                    description: '啟用或停用遙測。實驗階段預設為停用。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordInputs',
                    type: 'boolean',
                    isOptional: true,
                    description: '啟用或停用輸入記錄。預設為啟用；你可停用輸入記錄，以免記錄敏感資料。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordOutputs',
                    type: 'boolean',
                    isOptional: true,
                    description: '啟用或停用輸出記錄。預設為啟用；你可停用輸出記錄，以免記錄敏感資料。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'functionId',
                    type: 'string',
                    isOptional: true,
                    description: '此函式的標識符，用於按函式分組遙測資料。',
                  },
                ],
              },
            ],
          },
          {
            name: 'temperature',
            type: 'number',
            isOptional: true,
            description:
              '控制模型輸出的隨機程度。較高的值（例如 0.8）令輸出更隨機；較低的值（例如 0.2）令輸出更聚焦且更具確定性。',
          },
          {
            name: 'threadId',
            type: 'string',
            isOptional: true,
            description:
              '**已棄用。** 請改用 `memory.thread`。對話 thread 的標識符，可在多次互動之間保留內容脈絡。如有提供 resourceId，亦必須提供此值。',
          },
          {
            name: 'toolChoice',
            type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
            isOptional: true,
            defaultValue: "'auto'",
            description: '控制 Agent 在串流期間如何使用 Tool。',
            properties: [
              {
                parameters: [
                  {
                    name: "'auto'",
                    type: 'string',
                    description: '由模型決定是否使用 Tool（預設）。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "'none'",
                    type: 'string',
                    description: '不使用任何 Tool。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "'required'",
                    type: 'string',
                    description: '要求模型使用最少一個 Tool。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "{ type: 'tool'; toolName: string }",
                    type: 'object',
                    description: '要求模型使用指定名稱的 Tool。',
                  },
                ],
              },
            ],
          },
          {
            name: 'toolsets',
            type: 'ToolsetsInput',
            isOptional: true,
            description: '串流期間供 Agent 使用的額外 Toolset。',
          },
          {
            name: 'clientTools',
            type: 'ToolsInput',
            isOptional: true,
            description: "在請求的 'client' 端執行的 Tool。這些 Tool 的定義不包含 execute 函式。",
          },
          {
            name: 'hooks',
            type: 'ToolHooks',
            isOptional: true,
            description:
              '每次執行專用的 hook，會在 Tool 呼叫前後運行，並在這次執行中覆蓋相符的 Agent 層級 hook。`beforeToolCall` 可傳回 `{ proceed: false, output }` 以略過 Tool 呼叫。',
          },
          {
            name: 'savePerStep',
            type: 'boolean',
            isOptional: true,
            description: '每個串流步驟完成後逐步儲存訊息（預設：false）。',
          },
          {
            name: 'providerOptions',
            type: 'Record<string, Record<string, JSONValue>>',
            isOptional: true,
            description:
              "傳遞至底層 LLM Provider 的額外 Provider 專用選項。結構為 `{ providerName: { optionKey: value } }`。例如：`{ openai: { reasoningEffort: 'high' }, anthropic: { maxTokens: 1000 } }`。",
            properties: [
              {
                parameters: [
                  {
                    name: 'openai',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: "OpenAI 專用選項。例如：`{ reasoningEffort: 'high' }`",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'anthropic',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Anthropic 專用選項。例如：`{ maxTokens: 1000 }`',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'google',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Google 專用選項。例如：`{ safetySettings: [...] }`',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: '[providerName]',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: '其他 Provider 專用選項。鍵是 Provider 名稱，值是 Provider 專用選項的記錄。',
                  },
                ],
              },
            ],
          },
          {
            name: 'runId',
            type: 'string',
            isOptional: true,
            description: '這次產生作業的唯一 ID，適合用於追蹤及除錯。',
          },
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            description: '用於依賴注入及內容脈絡資料的 Request Context。',
          },
          {
            name: 'maxTokens',
            type: 'number',
            isOptional: true,
            description: '要產生的最大 token 數量。',
          },
          {
            name: 'topP',
            type: 'number',
            isOptional: true,
            description: '核心取樣。值介乎 0 至 1；建議設定 `temperature` 或 `topP` 其中一項，而非同時設定兩者。',
          },
          {
            name: 'topK',
            type: 'number',
            isOptional: true,
            description: '每個後續 token 只從排名最高的 K 個選項取樣，用於排除屬於「長尾」的低概率回應。',
          },
          {
            name: 'presencePenalty',
            type: 'number',
            isOptional: true,
            description:
              '存在懲罰設定，影響模型重複提示中既有資料的可能性。值介乎 -1（增加重複）至 1（最大懲罰，減少重複）。',
          },
          {
            name: 'frequencyPenalty',
            type: 'number',
            isOptional: true,
            description:
              '頻率懲罰設定，影響模型重複使用相同字詞或片語的可能性。值介乎 -1（增加重複）至 1（最大懲罰，減少重複）。',
          },
          {
            name: 'stopSequences',
            type: 'string[]',
            isOptional: true,
            description: '停止序列。如有設定，模型產生其中一個停止序列時便會停止產生文字。',
          },
          {
            name: 'seed',
            type: 'number',
            isOptional: true,
            description: '用於隨機取樣的種子（整數）。如有設定且模型支援，呼叫會產生確定性結果。',
          },
          {
            name: 'headers',
            type: 'Record<string, string | undefined>',
            isOptional: true,
            description: '隨請求傳送的額外 HTTP header。只適用於以 HTTP 為基礎的 Provider。',
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'textStream',
    type: 'AsyncGenerator<string>',
    isOptional: true,
    description: '非同步產生器，在文字區塊可用時逐一產生。',
  },
  {
    name: 'fullStream',
    type: 'Promise<ReadableStream>',
    isOptional: true,
    description: '解析為完整回應 ReadableStream 的 Promise。',
  },
  {
    name: 'text',
    type: 'Promise<string>',
    isOptional: true,
    description: '解析為完整文字回應的 Promise。',
  },
  {
    name: 'usage',
    type: 'Promise<{ totalTokens: number; promptTokens: number; completionTokens: number }>',
    isOptional: true,
    description: '解析為 token 使用量資料的 Promise。',
  },
  {
    name: 'finishReason',
    type: 'Promise<string>',
    isOptional: true,
    description: '解析為串流結束原因的 Promise。',
  },
  {
    name: 'toolCalls',
    type: 'Promise<Array<ToolCall>>',
    isOptional: true,
    description: '解析為串流期間所作 Tool 呼叫的 Promise。',
    properties: [
      {
        parameters: [
          {
            name: 'toolName',
            type: 'string',
            required: true,
            description: '所呼叫 Tool 的名稱。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'args',
            type: 'any',
            required: true,
            description: '傳遞給 Tool 的引數。',
          },
        ],
      },
    ],
  },
]}
/>

## 進階使用範例

```typescript
await agent.streamLegacy('message for agent', {
  temperature: 0.7,
  maxSteps: 3,
  memory: {
    thread: 'user-123',
    resource: 'test-app',
  },
  toolChoice: 'auto',
})
```

## 遷移至新 API

:::info

新的 `.stream()` 方法提供更強的功能，包括 AI SDK v5+ 相容性、更完善的結構化輸出處理，以及改良的回呼系統。詳細遷移指示請參閱[遷移指南](/guides/migrations/vnext-to-standard-apis)。

:::

### 快速遷移範例

#### 遷移前（舊版）

```typescript
const result = await agent.streamLegacy('message', {
  temperature: 0.7,
  maxSteps: 3,
  onFinish: result => console.log(result),
})
```

#### 遷移後（新 API）

```typescript
const result = await agent.stream('message', {
  modelSettings: {
    temperature: 0.7,
  },
  maxSteps: 3,
  onFinish: result => console.log(result),
})
```

## 相關內容

- [遷移指南](/guides/migrations/vnext-to-standard-apis)
- [新的 .stream() 方法](./stream)
- [產生回應](/docs/agents/overview#use-your-agent)
- [串流回應](/docs/agents/overview#use-your-agent)
