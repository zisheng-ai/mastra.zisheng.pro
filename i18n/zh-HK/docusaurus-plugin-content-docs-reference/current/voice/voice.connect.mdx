---
title: "參考：voice.connect() | Voice"
description: "實時語音 Provider 所提供 connect() 方法的文件；此方法會為語音對語音通訊建立連線。"
packages:
  - "@mastra/core"
  - "@mastra/node-speaker"
  - "@mastra/voice-openai-realtime"
---

# voice.connect()

`connect()` 方法會建立 WebSocket 或 WebRTC 連線，以進行實時語音對語音通訊。使用 `send()` 或 `answer()` 等其他實時功能前，必須先呼叫此方法。

## 使用範例

```typescript
import { OpenAIRealtimeVoice } from '@mastra/voice-openai-realtime'
import Speaker from '@mastra/node-speaker'

const speaker = new Speaker({
  sampleRate: 24100, // Audio sample rate in Hz - standard for high-quality audio on MacBook Pro
  channels: 1, // Mono audio output (as opposed to stereo which would be 2)
  bitDepth: 16, // Bit depth for audio quality - CD quality standard (16-bit resolution)
})

// Initialize a real-time voice provider
const voice = new OpenAIRealtimeVoice({
  realtimeConfig: {
    model: 'gpt-5.1-realtime',
    apiKey: process.env.OPENAI_API_KEY,
    options: {
      sessionConfig: {
        turn_detection: {
          type: 'server_vad',
          threshold: 0.6,
          silence_duration_ms: 1200,
        },
      },
    },
  },
  speaker: 'alloy', // Default voice
})
// Connect to the real-time service
await voice.connect()
// Now you can use real-time features
voice.on('speaker', stream => {
  stream.pipe(speaker)
})
// With connection options
await voice.connect({
  timeout: 10000, // 10 seconds timeout
  reconnect: true,
})
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'Record<string, unknown>',
    description: 'Provider 專屬的連線選項',
    isOptional: true,
  },
]}
/>

## 傳回值

傳回 `Promise<void>`，並在成功建立連線後 resolve。

## Provider 專屬選項

每個實時語音 Provider 可為 `connect()` 方法支援不同選項：

### OpenAI Realtime

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'Options',
    description: '設定選項。',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'timeout',
            type: 'number',
            description: '連線逾時時間（毫秒）',
            isOptional: true,
            defaultValue: '30000',
          },
          {
            name: 'reconnect',
            type: 'boolean',
            description: '連線中斷時是否自動重新連線',
            isOptional: true,
            defaultValue: 'false',
          },
        ],
      },
    ],
  },
]}
/>

## 配合 `CompositeVoice` 使用

使用 `CompositeVoice` 時，`connect()` 方法會委派給已設定的實時 Provider：

```typescript
import { CompositeVoice } from '@mastra/core/voice'
import { OpenAIRealtimeVoice } from '@mastra/voice-openai-realtime'
const realtimeVoice = new OpenAIRealtimeVoice()
const voice = new CompositeVoice({
  realtime: realtimeVoice,
})
// This will use the OpenAIRealtimeVoice provider
await voice.connect()
```

## 注意事項

- 此方法只由支援語音對語音功能的實時語音 Provider 實作
- 如在不支援此功能的語音 Provider 上呼叫，系統會記錄警告並立即 resolve
- 使用 `send()` 或 `answer()` 等其他實時方法前，必須先建立連線
- 使用完語音 instance 後，請呼叫 `close()` 以妥善清理資源
- 部分 Provider 可能會在連線中斷時自動重新連線，實際行為視乎其實作方式
- 連線錯誤通常會作為 exception 拋出，應將其 catch 並處理

## 相關方法

- [voice.send()](./voice.send)：將音訊資料傳送至語音 Provider
- [voice.answer()](./voice.answer)：觸發語音 Provider 作出回應
- [voice.close()](./voice.close)：中斷與實時服務的連線
- [voice.on()](./voice.on)：為語音事件註冊事件 listener
