---
title: "參考：Murf | 語音"
description: "Murf 語音實作的文件，提供文字轉語音功能。"
packages:
  - "@mastra/voice-murf"
---

# Murf

Mastra 的 Murf 語音實作使用 Murf 的 AI 語音服務提供文字轉語音（TTS）功能，並支援多種語言的多款語音。

## 使用範例 {/* #usage-example */}

```typescript
import { MurfVoice } from '@mastra/voice-murf'

// Initialize with default configuration (uses MURF_API_KEY environment variable)
const voice = new MurfVoice()

// Initialize with custom configuration
const voice = new MurfVoice({
  speechModel: {
    name: 'GEN2',
    apiKey: 'your-api-key',
    properties: {
      format: 'MP3',
      rate: 1.0,
      pitch: 1.0,
      sampleRate: 48000,
      channelType: 'STEREO',
    },
  },
  speaker: 'en-US-cooper',
})

// Text-to-Speech with default settings
const audioStream = await voice.speak('Hello, world!')

// Text-to-Speech with custom properties
const audioStream = await voice.speak('Hello, world!', {
  speaker: 'en-UK-hazel',
  properties: {
    format: 'WAV',
    rate: 1.2,
    style: 'casual',
  },
})

// Get available voices
const voices = await voice.getSpeakers()
```

## 建構函式參數 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'MurfConfig',
    description: '文字轉語音功能的設定',
    isOptional: true,
    defaultValue: "{ name: 'GEN2' }",
    properties: [
      {
        type: 'MurfConfig',
        parameters: [
          {
            name: 'name',
            type: "'GEN1' | 'GEN2'",
            description: '要使用的 Murf 模型世代',
            isOptional: false,
            defaultValue: "'GEN2'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Murf API 金鑰。若未提供，則使用 MURF_API_KEY 環境變數',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'object',
            description: '所有語音合成請求的預設屬性',
            isOptional: true,
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'style',
                    type: 'string',
                    description: '語音的說話風格',
                    isOptional: true,
                  },
                  {
                    name: 'rate',
                    type: 'number',
                    description: '語速倍數',
                    isOptional: true,
                  },
                  {
                    name: 'pitch',
                    type: 'number',
                    description: '語音音高調整',
                    isOptional: true,
                  },
                  {
                    name: 'sampleRate',
                    type: '8000 | 24000 | 44100 | 48000',
                    description: '音訊取樣率（Hz）',
                    isOptional: true,
                  },
                  {
                    name: 'format',
                    type: "'MP3' | 'WAV' | 'FLAC' | 'ALAW' | 'ULAW'",
                    description: '輸出音訊格式',
                    isOptional: true,
                  },
                  {
                    name: 'channelType',
                    type: "'STEREO' | 'MONO'",
                    description: '音訊聲道設定',
                    isOptional: true,
                  },
                  {
                    name: 'pronunciationDictionary',
                    type: 'Record<string, string>',
                    description: '自訂發音對應',
                    isOptional: true,
                  },
                  {
                    name: 'encodeAsBase64',
                    type: 'boolean',
                    description: '是否將音訊編碼為 base64',
                    isOptional: true,
                  },
                  {
                    name: 'variation',
                    type: 'number',
                    description: '語音變化參數',
                    isOptional: true,
                  },
                  {
                    name: 'audioDuration',
                    type: 'number',
                    description: '目標音訊長度（秒）',
                    isOptional: true,
                  },
                  {
                    name: 'multiNativeLocale',
                    type: 'string',
                    description: '多語言支援的地區設定',
                    isOptional: true,
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: '文字轉語音所使用的預設語音 ID',
    isOptional: true,
    defaultValue: "'en-UK-hazel'",
  },
]}
/>

## 方法 {/* #methods */}

### `speak()` {/* #speak */}

使用 Murf API 將文字轉換為語音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要轉換為語音的文字。如提供串流，會先將其轉換為文字。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '語音合成選項',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '為此請求覆寫預設說話者',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'object',
            description: '為此請求覆寫預設語音屬性',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

傳回：`Promise<NodeJS.ReadableStream>`

### `getSpeakers()` {/* #getspeakers */}

傳回可用語音選項的陣列，每個節點包含：

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: '語音的顯示名稱',
    isOptional: false,
  },
  {
    name: 'language',
    type: 'string',
    description: '語音的語言代碼',
    isOptional: false,
  },
  {
    name: 'gender',
    type: 'string',
    description: '語音的性別',
    isOptional: false,
  },
]}
/>

### `listen()` {/* #listen */}

Murf 不支援此方法，呼叫時會擲回錯誤。Murf 不提供語音轉文字功能。

## 重要事項 {/* #important-notes */}

1. 必須提供 Murf API 金鑰。請透過 `MURF_API_KEY` 環境變數設定，或在建構函式中傳入。
1. 此服務預設使用 GEN2 模型版本。
1. 語音屬性可在建構函式層級設定，亦可按個別請求覆寫。
1. 此服務可透過格式、取樣率及聲道類型等屬性，廣泛自訂音訊。
1. 不支援語音轉文字功能。
