---
title: "參考：ElevenLabs | 語音"
description: "ElevenLabs 語音實作的文件，使用多種語音模型及自然流暢的語音合成，提供高質素的文字轉語音功能。"
packages:
  - "@mastra/voice-elevenlabs"
---

# ElevenLabs {/* #elevenlabs */}

Mastra 的 ElevenLabs 語音實作使用 ElevenLabs API 提供高質素的文字轉語音（TTS）和語音轉文字（STT）功能。

## 使用範例 {/* #usage-example */}

```typescript
import { ElevenLabsVoice } from '@mastra/voice-elevenlabs'

// Initialize with default configuration (uses ELEVENLABS_API_KEY environment variable)
const voice = new ElevenLabsVoice()

// Initialize with custom configuration
const voice = new ElevenLabsVoice({
  speechModel: {
    name: 'eleven_multilingual_v2',
    apiKey: 'your-api-key',
  },
  speaker: 'custom-speaker-id',
})

// Text-to-Speech
const audioStream = await voice.speak('Hello, world!')

// Get available speakers
const speakers = await voice.getSpeakers()
```

## 建構函數參數 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'ElevenLabsVoiceConfig',
    description: '文字轉語音功能的設定。',
    isOptional: true,
    defaultValue: "{ name: 'eleven_multilingual_v2' }",
    properties: [
      {
        type: 'ElevenLabsVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'ElevenLabsModel',
            description: '要使用的 ElevenLabs 模型',
            isOptional: true,
            defaultValue: "'eleven_multilingual_v2'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'ElevenLabs API 金鑰。如未提供，則使用 ELEVENLABS_API_KEY 環境變數',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: '文字轉語音所使用的說話者 ID',
    isOptional: true,
    defaultValue: "'9BWtsMINqrJLrRacOk9x'（Aria 聲音）",
  },
]}
/>

## 方法 {/* #methods */}

### `speak()` {/* #speak */}

使用已設定的語音模型和聲音將文字轉換成語音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要轉換成語音的文字。如提供串流，會先將其轉換成文字。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '語音合成的額外選項',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '在此請求中取代預設說話者 ID',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

傳回：`Promise<NodeJS.ReadableStream>`

### `getSpeakers()` {/* #getspeakers */}

傳回可用聲音選項的陣列，其中每個節點包含：

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '聲音的唯一識別碼',
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: '聲音的顯示名稱',
    isOptional: false,
  },
  {
    name: 'language',
    type: 'string',
    description: '聲音的語言代碼',
    isOptional: false,
  },
  {
    name: 'gender',
    type: 'string',
    description: '聲音的性別',
    isOptional: false,
  },
]}
/>

### `listen()` {/* #listen */}

使用 ElevenLabs Speech-to-Text API 將音訊輸入轉換成文字。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'NodeJS.ReadableStream',
    description: '包含要轉錄音訊資料的可讀取串流',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '轉錄的設定選項',
    isOptional: true,
  },
]}
/>

options 物件支援以下屬性：

<PropertiesTable
  content={[
  {
    name: 'language_code',
    type: 'string',
    description: "ISO 語言代碼（例如 'en'、'fr'、'es'）",
    isOptional: true,
  },
  {
    name: 'tag_audio_events',
    type: 'boolean',
    description: '是否標記 [MUSIC]、[LAUGHTER] 等音訊事件',
    isOptional: true,
  },
  {
    name: 'num_speakers',
    type: 'number',
    description: '要在音訊中偵測的說話者人數',
    isOptional: true,
  },
  {
    name: 'filetype',
    type: 'string',
    description: "音訊檔案格式（例如 'mp3'、'wav'、'ogg'）",
    isOptional: true,
  },
  {
    name: 'timeoutInSeconds',
    type: 'number',
    description: '請求逾時秒數',
    isOptional: true,
  },
  {
    name: 'maxRetries',
    type: 'number',
    description: '最多重試次數',
    isOptional: true,
  },
  {
    name: 'abortSignal',
    type: 'AbortSignal',
    description: '中止請求的訊號',
    isOptional: true,
  },
]}
/>

傳回：`Promise<string>`——解析為轉錄文字的 Promise

## 重要事項 {/* #important-notes */}

1. 必須有 ElevenLabs API 金鑰。請透過 `ELEVENLABS_API_KEY` 環境變數設定，或在建構函數中傳入。
1. 預設說話者設為 Aria（ID：'9BWtsMINqrJLrRacOk9x'）。
1. ElevenLabs 不支援語音轉文字功能。
1. 可使用 `getSpeakers()` 方法取得可用的說話者；該方法會傳回每個聲音的詳細資料，包括語言和性別。
