---
title: "參考：voice.getSpeakers() | 語音 Provider"
description: "語音 Provider 所提供的 getSpeakers() 方法文件；此方法會擷取可用的語音選項。"
packages:
  - "@mastra/voice-elevenlabs"
  - "@mastra/voice-openai"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# voice.getSpeakers() {/* #voicegetspeakers */}

`getSpeakers()` 方法會從語音 Provider 擷取可用的語音選項（講者）清單。應用程式可向用戶提供語音選擇，或以程式方式為不同情境選取最合適的語音。

## 使用範例 {/* #usage-example */}

```typescript
import { OpenAIVoice } from '@mastra/voice-openai'
import { ElevenLabsVoice } from '@mastra/voice-elevenlabs'

// Initialize voice providers
const openaiVoice = new OpenAIVoice()
const elevenLabsVoice = new ElevenLabsVoice({
  apiKey: process.env.ELEVENLABS_API_KEY,
})

// Get available speakers from OpenAI
const openaiSpeakers = await openaiVoice.getSpeakers()
console.log('OpenAI voices:', openaiSpeakers)
// Example output: [{ voiceId: "alloy" }, { voiceId: "echo" }, { voiceId: "fable" }, ...]

// Get available speakers from ElevenLabs
const elevenLabsSpeakers = await elevenLabsVoice.getSpeakers()
console.log('ElevenLabs voices:', elevenLabsSpeakers)
// Example output: [{ voiceId: "21m00Tcm4TlvDq8ikWAM", name: "Rachel" }, ...]

// Use a specific voice for speech
const text = 'Hello, this is a test of different voices.'
await openaiVoice.speak(text, { speaker: openaiSpeakers[2].voiceId })
await elevenLabsVoice.speak(text, { speaker: elevenLabsSpeakers[0].voiceId })
```

## 參數 {/* #parameters */}

此方法不接受任何參數。

## 傳回值 {/* #return-value */}

<PropertiesTable
  content={[
  {
    name: 'Promise<Array<{ voiceId: string } & TSpeakerMetadata>>',
    type: 'Promise',
    description:
      '解析為語音選項陣列的 Promise；每個選項最少包含 voiceId 屬性，亦可能包含 Provider 特定的額外中繼資料。',
  },
]}
/>

## Provider 特定的中繼資料 {/* #provider-specific-metadata */}

不同語音 Provider 會為其語音傳回不同的中繼資料：

<Tabs>
  <TabItem value="OpenAI">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: "語音的唯一識別碼（例如 'alloy'、'echo'、'fable'、'onyx'、'nova'、'shimmer'）",
  },
]}
    />
  </TabItem>

  <TabItem value="OpenAI Realtime">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: "語音的唯一識別碼（例如 'alloy'、'echo'、'fable'、'onyx'、'nova'、'shimmer'）",
  },
]}
    />
  </TabItem>

  <TabItem value="Deepgram">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'language',
    type: 'string',
    description: "嵌入語音 ID 的語言代碼（例如 'en'）",
  },
]}
    />
  </TabItem>

  <TabItem value="ElevenLabs">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: '人類可讀的語音名稱',
  },
  {
    name: 'category',
    type: 'string',
    description: "語音類別（例如 'premade'、'cloned'）",
  },
]}
    />
  </TabItem>

  <TabItem value="Google">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'languageCodes',
    type: 'string[]',
    description: "語音所支援的語言代碼陣列（例如 ['en-US']）",
  },
]}
    />
  </TabItem>

  <TabItem value="Azure">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'language',
    type: 'string',
    description: "從語音 ID 擷取的語言代碼（例如 'en'）",
  },
  {
    name: 'region',
    type: 'string',
    description: "從語音 ID 擷取的地區代碼（例如 'US'）",
  },
]}
    />
  </TabItem>

  <TabItem value="Murf">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: '語音名稱（與 voiceId 相同）',
  },
  {
    name: 'language',
    type: 'string',
    description: "從語音 ID 擷取的語言代碼（例如 'en'）",
  },
  {
    name: 'gender',
    type: 'string',
    description: "語音的性別（目前實作一律為 'neutral'）",
  },
]}
    />
  </TabItem>

  <TabItem value="PlayAI">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼（指向 manifest.json 的 S3 URL）',
  },
  {
    name: 'name',
    type: 'string',
    description: "人類可讀的語音名稱（例如 'Angelo'、'Arsenio'）",
  },
  {
    name: 'accent',
    type: 'string',
    description: "語音的口音（例如 'US'、'Irish'、'US African American'）",
  },
  {
    name: 'gender',
    type: 'string',
    description: "語音的性別（'M' 或 'F'）",
  },
  {
    name: 'age',
    type: 'string',
    description: "語音的年齡類別（例如 'Young'、'Middle'）",
  },
  {
    name: 'style',
    type: 'string',
    description: "語音的說話風格（例如 'Conversational'）",
  },
]}
    />
  </TabItem>

  <TabItem value="Speechify">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: '人類可讀的語音名稱',
  },
  {
    name: 'language',
    type: 'string',
    description: "語音的語言代碼（例如 'en-US'）",
  },
]}
    />
  </TabItem>

  <TabItem value="Sarvam">
    <PropertiesTable
      content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: '人類可讀的語音名稱',
  },
  {
    name: 'language',
    type: 'string',
    description: "語音的語言（例如 'english'、'hindi'）",
  },
  {
    name: 'gender',
    type: 'string',
    description: "語音的性別（'male' 或 'female'）",
  },
]}
    />
  </TabItem>
</Tabs>

## 備註 {/* #notes */}

- 不同 Provider 可用的語音差異很大
- 部分 Provider 可能需要驗證，才能擷取完整的語音清單
- 如 Provider 不支援此方法，預設實作會傳回空陣列
- 基於效能考慮，如需經常顯示清單，可考慮快取結果
- 所有 Provider 均保證包含 `voiceId` 屬性，但額外中繼資料各有不同
