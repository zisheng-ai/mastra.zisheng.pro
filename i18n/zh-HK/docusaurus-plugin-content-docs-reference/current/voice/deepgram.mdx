---
title: "參考：Deepgram | 語音"
description: "Deepgram 語音實作的文件，使用多種語音模型及語言提供文字轉語音和語音轉文字功能。"
packages:
  - "@mastra/voice-deepgram"
---

# Deepgram {/* #deepgram */}

Mastra 的 Deepgram 語音實作使用 Deepgram API 提供文字轉語音（TTS）和語音轉文字（STT）功能。它支援多種語音模型及語言，並可設定語音合成和轉錄選項。

## 使用範例 {/* #usage-example */}

```typescript
import { DeepgramVoice } from '@mastra/voice-deepgram'

// Initialize with default configuration (uses DEEPGRAM_API_KEY environment variable)
const voice = new DeepgramVoice()

// Initialize with custom configuration
const voice = new DeepgramVoice({
  speechModel: {
    name: 'aura',
    apiKey: 'your-api-key',
  },
  listeningModel: {
    name: 'nova-2',
    apiKey: 'your-api-key',
  },
  speaker: 'asteria-en',
})

// Text-to-Speech
const audioStream = await voice.speak('Hello, world!')

// Speech-to-Text
const transcript = await voice.listen(audioStream)
```

## 建構函數參數 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'DeepgramVoiceConfig',
    description: '文字轉語音功能的設定。',
    isOptional: true,
    defaultValue: "{ name: 'aura' }",
    properties: [
      {
        type: 'DeepgramVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'DeepgramModel',
            description: '要使用的 Deepgram 模型',
            isOptional: true,
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Deepgram API 金鑰。如未提供，則使用 DEEPGRAM_API_KEY 環境變數',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'Record<string, any>',
            description: '傳送至 Deepgram API 的額外屬性',
            isOptional: true,
          },
          {
            name: 'language',
            type: 'string',
            description: '模型的語言代碼',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'listeningModel',
    type: 'DeepgramVoiceConfig',
    description: '語音轉文字功能的設定。',
    isOptional: true,
    defaultValue: "{ name: 'nova' }",
    properties: [
      {
        type: 'DeepgramVoiceConfig',
        parameters: [
          {
            name: 'name',
            type: 'DeepgramModel',
            description: '要使用的 Deepgram 模型',
            isOptional: true,
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Deepgram API 金鑰。如未提供，則使用 DEEPGRAM_API_KEY 環境變數',
            isOptional: true,
          },
          {
            name: 'properties',
            type: 'Record<string, any>',
            description: '傳送至 Deepgram API 的額外屬性',
            isOptional: true,
          },
          {
            name: 'language',
            type: 'string',
            description: '模型的語言代碼',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'DeepgramVoiceId',
    description: '文字轉語音預設使用的聲音',
    isOptional: true,
    defaultValue: "'asteria-en'",
  },
]}
/>

## 方法 {/* #methods */}

### `speak()` {/* #speak */}

使用已設定的語音模型和聲音將文字轉換成語音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要轉換成語音的文字。如提供串流，會先將其轉換成文字。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '語音合成的額外選項',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '在此請求中取代預設說話者',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

傳回：`Promise<NodeJS.ReadableStream>`

### `listen()` {/* #listen */}

使用已設定的聆聽模型將語音轉換成文字。

<PropertiesTable
  content={[
  {
    name: 'audioStream',
    type: 'NodeJS.ReadableStream',
    description: '要轉錄的音訊串流',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'object',
    description: '傳送至 Deepgram API 的額外選項',
    isOptional: true,
  },
]}
/>

傳回：`Promise<string>`

### `getSpeakers()` {/* #getspeakers */}

傳回可用的聲音選項清單。

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '聲音的唯一識別碼',
    isOptional: false,
  },
]}
/>
