---
title: "參考：PlayAI | 語音"
description: "PlayAI 語音實作的文件，提供文字轉語音功能。"
packages:
  - "@mastra/voice-playai"
---

# PlayAI {/* #playai */}

Mastra 的 PlayAI 語音實作使用 PlayAI API 提供文字轉語音功能。

## 使用範例 {/* #usage-example */}

```typescript
import { PlayAIVoice } from '@mastra/voice-playai'

// Initialize with default configuration (uses PLAYAI_API_KEY environment variable and PLAYAI_USER_ID environment variable)
const voice = new PlayAIVoice()

// Initialize with default configuration
const voice = new PlayAIVoice({
  speechModel: {
    name: 'PlayDialog',
    apiKey: process.env.PLAYAI_API_KEY,
    userId: process.env.PLAYAI_USER_ID,
  },
  speaker: 'Angelo', // Default voice
})

// Convert text to speech with a specific voice
const audioStream = await voice.speak('Hello, world!', {
  speaker:
    's3://voice-cloning-zero-shot/b27bc13e-996f-4841-b584-4d35801aea98/original/manifest.json', // Dexter voice
})
```

## 建構函式參數 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'PlayAIConfig',
    description: '文字轉語音功能的設定',
    isOptional: true,
    defaultValue: "{ name: 'PlayDialog' }",
    properties: [
      {
        type: 'PlayAIConfig',
        parameters: [
          {
            name: 'name',
            type: "'PlayDialog' | 'Play3.0-mini'",
            description: '要使用的 PlayAI 模型',
            isOptional: true,
            defaultValue: "'PlayDialog'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'PlayAI API key。未提供時會改用 PLAYAI_API_KEY 環境變數',
            isOptional: true,
          },
          {
            name: 'userId',
            type: 'string',
            description: 'PlayAI 使用者 ID。未提供時會改用 PLAYAI_USER_ID 環境變數',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'string',
    description: '用於語音合成的預設語音 ID',
    isOptional: true,
    defaultValue: '第一個可用的語音 ID',
  },
]}
/>

## 方法 {/* #methods */}

### `speak()` {/* #speak */}

使用已設定的語音模型和語音將文字轉換成語音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要轉換成語音的文字。如提供串流，系統會先將其轉換成文字。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'Options',
    description: '設定選項。',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '為此請求覆寫預設 speaker',
            isOptional: true,
            defaultValue: '建構函式的 speaker 值',
          },
        ],
      },
    ],
  },
]}
/>

傳回：`Promise<NodeJS.ReadableStream>`。

### `getSpeakers()` {/* #getspeakers */}

傳回可用語音選項的陣列，其中每個節點包含：

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: '語音名稱',
    isOptional: false,
  },
  {
    name: 'accent',
    type: 'string',
    description: "語音的口音（例如 'US'、'British'、'Australian'）",
    isOptional: false,
  },
  {
    name: 'gender',
    type: "'M' | 'F'",
    description: '語音的性別',
    isOptional: false,
  },
  {
    name: 'age',
    type: "'Young' | 'Middle' | 'Old'",
    description: '語音的年齡類別',
    isOptional: false,
  },
  {
    name: 'style',
    type: "'Conversational' | 'Narrative'",
    description: '語音的說話風格',
    isOptional: false,
  },
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
    isOptional: false,
  },
]}
/>

### `listen()` {/* #listen */}

PlayAI 不支援此方法，呼叫時會拋出錯誤。PlayAI 不提供語音轉文字功能。

## 注意事項 {/* #notes */}

- PlayAI 需要 API key 和使用者 ID 以進行驗證
- 此服務提供兩個模型：'PlayDialog' 和 'Play3.0-mini'
- 每個語音都有唯一的 S3 manifest ID，呼叫 API 時必須使用該 ID
