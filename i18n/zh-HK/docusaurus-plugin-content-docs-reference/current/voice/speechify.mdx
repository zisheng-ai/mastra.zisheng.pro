---
title: "參考：Speechify | 語音"
description: "Speechify 語音實作的文件，提供文字轉語音功能。"
packages:
  - "@mastra/voice-speechify"
---

# Speechify

Mastra 的 Speechify 語音實作使用 Speechify API 提供文字轉語音功能。

## 使用範例

```typescript
import { SpeechifyVoice } from '@mastra/voice-speechify'

// Initialize with default configuration (uses SPEECHIFY_API_KEY environment variable)
const voice = new SpeechifyVoice()

// Initialize with custom configuration
const voice = new SpeechifyVoice({
  speechModel: {
    name: 'simba-3.2',
    apiKey: 'your-api-key',
  },
  speaker: 'harper_32', // Default voice (simba-3.2 serves the curated Simba 3 voices only)
})

// Convert text to speech
const audioStream = await voice.speak('Hello, world!', {
  speaker: 'imogen_32', // Override default voice
})
```

## 建構函式參數

<PropertiesTable
  content={[
  {
    name: 'speechModel',
    type: 'SpeechifyConfig',
    description: '文字轉語音功能的設定',
    isOptional: true,
    defaultValue: "{ name: 'simba-english' }",
    properties: [
      {
        type: 'SpeechifyConfig',
        parameters: [
          {
            name: 'name',
            type: 'SpeechifyModel',
            description: "要使用的 Speechify 模型（'simba-3.2'、'simba-3.0'、'simba-english' 或 'simba-multilingual'）",
            isOptional: true,
            defaultValue: "'simba-english'",
          },
          {
            name: 'apiKey',
            type: 'string',
            description: 'Speechify API key。未提供時會改用 SPEECHIFY_API_KEY 環境變數',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'speaker',
    type: 'SpeechifyVoiceId',
    description:
      '語音合成所使用的預設語音 ID。Simba 3 模型只提供精選語音組合（harper_32、imogen_32 等）；傳統目錄中的語音（george、henry 等）可用於 simba-english 和 simba-multilingual',
    isOptional: true,
    defaultValue: "Simba 3 模型為 'harper_32'，其他模型則為 'george'",
  },
]}
/>

## 方法

### `speak()`

使用已設定的語音模型和語音將文字轉換成語音。

<PropertiesTable
  content={[
  {
    name: 'input',
    type: 'string | NodeJS.ReadableStream',
    description: '要轉換成語音的文字。如提供串流，系統會先將其轉換成文字。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'Options',
    description: '設定選項。',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'speaker',
            type: 'string',
            description: '為此請求覆寫預設 speaker',
            isOptional: true,
            defaultValue: '建構函式的 speaker 值',
          },
          {
            name: 'model',
            type: 'SpeechifyModel',
            description: '為此請求覆寫預設模型',
            isOptional: true,
            defaultValue: '建構函式的 model 值',
          },
        ],
      },
    ],
  },
]}
/>

傳回：`Promise<NodeJS.ReadableStream>`

### `getSpeakers()`

傳回可用語音選項的陣列，其中每個節點包含：

<PropertiesTable
  content={[
  {
    name: 'voiceId',
    type: 'string',
    description: '語音的唯一識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: '語音的顯示名稱',
  },
  {
    name: 'language',
    type: 'string',
    description: '語音的語言代碼',
  },
  {
    name: 'gender',
    type: 'string',
    description: '語音的性別',
  },
]}
/>

### `listen()`

Speechify 不支援此方法，呼叫時會拋出錯誤。Speechify 不提供語音轉文字功能。

## 注意事項

- Speechify 需要 API key 以進行驗證
- 預設模型是 'simba-english'
- 'simba-3.2' 是 Speechify 最新的串流模型，延遲最低且表達力最豐富，也是英文的建議模型
- 'simba-3.2' 和 'simba-3.0' 目前只支援英文；如輸入非英文或混合語言，請使用 'simba-multilingual'
- 'simba-3.2' 和 'simba-3.0' 只提供精選語音組合：'beatrice\_32'、'dominic\_32'、'edmund\_32'、'geffen\_32'、'harper\_32'、'hugh\_32'、'imogen\_32'、'wyatt\_32'。在這些模型中使用 'george' 等傳統目錄語音會傳回錯誤
- 預設 speaker 取決於已設定的模型：Simba 3 模型使用 'harper\_32'，其他模型則使用 'george'
- 不支援語音轉文字功能
- 可透過 speak() 方法的 options 參數傳入其他音訊串流選項
