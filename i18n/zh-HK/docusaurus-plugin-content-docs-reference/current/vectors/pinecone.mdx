---
title: "參考：Pinecone 向量儲存 | Vectors"
description: Mastra 中 PineconeVector class 的文件；此 class 提供 Pinecone 向量資料庫的介面。
packages:
  - "@mastra/pinecone"
---

# Pinecone 向量儲存 {/* #pinecone-vector-store */}

PineconeVector class 提供連接 [Pinecone](https://www.pinecone.io/) 向量資料庫的介面。
它提供即時向量搜尋，並具備混合搜尋、metadata 篩選及 namespace 管理等功能。

## Constructor 選項 {/* #constructor-options */}

Constructor 接受所有 [Pinecone 設定選項](https://docs.pinecone.io/reference/typescript-sdk)，以及 Mastra 特有的欄位。

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '此向量 store instance 的唯一識別碼',
  },
  {
    name: 'apiKey',
    type: 'string',
    description: 'Pinecone API 金鑰',
  },
  {
    name: 'controllerHostUrl',
    type: 'string',
    isOptional: true,
    description: '自訂 Pinecone controller host URL',
  },
  {
    name: 'additionalHeaders',
    type: 'Record<string, string>',
    isOptional: true,
    description: '請求中包含的額外 HTTP header',
  },
  {
    name: 'sourceTag',
    type: 'string',
    isOptional: true,
    description: '用於追蹤請求的來源標籤',
  },
  {
    name: 'cloud',
    type: "'aws' | 'gcp' | 'azure'",
    isOptional: true,
    defaultValue: 'aws',
    description: '建立新 index 時使用的雲端 Provider',
  },
  {
    name: 'region',
    type: 'string',
    isOptional: true,
    defaultValue: 'us-east-1',
    description: '建立新 index 時使用的區域',
  },
]}
/>

## 方法 {/* #methods */}

### `createIndex()` {/* #createindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要建立的 index 名稱',
  },
  {
    name: 'dimension',
    type: 'number',
    description: '向量維度（必須與你的 embedding model 相符）',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: "相似度搜尋的距離度量。如計劃使用混合搜尋，請使用 'dotproduct'。",
  },
]}
/>

### `upsert()` {/* #upsert */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '你的 Pinecone index 名稱',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: '密集 embedding 向量陣列',
  },
  {
    name: 'sparseVectors',
    type: '{ indices: number[], values: number[] }[]',
    isOptional: true,
    description: '用於混合搜尋的稀疏向量陣列。每個向量的 indices 與 values 陣列必須互相對應。',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: '每個向量的 metadata',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '選填的向量 ID（如未提供則自動產生）',
  },
  {
    name: 'namespace',
    type: 'string',
    isOptional: true,
    description: '用於儲存向量的選填 namespace。不同 namespace 中的向量會互相隔離。',
  },
]}
/>

### `query()` {/* #query */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要查詢的 index 名稱',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: '用於尋找相似向量的密集查詢向量',
  },
  {
    name: 'sparseVector',
    type: '{ indices: number[], values: number[] }',
    isOptional: true,
    description: '用於混合搜尋的選填稀疏向量。indices 與 values 陣列必須互相對應。',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: '要傳回的結果數目',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '查詢的 metadata 篩選條件',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: '是否在結果中包含向量',
  },
  {
    name: 'namespace',
    type: 'string',
    isOptional: true,
    description: '用於查詢向量的選填 namespace。只傳回指定 namespace 中的結果。',
  },
]}
/>

### `listIndexes()` {/* #listindexes */}

以字串陣列傳回 index 名稱。

### `describeIndex()` {/* #describeindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要描述的 index 名稱',
  },
]}
/>

傳回：

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()` {/* #deleteindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要刪除的 index 名稱',
  },
]}
/>

### `updateVector()` {/* #updatevector */}

按 ID 或 metadata 篩選條件更新單一向量。必須提供 `id` 或 `filter`，但不可同時提供兩者。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含該向量的 index 名稱',
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: '要更新的向量 ID（與 filter 互斥）',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '用於識別要更新向量的 metadata 篩選條件（與 id 互斥）',
  },
  {
    name: 'namespace',
    type: 'string',
    isOptional: true,
    description: '更新操作所用的選填 namespace',
  },
  {
    name: 'update',
    type: 'object',
    description: '更新參數',
  },
  {
    name: 'update.vector',
    type: 'number[]',
    isOptional: true,
    description: '要更新的新向量值',
  },
  {
    name: 'update.metadata',
    type: 'Record<string, any>',
    isOptional: true,
    description: '要更新的新 metadata',
  },
]}
/>

### `deleteVector()` {/* #deletevector */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含該向量的 index 名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要刪除的向量 ID',
  },
]}
/>

### `deleteVectors()` {/* #deletevectors */}

按 ID 或 metadata 篩選條件刪除多個向量。必須提供 `ids` 或 `filter`，但不可同時提供兩者。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含要刪除向量的 index 名稱',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '要刪除的向量 ID 陣列（與 filter 互斥）',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '用於識別要刪除向量的 metadata 篩選條件（與 ids 互斥）',
  },
  {
    name: 'namespace',
    type: 'string',
    isOptional: true,
    description: '刪除操作所用的選填 namespace',
  },
]}
/>

## 回應類型 {/* #response-types */}

查詢結果會以下列格式傳回：

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
}
```

## 錯誤處理 {/* #error-handling */}

此 store 會拋出可供捕捉的型別化錯誤：

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

### 環境變數 {/* #environment-variables */}

必要的環境變數：

- `PINECONE_API_KEY`：你的 Pinecone API 金鑰

## 混合搜尋 {/* #hybrid-search */}

Pinecone 透過結合密集向量與稀疏向量支援混合搜尋。使用混合搜尋的方法如下：

1. 使用 `metric: 'dotproduct'` 建立 index
1. upsert 時，透過 `sparseVectors` 參數提供稀疏向量
1. 查詢時，透過 `sparseVector` 參數提供稀疏向量

## 相關內容 {/* #related */}

- [Metadata 篩選條件](../rag/metadata-filters)
