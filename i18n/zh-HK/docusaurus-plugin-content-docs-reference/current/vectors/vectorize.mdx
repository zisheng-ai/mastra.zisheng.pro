---
title: "參考：Cloudflare 向量儲存 | Vectors"
description: Mastra 中 CloudflareVector 類別的文件，該類別使用 Cloudflare Vectorize 提供向量搜尋。
packages:
  - "@mastra/vectorize"
---

# Cloudflare 向量儲存

CloudflareVector 類別使用 [Cloudflare Vectorize](https://developers.cloudflare.com/vectorize/) 提供向量搜尋。Cloudflare Vectorize 是整合至 Cloudflare 邊緣網絡的向量資料庫服務。

## 建構函數選項

<PropertiesTable
  content={[
  {
    name: 'accountId',
    type: 'string',
    description: 'Cloudflare 帳戶 ID',
  },
  {
    name: 'apiToken',
    type: 'string',
    description: '具備 Vectorize 權限的 Cloudflare API token',
  },
]}
/>

## 方法

### `createIndex()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要建立的索引名稱',
  },
  {
    name: 'dimension',
    type: 'number',
    description: '向量維度（必須與你的嵌入模型相符）',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: '相似度搜尋所用的距離度量（dotproduct 對應 dot-product）',
  },
]}
/>

### `upsert()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要執行 upsert 的索引名稱',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: '嵌入向量陣列',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: '每個向量的中繼資料',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '可選的向量 ID（如未提供則自動產生）',
  },
]}
/>

### `query()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要查詢的索引名稱',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: '用於尋找相似向量的查詢向量',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: '要傳回的結果數目',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '查詢所用的中繼資料篩選條件',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: '是否在結果中包含向量',
  },
]}
/>

### `listIndexes()`

以字串陣列形式傳回索引名稱。

### `describeIndex()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要描述的索引名稱',
  },
]}
/>

傳回：

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要刪除的索引名稱',
  },
]}
/>

### `createMetadataIndex()`

在中繼資料欄位上建立索引，以啟用篩選功能。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含該中繼資料欄位的索引名稱',
  },
  {
    name: 'propertyName',
    type: 'string',
    description: '要建立索引的中繼資料欄位名稱',
  },
  {
    name: 'indexType',
    type: "'string' | 'number' | 'boolean'",
    description: '中繼資料欄位的類型',
  },
]}
/>

### `deleteMetadataIndex()`

移除中繼資料欄位的索引。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含該中繼資料欄位的索引名稱',
  },
  {
    name: 'propertyName',
    type: 'string',
    description: '要移除索引的中繼資料欄位名稱',
  },
]}
/>

### `listMetadataIndexes()`

列出某個索引的所有中繼資料欄位索引。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要列出中繼資料索引的索引名稱',
  },
]}
/>

### `updateVector()`

更新索引內特定 ID 的向量或中繼資料。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含要更新 ID 的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要更新的向量或中繼資料的唯一識別碼',
  },
  {
    name: 'update',
    type: '{ vector?: number[]; metadata?: Record<string, any>; }',
    description: '包含要更新的向量及／或中繼資料的物件',
  },
]}
/>

### `deleteVector()`

刪除索引內特定 ID 的向量及其相關中繼資料。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含要刪除 ID 的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要刪除的向量及中繼資料的唯一識別碼',
  },
]}
/>

## 回應類型

查詢結果會以以下格式傳回：

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[]
}
```

## 錯誤處理

此儲存會拋出可捕捉的具類型錯誤：

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

## 環境變數

必要的環境變數：

- `CLOUDFLARE_ACCOUNT_ID`：你的 Cloudflare 帳戶 ID
- `CLOUDFLARE_API_TOKEN`：具備 Vectorize 權限的 Cloudflare API token

## 相關內容

- [中繼資料篩選條件](../rag/metadata-filters)
