---
title: "參考：Astra 向量儲存 | Vectors"
description: Mastra 中 AstraVector 類別的文檔，提供使用 DataStax Astra DB 的向量搜尋功能。
packages:
  - "@mastra/astra"
---

# Astra 向量儲存

AstraVector 類別使用 [DataStax Astra DB](https://www.datastax.com/products/datastax-astra) 提供向量搜尋功能。DataStax Astra DB 是建基於 Apache Cassandra 的雲端原生無伺服器數據庫。
它提供企業級容量及高可用性的向量搜尋功能。

## 建構函數選項

<PropertiesTable
  content={[
  {
    name: 'token',
    type: 'string',
    description: 'Astra DB API 金鑰',
  },
  {
    name: 'endpoint',
    type: 'string',
    description: 'Astra DB API 端點',
  },
  {
    name: 'keyspace',
    type: 'string',
    isOptional: true,
    description: '選填 keyspace 名稱',
  },
]}
/>

## 方法

### `createIndex()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要建立的索引名稱',
  },
  {
    name: 'dimension',
    type: 'number',
    description: '向量維度（必須與你的嵌入模型相符）',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: '相似度搜尋的距離度量（dotproduct 會對應至 dot_product）',
  },
]}
/>

### `upsert()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要 upsert 至其中的索引名稱',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: '嵌入向量陣列',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: '每個向量的 metadata',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '選填向量 ID（如未提供則自動產生）',
  },
]}
/>

### `query()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要查詢的索引名稱',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: '用於尋找相似向量的查詢向量',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: '要傳回的結果數目',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '查詢的 metadata 篩選條件',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: '是否在結果中包含向量',
  },
]}
/>

### `listIndexes()`

傳回由索引名稱字串組成的陣列。

### `describeIndex()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要描述的索引名稱',
  },
]}
/>

傳回：

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要刪除的索引名稱',
  },
]}
/>

### `updateVector()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含該向量的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要更新的向量 ID',
  },
  {
    name: 'update',
    type: 'object',
    description: '包含向量及／或 metadata 變更的更新物件',
    properties: [
      {
        name: 'vector',
        type: 'number[]',
        isOptional: true,
        description: '新的向量值',
      },
      {
        name: 'metadata',
        type: 'Record<string, any>',
        isOptional: true,
        description: '新的 metadata 值',
      },
    ],
  },
]}
/>

### `deleteVector()`

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含該向量的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要刪除的向量 ID',
  },
]}
/>

## 回應類型

查詢結果會以此格式傳回：

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
}
```

## 錯誤處理

此儲存會拋出可捕捉的具類型錯誤：

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

## 環境變數

所需環境變數：

- `ASTRA_DB_TOKEN`：你的 Astra DB API 金鑰
- `ASTRA_DB_ENDPOINT`：你的 Astra DB API 端點

## 相關內容

- [Metadata 篩選條件](../rag/metadata-filters)
