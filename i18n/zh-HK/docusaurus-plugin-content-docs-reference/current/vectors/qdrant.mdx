---
title: "參考：Qdrant 向量儲存 | 向量"
description: 將 Qdrant 與 Mastra 整合的文件。Qdrant 是用於管理向量及 payload 的向量相似度搜尋引擎。
packages:
  - "@mastra/qdrant"
---

# Qdrant 向量儲存

QdrantVector 類別使用向量相似度搜尋引擎 [Qdrant](https://qdrant.tech/) 提供向量搜尋。
它提供可直接用於生產環境的服務，並備有便捷 API，可儲存、搜尋及管理向量，亦支援額外 payload 及進階篩選功能。

## 建構函式選項 {/* #constructor-options */}

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: 'Qdrant 執行個體的 REST URL。例如：https://xyz-example.eu-central.aws.cloud.qdrant.io:6333',
  },
  {
    name: 'apiKey',
    type: 'string',
    description: '可選的 Qdrant API 金鑰',
  },
  {
    name: 'https',
    type: 'boolean',
    description: '設定連線時是否使用 TLS。建議使用。',
  },
]}
/>

## 方法 {/* #methods */}

### `createIndex()` {/* #createindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要建立的索引名稱',
  },
  {
    name: 'dimension',
    type: 'number',
    description: '向量維度（必須與 embedding 模型相符）。單向量集合必須提供。',
  },
  {
    name: 'metric',
    type: "'cosine' | 'euclidean' | 'dotproduct'",
    isOptional: true,
    defaultValue: 'cosine',
    description: '相似度搜尋所用的距離度量',
  },
  {
    name: 'namedVectors',
    type: "Record<string, { size: number; distance: 'cosine' | 'euclidean' | 'dotproduct' }>",
    isOptional: true,
    description: '具名向量空間的設定。提供此設定後，會建立包含多個具名向量欄位的集合。',
  },
]}
/>

#### 建立具名向量集合 {/* #creating-a-named-vectors-collection */}

```typescript
// Create a collection with multiple named vector spaces
await store.createIndex({
  indexName: 'multi_modal',
  dimension: 768, // fallback
  namedVectors: {
    text: { size: 768, distance: 'cosine' },
    image: { size: 512, distance: 'euclidean' },
  },
})
```

### `upsert()` {/* #upsert */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要 upsert 至的索引名稱',
  },
  {
    name: 'vectors',
    type: 'number[][]',
    description: 'embedding 向量陣列',
  },
  {
    name: 'metadata',
    type: 'Record<string, any>[]',
    isOptional: true,
    description: '每個向量的元資料',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '可選的向量 ID（如未提供則自動產生）',
  },
  {
    name: 'vectorName',
    type: 'string',
    isOptional: true,
    description: '使用具名向量時，要 upsert 至的向量空間名稱。',
  },
]}
/>

#### Upsert 至具名向量空間 {/* #upserting-into-named-vector-spaces */}

```typescript
// Upsert into the "text" vector space
await store.upsert({
  indexName: 'multi_modal',
  vectors: textEmbeddings,
  metadata: textMetadata,
  vectorName: 'text',
})

// Upsert into the "image" vector space
await store.upsert({
  indexName: 'multi_modal',
  vectors: imageEmbeddings,
  metadata: imageMetadata,
  vectorName: 'image',
})
```

### `query()` {/* #query */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要查詢的索引名稱',
  },
  {
    name: 'queryVector',
    type: 'number[]',
    description: '用來尋找相似向量的查詢向量',
  },
  {
    name: 'topK',
    type: 'number',
    isOptional: true,
    defaultValue: '10',
    description: '要傳回的結果數目',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '查詢所用的元資料篩選條件',
  },
  {
    name: 'includeVector',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'false',
    description: '結果中是否包含向量',
  },
  {
    name: 'using',
    type: 'string',
    isOptional: true,
    description: '使用具名向量時要查詢的向量欄位名稱。當集合包含多個具名向量欄位時使用此選項。',
  },
]}
/>

#### 具名向量 {/* #named-vectors */}

Qdrant 支援[每個集合包含多個向量](https://qdrant.tech/documentation/concepts/vectors/#named-vectors)，並為每個向量欄位指定名稱。使用 `using` 參數選擇要查詢的向量欄位：

```typescript
const results = await store.query({
  indexName: 'my_index',
  queryVector: embedding,
  topK: 10,
  using: 'title_embedding', // Query against a specific named vector
})
```

### `listIndexes()` {/* #listindexes */}

以字串陣列形式傳回索引名稱。

### `describeIndex()` {/* #describeindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要描述的索引名稱',
  },
]}
/>

傳回：

```typescript
interface IndexStats {
  dimension: number
  count: number
  metric: 'cosine' | 'euclidean' | 'dotproduct'
}
```

### `deleteIndex()` {/* #deleteindex */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要刪除的索引名稱',
  },
]}
/>

### `updateVector()` {/* #updatevector */}

按 ID 或元資料篩選條件更新單一向量。必須提供 `id` 或 `filter`，但不可同時提供兩者。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要更新的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    isOptional: true,
    description: '要更新向量的 ID（與 filter 互斥）',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '用於識別要更新向量的元資料篩選條件（與 id 互斥）',
  },
  {
    name: 'update',
    type: '{ vector?: number[]; metadata?: Record<string, any>; }',
    description: '包含要更新的向量及／或元資料的物件',
  },
]}
/>

更新指定索引中的向量及／或其元資料。如同時提供向量及元資料，兩者都會更新。只提供其中一個值時，則只更新該值。

### `deleteVector()` {/* #deletevector */}

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要從中刪除向量的索引名稱',
  },
  {
    name: 'id',
    type: 'string',
    description: '要刪除向量的 ID',
  },
]}
/>

按向量 ID 從指定索引刪除向量。

### `deleteVectors()` {/* #deletevectors */}

按 ID 或元資料篩選條件刪除多個向量。必須提供 `ids` 或 `filter`，但不可同時提供兩者。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '包含要刪除向量的索引名稱',
  },
  {
    name: 'ids',
    type: 'string[]',
    isOptional: true,
    description: '要刪除的向量 ID 陣列（與 filter 互斥）',
  },
  {
    name: 'filter',
    type: 'Record<string, any>',
    isOptional: true,
    description: '用於識別要刪除向量的元資料篩選條件（與 ids 互斥）',
  },
]}
/>

### `createPayloadIndex()` {/* #createpayloadindex */}

在集合欄位上建立 payload（元資料）索引，以便高效篩選。Qdrant Cloud 及任何設有 `strict_mode_config = true` 的 Qdrant 執行個體都**必須**建立此索引。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要在其上建立 payload 索引的集合名稱',
  },
  {
    name: 'fieldName',
    type: 'string',
    description: '要建立索引的 payload 欄位名稱',
  },
  {
    name: 'fieldSchema',
    type: "'keyword' | 'integer' | 'float' | 'geo' | 'text' | 'bool' | 'datetime' | 'uuid'",
    description: 'payload 欄位的 schema 類型',
  },
  {
    name: 'wait',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'true',
    description: '是否等待操作完成',
  },
]}
/>

```typescript
// Create a keyword index for filtering by source
await store.createPayloadIndex({
  indexName: 'my_index',
  fieldName: 'source',
  fieldSchema: 'keyword',
})

const results = await store.query({
  indexName: 'my_index',
  queryVector: queryVector,
  filter: { source: 'document-a' },
})
```

### `deletePayloadIndex()` {/* #deletepayloadindex */}

從集合欄位移除 payload 索引。

<PropertiesTable
  content={[
  {
    name: 'indexName',
    type: 'string',
    description: '要從中刪除 payload 索引的集合名稱',
  },
  {
    name: 'fieldName',
    type: 'string',
    description: '要刪除的 payload 欄位索引名稱',
  },
  {
    name: 'wait',
    type: 'boolean',
    isOptional: true,
    defaultValue: 'true',
    description: '是否等待操作完成',
  },
]}
/>

## 回應類型 {/* #response-types */}

查詢結果會以下列格式傳回：

```typescript
interface QueryResult {
  id: string
  score: number
  metadata: Record<string, any>
  vector?: number[] // Only included if includeVector is true
}
```

## 錯誤處理 {/* #error-handling */}

此儲存會擲出可被捕捉的具型別錯誤：

```typescript
try {
  await store.query({
    indexName: 'index_name',
    queryVector: queryVector,
  })
} catch (error) {
  if (error instanceof VectorStoreError) {
    console.log(error.code) // 'connection_failed' | 'invalid_dimension' | etc
    console.log(error.details) // Additional error context
  }
}
```

## 相關內容 {/* #related */}

- [元資料篩選條件](../rag/metadata-filters)
