---
title: "參考：Mastra.listGateways() | Core"
description: "列出所有已登錄的閘道"
packages:
  - "@mastra/core"
---

# Mastra.listGateways() {/* #mastralistgateways */}

傳回所有已登錄閘道的記錄，並以其登錄鍵作為索引。

## 使用範例 {/* #usage-example */}

```typescript
import { Mastra } from '@mastra/core'

const mastra = new Mastra({
  gateways: {
    myGateway: new MyCustomGateway(),
    anotherGateway: new AnotherGateway(),
  },
})

const gateways = mastra.listGateways()
console.log(Object.keys(gateways)) // ['myGateway', 'anotherGateway']

// Iterate over all gateways
for (const [key, gateway] of Object.entries(gateways)) {
  console.log(`${key}: ${gateway.name}`)
}
```

## 參數 {/* #parameters */}

無。

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'gateways',
    type: 'Record<string, MastraModelGateway>',
    description: '以登錄鍵作為索引的閘道實例記錄',
  },
]}
/>

## 相關內容 {/* #related */}

- [Mastra.getGateway()](/reference/core/getGateway)：按登錄鍵取得閘道
- [Mastra.getGatewayById()](/reference/core/getGatewayById)：按 ID 取得閘道
- [Mastra.addGateway()](/reference/core/addGateway)：加入閘道
- [MastraModelGateway](/reference/core/mastra-model-gateway)：閘道基礎類別
- [自訂閘道指南](/models/gateways/custom-gateways)：建立自訂閘道
