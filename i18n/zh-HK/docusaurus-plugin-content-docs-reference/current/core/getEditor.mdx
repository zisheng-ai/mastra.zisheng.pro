---
title: "參考：Mastra.getEditor() | Core"
description: "Mastra 中 `Mastra.getEditor()` 方法的文件，此方法擷取已配置的編輯器實例。"
packages:
  - "@mastra/core"
  - "@mastra/editor"
---

# Mastra.getEditor()

`.getEditor()` 方法用於擷取已在 Mastra 實例配置的編輯器實例。編輯器提供 CRUD 命名空間，讓你以程式方式管理已儲存的 Agent、提示區塊、MCP 用戶端、評分器、Skill 及 Workspace。

## 使用範例

```typescript
import { Mastra } from '@mastra/core'
import { MastraEditor } from '@mastra/editor'

const mastra = new Mastra({
  editor: new MastraEditor(),
})

const editor = mastra.getEditor()

const agent = await editor?.agent.create({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'Help customers with their questions.',
})
```

## 參數

此方法不接受任何參數。

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'editor',
    type: 'MastraEditor | undefined',
    description: '已配置的編輯器實例；如未配置編輯器，則為 undefined。',
  },
]}
/>

## 相關內容

- [編輯器](/docs/editor/overview)
- [MastraEditor 參考](/reference/editor/mastra-editor)
