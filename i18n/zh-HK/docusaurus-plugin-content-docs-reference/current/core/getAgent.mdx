---
title: "參考：Mastra.getAgent() | Core"
description: "Mastra 中 `Agent.getAgent()` 方法的文件，此方法按名稱擷取 Agent。"
packages:
  - "@mastra/core"
---

# Mastra.getAgent() {/* #mastragetagent */}

`.getAgent()` 方法用於擷取 Agent。此方法接受一個代表 Agent 名稱的 `string` 參數。

## 使用範例 {/* #usage-example */}

```typescript
mastra.getAgent('testAgent')
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'TAgentName extends keyof TAgents',
    description: '要擷取的 Agent 名稱。必須是 Mastra 配置中存在的有效 Agent 名稱。',
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'agent',
    type: 'TAgents[TAgentName]',
    description: '具有指定名稱的 Agent 實例。如找不到 Agent，便會拋出錯誤。',
  },
]}
/>

## 相關內容 {/* #related */}

- [Agent 概覽](/docs/agents/overview)
