---
title: "參考：Mastra.getTool() | Core"
description: "`Mastra.getTool()` 方法的文件，此方法按 Mastra 註冊鍵擷取 Tool。"
packages:
  - "@mastra/core"
---

# Mastra.getTool()

`.getTool()` 方法按註冊鍵從 Mastra 層級的登錄中擷取 Tool。

## 使用範例

以 `weather` 鍵註冊 Tool，然後使用該鍵擷取它。

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core/mastra'
import { weatherTool } from './tools/weather-tool'

export const mastra = new Mastra({
  tools: {
    weather: weatherTool,
  },
})

const tool = mastra.getTool('weather')
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'TToolName extends keyof TTools',
    description: 'Tool 在 Mastra `tools` 登錄中的註冊鍵。',
  },
]}
/>

## 相關內容

- [在 Agent 之間共享 Tool](/docs/agents/using-tools#share-tools-across-agents)
- [Mastra 類別](/reference/core/mastra-class)
- [Mastra.getToolById()](/reference/core/getToolById)
- [Mastra.listTools()](/reference/core/listTools)
