---
title: "參考：Mastra 類別 | Core"
description: "Mastra 中 `Mastra` 類別的文件；此類別是管理 Agent、Workflow、MCP 伺服器及伺服器端點的核心入口。"
packages:
  - "@mastra/core"
  - "@mastra/libsql"
  - "@mastra/loggers"
---

# Mastra 類別

`Mastra` 類別是所有 Mastra 應用程式的中央協調器，負責管理 Agent、Workflow、儲存空間、記錄、可觀測性等。一般而言，你會建立單一 `Mastra` 實例來協調應用程式。

你可以將 `Mastra` 視為頂層註冊表，用來註冊需要在整個應用程式中存取的 Agent、Workflow、Tool 及其他元件。

## 使用範例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { PinoLogger } from '@mastra/loggers'
import { LibSQLStore } from '@mastra/libsql'
import { weatherWorkflow } from './workflows/weather-workflow'
import { weatherAgent } from './agents/weather-agent'

export const mastra = new Mastra({
  workflows: { weatherWorkflow },
  agents: { weatherAgent },
  storage: new LibSQLStore({
    id: 'mastra-storage',
    url: ':memory:',
  }),
  logger: new PinoLogger({
    name: 'Mastra',
    level: 'info',
  }),
})
```

如要透過 Workflow 排程器自動傳送延後的通知記錄及通知摘要，請啟用排程通知派送：

```typescript title="src/mastra/index.ts"
export const mastra = new Mastra({
  agents: { supportAgent },
  storage,
  notifications: {
    dispatch: {
      enabled: true,
      cron: '*/1 * * * *',
      batchSize: 100,
    },
  },
})
```

`notifications.dispatch.enabled` 允許內部派送器 Workflow 按預設 cron `*/1 * * * *` 執行。派送器會從儲存空間讀取到期的通知記錄，按 `agentId`、`resourceId` 及 `threadId` 將摘要分組，然後透過 Agent thread runtime 發出信號。它並非面向使用者的入口點。派送排程（以及支援它的 Workflow 排程器）會在首次出現延後或摘要通知時才按需啟動，因此從不延後通知的應用程式完全不會執行排程器。

## 建構函式參數 {/* #constructor-parameters */}

有關所有可用設定選項的詳細文件，請參閱[設定參考](/reference/configuration)。

<PropertiesTable
  content={[
  {
    name: 'agents',
    type: 'Record<string, Agent>',
    description: '要註冊的 Agent 實例，以名稱作為鍵',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'tools',
    type: 'Record<string, ToolApi>',
    description:
      '要註冊的 Tool 實例。鍵是 [`getTool()`](/reference/core/getTool) 使用的註冊鍵，值則是 Tool 實例。使用 [`getToolById()`](/reference/core/getToolById) 按內在 ID 查找，並使用 [`listTools()`](/reference/core/listTools) 讀取註冊表。',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'storage',
    type: 'MastraCompositeStore',
    description: '用來保存資料的儲存引擎實例',
    isOptional: true,
  },
  {
    name: 'vectors',
    type: 'Record<string, MastraVector>',
    description: '向量儲存實例，用於語意搜尋及向量式 Tool（例如 Pinecone、PgVector 或 Qdrant）',
    isOptional: true,
  },
  {
    name: 'logger',
    type: 'Logger',
    description: '使用 new PinoLogger() 建立的記錄器實例',
    isOptional: true,
    defaultValue: 'INFO 層級的控制台記錄器',
  },
  {
    name: 'idGenerator',
    type: '(context?: IdGeneratorContext) => string',
    description:
      '自訂 ID 產生器函式。Agent、Workflow、記憶體及其他元件會用它產生唯一識別碼。它會接收可選的上下文，例如 idType、source、entityId 及 threadId，以支援可感知上下文的 ID 格式。',
    isOptional: true,
  },
  {
    name: 'workflows',
    type: 'Record<string, Workflow>',
    description: '要註冊的 Workflow。結構為鍵值組合，鍵是 Workflow 名稱，值是 Workflow 實例。',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'tts',
    type: 'Record<string, MastraVoice>',
    isOptional: true,
    description: '用於語音合成的文字轉語音 Provider',
  },
  {
    name: 'observability',
    type: 'ObservabilityEntrypoint',
    isOptional: true,
    description: '用於追蹤及監察的可觀測性設定',
  },
  {
    name: 'environment',
    type: 'string',
    isOptional: true,
    description:
      '部署環境名稱（例如 `production`、`staging`、`development`）。設定後，它會自動附加至所有可觀測性信號，讓你毋須在每次呼叫時傳入 `tracingOptions.metadata.environment`，亦可按環境篩選信號。如未設定，則回退至 `process.env.NODE_ENV`；若兩者均未設定，便維持 undefined。每次呼叫的 `tracingOptions.metadata.environment` 永遠優先。',
  },
  {
    name: 'deployer',
    type: 'MastraDeployer',
    isOptional: true,
    description: '用於管理部署的 MastraDeployer 實例。',
  },
  {
    name: 'server',
    type: 'ServerConfig',
    description:
      '伺服器設定，包括連接埠、主機、逾時、API 路由、middleware、CORS 設定，以及 Swagger UI、API 請求記錄和 OpenAPI 文件的建置選項。',
    isOptional: true,
  },
  {
    name: 'mcpServers',
    type: 'Record<string, MCPServerBase>',
    isOptional: true,
    description:
      '此物件的鍵是註冊鍵（供 getMCPServer() 使用），值是 MCPServer 實例或繼承 MCPServerBase 的類別。每個 MCPServer 都必須有 id 屬性。你可使用 getMCPServer() 按註冊鍵擷取伺服器，或使用 getMCPServerById() 按其內在 id 擷取。',
  },
  {
    name: 'bundler',
    type: 'BundlerConfig',
    description: '資產 bundler 的設定，提供 externals、sourcemap、transpilePackages 及 dynamicPackages 選項。',
    isOptional: true,
    defaultValue: '{ externals: [], sourcemap: false, transpilePackages: [], dynamicPackages: [] }',
  },
  {
    name: 'scorers',
    type: 'Record<string, Scorer>',
    description: '用於評估 Agent 回應及 Workflow 輸出的 Scorer',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'processors',
    type: 'Record<string, Processor>',
    description: '用於轉換 Agent 輸入及輸出的輸入／輸出 Processor',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'gateways',
    type: 'Record<string, MastraModelGateway>',
    description:
      '要註冊的自訂模型 gateway，用於透過替代 Provider 或私有部署存取 AI 模型。結構為鍵值組合，鍵是註冊鍵（供 getGateway() 使用），值是 gateway 實例。',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'memory',
    type: 'Record<string, MastraMemory>',
    description:
      '要註冊的記憶體實例。已儲存的 Agent 可以參照這些實例，並在 runtime 解析。結構為鍵值組合，鍵是註冊鍵，值是記憶體實例。',
    isOptional: true,
    defaultValue: '{}',
  },
  {
    name: 'notifications',
    type: 'object',
    description: '通知信號派送的 runtime 設定。',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'dispatch',
            type: 'NotificationDispatchConfig',
            isOptional: true,
            description: '延後通知及通知摘要的排程派送設定。派送預設為啟用。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'enabled',
                    type: 'boolean',
                    isOptional: true,
                    defaultValue: 'true',
                    description: '設定為 `false`，即可停用自動排程通知派送。',
                  },
                  {
                    name: 'cron',
                    type: 'string',
                    isOptional: true,
                    defaultValue: '`*/1 * * * *`',
                    description: '內部通知派送器 Workflow 使用的 Cron 排程。',
                  },
                  {
                    name: 'batchSize',
                    type: 'number',
                    isOptional: true,
                    defaultValue: '100',
                    description: '每次派送執行最多可處理的到期通知記錄數目。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'versions',
    type: 'VersionOverrides',
    description:
      '子 Agent 委派的全域版本覆寫。當 supervisor Agent 委派工作給子 Agent 時，這些覆寫會決定使用該子 Agent 的哪個已儲存版本，而非程式碼所定義的預設版本。必須設定 editor package。詳情請參閱 [Editor 版本控制](/docs/editor/overview#versioning)。',
    isOptional: true,
    properties: [
      {
        type: 'VersionOverrides',
        parameters: [
          {
            name: 'agents',
            type: 'Record<string, VersionSelector>',
            isOptional: true,
            description: 'Agent ID 至其版本選擇器的對應表。每個選擇器可按 ID 或發佈狀態指定特定版本。',
            properties: [
              {
                type: 'VersionSelector',
                parameters: [
                  {
                    name: 'versionId',
                    type: 'string',
                    isOptional: true,
                    description: '要使用的特定版本 ID。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'status',
                    type: "'draft' | 'published'",
                    isOptional: true,
                    description: '選取具有此發佈狀態的最新版本。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'workers',
    type: 'MastraWorker[] | false',
    description:
      '設定哪些 [worker](/docs/deployment/workers) 在此 Mastra 實例中執行。如省略，Mastra 會根據你的 PubSub 及設定自動建立預設 worker。傳入 `false` 可停用所有事件處理（適合另外執行獨立 worker 時使用）。傳入 `MastraWorker[]` 可加入自訂 worker——它們會與自動建立的預設 worker 合併，而與預設 worker 具有相同 `name` 的自訂 worker 會取代該預設 worker。',
    isOptional: true,
  },
  {
    name: 'backgroundTasks',
    type: 'BackgroundTaskManagerConfig',
    description:
      '設定 Agent 的[背景工作](/docs/long-running-agents/background-tasks)執行方式。所有選項請參閱[背景工作設定參考](/reference/configuration#backgroundtasks)。',
    isOptional: true,
    properties: [
      {
        type: 'BackgroundTaskManagerConfig',
        parameters: [
          {
            name: 'enabled',
            type: 'boolean',
            isOptional: true,
            defaultValue: 'false',
            description: '啟用背景工作派送。',
          },
          {
            name: 'globalConcurrency',
            type: 'number',
            isOptional: true,
            defaultValue: '10',
            description: '所有 Agent 合計可同時執行的工作數目上限。',
          },
          {
            name: 'perAgentConcurrency',
            type: 'number',
            isOptional: true,
            defaultValue: '5',
            description: '每個 Agent 可同時執行的工作數目上限。',
          },
          {
            name: 'backpressure',
            type: "'queue' | 'reject' | 'fallback-sync'",
            isOptional: true,
            defaultValue: "'queue'",
            description: '達到並行處理上限時的行為。',
          },
          {
            name: 'defaultTimeoutMs',
            type: 'number',
            isOptional: true,
            defaultValue: '300000',
            description: '預設工作逾時（毫秒）。',
          },
          {
            name: 'defaultRetries',
            type: 'RetryConfig',
            isOptional: true,
            description: '預設重試設定。',
          },
        ],
      },
    ],
  },
  {
    name: 'scheduler',
    type: 'object',
    description:
      '設定用於 cron 驅動 Workflow 觸發程序的[排程器 worker](/docs/deployment/workers#scheduler-worker)。任何 Workflow 宣告 `schedule` 時便會自動啟用。請參閱[排程 Workflow](/docs/workflows/scheduled-workflows)。',
    isOptional: true,
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'enabled',
            type: 'boolean',
            isOptional: true,
            description: '明確啟用或停用排程器。',
          },
        ],
      },
    ],
  },
  {
    name: 'recovery',
    type: 'MastraRecoveryConfig',
    description:
      '啟動時復原孤立 Agent 及 Workflow 執行的行為。請參閱[崩潰復原](/docs/long-running-agents/durable-agents#crash-recovery)。',
    isOptional: true,
    defaultValue: "{ durableAgents: 'off' }",
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'durableAgents',
            type: "'auto' | 'off'",
            isOptional: true,
            defaultValue: "'off'",
            description:
              "設定為 `'auto'`，可在伺服器啟動時自動重新驅動孤立且處於 RUNNING 狀態的 durable Agent 執行。復原會再次發出 LLM 呼叫並重新執行 Tool 呼叫，因此 Tool 必須具備冪等性。請參閱[崩潰復原](/docs/long-running-agents/durable-agents#crash-recovery)。",
          },
        ],
      },
    ],
  },
]}
/>

## 方法 {/* #methods */}

### `recoverAllDurableAgents()` {/* #recoveralldurableagents */}

重新驅動所有已註冊 durable Agent 中每個孤立且處於 `running` 狀態的 durable Agent 執行。當 `recovery.durableAgents` 為 `'auto'` 時，系統會在啟動時自動呼叫此方法。你亦可直接呼叫它，以手動復原或從排程工作執行復原。

此方法需要持久儲存空間。如使用記憶體內儲存空間，程序重新啟動後便沒有內容可供復原。

```typescript
const result = await mastra.recoverAllDurableAgents()
// { agents: 2, recovered: 3, succeeded: 3, failed: 0 }
```

傳回：

<PropertiesTable
  content={[
  {
    name: 'agents',
    type: 'number',
    description: '已掃描的 durable Agent 數目。',
  },
  {
    name: 'recovered',
    type: 'number',
    description: '已重新驅動的執行總數。',
  },
  {
    name: 'succeeded',
    type: 'number',
    description: '成功重新啟動的執行數目。',
  },
  {
    name: 'failed',
    type: 'number',
    description: '重新啟動時擲回錯誤的執行數目。',
  },
]}
/>
