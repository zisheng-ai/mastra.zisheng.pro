---
title: "參考：ModerationProcessor | Processors"
description: "Mastra 中 ModerationProcessor 的文件；此 processor 使用 LLM 偵測多個類別的不當內容，從而提供內容審核。"
packages:
  - "@mastra/core"
---

# ModerationProcessor

`ModerationProcessor` 是一個**混合 processor**，可同時用於輸入及輸出處理，並使用 LLM 偵測多個類別的不當內容，從而提供內容審核。此 processor 會根據可設定的審核類別評估訊息，並以靈活的策略處理被標記的內容，有助維持內容安全。

## 使用範例 {/* #usage-example */}

```typescript
import { ModerationProcessor } from '@mastra/core/processors'

const processor = new ModerationProcessor({
  model: 'openrouter/openai/gpt-oss-safeguard-20b',
  threshold: 0.7,
  strategy: 'block',
  categories: ['hate', 'harassment', 'violence'],
  lastMessageOnly: true,
})
```

## Constructor 參數 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'Options',
    description: '內容審核的設定選項',
    isOptional: false,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'model',
            type: 'MastraModelConfig',
            description: '審核 Agent 的模型設定',
            isOptional: false,
          },
          {
            name: 'categories',
            type: 'string[]',
            description: '要檢查的審核類別。如未指定，則使用預設 OpenAI 類別',
            isOptional: true,
            default:
              "['hate', 'hate/threatening', 'harassment', 'harassment/threatening', 'self-harm', 'self-harm/intent', 'self-harm/instructions', 'sexual', 'sexual/minors', 'violence', 'violence/graphic']",
          },
          {
            name: 'threshold',
            type: 'number',
            description: '標記內容的信心門檻（0 至 1）。如任何類別的分數超過此門檻，內容便會被標記',
            isOptional: true,
            default: '0.5',
          },
          {
            name: 'strategy',
            type: "'block' | 'warn' | 'filter'",
            description:
              "內容被標記時採用的策略：'block' 會傳回錯誤並拒絕內容，'warn' 會記錄警告但讓內容通過，'filter' 會移除被標記的訊息",
            isOptional: true,
            default: "'block'",
          },
          {
            name: 'instructions',
            type: 'string',
            description: 'Agent 的自訂審核指示。如未提供，則使用根據類別制定的預設指示',
            isOptional: true,
            default: 'undefined',
          },
          {
            name: 'includeScores',
            type: 'boolean',
            description: '是否在 log 中包含信心分數。適用於調整門檻及偵錯',
            isOptional: true,
            default: 'false',
          },
          {
            name: 'lastMessageOnly',
            type: 'boolean',
            description:
              '是否只對批次中最新的訊息執行審核，而非檢查每一則訊息。對話較長時，可使用此選項避免為每一則較早的訊息額外呼叫一次 LLM。',
            isOptional: true,
            default: 'false',
          },
          {
            name: 'chunkWindow',
            type: 'number',
            description:
              '審核串流 chunk 時，要加入作為 context 的先前 chunk 數目。如設為 1，則會包含上一個部分，如此類推。',
            isOptional: true,
            default: '0（沒有 context window）',
          },
          {
            name: 'providerOptions',
            type: 'ProviderOptions',
            description:
              "傳遞給內部審核 Agent 的 Provider 特有選項。可用來控制模型行為，例如推理模型的 reasoning effort（例如 `{ openai: { reasoningEffort: 'low' } }`）",
            isOptional: true,
            default: 'undefined',
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "Processor 標識符，設為 'moderation'",
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: '可選的 processor 顯示名稱',
    isOptional: true,
  },
  {
    name: 'processInput',
    type: '(args: { messages: MastraDBMessage[]; abort: (reason?: string) => never; tracingContext?: TracingContext }) => Promise<MastraDBMessage[]>',
    description: '在將輸入訊息傳送給 LLM 前處理訊息，以審核內容',
    isOptional: false,
  },
  {
    name: 'processOutputStream',
    type: '(args: { part: ChunkType; streamParts: ChunkType[]; state: Record<string, any>; abort: (reason?: string) => never; tracingContext?: TracingContext }) => Promise<ChunkType | null | undefined>',
    description: '在串流期間處理串流輸出的各個部分，以審核內容',
    isOptional: false,
  },
]}
/>

## 進階使用範例 {/* #extended-usage-example */}

### 輸入處理 {/* #input-processing */}

```typescript title="src/mastra/agents/moderated-agent.ts"
import { Agent } from '@mastra/core/agent'
import { ModerationProcessor } from '@mastra/core/processors'

export const agent = new Agent({
  id: 'moderated-agent',
  name: 'moderated-agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
  inputProcessors: [
    new ModerationProcessor({
      model: 'openrouter/openai/gpt-oss-safeguard-20b',
      categories: ['hate', 'harassment', 'violence'],
      threshold: 0.7,
      strategy: 'block',
      instructions: 'Detect and flag inappropriate content in user messages',
      includeScores: true,
    }),
  ],
})
```

### 使用批次處理的輸出處理 {/* #output-processing-with-batching */}

將 `ModerationProcessor` 用作輸出 processor 時，建議配合 `BatchPartsProcessor` 使用，以改善效能。`BatchPartsProcessor` 會先將串流 chunk 組成批次，再傳遞給審核器，從而減少審核所需的 LLM 呼叫次數。

```typescript title="src/mastra/agents/output-moderated-agent.ts"
import { Agent } from '@mastra/core/agent'
import { BatchPartsProcessor, ModerationProcessor } from '@mastra/core/processors'

export const agent = new Agent({
  id: 'output-moderated-agent',
  name: 'output-moderated-agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
  outputProcessors: [
    // Batch stream parts first to reduce LLM calls
    new BatchPartsProcessor({
      batchSize: 10,
    }),
    // Then apply moderation on batched content
    new ModerationProcessor({
      model: 'openrouter/openai/gpt-oss-safeguard-20b',
      strategy: 'filter',
      chunkWindow: 1,
    }),
  ],
})
```

## 相關內容 {/* #related */}

- [Guardrails](/docs/agents/guardrails)
