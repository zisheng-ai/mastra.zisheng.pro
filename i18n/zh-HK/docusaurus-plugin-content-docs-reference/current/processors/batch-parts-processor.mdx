---
title: "參考：BatchPartsProcessor | Processors"
description: "Mastra 中 BatchPartsProcessor 的文件；此 Processor 會將多個串流部分批次合併，以減少發送頻率。"
packages:
  - "@mastra/core"
---

# BatchPartsProcessor

`BatchPartsProcessor` 是一個 **output processor**，會將多個串流部分批次合併，以減少串流期間的發送頻率。此 Processor 可減少網絡開銷、透過整合細小的文字區塊改善用戶體驗，並藉由控制何時向用戶端發送各部分來最佳化串流效能。

## 使用範例

```typescript
import { BatchPartsProcessor } from '@mastra/core/processors'

const processor = new BatchPartsProcessor({
  batchSize: 5,
  maxWaitTime: 100,
  emitOnNonText: true,
})
```

## 建構函數參數

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'Options',
    description: '批次處理串流部分的設定選項',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'batchSize',
            type: 'number',
            description: '發送前要合併成一個批次的部分數目',
            isOptional: true,
            default: '5',
          },
          {
            name: 'maxWaitTime',
            type: 'number',
            description:
              '發送批次前的最長等候時間（以毫秒為單位）。如已設定，即使目前批次尚未達到 batchSize，亦會將其發送',
            isOptional: true,
            default: 'undefined (沒有逾時限制)',
          },
          {
            name: 'emitOnNonText',
            type: 'boolean',
            description: '遇到非文字部分時是否立即發送',
            isOptional: true,
            default: 'true',
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "Processor 識別符，設定為 'batch-parts'",
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: '可選的 Processor 顯示名稱',
    isOptional: true,
  },
  {
    name: 'processOutputStream',
    type: '(args: { part: ChunkType; streamParts: ChunkType[]; state: Record<string, any>; abort: (reason?: string) => never }) => Promise<ChunkType | null>',
    description: '處理串流輸出部分，將它們批次合併',
    isOptional: false,
  },
  {
    name: 'flush',
    type: '(state?: BatchPartsState) => ChunkType | null',
    description: '串流結束時，強制發送所有剩餘的已批次合併部分',
    isOptional: false,
  },
]}
/>

## 進階使用範例

```typescript title="src/mastra/agents/batched-agent.ts"
import { Agent } from '@mastra/core/agent'
import { BatchPartsProcessor } from '@mastra/core/processors'

export const agent = new Agent({
  id: 'batched-agent',
  name: 'batched-agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
  outputProcessors: [
    new BatchPartsProcessor({
      batchSize: 5,
      maxWaitTime: 100,
      emitOnNonText: true,
    }),
  ],
})
```

## 相關內容

- [Guardrails](/docs/agents/guardrails)
