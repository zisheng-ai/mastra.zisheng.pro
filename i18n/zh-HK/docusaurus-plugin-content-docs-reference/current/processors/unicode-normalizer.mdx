---
title: "參考：UnicodeNormalizer | Processors"
description: "Mastra 中 UnicodeNormalizer 的文件，用於正規化 Unicode 文字，以確保格式一致並移除可能引致問題的字元。"
packages:
  - "@mastra/core"
---

# UnicodeNormalizer

`UnicodeNormalizer` 是一個**輸入 processor**，可在訊息傳送至語言模型前正規化 Unicode 文字，以確保格式一致並移除可能引致問題的字元。此 processor 會處理不同的 Unicode 表示方式並移除控制字元，亦會統一空白格式。

## 使用範例

```typescript
import { UnicodeNormalizer } from '@mastra/core/processors'

const processor = new UnicodeNormalizer({
  stripControlChars: true,
  collapseWhitespace: true,
})
```

## Constructor 參數

<PropertiesTable
  content={[
  {
    name: 'options',
    type: 'Options',
    description: 'Unicode 文字正規化的設定選項',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'stripControlChars',
            type: 'boolean',
            description:
              '是否移除控制字元。啟用後，會移除 `\t`、`\n`、`\r` 以外的控制字元',
            isOptional: true,
            default: 'false',
          },
          {
            name: 'preserveEmojis',
            type: 'boolean',
            description:
              '是否保留 emoji。停用後，如 emoji 包含控制字元，便可能會被移除',
            isOptional: true,
            default: 'true',
          },
          {
            name: 'collapseWhitespace',
            type: 'boolean',
            description:
              '是否合併連續空白。啟用後，多個空格、Tab 或換行會分別合併為一個',
            isOptional: true,
            default: 'true',
          },
          {
            name: 'trim',
            type: 'boolean',
            description: '是否移除開頭及結尾的空白',
            isOptional: true,
            default: 'true',
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: "Processor 識別碼，設為 'unicode-normalizer'",
    isOptional: false,
  },
  {
    name: 'name',
    type: 'string',
    description: '可選的 processor 顯示名稱',
    isOptional: true,
  },
  {
    name: 'processInput',
    type: '(args: { messages: MastraDBMessage[]; abort: (reason?: string) => never }) => MastraDBMessage[]',
    description: '處理輸入訊息以正規化 Unicode 文字',
    isOptional: false,
  },
]}
/>

## 延伸使用範例

```typescript title="src/mastra/agents/normalized-agent.ts"
import { Agent } from '@mastra/core/agent'
import { UnicodeNormalizer } from '@mastra/core/processors'

export const agent = new Agent({
  id: 'normalized-agent',
  name: 'normalized-agent',
  instructions: 'You are a helpful assistant',
  model: '__GATEWAY_OPENAI_MODEL__',
  inputProcessors: [
    new UnicodeNormalizer({
      stripControlChars: true,
      preserveEmojis: true,
      collapseWhitespace: true,
      trim: true,
    }),
  ],
})
```

## 相關內容

- [Guardrails](/docs/agents/guardrails)
