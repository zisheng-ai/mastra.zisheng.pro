---
title: "參考：MastraAuthSupabase 類別 | 驗證"
description: "MastraAuthSupabase 類別的 API 參考，此類別使用 Supabase Auth 驗證 Mastra 應用程式。"
packages:
  - "@mastra/auth-supabase"
  - "@mastra/core"
---

# MastraAuthSupabase 類別

`MastraAuthSupabase` 類別使用 Supabase Auth 為 Mastra 提供驗證功能。此類別使用 Supabase 的驗證系統來驗證傳入的請求，並透過 `auth` 選項與 Mastra 伺服器整合。

## 使用範例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraAuthSupabase } from '@mastra/auth-supabase'

export const mastra = new Mastra({
  server: {
    auth: new MastraAuthSupabase({
      url: process.env.SUPABASE_URL,
      anonKey: process.env.SUPABASE_ANON_KEY,
    }),
  },
})
```

## 建構函數參數 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'url',
    type: 'string',
    description: '你的 Supabase 項目 URL，可在 Supabase 項目設定中找到。',
    isOptional: true,
    defaultValue: 'process.env.SUPABASE_URL',
  },
  {
    name: 'anonKey',
    type: 'string',
    description: '你的 Supabase 項目匿名／公開金鑰，用於客戶端驗證。',
    isOptional: true,
    defaultValue: 'process.env.SUPABASE_ANON_KEY',
  },
  {
    name: 'name',
    type: 'string',
    description: '驗證 Provider 執行個體的自訂名稱。',
    isOptional: true,
  },
  {
    name: 'authorizeUser',
    type: '(user: User, request: HoneRequest) => Promise<boolean> | boolean',
    description:
      "用於判斷是否應授予使用者存取權限的自訂授權函數，會在驗證 token 後呼叫。預設會檢查 'users' 資料表中的 'isAdmin' 欄。",
    isOptional: true,
  },
]}
/>

## 相關內容 {/* #related */}

[MastraAuthSupabase](/docs/server/auth/supabase)
