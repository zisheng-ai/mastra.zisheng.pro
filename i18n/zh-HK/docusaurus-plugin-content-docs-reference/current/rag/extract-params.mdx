---
title: "參考：ExtractParams | RAG"
description: Mastra 中繼資料擷取設定的說明文件。
packages:
  - "@mastra/rag"
---

# ExtractParams {/* #extractparams */}

ExtractParams 使用 LLM 分析，設定從文件區塊擷取中繼資料的方式。

## 範例 {/* #example */}

```typescript
import { MDocument } from '@mastra/rag'

const doc = MDocument.fromText(text)
const chunks = await doc.chunk({
  extract: {
    title: true, // Extract titles using default settings
    summary: true, // Generate summaries using default settings
    keywords: true, // Extract keywords using default settings
  },
})

// Example output:
// chunks[0].metadata = {
//   documentTitle: "AI Systems Overview",
//   sectionSummary: "Overview of artificial intelligence concepts and applications",
//   excerptKeywords: "KEYWORDS: AI, machine learning, algorithms"
// }
```

## 參數 {/* #parameters */}

`extract` 參數接受以下欄位：

<PropertiesTable
  content={[
  {
    name: 'title',
    type: 'boolean | TitleExtractorsArgs',
    isOptional: true,
    description: '啟用標題擷取。設為 true 可使用預設設定，亦可提供自訂設定。',
  },
  {
    name: 'summary',
    type: 'boolean | SummaryExtractArgs',
    isOptional: true,
    description: '啟用摘要擷取。設為 true 可使用預設設定，亦可提供自訂設定。',
  },
  {
    name: 'questions',
    type: 'boolean | QuestionAnswerExtractArgs',
    isOptional: true,
    description: '啟用問題產生功能。設為 true 可使用預設設定，亦可提供自訂設定。',
  },
  {
    name: 'keywords',
    type: 'boolean | KeywordExtractArgs',
    isOptional: true,
    description: '啟用關鍵字擷取。設為 true 可使用預設設定，亦可提供自訂設定。',
  },
  {
    name: 'schema',
    type: 'SchemaExtractArgs',
    isOptional: true,
    description: '使用 Zod schema 啟用結構化中繼資料擷取。',
  },
]}
/>

## 擷取器參數 {/* #extractor-arguments */}

### `TitleExtractorsArgs` {/* #titleextractorsargs */}

<PropertiesTable
  content={[
  {
    name: 'llm',
    type: 'MastraLanguageModel',
    isOptional: true,
    description: '用於擷取標題的 AI SDK 語言模型',
  },
  {
    name: 'nodes',
    type: 'number',
    isOptional: true,
    description: '要擷取的標題節點數目',
  },
  {
    name: 'nodeTemplate',
    type: 'string',
    isOptional: true,
    description: '用於擷取標題節點的自訂提示詞範本。必須包含 {context} 預留位置',
  },
  {
    name: 'combineTemplate',
    type: 'string',
    isOptional: true,
    description: '用於合併標題的自訂提示詞範本。必須包含 {context} 預留位置',
  },
]}
/>

### `SummaryExtractArgs` {/* #summaryextractargs */}

<PropertiesTable
  content={[
  {
    name: 'llm',
    type: 'MastraLanguageModel',
    isOptional: true,
    description: '用於擷取摘要的 AI SDK 語言模型',
  },
  {
    name: 'summaries',
    type: "('self' | 'prev' | 'next')[]",
    isOptional: true,
    description: "要產生的摘要類型清單。只可包含 'self'（目前區塊）、'prev'（上一個區塊）或 'next'（下一個區塊）",
  },
  {
    name: 'promptTemplate',
    type: 'string',
    isOptional: true,
    description: '用於產生摘要的自訂提示詞範本。必須包含 {context} 預留位置',
  },
]}
/>

### `QuestionAnswerExtractArgs` {/* #questionanswerextractargs */}

<PropertiesTable
  content={[
  {
    name: 'llm',
    type: 'MastraLanguageModel',
    isOptional: true,
    description: '用於產生問題的 AI SDK 語言模型',
  },
  {
    name: 'questions',
    type: 'number',
    isOptional: true,
    description: '要產生的問題數目',
  },
  {
    name: 'promptTemplate',
    type: 'string',
    isOptional: true,
    description: '用於產生問題的自訂提示詞範本。必須同時包含 {context} 及 {numQuestions} 預留位置',
  },
  {
    name: 'embeddingOnly',
    type: 'boolean',
    isOptional: true,
    description: '若為 true，只產生 embedding，不會產生實際問題',
  },
]}
/>

### `KeywordExtractArgs` {/* #keywordextractargs */}

<PropertiesTable
  content={[
  {
    name: 'llm',
    type: 'MastraLanguageModel',
    isOptional: true,
    description: '用於擷取關鍵字的 AI SDK 語言模型',
  },
  {
    name: 'keywords',
    type: 'number',
    isOptional: true,
    description: '要擷取的關鍵字數目',
  },
  {
    name: 'promptTemplate',
    type: 'string',
    isOptional: true,
    description: '用於擷取關鍵字的自訂提示詞範本。必須同時包含 {context} 及 {maxKeywords} 預留位置',
  },
]}
/>

### `SchemaExtractArgs` {/* #schemaextractargs */}

<PropertiesTable
  content={[
  {
    name: 'schema',
    type: 'ZodType',
    isOptional: false,
    description: '定義待擷取資料結構的 Zod schema。',
  },
  {
    name: 'llm',
    type: 'MastraLanguageModel',
    isOptional: true,
    description: '用於擷取的 AI SDK 語言模型。',
  },
  {
    name: 'instructions',
    type: 'string',
    isOptional: true,
    description: '指示 LLM 要擷取哪些內容。',
  },
  {
    name: 'metadataKey',
    type: 'string',
    isOptional: true,
    description: '用作巢狀存放擷取結果的鍵。若省略，結果會展開至中繼資料物件內。',
  },
]}
/>

## 進階範例 {/* #advanced-example */}

```typescript
import { MDocument } from '@mastra/rag'

const doc = MDocument.fromText(text)
const chunks = await doc.chunk({
  extract: {
    // Title extraction with custom settings
    title: {
      nodes: 2, // Extract 2 title nodes
      nodeTemplate: 'Generate a title for this: {context}',
      combineTemplate: 'Combine these titles: {context}',
    },

    // Summary extraction with custom settings
    summary: {
      summaries: ['self'], // Generate summaries for current chunk
      promptTemplate: 'Summarize this: {context}',
    },

    // Question generation with custom settings
    questions: {
      questions: 3, // Generate 3 questions
      promptTemplate: 'Generate {numQuestions} questions about: {context}',
      embeddingOnly: false,
    },

    // Keyword extraction with custom settings
    keywords: {
      keywords: 5, // Extract 5 keywords
      promptTemplate: 'Extract {maxKeywords} key terms from: {context}',
    },

    // Schema extraction with Zod
    schema: {
      schema: z.object({
        productName: z.string(),
        category: z.enum(['electronics', 'clothing']),
      }),
      instructions: 'Extract product information.',
      metadataKey: 'product',
    },
  },
})

// Example output:
// chunks[0].metadata = {
//   documentTitle: "AI in Modern Computing",
//   sectionSummary: "Overview of AI concepts and their applications in computing",
//   questionsThisExcerptCanAnswer: "1. What is machine learning?\n2. How do neural networks work?",
//   excerptKeywords: "1. Machine learning\n2. Neural networks\n3. Training data",
//   product: {
//     productName: "Neural Net 2000",
//     category: "electronics"
//   }
// }
```

## 將文件分組以擷取標題 {/* #document-grouping-for-title-extraction */}

使用 `TitleExtractor` 時，你可以在每個區塊的 `metadata` 欄位指定共用的 `docId`，將多個區塊分組以擷取標題。所有具有相同 `docId` 的區塊都會獲得相同的擷取標題。若未設定 `docId`，擷取標題時每個區塊都會視為獨立文件。

**範例：**

```ts
import { MDocument } from '@mastra/rag'

const doc = new MDocument({
  docs: [
    { text: 'chunk 1', metadata: { docId: 'docA' } },
    { text: 'chunk 2', metadata: { docId: 'docA' } },
    { text: 'chunk 3', metadata: { docId: 'docB' } },
  ],
  type: 'text',
})

await doc.extractMetadata({ title: true })
// The first two chunks will share a title, while the third chunk will be assigned a separate title.
```
