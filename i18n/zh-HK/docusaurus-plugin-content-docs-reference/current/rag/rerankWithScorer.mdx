---
title: "參考：rerankWithScorer() | RAG"
description: Mastra rerank 函數的文件，此函數為向量搜尋結果提供進階重新排序功能。
packages:
  - "@mastra/rag"
---

# rerankWithScorer()

`rerankWithScorer()` 函數結合語意相關性、向量相似度及按位置評分，為向量搜尋結果提供進階重新排序功能。

```typescript prettier:false
function rerankWithScorer({
  results: QueryResult[],
  query: string,
  scorer: RelevanceScoreProvider,
  options?: RerankerFunctionOptions,
}): Promise<RerankResult[]>;
```

## 使用範例

```typescript
import { rerankWithScorer as rerank, CohereRelevanceScorer } from '@mastra/rag'

const scorer = new CohereRelevanceScorer('rerank-v3.5')

const rerankedResults = await rerank({
  results: vectorSearchResults,
  query: 'How do I deploy to production?',
  scorer,
  options: {
    weights: {
      semantic: 0.5,
      vector: 0.3,
      position: 0.2,
    },
    topK: 3,
  },
})
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'results',
    type: 'QueryResult[]',
    description: '要重新排序的向量搜尋結果',
    isOptional: false,
  },
  {
    name: 'query',
    type: 'string',
    description: '用於評估相關性的搜尋查詢文字',
    isOptional: false,
  },
  {
    name: 'scorer',
    type: 'RelevanceScoreProvider',
    description: '用於重新排序的相關性評分器',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'RerankerFunctionOptions',
    description: '重新排序模型的選項',
    isOptional: true,
    properties: [
      {
        type: 'RerankerFunctionOptions',
        parameters: [
          {
            name: 'weights',
            type: 'WeightConfig',
            description: '不同評分組成部分的權重（總和必須為 1）',
            isOptional: true,
            properties: [
              {
                type: 'number',
                parameters: [
                  {
                    name: 'semantic',
                    description: '語意相關性的權重',
                    isOptional: true,
                    type: 'number (default: 0.4)',
                  },
                ],
              },
              {
                type: 'number',
                parameters: [
                  {
                    name: 'vector',
                    description: '向量相似度的權重',
                    isOptional: true,
                    type: 'number (default: 0.4)',
                  },
                ],
              },
              {
                type: 'number',
                parameters: [
                  {
                    name: 'position',
                    description: '按位置評分的權重',
                    isOptional: true,
                    type: 'number (default: 0.2)',
                  },
                ],
              },
            ],
          },
          {
            name: 'queryEmbedding',
            type: 'number[]',
            description: '查詢的 embedding',
            isOptional: true,
          },
          {
            name: 'topK',
            type: 'number',
            description: '要傳回的最高排名結果數目',
            isOptional: true,
            defaultValue: '3',
          },
        ],
      },
    ],
  },
]}
/>

`rerankWithScorer` 函數接受 @mastra/rag 的任何 `RelevanceScoreProvider`。

:::note

要讓語意評分在重新排序期間正常運作，每項結果都必須在其 `metadata.text` 欄位中包含文字內容。

:::

## 傳回值

此函數傳回一個由 `RerankResult` 物件組成的陣列：

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'QueryResult',
    description: '原始查詢結果',
  },
  {
    name: 'score',
    type: 'number',
    description: '合併後的重新排序分數（0 至 1）',
  },
  {
    name: 'details',
    type: 'ScoringDetails',
    description: '詳細評分資料',
  },
]}
/>

### `ScoringDetails`

<PropertiesTable
  content={[
  {
    name: 'semantic',
    type: 'number',
    description: '語意相關性分數（0 至 1）',
  },
  {
    name: 'vector',
    type: 'number',
    description: '向量相似度分數（0 至 1）',
  },
  {
    name: 'position',
    type: 'number',
    description: '按位置計算的分數（0 至 1）',
  },
  {
    name: 'queryAnalysis',
    type: 'object',
    description: '查詢分析詳情',
    isOptional: true,
    properties: [
      {
        type: 'number',
        parameters: [
          {
            name: 'magnitude',
            description: '查詢的量值',
          },
        ],
      },
      {
        type: 'number[]',
        parameters: [
          {
            name: 'dominantFeatures',
            description: '查詢的主要特徵',
          },
        ],
      },
    ],
  },
]}
/>

## 相關內容

- [createVectorQueryTool](../tools/vector-query-tool)
