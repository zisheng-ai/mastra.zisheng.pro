---
title: '參考：Agent.getMetadata() | Agents'
description: "Mastra Agent 中 `Agent.getMetadata()` 方法的文件；此方法會擷取為 Agent 設定的中繼資料。"
packages:
  - '@mastra/core'
---

# Agent.getMetadata() {/* #agentgetmetadata */}

`.getMetadata()` 方法會擷取為 Agent 設定的中繼資料；如設定值為函式，則會先解析。你可以使用中繼資料在用戶端分類 Agent，毋須將該資料編碼至 Agent ID 或名稱中。

## 使用範例 {/* #usage-example */}

```typescript
await agent.getMetadata()
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ requestContext?: RequestContext }',
    isOptional: true,
    defaultValue: '{}',
    description: '包含請求情境的選用設定物件。',
    properties: [
      {
        type: '{ requestContext?: RequestContext }',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            defaultValue: 'undefined',
            description: '傳送至動態中繼資料函式的 Request Context。中繼資料為靜態時會忽略此值。',
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'metadata',
    type: 'Record<string, unknown> | Promise<Record<string, unknown>>',
    description:
      '為 Agent 設定的中繼資料；如未設定中繼資料，則為 `undefined`。如中繼資料定義為函式，可以直接傳回，亦可以是解析後傳回中繼資料的 promise。',
  },
]}
/>

## 進階使用範例 {/* #extended-usage-example */}

靜態中繼資料：

```typescript title="src/mastra/agents/support-agent.ts"
import { Agent } from '@mastra/core/agent'

export const supportAgent = new Agent({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'You help customers with support requests.',
  model: '__GATEWAY_OPENAI_MODEL__',
  metadata: { type: 'support' },
})

const metadata = supportAgent.getMetadata()
```

從請求情境解析的動態中繼資料：

```typescript title="src/mastra/agents/support-agent.ts"
import { Agent } from '@mastra/core/agent'

export const supportAgent = new Agent({
  id: 'support-agent',
  name: 'Support Agent',
  instructions: 'You help customers with support requests.',
  model: '__GATEWAY_OPENAI_MODEL__',
  metadata: ({ requestContext }) => ({
    type: 'support',
    tenant: requestContext.get('tenant'),
  }),
})

const metadata = await supportAgent.getMetadata({ requestContext })
```

## 相關內容 {/* #related */}

- [Agent 概覽](/docs/agents/overview)
- [Request Context](/docs/server/request-context)
