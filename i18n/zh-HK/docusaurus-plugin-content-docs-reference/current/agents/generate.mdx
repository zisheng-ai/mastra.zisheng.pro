---
title: "參考：Agent.generate() | Agents"
description: "Mastra Agent 中 `Agent.generate()` 方法的文件；此方法支援以進階功能產生非串流回應。"
packages:
  - "@mastra/core"
---

import { MODEL_SETTINGS_OBJECT } from "@site/src/components/ModelSettingsProperties";

# Agent.generate() {/* #agentgenerate */}

`.generate()` 方法讓 Agent 能夠以進階功能產生非串流回應。它接受訊息及可選的產生選項。

## 使用範例 {/* #usage-example */}

向 Agent 傳送訊息以產生回應：

```ts
const result = await agent.generate('message for agent')
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'string | string[] | CoreMessage[] | AiMessageType[] | UIMessageWithMetadata[]',
    description: '要傳送給 Agent 的訊息。可以是單一字串、字串陣列或結構化訊息物件。',
  },
  {
    name: 'options',
    type: 'AgentExecutionOptions<Output, Format>',
    isOptional: true,
    description: '產生程序的可選設定。',
    properties: [
      {
        type: 'AgentExecutionOptions<Output, Format>',
        parameters: [
          {
            name: 'maxSteps',
            type: 'number',
            isOptional: true,
            description: '執行期間可運行的步驟上限。',
          },
          {
            name: 'stopWhen',
            type: "LoopOptions['stopWhen']",
            isOptional: true,
            description: '停止執行的條件（例如步驟數目、token 上限）。',
          },
          {
            name: 'onIterationComplete',
            type: '(context: IterationCompleteContext) => { continue?: boolean; feedback?: string } | void | Promise<{ continue?: boolean; feedback?: string } | void>',
            isOptional: true,
            description:
              '每次迭代完成後呼叫的回呼函式。可用來監察進度、提供意見以引導 Agent，或提前停止執行。回呼會接收迭代的情境資料，包括目前文字、Tool 呼叫及完成原因。',
            properties: [
              {
                type: 'IterationCompleteContext',
                parameters: [
                  {
                    name: 'context.iteration',
                    type: 'number',
                    description: '目前的迭代編號（由 1 開始）。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.maxIterations',
                    type: 'number | undefined',
                    description: '允許的迭代次數上限（如已設定）。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.text',
                    type: 'string',
                    description: '此迭代的文字回應。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.isFinal',
                    type: 'boolean',
                    description: '這是否為最後一次迭代。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.finishReason',
                    type: 'string',
                    description: "此迭代完成的原因（例如 'stop'、'length'、'tool-calls'）。",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.toolCalls',
                    type: 'ToolCall[]',
                    description: '此迭代中發出的 Tool 呼叫。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'context.messages',
                    type: 'MastraDBMessage[]',
                    description: '截至目前累積的所有訊息。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'return.continue',
                    type: 'boolean',
                    isOptional: true,
                    description: '設為 false 可提前停止執行。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'return.feedback',
                    type: 'string',
                    isOptional: true,
                    description: '用來引導 Agent 下一次迭代的意見訊息。',
                  },
                ],
              },
            ],
          },
          {
            name: 'isTaskComplete',
            type: 'IsTaskCompleteConfig',
            isOptional: true,
            description:
              '用來驗證工作是否完成的工作完成評分設定。它使用 Mastra 的評估評分器，自動檢查 Agent 回應是否符合完成條件。',
            properties: [
              {
                type: 'IsTaskCompleteConfig',
                parameters: [
                  {
                    name: 'scorers',
                    type: 'MastraScorer[]',
                    description: '評估工作完成情況的評分器陣列。每個評分器會傳回 0（失敗）或 1（通過）。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'strategy',
                    type: "'all' | 'any'",
                    isOptional: true,
                    defaultValue: "'all'",
                    description: "合併評分器結果的策略。'all' 要求所有評分器均通過；'any' 則要求至少一個通過。",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'onComplete',
                    type: '(result: IsTaskCompleteRunResult) => void | Promise<void>',
                    isOptional: true,
                    description: '工作完成檢查結束時呼叫的回呼。它會接收包含各評分器分數的結果。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'parallel',
                    type: 'boolean',
                    isOptional: true,
                    defaultValue: 'true',
                    description: '是否並行運行評分器。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'timeout',
                    type: 'number',
                    isOptional: true,
                    defaultValue: '600000',
                    description: '等待所有評分器完成的最長時間（毫秒）。',
                  },
                ],
              },
            ],
          },
          {
            name: 'delegation',
            type: 'DelegationConfig',
            isOptional: true,
            description:
              '子 Agent 委派的設定。用來控制及監察 Agent 何時將工作委派給其他 Agent，亦可修改或拒絕委派，以及提供意見來引導監督 Agent。',
            properties: [
              {
                type: 'DelegationConfig',
                parameters: [
                  {
                    name: 'onDelegationStart',
                    type: '(context: DelegationStartContext) => DelegationStartResult | void | Promise<DelegationStartResult | void>',
                    isOptional: true,
                    description:
                      '委派給子 Agent 前呼叫。可用來修改委派參數、完全拒絕委派，或改動 `context.requestContext`，在子 Agent 運行的要求情境中加入項目。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'onDelegationComplete',
                    type: '(context: DelegationCompleteContext) => { feedback?: string } | void | Promise<{ feedback?: string } | void>',
                    isOptional: true,
                    description:
                      '子 Agent 委派完成後呼叫。情境包含可停止後續執行的 `bail()` 方法；你亦可傳回 `{ feedback }` 來引導監督 Agent 的下一步。意見會以助理訊息形式儲存至監督 Agent 的記憶體。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'messageFilter',
                    type: '(context: MessageFilterContext) => MastraDBMessage[] | Promise<MastraDBMessage[]>',
                    isOptional: true,
                    description: '委派給子 Agent 前呼叫的回呼函式。可用來篩選傳送給子 Agent 的訊息。',
                  },
                ],
              },
            ],
          },
          {
            name: 'scorers',
            type: "MastraScorers | Record<string, { scorer: MastraScorer['name']; sampling?: ScoringSamplingConfig }>",
            isOptional: true,
            description: '要對執行結果運行的評估評分器。',
            properties: [
              {
                type: "MastraScorers | Record<string, { scorer: MastraScorer['name']; sampling?: ScoringSamplingConfig }>",
                parameters: [
                  {
                    name: 'scorer',
                    type: 'string',
                    isOptional: false,
                    description: '要使用的評分器名稱。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'sampling',
                    type: 'ScoringSamplingConfig',
                    isOptional: true,
                    description: '評分器的抽樣設定。',
                    properties: [
                      {
                        type: 'ScoringSamplingConfig',
                        parameters: [
                          {
                            name: 'type',
                            type: "'none' | 'ratio'",
                            isOptional: false,
                            description: "抽樣策略類型。使用 'none' 停用抽樣，或使用 'ratio' 按百分比抽樣。",
                          },
                        ],
                      },
                      {
                        parameters: [
                          {
                            name: 'rate',
                            type: 'number',
                            isOptional: true,
                            description: "抽樣率（0 至 1）。當類型為 'ratio' 時必須提供。",
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
          {
            name: 'returnScorerData',
            type: 'boolean',
            isOptional: true,
            description: '是否在回應中傳回詳細評分資料。',
          },
          {
            name: 'onChunk',
            type: '(chunk: ChunkType) => Promise<void> | void',
            isOptional: true,
            description: '產生期間每個區塊都會呼叫的回呼函式。',
          },
          {
            name: 'onError',
            type: '({ error }: { error: Error | string }) => Promise<void> | void',
            isOptional: true,
            description: '產生期間發生錯誤時呼叫的回呼函式。',
          },
          {
            name: 'onAbort',
            type: '(event: any) => Promise<void> | void',
            isOptional: true,
            description: '產生程序中止時呼叫的回呼函式。',
          },
          {
            name: 'activeTools',
            type: 'Array<keyof ToolSet> | undefined',
            isOptional: true,
            description: '執行期間應啟用的 Tool 名稱陣列。如為 undefined，則會啟用所有可用 Tool。',
          },
          {
            name: 'abortSignal',
            type: 'AbortSignal',
            isOptional: true,
            description:
              '可用來中止 Agent 執行的訊號物件。訊號中止後，所有進行中的操作都會終止，包括 Agent 已委派而仍在運行的子 Agent。',
          },
          {
            name: 'prepareStep',
            type: 'PrepareStepFunction',
            isOptional: true,
            description: '多步驟執行中每個步驟開始前呼叫的回呼函式。',
          },
          {
            name: 'requireToolApproval',
            type: 'boolean',
            isOptional: true,
            description:
              "設為 true 時，所有 Tool 呼叫在執行前都必須獲明確批准。generate() 方法會傳回 `finishReason: 'suspended'`，並附上包含 Tool 呼叫詳情（`toolCallId`、`toolName`、`args`）的 `suspendPayload`。使用 `approveToolCallGenerate()` 或 `declineToolCallGenerate()` 繼續。詳情請參閱 [Agent 批准](/docs/agents/agent-approval#tool-approval-with-generate)。",
          },
          {
            name: 'autoResumeSuspendedTools',
            type: 'boolean',
            isOptional: true,
            description:
              '設為 true 時，使用者在同一 thread 傳送新訊息便會自動恢復已暫停的 Tool。Agent 會根據 Tool 的 `resumeSchema`，從使用者訊息擷取 `resumeData`。必須設定記憶體。',
          },
          {
            name: 'toolCallConcurrency',
            type: 'number',
            isOptional: true,
            description: '可同時執行的 Tool 呼叫數目上限。可能需要批准時預設為 1，否則為 10。',
          },
          {
            name: 'context',
            type: 'ModelMessage[]',
            isOptional: true,
            description: '提供給 Agent 的額外情境訊息。',
          },
          {
            name: 'structuredOutput',
            type: 'StructuredOutputOptions<S extends ZodTypeAny = ZodTypeAny>',
            isOptional: true,
            description: '微調結構化輸出產生程序的選項。',
            properties: [
              {
                type: 'StructuredOutputOptions<S extends ZodTypeAny = ZodTypeAny>',
                parameters: [
                  {
                    name: 'schema',
                    type: 'StandardJSONSchemaV1',
                    isOptional: false,
                    description: '定義預期輸出結構的標準 JSON Schema。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'model',
                    type: 'MastraLanguageModel',
                    isOptional: true,
                    description:
                      '用於產生結構化輸出的語言模型。提供後，Agent 可透過多個步驟，以 Tool 呼叫、文字及結構化輸出作出回應',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'errorStrategy',
                    type: "'strict' | 'warn' | 'fallback'",
                    isOptional: true,
                    description:
                      "處理 schema 驗證錯誤的策略。'strict' 會拋出錯誤，'warn' 會記錄警告，'fallback' 則使用後備值。",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'fallbackValue',
                    type: '<S extends ZodTypeAny>',
                    isOptional: true,
                    description: "schema 驗證失敗且 errorStrategy 為 'fallback' 時使用的後備值。",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'instructions',
                    type: 'string',
                    isOptional: true,
                    description: '結構化輸出模型的額外指示。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'jsonPromptInjection',
                    type: "boolean | 'system' | 'inline' | 'auto'",
                    isOptional: true,
                    description:
                      "控制 JSON schema 如何傳送至模型。設為 'auto'，支援時會使用原生結構化輸出，否則會使用行內提示注入。",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'logger',
                    type: 'IMastraLogger',
                    isOptional: true,
                    description: '產生輸出期間用於結構化記錄的可選 logger 實例。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'providerOptions',
                    type: 'ProviderOptions',
                    isOptional: true,
                    description:
                      "傳送給內部結構化 Agent 的 Provider 專用選項。可用來控制模型行為，例如思考模型的推理強度（例如 `{ openai: { reasoningEffort: 'low' } }`）。",
                  },
                ],
              },
            ],
          },
          {
            name: 'outputProcessors',
            type: 'OutputProcessorOrWorkflow[]',
            isOptional: true,
            description: '此執行使用的輸出處理器（覆寫 Agent 的預設值）。',
          },
          {
            name: 'maxProcessorRetries',
            type: 'number',
            isOptional: true,
            description: '處理器可觸發此產生程序重試的次數上限。覆寫 Agent 的預設 maxProcessorRetries。',
          },
          {
            name: 'inputProcessors',
            type: 'InputProcessorOrWorkflow[]',
            isOptional: true,
            description: '此執行使用的輸入處理器（覆寫 Agent 的預設值）。',
          },
          {
            name: 'instructions',
            type: 'string | string[] | CoreSystemMessage | SystemModelMessage | CoreSystemMessage[] | SystemModelMessage[]',
            isOptional: true,
            description: '覆寫 Agent 在此執行中預設指示的自訂指示。可以是單一字串、訊息物件或兩者之一的陣列。',
          },
          {
            name: 'system',
            type: 'string | string[] | CoreSystemMessage | SystemModelMessage | CoreSystemMessage[] | SystemModelMessage[]',
            isOptional: true,
            description:
              '要加入提示的自訂 system 訊息。可以是單一字串、訊息物件或兩者之一的陣列。system 訊息會提供額外情境或行為指示，以補充 Agent 的主要指示。',
          },
          {
            name: 'output',
            type: 'Zod schema | JsonSchema7',
            isOptional: true,
            description:
              '**已棄用。** 使用不含模型的 structuredOutput 可達到相同效果。定義預期的輸出結構。可以是 JSON Schema 物件或 Zod schema。',
          },
          {
            name: 'memory',
            type: 'object',
            isOptional: true,
            description: '用於保存及擷取對話的記憶體設定。',
            properties: [
              {
                type: 'object',
                parameters: [
                  {
                    name: 'thread',
                    type: 'string | { id: string; metadata?: Record<string, any>, title?: string }',
                    isOptional: false,
                    description:
                      '維持對話連續性的 thread 識別碼。可以是字串 ID，或包含 ID 及可選 metadata/title 的物件。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'resource',
                    type: 'string',
                    isOptional: false,
                    description: '按使用者、session 或情境整理對話的資源識別碼。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'options',
                    type: 'MemoryConfig',
                    isOptional: true,
                    description:
                      '額外記憶體設定選項，包括 lastMessages、readOnly、semanticRecall、workingMemory 及 filterIncompleteToolCalls。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'onTitleGenerated',
                    type: '(title: string) => void | Promise<void>',
                    isOptional: true,
                    description:
                      '產生 thread 標題並保存至儲存空間後，以非同步方式觸發的回呼。標題會在背景產生，並可能在 `generate()` 傳回後才完成。只有在記憶體選項啟用 `generateTitle`，而且 thread 沒有現有標題時才會觸發。',
                  },
                ],
              },
            ],
          },
          {
            name: 'onFinish',
            type: "LoopConfig['onFinish']",
            isOptional: true,
            description: '產生程序完成時觸發的回呼。',
          },
          {
            name: 'onStepFinish',
            type: "LoopConfig['onStepFinish']",
            isOptional: true,
            description: '每個產生步驟完成後觸發的回呼。',
          },
          {
            name: 'telemetry',
            type: 'TelemetrySettings',
            isOptional: true,
            description: '產生期間收集 OTLP 遙測資料的設定（並非 Tracing）。',
            properties: [
              {
                type: 'TelemetrySettings',
                parameters: [
                  {
                    name: 'isEnabled',
                    type: 'boolean',
                    isOptional: true,
                    description: '是否啟用遙測資料收集。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordInputs',
                    type: 'boolean',
                    isOptional: true,
                    description: '是否在遙測資料中記錄輸入資料。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'recordOutputs',
                    type: 'boolean',
                    isOptional: true,
                    description: '是否在遙測資料中記錄輸出資料。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'functionId',
                    type: 'string',
                    isOptional: true,
                    description: '正在執行之函式的識別碼。',
                  },
                ],
              },
            ],
          },
          MODEL_SETTINGS_OBJECT,
          {
            name: 'toolChoice',
            type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
            isOptional: true,
            description: '控制產生期間如何選擇 Tool。',
            properties: [
              {
                type: "'auto' | 'none' | 'required' | { type: 'tool'; toolName: string }",
                parameters: [
                  {
                    name: "'auto'",
                    type: 'string',
                    isOptional: false,
                    description: '讓模型決定何時使用 Tool（預設）。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "'none'",
                    type: 'string',
                    isOptional: false,
                    description: '完全停用 Tool。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "'required'",
                    type: 'string',
                    isOptional: false,
                    description: '強制模型使用至少一個 Tool。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: "{ type: 'tool'; toolName: string }",
                    type: 'object',
                    isOptional: false,
                    description: '強制模型使用指定 Tool。',
                  },
                ],
              },
            ],
          },
          {
            name: 'toolsets',
            type: 'ToolsetsInput',
            isOptional: true,
            description: '此執行可使用的額外 Tool 集合。',
          },
          {
            name: 'clientTools',
            type: 'ToolsInput',
            isOptional: true,
            description: '執行期間可用的客戶端 Tool。',
          },
          {
            name: 'hooks',
            type: 'ToolHooks',
            isOptional: true,
            description:
              '在 Tool 呼叫前後運行、按每次執行設定的 hook。覆寫此執行中相符的 Agent 層級 hook。`beforeToolCall` 可傳回 `{ proceed: false, output }` 以略過 Tool 呼叫。',
          },
          {
            name: 'savePerStep',
            type: 'boolean',
            isOptional: true,
            description: '每個產生步驟完成後逐步儲存訊息（預設：false）。啟用觀察式記憶體時會在內部停用。',
          },
          {
            name: 'providerOptions',
            type: 'Record<string, Record<string, JSONValue>>',
            isOptional: true,
            description: '傳送給語言模型的 Provider 專用選項。',
            properties: [
              {
                type: 'Record<string, Record<string, JSONValue>>',
                parameters: [
                  {
                    name: 'openai',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'OpenAI 專用選項，例如 reasoningEffort、responseFormat 等。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'anthropic',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Anthropic 專用選項，例如 maxTokens 等。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'google',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: 'Google 專用選項。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: '[providerName]',
                    type: 'Record<string, JSONValue>',
                    isOptional: true,
                    description: '任何 Provider 專用選項。',
                  },
                ],
              },
            ],
          },
          {
            name: 'runId',
            type: 'string',
            isOptional: true,
            description: '此執行運行的唯一識別碼。',
          },
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            description: '包含動態設定及狀態的要求情境。',
          },
          {
            name: 'tracingContext',
            type: 'TracingContext',
            isOptional: true,
            description: '用於建立子 span 及加入 metadata 的 Tracing 情境。使用 Mastra 的 tracing 系統時會自動注入。',
            properties: [
              {
                type: 'TracingContext',
                parameters: [
                  {
                    name: 'currentSpan',
                    type: 'Span',
                    isOptional: true,
                    description:
                      '用於建立子 span 及加入 metadata 的目前 span。可在執行期間用來建立自訂子 span 或更新 span 屬性。',
                  },
                ],
              },
            ],
          },
          {
            name: 'tracingOptions',
            type: 'TracingOptions',
            isOptional: true,
            description: 'Tracing 設定選項。',
            properties: [
              {
                type: 'TracingOptions',
                parameters: [
                  {
                    name: 'metadata',
                    type: 'Record<string, any>',
                    isOptional: true,
                    description:
                      '要加入根 trace span 的 metadata。適合加入使用者 ID、session ID 或功能旗標等自訂屬性。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'requestContextKeys',
                    type: 'string[]',
                    isOptional: true,
                    description:
                      "要擷取作此 trace metadata 的額外 RequestContext key。支援以點標記法表示巢狀值（例如 'user.id'）。",
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'traceId',
                    type: 'string',
                    isOptional: true,
                    description:
                      '此執行使用的 Trace ID（1 至 32 個十六進位字元）。如有提供，此 trace 將屬於指定 trace。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'parentSpanId',
                    type: 'string',
                    isOptional: true,
                    description:
                      '此執行使用的上層 span ID（1 至 16 個十六進位字元）。如有提供，根 span 會建立為此 span 的子項。',
                  },
                ],
              },
              {
                parameters: [
                  {
                    name: 'tags',
                    type: 'string[]',
                    isOptional: true,
                    description: '要套用至此 trace 的標籤。用於分類及篩選 trace 的字串標籤。',
                  },
                ],
              },
            ],
          },
          {
            name: 'versions',
            type: 'VersionOverrides',
            isOptional: true,
            description:
              '每次呼叫的子 Agent 委派版本覆寫。它會合併至 Mastra 實例層級版本之上，並透過 requestContext 在子 Agent 呼叫之間自動傳遞。需要 editor 套件。請參閱 [Editor 版本控制](/docs/editor/overview#versioning)。',
            properties: [
              {
                type: 'VersionOverrides',
                parameters: [
                  {
                    name: 'agents',
                    type: 'Record<string, VersionSelector>',
                    isOptional: true,
                    description: 'Agent ID 至其版本選擇器的對應。',
                    properties: [
                      {
                        type: 'VersionSelector',
                        parameters: [
                          {
                            name: 'versionId',
                            type: 'string',
                            isOptional: true,
                            description: '按 ID 指定特定版本。',
                          },
                        ],
                      },
                      {
                        parameters: [
                          {
                            name: 'status',
                            type: "'draft' | 'published'",
                            isOptional: true,
                            description: '指定具此發佈狀態的最新版本。',
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
          {
            name: 'includeRawChunks',
            type: 'boolean',
            isOptional: true,
            description: '是否在串流輸出中包含原始區塊。並非所有模型 Provider 均支援。',
          },
        ],
      },
    ],
  },
]}
/>

## 回應結構 {/* #response-structure */}

`Agent.generate()` 會傳回執行期間收集的最終資料。`steps` 是步驟物件陣列。結果中的 Tool 陣列，包括最上層的 `toolCalls` 和 `toolResults`，以及巢狀的 `step.toolCalls` 和 `step.toolResults` 陣列，均使用 Mastra 的區塊格式。

亦即 Tool 資料會包裝在 `payload` 中：

```ts
const response = await agent.generate('Check the weather in Lagos')

for (const toolCall of response.toolCalls) {
  console.log(toolCall.type) // 'tool-call'
  console.log(toolCall.runId)
  console.log(toolCall.from)
  console.log(toolCall.payload.toolName)
  console.log(toolCall.payload.args)
}

for (const step of response.steps) {
  for (const toolResult of step.toolResults) {
    console.log(toolResult.type) // 'tool-result'
    console.log(toolResult.payload.toolName)
    console.log(toolResult.payload.result)
  }
}
```

如需相同區塊結構的串流版本，請參閱 [ChunkType 參考](/reference/streaming/ChunkType)。

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'result',
    type: "Awaited<ReturnType<MastraModelOutput<Output>['getFullOutput']>>",
    description: '傳回產生程序的完整輸出，包括文字、物件（如為結構化輸出）、Tool 呼叫、Tool 結果、用量統計及步驟資料。',
  },
  {
    name: 'text',
    type: 'string',
    description: 'Agent 產生的文字回應。',
  },
  {
    name: 'object',
    type: 'Output | undefined',
    isOptional: true,
    description: '如有提供 structuredOutput，則為已按 schema 驗證的結構化輸出物件。',
  },
  {
    name: 'toolCalls',
    type: 'ToolCallChunk[]',
    description: '產生期間發出的 Tool 呼叫區塊陣列。',
    properties: [
      {
        type: 'ToolCallChunk',
        parameters: [
          {
            name: 'type',
            type: "'tool-call'",
            description: '區塊類型識別碼。',
          },
          {
            name: 'runId',
            type: 'string',
            description: '執行運行識別碼。',
          },
          {
            name: 'from',
            type: 'ChunkFrom',
            description: '區塊來源，例如 AGENT 或 WORKFLOW。',
          },
          {
            name: 'payload',
            type: 'ToolCallPayload',
            description: 'Tool 呼叫資料。',
            properties: [
              {
                type: 'ToolCallPayload',
                parameters: [
                  {
                    name: 'toolCallId',
                    type: 'string',
                    description: 'Tool 呼叫的唯一識別碼。',
                  },
                  {
                    name: 'toolName',
                    type: 'string',
                    description: '被呼叫的 Tool 名稱。',
                  },
                  {
                    name: 'args',
                    type: 'Record<string, unknown>',
                    isOptional: true,
                    description: '傳送給 Tool 的引數。',
                  },
                  {
                    name: 'providerExecuted',
                    type: 'boolean',
                    isOptional: true,
                    description: '模型 Provider 是否直接執行 Tool。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'toolResults',
    type: 'ToolResultChunk[]',
    description: 'Tool 執行所產生的 Tool 結果區塊陣列。',
    properties: [
      {
        type: 'ToolResultChunk',
        parameters: [
          {
            name: 'type',
            type: "'tool-result'",
            description: '區塊類型識別碼。',
          },
          {
            name: 'runId',
            type: 'string',
            description: '執行運行識別碼。',
          },
          {
            name: 'from',
            type: 'ChunkFrom',
            description: '區塊來源，例如 AGENT 或 WORKFLOW。',
          },
          {
            name: 'payload',
            type: 'ToolResultPayload',
            description: 'Tool 結果資料。',
            properties: [
              {
                type: 'ToolResultPayload',
                parameters: [
                  {
                    name: 'toolCallId',
                    type: 'string',
                    description: 'Tool 呼叫的唯一識別碼。',
                  },
                  {
                    name: 'toolName',
                    type: 'string',
                    description: '產生結果的 Tool 名稱。',
                  },
                  {
                    name: 'result',
                    type: 'unknown',
                    description: 'Tool 傳回的值。',
                  },
                  {
                    name: 'isError',
                    type: 'boolean',
                    isOptional: true,
                    description: 'Tool 執行是否失敗。',
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
  {
    name: 'usage',
    type: 'TokenUsage',
    description: '產生程序的 token 用量統計。',
  },
  {
    name: 'steps',
    type: 'object[]',
    description: '執行步驟陣列，適合用來偵錯多步驟產生程序。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'text',
            type: 'string',
            description: '此步驟產生的文字。',
          },
          {
            name: 'toolCalls',
            type: 'ToolCallChunk[]',
            description: '此步驟發出的 Tool 呼叫。',
          },
          {
            name: 'toolResults',
            type: 'ToolResultChunk[]',
            description: '此步驟發出的 Tool 結果。',
          },
          {
            name: 'finishReason',
            type: 'string',
            isOptional: true,
            description: '此步驟完成的原因。',
          },
          {
            name: 'usage',
            type: 'LanguageModelUsage',
            description: '此步驟的 token 用量。',
          },
          {
            name: 'request',
            type: '{ body?: unknown }',
            description: '此步驟的要求 metadata。',
          },
          {
            name: 'response',
            type: 'object',
            description: '此步驟的回應 metadata。',
          },
        ],
      },
    ],
  },
  {
    name: 'finishReason',
    type: 'string',
    description:
      "產生程序完成的原因。值包括 'stop'（正常完成）、'tool-calls'（以 Tool 呼叫結束）、'suspended'（等待 Tool 批准）或 'error'（發生錯誤）。",
  },
  {
    name: 'response',
    type: 'object',
    description: '模型 Provider 傳回的回應 metadata。適合用來存取速率限制 header 及要求 ID。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'id',
            type: 'string',
            isOptional: true,
            description: '模型 Provider 傳回的回應 ID。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'timestamp',
            type: 'Date',
            isOptional: true,
            description: '產生回應時的時間戳記。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'modelId',
            type: 'string',
            isOptional: true,
            description: '此回應所用的模型識別碼。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'headers',
            type: 'Record<string, string>',
            isOptional: true,
            description:
              '模型 Provider 傳回的 HTTP 回應 header。包含速率限制資料（例如 `anthropic-ratelimit-requests-remaining`、`x-ratelimit-remaining-tokens`）及其他 Provider 專用 metadata。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'messages',
            type: 'ResponseMessage[]',
            isOptional: true,
            description: '模型格式的回應訊息。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'uiMessages',
            type: 'UIMessage[]',
            isOptional: true,
            description: 'UI 格式的回應訊息，包括輸出處理器加入的任何 metadata。',
          },
        ],
      },
    ],
  },
  {
    name: 'request',
    type: 'object',
    isOptional: true,
    description: '傳送給模型的要求。',
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'body',
            type: 'unknown',
            isOptional: true,
            description: '傳送給模型 Provider 的要求 body。',
          },
        ],
      },
    ],
  },
  {
    name: 'warnings',
    type: 'LanguageModelWarning[]',
    isOptional: true,
    description: '產生期間模型 Provider 傳回的任何警告。',
  },
  {
    name: 'providerMetadata',
    type: 'Record<string, unknown>',
    isOptional: true,
    description: '隨回應傳回的 Provider 專用 metadata。',
  },
  {
    name: 'reasoning',
    type: 'ReasoningChunk[]',
    isOptional: true,
    description: '支援推理的模型所傳回的推理詳情（例如 OpenAI o1 系列）。',
  },
  {
    name: 'reasoningText',
    type: 'string',
    isOptional: true,
    description: '推理模型的合併推理文字。',
  },
  {
    name: 'sources',
    type: 'SourceChunk[]',
    isOptional: true,
    description: '模型在產生期間引用的來源。',
  },
  {
    name: 'files',
    type: 'FileChunk[]',
    isOptional: true,
    description: '模型產生的檔案。',
  },
  {
    name: 'suspendPayload',
    type: 'object',
    isOptional: true,
    description: "當 `finishReason` 為 'suspended' 時存在。包含批准或拒絕待處理 Tool 呼叫所需的 Tool 呼叫詳情。",
    properties: [
      {
        type: 'object',
        parameters: [
          {
            name: 'toolCallId',
            type: 'string',
            description: '待處理 Tool 呼叫的唯一識別碼。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'toolName',
            type: 'string',
            description: '需要批准的 Tool 名稱。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'args',
            type: 'Record<string, any>',
            description: '將傳送給 Tool 的引數。',
          },
        ],
      },
    ],
  },
  {
    name: 'runId',
    type: 'string',
    isOptional: true,
    description:
      '此執行運行的唯一識別碼。 Required when calling `approveToolCallGenerate()` or `declineToolCallGenerate()` to resume a suspended execution.',
  },
  {
    name: 'traceId',
    type: 'string',
    isOptional: true,
    description: '啟用 Tracing 時與此執行相關聯的 trace ID。可用來關聯記錄及偵錯執行流程。',
  },
  {
    name: 'spanId',
    type: 'string',
    isOptional: true,
    description: '啟用 Tracing 時與此執行相關聯的根 span ID。可用於 span 層級查找及關聯。',
  },
  {
    name: 'messages',
    type: 'MastraDBMessage[]',
    description: '此執行的所有訊息，包括輸入、記憶體記錄及回應。',
  },
  {
    name: 'rememberedMessages',
    type: 'MastraDBMessage[]',
    description: '僅從記憶體載入的訊息（對話記錄）。',
  },
  {
    name: 'error',
    type: 'Error',
    isOptional: true,
    description: '產生失敗時的錯誤物件。',
  },
  {
    name: 'tripwire',
    type: 'StepTripwireData',
    isOptional: true,
    description: '內容被處理器封鎖時的 tripwire 資料。',
  },
  {
    name: 'scoringData',
    type: 'object',
    isOptional: true,
    description: '啟用 `returnScorerData` 時供 Evals 使用的評分資料。',
  },
]}
/>

## 更多範例 {/* #more-examples */}

### 使用模型設定 {/* #with-model-settings */}

限制輸出 token 並設定 temperature 的範例：

```ts
const limitedResult = await agent.generate('Write a short poem about coding', {
  modelSettings: {
    maxOutputTokens: 50,
    temperature: 0.7,
  },
})
```

### 使用記憶體 {/* #with-memory */}

透過設定記憶體選項，讓 Agent 存取對話記錄及保存資料。這讓 Agent 能記住先前的互動，並在訊息之間維持情境。

```ts
const memoryResult = await agent.generate('Remember my favorite color is blue', {
  memory: {
    thread: 'user-123-thread',
    resource: 'user-123',
  },
})
```

### 存取回應 header {/* #accessing-response-headers */}

部分模型 Provider 會在回應 header 中傳回實用資料，例如剩餘 token 數目或速率限制狀態。產生程序完成後，你可以從結果物件存取這些 header。

```ts
const result = await agent.generate('Hello!')
const remainingRequests = result.response?.headers?.['anthropic-ratelimit-requests-remaining']
const remainingTokens = result.response?.headers?.['x-ratelimit-remaining-tokens']
console.log(`Remaining requests: ${remainingRequests}, Remaining tokens: ${remainingTokens}`)
```

### 分析圖像 {/* #analyzing-images */}

Agent 可同時處理視覺內容及圖像內的任何文字，以分析及描述圖像。要啟用圖像分析，請在 `content` 陣列中傳入包含 `type: 'image'` 及圖像 URL 的物件。你可將圖像內容與文字提示結合，以引導 Agent 分析。

```typescript
const response = await agent.generate([
  {
    role: 'user',
    content: [
      {
        type: 'image',
        image: 'https://placebear.com/cache/395-205.jpg',
        mimeType: 'image/jpeg',
      },
      {
        type: 'text',
        text: 'Describe the image in detail, and extract all the text in the image.',
      },
    ],
  },
])

console.log(response.text)
```

### 使用 `maxSteps` {/* #using-maxsteps */}

`maxSteps` 參數控制 Agent 可連續呼叫 LLM 的次數上限。每個步驟會產生回應並執行任何 Tool 呼叫，然後才處理結果。限制步驟有助防止無限循環及減少延遲，亦可控制使用 Tool 的 Agent 所用的 token。預設值為 5，但可以提高：

```typescript
const response = await agent.generate('Help me organize my day', {
  maxSteps: 10,
})

console.log(response.text)
```

### 使用 `onStepFinish` {/* #using-onstepfinish */}

你可以使用 `onStepFinish` 回呼監察多步驟操作的進度。這適合用於偵錯，或向使用者提供進度更新。

只有在串流傳輸或產生不含結構化輸出的文字時，才可使用 `onStepFinish`。

```typescript
const response = await agent.generate('Help me organize my day', {
  onStepFinish: ({ text, toolCalls, toolResults, finishReason, usage }) => {
    console.log({ text, toolCalls, toolResults, finishReason, usage })
  },
})
```

### 使用 `onTitleGenerated` {/* #using-ontitlegenerated */}

在記憶體選項中啟用 `generateTitle` 後，標題產生程序會在回應完成後以非同步方式運行。標題準備好時，可使用 `onTitleGenerated` 作出反應，例如透過 SSE 推送至客戶端。

```typescript
const response = await agent.generate('What is quantum computing?', {
  memory: {
    thread: threadId,
    resource: userId,
    onTitleGenerated: title => {
      console.log('Thread title:', title)
    },
  },
})
```
