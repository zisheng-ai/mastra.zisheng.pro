---
title: "參考：Agent.getInstructions() | Agent"
description: "Mastra Agent 中 `Agent.getInstructions()` 方法的文件；此方法會取得用於引導 Agent 行為的指示。"
packages:
  - "@mastra/core"
---

# Agent.getInstructions() {/* #agentgetinstructions */}

`.getInstructions()` 方法會取得為 Agent 設定的指示；如設定為函數，則會先解析函數。這些指示會引導 Agent 的行為，並定義其能力及限制。

## 使用範例 {/* #usage-example */}

```typescript
await agent.getInstructions()
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'options',
    type: '{ requestContext?: RequestContext }',
    isOptional: true,
    defaultValue: '{}',
    description: '包含請求內容的可選設定物件。',
    properties: [
      {
        type: '{ requestContext?: RequestContext }',
        parameters: [
          {
            name: 'requestContext',
            type: 'RequestContext',
            isOptional: true,
            defaultValue: 'undefined',
            description: '用於依賴注入及內容資訊的 Request Context。',
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'instructions',
    type: 'SystemMessage | Promise<SystemMessage>',
    description:
      '為 Agent 設定的指示。SystemMessage 可以是：string | string[] | CoreSystemMessage | CoreSystemMessage[] | SystemModelMessage | SystemModelMessage[]。此方法會直接傳回指示，或傳回解析為指示的 promise。',
  },
]}
/>

## 延伸使用範例 {/* #extended-usage-example */}

```typescript
await agent.getInstructions({
  requestContext: new RequestContext(),
})
```

## 相關內容 {/* #related */}

- [Agent 概覽](/docs/agents/overview)
- [Request Context](/docs/server/request-context)
