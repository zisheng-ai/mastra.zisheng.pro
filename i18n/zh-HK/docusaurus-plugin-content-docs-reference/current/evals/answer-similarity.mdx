---
title: "參考：答案相似度評分器 | Evals"
description: Mastra 答案相似度評分器的文件。此評分器會將 Agent 輸出與標準答案比較，用於 CI/CD 測試。
packages:
  - "@mastra/core"
  - "@mastra/evals"
---

# 答案相似度評分器

`createAnswerSimilarityScorer()` 函式會建立一個評分器，用於評估 Agent 輸出與標準答案的相似程度。此評分器專為 CI/CD 測試情境而設，適合已有預期答案並希望確保結果長期一致的情況。

## 參數

<PropertiesTable
  content={[
  {
    name: 'model',
    type: 'LanguageModel',
    required: true,
    description: '用於評估輸出與標準答案之間語義相似度的語言模型。',
  },
  {
    name: 'options',
    type: 'AnswerSimilarityOptions',
    required: false,
    description: '評分器的設定選項。',
    properties: [
      {
        type: 'AnswerSimilarityOptions',
        parameters: [
          {
            name: 'requireGroundTruth',
            type: 'boolean',
            required: false,
            defaultValue: 'true',
            description:
              '評估時是否必須提供標準答案。如設為 false，缺少標準答案時會傳回 0 分。',
          },
          {
            name: 'semanticThreshold',
            type: 'number',
            required: false,
            defaultValue: '0.8',
            description: '語義相符相對於完全相符的權重（0–1）。',
          },
          {
            name: 'exactMatchBonus',
            type: 'number',
            required: false,
            defaultValue: '0.2',
            description: '完全相符時的額外加分（0–1）。',
          },
          {
            name: 'missingPenalty',
            type: 'number',
            required: false,
            defaultValue: '0.15',
            description: '每缺少一項標準答案中的關鍵概念所扣的分數。',
          },
          {
            name: 'contradictionPenalty',
            type: 'number',
            required: false,
            defaultValue: '1.0',
            description: '資訊互相矛盾時所扣的分數。較高的值可確保錯誤答案的分數接近 0。',
          },
          {
            name: 'extraInfoPenalty',
            type: 'number',
            required: false,
            defaultValue: '0.05',
            description: '包含標準答案中沒有的額外資訊時所扣的少量分數（上限為 0.2）。',
          },
          {
            name: 'scale',
            type: 'number',
            required: false,
            defaultValue: '1',
            description: '分數縮放系數。',
          },
        ],
      },
    ],
  },
]}
/>

此函式會傳回 MastraScorer 類別的實例。`.run()` 方法接受與其他評分器相同的輸入（請參閱 [MastraScorer 參考文件](./mastra-scorer)），但**必須在執行物件中提供標準答案**。

## `.run()` 傳回值

<PropertiesTable
  content={[
  {
    name: 'runId',
    type: 'string',
    description: '執行的 ID（選填）。',
  },
  {
    name: 'score',
    type: 'number',
    description:
      '介乎 0–1 的相似度分數（如使用自訂 scale，則為 0–scale）。分數越高，代表與標準答案越相似。',
  },
  {
    name: 'reason',
    type: 'string',
    description: '以易讀方式解釋分數，並提供可採取行動的意見。',
  },
  {
    name: 'preprocessStepResult',
    type: 'object',
    description: '從輸出及標準答案擷取的語義單元。',
  },
  {
    name: 'analyzeStepResult',
    type: 'object',
    description: '相符內容、矛盾內容及額外資訊的詳細分析。',
  },
  {
    name: 'preprocessPrompt',
    type: 'string',
    description: '用於擷取語義單元的提示。',
  },
  {
    name: 'analyzePrompt',
    type: 'string',
    description: '用於分析相似度的提示。',
  },
  {
    name: 'generateReasonPrompt',
    type: 'string',
    description: '用於產生解釋的提示。',
  },
]}
/>

## 評分詳情

評分器採用多步驟流程：

1. **擷取**：將輸出及標準答案拆分為多個語義單元
1. **分析**：比較各單元，並識別相符、矛盾及缺漏的內容
1. **評分**：計算加權相似度，並就矛盾內容扣分
1. **解釋**：產生易讀的解釋

分數計算：`max(0, base_score - contradiction_penalty - missing_penalty - extra_info_penalty) × scale`

## 範例

在不同情境下，評估 Agent 回應與標準答案的相似度：

```typescript title="src/example-answer-similarity.ts"
import { runEvals } from '@mastra/core/evals'
import { createAnswerSimilarityScorer } from '@mastra/evals/scorers/prebuilt'
import { myAgent } from './agent'

const scorer = createAnswerSimilarityScorer({ model: '__GATEWAY_OPENAI_MODEL__' })

const result = await runEvals({
  data: [
    {
      input: 'What is 2+2?',
      groundTruth: '4',
    },
    {
      input: 'What is the capital of France?',
      groundTruth: 'The capital of France is Paris',
    },
    {
      input: 'What are the primary colors?',
      groundTruth: 'The primary colors are red, blue, and yellow',
    },
  ],
  scorers: [scorer],
  target: myAgent,
  onItemComplete: ({ scorerResults }) => {
    console.log({
      score: scorerResults[scorer.id].score,
      reason: scorerResults[scorer.id].reason,
    })
  },
})

console.log(result.scores)
```

如要進一步了解 `runEvals`，請參閱 [runEvals 參考文件](/reference/evals/run-evals)。

如要將此評分器加入 Agent，請參閱[評分器概覽](/docs/evals/overview#adding-scorers-to-agents)指南。
