---
title: "參考：withMastra() | AI SDK"
description: withMastra() 的 API 參考。此函數可在 AI SDK 中使用 Mastra 功能。
packages:
  - "@mastra/ai-sdk"
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# withMastra()

使用 Mastra processors 及／或記憶體包裝 AI SDK 模型。

## 使用範例 {/* #usage-example */}

```typescript title="src/example.ts"
import { openai } from '@ai-sdk/openai'
import { generateText } from 'ai'
import { withMastra } from '@mastra/ai-sdk'
import type { Processor } from '@mastra/core/processors'

const loggingProcessor: Processor<'logger'> = {
  id: 'logger',
  async processInput({ messages }) {
    console.log('Input:', messages.length, 'messages')
    return messages
  },
}

const model = withMastra(openai('gpt-5.4'), {
  inputProcessors: [loggingProcessor],
})

const { text } = await generateText({
  model,
  prompt: 'What is 2 + 2?',
})
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'model',
    type: 'LanguageModelV2 | LanguageModelV3',
    description: "任何 AI SDK v5 或 v6 語言模型（例如 `openai('gpt-5.4')`、`anthropic('claude-opus-4-6')`）。",
    isOptional: false,
  },
  {
    name: 'options',
    type: 'WithMastraOptions',
    description: 'processors 及記憶體的設定物件。',
    isOptional: true,
    properties: [
      {
        type: 'WithMastraOptions',
        parameters: [
          {
            name: 'inputProcessors',
            type: 'InputProcessor[]',
            description: '在呼叫 LLM 前執行的輸入 processors。',
            isOptional: true,
          },
          {
            name: 'outputProcessors',
            type: 'OutputProcessor[]',
            description: '對 LLM 回應執行的輸出 processors。',
            isOptional: true,
          },
          {
            name: 'memory',
            type: 'WithMastraMemoryOptions',
            description: '記憶體設定，可啟用自動持久保存訊息記錄。',
            isOptional: true,
            properties: [
              {
                type: 'WithMastraMemoryOptions',
                parameters: [
                  {
                    name: 'storage',
                    type: 'MemoryStorage',
                    description:
                      "用於持久保存訊息的記憶體儲存網域。可使用 `await storage.getStore('memory')` 從複合儲存空間取得。",
                    isOptional: false,
                  },
                  {
                    name: 'threadId',
                    type: 'string',
                    description: '用於持久保存對話的執行緒 ID。',
                    isOptional: false,
                  },
                  {
                    name: 'resourceId',
                    type: 'string',
                    description: '資源 ID（使用者／工作階段識別碼）。',
                    isOptional: true,
                  },
                  {
                    name: 'lastMessages',
                    type: 'number | false',
                    description: '要擷取的最近訊息數目，或設為 false 以停用。',
                    isOptional: true,
                  },
                  {
                    name: 'semanticRecall',
                    type: 'WithMastraSemanticRecallOptions',
                    description: '語意回憶設定（以 RAG 為基礎的記憶體擷取）。',
                    isOptional: true,
                  },
                  {
                    name: 'workingMemory',
                    type: "MemoryConfig['workingMemory']",
                    description: '工作記憶體設定（持久保存的使用者資料）。',
                    isOptional: true,
                  },
                  {
                    name: 'readOnly',
                    type: 'boolean',
                    description: '唯讀模式，可防止儲存新訊息。',
                    isOptional: true,
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值 {/* #returns */}

傳回與 `generateText`、`streamText`、`generateObject` 及 `streamObject` 相容的已包裝模型。

## 串流行為 {/* #streaming-behavior */}

實作 `processOutputResult` 的輸出 processors 會在串流結束後執行。請完整接收串流，以持久保存訊息記錄及語意回憶。
