---
title: "參考：toAISdkMessages() | AI SDK"
description: toAISdkMessages() 的 API 參考。此函數可將 Mastra 訊息轉換成 AI SDK v5 或 v6 的 AI SDK UI 訊息。
packages:
  - "@mastra/ai-sdk"
  - "@mastra/client-js"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# toAISdkMessages() {/* #toaisdkmessages */}

將輸入格式的訊息轉換成 AI SDK UI 訊息格式。`toAISdkMessages()` 會保留現有的 AI SDK v5／預設行為。如果你的應用程式是按 AI SDK v6 定型，請傳入 `version: 'v6'`。

`version: 'v6'` 會使用 MessageList 的 AI SDK v6 UI 輸出路徑。MessageList 的輸入偵測及擷取方式維持不變。

## 使用範例 {/* #usage-example */}

```typescript title="app/chat/page.tsx"
import { toAISdkMessages } from '@mastra/ai-sdk/ui'
import { useChat } from 'ai/react'

// Stored messages from your database, memory, or API
const storedMessages = [
  { id: '1', role: 'user', content: 'Hello', parts: [{ type: 'text', text: 'Hello' }] },
  {
    id: '2',
    role: 'assistant',
    content: 'Hi there!',
    parts: [{ type: 'text', text: 'Hi there!' }],
  },
]

export default function Chat() {
  const { messages } = useChat({
    initialMessages: toAISdkMessages(storedMessages, { version: 'v6' }),
  })

  return (
    <div>
      {messages.map(message => (
        <div key={message.id}>
          {message.role}: {message.parts.map(part => (part.type === 'text' ? part.text : null))}
        </div>
      ))}
    </div>
  )
}
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'messages',
    type: 'MessageListInput',
    description: '要轉換的訊息。可以是字串、字串陣列、單一訊息物件，或任何支援格式的訊息物件陣列。',
    isOptional: false,
  },
  {
    name: 'options.version',
    type: "'v5' | 'v6'",
    description:
      "選擇要傳回的 AI SDK 訊息類型。如要使用現有的預設行為，請省略此項或傳入 `'v5'`。如果你的應用程式是按 AI SDK v6 的 `useChat()` 訊息類型定型，請傳入 `'v6'`。",
    isOptional: true,
  },
]}
/>

## 傳回值 {/* #returns */}

傳回按所選版本定型的 AI SDK `UIMessage` 物件陣列。

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '訊息的唯一識別碼。',
  },
  {
    name: 'role',
    type: "'user' | 'assistant' | 'system'",
    description: '訊息傳送者的角色。',
  },
  {
    name: 'parts',
    type: 'UIMessagePart[]',
    description: 'UI 部分的陣列，當中包括文字、Tool 結果、檔案、推理、來源及步驟標記。',
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: '可選的元資料，包括 createdAt、threadId、resourceId 及自訂欄位。',
    isOptional: true,
  },
]}
/>

## 範例 {/* #examples */}

### 使用預設的 AI SDK v5 類型 {/* #using-the-default-ai-sdk-v5-types */}

```typescript
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const messages = toAISdkMessages(['Hello', 'How can I help you today?'])
```

### 傳回 AI SDK v6 訊息類型 {/* #returning-ai-sdk-v6-message-types */}

```typescript
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const messages = toAISdkMessages(['Hello', 'How can I help you today?'], {
  version: 'v6',
})
```

### 使用 Mastra client 載入訊息 {/* #loading-messages-with-mastra-client */}

```typescript
import { MastraClient } from '@mastra/client-js'
import { toAISdkMessages } from '@mastra/ai-sdk/ui'

const client = new MastraClient()

const { messages } = await client.listThreadMessages('thread-id', { agentId: 'myAgent' })
const uiMessages = toAISdkMessages(messages)
```

## 相關內容 {/* #related */}

- [`toAISdkStream()`](/reference/ai-sdk/to-ai-sdk-stream)
- [`toAISdkV5Messages()`](/reference/ai-sdk/to-ai-sdk-v5-messages)
- [`toAISdkV4Messages()`](/reference/ai-sdk/to-ai-sdk-v4-messages)
