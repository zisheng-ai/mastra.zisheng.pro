---
title: "參考：Run.start() | Workflows"
description: Workflow 中 `Run.start()` 方法的文件，此方法會使用輸入資料開始執行 Workflow。
packages:
  - "@mastra/core"
---

# Run.start()

`.start()` 方法會使用輸入資料從頭開始執行 Workflow。

## 使用範例

```typescript
const run = await workflow.createRun()

const result = await run.start({
  inputData: {
    value: 'initial data',
  },
})
```

## 參數

<PropertiesTable
  content={[
  {
    name: 'inputData',
    type: 'z.infer<TInput>',
    description: "符合 Workflow 輸入 schema 的輸入資料",
    isOptional: true,
  },
  {
    name: 'requestContext',
    type: 'RequestContext',
    description: '執行 Workflow 時使用的 Request Context 資料',
    isOptional: true,
  },
  {
    name: 'outputWriter',
    type: '(chunk: TOutput) => Promise<void>',
    description: '用於在輸出區塊產生時加以處理的可選非同步函式',
    isOptional: true,
  },
  {
    name: 'tracingContext',
    type: 'TracingContext',
    isOptional: true,
    description:
      "用於建立子 span 及加入 metadata 的 Tracing context。使用 Mastra 的 tracing 系統時會自動注入。",
    properties: [
      {
        parameters: [
          {
            name: 'currentSpan',
            type: 'Span',
            isOptional: true,
            description:
              '用於建立子 span 及加入 metadata 的目前 span。可用於建立自訂子 span，或在執行期間更新 span 屬性。',
          },
        ],
      },
    ],
  },
  {
    name: 'tracingOptions',
    type: 'TracingOptions',
    isOptional: true,
    description: 'Tracing 設定選項。',
    properties: [
      {
        parameters: [
          {
            name: 'metadata',
            type: 'Record<string, any>',
            isOptional: true,
            description:
              '加入根 trace span 的 metadata。適合用於加入自訂屬性，例如用戶 ID、session ID 或 feature flag。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'requestContextKeys',
            type: 'string[]',
            isOptional: true,
            description:
              "額外擷取為此 trace metadata 的 RequestContext key。支援以點號標記法指定巢狀值（例如 'user.id'）。",
          },
        ],
      },
      {
        parameters: [
          {
            name: 'traceId',
            type: 'string',
            isOptional: true,
            description:
              '此執行使用的 Trace ID（1 至 32 個十六進制字元）。如有提供，此 trace 將會成為指定 trace 的一部分。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'parentSpanId',
            type: 'string',
            isOptional: true,
            description:
              '此執行使用的父 span ID（1 至 16 個十六進制字元）。如有提供，根 span 將會建立為此 span 的子項。',
          },
        ],
      },
      {
        parameters: [
          {
            name: 'tags',
            type: 'string[]',
            isOptional: true,
            description: '套用至此 trace 的標籤。這些字串標籤可用於分類及篩選 trace。',
          },
        ],
      },
    ],
  },
  {
    name: 'outputOptions',
    type: 'OutputOptions',
    isOptional: true,
    description: '輸出設定選項。',
    properties: [
      {
        parameters: [
          {
            name: 'includeState',
            type: 'boolean',
            isOptional: true,
            description: '是否在結果中包含 Workflow 執行狀態。',
          },
        ],
      },
    ],
  },
]}
/>

## 傳回值

<PropertiesTable
  content={[
  {
    name: 'result',
    type: 'Promise<WorkflowResult<TState, TOutput, TSteps>>',
    description: '解析為 Workflow 執行結果的 promise，當中包含步驟輸出及狀態',
  },
  {
    name: 'traceId',
    type: 'string',
    isOptional: true,
    description:
      '啟用 Tracing 時與此執行相關的 trace ID。可用於關聯日誌及偵錯執行流程。',
  },
  {
    name: 'spanId',
    type: 'string',
    isOptional: true,
    description:
      '啟用 Tracing 時與此執行相關的根 span ID。可用於 span 層級的查找及關聯。',
  },
]}
/>

## 延伸使用範例

```typescript
import { RequestContext } from '@mastra/core/request-context'

const run = await workflow.createRun()

const requestContext = new RequestContext()
requestContext.set('variable', false)

const result = await run.start({
  inputData: {
    value: 'initial data',
  },
  requestContext,
})
```

## 相關內容

- [Workflows 概覽](/docs/workflows/overview#running-workflows)
- [Workflow.createRun()](../workflow-methods/create-run)
