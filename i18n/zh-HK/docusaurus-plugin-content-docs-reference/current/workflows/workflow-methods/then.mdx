---
title: "參考：Workflow.then() | Workflows"
description: Workflow 中 `Workflow.then()` 方法的文件；此方法會在步驟之間建立順序依賴關係。
packages:
  - "@mastra/core"
---

# Workflow\.then()

`.then()` 方法會在 Workflow 步驟之間建立順序依賴關係，確保步驟按特定次序執行。

## 使用範例 {/* #usage-example */}

```typescript
workflow.then(step1).then(step2)
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'step',
    type: 'Step',
    description: '上一個步驟完成後應執行的步驟實例',
    isOptional: false,
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'NewWorkflow',
    description: '用於串連方法的 Workflow 實例',
  },
]}
/>

## 相關內容 {/* #related */}

- [控制流程](/docs/workflows/control-flow)
