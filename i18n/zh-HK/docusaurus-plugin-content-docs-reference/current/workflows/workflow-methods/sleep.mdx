---
title: "參考：Workflow.sleep() | Workflows"
description: Workflow 中 `Workflow.sleep()` 方法的文件；此方法會暫停執行指定的毫秒數。
packages:
  - "@mastra/core"
---

# Workflow\.sleep()

`.sleep()` 方法會暫停執行指定的毫秒數。延遲時間可接受靜態數值，亦可接受在執行階段決定延遲時間的 callback function。

## 使用範例 {/* #usage-example */}

```typescript
workflow.sleep(5000)
```

## 參數 {/* #parameters */}

<PropertiesTable
  content={[
  {
    name: 'milliseconds',
    type: 'number | ((context: { inputData: any }) => number | Promise<number>)',
    description: '暫停執行的毫秒數，或傳回延遲時間的 callback function',
    isOptional: false,
  },
]}
/>

## 傳回值 {/* #returns */}

<PropertiesTable
  content={[
  {
    name: 'workflow',
    type: 'Workflow',
    description: '用於串連方法的 Workflow 實例',
  },
]}
/>

## 延伸使用範例 {/* #extended-usage-example */}

```typescript prettier:false
import { createWorkflow, createStep } from "@mastra/core/workflows";

const step1 = createStep({...});
const step2 = createStep({...});

export const testWorkflow = createWorkflow({...})
  .then(step1)
  .sleep(async ({ inputData }) => {
    const { delayInMs } = inputData;
    return delayInMs;
  })
  .then(step2)
  .commit();
```
