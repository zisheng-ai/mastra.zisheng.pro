---
title: "參考：StorageBrowserRef | Editor"
description: "StorageBrowserRef 和 StorageBrowserConfig 參考，即附加至已儲存 Agent 的 inline 瀏覽器配置。"
packages:
  - "@mastra/core"
---

# StorageBrowserRef

`StorageBrowserRef` 是附加至已儲存 Agent 的 inline 瀏覽器配置。`provider` id 會在 hydrate 時，根據已在 [`MastraEditor.browsers`](/reference/editor/mastra-editor) 註冊的 [`BrowserProvider`](/reference/editor/browser-provider) 解析。

這是 [`BuilderAgentDefaults.browser`](https://agent-builder.mastra.ai/reference/builder-agent-defaults) 及已儲存 Agent 記錄使用的類型。

## 使用範例 {/* #usage-example */}

```typescript title="src/mastra/index.ts"
import { MastraEditor } from '@mastra/editor'
import { stagehandBrowserProvider } from '@mastra/stagehand'

new MastraEditor({
  browsers: { [stagehandBrowserProvider.id]: stagehandBrowserProvider },
  builder: {
    enabled: true,
    configuration: {
      agent: {
        browser: {
          type: 'inline',
          config: {
            provider: 'stagehand',
            headless: true,
            viewport: { width: 1280, height: 720 },
          },
        },
      },
    },
  },
})
```

## 類型 {/* #type */}

```typescript
type StorageBrowserRef = { type: 'inline'; config: StorageBrowserConfig }
```

瀏覽器不提供 `{ type: 'id' }` 變體：它們一律以 inline 方式加入。

## 屬性 {/* #properties */}

<PropertiesTable
  content={[
  { name: 'type', type: "'inline'", description: '判別欄位。必須是字面值 `"inline"`。' },
  { name: 'config', type: 'StorageBrowserConfig', description: 'Provider id 及每個實例的瀏覽器配置。請參閱下節。' },
]}
/>

## `StorageBrowserConfig` {/* #storagebrowserconfig */}

嵌入 `config` 下的資料結構，定義於 `@mastra/core/storage`。

<PropertiesTable
  content={[
  {
    name: 'provider',
    type: 'string',
    description:
      'Provider 標識符（例如 `"stagehand"`）。必須符合已在 `MastraEditor.browsers` 註冊的 `BrowserProvider.id`。沒有內置 Provider。',
  },
  {
    name: 'headless',
    type: 'boolean',
    description: '以 headless 模式執行瀏覽器（不顯示 UI）。',
    isOptional: true,
    defaultValue: 'true',
  },
  {
    name: 'viewport',
    type: '{ width: number; height: number }',
    description: '瀏覽器 viewport 尺寸，控制視窗大小及網頁呈現方式。',
    isOptional: true,
  },
  {
    name: 'timeout',
    type: 'number',
    description: '瀏覽器操作的預設逾時時間（毫秒）。',
    isOptional: true,
    defaultValue: '10000',
  },
  {
    name: 'screencast',
    type: 'ScreencastOptions',
    description: '將瀏覽器影格串流至 UI 的 screencast 選項。',
    isOptional: true,
    properties: [
      {
        type: 'ScreencastOptions',
        parameters: [
          {
            name: 'format',
            type: "'jpeg' | 'png'",
            description: '圖片格式。',
            isOptional: true,
            defaultValue: "'jpeg'",
          },
          {
            name: 'quality',
            type: 'number',
            description: 'JPEG 品質，範圍為 0–100。',
            isOptional: true,
            defaultValue: '80',
          },
          {
            name: 'maxWidth',
            type: 'number',
            description: '最大闊度（像素）。',
            isOptional: true,
            defaultValue: '1280',
          },
          {
            name: 'maxHeight',
            type: 'number',
            description: '最大高度（像素）。',
            isOptional: true,
            defaultValue: '720',
          },
          {
            name: 'everyNthFrame',
            type: 'number',
            description: '每隔 N 個影格擷取一次。',
            isOptional: true,
            defaultValue: '1',
          },
        ],
      },
    ],
  },
]}
/>

## Hydrate 流程 {/* #hydration */}

`StorageBrowserRef` 會在 `mastra.editor.agent.getById()` 中延遲解析。Editor 會在 `MastraEditor.browsers` 查找 `config.provider`，再呼叫 `provider.createBrowser(config)`。如果 Provider 未註冊，Editor 會記錄警告並傳回 `undefined`：Agent 仍會載入，但不會有瀏覽器。

## 相關內容 {/* #related */}

- [Browser](https://agent-builder.mastra.ai/browser)：概念及完整範例。
- [BrowserProvider](/reference/editor/browser-provider)：供實作者使用的 Provider 介面。
- [BuilderAgentDefaults](https://agent-builder.mastra.ai/reference/builder-agent-defaults)：將此類型固定為 Builder 預設值的位置。
- [StorageWorkspaceRef](/reference/editor/storage-workspace-ref)：Workspace 配置的同類引用類型。
