---
title: "參考：WorkspaceSandbox | Workspace"
description: "定義指令執行操作的 WorkspaceSandbox 介面文件。"
packages:
  - "@mastra/core"
---

# WorkspaceSandbox

**新增於：** `@mastra/core@1.1.0`

`WorkspaceSandbox` 介面定義 Workspace 如何執行指令及管理背景程序。

## 屬性

<PropertiesTable
  content={[
  {
    name: 'processes',
    type: 'SandboxProcessManager',
    description: '背景程序管理器。如未實作，程序管理 Tool 將無法使用。請參閱 SandboxProcessManager 參考資料。',
    isOptional: true,
  },
]}
/>

## 方法

### `start()`

啟動 Sandbox，並由 `workspace.init()` 或首次呼叫 `executeCommand()` 時自動呼叫。

```typescript
await sandbox.start()
```

此方法會準備 Sandbox 以執行指令。

### `stop()`

停止 Sandbox。

```typescript
await sandbox.stop?.()
```

### `destroy()`

清理 Sandbox 資源。由 `workspace.destroy()` 呼叫。

```typescript
await sandbox.destroy()
```

### `executeCommand(command, args?, options?)`

執行 shell 指令。

```typescript
const result = await sandbox.executeCommand('ls', ['-la', '/docs'])
const result = await sandbox.executeCommand('npm', ['install', 'lodash'])
```

**參數：**

<PropertiesTable
  content={[
  {
    name: 'command',
    type: 'string',
    description: '要執行的指令',
  },
  {
    name: 'args',
    type: 'string[]',
    description: '指令引數',
    isOptional: true,
  },
  {
    name: 'options',
    type: 'Options',
    description: '設定選項。',
    isOptional: true,
    properties: [
      {
        type: 'Options',
        parameters: [
          {
            name: 'timeout',
            type: 'number',
            description: '執行逾時時間（毫秒）',
            isOptional: true,
          },
          {
            name: 'cwd',
            type: 'string',
            description: '指令的工作目錄',
            isOptional: true,
          },
          {
            name: 'env',
            type: 'Record<string, string>',
            description: '額外環境變數',
            isOptional: true,
          },
          {
            name: 'onStdout',
            type: '(data: string) => void',
            description: 'stdout 串流的回呼函數',
            isOptional: true,
          },
          {
            name: 'onStderr',
            type: '(data: string) => void',
            description: 'stderr 串流的回呼函數',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

### `getInfo()`

取得 Sandbox 狀態及資源資料。

```typescript
const info = await sandbox.getInfo()
// { status: 'running', resources: { memoryMB: 512, cpuPercent: 5 } }
```

### `getInstructions(opts?)`

傳回此 Sandbox 運作方式的說明。當 Workspace 指派給 Agent 時，該說明會注入 Agent 的系統訊息。

```typescript
const instructions = sandbox.getInstructions?.()
// 'Local command execution. Working directory: "/workspace".'
```

**參數：**

<PropertiesTable
  content={[
  {
    name: 'opts.requestContext',
    type: 'RequestContext',
    description: '如果建構函數有提供 `instructions` 函數，便會將此值轉交給該函數。',
    isOptional: true,
  },
]}
/>

**傳回：** `string`

## 相關內容

- [SandboxProcessManager 參考資料](/reference/workspace/process-manager)
- [Sandbox](/docs/workspace/sandbox)
- [Workspace 類別](/reference/workspace/workspace-class)
- [檔案系統介面](/reference/workspace/filesystem)
