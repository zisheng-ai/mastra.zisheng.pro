---
title: "參考：MesaFilesystem | Workspace"
description: "用於版本化 Mesa repo 的 MesaFilesystem Provider API 參考。"
packages:
  - "@mastra/mesa"
---

# MesaFilesystem

透過標準 Mastra `WorkspaceFilesystem` 介面，將 Workspace 檔案儲存在 [Mesa](https://docs.mesa.dev/content/getting-started/introduction) repo。

當 Agent 需要版本化檔案儲存空間時，請使用 `MesaFilesystem`。本機目錄請使用 [`LocalFilesystem`](/reference/workspace/local-filesystem)。物件儲存空間請使用 [`S3Filesystem`](/reference/workspace/s3-filesystem)、[`GCSFilesystem`](/reference/workspace/gcs-filesystem) 或 [`AzureBlobFilesystem`](/reference/workspace/azure-blob-filesystem)。

:::info

`MesaFilesystem` 在 Mastra 程序中執行。

[Mesa 的 POSIX mount](https://docs.mesa.dev/content/mesafs/posix-mount)（用於在 Sandbox 內使用 Mesa 檔案系統）尚未包含在 `@mastra/mesa` 套件中。即將提供支援。

:::

## 安裝 {/* #installation */}

```bash npm2yarn
npm install @mastra/mesa
```

## 用法範例 {/* #usage-example */}

Mount 一個 Mesa repo，並將檔案系統傳給 Workspace：

```typescript title="src/mastra/workspace.ts"
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { MesaFilesystem } from '@mastra/mesa'

const workspace = new Workspace({
  filesystem: new MesaFilesystem({
    apiKey: process.env.MESA_API_KEY,
    org: 'acme',
    repos: [{ name: 'docs', bookmark: 'main' }],
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_ANTHROPIC_MODEL_OPUS__',
  workspace,
})
```

省略 `apiKey` 時，會後備使用 `MESA_API_KEY`。

## 建構函式參數 {/* #constructor-parameters */}

<PropertiesTable
  content={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Mesa API key。省略時會後備使用 `MESA_API_KEY`。',
    isOptional: true,
  },
  {
    name: 'org',
    type: 'string',
    description: 'Mesa org slug。省略時，Mesa SDK 會解析預設 org。',
    isOptional: true,
  },
  {
    name: 'repos',
    type: 'RepoConfig[]',
    description: '要 mount 的 Mesa repo。',
  },
  {
    name: 'cache',
    type: '{ diskCache?: { path: string; maxSizeBytes?: number } }',
    description: 'Mesa 檔案系統快取設定。',
    isOptional: true,
  },
  {
    name: 'ttl',
    type: 'number',
    description: 'Mesa mount token 的有效期（秒）。',
    isOptional: true,
  },
  {
    name: 'readOnly',
    type: 'boolean',
    description: '以唯讀方式 mount 所有 repo。',
    isOptional: true,
    defaultValue: 'false',
  },
]}
/>

## 屬性 {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '檔案系統實例識別碼。',
  },
  {
    name: 'name',
    type: 'string',
    description: "Provider 名稱（`'MesaFilesystem'`）。",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Provider 識別碼（`'mesa'`）。",
  },
  {
    name: 'readOnly',
    type: 'boolean | undefined',
    description: '是否封鎖寫入操作。',
  },
  {
    name: 'client',
    type: 'Mesa | undefined',
    description: '由此 Provider 建立的 Mesa SDK client。初始化前為 undefined。',
  },
  {
    name: 'filesystem',
    type: 'MesaFileSystem',
    description: '使用中的 Mesa 檔案系統。初始化後如需原始 Mesa SDK 檔案系統，可存取此屬性。',
  },
  {
    name: 'change',
    type: 'MesaFileSystem["change"]',
    description: '已 mount 檔案系統的 Mesa 變更管理操作。',
  },
  {
    name: 'bookmark',
    type: 'MesaFileSystem["bookmark"]',
    description: '已 mount 檔案系統的 Mesa bookmark 管理操作。',
  },
]}
/>

## 方法 {/* #methods */}

`MesaFilesystem` 會實作 [WorkspaceFilesystem 介面](/reference/workspace/filesystem)。

### 檔案操作 {/* #file-operations */}

#### `readFile(path, options?)` {/* #readfilepath-options */}

從 Mesa 讀取檔案。

```typescript
const content = await filesystem.readFile('/acme/docs/README.md', {
  encoding: 'utf-8',
})
```

傳回：`Promise<string | Buffer>`

#### `writeFile(path, content, options?)` {/* #writefilepath-content-options */}

將檔案寫入 Mesa。

```typescript
await filesystem.writeFile('/acme/docs/report.md', '# Report')
```

傳回：`Promise<void>`

#### `appendFile(path, content)` {/* #appendfilepath-content */}

將內容附加至檔案。

```typescript
await filesystem.appendFile('/acme/docs/log.txt', 'new line\n')
```

傳回：`Promise<void>`

#### `deleteFile(path, options?)` {/* #deletefilepath-options */}

刪除檔案。

```typescript
await filesystem.deleteFile('/acme/docs/old-report.md')
```

傳回：`Promise<void>`

#### `copyFile(src, dest, options?)` {/* #copyfilesrc-dest-options */}

複製檔案。

```typescript
await filesystem.copyFile('/acme/docs/report.md', '/acme/docs/archive/report.md')
```

傳回：`Promise<void>`

#### `moveFile(src, dest, options?)` {/* #movefilesrc-dest-options */}

移動檔案或重新命名。

```typescript
await filesystem.moveFile('/acme/docs/draft.md', '/acme/docs/final.md')
```

傳回：`Promise<void>`

### 目錄操作 {/* #directory-operations */}

#### `mkdir(path, options?)` {/* #mkdirpath-options */}

建立目錄。

```typescript
await filesystem.mkdir('/acme/docs/reports', { recursive: true })
```

傳回：`Promise<void>`

#### `rmdir(path, options?)` {/* #rmdirpath-options */}

移除目錄。

```typescript
await filesystem.rmdir('/acme/docs/reports', { recursive: true })
```

傳回：`Promise<void>`

#### `readdir(path, options?)` {/* #readdirpath-options */}

列出目錄項目。

```typescript
const entries = await filesystem.readdir('/acme/docs', {
  recursive: true,
  extension: '.md',
})
```

傳回：`Promise<FileEntry[]>`

### 路徑操作 {/* #path-operations */}

#### `exists(path)` {/* #existspath */}

檢查路徑是否存在。

```typescript
const exists = await filesystem.exists('/acme/docs/README.md')
```

傳回：`Promise<boolean>`

#### `stat(path)` {/* #statpath */}

傳回檔案或目錄的元數據。

```typescript
const stat = await filesystem.stat('/acme/docs/README.md')
```

傳回：`Promise<FileStat>`

#### `realpath(path)` {/* #realpathpath */}

傳回 Mesa 的標準路徑。

```typescript
const realPath = await filesystem.realpath('/acme/docs/README.md')
```

傳回：`Promise<string>`

### Mesa 操作 {/* #mesa-operations */}

#### `bash(options?)` {/* #bashoptions */}

為此檔案系統建立由 Mesa 支援的 Bash runtime。

```typescript
const bash = await filesystem.bash({
  cwd: '/acme/docs',
})
```

傳回：`Promise<Bash>`

## 路徑語意 {/* #path-semantics */}

方法需要使用絕對路徑。對於 `MesaFilesystem`，路徑以 Mesa mount 為根目錄，並必須包括 org slug 及 repo 名稱：

```typescript
await filesystem.readFile('/acme/docs/README.md')
```

請勿省略開首斜線：

```typescript
await filesystem.readFile('acme/docs/README.md') // Incorrect
await filesystem.readFile('/acme/docs/README.md') // Correct
```

Org 來自建構函式中的 `org`；如省略 `org`，則來自 Mesa SDK 的預設 org 推斷。它仍會是路徑的第一個部分。

Mount 多個 repo 時，每個 repo 都可在 org 部分下使用：

```typescript
const filesystem = new MesaFilesystem({
  org: 'acme',
  repos: [
    { name: 'docs', bookmark: 'main' },
    { name: 'website', bookmark: 'main' },
  ],
})

await filesystem.readFile('/acme/docs/README.md')
await filesystem.readFile('/acme/website/package.json')
```

## Mesa 版本控制 API {/* #mesa-versioning-apis */}

存取底層 Mesa 檔案系統，以執行 Mesa 特有的變更及 bookmark 操作：

```typescript
await filesystem.writeFile('/acme/docs/draft.md', 'Draft')

const current = await filesystem.change.current({
  repo: 'docs',
})

await filesystem.bookmark.move({
  repo: 'docs',
  name: 'main',
  changeId: current.changeId,
})
```

有關版本控制語意的詳情，請參閱 [Mesa 文件](https://docs.mesa.dev/content/concepts/versioning)。

## 唯讀模式 {/* #read-only-mode */}

設定 `readOnly: true`，封鎖透過 Mastra 進行的寫入操作：

```typescript
const filesystem = new MesaFilesystem({
  repos: [{ name: 'docs', bookmark: 'main' }],
  readOnly: true,
})
```

讀取操作仍可正常運作。寫入操作會拋出 `WorkspaceReadOnlyError`。

## 並行處理 {/* #concurrency */}

`overwrite: false` 及 `expectedMtime` 會在寫入前進行預檢。除非 Mesa 為 app mount 加入原生條件式寫入功能，否則這些檢查不具原子性。

## 相關內容 {/* #related */}

- [WorkspaceFilesystem 介面](/reference/workspace/filesystem)
- [檔案系統文件](/docs/workspace/filesystem)
- [Workspace 概覽](/docs/workspace/overview)
- [Mesa 文件](https://docs.mesa.dev/content/getting-started/introduction)
