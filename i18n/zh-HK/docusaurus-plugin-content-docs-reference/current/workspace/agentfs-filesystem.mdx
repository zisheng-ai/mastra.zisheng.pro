---
title: "參考：AgentFSFilesystem | Workspace"
description: "Turso/SQLite 支援儲存空間的 AgentFSFilesystem Provider 文件。"
packages:
  - "@mastra/agentfs"
---

# AgentFSFilesystem

透過 [AgentFS](https://github.com/nichochar/agentfs) SDK 將檔案儲存在 Turso/SQLite 資料庫中。檔案會跨工作階段持久保存於本機 SQLite 資料庫，讓 Agent 無需外部雲端服務亦可擁有持久儲存空間。如需介面詳情，請參閱 [WorkspaceFilesystem 介面](/reference/workspace/filesystem)。

## 安裝 {/* #installation */}

```bash npm2yarn
npm install @mastra/agentfs
```

## 使用方式 {/* #usage */}

將 `AgentFSFilesystem` 加入 Workspace，並將其指派給 Agent。建立 `AgentFSFilesystem` 類別實例時，必須至少提供 `agentId`、`path` 或 `agent` 其中一項。

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { AgentFSFilesystem } from '@mastra/agentfs'

const workspace = new Workspace({
  filesystem: new AgentFSFilesystem({
    agentId: 'my-agent',
  }),
})

const agent = new Agent({
  id: 'file-agent',
  name: 'file-agent',
  model: '__GATEWAY_OPENAI_MODEL__',
  workspace,
})
```

### 使用明確指定的資料庫路徑 {/* #using-an-explicit-database-path */}

根據預設設定，資料庫會儲存在 `.agentfs` 目錄內，並以 `agentId` 作為檔案名稱。你可以指定自訂資料庫路徑：

```typescript
import { AgentFSFilesystem } from '@mastra/agentfs'

const filesystem = new AgentFSFilesystem({
  path: '/data/my-agent.db',
})
```

### 使用預先開啟的 AgentFS 實例 {/* #using-a-pre-opened-agentfs-instance */}

如果你需要自行管理 AgentFS 生命週期：

```typescript
import { AgentFS } from 'agentfs-sdk'
import { AgentFSFilesystem } from '@mastra/agentfs'

const agent = await AgentFS.open({ id: 'my-agent' })

const filesystem = new AgentFSFilesystem({
  agent, // caller manages open/close
})
```

## 建構函數參數 {/* #constructor-parameters */}

你必須至少提供 `agentId`、`path` 或 `agent` 其中一項。

<PropertiesTable
  content={[
  {
    name: 'agentId',
    type: 'string',
    description: 'Agent ID — 在 `.agentfs/<agentId>.db` 建立資料庫',
    isOptional: true,
  },
  {
    name: 'path',
    type: 'string',
    description: '明確指定的資料庫檔案路徑（可取代 agentId）',
    isOptional: true,
  },
  {
    name: 'agent',
    type: 'AgentFS',
    description: '預先開啟的 AgentFS 實例。提供此項時，呼叫者負責管理生命週期（開啟／關閉）。',
    isOptional: true,
  },
  {
    name: 'id',
    type: 'string',
    description: '此檔案系統實例的唯一識別碼',
    isOptional: true,
    defaultValue: '自動產生',
  },
  {
    name: 'displayName',
    type: 'string',
    description: '供使用者介面顯示、方便閱讀的名稱',
    isOptional: true,
    defaultValue: "'AgentFS'",
  },
  {
    name: 'icon',
    type: 'FilesystemIcon',
    description: '使用者介面的圖示識別碼',
    isOptional: true,
    defaultValue: "'database'",
  },
  {
    name: 'description',
    type: 'string',
    description: '在使用者介面中顯示的檔案系統簡短說明',
    isOptional: true,
  },
  {
    name: 'readOnly',
    type: 'boolean',
    description: '設為 true 時，所有寫入操作都會被封鎖',
    isOptional: true,
    defaultValue: 'false',
  },
]}
/>

## 屬性 {/* #properties */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '檔案系統實例識別碼',
  },
  {
    name: 'name',
    type: 'string',
    description: "Provider 名稱（'AgentFSFilesystem'）",
  },
  {
    name: 'provider',
    type: 'string',
    description: "Provider 識別碼（'agentfs'）",
  },
  {
    name: 'readOnly',
    type: 'boolean | undefined',
    description: '檔案系統是否處於唯讀模式',
  },
]}
/>
