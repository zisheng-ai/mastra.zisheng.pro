---
title: "參考：VercelServerlessSandbox | Workspace"
description: "VercelServerlessSandbox 的 API 參考資料。這個無伺服器 Sandbox Provider 會以 Vercel Functions 的形式部署及執行指令。"
packages:
  - "@mastra/vercel"
---

# VercelServerlessSandbox

以 [Vercel](https://vercel.com) 無伺服器函數的形式執行指令。提供全球分佈、自動擴展且毋須管理基礎架構的執行環境。如需介面詳情，請參閱 [WorkspaceSandbox 介面](/reference/workspace/sandbox)。

:::warning

VercelServerlessSandbox 不保留狀態。它不提供持久檔案系統、互動式 shell 或長時間運行的背景程序。只有 `/tmp` 可供寫入，而且每次調用之間的內容都是暫時的。

:::

:::note

如需具備持久檔案系統、`sudo` 存取權、公開連接埠及背景程序的完整 Linux MicroVM，請使用由 [Vercel Sandbox](https://vercel.com/docs/vercel-sandbox) 產品支援的 [`VercelSandbox`](/reference/workspace/vercel-sandbox)。

:::

## 安裝

```bash npm2yarn
npm install @mastra/vercel
```

## 使用方式

將 `VercelServerlessSandbox` 加入 Workspace，並將其指派給 Agent：

```typescript
import { Agent } from '@mastra/core/agent'
import { Workspace } from '@mastra/core/workspace'
import { VercelServerlessSandbox } from '@mastra/vercel'

const workspace = new Workspace({
  sandbox: new VercelServerlessSandbox({
    token: process.env.VERCEL_TOKEN,
  }),
})

const agent = new Agent({
  id: 'dev-agent',
  name: 'dev-agent',
  instructions: 'You are a coding assistant working in this workspace.',
  model: '__GATEWAY_ANTHROPIC_MODEL_SONNET__',
  workspace,
})
```

### 使用自訂資源的團隊範圍部署

```typescript
const workspace = new Workspace({
  sandbox: new VercelServerlessSandbox({
    token: process.env.VERCEL_TOKEN,
    teamId: 'team_abc123',
    regions: ['iad1', 'sfo1'],
    maxDuration: 120,
    memory: 3008,
    env: {
      NODE_ENV: 'production',
    },
  }),
})
```

## 建構函數參數

<PropertiesTable
  content={[
  {
    name: 'token',
    type: 'string',
    description: 'Vercel API token。未提供時會改用 `VERCEL_TOKEN` 環境變數。',
    isOptional: true,
  },
  {
    name: 'teamId',
    type: 'string',
    description: '用於團隊範圍部署的 Vercel 團隊 ID。',
    isOptional: true,
  },
  {
    name: 'projectName',
    type: 'string',
    description: '現有 Vercel 專案名稱。如省略，系統會自動產生。',
    isOptional: true,
  },
  {
    name: 'regions',
    type: 'string[]',
    description: '部署區域。',
    isOptional: true,
    defaultValue: "['iad1']",
  },
  {
    name: 'maxDuration',
    type: 'number',
    description: '函數的最長持續時間（秒）。',
    isOptional: true,
    defaultValue: '60',
  },
  {
    name: 'memory',
    type: 'number',
    description: '函數記憶體（MB）。',
    isOptional: true,
    defaultValue: '1024',
  },
  {
    name: 'env',
    type: 'Record<string, string>',
    description:
      '內嵌於已部署函數原始碼的環境變數。這些變數在部署時嵌入，而非動態設定。',
    isOptional: true,
  },
  {
    name: 'commandTimeout',
    type: 'number',
    description: '每次調用的指令逾時時間（毫秒）。',
    isOptional: true,
    defaultValue: '55000',
  },
  {
    name: 'instructions',
    type: 'string | ((opts) => string)',
    description:
      '自訂指示，用以覆寫 `getInstructions()` 傳回的預設指示。傳入字串可完全取代預設指示，亦可傳入函數以擴充預設內容。',
    isOptional: true,
  },
  {
    name: 'onStart',
    type: 'SandboxLifecycleHook',
    description: 'Sandbox 達到 `running` 狀態後呼叫的生命週期 hook。',
    isOptional: true,
  },
  {
    name: 'onStop',
    type: 'SandboxLifecycleHook',
    description: 'Sandbox 停止前呼叫的生命週期 hook。',
    isOptional: true,
  },
  {
    name: 'onDestroy',
    type: 'SandboxLifecycleHook',
    description: 'Sandbox 銷毀前呼叫的生命週期 hook。',
    isOptional: true,
  },
]}
/>

## 屬性

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '此 Sandbox 實例的唯一識別碼。',
  },
  {
    name: 'name',
    type: "'VercelServerlessSandbox'",
    description: '方便閱讀的名稱。',
  },
  {
    name: 'provider',
    type: "'vercel-serverless'",
    description: 'Provider 類型識別碼。',
  },
  {
    name: 'status',
    type: 'ProviderStatus',
    description:
      "目前的生命週期狀態：`'pending'`、`'starting'`、`'running'`、`'stopping'`、`'stopped'`、`'destroying'`、`'destroyed'` 或 `'error'`。",
  },
]}
/>

## 限制

VercelServerlessSandbox 在底層使用無伺服器函數，這表示：

- **沒有持久檔案系統**：寫入 `/tmp` 的檔案是暫時的，並會在每次調用之間清除。
- **沒有互動式 shell**：指令透過 `/bin/sh -c` 執行，不支援 stdin 串流。
- **沒有背景程序**：不提供程序管理器、PID 或長時間運行的工作。
- **不支援掛載**：不支援掛載雲端儲存空間（S3、GCS）。
- **逾時限制**：最長執行時間受 `maxDuration` 限制（預設為 60 秒）。

## 相關內容

- [WorkspaceSandbox 介面](/reference/workspace/sandbox)
- [Workspace 類別](/reference/workspace/workspace-class)
- [E2BSandbox](/reference/workspace/e2b-sandbox)：具備持久檔案系統及程序管理功能的完整雲端 Sandbox
