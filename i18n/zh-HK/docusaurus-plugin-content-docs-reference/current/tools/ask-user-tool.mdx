---
title: "參考：askUserTool | Tools"
description: "askUserTool 的 API 參考；這個內置 Tool 會暫停 Agent 執行以向用戶提問，並在收到答案後恢復執行。"
packages:
  - "@mastra/core"
---

# askUserTool

這是一個與 Agent 無關的內置 Tool，可向用戶提問並等候回覆。此 Tool 支援自由文字問題、單選提示及多選提示。

此 Tool 透過原生的 [Tool 暫停](/docs/agents/agent-approval#runtime-suspension-with-suspend)原語來暫停：它會使用問題負載呼叫 `suspend()`，令 Agent 發出 `tool-call-suspended` 事件並保存執行狀態。使用 `agent.resumeStream(answer, { runId })` 恢復執行。

在 Agent 執行以外的環境執行時（沒有可用的 `suspend`），此 Tool 會傳回包含問題及選項、清晰易讀的後備字串。

## 用法範例 {/* #usage-example */}

將 `askUserTool` 加入 Agent 的 Tool 集合：

```typescript title="src/mastra/agents/index.ts"
import { Agent } from '@mastra/core/agent'
import { askUserTool } from '@mastra/core/tools'

const agent = new Agent({
  id: 'assistant',
  name: 'Assistant',
  instructions: 'Ask the user for clarification when the request is ambiguous.',
  model,
  tools: { askUserTool },
})
```

處理暫停並恢復執行：

```typescript title="src/run.ts"
const stream = await agent.stream('Summarize my project')

for await (const chunk of stream.fullStream) {
  if (chunk.type === 'tool-call-suspended') {
    const { question, options, selectionMode } = chunk.payload.suspendPayload
    // Present the question to the user, collect their answer, then resume:
    const resumed = await agent.resumeStream('The main repo', { runId: stream.runId })
    for await (const c of resumed.textStream) process.stdout.write(c)
  }
}
```

如屬多選提示，請使用字串陣列恢復執行：

```typescript
await agent.resumeStream(['Add tests', 'Update docs'], { runId: stream.runId })
```

## 輸入結構 {/* #input-schema */}

模型會使用以下參數呼叫此 Tool：

<PropertiesTable
  content={[
  {
    name: 'question',
    type: 'string',
    description: '要向用戶提出的問題。不得為空白。',
    isOptional: false,
  },
  {
    name: 'options',
    type: 'AskUserOption[]',
    description: '供用戶選擇的結構化選項。如省略，提示會採用自由文字形式。',
    isOptional: true,
    properties: [
      {
        type: 'AskUserOption',
        parameters: [
          {
            name: 'label',
            type: 'string',
            description: '此選項的簡短顯示文字。選取後，此值會傳回模型。',
          },
          {
            name: 'description',
            type: 'string',
            description: '此選項含義的說明。',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'selectionMode',
    type: "'single_select' | 'multi_select'",
    description: "控制用戶可選取的選項數目。提供選項時，預設為 'single_select'。必須提供選項。",
    isOptional: true,
  },
]}
/>

## 暫停負載 {/* #suspend-payload */}

`tool-call-suspended` 事件會包含一個與輸入結構相同的 `suspendPayload`：

<PropertiesTable
  content={[
  {
    name: 'question',
    type: 'string',
    description: '正在向用戶提出的問題。',
  },
  {
    name: 'options',
    type: 'AskUserOption[]',
    description: '結構化選項（如有）。',
    isOptional: true,
  },
  {
    name: 'selectionMode',
    type: "'single_select' | 'multi_select'",
    description: '解析後的選取模式。自由文字提示會省略此項。',
    isOptional: true,
  },
]}
/>

## 恢復資料 {/* #resume-data */}

將用戶的答案傳入 `agent.resumeStream()`：

- **自由文字及單選：** 一個 `string`。
- **多選：** 一個由已選選項標籤組成的 `string[]`。
