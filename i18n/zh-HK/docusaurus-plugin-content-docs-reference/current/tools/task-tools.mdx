---
title: "參考：Task tools | Tools"
description: "內置 Task tools（task_write、task_update、task_complete、task_check）的 API 參考，這些 Tools 可為任何 Agent 加入結構化任務追蹤。"
packages:
  - "@mastra/core"
---

# Task tools

四個內置且不依賴特定 Agent 的 Tools，用於管理某次 Agent 執行的結構化任務清單。任務清單會保存在以執行緒為範圍的 `threadState` 儲存網域中，並投射到 Agent 的 [state-signal](/docs/long-running-agents/signals) 通道，因此可在 [observational-memory](/docs/memory/observational-memory) 截斷後保留。

任務追蹤需要由記憶體支援的執行緒（`threadId` + `resourceId`）。如沒有記憶體，Tools 會傳回錯誤，說明任務追蹤需要 Agent 記憶體。

建議設定是 [`TaskSignalProvider`](/reference/signals/task-signal-provider)，它在一次註冊中包含全部四個 Tools 及 `TaskStateProcessor`。概念指南請參閱[內置 Tools](/docs/agents/using-tools#task-tracking)。

## 使用範例 {/* #usage-example */}

```typescript title="src/mastra/agents/index.ts"
import { Agent } from '@mastra/core/agent'
import { Memory } from '@mastra/memory'
import { TaskSignalProvider } from '@mastra/core/signals'

const agent = new Agent({
  id: 'coder',
  name: 'Coder',
  instructions: 'Track your progress with the task tools.',
  model,
  memory: new Memory(),
  signals: [new TaskSignalProvider()],
})
```

或者直接匯入 Tools：

```typescript title="src/mastra/agents/index.ts"
import { taskWriteTool, taskUpdateTool, taskCompleteTool, taskCheckTool } from '@mastra/core/tools'

const agent = new Agent({
  id: 'coder',
  name: 'Coder',
  instructions: 'Track your progress with the task tools.',
  model,
  memory: new Memory(),
  tools: { taskWriteTool, taskUpdateTool, taskCompleteTool, taskCheckTool },
})
```

## `task_write` {/* #task_write */}

建立或取代整份任務清單。每次呼叫都會取代上一份清單。

### 輸入結構描述 {/* #input-schema */}

<PropertiesTable
  content={[
  {
    name: 'tasks',
    type: 'TaskItemInput[]',
    description: '完整的已更新任務清單。',
    isOptional: false,
    properties: [
      {
        type: 'TaskItemInput',
        parameters: [
          {
            name: 'id',
            type: 'string',
            description:
              "穩定的任務識別符（例如 'task_investigate_tests'）。更新時請保持不變。如省略，系統會自動產生。",
            isOptional: true,
          },
          {
            name: 'content',
            type: 'string',
            description: "使用祈使語氣撰寫的任務描述（例如 'Fix authentication bug'）。",
          },
          {
            name: 'status',
            type: "'pending' | 'in_progress' | 'completed'",
            description: '目前的任務狀態。',
          },
          {
            name: 'activeForm',
            type: 'string',
            description: "執行期間顯示的現在進行式（例如 'Fixing authentication bug'）。",
          },
        ],
      },
    ],
  },
]}
/>

### 輸出 {/* #output */}

傳回 `TaskToolResult`，當中包含便於閱讀的 `content` 摘要、已獲指派 ID 的完整 `tasks` 陣列，以及 `isError` 標記。

### 行為 {/* #behavior */}

- 每次呼叫中的 ID 必須唯一。重複的明確 ID 會獲指派一個後備產生值。
- 重寫現有清單時，如省略 ID，而只有一項任務明確相符，該任務會重用先前的 ID，以保持穩定。
- 每次只能有一項任務處於 `in_progress` 狀態。提交多項 `in_progress` 任務會傳回錯誤。

## `task_update` {/* #task_update */}

透過穩定 ID 更新一項任務。只需包含已變更的欄位。

### 輸入結構描述 {/* #input-schema-1 */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '要更新的穩定任務識別符。',
    isOptional: false,
  },
  {
    name: 'content',
    type: 'string',
    description: '使用祈使語氣撰寫的新任務描述。',
    isOptional: true,
  },
  {
    name: 'status',
    type: "'pending' | 'in_progress' | 'completed'",
    description: '新的任務狀態。',
    isOptional: true,
  },
  {
    name: 'activeForm',
    type: 'string',
    description: '新的現在進行式。',
    isOptional: true,
  },
]}
/>

必須提供 `content`、`status` 或 `activeForm` 其中至少一項。

### 行為 {/* #behavior-1 */}

- 更新將任務設為 `in_progress` 時，任何其他 `in_progress` 任務都會自動降為 `pending`。
- 如果找不到該 ID，會傳回錯誤及可用的任務 ID。

## `task_complete` {/* #task_complete */}

透過穩定 ID 將一項任務標記為已完成。

### 輸入結構描述 {/* #input-schema-2 */}

<PropertiesTable
  content={[
  {
    name: 'id',
    type: 'string',
    description: '要標記為已完成的穩定任務識別符。',
    isOptional: false,
  },
]}
/>

### 行為 {/* #behavior-2 */}

如果找不到該 ID，會傳回錯誤及可用的任務 ID。

## `task_check` {/* #task_check */}

檢查任務清單的完成狀態。不接受任何輸入參數。

### 輸出 {/* #output-1 */}

傳回包含以下內容的 `TaskCheckResult`：

<PropertiesTable
  content={[
  {
    name: 'content',
    type: 'string',
    description: '便於閱讀的摘要，包含任務數目及未完成任務 ID。',
  },
  {
    name: 'tasks',
    type: 'TaskItem[]',
    description: '含穩定 ID 的完整任務清單快照。',
  },
  {
    name: 'summary',
    type: 'TaskCheckSummary',
    description: '結構化計數。',
    properties: [
      {
        type: 'TaskCheckSummary',
        parameters: [
          { name: 'total', type: 'number', description: '追蹤中的任務總數。' },
          { name: 'completed', type: 'number', description: '已完成的任務數目。' },
          { name: 'inProgress', type: 'number', description: '進行中的任務數目。' },
          { name: 'pending', type: 'number', description: '待處理的任務數目。' },
          { name: 'incomplete', type: 'number', description: '尚未完成的任務數目（進行中 + 待處理）。' },
          { name: 'hasTasks', type: 'boolean', description: '至少存在一項任務時為 True。' },
          {
            name: 'allCompleted',
            type: 'boolean',
            description: '至少存在一項任務，而且每項任務均已完成時為 True。',
          },
        ],
      },
    ],
  },
  {
    name: 'incompleteTasks',
    type: 'TaskItem[]',
    description: '仍需處理的任務（進行中及待處理）。',
  },
  {
    name: 'isError',
    type: 'boolean',
    description: '檢查期間是否遇到錯誤。',
  },
]}
/>
