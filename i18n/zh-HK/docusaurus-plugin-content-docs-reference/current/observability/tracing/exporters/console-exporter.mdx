---
title: "參考：ConsoleExporter | 可觀測性"
description: ConsoleExporter 的 API 參考
packages:
  - "@mastra/core"
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# ConsoleExporter {/* #consoleexporter */}

將 tracing 事件輸出至控制台，以供除錯及開發使用。

## 建構函式 {/* #constructor */}

```typescript prettier:false
new ConsoleExporter(config?: BaseExporterConfig)
```

<PropertiesTable
  content={[
  {
    name: 'config',
    type: 'BaseExporterConfig',
    description: '設定選項',
    isOptional: true,
    properties: [
      {
        type: 'BaseExporterConfig',
        parameters: [
          {
            name: 'logger',
            type: 'IMastraLogger',
            description: 'Logger 執行個體（如未設定，會改用 INFO 層級的 ConsoleLogger）',
            isOptional: true,
          },
          {
            name: 'logLevel',
            type: "LogLevel | 'debug' | 'info' | 'warn' | 'error'",
            description: '匯出器的日誌層級（預設：INFO）',
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

## 屬性 {/* #properties */}

```typescript prettier:false
readonly name = 'tracing-console-exporter';
```

## 方法 {/* #methods */}

### `exportTracingEvent` {/* #exporttracingevent */}

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

將 tracing 事件匯出至控制台。

<PropertiesTable
  props={[
  {
    name: 'event',
    type: 'TracingEvent',
    description: '要匯出的 tracing 事件',
    required: true,
  },
]}
/>

### shutdown {/* #shutdown */}

```typescript prettier:false
async shutdown(): Promise<void>
```

記錄關閉訊息。

## 輸出格式 {/* #output-format */}

匯出器會按事件類型輸出不同格式：

### SPAN\_STARTED {/* #span_started */}

```
🚀 SPAN_STARTED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Trace ID: [trace id]
   Input: [formatted input]
   Attributes: [formatted attributes]
────────────────────────────────────────
```

### SPAN\_ENDED {/* #span_ended */}

```
✅ SPAN_ENDED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Duration: [duration]ms
   Trace ID: [trace id]
   Input: [formatted input]
   Output: [formatted output]
   Error: [formatted error if present]
   Attributes: [formatted attributes]
────────────────────────────────────────
```

### SPAN\_UPDATED {/* #span_updated */}

```
📝 SPAN_UPDATED
   Type: [span type]
   Name: [span name]
   ID: [span id]
   Trace ID: [trace id]
   Input: [formatted input]
   Output: [formatted output]
   Error: [formatted error if present]
   Updated Attributes: [formatted attributes]
────────────────────────────────────────
```

## 用法 {/* #usage */}

```typescript
import { ConsoleExporter } from '@mastra/observability'
import { ConsoleLogger, LogLevel } from '@mastra/core/logger'

// Use default logger (INFO level)
const exporter = new ConsoleExporter()

// Use custom log level
const exporter = new ConsoleExporter({
  logLevel: 'debug',
})

// Use custom logger instance
const customLogger = new ConsoleLogger({ level: LogLevel.DEBUG })
const exporterWithLogger = new ConsoleExporter({
  logger: customLogger,
})
```

## 實作詳情 {/* #implementation-details */}

- 將屬性格式化為縮排 2 個空格的 JSON
- 計算並以毫秒顯示 span 持續時間
- 妥善處理序列化錯誤
- 將尚未實作的事件類型記錄為警告
- 在事件之間使用 80 個字元長的分隔線

## 另請參閱 {/* #see-also */}

### 文件 {/* #documentation */}

- [Tracing 概覽](/docs/observability/tracing/overview)：完整指南
- [Exporter](/docs/observability/integrations/overview#exporters)：exporter 概念

### 其他匯出器 {/* #other-exporters */}

- [MastraStorageExporter](/reference/observability/tracing/exporters/mastra-storage-exporter)：儲存持久化
- [MastraPlatformExporter](/reference/observability/tracing/exporters/mastra-platform-exporter)：Mastra 平台
- [Langfuse](/reference/observability/tracing/exporters/langfuse)：Langfuse 整合
- [Braintrust](/reference/observability/tracing/exporters/braintrust)：Braintrust 整合

### 參考 {/* #reference */}

- [設定](/reference/observability/tracing/configuration)：設定選項
- [介面](/reference/observability/tracing/interfaces)：類型定義
