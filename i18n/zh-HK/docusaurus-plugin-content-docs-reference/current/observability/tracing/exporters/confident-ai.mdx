---
title: "參考：DeepEvalExporter | 可觀測性"
description: 用於 Tracing 的 Confident AI 匯出器
packages:
  - "@mastra/deepeval"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# DeepEvalExporter

將 Tracing 資料傳送至 [Confident AI](https://www.confident-ai.com/)，以進行評估及可觀測性分析。由建基於 DeepEval SDK 的 `@mastra/deepeval` 匯出。

設定及用法請參閱 [Confident AI 匯出器](/docs/observability/integrations/exporters/confident-ai)。

## 建構函數

```typescript prettier:false
new DeepEvalExporter(config?: DeepEvalExporterConfig)
```

## `DeepEvalExporterConfig`

```typescript
interface DeepEvalExporterConfig extends BaseExporterConfig {
  apiKey?: string
  environment?: string
  name?: string
  tags?: string[]
  metadata?: Record<string, any>
  threadId?: string
  userId?: string
  testCaseId?: string
  turnId?: string
  metricCollection?: string
  traceMetricCollection?: string
  llmMetricCollection?: string
  agentMetricCollection?: string
  toolMetricCollectionMap?: Record<string, string>
  prompt?: Prompt
  debug?: boolean
  traceCaptureSink?: (trace: Trace) => void
}
```

擴充 `BaseExporterConfig`，當中包括：

- `logger?: IMastraLogger` - 記錄器實例
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - log 層級（預設：INFO）

<PropertiesTable
  content={[
  {
    name: 'apiKey',
    type: 'string',
    description:
      'Confident AI API key。若未設定，則使用 CONFIDENT_API_KEY 環境變數。找不到 key 時，匯出器會自行停用。',
    isOptional: true,
  },
  {
    name: 'environment',
    type: 'string',
    description: "Trace 環境。若未設定，會先使用 CONFIDENT_TRACE_ENVIRONMENT，然後使用 'development'。",
    isOptional: true,
  },
  {
    name: 'name',
    type: 'string',
    description: '套用至每個 Trace 的名稱。預設為 Mastra serviceName。',
    isOptional: true,
  },
  {
    name: 'tags',
    type: 'string[]',
    description: '套用至每個 Trace 的 tag。',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, any>',
    description: '套用至每個 Trace 的 metadata。',
    isOptional: true,
  },
  {
    name: 'threadId',
    type: 'string',
    description: '套用至每個 Trace 的 thread ID。root span metadata 中的 threadId 會按每個 request 覆寫此值。',
    isOptional: true,
  },
  {
    name: 'userId',
    type: 'string',
    description: '套用至每個 Trace 的 user ID。root span metadata 中的 userId 會按每個 request 覆寫此值。',
    isOptional: true,
  },
  {
    name: 'testCaseId',
    type: 'string',
    description:
      '套用至每個 Trace 的 test case ID。root span metadata 中的 testCaseId 會按每個 request 覆寫此值。',
    isOptional: true,
  },
  {
    name: 'turnId',
    type: 'string',
    description: '套用至每個 Trace 的 turn ID。root span metadata 中的 turnId 會按每個 request 覆寫此值。',
    isOptional: true,
  },
  {
    name: 'metricCollection',
    type: 'string',
    description: 'Trace 層級的 metric collection。',
    isOptional: true,
  },
  {
    name: 'traceMetricCollection',
    type: 'string',
    description: 'Trace 層級的 metric collection。優先於 metricCollection。',
    isOptional: true,
  },
  {
    name: 'llmMetricCollection',
    type: 'string',
    description: '套用至 LLM span 的 metric collection。',
    isOptional: true,
  },
  {
    name: 'agentMetricCollection',
    type: 'string',
    description: '套用至 Agent span 的 metric collection。',
    isOptional: true,
  },
  {
    name: 'toolMetricCollectionMap',
    type: 'Record<string, string>',
    description: '按每個 Tool 名稱套用的 metric collection。',
    isOptional: true,
  },
  {
    name: 'prompt',
    type: 'Prompt',
    description: '附加至 LLM span、由 Confident AI 管理的 prompt 連結。',
    isOptional: true,
  },
  {
    name: 'debug',
    type: 'boolean',
    description: '將匯出器設定及匯出錯誤記錄至 console。',
    isOptional: true,
  },
  {
    name: 'traceCaptureSink',
    type: '(trace: Trace) => void',
    description:
      '在本機接收已完成的 Trace，而非將其發佈至 Confident AI。用於離線評估。',
    isOptional: true,
  },
  {
    name: 'logLevel',
    type: "'debug' | 'info' | 'warn' | 'error'",
    description: "Logger 層級（預設：'info'）",
    isOptional: true,
  },
]}
/>

## 方法

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

將 tracing event 匯出至 Confident AI。從 Mastra span stream 建立 Trace tree，並在 root span 結束時發佈 Trace。

### `flush`

```typescript prettier:false
async flush(): Promise<void>
```

等待所有處理中的 Trace 發佈完成，而不關閉匯出器。

### `shutdown`

```typescript prettier:false
async shutdown(): Promise<void>
```

完成所有仍開啟的 Trace，並 flush 待處理的發佈。

## 用法

### 零設定（使用環境變數）

```typescript
import { DeepEvalExporter } from '@mastra/deepeval'

// Reads from CONFIDENT_API_KEY, CONFIDENT_TRACE_ENVIRONMENT
const exporter = new DeepEvalExporter()
```

### 明確設定

```typescript
import { DeepEvalExporter } from '@mastra/deepeval'

const exporter = new DeepEvalExporter({
  apiKey: process.env.CONFIDENT_API_KEY,
  environment: 'production',
})
```

## Span 類型對應

| Mastra span 類型 | Confident AI span 類型 |
| - | - |
| `AGENT_RUN`, `WORKFLOW_RUN` | `AGENT` |
| `MODEL_GENERATION` | `LLM` |
| `TOOL_CALL`, `MCP_TOOL_CALL`, `PROVIDER_TOOL_CALL`, `CLIENT_TOOL_CALL` | `TOOL` |
| `RAG_EMBEDDING`, `RAG_VECTOR_OPERATION` | `RETRIEVER` |
| 所有其他已匯出的 span 類型 | `CUSTOM` |

Event span（例如 `MODEL_CHUNK`）會被捨棄。
