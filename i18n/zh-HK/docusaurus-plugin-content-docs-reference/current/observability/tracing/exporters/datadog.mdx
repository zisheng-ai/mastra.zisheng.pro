---
title: "參考：DatadogExporter | 可觀測性"
description: 用於 Tracing 的 Datadog LLM Observability exporter
packages:
  - "@mastra/core"
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# DatadogExporter

將 Tracing 資料傳送至 Datadog 的 LLM Observability 產品，以作監察及分析。

:::info
如你亦使用 `dd-trace` APM 自動插樁，並需要將其正確設為 Mastra span 的子項，請改用 [DatadogBridge](/reference/observability/tracing/bridges/datadog)。exporter 會在執行完成後傳送 LLM Observability 資料，因此不會參與 `dd-trace` 的即時 scope。
:::

## 建構函式

```typescript prettier:false
new DatadogExporter(config: DatadogExporterConfig)
```

## `DatadogExporterConfig`

```typescript
interface DatadogExporterConfig extends BaseExporterConfig {
  apiKey?: string
  mlApp?: string
  site?: string
  service?: string
  env?: string
  agentless?: boolean
  integrationsEnabled?: boolean
}
```

擴充 `BaseExporterConfig`，當中包括：

- `logger?: IMastraLogger` - Logger 實例
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - Log 級別（預設：INFO）

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Datadog API key。agentless 模式必須提供。如未設定，會改用 DD_API_KEY 環境變數。',
    required: false,
  },
  {
    name: 'mlApp',
    type: 'string',
    description: '用於將 trace 分組的 ML 應用程式名稱。必須提供。如未設定，會改用 DD_LLMOBS_ML_APP 環境變數。',
    required: false,
  },
  {
    name: 'site',
    type: 'string',
    description:
      "Datadog 網站（例如 'datadoghq.com'、'datadoghq.eu'、'us3.datadoghq.com'）。如未設定，會改用 DD_SITE 環境變數。預設：'datadoghq.com'。",
    required: false,
  },
  {
    name: 'service',
    type: 'string',
    description: '應用程式的服務名稱。預設使用 mlApp 的值。',
    required: false,
  },
  {
    name: 'env',
    type: 'string',
    description: "環境名稱（例如 'production'、'staging'）。如未設定，會改用 DD_ENV 環境變數。",
    required: false,
  },
  {
    name: 'agentless',
    type: 'boolean',
    description: '使用直接 HTTPS 接收端點（true）或本機 Datadog Agent（false）。預設：true。',
    required: false,
  },
  {
    name: 'integrationsEnabled',
    type: 'boolean',
    description: '啟用 dd-trace 自動整合。預設：false。',
    required: false,
  },
]}
/>

## 方法

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

將 tracing 事件匯出至 Datadog LLM Observability。

### flush

```typescript prettier:false
async flush(): Promise<void>
```

在不關閉 exporter 的情況下，強制將任何已緩衝的 span 排清至 Datadog。這在 serverless 環境中十分實用，可確保在 runtime 終止前匯出 span。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

排清待處理資料並關閉 exporter。取消任何待處理的清理計時器，並停用 LLM Observability。

## 用法

### 零設定（使用環境變數）

```typescript
import { DatadogExporter } from '@mastra/datadog'

// Reads from DD_LLMOBS_ML_APP, DD_API_KEY, DD_SITE, DD_ENV
const exporter = new DatadogExporter()
```

### 明確設定

```typescript
import { DatadogExporter } from '@mastra/datadog'

const exporter = new DatadogExporter({
  mlApp: 'my-llm-app',
  apiKey: process.env.DD_API_KEY,
  site: 'datadoghq.com',
  env: 'production',
})
```

### 配合本機 Datadog Agent

```typescript
const exporter = new DatadogExporter({
  mlApp: 'my-llm-app',
  agentless: false, // Use local Datadog Agent
  env: 'production',
})
```

## Span 對應

Mastra span 類型會對應至 Datadog LLM Observability span 種類：

| Mastra SpanType | Datadog 種類 |
| - | - |
| `AGENT_RUN` | `agent` |
| `MODEL_GENERATION` | `workflow` |
| `MODEL_STEP` | `llm` |
| `TOOL_CALL` | `tool` |
| `MCP_TOOL_CALL` | `tool` |
| `PROVIDER_TOOL_CALL` | `tool` |
| `WORKFLOW_RUN` | `workflow` |
| 所有其他類型 | `task` |

所有未對應的 span 類型（包括 `MODEL_CHUNK`、`WORKFLOW_STEP`、`GENERIC` 及日後新增的 span 類型）都會自動預設為 `task`。

## 環境變數

exporter 會從以下環境變數讀取設定：

| 變數 | 說明 |
| - | - |
| `DD_API_KEY` | Datadog API key |
| `DD_LLMOBS_ML_APP` | ML 應用程式名稱 |
| `DD_SITE` | Datadog 網站 |
| `DD_ENV` | 環境名稱 |
| `DD_LLMOBS_AGENTLESS_ENABLED` | 設為 'false' 或 '0' 以使用本機 agent |
