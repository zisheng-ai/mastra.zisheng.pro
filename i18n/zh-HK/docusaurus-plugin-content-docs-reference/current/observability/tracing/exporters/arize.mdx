---
title: "參考：ArizeExporter | 可觀測性"
description: 使用 OpenInference 的 Tracing Arize 匯出器
packages:
  - "@mastra/arize"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# ArizeExporter

將 Tracing 資料傳送至 Arize Phoenix、Arize AX，或任何支援 OpenInference 語義慣例且兼容 OpenTelemetry 的可觀測性平台。

## 建構函數

```typescript prettier:false
new ArizeExporter(config: ArizeExporterConfig)
```

## `ArizeExporterConfig`

```typescript
type ArizeExporterConfig = Omit<OtelExporterConfig, 'provider'> & {
  // Phoenix / OpenTelemetry configuration
  endpoint?: string
  apiKey?: string

  // Arize AX configuration
  spaceId?: string

  // Common configuration
  projectName?: string
  headers?: Record<string, string>
}
```

繼承自 `OtelExporterConfig`（不包括 `provider`），當中包括：

- `timeout?: number` - 匯出逾時時間（毫秒）（預設：30000）
- `batchSize?: number` - 每個批次的 span 數目（預設：512）
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - log 層級（預設：WARN）
- `resourceAttributes?: Record<string, any>` - 自訂資源屬性

### Metadata 傳遞

非保留的 span 屬性會序列化至 OpenInference `metadata` payload。透過 `tracingOptions.metadata` 加入這些屬性（例如 `companyId`、`tier`）。`input`、`output`、`sessionId`、thread/user ID 及 OpenInference ID 等保留欄位會自動排除。

<PropertiesTable
  props={[
  {
    name: 'endpoint',
    type: 'string',
    description:
      '用於匯出 Trace 的 Collector endpoint。若未設定，則使用 PHOENIX_COLLECTOR_ENDPOINT 環境變數。Phoenix 必須設定此項。',
    required: false,
  },
  {
    name: 'apiKey',
    type: 'string',
    description: '用於驗證的 API key。若未設定，則使用 ARIZE_API_KEY 或 PHOENIX_API_KEY 環境變數。',
    required: false,
  },
  {
    name: 'spaceId',
    type: 'string',
    description: 'Arize AX space 識別碼。若未設定，則使用 ARIZE_SPACE_ID 環境變數。Arize AX 必須設定此項。',
    required: false,
  },
  {
    name: 'projectName',
    type: 'string',
    description: '專案名稱。若未設定，則使用 ARIZE_PROJECT_NAME 或 PHOENIX_PROJECT_NAME 環境變數。',
    required: false,
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: 'OTLP request 的額外 header',
    required: false,
  },
  {
    name: 'timeout',
    type: 'number',
    description: '匯出 span 前的逾時時間（毫秒）（預設：30000）',
    required: false,
  },
  {
    name: 'batchSize',
    type: 'number',
    description: '匯出前組成一個批次的 span 數目（預設：512）',
    required: false,
  },
  {
    name: 'logLevel',
    type: "'debug' | 'info' | 'warn' | 'error'",
    description: "Logger 層級（預設：'warn'）",
    required: false,
  },
  {
    name: 'resourceAttributes',
    type: 'Record<string, any>',
    description: '加入每個 span 的自訂資源屬性',
    required: false,
  },
]}
/>

## 方法

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

將 tracing event 匯出至已設定的 endpoint。

### export

```typescript prettier:false
async export(spans: ReadOnlySpan[]): Promise<void>
```

使用採用 OpenInference 語義慣例的 OpenTelemetry，批次匯出 span。

### flush

```typescript prettier:false
async flush(): Promise<void>
```

在不關閉匯出器的情況下，強制將所有已緩衝的 span flush 至已設定的 endpoint。這對 serverless 環境尤其有用，可確保在 runtime 終止前完成匯出 span。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

Flush 待處理的資料並關閉 client。

## 用法

### 零設定（使用環境變數）

```typescript
import { ArizeExporter } from '@mastra/arize'

// For Phoenix: Set PHOENIX_COLLECTOR_ENDPOINT, PHOENIX_API_KEY, PHOENIX_PROJECT_NAME
// For Arize AX: Set ARIZE_SPACE_ID, ARIZE_API_KEY, ARIZE_PROJECT_NAME
const exporter = new ArizeExporter()
```

### Phoenix 設定

```typescript
import { ArizeExporter } from '@mastra/arize'

const exporter = new ArizeExporter({
  endpoint: 'http://localhost:6006/v1/traces',
  apiKey: process.env.PHOENIX_API_KEY, // Optional for local Phoenix
  projectName: 'my-ai-project',
})
```

### Arize AX 設定

```typescript
import { ArizeExporter } from '@mastra/arize'

const exporter = new ArizeExporter({
  spaceId: process.env.ARIZE_SPACE_ID!,
  apiKey: process.env.ARIZE_API_KEY!,
  projectName: 'my-ai-project',
})
```

## `OpenInference` 語義慣例

ArizeExporter 為生成式 AI 應用程式實作 [OpenInference 語義慣例](https://github.com/Arize-ai/openinference/tree/main/spec)，在不同可觀測性平台之間提供標準化的 Trace 結構。

## Tag 支援

ArizeExporter 支援以 Trace tag 進行分類及篩選。Tag 只會套用至 root span，並對應至原生 OpenInference `tag.tags` 語義慣例。

### 用法

```typescript
const result = await agent.generate('Hello', {
  tracingOptions: {
    tags: ['production', 'experiment-v2', 'user-request'],
  },
})
```

### Tag 的儲存方式

Tag 使用 OpenInference `tag.tags` 屬性儲存：

```json
{
  "tag.tags": ["production", "experiment-v2", "user-request"]
}
```

## 相關內容

- [ArizeExporter 文件](/docs/observability/integrations/exporters/arize)
- [Phoenix 文件](https://docs.arize.com/phoenix)
- [Arize AX 文件](https://docs.arize.com/)
