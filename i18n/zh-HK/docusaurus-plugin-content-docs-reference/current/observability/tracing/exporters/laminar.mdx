---
title: "參考：LaminarExporter | 可觀測性"
description: 用於 Tracing 的 Laminar exporter
packages:
  - "@mastra/laminar"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# LaminarExporter

透過 OTLP/HTTP（protobuf）將 Tracing 資料傳送至 Laminar。

## 建構函式

```typescript prettier:false
new LaminarExporter(config?: LaminarExporterConfig)
```

## `LaminarExporterConfig`

```typescript
interface LaminarExporterConfig extends BaseExporterConfig {
  apiKey?: string
  baseUrl?: string
  endpoint?: string
  headers?: Record<string, string>
  realtime?: boolean
  disableBatch?: boolean
  batchSize?: number
  timeoutMillis?: number
}
```

擴充 `BaseExporterConfig`，當中包括：

- `logger?: IMastraLogger` - Logger 實例
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - Log 級別（預設：INFO）

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Laminar 項目 API 金鑰。未設定時會使用 LMNR_PROJECT_API_KEY 環境變數。',
    required: false,
  },
  {
    name: 'baseUrl',
    type: 'string',
    description:
      'Laminar 基礎 URL（用於匯出 trace 及評分）。未設定時會使用 LMNR_BASE_URL 環境變數（預設：https://api.lmnr.ai）。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description:
      'OTLP/HTTP trace endpoint（以 /v1/traces 結尾）。未設定時會使用 LAMINAR_ENDPOINT 環境變數（預設：`${baseUrl}/v1/traces`）。',
    required: false,
  },
  {
    name: 'headers',
    type: 'Record<string, string>',
    description: '加入 OTLP 請求的額外 header（與 Authorization 合併）。',
    required: false,
  },
  {
    name: 'realtime',
    type: 'boolean',
    description: '在每個 span 後清送，以便立即查看（預設：false）。',
    required: false,
  },
  {
    name: 'disableBatch',
    type: 'boolean',
    description: '停用批次處理（SimpleSpanProcessor）。預設：false。',
    required: false,
  },
  {
    name: 'batchSize',
    type: 'number',
    description: '每個批次最多可包含的 span 數目（BatchSpanProcessor）。預設：512。',
    required: false,
  },
  {
    name: 'timeoutMillis',
    type: 'number',
    description: 'OTLP 匯出逾時，以毫秒為單位。預設：30000。',
    required: false,
  },
]}
/>

## 方法

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

將 tracing event 匯出至 Laminar。

### flush

```typescript prettier:false
async flush(): Promise<void>
```

在不關閉 exporter 的情況下，強制將所有已緩衝的 span 清送至 Laminar。這對無伺服器環境十分有用，可確保 span 在執行環境終止前完成匯出。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

清送待處理資料並關閉 exporter。

## 用法

### 零設定（使用環境變數）

```typescript
import { LaminarExporter } from '@mastra/laminar'

// Reads from LMNR_PROJECT_API_KEY, LMNR_BASE_URL, LAMINAR_ENDPOINT
const exporter = new LaminarExporter()
```

### 明確設定

```typescript
import { LaminarExporter } from '@mastra/laminar'

const exporter = new LaminarExporter({
  apiKey: process.env.LMNR_PROJECT_API_KEY,
  baseUrl: 'https://api.lmnr.ai',
  realtime: true,
})
```
