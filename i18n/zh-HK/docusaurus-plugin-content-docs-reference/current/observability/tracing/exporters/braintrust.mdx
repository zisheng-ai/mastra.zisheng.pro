---
title: "參考：BraintrustExporter | 可觀測性"
description: Braintrust 的 Tracing 匯出器
packages:
  - "@mastra/braintrust"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# BraintrustExporter

將 Tracing 資料傳送至 Braintrust，以供評估及可觀測性使用。

## 建構函數

```typescript prettier:false
new BraintrustExporter(config: BraintrustExporterConfig)
```

## `BraintrustExporterConfig`

```typescript
interface BraintrustExporterConfig extends BaseExporterConfig {
  apiKey?: string
  endpoint?: string
  projectName?: string
  braintrustLogger?: Logger<true>
  currentSpan?: () => Span | undefined
  tuningParameters?: Record<string, any>
}
```

擴充 `BaseExporterConfig`，當中包括：

- `logger?: IMastraLogger` - Logger 實例
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - 日誌層級（預設：INFO）

<PropertiesTable
  props={[
  {
    name: 'apiKey',
    type: 'string',
    description: 'Braintrust API 金鑰。未有設定時使用 BRAINTRUST_API_KEY 環境變數。',
    required: false,
  },
  {
    name: 'endpoint',
    type: 'string',
    description: '自訂 Braintrust 端點。未有設定時使用 BRAINTRUST_ENDPOINT 環境變數。',
    required: false,
  },
  {
    name: 'projectName',
    type: 'string',
    description: "專案名稱（預設：'mastra-tracing'）",
    required: false,
  },
  {
    name: 'braintrustLogger',
    type: 'Logger<true>',
    description: '用於整合評估及已追蹤 span 情境的 Braintrust logger 實例。',
    required: false,
  },
  {
    name: 'currentSpan',
    type: '() => Span | undefined',
    description:
      '來自應用程式 Braintrust 套件實例的解析器。當 Eval() 或 logger.traced() span 是由另一個已安裝的 Braintrust SDK 副本建立時，請與 braintrustLogger 一併使用。',
    required: false,
  },
  {
    name: 'logLevel',
    type: "'debug' | 'info' | 'warn' | 'error'",
    description: "Logger 層級（預設：'warn'）",
    required: false,
  },
  {
    name: 'tuningParameters',
    type: 'Record<string, any>',
    description: 'Braintrust 的調整參數',
    required: false,
  },
]}
/>

## 方法

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

將 Tracing 事件匯出至 Braintrust。

### export

```typescript prettier:false
async export(spans: ReadOnlySpan[]): Promise<void>
```

將 span 批次匯出至 Braintrust。

### flush

```typescript prettier:false
async flush(): Promise<void>
```

強制將緩衝區內的所有 span 匯出至 Braintrust，而不關閉匯出器。這適用於無伺服器環境，讓你可確保 span 在執行階段終止前已匯出。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

清空待處理資料並關閉用戶端。

## 用法

### 零設定（使用環境變數）

```typescript
import { BraintrustExporter } from '@mastra/braintrust'

// Reads from BRAINTRUST_API_KEY, BRAINTRUST_ENDPOINT
const exporter = new BraintrustExporter()
```

### 明確設定

```typescript
import { BraintrustExporter } from '@mastra/braintrust'

const exporter = new BraintrustExporter({
  apiKey: process.env.BRAINTRUST_API_KEY,
  projectName: 'my-ai-project',
})
```

## Span 類型對應

| Span 類型 | Braintrust 類型 |
| - | - |
| `MODEL_GENERATION` | `llm` |
| `MODEL_CHUNK` | `llm` |
| `TOOL_CALL` | `tool` |
| `MCP_TOOL_CALL` | `tool` |
| `PROVIDER_TOOL_CALL` | `tool` |
| `WORKFLOW_CONDITIONAL_EVAL` | `function` |
| `WORKFLOW_WAIT_EVENT` | `function` |
| 其他所有類型 | `task` |
