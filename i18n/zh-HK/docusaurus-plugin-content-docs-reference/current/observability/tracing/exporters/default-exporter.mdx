---
title: "參考：DefaultExporter | 可觀測性"
description: 已棄用的 DefaultExporter API 參考（由 MastraStorageExporter 取代）
packages:
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# DefaultExporter

**已於 `@mastra/observability@1.12.0` 棄用**，請改用 [`MastraStorageExporter`](/reference/observability/tracing/exporters/mastra-storage-exporter)。

:::warning[已棄用]

`DefaultExporter` 只為向後兼容而保留，並會在未來的主要版本中移除。新專案請使用 [`MastraStorageExporter`](/reference/observability/tracing/exporters/mastra-storage-exporter)。兩個類別的建構函式、設定及執行階段行為相同。`DefaultExporter` 會保留原有的 `mastra-default-observability-exporter` exporter `name`，讓以此建立的監察規則繼續運作。

:::

透過自動批次處理及重試邏輯，將可觀測性事件保存至 Mastra Storage。

## 建構函式

```typescript prettier:false
new DefaultExporter(config?: DefaultExporterConfig)
```

<PropertiesTable
  props={[
  {
    name: 'config',
    type: 'DefaultExporterConfig',
    description: '批次處理及設定選項',
    required: false,
  },
]}
/>

## `DefaultExporterConfig`

```typescript
interface DefaultExporterConfig extends BaseExporterConfig {
  /** Maximum number of spans per batch. Default: 1000 */
  maxBatchSize?: number

  /** Maximum total buffer size before emergency flush. Default: 10000 */
  maxBufferSize?: number

  /** Maximum time to wait before flushing batch in milliseconds. Default: 5000 */
  maxBatchWaitMs?: number

  /** Maximum number of retry attempts. Default: 4 */
  maxRetries?: number

  /** Base retry delay in milliseconds (uses exponential backoff). Default: 500 */
  retryDelayMs?: number

  /** Tracing storage strategy or 'auto' for automatic selection. Default: 'auto' */
  strategy?: TracingStorageStrategy | 'auto'
}
```

延伸自 `BaseExporterConfig`，當中包括：

- `logger?: IMastraLogger` - Logger 實例
- `logLevel?: LogLevel | 'debug' | 'info' | 'warn' | 'error'` - 日誌級別（預設：INFO）

## `TracingStorageStrategy`

```typescript
type TracingStorageStrategy = 'realtime' | 'batch-with-updates' | 'insert-only'
```

### 策略行為

- **realtime**：立即將每個事件保存至儲存空間
- **batch-with-updates**：分開批次處理建立及更新操作，並按次序套用
- **insert-only**：只處理 SPAN\_ENDED 事件，忽略更新

## 屬性

```typescript prettier:false
readonly name = 'mastra-default-observability-exporter';
```

## 方法

### init

```typescript prettier:false
init(options: InitExporterOptions): void
```

在依賴項準備就緒後初始化 exporter，並根據儲存空間的功能決定 Tracing 策略。

<PropertiesTable
  props={[
  {
    name: 'options',
    type: 'InitExporterOptions',
    description: '包含 Mastra 實例及設定的初始化選項',
    required: true,
  },
]}
/>

### `exportTracingEvent`

```typescript prettier:false
async exportTracingEvent(event: TracingEvent): Promise<void>
```

按照已決定的策略處理 Tracing 事件。

<PropertiesTable
  props={[
  {
    name: 'event',
    type: 'TracingEvent',
    description: '要匯出的 Tracing 事件',
    required: true,
  },
]}
/>

### flush

```typescript prettier:false
async flush(): Promise<void>
```

強制將所有已緩衝的事件寫入儲存空間，而不會關閉 exporter。這適用於無伺服器環境，讓你可確保 span 在執行階段終止前完成匯出。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

寫入剩餘的已緩衝事件並執行清理。

## 自動選擇策略

當 `strategy: 'auto'`（預設）時，exporter 會查詢儲存 adapter 支援的功能：

```typescript
interface TracingStrategy {
  /** Strategies supported by this adapter */
  supported: TracingStorageStrategy[]

  /** Preferred strategy for optimal performance */
  preferred: TracingStorageStrategy
}
```

exporter 將會：

1. 如儲存 adapter 的首選策略可用，便採用該策略
1. 如首選策略不可用，便改用第一個支援的策略
1. 如用戶指定的策略不受支援，便記錄警告

## 批次處理行為

### 寫入觸發條件

符合以下任何條件時，緩衝區便會寫入：

- 緩衝區大小達到 `maxBatchSize`
- 自第一個事件加入緩衝區起計的時間超過 `maxBatchWaitMs`
- 緩衝區大小達到 `maxBufferSize`（緊急寫入）
- 呼叫 `shutdown()`

### 重試邏輯

寫入失敗時，系統會使用指數退避重試：

- 重試延遲：`retryDelayMs * 2^attempt`
- 嘗試次數上限：`maxRetries`
- 所有重試失敗後，該批次會被捨棄

當儲存空間不支援某個訊號，或重試次數已用盡時，`DefaultExporter` 會透過在 exporter 及 bridge 上註冊的 `onDroppedEvent` handler 發出 `ObservabilityDropEvent`。捨棄事件包括訊號、原因、數量、exporter 名稱、已知的儲存空間名稱，以及經過清理的錯誤詳情。

### 處理次序錯亂

使用 `batch-with-updates` 策略時：

- 追蹤已建立的 span
- 拒絕尚未建立的 span 的更新／結束事件
- 為次序錯亂的事件記錄警告
- 維護序列編號，確保按次序更新

## 用法

```typescript
import { DefaultExporter } from '@mastra/observability'

// Default configuration
const exporter = new DefaultExporter()

// Custom batching configuration
const customExporter = new DefaultExporter({
  maxBatchSize: 500,
  maxBatchWaitMs: 2000,
  strategy: 'batch-with-updates',
  logLevel: 'debug',
})
```

## 另請參閱

### 說明文件

- [Tracing 概覽](/docs/observability/tracing/overview)：完整指南
- [Exporters](/docs/observability/integrations/overview#exporters)：exporter 概念

### 其他 Exporter

- [CloudExporter](/reference/observability/tracing/exporters/cloud-exporter)：Mastra 平台
- [ConsoleExporter](/reference/observability/tracing/exporters/console-exporter)：除錯輸出
- [Langfuse](/reference/observability/tracing/exporters/langfuse)：Langfuse 整合
- [Braintrust](/reference/observability/tracing/exporters/braintrust)：Braintrust 整合

### 參考

- [設定](/reference/observability/tracing/configuration)：設定選項
- [介面](/reference/observability/tracing/interfaces)：類型定義
