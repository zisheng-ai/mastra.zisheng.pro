---
title: "參考：設定 | 可觀測性"
description: Tracing 設定類型及註冊表函數
packages:
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# 設定

## `ObservabilityRegistryConfig`

```typescript
interface ObservabilityRegistryConfig {
  default?: { enabled?: boolean }
  configs?: Record<string, Omit<ObservabilityInstanceConfig, 'name'> | ObservabilityInstance>
  configSelector?: ConfigSelector
  sensitiveDataFilter?: boolean | SensitiveDataFilterOptions
}
```

<PropertiesTable
  content={[
  {
    name: 'default',
    type: '{ enabled?: boolean }',
    description: '啟用包含 MastraStorageExporter 及 MastraPlatformExporter 的預設設定',
    required: false,
  },
  {
    name: 'configs',
    type: "Record<string, Omit<ObservabilityInstanceConfig, 'name'> | ObservabilityInstance>",
    description: '具名的可觀測性實例設定或已預先實例化的實例',
    required: false,
  },
  {
    name: 'configSelector',
    type: 'ConfigSelector',
    description: '執行階段設定選擇器函數',
    required: false,
  },
  {
    name: 'sensitiveDataFilter',
    type: 'boolean | SensitiveDataFilterOptions',
    description:
      '控制是否為每個已設定的實例自動套用 [SensitiveDataFilter](/reference/observability/tracing/processors/sensitive-data-filter)。預設為 `true`。傳入 `false` 可選擇停用，或傳入 `SensitiveDataFilterOptions` 物件以自訂欄位、遮蓋標記或遮蓋樣式。如果設定的 `spanOutputProcessors` 已包含 `SensitiveDataFilter`，則不會重複加入。已預先實例化的實例不會被修改。',
    required: false,
  },
]}
/>

## `ObservabilityInstanceConfig`

```typescript
interface ObservabilityInstanceConfig {
  name: string
  serviceName: string
  sampling?: SamplingStrategy
  exporters?: ObservabilityExporter[]
  spanOutputProcessors?: SpanOutputProcessor[]
  includeInternalSpans?: boolean
  excludeSpanTypes?: SpanType[]
  spanFilter?: (span: AnyExportedSpan) => boolean
  requestContextKeys?: string[]
  serializationOptions?: SerializationOptions
  logging?: { enabled?: boolean; level?: LogLevel }
}
```

<PropertiesTable
  content={[
  {
    name: 'name',
    type: 'string',
    description: '設定識別碼',
    required: true,
  },
  {
    name: 'serviceName',
    type: 'string',
    description: 'Trace 中的服務名稱',
    required: true,
  },
  {
    name: 'sampling',
    type: 'SamplingStrategy',
    description: '取樣設定（預設為 ALWAYS）',
    required: false,
  },
  {
    name: 'exporters',
    type: 'ObservabilityExporter[]',
    description: 'Trace 資料 exporter',
    required: false,
  },
  {
    name: 'spanOutputProcessors',
    type: 'SpanOutputProcessor[]',
    description: 'span 輸出處理器',
    required: false,
  },
  {
    name: 'includeInternalSpans',
    type: 'boolean',
    description: '包括 Mastra 操作內部的 span',
    required: false,
  },
  {
    name: 'excludeSpanTypes',
    type: 'SpanType[]',
    description:
      '不匯出的 span 類型。適合用於減少雜訊，以及降低按 span 收費平台的成本。詳情請參閱 [Span 篩選參考](/reference/observability/tracing/span-filtering)。',
    required: false,
  },
  {
    name: 'spanFilter',
    type: '(span: AnyExportedSpan) => boolean',
    description:
      '控制要匯出哪些 span 的篩選函數。傳回 true 以保留，傳回 false 以捨棄。此函數在 excludeSpanTypes 及 spanOutputProcessors 之後執行。詳情請參閱 [Span 篩選參考](/reference/observability/tracing/span-filtering)。',
    required: false,
  },
  {
    name: 'requestContextKeys',
    type: 'string[]',
    description: '要擷取為元數據的 RequestContext 鍵（支援點號標記法）',
    required: false,
  },
  {
    name: 'serializationOptions',
    type: 'SerializationOptions',
    description: '控制 span 資料（輸入／輸出／屬性）序列化的選項',
    required: false,
    properties: [
      {
        type: 'SerializationOptions',
        parameters: [
          {
            name: 'maxStringLength',
            type: 'number',
            description: '字串值的長度上限（預設：1024）',
            isOptional: true,
          },
          {
            name: 'maxDepth',
            type: 'number',
            description: '巢狀物件的深度上限（預設：6）',
            isOptional: true,
          },
          {
            name: 'maxArrayLength',
            type: 'number',
            description: '陣列的項目數目上限（預設：50）',
            isOptional: true,
          },
          {
            name: 'maxObjectKeys',
            type: 'number',
            description: '物件的鍵數目上限（預設：50）',
            isOptional: true,
          },
        ],
      },
    ],
  },
  {
    name: 'logging',
    type: '{ enabled?: boolean; level?: LogLevel }',
    description:
      '控制將日誌轉送至可觀測性儲存空間。詳情請參閱[將日誌記錄至可觀測性儲存空間](/docs/observability/logging#logging-to-observability-storage)。',
    required: false,
    properties: [
      {
        type: '{ enabled?: boolean; level?: LogLevel }',
        parameters: [
          {
            name: 'enabled',
            type: 'boolean',
            description: '設為 false 可停用將日誌轉送至可觀測性儲存空間（預設：true）',
            isOptional: true,
          },
          {
            name: 'level',
            type: 'LogLevel',
            description:
              "要轉送的最低日誌層級。可以是 'debug'、'info'、'warn'、'error'、'fatal' 其中之一（預設：'debug'）",
            isOptional: true,
          },
        ],
      },
    ],
  },
]}
/>

## `SamplingStrategy`

```typescript
type SamplingStrategy =
  | { type: 'always' }
  | { type: 'never' }
  | { type: 'ratio'; probability: number }
  | { type: 'custom'; sampler: (options?: TracingOptions) => boolean }
```

## `ConfigSelector`

```typescript
type ConfigSelector = (
  options: ConfigSelectorOptions,
  availableConfigs: ReadonlyMap<string, ObservabilityInstance>,
) => string | undefined
```

## `ConfigSelectorOptions`

```typescript
interface ConfigSelectorOptions {
  requestContext?: RequestContext
}
```

## 註冊表方法

Observability 類別提供以下方法來管理可觀測性實例：

### `registerInstance`

```typescript prettier:false
registerInstance(
  name: string,
  instance: ObservabilityInstance,
  isDefault?: boolean
): void;
```

在註冊表中註冊可觀測性實例。

### `getInstance`

```typescript prettier:false
getInstance(name: string): ObservabilityInstance | undefined;
```

按名稱擷取可觀測性實例。

### `getDefaultInstance`

```typescript prettier:false
getDefaultInstance(): ObservabilityInstance | undefined;
```

傳回預設的可觀測性實例。

### `getSelectedInstance`

```typescript prettier:false
getSelectedInstance(
  options: ConfigSelectorOptions
): ObservabilityInstance | undefined;
```

傳回由設定選擇器選取的可觀測性實例，或預設實例。

### `listInstances`

```typescript prettier:false
listInstances(): ReadonlyMap<string, ObservabilityInstance>;
```

傳回所有已註冊的可觀測性實例。

### `hasInstance`

```typescript prettier:false
hasInstance(name: string): boolean;
```

檢查可觀測性實例是否存在。

### `setConfigSelector`

```typescript prettier:false
setConfigSelector(selector: ConfigSelector): void;
```

設定設定選擇器函數。

### `unregisterInstance`

```typescript prettier:false
unregisterInstance(name: string): boolean;
```

從註冊表移除可觀測性實例。

### clear

```typescript prettier:false
clear(): void;
```

清除所有實例而不關閉它們。

### shutdown

```typescript prettier:false
async shutdown(): Promise<void>;
```

關閉所有可觀測性實例並清除註冊表。

## 另請參閱

### 文件

- [Tracing 概覽](/docs/observability/tracing/overview)：概念及使用指南
- [取樣策略](/docs/observability/tracing/overview#sampling-strategies)：取樣設定詳情
- [多重設定](/docs/observability/overview#multi-config-setup)：使用多個設定

### 參考

- [Tracing 類別](/reference/observability/tracing/instances)：核心 tracing 類別
- [介面](/reference/observability/tracing/interfaces)：類型定義
- [Span 參考](/reference/observability/tracing/spans)：span 生命週期

### Exporter

- [MastraStorageExporter](/reference/observability/tracing/exporters/mastra-storage-exporter)：將可觀測性事件保留至 Mastra Storage
- [MastraPlatformExporter](/reference/observability/tracing/exporters/mastra-platform-exporter)：將可觀測性事件傳送至 Mastra 平台
- [Braintrust](/reference/observability/tracing/exporters/braintrust)：Braintrust 整合
- [Langfuse](/reference/observability/tracing/exporters/langfuse)：Langfuse 整合
- [LangSmith](/reference/observability/tracing/exporters/langsmith)：LangSmith 整合
- [Span 篩選](/reference/observability/tracing/span-filtering)：選擇性排除 span
