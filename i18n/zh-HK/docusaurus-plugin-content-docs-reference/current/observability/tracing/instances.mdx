---
title: "參考：Tracing | 可觀測性"
description: 核心 Tracing 類別及方法
packages:
  - "@mastra/core"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# 可觀測性實例

## `DefaultObservabilityInstance`

ObservabilityInstance 介面的預設實作。

### 建構函式

```typescript prettier:false
new DefaultObservabilityInstance(config: ObservabilityInstanceConfig)
```

使用指定的設定建立新的 DefaultObservabilityInstance。

### 屬性

繼承 BaseObservabilityInstance 的所有屬性及方法。

## `BaseObservabilityInstance`

自訂 ObservabilityInstance 實作的基礎類別。

### 方法

#### `getConfig`

```typescript prettier:false
getConfig(): Readonly<Required<ObservabilityInstanceConfig>>
```

傳回目前的可觀測性設定。

#### `getExporters`

```typescript prettier:false
getExporters(): readonly ObservabilityExporter[]
```

傳回所有已設定的 exporter。

#### `getSpanOutputProcessors`

```typescript prettier:false
getSpanOutputProcessors(): readonly SpanOutputProcessor[]
```

傳回所有已設定的 span 輸出處理器。

#### `getLogger`

```typescript prettier:false
getLogger(): IMastraLogger
```

傳回供 exporter 及其他元件使用的 logger 實例。

#### `startSpan`

```typescript prettier:false
startSpan<TType extends SpanType>(
  options: StartSpanOptions<TType>
): Span<TType>
```

啟動指定 SpanType 的新 span。如未提供父項，便會建立 Trace 的根 span。

<PropertiesTable
  props={[
  {
    name: 'type',
    type: 'SpanType',
    description: '要建立的 span 類型',
    required: true,
  },
  {
    name: 'name',
    type: 'string',
    description: 'span 的名稱',
    required: true,
  },
  {
    name: 'parent',
    type: 'AnySpan',
    description: '父 span（如非根項）',
    required: false,
  },
  {
    name: 'attributes',
    type: 'SpanTypeMap[TType]',
    description: '類型專屬屬性',
    required: false,
  },
  {
    name: 'metadata',
    type: 'Record<string, any>',
    description: '使用者定義的 metadata',
    required: false,
  },
  {
    name: 'input',
    type: 'any',
    description: '初始輸入資料',
    required: false,
  },
  {
    name: 'customSamplerOptions',
    type: 'CustomSamplerOptions',
    description: '自訂 sampler 的選項',
    required: false,
  },
]}
/>

#### shutdown

```typescript prettier:false
async shutdown(): Promise<void>
```

關閉所有 exporter 及處理器，並清理資源。

## 自訂實作

如要建立自訂 ObservabilityInstance 實作，請擴充 BaseObservabilityInstance：

```typescript
class CustomObservabilityInstance extends BaseObservabilityInstance {
  constructor(config: ObservabilityInstanceConfig) {
    super(config)
    // Custom initialization
  }

  // Override methods as needed
  startSpan<TType extends SpanType>(options: StartSpanOptions<TType>): Span<TType> {
    // Custom span creation logic
    return super.startSpan(options)
  }
}
```

## 另請參閱

### 文件

- [Tracing 概覽](/docs/observability/tracing/overview)：概念及使用指南
- [設定參考](/reference/observability/tracing/configuration)：設定選項
- [介面參考](/reference/observability/tracing/interfaces)：類型定義
- [Span 參考](/reference/observability/tracing/spans)：span 生命週期及方法

### Exporter

- [MastraStorageExporter](/reference/observability/tracing/exporters/mastra-storage-exporter)：儲存持久化
- [MastraPlatformExporter](/reference/observability/tracing/exporters/mastra-platform-exporter)：Mastra 平台整合
- [ConsoleExporter](/reference/observability/tracing/exporters/console-exporter)：偵錯輸出
