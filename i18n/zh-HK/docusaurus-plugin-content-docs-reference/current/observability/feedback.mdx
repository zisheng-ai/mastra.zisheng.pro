---
title: "參考：Feedback | 可觀測性"
description: "可觀測性 Feedback 記錄、篩選條件及查詢方法的 API 參考。"
packages:
  - "@mastra/core"
  - "@mastra/observability"
---

import PropertiesTable from "@site/src/components/PropertiesTable";

# Feedback

**新增於：** `@mastra/core@1.18.0`

Feedback API 可儲存及查詢人工介入流程中的可觀測性訊號，例如評分、讚好／不讚好、留言及修正。使用方式請參閱 [Feedback 指南](/docs/observability/feedback)。

## 使用範例

以下範例透過可觀測性進入點，為已持久保存的 Trace 記錄評分。進入點上的 `addFeedback()` 是選用方法，因此呼叫前請檢查目前使用的可觀測性實作是否支援此方法。

```typescript
if (!mastra.observability.addFeedback) {
  throw new Error('Feedback is not supported by the active observability implementation')
}

await mastra.observability.addFeedback({
  traceId: 'trace-123',
  spanId: 'span-456',
  feedback: {
    feedbackSource: 'user',
    feedbackType: 'rating',
    value: 1,
    comment: 'Helpful answer.',
  },
})
```

## 建立 Feedback

### `addFeedback(args)`

透過可觀測性進入點，將 Feedback 加到已持久保存的 Trace 或 span。

```typescript
await mastra.observability.addFeedback?.({
  traceId: 'trace-123',
  spanId: 'span-456',
  feedback: {
    feedbackSource: 'user',
    feedbackType: 'rating',
    value: 1,
    comment: 'Helpful answer.',
  },
})
```

<PropertiesTable
  content={[
  { name: 'traceId', type: 'string', description: '作為 Feedback 目標定位基準的 Trace。', isOptional: true },
  { name: 'spanId', type: 'string', description: '作為 Feedback 目標定位基準的 span。', isOptional: true },
  {
    name: 'correlationContext',
    type: 'CorrelationContext',
    description: '用於直接發出 Feedback 的即時 span 或 Trace context，毋須從儲存空間重新載入目標。',
    isOptional: true,
  },
  { name: 'feedback', type: 'FeedbackInput', description: '要加入的 Feedback payload。' },
]}
/>

### `createFeedback(args)`

透過可觀測性儲存 domain 建立一筆 Feedback 記錄。儲存層級的呼叫會直接寫入儲存空間，因此須包含 `timestamp`。

```typescript
await observability.createFeedback({
  feedback: {
    feedbackId: 'feedback-1',
    timestamp: new Date(),
    traceId: 'trace-123',
    spanId: 'span-456',
    feedbackSource: 'user',
    feedbackType: 'rating',
    value: 1,
    comment: 'Helpful answer.',
  },
})
```

HTTP 及用戶端 SDK 的建立路由接受 `CreateFeedbackBody`，並由伺服器端設定 `timestamp`。如省略 `feedbackId`，系統會自動產生：

```typescript
await mastraClient.createFeedback({
  feedback: {
    traceId: 'trace-123',
    spanId: 'span-456',
    feedbackSource: 'user',
    feedbackType: 'rating',
    value: 1,
  },
})
```

### `batchCreateFeedback(args)`

透過可觀測性儲存 domain 建立多筆 Feedback 記錄。HTTP 路由及 `@mastra/client-js` 並未公開此方法。

```typescript
await observability.batchCreateFeedback({
  feedbacks: [
    {
      feedbackId: 'feedback-1',
      timestamp: new Date(),
      traceId: 'trace-123',
      feedbackSource: 'user',
      feedbackType: 'rating',
      value: 1,
    },
    {
      feedbackId: 'feedback-2',
      timestamp: new Date(),
      traceId: 'trace-123',
      feedbackSource: 'qa',
      feedbackType: 'comment',
      value: 'Needs a citation before shipping.',
    },
  ],
})
```

## 列出 Feedback

### `listFeedback(args?)`

以分頁模式或增量模式傳回 Feedback 記錄。

```typescript
const response = await mastraClient.listFeedback({
  filters: {
    feedbackType: 'rating',
    feedbackSource: 'studio',
  },
  pagination: { page: 0, perPage: 20 },
  orderBy: { field: 'timestamp', direction: 'DESC' },
})
```

<PropertiesTable
  content={[
  { name: 'mode', type: "'page' | 'delta'", description: "列表模式。預設為 'page'。", isOptional: true },
  { name: 'filters', type: 'FeedbackFilter', description: 'Feedback 記錄的篩選條件。', isOptional: true },
  {
    name: 'pagination',
    type: '{ page?: number; perPage?: number }',
    description: '分頁模式的分頁設定。`page` 由零開始計算。',
    isOptional: true,
  },
  {
    name: 'orderBy',
    type: "{ field?: 'timestamp'; direction?: 'ASC' | 'DESC' }",
    description: '分頁模式的排序設定。',
    isOptional: true,
  },
  {
    name: 'after',
    type: 'string',
    description: '增量輪詢所用的增量游標。只適用於增量模式。',
    isOptional: true,
  },
  {
    name: 'limit',
    type: 'number',
    description: '在增量模式中傳回的更新數目上限。',
    isOptional: true,
  },
]}
/>

## OLAP 查詢

OLAP Feedback 查詢會處理數值型別的 `value` 欄位。

### `getFeedbackAggregate(args)`

傳回一個彙總 Feedback 值。

```typescript
const response = await mastraClient.getFeedbackAggregate({
  feedbackType: 'rating',
  feedbackSource: 'user',
  aggregation: 'avg',
  comparePeriod: 'previous_day',
})
```

<PropertiesTable
  content={[
  { name: 'feedbackType', type: 'string', description: '要彙總的 Feedback 類型。' },
  { name: 'feedbackSource', type: 'string', description: '要彙總的 Feedback 來源。', isOptional: true },
  {
    name: 'aggregation',
    type: "'sum' | 'avg' | 'min' | 'max' | 'count' | 'count_distinct' | 'last'",
    description: '要套用的彙總方式。',
  },
  { name: 'filters', type: 'FeedbackFilter', description: '其他篩選條件。', isOptional: true },
  {
    name: 'comparePeriod',
    type: "'previous_period' | 'previous_day' | 'previous_week'",
    description: '選用的逐期比較。',
    isOptional: true,
  },
]}
/>

### `getFeedbackBreakdown(args)`

傳回按維度分組的 Feedback 值。

```typescript
const response = await mastraClient.getFeedbackBreakdown({
  feedbackType: 'rating',
  groupBy: ['entityName'],
  aggregation: 'avg',
})
```

### `getFeedbackTimeSeries(args)`

傳回按時間間距分桶的 Feedback 值。

```typescript
const response = await mastraClient.getFeedbackTimeSeries({
  feedbackType: 'rating',
  interval: '1h',
  aggregation: 'avg',
  groupBy: ['feedbackSource'],
})
```

### `getFeedbackPercentiles(args)`

傳回按時間間距分桶的百分位數值。

```typescript
const response = await mastraClient.getFeedbackPercentiles({
  feedbackType: 'rating',
  percentiles: [0.5, 0.95],
  interval: '1d',
})
```

## 類型

### `FeedbackRecord`

<PropertiesTable
  content={[
  {
    name: 'feedbackId',
    type: 'string | null',
    description: '此 Feedback 事件的唯一 ID。如省略，伺服器路由會自動產生。',
    isOptional: true,
  },
  { name: 'timestamp', type: 'Date', description: '記錄 Feedback 的時間。' },
  {
    name: 'traceId',
    type: 'string | null',
    description: '如有 Trace，則以其作為 Feedback 目標的定位基準。',
    isOptional: true,
  },
  {
    name: 'spanId',
    type: 'string | null',
    description: '如有 span，則以其作為 Feedback 目標的定位基準。',
    isOptional: true,
  },
  {
    name: 'feedbackSource',
    type: 'string | null',
    description: "選用的來源 metadata，例如 'user'、'qa'、'studio' 或 'system'。",
    isOptional: true,
  },
  {
    name: 'source',
    type: 'string | null',
    description: '`feedbackSource` 已棄用的別名。',
    isOptional: true,
  },
  {
    name: 'feedbackType',
    type: 'string',
    description: "Feedback 類型，例如 'rating'、'thumbs'、'comment' 或 'correction'。",
  },
  {
    name: 'value',
    type: 'number | string',
    description: 'Feedback 值。數值支援彙總、明細、時間序列及百分位數查詢。',
  },
  {
    name: 'comment',
    type: 'string | null',
    description: 'Feedback 的其他留言或 context。',
    isOptional: true,
  },
  {
    name: 'feedbackUserId',
    type: 'string | null',
    description: '提供 Feedback 的用戶。',
    isOptional: true,
  },
  {
    name: 'sourceId',
    type: 'string | null',
    description: '此 Feedback 所連結的來源記錄 ID，例如實驗結果 ID。',
    isOptional: true,
  },
  {
    name: 'metadata',
    type: 'Record<string, unknown> | null',
    description: 'Feedback 記錄的用戶自訂 metadata。',
    isOptional: true,
  },
]}
/>

### 共用 context 欄位

Feedback 記錄可包含共用的可觀測性 context 欄位，用於篩選、分組，以及與 Trace、日誌、指標和分數建立關聯。

<PropertiesTable
  content={[
  {
    name: 'entityType',
    type: 'EntityType | null',
    description: '產生訊號的實體類型。',
    isOptional: true,
  },
  {
    name: 'entityId',
    type: 'string | null',
    description: '產生訊號的實體 ID。',
    isOptional: true,
  },
  {
    name: 'entityName',
    type: 'string | null',
    description: '產生訊號的實體名稱。',
    isOptional: true,
  },
  {
    name: 'parentEntityType',
    type: 'EntityType | null',
    description: '上層實體的實體類型。',
    isOptional: true,
  },
  { name: 'parentEntityId', type: 'string | null', description: '上層實體的 ID。', isOptional: true },
  { name: 'parentEntityName', type: 'string | null', description: '上層實體的名稱。', isOptional: true },
  {
    name: 'rootEntityType',
    type: 'EntityType | null',
    description: '根實體的實體類型。',
    isOptional: true,
  },
  { name: 'rootEntityId', type: 'string | null', description: '根實體的 ID。', isOptional: true },
  { name: 'rootEntityName', type: 'string | null', description: '根實體的名稱。', isOptional: true },
  { name: 'userId', type: 'string | null', description: '觸發執行的人類終端用戶。', isOptional: true },
  {
    name: 'organizationId',
    type: 'string | null',
    description: '多租戶組織或帳戶。',
    isOptional: true,
  },
  { name: 'resourceId', type: 'string | null', description: '較廣泛的資源 context。', isOptional: true },
  { name: 'runId', type: 'string | null', description: '執行 run 的識別碼。', isOptional: true },
  {
    name: 'sessionId',
    type: 'string | null',
    description: '用於將 Trace 分組的 session 識別碼。',
    isOptional: true,
  },
  { name: 'threadId', type: 'string | null', description: '對話 thread 識別碼。', isOptional: true },
  { name: 'requestId', type: 'string | null', description: '用於建立關聯的 HTTP 請求 ID。', isOptional: true },
  { name: 'environment', type: 'string | null', description: '部署環境。', isOptional: true },
  { name: 'serviceName', type: 'string | null', description: '服務名稱。', isOptional: true },
  {
    name: 'scope',
    type: 'Record<string, unknown> | null',
    description: '套件、應用程式版本或部署 metadata。',
    isOptional: true,
  },
  {
    name: 'entityVersionId',
    type: 'string | null',
    description: '產生訊號的實體版本 ID。',
    isOptional: true,
  },
  {
    name: 'parentEntityVersionId',
    type: 'string | null',
    description: '上層實體的版本 ID。',
    isOptional: true,
  },
  {
    name: 'rootEntityVersionId',
    type: 'string | null',
    description: '根實體的版本 ID。',
    isOptional: true,
  },
  { name: 'experimentId', type: 'string | null', description: '實驗或 eval run 識別碼。', isOptional: true },
  {
    name: 'executionSource',
    type: 'string | null',
    description: '執行來源，例如本機、雲端或 CI。',
    isOptional: true,
  },
  { name: 'tags', type: 'string[] | null', description: '用於篩選的標籤。', isOptional: true },
]}
/>

### `FeedbackInput`

將 `FeedbackInput` 與 `mastra.observability.addFeedback()`、`recordedTrace.addFeedback()` 及 `recordedSpan.addFeedback()` 一併使用。

<PropertiesTable
  content={[
  {
    name: 'feedbackSource',
    type: 'string',
    description: 'Feedback 的選用來源 metadata。',
    isOptional: true,
  },
  { name: 'source', type: 'string', description: '`feedbackSource` 已棄用的別名。', isOptional: true },
  { name: 'feedbackType', type: 'string', description: '要記錄的 Feedback 類型。' },
  { name: 'value', type: 'number | string', description: '要記錄的 Feedback 值。' },
  { name: 'comment', type: 'string', description: '其他留言或 context。', isOptional: true },
  { name: 'feedbackUserId', type: 'string', description: '提供 Feedback 的用戶。', isOptional: true },
  { name: 'userId', type: 'string', description: '`feedbackUserId` 已棄用的別名。', isOptional: true },
  {
    name: 'metadata',
    type: 'Record<string, unknown>',
    description: '其他 Feedback 專用 metadata。',
    isOptional: true,
  },
  { name: 'experimentId', type: 'string', description: '實驗或 eval run 識別碼。', isOptional: true },
  {
    name: 'sourceId',
    type: 'string',
    description: '此 Feedback 所連結的來源記錄 ID。',
    isOptional: true,
  },
]}
/>

### `FeedbackFilter`

在 `listFeedback()` 及 OLAP 查詢的 `filters` 中使用 `FeedbackFilter`。

<PropertiesTable
  content={[
  {
    name: 'timestamp',
    type: '{ start?: Date; end?: Date; startExclusive?: boolean; endExclusive?: boolean }',
    description: '按時間戳範圍篩選。',
    isOptional: true,
  },
  { name: 'traceId', type: 'string', description: '按 Trace ID 篩選。', isOptional: true },
  { name: 'spanId', type: 'string', description: '按 span ID 篩選。', isOptional: true },
  {
    name: 'feedbackType',
    type: 'string | string[]',
    description: '按一個或多個 Feedback 類型篩選。',
    isOptional: true,
  },
  { name: 'feedbackSource', type: 'string', description: '按 Feedback 來源篩選。', isOptional: true },
  { name: 'source', type: 'string', description: '`feedbackSource` 已棄用的別名。', isOptional: true },
  {
    name: 'feedbackUserId',
    type: 'string',
    description: '按提供 Feedback 的用戶篩選。',
    isOptional: true,
  },
  { name: 'entityType', type: 'EntityType', description: '按實體類型篩選。', isOptional: true },
  { name: 'entityName', type: 'string', description: '按實體名稱篩選。', isOptional: true },
  { name: 'entityVersionId', type: 'string', description: '按實體版本 ID 篩選。', isOptional: true },
  { name: 'parentEntityType', type: 'EntityType', description: '按上層實體類型篩選。', isOptional: true },
  { name: 'parentEntityName', type: 'string', description: '按上層實體名稱篩選。', isOptional: true },
  {
    name: 'parentEntityVersionId',
    type: 'string',
    description: '按上層實體版本 ID 篩選。',
    isOptional: true,
  },
  { name: 'rootEntityType', type: 'EntityType', description: '按根實體類型篩選。', isOptional: true },
  { name: 'rootEntityName', type: 'string', description: '按根實體名稱篩選。', isOptional: true },
  { name: 'rootEntityVersionId', type: 'string', description: '按根實體版本 ID 篩選。', isOptional: true },
  { name: 'userId', type: 'string', description: '按人類終端用戶 ID 篩選。', isOptional: true },
  { name: 'organizationId', type: 'string', description: '按組織 ID 篩選。', isOptional: true },
  { name: 'resourceId', type: 'string', description: '按資源 ID 篩選。', isOptional: true },
  { name: 'runId', type: 'string', description: '按 run ID 篩選。', isOptional: true },
  { name: 'sessionId', type: 'string', description: '按 session ID 篩選。', isOptional: true },
  { name: 'threadId', type: 'string', description: '按 thread ID 篩選。', isOptional: true },
  { name: 'requestId', type: 'string', description: '按請求 ID 篩選。', isOptional: true },
  { name: 'serviceName', type: 'string', description: '按服務名稱篩選。', isOptional: true },
  { name: 'environment', type: 'string', description: '按環境篩選。', isOptional: true },
  { name: 'executionSource', type: 'string', description: '按執行來源篩選。', isOptional: true },
  {
    name: 'experimentId',
    type: 'string',
    description: '按實驗或 eval run 識別碼篩選。',
    isOptional: true,
  },
  {
    name: 'tags',
    type: 'string[]',
    description: '按標籤篩選。相符記錄必須包含所有指定標籤。',
    isOptional: true,
  },
]}
/>

## HTTP routes

| Method | Path | Purpose | Permission |
| - | - | - | - |
| `GET` | `/api/observability/feedback` | 列出 Feedback records | None derived |
| `POST` | `/api/observability/feedback` | Create a feedback record | None derived |
| `POST` | `/api/observability/feedback/aggregate` | Return one aggregate value | `observability:read` |
| `POST` | `/api/observability/feedback/breakdown` | Group feedback by dimensions | `observability:read` |
| `POST` | `/api/observability/feedback/timeseries` | Bucket feedback by interval | `observability:read` |
| `POST` | `/api/observability/feedback/percentiles` | Return percentile series | `observability:read` |

## Related

- [Feedback guide](/docs/observability/feedback)
- [Client SDK observability reference](/reference/client-js/observability)
- [Observability configuration](/reference/observability/tracing/configuration)
