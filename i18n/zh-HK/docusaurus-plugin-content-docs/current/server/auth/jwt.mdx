---
title: "JSON Web Token | Auth"
description: "在 Mastra 內使用 JSON Web Token 的文件。"
packages:
  - "@mastra/auth"
  - "@mastra/client-js"
  - "@mastra/core"
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# JSON Web Token

`MastraJwtAuth` 類別透過 JSON Web Token（JWT）為 Mastra 提供輕量驗證機制。它會根據共享密鑰驗證傳入請求，並透過 `auth` 選項與 Mastra 伺服器整合。

## 安裝

使用 `MastraJwtAuth` 類別前，必須先安裝 `@mastra/auth` 依賴套件。

```bash npm2yarn
npm install @mastra/auth@latest
```

## 建立 JWT

如要驗證傳送至 Mastra 伺服器的請求，你需要一個以 `MASTRA_JWT_SECRET` 簽署的有效 JSON Web Token（JWT）。

最簡單的方法是使用 [jwt.io](https://www.jwt.io/) 產生：

1. 選擇 **JWT Encoder**。
1. 向下捲動至 **Sign JWT: Secret** 部分。
1. 輸入密鑰（例如：`supersecretdevkeythatishs256safe!`）。
1. 按一下 **Generate example**，建立有效 JWT。
1. 複製產生的權杖，將其設定為 `MASTRA_JWT_TOKEN`，並儲存在 `.env` 檔案中。

## 使用範例

使用產生的 JWT，在 Mastra 伺服器中設定 `MastraJwtAuth`：

```typescript {2,6-8} title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { MastraJwtAuth } from '@mastra/auth'

export const mastra = new Mastra({
  server: {
    auth: new MastraJwtAuth({
      secret: process.env.MASTRA_JWT_SECRET,
    }),
  },
})
```

如要查看所有可用設定選項，請參閱 [MastraJwtAuth](/reference/auth/jwt)。

在 [Studio](/docs/studio/overview) 中前往 **Settings**，然後在 **Headers** 下選擇 **"Add Header"** 按鈕。輸入 `Authorization` 作為標頭名稱，並以 `Bearer <your-jwt>` 作為值。

## 設定 `MastraClient`

啟用 `auth` 後，所有透過 `MastraClient` 發出的請求都必須在 `Authorization` 標頭中包含有效 JWT：

```typescript {6} title="lib/mastra/mastra-client.ts"
import { MastraClient } from '@mastra/client-js'

export const mastraClient = new MastraClient({
  baseUrl: 'https://<mastra-api-url>',
  headers: {
    Authorization: `Bearer ${process.env.MASTRA_JWT_TOKEN}`,
  },
})
```

如要查看更多設定選項，請參閱 [Mastra Client SDK](/docs/server/mastra-client)。

### 發出已驗證請求

設定 `MastraClient` 後，你可從前端應用程式傳送已驗證請求，亦可使用 `curl` 快速在本機測試：

<Tabs>
  <TabItem value="react" label="React">
    ```tsx title="src/components/test-agent.tsx"
    import { mastraClient } from '../../lib/mastra-client'

    export const TestAgent = () => {
      async function handleClick() {
        const agent = mastraClient.getAgent('weatherAgent')

        const response = await agent.generate('Weather in London')

        console.log(response)
      }

      return <button onClick={handleClick}>Test Agent</button>
    }
    ```
  </TabItem>

  <TabItem value="curl" label="cURL">
    ```bash
    curl -X POST http://localhost:4111/api/agents/weatherAgent/generate \
      -H "Content-Type: application/json" \
      -H "Authorization: Bearer <your-jwt>" \
      -d '{
        "messages": "Weather in London"
      }'
    ```
  </TabItem>
</Tabs>
