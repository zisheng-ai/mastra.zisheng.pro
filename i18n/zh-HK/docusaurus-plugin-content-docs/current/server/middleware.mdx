---
title: "Middleware | Server"
description: "套用自訂 middleware function 以攔截請求。"
packages:
  - "@mastra/core"
  - "@mastra/server"
---

# Middleware

Mastra 伺服器可在呼叫 API route handler 前後執行自訂 middleware function。這適用於驗證、記錄、注入請求專屬的 context，或加入 CORS header 等用途。

Middleware 會接收 [Hono](https://hono.dev) `Context`（`c`）及 `next` function。如它傳回 `Response`，請求便會提早結束。呼叫 `next()` 會繼續處理下一個 middleware 或 route handler。

```typescript
import { Mastra } from '@mastra/core'

export const mastra = new Mastra({
  server: {
    middleware: [
      {
        handler: async (c, next) => {
          // Example: Add authentication check
          const authHeader = c.req.header('Authorization')
          if (!authHeader) {
            return new Response('Unauthorized', { status: 401 })
          }

          await next()
        },
        path: '/api/*',
      },
      // Add a global request logger
      async (c, next) => {
        console.log(`${c.req.method} ${c.req.url}`)
        await next()
      },
    ],
  },
})
```

如要將 middleware 附加至單一 route，請將 `middleware` 選項傳遞至 `registerApiRoute`：

```typescript
registerApiRoute('/my-custom-route', {
  method: 'GET',
  middleware: [
    async (c, next) => {
      console.log(`${c.req.method} ${c.req.url}`)
      await next()
    },
  ],
  handler: async c => {
    const mastra = c.get('mastra')
    return c.json({ message: 'Hello, world!' })
  },
})
```

## 常見範例

### 使用 `RequestContext`

你可在 runtime 伺服器 middleware 中從請求擷取資料，以填入 `RequestContext`。在此範例中，系統會設定 `temperature-unit`；其值以 Cloudflare `CF-IPCountry` header 為依據，確保回應符合使用者的地區設定。

```typescript title="src/mastra/index.ts"
import { Mastra } from '@mastra/core'
import { RequestContext } from '@mastra/core/request-context'
import { testWeatherAgent } from './agents/test-weather-agent'

export const mastra = new Mastra({
  agents: { testWeatherAgent },
  server: {
    middleware: [
      async (context, next) => {
        const country = context.req.header('CF-IPCountry')
        const requestContext = context.get('requestContext')

        requestContext.set('temperature-unit', country === 'US' ? 'fahrenheit' : 'celsius')

        await next()
      },
    ],
  },
})
```

### 驗證

```typescript prettier:false
{
  handler: async (c, next) => {
    const authHeader = c.req.header('Authorization');
    if (!authHeader || !authHeader.startsWith('Bearer ')) {
      return new Response('Unauthorized', { status: 401 });
    }

    // Validate token here
    await next();
  },
  path: '/api/*',
}
```

### 授權（使用者隔離）

驗證會確認使用者身分，授權則控制他們可存取的內容。如未按 resource ID 劃分範圍，已驗證使用者可能透過猜測 ID 或操控 `resourceId` parameter，存取其他使用者的 thread。

要將 memory 及 thread 範圍限制於已驗證使用者，最簡單的方法是在 auth config 中使用 `mapUserToResourceId` callback：

```typescript
import { Mastra } from '@mastra/core'

export const mastra = new Mastra({
  server: {
    auth: {
      authenticateToken: async token => {
        return verifyToken(token) // { id: 'user-123', orgId: 'org-456', ... }
      },
      mapUserToResourceId: user => user.id,
    },
  },
})
```

驗證成功後，系統會以已驗證的 user object 呼叫 `mapUserToResourceId`。傳回值會在 Request Context 中設定為 `MASTRA_RESOURCE_ID_KEY`，並適用於所有伺服器 Adapter（Hono、Express、Next.js 等）。

Resource ID 不一定要是 `user.id`。常見模式如下：

```typescript
// Org-scoped
mapUserToResourceId: user => `${user.orgId}:${user.id}`

// From a JWT claim
mapUserToResourceId: user => user.tenantId

// Composite key
mapUserToResourceId: user => `${user.workspaceId}:${user.projectId}:${user.id}`
```

設定 resource ID 後，伺服器會自動：

- **篩選 thread 清單**，只傳回屬於該使用者的 thread
- **驗證 thread 存取權**，如存取其他使用者的 thread，便傳回 403
- **強制建立 thread** 時使用已驗證使用者的 ID
- **驗證 message 操作**，包括刪除，確保 message 屬於使用者擁有的 thread

即使客戶端傳遞 `?resourceId=other-user-id`，auth 所設定的值亦有優先權。嘗試存取其他使用者擁有的 thread 或 message 時，系統會傳回 403 錯誤。

#### 進階：在 middleware 中設定 resource ID

如情況較複雜（例如從 database 查找 resource ID），可直接在 middleware 中設定 `MASTRA_RESOURCE_ID_KEY`：

```typescript
import { Mastra } from '@mastra/core'
import { MASTRA_RESOURCE_ID_KEY } from '@mastra/core/request-context'
import { getAuthenticatedUser } from '@mastra/server/auth'

export const mastra = new Mastra({
  server: {
    auth: {
      authenticateToken: async token => verifyToken(token),
    },
    middleware: [
      {
        path: '/api/*',
        handler: async (c, next) => {
          const token = c.req.header('Authorization')
          if (!token) {
            return c.json({ error: 'Unauthorized' }, 401)
          }

          const user = await getAuthenticatedUser<{ id: string }>({
            mastra: c.get('mastra'),
            token,
            request: c.req.raw,
          })
          const requestContext = c.get('requestContext')

          if (!user) {
            return c.json({ error: 'Unauthorized' }, 401)
          }

          requestContext.set(MASTRA_RESOURCE_ID_KEY, user.id)
          return next()
        },
      },
    ],
  },
})
```

`server.middleware` 會在 Mastra 的逐 route auth check 前運行。當 middleware 需要已驗證使用者時，請呼叫 `getAuthenticatedUser()`，從已配置的 auth Provider 解析使用者，而不改變預設的 route auth flow。

#### 使用 `MASTRA_THREAD_ID_KEY`

你亦可設定 `MASTRA_THREAD_ID_KEY`，以覆寫客戶端提供的 thread ID：

```typescript
import { MASTRA_RESOURCE_ID_KEY, MASTRA_THREAD_ID_KEY } from '@mastra/core/request-context'

// Force operations to use a specific thread
requestContext.set(MASTRA_THREAD_ID_KEY, validatedThreadId)
```

如你想將操作限制於已透過其他方式驗證的特定 thread，便可使用此設定。

### CORS 支援

```typescript prettier:false
{
  handler: async (c, next) => {
    c.header('Access-Control-Allow-Origin', '*');
    c.header(
      'Access-Control-Allow-Methods',
      'GET, POST, PUT, DELETE, OPTIONS',
    );
    c.header(
      'Access-Control-Allow-Headers',
      'Content-Type, Authorization',
    );

    if (c.req.method === 'OPTIONS') {
      return new Response(null, { status: 204 });
    }

    await next();
  },
}
```

### 請求記錄

```typescript prettier:false
{
  handler: async (c, next) => {
    const start = Date.now();
    await next();
    const duration = Date.now() - start;
    console.log(`${c.req.method} ${c.req.url} - ${duration}ms`);
  },
}
```

# 相關內容

- [Request Context](/docs/server/request-context)
- [保留 key](/docs/server/request-context#reserved-keys)
