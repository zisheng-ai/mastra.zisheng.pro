---
title: "Subconscious | 模型"
description: "在 Mastra 使用 Subconscious 模型。共有 2 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/subconscious.svg" alt="Subconscious 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Subconscious

透過 Mastra 的模型路由器存取 2 個 Subconscious 模型。系統會自動使用 `SUBCONSCIOUS_API_KEY` 環境變數進行驗證。

詳情請參閱[Subconscious 文件](https://docs.subconscious.dev)。

```bash title=".env"
SUBCONSCIOUS_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'subconscious/subconscious/glm-5.2',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` 端點。部分 Provider 專屬功能可能無法使用，詳情請參閱[Subconscious 文件](https://docs.subconscious.dev)。

:::

## 模型

<ProviderModelsTable
  models={[
  {
    model: 'subconscious/subconscious/glm-5.2',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1000000,
    maxOutput: 131072,
    inputCost: 1.4,
    outputCost: 4.4,
  },
  {
    model: 'subconscious/subconscious/tim-qwen3.6-27b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 8192,
    maxOutput: 5000,
    inputCost: 0.3,
    outputCost: 3,
  },
]}
/>

## 進階設定

### 自訂標頭

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://api.subconscious.dev/v1',
    id: 'subconscious/subconscious/glm-5.2',
    apiKey: process.env.SUBCONSCIOUS_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動態選擇模型

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced
      ? 'subconscious/subconscious/tim-qwen3.6-27b'
      : 'subconscious/subconscious/glm-5.2'
  },
})
```

## 直接安裝 Provider

此 Provider 亦可直接安裝為獨立套件，用來取代 Mastra 模型路由器字串。詳情請參閱[套件文件](https://www.npmjs.com/package/@ai-sdk/anthropic)。

```bash npm2yarn
npm install @ai-sdk/anthropic
```
