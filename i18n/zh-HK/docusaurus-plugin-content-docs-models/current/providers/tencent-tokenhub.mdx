---
title: "Tencent TokenHub | 模型"
description: "在 Mastra 使用 Tencent TokenHub 模型。共有 2 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/tencent-tokenhub.svg" alt="Tencent TokenHub 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Tencent TokenHub

透過 Mastra 的模型路由器存取 2 個 Tencent TokenHub 模型。系統會自動使用 `TENCENT_TOKENHUB_API_KEY` 環境變數進行驗證。

詳情請參閱[Tencent TokenHub 文件](https://cloud.tencent.com/document/product/1823/130050)。

```bash title=".env"
TENCENT_TOKENHUB_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from "@mastra/core/agent";

const agent = new Agent({
  id: "my-agent",
  name: "My Agent",
  instructions: "You are a helpful assistant",
  model: "tencent-tokenhub/hy3"
});

// Generate a response
const response = await agent.generate("Hello!");

// Stream a response
const stream = await agent.stream("Tell me a story");
for await (const chunk of stream) {
  console.log(chunk);
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` 端點。部分 Provider 專屬功能可能無法使用，詳情請參閱[Tencent TokenHub 文件](https://cloud.tencent.com/document/product/1823/130050)。

:::

## 模型

<ProviderModelsTable
  models={[
  {
    "model": "tencent-tokenhub/hy3",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 256000,
    "maxOutput": 64000,
    "inputCost": null,
    "outputCost": null
  },
  {
    "model": "tencent-tokenhub/hy3-preview",
    "imageInput": false,
    "audioInput": false,
    "videoInput": false,
    "toolUsage": true,
    "reasoning": true,
    "contextWindow": 256000,
    "maxOutput": 64000,
    "inputCost": null,
    "outputCost": null
  }
]}
/>

## 進階設定

### 自訂標頭

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "custom-agent",
  name: "custom-agent",
  model: {
    url: "https://tokenhub.tencentmaas.com/v1",
    id: "tencent-tokenhub/hy3",
    apiKey: process.env.TENCENT_TOKENHUB_API_KEY,
    headers: {
      "X-Custom-Header": "value"
    }
  }
});
```

### 動態選擇模型

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: "dynamic-agent",
  name: "Dynamic Agent",
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === "complex";
    return useAdvanced
      ? "tencent-tokenhub/hy3-preview"
      : "tencent-tokenhub/hy3";
  }
});
```


