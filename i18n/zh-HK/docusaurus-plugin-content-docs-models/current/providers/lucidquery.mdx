---
title: "LucidQuery | 模型"
description: "在 Mastra 使用 LucidQuery 模型。共有 4 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/lucidquery.svg" alt="LucidQuery 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />LucidQuery

透過 Mastra 的模型路由器存取 4 個 LucidQuery 模型。系統會自動使用 `LUCIDQUERY_API_KEY` 環境變數進行驗證。

詳情請參閱[LucidQuery 文件](https://lucidquery.com/docs)。

```bash title=".env"
LUCIDQUERY_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'lucidquery/lucidnova-rf1-100b',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` 端點。部分 Provider 專屬功能可能無法使用，詳情請參閱[LucidQuery 文件](https://lucidquery.com/docs)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'lucidquery/lucidnova-rf1-100b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 120000,
    maxOutput: 8000,
    inputCost: 2,
    outputCost: 5,
  },
  {
    model: 'lucidquery/lucidquery-agi-01-frontier',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 300000,
    maxOutput: 120000,
    inputCost: 4.5,
    outputCost: 22,
  },
  {
    model: 'lucidquery/lucidquery-agi-01-swift',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 300000,
    maxOutput: 120000,
    inputCost: 2.5,
    outputCost: 15,
  },
  {
    model: 'lucidquery/lucidquery-nexus-coder',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 250000,
    maxOutput: 60000,
    inputCost: 2,
    outputCost: 5,
  },
]}
/>

## 進階設定 {/* #advanced-configuration */}

### 自訂標頭 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://api.lucidquery.com/v1',
    id: 'lucidquery/lucidnova-rf1-100b',
    apiKey: process.env.LUCIDQUERY_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動態選擇模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'lucidquery/lucidquery-nexus-coder' : 'lucidquery/lucidnova-rf1-100b'
  },
})
```
