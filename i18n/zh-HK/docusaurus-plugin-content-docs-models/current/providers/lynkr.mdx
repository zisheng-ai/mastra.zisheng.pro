---
title: "Lynkr | 模型"
description: "在 Mastra 使用 Lynkr 模型。共有 1 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/lynkr.svg" alt="Lynkr 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Lynkr

透過 Mastra 的模型路由器存取 1 個 Lynkr 模型。系統會自動使用 `LYNKR_API_KEY` 環境變數進行驗證。

詳情請參閱[Lynkr 文件](https://github.com/Fast-Editor/Lynkr)。

```bash title=".env"
LYNKR_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'lynkr/lynkr-auto',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` 端點。部分 Provider 專屬功能可能無法使用，詳情請參閱[Lynkr 文件](https://github.com/Fast-Editor/Lynkr)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'lynkr/lynkr-auto',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: false,
    contextWindow: 128000,
    maxOutput: 8192,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 進階設定 {/* #advanced-configuration */}

### 自訂標頭 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'http://127.0.0.1:8081/v1',
    id: 'lynkr/lynkr-auto',
    apiKey: process.env.LYNKR_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動態選擇模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'lynkr/lynkr-auto' : 'lynkr/lynkr-auto'
  },
})
```
