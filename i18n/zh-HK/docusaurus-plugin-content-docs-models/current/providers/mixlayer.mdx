---
title: "Mixlayer | 模型"
description: "在 Mastra 使用 Mixlayer 模型。共有 5 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/mixlayer.svg" alt="Mixlayer 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Mixlayer {/* #mixlayer */}

透過 Mastra 的模型路由器存取 5 個 Mixlayer 模型。系統會自動使用 `MIXLAYER_API_KEY` 環境變數進行驗證。

詳情請參閱[Mixlayer 文件](https://docs.mixlayer.com)。

```bash title=".env"
MIXLAYER_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'mixlayer/qwen/qwen3.5-122b-a10b',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` 端點。部分 Provider 專屬功能可能無法使用，詳情請參閱[Mixlayer 文件](https://docs.mixlayer.com)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'mixlayer/qwen/qwen3.5-122b-a10b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 262144,
    inputCost: 0.4,
    outputCost: 3.2,
  },
  {
    model: 'mixlayer/qwen/qwen3.5-27b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 262144,
    inputCost: 0.3,
    outputCost: 2.4,
  },
  {
    model: 'mixlayer/qwen/qwen3.5-35b-a3b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 262144,
    inputCost: 0.25,
    outputCost: 1.3,
  },
  {
    model: 'mixlayer/qwen/qwen3.5-397b-a17b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 262144,
    inputCost: 0.6,
    outputCost: 3.6,
  },
  {
    model: 'mixlayer/qwen/qwen3.5-9b',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 262144,
    maxOutput: 262144,
    inputCost: 0.1,
    outputCost: 0.4,
  },
]}
/>

## 進階設定 {/* #advanced-configuration */}

### 自訂標頭 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://models.mixlayer.ai/v1',
    id: 'mixlayer/qwen/qwen3.5-122b-a10b',
    apiKey: process.env.MIXLAYER_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動態選擇模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced ? 'mixlayer/qwen/qwen3.5-9b' : 'mixlayer/qwen/qwen3.5-122b-a10b'
  },
})
```
