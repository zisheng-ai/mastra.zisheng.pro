---
title: "abliteration.ai | 模型"
description: "在 Mastra 使用 abliteration.ai 模型。共有 2 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/abliteration-ai.svg" alt="abliteration.ai 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />abliteration.ai

透過 Mastra 的模型路由器存取 2 個 abliteration.ai 模型。系統會自動使用 `ABLIT_KEY` 環境變數進行驗證。

詳情請參閱[abliteration.ai 文件](https://docs.abliteration.ai/models)。

```bash title=".env"
ABLIT_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'abliteration-ai/abliterated-model',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` 端點。部分 Provider 專屬功能可能無法使用，詳情請參閱[abliteration.ai 文件](https://docs.abliteration.ai/models)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'abliteration-ai/abliterated-model',
    imageInput: true,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 150000,
    maxOutput: 8192,
    inputCost: 3,
    outputCost: 3,
  },
  {
    model: 'abliteration-ai/abliterated-model-large',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1000000,
    maxOutput: 999990,
    inputCost: 5,
    outputCost: 5,
  },
]}
/>

## 進階設定 {/* #advanced-configuration */}

### 自訂標頭 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://api.abliteration.ai/v1',
    id: 'abliteration-ai/abliterated-model',
    apiKey: process.env.ABLIT_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動態選擇模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced
      ? 'abliteration-ai/abliterated-model-large'
      : 'abliteration-ai/abliterated-model'
  },
})
```
