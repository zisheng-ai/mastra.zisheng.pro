---
title: "Xiaomi Token Plan （中國） | 模型"
description: "在 Mastra 使用 Xiaomi Token Plan （中國） 模型。共有 7 個可用模型。"
---

{/* This file is auto-generated by generate-model-docs.ts - DO NOT EDIT MANUALLY */}

# <img src="https://models.dev/logos/xiaomi-token-plan-cn.svg" alt="Xiaomi Token Plan （中國） 標誌" className="inline w-8 h-8 mr-2 align-middle dark:invert dark:brightness-0 dark:contrast-200" />Xiaomi Token Plan （中國）

透過 Mastra 的模型路由器存取 7 個 Xiaomi Token Plan （中國） 模型。系統會自動使用 `XIAOMI_API_KEY` 環境變數進行驗證。

詳情請參閱[Xiaomi Token Plan （中國） 文件](https://platform.xiaomimimo.com/#/docs)。

```bash title=".env"
XIAOMI_API_KEY=your-api-key
```

```typescript title="src/mastra/agents/my-agent.ts" {7}
import { Agent } from '@mastra/core/agent'

const agent = new Agent({
  id: 'my-agent',
  name: 'My Agent',
  instructions: 'You are a helpful assistant',
  model: 'xiaomi-token-plan-cn/mimo-v2-pro',
})

// Generate a response
const response = await agent.generate('Hello!')

// Stream a response
const stream = await agent.stream('Tell me a story')
for await (const chunk of stream) {
  console.log(chunk)
}
```

:::info

Mastra 使用與 OpenAI 相容的 `/chat/completions` 端點。部分 Provider 專屬功能可能無法使用，詳情請參閱[Xiaomi Token Plan （中國） 文件](https://platform.xiaomimimo.com/#/docs)。

:::

## 模型 {/* #models */}

<ProviderModelsTable
  models={[
  {
    model: 'xiaomi-token-plan-cn/mimo-v2-tts',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: false,
    reasoning: false,
    contextWindow: 8192,
    maxOutput: 8192,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'xiaomi-token-plan-cn/mimo-v2.5',
    imageInput: true,
    audioInput: true,
    videoInput: true,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1048576,
    maxOutput: 131072,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'xiaomi-token-plan-cn/mimo-v2.5-pro',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: true,
    reasoning: true,
    contextWindow: 1048576,
    maxOutput: 131072,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'xiaomi-token-plan-cn/mimo-v2.5-tts',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: false,
    reasoning: false,
    contextWindow: 8192,
    maxOutput: 8192,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'xiaomi-token-plan-cn/mimo-v2.5-tts-voiceclone',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: false,
    reasoning: false,
    contextWindow: 8192,
    maxOutput: 8192,
    inputCost: null,
    outputCost: null,
  },
  {
    model: 'xiaomi-token-plan-cn/mimo-v2.5-tts-voicedesign',
    imageInput: false,
    audioInput: false,
    videoInput: false,
    toolUsage: false,
    reasoning: false,
    contextWindow: 8192,
    maxOutput: 8192,
    inputCost: null,
    outputCost: null,
  },
]}
/>

## 進階設定 {/* #advanced-configuration */}

### 自訂標頭 {/* #custom-headers */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'custom-agent',
  name: 'custom-agent',
  model: {
    url: 'https://token-plan-cn.xiaomimimo.com/v1',
    id: 'xiaomi-token-plan-cn/mimo-v2-pro',
    apiKey: process.env.XIAOMI_API_KEY,
    headers: {
      'X-Custom-Header': 'value',
    },
  },
})
```

### 動態選擇模型 {/* #dynamic-model-selection */}

```typescript title="src/mastra/agents/my-agent.ts"
const agent = new Agent({
  id: 'dynamic-agent',
  name: 'Dynamic Agent',
  model: ({ requestContext }) => {
    const useAdvanced = requestContext.task === 'complex'
    return useAdvanced
      ? 'xiaomi-token-plan-cn/mimo-v2.5-tts-voicedesign'
      : 'xiaomi-token-plan-cn/mimo-v2-pro'
  },
})
```
