import fs from "node:fs";

const translations = new Map(Object.entries({
  'title: "Workflows overview"': 'title: "Workflow 概覽"',
  'description: "Workflows in Mastra help you orchestrate complex sequences of tasks with features like branching, parallel execution, resource suspension, and more."': 'description: "Mastra 中的 Workflow 可協助你編排複雜的任務序列，並提供分支、平行執行、資源暫停等功能。"',
  '# Workflows overview': '# Workflow 概覽',
  'Workflows let you define complex sequences of tasks using clear, structured steps rather than relying on the reasoning of a single agent. They give you full control over how tasks are broken down and how data moves between them, plus what gets executed when. Workflows run using the built-in execution engine by default, or can be deployed to [workflow runners](/docs/deployment/workflow-runners) like Inngest for managed infrastructure.': 'Workflow 可讓你透過清楚且結構化的步驟定義複雜的任務序列，而不必依賴單一 Agent 的推理。你可以完整控制任務如何拆分、資料如何在任務之間流動，以及何時執行哪些內容。Workflow 預設使用內建執行引擎，也可以部署至 Inngest 等 [Workflow runner](/docs/deployment/workflow-runners)，使用代管基礎架構。',
  '**For AI agents:** Run `npx mastra api workflow run start test-workflow \'{"inputData":{"message":"Hello, world!"}}\'` to start this workflow directly instead of writing a temporary script. The command requires a running Mastra server; start the local server with `npx mastra dev`, or pass the reachable server\'s base URL with `--url`. Run `npx mastra api workflow run start --schema` before constructing different input. Install Mastra\'s skill with `npx skills add mastra-ai/skills --skill mastra` for complete API CLI discovery, targeting, schema, authentication, and error-handling guidance.': '**給 AI Agent：** 執行 `npx mastra api workflow run start test-workflow \'{"inputData":{"message":"Hello, world!"}}\'` 可直接啟動此 Workflow，無須撰寫暫用指令碼。此命令需要執行中的 Mastra 伺服器；請使用 `npx mastra dev` 啟動本機伺服器，或透過 `--url` 傳入可連線伺服器的基底 URL。建立不同的輸入前，請先執行 `npx mastra api workflow run start --schema`。使用 `npx skills add mastra-ai/skills --skill mastra` 安裝 Mastra Skill，即可取得完整的 API CLI 探索、目標指定、結構描述、驗證及錯誤處理指引。',
  '## When to use workflows': '## 適合使用 Workflow 的時機',
  'Use workflows for tasks that are clearly defined upfront and involve multiple steps with a specific execution order. They give you fine-grained control over how data flows and transforms between steps, and which primitives are called at each stage.': '若任務可預先明確定義，且包含多個具特定執行順序的步驟，請使用 Workflow。你可以精細控制資料如何在步驟之間流動與轉換，以及每個階段會呼叫哪些基本元件。',
  '## Core principles': '## 核心原則',
  'Mastra workflows operate using these principles:': 'Mastra Workflow 依循以下原則運作：',
  '- Defining **steps** with [`createStep`](/reference/workflows/step), specifying input/output schemas and business logic.': '- 使用 [`createStep`](/reference/workflows/step) 定義**步驟**，並指定輸入／輸出結構描述與商業邏輯。',
  '- Composing **steps** with [`createWorkflow`](/reference/workflows/workflow) to define the execution flow.': '- 使用 [`createWorkflow`](/reference/workflows/workflow) 組合**步驟**，以定義執行流程。',
  '- Running **workflows** to execute the entire sequence, with built-in support for suspension, resumption, and streaming results.': '- 執行 **Workflow** 以運行整個序列，並透過內建功能支援暫停、繼續與串流結果。',
  '## Creating a workflow step': '## 建立 Workflow 步驟',
  'Steps are the building blocks of workflows. Create a step using `createStep()` with `inputSchema` and `outputSchema` to define the data it accepts and returns. You can define both schemas using [Standard JSON Schema](https://standardschema.dev/json-schema) ([Zod](https://zod.dev/), [Valibot](https://valibot.dev/), [ArkType](https://arktype.io/), etc.).': '步驟是 Workflow 的組成元件。使用 `createStep()` 建立步驟，並以 `inputSchema` 和 `outputSchema` 定義它接受與傳回的資料。這兩種結構描述都可以使用 [Standard JSON Schema](https://standardschema.dev/json-schema)（例如 [Zod](https://zod.dev/)、[Valibot](https://valibot.dev/)、[ArkType](https://arktype.io/)）定義。',
  'The `execute` function defines what the step does. Use it to call functions in your codebase, external APIs, agents, or tools.': '`execute` 函式會定義步驟的行為。你可以用它呼叫程式碼庫中的函式、外部 API、Agent 或 Tool。',
  'Visit [`Step`](/reference/workflows/step) for a full list of configuration options.': '如需完整的設定選項清單，請參閱 [`Step`](/reference/workflows/step)。',
  '### Using agents and tools': '### 使用 Agent 與 Tool',
  'Workflow steps can also call registered agents or import and execute tools directly, visit the [Using Tools](/docs/agents/using-tools) page for more information.': 'Workflow 步驟也可以呼叫已註冊的 Agent，或直接匯入並執行 Tool。如需更多資訊，請參閱[使用 Tool](/docs/agents/using-tools)。',
  '## Creating a workflow': '## 建立 Workflow',
  'Create a workflow using `createWorkflow()` with `inputSchema` and `outputSchema` to define the data it accepts and returns. You can define both schemas using [Standard JSON Schema](https://standardschema.dev/json-schema) ([Zod](https://zod.dev/), [Valibot](https://valibot.dev/), [ArkType](https://arktype.io/), etc.). Add steps using `.then()` and complete the workflow with `.commit()`.': '使用 `createWorkflow()` 建立 Workflow，並以 `inputSchema` 和 `outputSchema` 定義它接受與傳回的資料。這兩種結構描述都可以使用 [Standard JSON Schema](https://standardschema.dev/json-schema)（例如 [Zod](https://zod.dev/)、[Valibot](https://valibot.dev/)、[ArkType](https://arktype.io/)）定義。使用 `.then()` 新增步驟，再以 `.commit()` 完成 Workflow。',
  'Visit [Workflow Class](/reference/workflows/workflow) for a full list of configuration options.': '如需完整的設定選項清單，請參閱 [Workflow 類別](/reference/workflows/workflow)。',
  '### Understanding control flow': '### 瞭解控制流程',
  'Workflows can be composed using a number of different methods. The method you choose determines how each step\'s schema should be structured. Visit the [Control Flow](/docs/workflows/control-flow) page for more information.': 'Workflow 可以使用多種不同方法組合。你選擇的方法會決定每個步驟的結構描述應如何安排。如需更多資訊，請參閱[控制流程](/docs/workflows/control-flow)。',
  'Open [Studio](/docs/studio/overview) and select a workflow from the **Workflows** tab.': '開啟 [Studio](/docs/studio/overview)，然後從 **Workflows** 分頁選取一個 Workflow。',
  '- **Graph view**: The center panel visualizes the workflow\'s steps and execution flow.': '- **圖形檢視**：中央面板會以視覺化方式呈現 Workflow 的步驟與執行流程。',
  '- **Input form**: The right sidebar generates a form from the workflow\'s `inputSchema`. Fill it in and start the run.': '- **輸入表單**：右側邊欄會依據 Workflow 的 `inputSchema` 產生表單。填寫表單後即可開始執行。',
  '- **Live status**: During execution, the graph updates each step\'s status in real time. The sidebar shows the workflow\'s input, output, state, and logs.': '- **即時狀態**：執行期間，圖形會即時更新每個步驟的狀態。側邊欄會顯示 Workflow 的輸入、輸出、狀態與記錄。',
  '- [**Time travel**](/docs/workflows/time-travel): After a run completes, replay individual steps to inspect or retry them.': '- [**時間回溯**](/docs/workflows/time-travel)：執行完成後，可重新執行個別步驟，以便檢查或重試。',
  '## Workflow state': '## Workflow 狀態',
  'Workflow state lets you share values across steps without passing them through every step\'s inputSchema and outputSchema. Use state for tracking progress or accumulating results, or alternatively sharing configuration across the entire workflow.': 'Workflow 狀態可讓你在步驟之間共用值，而不必透過每個步驟的 inputSchema 與 outputSchema 傳遞。你可以使用狀態追蹤進度、累積結果，或在整個 Workflow 中共用設定。',
  'Visit [Workflow State](/docs/workflows/workflow-state) for complete documentation on state schemas, initial state, persistence across suspend/resume, and nested workflows.': '如需狀態結構描述、初始狀態、暫停／繼續期間的持久化，以及巢狀 Workflow 的完整說明，請參閱 [Workflow 狀態](/docs/workflows/workflow-state)。',
  '## Workflows as steps': '## 將 Workflow 作為步驟',
  'Use a workflow as a step to reuse its logic within a larger composition. Input and output follow the same schema rules described in [Core principles](/docs/workflows/control-flow).': '將 Workflow 作為步驟，即可在更大型的組合中重複使用其邏輯。輸入與輸出遵循[核心原則](/docs/workflows/control-flow)所述的相同結構描述規則。',
  '### Cloning a workflow': '### 複製 Workflow',
  'Clone a workflow using `cloneWorkflow()` when you want to reuse its logic but track it separately under a new ID. Each clone runs independently and shows up as a distinct workflow in logs and observability tools.': '若要重複使用 Workflow 邏輯，但以新的 ID 分別追蹤，請使用 `cloneWorkflow()` 複製 Workflow。每個複本都會獨立執行，並在記錄與可觀測性 Tool 中顯示為不同的 Workflow。',
  '## Registering a workflow': '## 註冊 Workflow',
  'Register your workflow in the Mastra instance to make it available throughout your application. Once registered, it can be called from agents or tools and has access to shared resources such as logging and observability features:': '在 Mastra 執行個體中註冊 Workflow，即可讓整個應用程式使用它。註冊後，Agent 或 Tool 可以呼叫它，而它也能存取記錄與可觀測性功能等共用資源：',
  '## Referencing a workflow': '## 取得 Workflow 參照',
  'You can run workflows from agents, tools, the Mastra Client, or the command line. Get a reference by calling `.getWorkflow()` on your `mastra` or `mastraClient` instance, depending on your setup:': '你可以從 Agent、Tool、Mastra Client 或命令列執行 Workflow。請依據你的設定，在 `mastra` 或 `mastraClient` 執行個體上呼叫 `.getWorkflow()` 以取得參照：',
  '`mastra.getWorkflow()` is preferred over a direct import for two reasons:': '建議使用 `mastra.getWorkflow()`，而非直接匯入，原因有二：',
  '1. It provides access to the Mastra instance configuration (logger, telemetry, storage, registered agents, and vector stores)': '1. 它可以存取 Mastra 執行個體設定（記錄器、遙測、儲存空間、已註冊的 Agent 與向量儲存區）',
  '1. It provides full TypeScript type inference for workflow input and output schemas': '1. 它可針對 Workflow 輸入與輸出結構描述提供完整的 TypeScript 型別推斷',
  'Use `getWorkflow()` with the workflow\'s **registration key** (the key used when adding it to Mastra). While `getWorkflowById()` is available for retrieving workflows by their `id` property, it doesn\'t provide the same level of type inference.': '請將 Workflow 的**註冊鍵**（將其加入 Mastra 時使用的鍵）傳給 `getWorkflow()`。雖然可以使用 `getWorkflowById()`，依 Workflow 的 `id` 屬性取得 Workflow，但它無法提供相同程度的型別推斷。',
  '## Running workflows': '## 執行 Workflow',
  'Workflows can be run in two modes: start waits for all steps to complete before returning, while stream emits events during execution. Choose the approach that fits your use case: start when you only need the final result and stream when you want to monitor progress or trigger actions as steps complete.': 'Workflow 可使用兩種模式執行：start 會等所有步驟完成後才傳回，而 stream 會在執行期間發出事件。請依使用情境選擇合適的方法：只需要最終結果時使用 start；想監控進度或在步驟完成時觸發動作，則使用 stream。',
  'Create a workflow run instance using `createRun()`, then call `.start()` with `inputData` matching the workflow\'s `inputSchema`. The workflow executes all steps and returns the final result.': '使用 `createRun()` 建立 Workflow 執行個體，再以符合 Workflow `inputSchema` 的 `inputData` 呼叫 `.start()`。Workflow 會執行所有步驟並傳回最終結果。',
  'Create a workflow run instance using `.createRun()`, then call `.stream()` with `inputData` matching the workflow\'s `inputSchema`. Iterate over `fullStream` to track progress, then await `result` to get the final workflow result.': '使用 `.createRun()` 建立 Workflow 執行個體，再以符合 Workflow `inputSchema` 的 `inputData` 呼叫 `.stream()`。逐一處理 `fullStream` 以追蹤進度，然後等候 `result` 取得 Workflow 的最終結果。',
  '### Workflow result type': '### Workflow 結果型別',
  'Both `run.start()` and `stream.result` return a discriminated union based on the `status` property, which can be `success`, `failed`, `suspended`, `tripwire`, or `paused`. You can always safely access `result.status`, `result.input`, `result.steps`, and optionally `result.state` regardless of the status.': '`run.start()` 與 `stream.result` 都會根據 `status` 屬性傳回可辨識聯集；其值可以是 `success`、`failed`、`suspended`、`tripwire` 或 `paused`。無論狀態為何，你都可以安全地存取 `result.status`、`result.input`、`result.steps`，以及選擇性的 `result.state`。',
  'Additionally, depending on the status, different properties are available:': '此外，可用的屬性會依狀態而異：',
  '| Status | Unique properties | Description |': '| 狀態 | 特有屬性 | 說明 |',
  '| `success` | `result` | The workflow\'s output data |': '| `success` | `result` | Workflow 的輸出資料 |',
  '| `failed` | `error` | The error that caused the failure |': '| `failed` | `error` | 導致失敗的錯誤 |',
  '| `tripwire` | `tripwire` | Contains `reason`, `retry?`, `metadata?`, `processorId?` |': '| `tripwire` | `tripwire` | 包含 `reason`、`retry?`、`metadata?`、`processorId?` |',
  '| `suspended` | `suspendPayload`, `suspended` | Suspension data and array of suspended step paths |': '| `suspended` | `suspendPayload`、`suspended` | 暫停資料與已暫停步驟路徑的陣列 |',
  '| `paused` | _(none)_ | Only common properties available |': '| `paused` | _（無）_ | 僅提供共用屬性 |',
  'To access status-specific properties, check the `status` first:': '若要存取特定狀態的屬性，請先檢查 `status`：',
  '### Workflow output': '### Workflow 輸出',
  'Here\'s an example of a successful workflow result, showing the `input`, `steps`, and `result` properties:': '以下是成功 Workflow 結果的範例，其中顯示 `input`、`steps` 與 `result` 屬性：',
  '## Streaming': '## 串流',
  'For generic `writer` API usage, see [Streaming](/guides/concepts/streaming#writer-api).': '如需一般 `writer` API 的用法，請參閱[串流](/guides/concepts/streaming#writer-api)。',
  '### Inspecting workflow stream payloads': '### 檢查 Workflow 串流承載資料',
  'Events written to the stream are included in the emitted chunks. Inspect these chunks to access custom fields such as event types, intermediate values, or step-specific data.': '寫入串流的事件會包含在發出的區塊中。檢查這些區塊，即可存取事件類型、中間值或步驟特定資料等自訂欄位。',
  '### Resuming an interrupted workflow stream': '### 繼續中斷的 Workflow 串流',
  'If a workflow stream is closed or interrupted for any reason, you can resume it with the `resumeStream` method. This will return a new `ReadableStream` that you can use to observe the workflow events.': '若 Workflow 串流因任何原因關閉或中斷，可以使用 `resumeStream` 方法繼續。此方法會傳回新的 `ReadableStream`，供你觀察 Workflow 事件。',
  '### Workflow using an agent': '### 使用 Agent 的 Workflow',
  'Pipe an agent\'s `textStream` to the workflow step\'s `writer`. This streams partial output, and Mastra automatically aggregates the agent\'s usage into the workflow run.': '將 Agent 的 `textStream` 以管線傳送至 Workflow 步驟的 `writer`。這會串流部分輸出，而 Mastra 會自動將 Agent 的用量彙總至 Workflow 執行。',
  '## Restarting active workflow runs': '## 重新啟動作用中的 Workflow 執行',
  'When a workflow run loses connection to the server and it can be restarted from the last active step. This is useful for long-running workflows that might still be running when the server loses connection. Restarting a workflow run will resume execution from the last active step, plus the workflow will continue from there.': '當 Workflow 執行與伺服器中斷連線時，可以從最後一個作用中步驟重新啟動。這對長時間執行的 Workflow 很有幫助，因為伺服器中斷連線時，Workflow 可能仍在執行。重新啟動 Workflow 執行會從最後一個作用中步驟恢復，並由該處繼續。',
  '### Restarting all active workflow runs of a workflow with `restartAllActiveWorkflowRuns()`': '### 使用 `restartAllActiveWorkflowRuns()` 重新啟動 Workflow 的所有作用中執行',
  'Use `restartAllActiveWorkflowRuns()` to restart all active workflow runs of a workflow. This helps restart all active workflow runs of a workflow, without having to manually loop through each run and restart.': '使用 `restartAllActiveWorkflowRuns()` 重新啟動某個 Workflow 的所有作用中執行。如此便不必手動逐一走訪並重新啟動每次執行。',
  '### Restarting an active workflow run with `restart()`': '### 使用 `restart()` 重新啟動作用中的 Workflow 執行',
  'Use `restart()` to restart an active workflow run from the last active step. This will resume execution from the last active step, and the workflow will continue from there.': '使用 `restart()`，從最後一個作用中步驟重新啟動作用中的 Workflow 執行。執行會從該步驟恢復，Workflow 也會由該處繼續。',
  '### Identifying active workflow runs': '### 識別作用中的 Workflow 執行',
  'When a workflow run is active, it will have a `status` of `running` or `waiting`. You can check the workflow\'s `status` to confirm it\'s active, and use `active` to identify the active workflow run.': '作用中的 Workflow 執行，其 `status` 會是 `running` 或 `waiting`。你可以檢查 Workflow 的 `status` 以確認它是否作用中，並使用 `active` 識別作用中的 Workflow 執行。',
  'When running the local mastra server, all active workflow runs will be restarted automatically when the server starts.': '執行本機 mastra 伺服器時，伺服器啟動後會自動重新啟動所有作用中的 Workflow 執行。',
  '## Using `RequestContext`': '## 使用 `RequestContext`',
  'Use [RequestContext](/docs/server/request-context) to access request-specific values. This lets you conditionally adjust behavior based on the context of the request.': '使用 [RequestContext](/docs/server/request-context) 存取要求特定的值。如此即可依要求的情境，有條件地調整行為。',
  'Visit [Request Context](/docs/server/request-context) for more information.': '如需更多資訊，請參閱 [Request Context](/docs/server/request-context)。',
  'For type-safe request context schema validation, see [Schema Validation](/docs/server/request-context#schema-validation).': '如需具型別安全性的要求情境結構描述驗證，請參閱[結構描述驗證](/docs/server/request-context#schema-validation)。',
  '## Related': '## 相關內容',
  'For a closer look at workflows, see our [Workflow Guide](/guides/guide/ai-recruiter), which walks through the core concepts with a practical example.': '如要深入瞭解 Workflow，請參閱 [Workflow 指南](/guides/guide/ai-recruiter)，其中會透過實際範例逐步說明核心概念。',
  '- [Workflow State](/docs/workflows/workflow-state)': '- [Workflow 狀態](/docs/workflows/workflow-state)',
  '- [Control Flow](/docs/workflows/control-flow)': '- [控制流程](/docs/workflows/control-flow)',
  '- [Suspend and Resume](/docs/workflows/suspend-and-resume)': '- [暫停與繼續](/docs/workflows/suspend-and-resume)',
  '- [Error Handling](/docs/workflows/error-handling)': '- [錯誤處理](/docs/workflows/error-handling)',
  '- [Workers](/docs/deployment/workers): Run workflow execution in dedicated background processes': '- [Worker](/docs/deployment/workers)：在專用的背景程序中執行 Workflow',
  '- 📹 [Agentic workflows with Mastra workshop](https://www.youtube.com/watch?v=HGt8pVPpX9g)': '- 📹 [使用 Mastra 建立 Agentic Workflow 工作坊](https://www.youtube.com/watch?v=HGt8pVPpX9g)',

  'title: "Control flow | Workflows"': 'title: "控制流程 | Workflow"',
  'description: "Control flow in Mastra workflows allows you to manage branching, merging, and conditions to construct workflows that meet your logic requirements."': 'description: "Mastra Workflow 的控制流程可讓你管理分支、合併與條件，建立符合邏輯需求的 Workflow。"',
  '# Control flow': '# 控制流程',
  'Workflows run a sequence of predefined tasks, and you can control how that flow is executed. Tasks are divided into **steps**, which can be executed in different ways depending on your requirements. They can run sequentially or in parallel, or alternatively follow different paths based on conditions.': 'Workflow 會執行一系列預先定義的任務，而你可以控制流程的執行方式。任務會分成多個**步驟**，並可依需求使用不同方式執行。這些步驟可以依序或平行執行，也可以根據條件採用不同路徑。',
  'Each step connects to the next in the workflow through defined schemas that keep data controlled and consistent.': '每個步驟都會透過已定義的結構描述連接至 Workflow 中的下一個步驟，確保資料受到控制且維持一致。',
  '- The first step’s `inputSchema` must match the workflow’s `inputSchema`.': '- 第一個步驟的 `inputSchema` 必須符合 Workflow 的 `inputSchema`。',
  '- The final step’s `outputSchema` must match the workflow’s `outputSchema`.': '- 最後一個步驟的 `outputSchema` 必須符合 Workflow 的 `outputSchema`。',
  '- Each step’s `outputSchema` must match the next step’s `inputSchema`.': '- 每個步驟的 `outputSchema` 必須符合下一個步驟的 `inputSchema`。',
  '- If it doesn’t, use [Input data mapping](#input-data-mapping) to transform the data into the required shape.': '- 若不符合，請使用[輸入資料對應](#input-data-mapping)，將資料轉換為所需形式。',
  '## Chaining steps with `.then()`': '## 使用 `.then()` 串接步驟',
  'Use `.then()` to run steps in order, allowing each step to access the result of the step before it.': '使用 `.then()` 依序執行步驟，讓每個步驟都能存取前一步驟的結果。',
  '![Chaining steps with .then()](/img/workflows/workflows-control-flow-then.jpg)': '![使用 .then() 串接步驟](/img/workflows/workflows-control-flow-then.jpg)',
  '## Simultaneous steps with `.parallel()`': '## 使用 `.parallel()` 同時執行步驟',
  'Use `.parallel()` to run steps simultaneously. All parallel steps must complete before the workflow continues to the next step. Each step\'s `id` is used when defining a following step\'s `inputSchema` and becomes the key on the `inputData` object used to access the previous step\'s values. The outputs of parallel steps can then be referenced or combined by a following step.': '使用 `.parallel()` 同時執行步驟。所有平行步驟都必須完成，Workflow 才會繼續執行下一個步驟。定義後續步驟的 `inputSchema` 時，會使用每個步驟的 `id`；該 `id` 也會成為 `inputData` 物件中的鍵，用來存取先前步驟的值。後續步驟可以參照或合併平行步驟的輸出。',
  '![Concurrent steps with .parallel()](/img/workflows/workflows-control-flow-parallel.jpg)': '![使用 .parallel() 並行執行步驟](/img/workflows/workflows-control-flow-parallel.jpg)',
  '### Output structure': '### 輸出結構',
  'When steps run in parallel, the output is an object where each key is the step\'s `id` and the value is that step\'s output. You can access each parallel step\'s result independently.': '步驟平行執行時，輸出會是一個物件，其中每個鍵都是步驟的 `id`，而值則是該步驟的輸出。你可以個別存取每個平行步驟的結果。',
  '**Key points:**': '**重點：**',
  '- Each parallel step\'s output is keyed by its `id`': '- 每個平行步驟的輸出都以其 `id` 作為鍵',
  '- All parallel steps execute simultaneously': '- 所有平行步驟會同時執行',
  '- The next step receives an object containing all parallel step outputs': '- 下一個步驟會收到包含所有平行步驟輸出的物件',
  '- You must define the `inputSchema` of the following step to match this structure': '- 後續步驟的 `inputSchema` 必須定義成符合此結構',
  '### Handling step failures': '### 處理步驟失敗',
  'If any parallel step throws an error, the entire parallel block fails. To build resilient parallel workflows where some steps may fail, for example, multiple research agents where one might have an expired auth token, handle errors inside the step itself using try/catch:': '若任何平行步驟擲回錯誤，整個平行區塊都會失敗。若要建立可容許部分步驟失敗的韌性平行 Workflow（例如多個研究 Agent 中，某個 Agent 的驗證權杖可能已過期），請在步驟本身使用 try/catch 處理錯誤：',
  'This way the step always succeeds with a typed result, and the downstream step can filter out failed results:': '如此一來，步驟一律會以具型別的結果成功完成，而下游步驟可以濾除失敗的結果：',
  'Visit [Choosing the right pattern](#choosing-the-right-pattern) to understand when to use `.parallel()` vs `.foreach()`.': '請參閱[選擇正確模式](#choosing-the-right-pattern)，瞭解何時該使用 `.parallel()` 或 `.foreach()`。',
  '## Conditional logic with `.branch()`': '## 使用 `.branch()` 建立條件邏輯',
  'Use `.branch()` to choose which step to run based on a condition. All steps in a branch need the same `inputSchema` and `outputSchema` because branching requires consistent schemas so workflows can follow different paths.': '使用 `.branch()` 根據條件選擇要執行的步驟。分支中的所有步驟都需要相同的 `inputSchema` 與 `outputSchema`，因為分支需要一致的結構描述，Workflow 才能採用不同路徑。',
  '![Conditional branching with .branch()](/img/workflows/workflows-control-flow-branch.jpg)': '![使用 .branch() 建立條件分支](/img/workflows/workflows-control-flow-branch.jpg)',
  'When using conditional branching, only one branch executes based on which condition evaluates to `true` first. The output structure is similar to `.parallel()`, where the result is keyed by the executed step\'s `id`.': '使用條件分支時，只會執行第一個條件評估為 `true` 的分支。輸出結構與 `.parallel()` 類似，結果會以已執行步驟的 `id` 作為鍵。',
  '- Only one branch executes based on condition evaluation order': '- 依條件評估順序，只會執行一個分支',
  '- The output is keyed by the executed step\'s `id`': '- 輸出會以已執行步驟的 `id` 作為鍵',
  '- Subsequent steps should handle all possible branch outputs': '- 後續步驟應處理所有可能的分支輸出',
  '- Use optional fields in the `inputSchema` when the next step needs to handle multiple possible branches': '- 當下一個步驟需要處理多個可能的分支時，請在 `inputSchema` 中使用選擇性欄位',
  '- Conditions are evaluated in the order they\'re defined': '- 條件會依定義順序進行評估',
  '## Input data mapping': '## 輸入資料對應',
  'When using `.then()`, `.parallel()`, or `.branch()`, it\'s sometimes necessary to transform the output of a previous step to match the input of the next. In these cases you can use `.map()` to access the `inputData` and transform it to create a suitable data shape for the next step.': '使用 `.then()`、`.parallel()` 或 `.branch()` 時，有時必須轉換前一步驟的輸出，使其符合下一個步驟的輸入。在這些情況下，你可以使用 `.map()` 存取 `inputData` 並加以轉換，為下一個步驟建立合適的資料形式。',
  '![Mapping with .map()](/img/workflows/workflows-data-mapping-map.jpg)': '![使用 .map() 對應資料](/img/workflows/workflows-data-mapping-map.jpg)',
  'The `.map()` method provides additional helper functions for more complex mapping scenarios.': '`.map()` 方法提供額外的輔助函式，可處理更複雜的對應情境。',
  '**Available helper functions:**': '**可用的輔助函式：**',
  '- [`getStepResult()`](/reference/workflows/workflow-methods/map#using-getstepresult): Access a specific step\'s full output': '- [`getStepResult()`](/reference/workflows/workflow-methods/map#using-getstepresult)：存取特定步驟的完整輸出',
  '- [`getInitData<any>()`](/reference/workflows/workflow-methods/map#using-getinitdata): Access the workflow\'s initial input data': '- [`getInitData<any>()`](/reference/workflows/workflow-methods/map#using-getinitdata)：存取 Workflow 的初始輸入資料',
  '- [`mapVariable()`](/reference/workflows/workflow-methods/map#using-mapvariable): Use declarative object syntax to extract and rename fields': '- [`mapVariable()`](/reference/workflows/workflow-methods/map#using-mapvariable)：使用宣告式物件語法擷取欄位並重新命名',
  '### Parallel and Branch outputs': '### Parallel 與 Branch 輸出',
  'When working with `.parallel()` or `.branch()` outputs, you can use `.map()` to transform the data structure before passing it to the next step. This is especially useful when you need to flatten or restructure the output.': '處理 `.parallel()` 或 `.branch()` 的輸出時，可以先使用 `.map()` 轉換資料結構，再將資料傳給下一個步驟。需要攤平或重新安排輸出結構時，此方法特別實用。',
  'You can also use the helper functions provided by `.map()`:': '你也可以使用 `.map()` 提供的輔助函式：',
  '## Looping steps': '## 循環步驟',
  'Workflows support different looping methods that let you repeat steps until or while a condition is met, or iterate over arrays. Loops can be combined with other control methods like `.then()`.': 'Workflow 支援多種循環方法，可重複步驟直到或只要條件成立，也可以逐一處理陣列。循環可與 `.then()` 等其他控制方法搭配使用。',
  '### Looping with `.dountil()`': '### 使用 `.dountil()` 循環',
  'Use `.dountil()` to run a step repeatedly until a condition becomes true.': '使用 `.dountil()` 重複執行步驟，直到條件變為 true。',
  '![Repeating with .dountil()](/img/workflows/workflows-control-flow-dountil.jpg)': '![使用 .dountil() 重複執行](/img/workflows/workflows-control-flow-dountil.jpg)',
  '### Looping with `.dowhile()`': '### 使用 `.dowhile()` 循環',
  'Use `.dowhile()` to run a step repeatedly while a condition remains true.': '使用 `.dowhile()`，在條件維持 true 時重複執行步驟。',
  '![Repeating with .dowhile()](/img/workflows/workflows-control-flow-dowhile.jpg)': '![使用 .dowhile() 重複執行](/img/workflows/workflows-control-flow-dowhile.jpg)',
  '### Looping with `.foreach()`': '### 使用 `.foreach()` 循環',
  'Use `.foreach()` to run the same step for each item in an array. The input must be of type `array` so the loop can iterate over its values, applying the step\'s logic to each one. See [Choosing the right pattern](#choosing-the-right-pattern) for guidance on when to use `.foreach()` vs other methods.': '使用 `.foreach()` 對陣列中的每個項目執行相同步驟。輸入必須是 `array` 型別，循環才能逐一處理其中的值，並對每個值套用步驟邏輯。如需選擇 `.foreach()` 或其他方法的指引，請參閱[選擇正確模式](#choosing-the-right-pattern)。',
  '![Repeating with .foreach()](/img/workflows/workflows-control-flow-foreach.jpg)': '![使用 .foreach() 重複執行](/img/workflows/workflows-control-flow-foreach.jpg)',
  '#### Output structure': '#### 輸出結構',
  'The `.foreach()` method always returns an array containing the output of each iteration. The order of outputs matches the order of inputs.': '`.foreach()` 方法一律會傳回包含每次迭代輸出的陣列。輸出順序與輸入順序相同。',
  '#### Concurrency limits': '#### 並行數限制',
  'Use `concurrency` to control the number of array items processed. The default is `1`, which runs steps sequentially. Increasing the value allows `.foreach()` to process multiple items simultaneously.': '使用 `concurrency` 控制處理的陣列項目數。預設值為 `1`，此時會依序執行步驟。提高此值可讓 `.foreach()` 同時處理多個項目。',
  '#### Aggregating results after `.foreach()`': '#### 彙總 `.foreach()` 之後的結果',
  'Since `.foreach()` outputs an array, you can use `.then()` or `.map()` to aggregate or transform the results. The step following `.foreach()` receives the entire array as its input.': '由於 `.foreach()` 會輸出陣列，因此可以使用 `.then()` 或 `.map()` 彙總或轉換結果。接在 `.foreach()` 之後的步驟會收到整個陣列作為輸入。',
  'You can also use `.map()` to transform the array output:': '你也可以使用 `.map()` 轉換陣列輸出：',
  '#### Chaining multiple `.foreach()` calls': '#### 串接多個 `.foreach()` 呼叫',
  'When you chain `.foreach()` calls, each operates on the array output of the previous step. This is useful when each item in your array needs to be transformed by multiple steps in sequence.': '串接 `.foreach()` 呼叫時，每次呼叫都會處理前一步驟輸出的陣列。若陣列中的每個項目都需要依序經過多個步驟轉換，此做法會很有用。',
  'For processing multiple documents where each produces multiple chunks, you have options:': '處理多份文件，且每份文件都會產生多個區塊時，你可以選擇以下做法：',
  '**Option 1: Process all documents in a single step with batching control**': '**選項 1：在單一步驟中處理所有文件並控制批次**',
  '**Option 2: Use foreach for documents and aggregate chunks, plus then foreach for embeddings**': '**選項 2：對文件使用 foreach 並彙總區塊，再對嵌入向量使用 foreach**',
  '**Key points about chaining `.foreach()`:**': '**串接 `.foreach()` 的重點：**',
  '- Each `.foreach()` operates on the array from the previous step': '- 每個 `.foreach()` 都會處理前一步驟傳來的陣列',
  '- If a step inside `.foreach()` returns an array, the output becomes an array of arrays': '- 若 `.foreach()` 內的步驟傳回陣列，輸出會成為陣列的陣列',
  '- Use `.map()` with `.flat()` to flatten nested arrays when needed': '- 必要時搭配使用 `.map()` 與 `.flat()`，將巢狀陣列攤平',
  '- For complex RAG pipelines, Option 1 (handling batching in a single step) often provides better control': '- 對複雜的 RAG 管線而言，選項 1（在單一步驟中處理批次）通常能提供更佳的控制能力',
  '#### Nested workflows inside foreach': '#### foreach 內的巢狀 Workflow',
  'The step after `.foreach()` only executes after all iterations complete. If you need to run multiple sequential operations per item, use a nested workflow instead of chaining multiple `.foreach()` calls. This keeps all operations for each item together and makes the data flow clearer.': '接在 `.foreach()` 之後的步驟，只有在所有迭代完成後才會執行。若每個項目都需要執行多個連續作業，請使用巢狀 Workflow，而不要串接多個 `.foreach()` 呼叫。這能將每個項目的所有作業集中在一起，讓資料流程更清楚。',
  '**Why use nested workflows:**': '**使用巢狀 Workflow 的原因：**',
  '- **Better parallelism**: With `concurrency: N`, multiple items run their full pipelines simultaneously. Chained `.foreach().foreach()` processes all items through step 1, waits, then all through step 2 - nested workflows let each item progress independently': '- **更佳的平行處理能力**：使用 `concurrency: N` 時，多個項目會同時執行各自的完整管線。串接的 `.foreach().foreach()` 會先讓所有項目完成步驟 1，等候後再全部執行步驟 2；巢狀 Workflow 則可讓每個項目獨立推進',
  '- All steps for one item complete together before results are collected': '- 收集結果前，同一項目的所有步驟會一起完成',
  '- Cleaner than multiple `.foreach()` calls which create nested arrays': '- 比會建立巢狀陣列的多個 `.foreach()` 呼叫更簡潔',
  '- Each nested workflow execution is independent with its own data flow': '- 每次巢狀 Workflow 執行都彼此獨立，並有自己的資料流程',
  '- Easier to test and reuse the per-item logic separately': '- 更容易個別測試及重複使用每個項目的邏輯',
  '**How it works:**': '**運作方式：**',
  '1. The parent workflow passes each array item to an instance of the nested workflow': '1. 父 Workflow 將每個陣列項目傳給一個巢狀 Workflow 執行個體',
  '1. Each nested workflow runs its full step sequence for that item': '1. 每個巢狀 Workflow 都會針對該項目執行完整的步驟序列',
  '1. With `concurrency > 1`, multiple nested workflows execute in parallel': '1. 當 `concurrency > 1` 時，多個巢狀 Workflow 會平行執行',
  '1. The nested workflow\'s final output becomes one element in the result array': '1. 巢狀 Workflow 的最終輸出會成為結果陣列中的一個元素',
  '1. After all nested workflows complete, the next step in the parent receives the full array': '1. 所有巢狀 Workflow 完成後，父 Workflow 的下一個步驟會收到完整陣列',
  '## Choosing the right pattern': '## 選擇正確模式',
  'Use this section as a reference for selecting the appropriate control flow method.': '選擇合適的控制流程方法時，可參考本節。',
  '### Quick reference': '### 快速參考',
  '| Method | Purpose | Input | Output | Concurrency |': '| 方法 | 用途 | 輸入 | 輸出 | 並行處理 |',
  '| `.then(step)` | Sequential processing | `T` | `U` | N/A (one at a time) |': '| `.then(step)` | 依序處理 | `T` | `U` | 不適用（一次一個） |',
  '| `.parallel([a, b])` | Different operations on same input | `T` | `{ a: U, b: V }` | All run simultaneously |': '| `.parallel([a, b])` | 對相同輸入執行不同作業 | `T` | `{ a: U, b: V }` | 全部同時執行 |',
  '| `.foreach(step)` | Same operation on each array item | `T[]` | `U[]` | Configurable (default: 1) |': '| `.foreach(step)` | 對每個陣列項目執行相同作業 | `T[]` | `U[]` | 可設定（預設：1） |',
  '| `.branch([...])` | Conditional path selection | `T` | `{ selectedStep: U }` | Only one branch runs |': '| `.branch([...])` | 依條件選擇路徑 | `T` | `{ selectedStep: U }` | 僅執行一個分支 |',
  '### `.parallel()` vs `.foreach()`': '### `.parallel()` 與 `.foreach()` 的比較',
  '**Use `.parallel()` when you have one input that needs different processing:**': '**單一輸入需要不同處理方式時，請使用 `.parallel()`：**',
  '**Use `.foreach()` when you have many inputs that need the same processing:**': '**多個輸入需要相同處理方式時，請使用 `.foreach()`：**',
  '### When to use nested workflows': '### 適合使用巢狀 Workflow 的時機',
  '**Inside `.foreach()`** - when each array item needs multiple sequential steps:': '**在 `.foreach()` 內**：當每個陣列項目都需要多個連續步驟時：',
  'The single `.foreach()` keeps the result flat. Chaining `.foreach().foreach()` creates nested arrays.': '單一 `.foreach()` 可讓結果保持扁平。串接 `.foreach().foreach()` 則會建立巢狀陣列。',
  '**Inside `.parallel()`** - when a parallel branch needs its own multi-step pipeline:': '**在 `.parallel()` 內**：當平行分支需要自己的多步驟管線時：',
  '### Chaining patterns': '### 串接模式',
  '| Pattern | What happens | Common use case |': '| 模式 | 行為 | 常見使用情境 |',
  '| `.then().then()` | Sequential steps | Simple pipelines |': '| `.then().then()` | 依序執行步驟 | 簡單管線 |',
  '| `.parallel().then()` | Run in parallel, then combine | Fan-out/fan-in |': '| `.parallel().then()` | 平行執行後再合併 | 扇出／扇入 |',
  '| `.foreach().then()` | Process all items, then aggregate | Map-reduce |': '| `.foreach().then()` | 處理所有項目後再彙總 | Map-reduce |',
  '| `.foreach().foreach()` | Creates array of arrays | Avoid - use nested workflow or `.map()` with `.flat()` |': '| `.foreach().foreach()` | 建立陣列的陣列 | 應避免；請使用巢狀 Workflow，或搭配 `.map()` 與 `.flat()` |',
  '| `.foreach(workflow)` | Full pipeline per item | Multi-step processing per array item |': '| `.foreach(workflow)` | 每個項目各自執行完整管線 | 對每個陣列項目進行多步驟處理 |',
  '### Synchronization: when does the next step run?': '### 同步：下一個步驟何時執行？',
  'Both `.parallel()` and `.foreach()` are synchronization points. The next step in the workflow only executes after all parallel branches or all array iterations have completed.': '`.parallel()` 與 `.foreach()` 都是同步點。只有在所有平行分支或所有陣列迭代完成後，Workflow 的下一個步驟才會執行。',
  'This means:': '這表示：',
  '- `.parallel()` collects all branch outputs into an object, then passes it to the next step': '- `.parallel()` 會將所有分支輸出收集至一個物件，再傳給下一個步驟',
  '- `.foreach()` collects all iteration outputs into an array, then passes it to the next step': '- `.foreach()` 會將所有迭代輸出收集至一個陣列，再傳給下一個步驟',
  '- Results can\'t be "streamed" to the next step as they complete': '- 結果無法在完成時以「串流」方式傳給下一個步驟',
  '### Concurrency behavior': '### 並行處理行為',
  '| Method | Behavior |': '| 方法 | 行為 |',
  '| `.then()` | Sequential - one step at a time |': '| `.then()` | 依序執行，一次一個步驟 |',
  '| `.parallel()` | All branches run simultaneously (no limit option) |': '| `.parallel()` | 所有分支同時執行（無限制選項） |',
  '| `.foreach()` | Controlled via `{ concurrency: N }` - default is 1 (sequential) |': '| `.foreach()` | 透過 `{ concurrency: N }` 控制；預設為 1（依序執行） |',
  '| Nested workflow in `.foreach()` | Respects parent\'s concurrency setting |': '| `.foreach()` 中的巢狀 Workflow | 遵循父 Workflow 的並行處理設定 |',
  '**Performance tip:** For I/O-bound operations in `.foreach()`, increase concurrency to process items in parallel:': '**效能提示：** 對 `.foreach()` 中受 I/O 限制的作業，請提高並行數以平行處理項目：',
  '## Loop management': '## 循環管理',
  'Loop conditions can be implemented in different ways depending on how you want the loop to end.': '你可以依希望循環如何結束，採用不同方式實作循環條件。',
  'Common patterns check values returned in `inputData` and set a maximum number of iterations. They can also abort execution when a limit is reached.': '常見模式會檢查 `inputData` 傳回的值，並設定最大迭代次數。到達限制時，也可以中止執行。',
  '### Aborting loops': '### 中止循環',
  'Use `iterationCount` to limit how many times a loop runs. If the count exceeds your threshold, throw an error to fail the step and stop the workflow.': '使用 `iterationCount` 限制循環執行次數。若次數超過臨界值，請擲回錯誤，讓步驟失敗並停止 Workflow。',
  '- [Human-in-the-loop](/docs/workflows/human-in-the-loop)': '- [人類參與流程](/docs/workflows/human-in-the-loop)',
}));

const jobs = [
  ['src/content/en/docs/workflows/overview.mdx', 'i18n/zh-TW/docusaurus-plugin-content-docs/current/workflows/overview.mdx'],
  ['src/content/en/docs/workflows/control-flow.mdx', 'i18n/zh-TW/docusaurus-plugin-content-docs/current/workflows/control-flow.mdx'],
];

let patch = '*** Begin Patch\n';
for (const [source, target] of jobs) {
  const lines = fs.readFileSync(source, 'utf8').split('\n');
  let fenced = false;
  const output = lines.map(line => {
    if (/^\s*```/.test(line)) {
      fenced = !fenced;
      return line;
    }
    if (fenced) return line;
    const indent = line.match(/^\s*/)[0];
    const body = line.slice(indent.length);
    return indent + (translations.get(body) ?? body);
  });
  patch += `*** Add File: ${target}\n`;
  patch += output.map(line => `+${line}`).join('\n') + '\n';
}
patch += '*** End Patch\n';
fs.writeFileSync('/tmp/workflows-zh-tw.patch', patch);
